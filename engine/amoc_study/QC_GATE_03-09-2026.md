# AMOC — QC gate, 3 September 2026

Alexandria Mineral Oils Company S.A.E. · Egyptian Exchange: AMOC · market EG · EGP

Audited from outside the study, on the delivered edition of 3 September 2026. This file
replaces the QC gate previously at this path, which was written before today's corrections
and audits a central the study no longer carries.

**Every row names the artefact, the command or the number that carries it.** Nothing is
self-certified. Where an item is met with a gap, the verdict says so and the gap is stated.

---

## VERDICT: NOT DELIVERABLE

The model, the numbers file and the workbook are in good order. **The three delivered
documents are not.** They carry a superseded valuation alongside the current one, publish
two negative probabilities, and describe constructions the committed records say the study
abandoned. The blocking list is at the end of this file.

---

## What was audited, and what carries it

| Item | Value | Evidence |
|---|---|---|
| Delivered study | `AMOC_Valuation_Study_03-09-2026_public.docx` / `.pdf` — 21 pages, 39 tables, 5 embedded figures | `docx.Document(...)`: 179 paragraphs, 39 tables; `pymupdf`: 21 pages; `word/media/` holds 5 PNGs |
| Delivered bibliography | `AMOC_Bibliography_03-09-2026.docx` / `.pdf` — 19 pages, 10 tables | same route |
| Delivered workbook | `AMOC_Valuation_Model_03092026_public.xlsx` / `.pdf` — 16 sheets, 190-page render | `openpyxl`: 16 sheets; `pymupdf`: 190 pages |
| Committed numbers | `study_numbers.json`, sha256 `10b8b6aa…ec260ff`, 41 top-level keys, 177 registered inputs | `sha256sum`; `json.load`. Audited first at `428286d6…`; a parallel commit (`f3167c3d`, [R-ANCHOR-01] clause two) added `forecast_anchor.forecast_path` mid-audit and moved no other key — re-verified below, and no delivered file changed. |
| Central | **EGP 11.834178** | `study_numbers.json → central` |
| Spot | **EGP 13.50, 3 September 2026** | `study_numbers.json → spot` / `meta.asof`; cross-checked against `engine/prices/SUPPLIED_03-09-2026.json` → `AMOC {price 13.5, date "2026-09-03"}` |
| Gap | **−12.34%** (central below the price) | 11.834178 / 13.50 − 1 |
| Range published | EGP **1.59 – 24.97** | `study_numbers.json → span`, `span_env`, `scen.bear.ps` / `scen.bull.ps`. **NOT 1.08 – 22.77**: the figures in the re-audit brief do not match the committed artefact, and the artefact governs. |
| Exchange | **EGX** | `assets/data.js` line 610: `code: "EGX:AMOC"` — read from the code prefix, not from the `raw_ohlc/EG/` folder |
| Class | refiner, commodity pass-through on a thin spread | `study_numbers.json → lens_record.class`; registered in `lessons_register.CLASSES`, asserted at import by `LENS_REGISTRY` |
| Reference pattern | **ADNOCLS** (operating company). The class fits none of the three closed reference names exactly; ADNOCLS is the nearest and the study uses its skeleton. | `research_protocol.REFERENCE_SET`; `engine/model_report/MODEL_REPORT_09-08-2026.docx` opened beside the delivered study |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates are called in the study's own code | **PASS** | `grep` over `engine/amoc_study/*.py`: `compute.py:2715 _rp.assert_beta_provenance(BETA_REC)`, `compute.py:2741 _rp.assert_ground_up(DRIVER_LINES, ticker='AMOC')`, `compute.py:2759 _rp.assert_sigcm(SIGCM)`, `compute.py:2773 _rp.assert_model_study(MODEL_STUDY)`; plus `beta_record.py:38`. All four are in the study's own committed source, not in a wrapper. |
| 2 | The entry point runs, and the numbers file reproduces | **PASS** | `python3 compute.py` re-run in an isolated copy (`scratchpad/eng/amoc_study`, engine modules symlinked, delivered files untouched) — exit 0, "ASSERT BLOCK PASSED". The emitted `study_numbers.json` is **byte-identical** to the committed one — `sha256 10b8b6aa3df2d597…ec260ff` on both sides. Run twice: first against the file as audited (`428286d6…`, 0 keys added / removed / changed on a flattened key-by-key diff), then again after commit `f3167c3d` re-emitted it with a five-element `forecast_anchor.forecast_path`, which is the **only** difference between the two (0 removed, 0 changed) and moves no delivered number. |
| 3 | Repo-level provenance gate | **PASS** | `python3 scripts/check_study_provenance.py` → exit 0, "24 study directories · OK — no new violations". AMOC is **not** on `engine/build_depth_audit/outstanding.json`. |
| 4 | Standard version stamp | **PASS** | `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` → `2026.09.01`. `study_numbers.json → standard_version` = `"2026.09.01"` and `gates.standard_version` = `"2026.09.01"`. The live note names the [R-GAP-01] gap-review artefact, and that artefact exists. |
| 5 | **Beta, inspected on the record** | **PASS** | `beta_result.json`: `index_file` = **`raw_indices/EG/EGX30.csv`** (under `raw_indices/`, not a composite), `index_asof` 2026-07-22, market EG, exchange EGX, β **0.9080**, R² **25.9%**, SE **0.1649**, n **253**, weekly W-THU, 4.89-year window, `usable: true`, `conforming: true`, `interim_note: null`. EGX30 is the published index of the exchange named by the `EGX:` prefix in `assets/data.js` — no interim note is due, no QATAR10 caveat applies. Independently re-derived from outside the study: `beta_regression.own_stock_beta('AMOC','EG','EGX')` returns β 0.9079854792123889, R² 0.2589419143627705, SE 0.16494576521273835, n 253, same `index_file` — **exact match to the last digit**. The withdrawn composite (0.9405, "33-name equal-weight EGX composite") is carried as `superseded_composite` with its reason, and published in the study at Table B.1 as a labelled superseded row. Usability gate cleared on all three limbs (n 253 ≥ 24; R² 25.9% ≥ 5%; SE 0.165 < |β| 0.908), so no fall to tier 2/3 was required. |
| 6 | **Ground-up clause, inspected on the driver record** | **PASS, with the gap named and priced** | `study_numbers.json → gates.ground_up`: 8 lines, **share_by_level = unit 0% · derived 100% · segment 0% · topdown 0%**, `unit_share: 0`. Every line carries the gap note verbatim: tonnage and realisation per product are disclosed (note 14-A, every year), **cost is disclosed by NATURE for the company as a whole (note 15-A) and never by product**, and the FY2023 auditor's emphasis of matter records that no per-product costing system existed before 1 July 2023 — so per-line cost is an allocation and every per-line margin is a construction. Filing at `derived` rather than claiming `unit` is the honest call and `assert_ground_up()` enforces the 100%-of-revenue coverage. The construction is visible to the reader at Table 9 (page 6): realisation, conversion, feedstock, total cost, spread and margin per line, footing exactly to note 15-A as a live workbook cell. |
| 7 | **SIGCM clause 1 — historicals official only** | **PASS** | Every historical figure in the input register cites a filing: `AUD` = audited consolidated FS for the transition period 1-Jul-2025 to 31-Dec-2025 (Crowe — Dr A. M. Hegazy & Co, unqualified, Giza 18-Feb-2026); `LRV` = limited-review FS six months to 31-Dec-2024; `REV` = reviewed FS three months to 31-Mar-2026; and **44 inputs sourced to the reviewed consolidated FS for the six months to 30-Jun-2026** (`rev_h1cy26`, `cogs_h1cy26`, `gp_h1cy26` … `div_declared`). The press-release variants are registered **separately** as `rev_h1cy26_rep` / `gp_h1cy26_rep` / `pat_h1cy26_rep` and are not the source of any modelled line. Base-year gross profit 4,533 = 1,274 (audited half) + 3,259 (filed half); `recalc_v5.py` confirms `Income Statement!B22` = 0.096534. |
| 8 | SIGCM clause 3 — debt split LC/FX | **PASS, immaterial and asserted as such** | `SIGCMChecklist.na_reasons['debt_lc_fx_split']`: borrowings EGP 20,977,437 against equity EGP 4,824,774,948, net cash, note 13 discloses no foreign-currency tranche. §B.2 of the study: gross borrowings EGP 17mn against a market capitalisation of EGP 17,435mn = 0.0966% of the structure; a 500bp error in Kd moves the weighted rate by **0.38bp** (`wacc.kd_swing_effect` = 3.76e-05). The immateriality is asserted in the build, so it fails if the structure changes. |
| 9 | SIGCM clause 4 — asset-conversion cycle projected | **PASS** | Working capital is projected on **days**, not a plug: `recalc_v5.py` headline reconciliation "inventory days, Income Statement!B44: 32.383351 vs 32.383351"; receivable/inventory/payable days are registered inputs (`recv_days`, `inv_days`, `pay_days`) and drive the balance-sheet and cash-flow roll (Appendix A.2/A.3, "less change in working capital" −397 / −388 / −366 / −332 / −333). |
| 10 | SIGCM clause 5 — competitors | **PASS, with the gap named** | Negative-results table, bibliography Table 9 row 10: "Listed peer multiples for Egyptian or regional lubricant base-oil processors — **NO DIRECT LISTED COMPARATOR EXISTS** — the company is the only listed refinery on the Egyptian Exchange." Recorded rather than filled in. The relative lens therefore stands on own history, which is where its defect sits (item 27). |
| 11 | SIGCM clause 7 — formula-based model | **PASS** | `python3 recalc_v5.py` on the delivered file: **6,069 formula cells, 0 unresolvable, 0 unchecked, 0 disagreements against the model**; formula share **6,069 / 251 pasted = 96.0%**. All thirty sensitivity grid points are written out as complete formula engines rather than pasted. |
| 12 | SIGCM clause 8 — flag before issue / stop-and-inform | **PASS** | Bibliography Table 9 records ten sought-and-not-found items with outcomes; §7 names the two unreachable exchange disclosures (−12% and +17% at face) and the un-re-verifiable risk-free rate; `engine/escalations.json` carries an **open** entry `EGX-ohlc-refresh-arcc-amoc-egch` (22 refs searched, 3 routes, 2 re-run) with a default date of 2026-09-10. `python3 scripts/check_escalations.py` → exit 0. |
| 13 | Step 0.0 data-quality gate | **PASS** | `study_numbers.json → step0.dq_log` and §2 of the study: 3,755 raw rows → **3,754 clean sessions** at 240.8 sessions a year, one row dropped (2013-05-07, non-positive OHLC), largest single-session move 0.1813 in logs tested separately against the EGX up-limit ln(1.20)=0.18232 and down-limit |ln(0.80)|=0.22314. The index series carries its own screen (`beta_result.json → index_dq: []`). |
| 14 | Step 0 calibration under the band record | **PASS** | `study_numbers.json → step0`: 17 windows scored (40 pre-break origins dropped), cov50 52.9% / cov80 88.2% / cov90 94.1%, PIT mean 0.461, w90 ratio 0.969x, market gate PASS (30 names, 494 windows, skill +1.58%, CI [0.9%, 2.2%]). Published band record verified live against the panel: `python3 -c "import band_record as br…"` returns EG/AMOC **n=57, cov50 0.5614, cov80 0.8070, cov90 0.8596, strength long, flag None** — which is exactly Table 20/22 of the study (57 resolved, "49 of 57 (86.0%)", 80.7%, 56.1%, 1.225x, LONG). **The count is printed beside the percentage**, the two-sided binomial at 5% raises no flag and the study says nothing further, and the width ratio is disclosed and not gated. See item 30 for the defect in how the two samples are presented side by side. |
| 15 | Retired verdict token in the committed numbers | **PASS on delivery, one gap named** | `study_numbers.json → step0.verdict = "PARITY"` and `ci_blocks` carry "PARITY" three times. [R-CAL-03] retires the skill verdict outright; it survives here as an internal field. It reaches **no** delivered surface — `band_record.assert_no_verdict_tokens()` over the full extracted text of both documents and the workbook raises nothing, and the case-sensitive `\bPARITY\b` scan returns **0**. Recorded so it is removed at the next re-issue rather than carried silently. |
| 16 | **[R-GAP-01] the answer, not only the process** | **PASS** | Central **11.8342** sits **−12.3%** against a spot of **13.50**, past the 10% trigger, so a review is required. `GAP_REVIEW_03-09-2026.md` exists in the study directory and covers **all eight headings** — LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK (verified by `grep -n "^##"`, headings at lines 41, 60, 137, 154, 177, 193, 218, 245). It carries `*AUDITED CENTRAL: 11.8342*`, which is the central the study publishes. `python3 scripts/check_valuation_gap.py` prints `AMOC −12.3% GAP_REVIEW_03-09-2026.md (audits 11.8342)` under "OUTSIDE THE BAND, AND REVIEWED", and under PRICE BASIS `AMOC last close **2026-09-03**` — the comparison is against a current price, not a stale one. The gate ran twice during this audit: **exit 1** at the start on one new violation, which was **EGCH** and not AMOC ("its committed numbers do not resolve to a central fair value and a spot"), and **exit 0** after that study was repaired in a parallel commit. AMOC's own row passed both times. |
| 17 | Spot is the latest known price, with its date | **FAIL** | The input is right and the presentation is wrong. `inputs.spot` = 13.50 dated 2026-09-03, matching `SUPPLIED_03-09-2026.json`. But the delivered study **dates that price 6 August 2026 in two places**: the masthead reads "Valuation study as of **6 August 2026**, issued 3 September 2026" (page 1), and Table 23 §4 reads "What does the market say today? · last close · **EGP 13.50** · **6 August 2026**" (page 14). The 6-August close was EGP 9.10 (`strike_result.json → spot: 9.1`). [R-GAP-01 AMENDED] requires the published spot to carry its own date; the document publishes the right price against the wrong date. |
| 18 | **§6 probability zones** | **FAIL — two negative probabilities in a delivered table** | Table 24, page 15, as rendered: "Below fair value (under EGP 11.83) **88.4%** · EGP 11.83 to 7.50 **−74.6%** · EGP 7.50 to 13.50 **82.9%** · EGP 13.50 to 11.00 **−16.0%** · Above EGP 11.00 **19.3%** · TOTAL 100.0%". Reproduced exactly from the committed numbers: `docx_v6.py:100` sets `CUTS = [C, 7.50, SPOT, 11.00]` = **[11.83, 7.50, 13.50, 11.00]**, which is **not ascending**, and differencing a CDF across a non-monotone cut list yields negatives. The guard at `docx_v6.py:105` asserts only `sum(ZP) == 1.0`, which **telescopes to 1.0 whatever the ordering** — a check that cannot fail ([R-ENF-04]). The list was descending when SPOT was 9.10 and the central ~9.9; the re-strike broke it. The caption directly above claims the opposite: "a genuine partition: the bands tile the line, **none overlaps**, and the total is asserted to be 100% in the build. The previous edition published five zones summing to 103.4%, with one band written descending and nested inside the band above it, so every zone probability in it was misstated." |
| 19 | **§3 probability map — anchor mixing** | **FAIL** | The cone is the 6-August strike (`strike_result.json`: anchor 2026-08-06, spot 9.10, 1M p5–p95 7.71–10.91, 3M 6.90–12.60). The touch ladder in Table 21 is built by `docx_v6.py:771 p_touch()`, which uses **`SPOT` = 13.50** as the anchor against **`h['pct']['p50']` from the 9.10 strike**. The implied one-month log drift is `m = ln(9.171/13.50) = −0.3866`, i.e. **−32.1%**, against the engine's own `drift_log_h` of **+0.0078 (+0.79%)** — a factor the model never produced. That manufactured drift is what makes the delivered ladder read "13.50 → **100.0%**", "11.00 → 96.9%" and "**11.83 → 99.5% / 95.6% — THIS STUDY'S FAIR VALUE**", and the caption then reads that number aloud: "The cone gives the price a real chance of reaching this study's fair value inside three months." The engine's own stored ladder disagrees by an order of magnitude (`touch_up10` 0.2999 at ~10.01 for 1M against the table's 82.9% at 10.00). §3's opening sentence — "NO part of it comes from the valuation" — is contradicted by the construction, which imports a stale median as if it were today's carry. Figure 5 draws the same mix: the left edge is plotted at 13.50 (`figures_v5.py:136 ys = [SPOT, h1[…], h3[…]]`) under a hardcoded tick label `'anchor\n06-Aug-26'` (`figures_v5.py:143`), producing a phantom one-month collapse. **Nowhere in the delivered study is the cone's anchor price of EGP 9.10 stated**, so a reader sees a three-month median of 9.33 against a page-1 price of 13.50 with no explanation. The stale library is a known, logged constraint (`escalations.json`, library ends 2026-08-06) whose own stated default is "state its date wherever a gap is quoted" — the document does not. |
| 20 | **§1.13 / §1.3 adversarial and give-back blocks** | **FAIL — a superseded model published beside the current one** | `case_adversarial.json` (unchanged since 31-Aug, base central **5.954**, dcf 5.500, rel 8.137, book 4.571) is read directly by `docx_v6.py:22` and `figures_v5.py:13`. It carries the pre-correction weighted-blend model. As delivered: **Table 18 / Figure 4 (pages 11–12)** print "None — as published · **5.95** · −55.9% · the study's own reading" against a published central of 11.83, on an axis labelled "**Weighted central**, EGP per share"; **Table 7 (page 5)** prints the same stale figures as "Central WITHOUT it" — 6.16, 6.56, 6.39 — described as "from a full model re-run"; and the **READ FIRST panel on page 1** states "Surrender every contested judgement in this study simultaneously … and the central still reaches only **EGP 7.47**, −44.6% against the price." Conceding value-reducing charges cannot lower the answer: 7.47 is **below** the study's own 11.83, which is arithmetically impossible and visible on page 1. The percentages are inconsistent between the two blocks as well — 6.16 is "−48.0%" in Table 7 (against the central) and "−54.4%" in Table 20 (against spot), neither labelled. None of these figures is in the workbook (`5.95`, `5.954`, `7.473` all return 0 hits in the worksheet XML), so the document and the workbook are running two different models — [L-016]. |
| 21 | **Numeric traceability — builder reads the numbers file exclusively** | **FAIL** | `compute.py` is clean: no financial numeral outside the four-field register, and the emitted file reproduces byte-identically (item 2). But `docx_v6.py` and `figures_v5.py` both read a **second, stale numbers source** (`case_adversarial.json`) for delivered figures and tables. Separately, the delivered workbook is **not reproducible from the committed numbers**: rebuilding with `python3 build_xlsx_v5.py` and diffing cell by cell against the delivered file gives **1 difference in 9,041 non-empty cells** — `Assumptions!E7`, delivered `"AMOC closing price on the Egyptian Exchange, 3 September 2026, from the prices supplied by the principal and committed at engine/prices/SUPPLIED_03-09-2026.json"` against a rebuild and a committed `inputs.spot.source` of `"AMOC closing price on the Egyptian Exchange, 3 September 2026"`. The delivered file was built from a numbers file that is no longer the committed one. |
| 22 | **Script-built files verified by a cell-by-cell diff** | **PASS, with the one difference named** | The diff above is the check itself, run on the delivered file rather than on the builder's intentions: 16 sheets in identical order, 9,041 non-empty cells compared, **1 difference**, itemised in row 21. A clean recalculation alone would not have found it. |
| 23 | Workbook structure | **PASS** | `python3 scripts/check_workbook_structure.py` → "examined 24 study directories … MATCHES THE MODEL REPORT (20): … AMOC …", exit 0. Direct check: `wb.sheetnames` = READ FIRST, Summary, Fundamental Valuation, Assumptions, SOTP Bridge, Segments, Relative & Normalized, DCF, Income Statement, Balance Sheet, Cash Flow, Summary Financials, Monte Carlo, Sensitivity, Per-Share & Ratios, Peer & Sector — `research_protocol.MODEL_STUDY['excel_sheets']`, in order. |
| 24 | Document structure against the model report | **PASS on the skeleton, FAIL on Appendix C** | Sections in the model order: masthead + READ FIRST · Headline · Valuation summary · Company overview · §1.1–1.14 · §2 · §3 · §4 · §5 · §6 · §7 · Appendix A (A.1 IS, A.2 BS, A.3 CF) · Appendix B · Appendix C · About · Disclosure. Two deviations, one benign and one not. **Benign**: Appendix B is "The cost of capital in detail" rather than the model's peers/risk register — the peer frame is genuinely absent (item 10) and the risk register is folded into §5/§7, which is a defensible adaptation. **Not benign**: Appendix C stops at Expert 3 and a panel table — see item 32. |
| 25 | **Depth 1 — standalone bibliography document** | **FAIL** | `AMOC_Bibliography_03-09-2026.docx`, 19 pages, 10 tables. Present: research-layer guide (Table 1), primary-documents table (Table 2), the **full input register of all 177 inputs grouped by layer** (Tables 4–8: Company 130, Industry 2, Country 17, Global 4, House 24), aggregator-discrepancy notes (Table 3, "What the first edition triangulated / What the filing says"), negative results (Table 9, 10 rows). **Missing: the judgements table, each with what would overturn it.** A scan of the full extracted text for `overturn` returns exactly two hits, both narrative sentences about the first edition, and no table. The delivered study's Disclosure (page 21) tells the reader the opposite: "judgements listed with **what would overturn each of them in the companion bibliography**." |
| 26 | **Depth 2 — four-field provenance on every input, validated** | **PASS** | 177 registered inputs; a direct scan for any of `value`/`source`/`date`/`ring` empty or absent returns **0**. Rings: Company 130, House 24, Country 17, Global 4, Industry 2. `compute.py`'s `I()` constructor makes a bare numeral in the inputs block a build failure, and the reachability gate names the **38 quotation-only inputs** explicitly rather than letting them pass silently. One note, not a defect: "House" is a fifth layer beyond the four sweep rings — 24 inputs are analyst judgements, each with its reasoning, and they are listed as such in bibliography Table 8. |
| 27 | **Depth 3 — independent recalculation of the delivered workbook** | **PASS** | `python3 recalc_v5.py` on the delivered file, through the independent evaluator in `xlcalc.py` rather than the library that wrote it: **GATE 1 6,069 formula cells, 0 unresolvable / unchecked** (nothing reported as a skip); **GATE 2 6,069 checked, 0 disagreements**; **GATE 3 11 headline reconciliations all PASS**, including weighted central `Fundamental Valuation!B15` 11.834178, base-year revenue `Income Statement!B19` 46,958.758266, base-year gross margin `B22` 0.096534, per-line cost footing `Segments!B36` 0.000000. Formula share 96.0%. |
| 28 | **Depth 4 — external-reader scrub** | **FAIL** | The study's own `python3 scrub.py` reports "scrubbed 3 delivered files · hard hits (must be zero): **0** · SCRUB PASS". An independent scan finds three real hits it cannot see, and one structural reason why. (a) **"research ring"** in the delivered study: "Every input carries a value, a source, a date and a **research ring** in the companion source register" — `ring` is named in the depth bar as internal-procedure vocabulary; `scrub.py`'s pattern is `\b(Global\|Country\|Industry\|Company) ring\b`, which does not match. (b) **"Ring"** as a column header on the delivered workbook's Assumptions sheet ("Driver · Value · **Ring** · Date · Source"). (c) **`engine/prices/SUPPLIED_03-09-2026.json`** presented as a source in the bibliography's Company-layer register and in `Assumptions!E7` — `scrub.py` has a pattern for exactly this (`repository path presented as a source`) whose character class `[A-Za-z0-9_/]` **excludes hyphens**, so `SUPPLIED_03-09-2026.json` slips past it. (d) Structural: the workbook has **no `sharedStrings.xml`** (openpyxl wrote inline strings), so `scrub.py`'s `xlsx_text()` returns `""` and **the workbook contributed zero characters to a scrub that reported it scrubbed** — [R-ENF-04], an empty result read as a clean one. Clean on the rest: `assert_no_verdict_tokens()` raises nothing; `PARITY` 0, `CRPS` 0, `SIGCM` 0, `R-XXX-nn` 0, `L-nnn` 0; the single `\bPASS\b` hit is "PASS-THROUGH PROCESSOR", ordinary English. **No calibration appendix** — the appendices are A, B, C and calibration evidence sits in §3 as plain-language sentences with the statistics inline, which is correct. |
| 29 | **Depth 5 — figure discipline** | **FAIL** | Five figures are embedded (matched by sha256 to `fig1_football.png`, `fig5_spread.png`, `fig3_bridge.png`, `fig4_adversarial.png`, `fig6_cone.png`). Programmatic: every one is RGBA with alpha extrema **(255, 255)** — zero transparency — and all four corners are pure white, luminance 255. **Every figure was inspected as a rendered image**, and three of the five are defective. **Figure 1 (football field)**: the x-axis stops at ~11.1 so the cash-flow bar (to 24.97), the relative bar (to 15.30) and the "WEIGHTED RANGE" bar are all silently clipped at the frame; neither the cash-flow nor the relative row shows its base diamond because both bases sit off-axis; the caption promises "The price (**red dashed**) is shown against every lens base" and **there is no red dashed line in the plot** — "spot 13.50" is bare text outside the axes; and the row labels publish the retired weights (45% / 20% / 20% / 15%) above a "WEIGHTED RANGE" bar. **Figure 4**: stale, see item 20, on an axis titled "Weighted central". **Figure 5**: the anchor-mixing defect of item 19, plus the "fair value 11.83" label overprints the gold line it annotates (`figures_v5.py:142` places it 0.32 below the line, which collides at this y-scale). Figures 2 and 3 are clean and readable, though Figure 3's "spot 13.50" label overlaps its own dashed line at the top right. |
| 30 | **Depth 6 — table discipline** | **FAIL** | Programmatic check across every table in every document, run independently rather than through the study's own scrub. **Study: 39 tables — starved 0, bloated 0, over-wide 6.** Tables 6, 7, 9, 10, 11 and 27 declare column widths summing to 7.16 / 7.30 / 7.35 / 7.60 / 7.68 / 7.80 inches against a **7.00-inch** text column. **Bibliography: 10 tables — starved 1, bloated 0, over-wide 0** (table 4, column 3, 2.67in against a 38-character unbreakable token). The study's `scrub.py::table_discipline()` tests starved and bloated only — **it has no over-wide test**, which is why it reports the study clean; the consequence is visible on page 1, where the summary table's "Weight" header breaks as "Weigh / t". |
| 31 | **Depth 7 — expert appendix at maximum detail** | **FAIL** | Present per expert: a method paragraph and a worked table. **Absent for all three**: a stated worldview, when-it-works / when-it-fails, and **a falsifier stated in advance** — a scan of the delivered text for `falsif` returns one hit, about a previous edition's headline span, and nothing in Appendix C. A named sensitivity with numbers exists for Expert 1 only ("5.0x to 9.5x multiple band"). **Absent entirely: C.4 cross-examination, C.5 the three in one room, C.6 the divergence table** — scans for `cross-examin`, `one room` and `divergence` return **0 hits**. Two arithmetic defects in the delivered tables: **Expert 2's central of 9.41 sits above the top of its own published range (7.53–9.00)** and **Expert 3's central of 12.74 sits above its own range (9.74–12.23)** (`study_numbers.json → experts.e2.base` 9.411962 vs `rng` [7.532192, 9.000504]; `e3.base` 12.743860 vs `rng` [9.736109, 12.225111]) — the defect ADNOCLS added a bracket-your-own-base assertion for, recurring here with no such assertion. Table C.4's caption reads "**All three land below the market price**" directly beneath its own column showing E1 at **+9.5%**. Expert 1's worked table has an intermediate line ("taxed at the statutory 22.5% and after the minority") with **no value in it**. *On the one thing this item asks that is met*: the labels are "Expert 1 / 2 / 3" and "E1 / E2 / E3" throughout and **no persona name appears anywhere**. |
| 32 | **Depth 8 — the central contested judgement computed both ways** | **FAIL** | The study's own most consequential contested judgement is the base anchor: `compute.py`'s log states "the most recent reviewed period is the half to 30-Jun-2026 at a gross margin of 12.428% … Anchoring there and holding it flat gives **EGP 18.35** a share (+35.9% against spot) against the adopted EGP 11.83. IT IS NOT TAKEN HERE … published as the study's most consequential contested judgement." It is **not published anywhere a reader can see it**: `18.35`, `18.3` and `35.9%` return **0 hits** in the delivered study text, **0 hits** in the workbook worksheet XML, and 0 in the bibliography. The depth bar requires it side by side in the summary table, the body, the workbook **and** an expert's range; it appears in none of the four. What the study does publish both ways — the rating-basis versus CDS-basis cost of capital (9.49 / 11.83) and gross versus net debt weights (11.84 / 11.83) — are second-order choices worth −19.8% and +0.0%, not the +55% one. |
| 33 | **Lens architecture [R-LENS-03]** | **FAIL — the record and the documents disagree** | `python3 scripts/check_lens_design.py` → exit 0 repo-wide, and AMOC's own row is **red on the ratchet**: "AMOC LENS FAIL — the relative multiple 4.8910x IS the traded multiple 4.8910x to within half [a basis point]". The committed `lens_record` is honest about it: `cross_checks[0]` carries `withdrawn: true` and `multiple_source: "THE CURRENT PRICE. The multiple is the company's own traded enterprise value over base-year EBITDA, re-rated by zero, so this lens values the company at what it already trades at". **Neither delivered document says so.** The study publishes it as a live lens: Table 1 row "Relative multiples · own trailing EV/EBITDA, no re-rating · 9.88 · **12.59** · 15.30 · **20%** · −6.8%", and §1.12 calls it "the lens closest to the price" without once saying it is the price. The workbook publishes `LENS 2 — relative multiples, on the trailing metric` at `Fundamental Valuation!C11` = 12.588696 with `B8 = B7*(1+re-rating)` and `B6 = spot × shares + net debt` — the circular construction, unlabelled. **The withdrawal exists only in a JSON file no reader receives.** Separately, the retired 45/20/20/15 blend is published as a **Weight column in Table 1**, as row labels and a "WEIGHTED RANGE" bar in Figure 1, and in §1.1 ("the three cross-checks of section 1.12 **weighted behind it**") — while Table 1's own caption, four lines below the Weight column, states "the other three are cross-checks, published beside it and **carrying no weight**", and the workbook gets it right (`Fundamental Valuation!A17`: "the retired 45/20/20/15 blend, published beside the answer and **unused**"; `A19` states the architecture correctly). `lens_weights` is still a live registered House input. |
| 34 | Bridge record [R-BRIDGE-01] | **PASS on the record, FAIL in the documents** | `python3 scripts/check_bridge.py` → "AMOC stands on the **2026-06-30** sheet; minority on the **value_share** basis", exit 0, and AMOC is **not** on `bridge_outstanding.json`. The record is right on all four clauses: latest disclosed sheet named with the register that establishes it (`amoc_walkforward` investor-relations register, which exists on disk); minority on a value share of the **whole** enterprise including cash, with book / profit-share / proportional published beside it; `cash_charged_once: true`, cash added at face **once**, with the previous net-weighted double-count recorded verbatim as this study's own defect; dividend deducted because declared and unpaid at the bridge date, not a post-balance-sheet declaration. The bridge foots: 13,429 + 3,002 − 487 − 997 − 258 + 595 = 15,284; /1,291.5 = **11.834** (Table 13, page 8). **The documents contradict the record three ways.** (a) The balance sheet is dated **31 December 2025** in three places — Table 3's caption ("the balance sheet at 31 December 2025, as filed"), the §A.2 heading, and the §A.2 column header — while every figure in them is 30 June 2026 (`cash` 3,018.4, `eq_parent` 6,070.3, `assets_snap` 11,405.1, `eq_nci` 72.8, `provisions` 996.9, `div_declared` 258.3, all sourced in the register to the reviewed FS at 30-Jun-2026). Table 7 repeats the wrong date for the provision and the dividend. (b) §B.3 and §1.13 state that **GROSS** weights are "the construction this study rejects, because it **counts the cash pile twice**" — the record says the opposite, that the **NET**-weighted rate plus a face-value add-back is the double count, and that this edition uses gross/unlevered weights (`bridge_record.cash.weights_basis: "gross"`). (c) Table 10 (page 7) publishes and **defends** the net-cash weighting the record abandons — "Debt weight −20.80% … the negative debt weight **RAISES the operating rate above the cost of equity** … correctly isolating and penalising the risk of the pure unlevered operating assets" — while its own two rows both read **27.45%**, so the reader is shown two identical numbers under a sentence saying one exceeds the other. `wacc.wacc_exp` = 0.2745428 = `ke_exp` exactly; the gross-weighted rate `wacc_exp_gross` = 0.2744605 is 0.8bp **below** it. |
| 35 | Macro coherence [R-MACRO-01] | **PASS, with a declared exemption** | `python3 scripts/check_macro_coherence.py` → 24 examined, AMOC among the **5 conforming** and **not** on `macro_outstanding.json`, exit 0. The record is coherent by construction: the currency path and the realised-price path are both **derived** from one inflation ladder by purchasing-power parity, so no second view of the economy exists inside the model (`macro_record.growth_lines[1].exempt_reason`: "DERIVED, not chosen: crude is held flat in dollars and the pound depreciates at the inflation differential"). The gap, named in the record itself: the study carries its **own** Egyptian inflation ladder (14.5 / 13.0 / 11.5 / 10.0 / 9.5), registered as an exemption because it predates the house path and differs from it year by year; conforming it moves every operating number and is scheduled for its own pass. Terminal: rf 12.50% = 7% inflation target + 5.5% real convention, derived not quoted; terminal growth 7.0% = terminal inflation + 0% real, published and reconciled against the reinvestment identity in §1.10. |
| 36 | Cost of capital v2 | **PASS, on the ratchet for the module** | rf 22.31% **normalised** to rf* 18.91% by subtracting Egypt's own 3.40% default spread, and the country premium then added back once inside the ERP — country risk counted exactly once (Table 10 row 2: "netted out of the risk-free rate so country risk is not counted in both the rate and the equity premium"). **Both ERP bases published**: CDS basis 9.41% → Ke 27.45% (central), rating basis → Ke 28.60% and a cash-flow lens of EGP 9.49, published in §1.13 and §B.3. Marginal **Kd 24.31% sits 200bp above the sovereign 22.31%** — the [R-COC-01] finding against the previous edition (Kd 22.00%, 31bp *below* its own sovereign) is corrected. Market-value equity weights (`mktcap` 17,435.25 = 13.50 × 1,291.5), never book. Glide inherited from the cost-of-debt path's own cumulative progress (`glide_frac` 0.000 / 0.517 / 0.731 / 0.893 / 1.000), terminal 18.14% < explicit 27.45%, terminal brought home on the year-5 cumulative factor 0.37676. **Gap named**: `python3 scripts/check_cost_of_capital.py` reports "AMOC carries no cost-of-capital schedule record" — the schedule is built inline in `compute.py` rather than through `engine/cost_of_capital.py`, and AMOC is on `coc_outstanding.json` for that reason. |
| 37 | Forecast anchor [R-ANCHOR-01] | **FAIL, deliberately ratcheted, and the reader is not told** | `forecast_anchor` is committed: latest reviewed period "six months to 30 June 2026, reviewed", rate **12.4278%**, first forecast year **9.6809%**, and since commit `f3167c3d` the whole `forecast_path` **[9.6809%, 9.7027%, 9.7199%, 9.7333%, 9.7450%]**, with **`mechanism: null`**. Every year of the path sits **22.1% to 21.6% relatively below** the latest reviewed period, with no mechanism from the closed list — a breach, and the added path clause shows it is the level rather than only the opening year. `python3 scripts/check_forecast_anchor.py` → exit 0 because AMOC is listed on `anchor_outstanding.json`, whose own note states the reason plainly: the study "**CANNOT name a mechanism its filings support**", a draft record claiming `one_off_in_the_latest_period` was refused on the like-for-like measurement supplied beside it, and the re-anchor is priced at +55% and withheld under [R-VCAL-01]'s one-lever-at-a-time guard. The deferral is legitimate and documented. **What is not**: nothing in the delivered study tells a reader that its forecast opens a fifth below the last period the company reported. Appendix A.1 makes it invisible by omission — see item 39. |
| 38 | Output records [R-ENF-05] | **PARTIAL, ratcheted** | The reverse read exists and is published in plain language: §1.14 "EGP 13.50 is fair if AMOC sustains a gross margin of **10.36%** in every forecast year and in perpetuity … against a base year of 9.65%", with the study's own value beside it. `diagnostics.json` carries it outside the numbers file with the correct rationale. **But `scripts/check_output_records.py` places AMOC on `output_outstanding.json`**, and the reason is real: the widened `assert_reverse_dcf` finds the diagnostic's own value committed **inside** `study_numbers.json` at `/gm_required/level` = 0.10355855194894102. Every use is display, so the rule's purpose is met and its device is broken; ARCC shows the satisfiable pattern. The contested-judgement sign test is committed (`contested_judgements.json`) but its result is not printed in the study. |
| 39 | **3 years historical + 5 years forecast, full FCFF waterfall** | **FAIL on the history, PASS on the waterfall** | The waterfall is shown **inline and complete** to PV of FCFF (Table 12, page 8): revenue → cost of sales → gross profit → operating expense → EBITDA → depreciation → EBIT → NOPAT after profit share → plus depreciation → less capital expenditure → less change in working capital → **free cash flow to the firm** → times discount factor → **present value**, five years, then the terminal block and the EV→equity bridge in Table 13. It does not stop at FCFF. **The history does not meet the bar.** Appendix A.1 is headed "**four filed periods** and a five-year forecast" and carries 6M Dec-2024, 3M Mar-2025, 6M Dec-2025 and 3M Mar-2026 — roughly **eighteen months**, not three years — and it **omits the reviewed six months to 30 June 2026 entirely**, which is the latest reviewed period, the one at 12.43%, and the one the whole anchor question turns on. Table 6 ("the filed margin record") and Table 14 (the reinvestment record) omit it too, so Table 6 states the filed range as "5.05% to 10.19%, a spread of 514 basis points" — a range that excludes the highest filed period in the study's own record and is quoted again in the READ FIRST panel and §7. The five audited fiscal years FY2021–FY2025 that "About this study" says the model rests on appear in no delivered statement table. |
| 40 | Sweep register — primary-source depth | **PASS on substance, FAIL on form** | There is **no Step 2A sweep register in the study directory** — no `sweep.py`, no `research_sweep` import, no four-ring register artefact. What exists in its place is the investor-relations register at `engine/amoc_walkforward/ir_register.json`, which the `bridge_record` names as the register establishing "latest", and the evidence in it is strong. **Company site attempted and logged either way**: §7 records both outcomes explicitly — "the previous edition also stated that the company's own investor-relations site refused connections. **THAT WAS NOT TRUE**, and it was not true because the wrong domain had been tried: amoc.com.eg does not resolve and is not the company's site. The archive is amoceg.com, and this edition is built on **104 documents downloaded from it, including every annual consolidated filing from FY2022 to FY2025**." **Audited fiscal years from the filing itself: four** (FY2022–FY2025) against a floor of two and a target of four. **Every study-year period already disclosed is swept in before the build**: the transition half (audited), Q1-2026 (reviewed) and H1-2026 (reviewed) are all registered inputs, and the H1 **results release** is swept **with** its statements — both are in the register, side by side, as `*_rep` and filed variants, which is what let the study settle the release against the filing to three hundredths of a point. Bibliography Table 2 tags the release-based sources distinctly from the audited-statement sources. **The form is missing**: no four-ring classified register, no B/S/D/C classification, and the IR sources are tagged by narrative rather than by an explicit COMPANY_IR tag a reviewer can count. |
| 41 | One escalator per driver class, with a sourced near-term anchor | **PASS** | The cost stack is split and escalated separately: **feedstock** (91.2% of cost of sales, note 15-A) moves with the barrel through the model's own currency path at `raw_pass` = 1.0, so the gross spread per tonne is held flat in real terms; **conversion** (salaries, supporting materials, other operating costs, depreciation — the 8.8% that is not feedstock) is allocated on registered processing-intensity weights and escalated on the domestic ladder; **operating expense** charges its three disclosed lines on three different drivers — "administrative on inflation, selling on inflation AND tonnage, other on inflation" (Table 12 caption). No single blended index is applied across physically distinct lines. Near-term anchors are sourced, not asserted: `cpi` 14.30% for June 2026 with the May comparative, `rf` 22.31% dated 2026-07-21, the Brent path from a stated flat-in-dollars convention. **This is the correction that moved the answer**: the previous real cost drift of +2.7 points a year is gone, and the forecast gross-margin path now reads 9.68 / 9.70 / 9.72 / 9.73 / 9.74% (Table 12) instead of declining from 9.494% to 8.764%. |
| 42 | Margins as OUTPUTS | **PASS** | No margin is an input anywhere the filings support building one. Table 9 (page 6) derives each line's margin as a residual from realisation less conversion less feedstock, and the eight per-line costs **rebuild the disclosed cost of sales exactly** — asserted as a live workbook cell and confirmed by `recalc_v5.py` ("per-line cost foots, `Segments!B36`: 0.000000"). The blended 9.7% is an output of the mix, not a dial. |
| 43 | Sensitivity in real observable units | **PASS, with two gaps named** | Table 15: six drivers × five points, thirty complete model re-runs written out as live formula blocks on the Sensitivity sheet (not add-backs). The crux is in its own observable unit — gross margin shifted in points on every forecast year, 9.46 → 14.21. **Gap 1**: two rows are dials rather than observables ("volume growth path, as a multiple of the assumed path"; "realisation path, as a multiple"). **Gap 2, reader-visible**: the column headed "**base**" carries **11.23** on the terminal-growth row while every other row carries 11.83, because that grid is sorted ascending and its base point (7%) falls in the "high" column — under a caption asserting "every row reproduces the base case at its own base point … all three properties are ASSERTED in the build". |
| 44 | Bear and bull rebuilt on filed evidence, macro held still | **PASS** | `lens_record.primary.range_basis`: gross margin moved from **5.053%** (the quarter to 31 March 2025, the worst filed) to **13.84%** (the year to 30 June 2022, the best full year filed), tonnage −4.5% / +3.0% a year against a flat base with the bear leg reverting to the five-year mean, `macro_held: true`. Table 19 makes the discipline visible to a reader: the exchange-rate row reads 1.00x / 1.00x / 1.00x and the cost-of-capital row 0.0% / 0.0% / 0.0% across bear, base and bull. The best single **quarter** (13.92%) is deliberately not used, and the record says why: "a forecast margin is sustained for five years and a quarter is not a year." Range **1.59 – 24.97**, published as an envelope with no probability attached. |
| 45 | Terminal value share visible to the reader | **FAIL on consistency** | The computed share is **58.0%** of enterprise value, stated correctly in three places — §1.9 ("Terminal value carries 58.0% of enterprise value, down from 58.7% in the previous edition"), Table 13's source column, and the READ FIRST panel ("the terminal block is 58% of enterprise value, which is high"). **§7 states it as 36%**: "why the terminal block is disclosed as **36%** of enterprise value". A typed number against a computed one, in the section the study tells the reader not to skip. |
| 46 | Fundamental walk-forward — scope stated in the study | **PASS** | `study_numbers.json → walkforward`: LIGHT scope, **9 scoreable cells over five origins FY2021–FY2025**. Stated to the reader in §7 in plain language rather than protocol vocabulary, and led with rather than buried: "THE METHOD DID NOT BEAT 'NO CHANGE' ON THIS COMPANY … profit was under-forecast every single time, by 64% on average", skill **−1.128** against the freeze benchmark. It changed a delivered driver (volume is now flat, not growing) and its far-year bands are carried into Appendix A rather than a point. |
| 47 | Non-sourced drivers logged to the Driver Ledger | **PASS, with one gap named** | `engine/Fundamental_Driver_Ledger.md` line 329 carries a full AMOC entry with the class registration, the LIGHT scope, and each driver decision with **what would overturn it** — the `derived`-not-`unit` filing, β=1.0 pass-through on revenue and feedstock, the undefined borrowing rate, flat volume. **Gap**: the entry is dated **1 September 2026** and has not been refreshed for this edition, so the drivers this edition changed or retired — the corrected cost escalation, the withdrawn `rel_rerating`, and the still-live `lens_weights` — are not in it. |
| 48 | Driver-propagation test on the delivered workbook | **FAIL — the test cannot run, and a delivered document says it did** | `python3 driver_test.py` → **exit 1**: "driver_test REFUSES rather than reporting on a superseded file · delivered workbook: `AMOC_Valuation_Model_03092026_public.xlsx` · missing: the 'anchors' block (dcf, bridge, bs, sum, rn, cols)". The refusal is correct behaviour under [L-067] — it previously opened the 06-08-2026 workbook and reported clean on a file nobody receives — but it means **this edition ships with no driver-propagation evidence at all**. The delivered bibliography asserts the opposite (Method note, page 19): "**every input was perturbed in place with the whole workbook re-evaluated to confirm it moves the headline in the asserted direction**", and the About panel repeats it. `build_xlsx_v5.py` must record the `anchors` block before this can be re-pointed. |
| 49 | Caption and cross-reference accuracy | **FAIL** | Four further computable errors, all found by reading the rendered PDF and none visible to any programmatic check. (a) **Table 16 caption**: "**Not one cell in this grid reaches the market price**" — the top-left cell of the grid it captions reads **13.59** against a market price of **13.50**. (b) **Figure 3 caption**: "the three disclosed claims, worth **EGP 0.89** a share between them" — the three claims drawn in the figure are 0.38 + 0.77 + 0.20 = **1.35**; 0.89 is the net gap after the +0.46 investments add-back, which is not one of the three. (c) **Table A.3 caption**: "Capital expenditure **EXCEEDS depreciation in every forecast year**" — the row it points at reads **0.99x for 2026E**, and the same caption then says "that held in the first forecast year only", contradicting itself in one sentence. (d) **§1.14**: "The figure is now computed by the model and read into this page and into **Figure 2**, so it cannot drift from the model again" — Figure 2 is the gross-spread-per-tonne chart and carries no such figure. |
| 50 | Internal consistency of the base-year account | **FAIL — the study contradicts itself, and the bibliography carries the superseded version** | §1.2 opens: "the half AMOC disclosed to the Egyptian Exchange on 29-30 July 2026 … **HALF OF THIS BASE YEAR IS A PRESS RELEASE AND NOT A FILING**, and this study says so on every page that uses it", and Table 4's basis cell reads "second half **SOLVED**, see below". The **next paragraph** reads "**THE HALF IS FILED, AND THE RELEASED GROSS PROFIT WAS RIGHT** … The previous edition … rejected the released gross-profit line on a coherence test and solved gross profit from the profit line instead. **The filing settles it in the release's favour**." §7 and the READ FIRST panel carry the corrected version ("reviewed, not audited"); §1.2, Table 4, the About panel and the workbook's own **READ FIRST sheet** (`A15`, `A17`: "The gross profit of that reported half is **NOT used as released** … the study solves it") carry the superseded one. The **delivered bibliography page 1** states it to the reader in the same paragraph as the current fair value: "The disclosure is a press release, not a filing … **its gross-profit line is REJECTED on a coherence test recorded below and SOLVED from its own profit line instead**. The fair-value estimate is EGP 11.83 in this [edition]." The model uses the filed figure — `gp_h1cy26` = 3,258,947,633 from the reviewed statements. This is the defect `GAP_REVIEW_01-09-2026.md` found and closed, surviving in the prose of every delivered document. |
| 51 | Bibliography currency | **FAIL** | Page 1: "Companion document to the valuation study dated **6 August 2026**." The study is dated 3 September 2026. The list of filings in hand omits the reviewed six months to 30 June 2026 that 44 of the register's own inputs cite. The negative-results table (Table 9) is stale from the pre-filings edition: "A disclosed non-controlling-interest balance or profit share — **NOT FOUND**. Held at **4.6%**" against a model that uses **2.963%** from a disclosed carrying amount, and page 1 of the same document quotes a third figure ("a minority interest inferred at 3.0% against a disclosed 4.645%"); and "A disclosed depreciation charge, capital-expenditure actual, or property, plant [and equipment] — **NOT FOUND as disclosed lines**", four paragraphs after the same document says the filings overturned both ("a capital-expenditure line modelled at roughly five times the actual cash spend, a depreciation charge modelled at three times the actual"). |
| 52 | Every deliverable ships as a PDF, and both were read | **PASS, and it is what caught the most** | Study 21 pages, bibliography 19 pages, workbook 190 pages, all rendered. **All 21 pages of the study PDF and the bibliography's front matter were read as rendered images at 125 dpi, not as extracted text**, and all five embedded figures were opened individually. That read is the sole source of items 18, 20, 29, 39, 43(gap 2), 45, 49 and 50 — the negative probabilities, the superseded central on page 1, the clipped football field with its missing price line, the missing latest period in Appendix A, the "base" column that is not the base, the 36%/58% terminal contradiction, the four false captions and the base-year self-contradiction. No programmatic check in this repository could see any of them. One typographic note: page 21 is roughly 85% blank. |
| 53 | No rating, no price target | **PASS** | Disclosure, page 21: "no price target is expressed or implied … A fair-value range is an estimate of what a business is worth on stated assumptions". A scan of all three delivered files finds no rating vocabulary and no target. |

---

## What blocks delivery

In the order a reader would meet them.

1. **Page 1 publishes an impossible number.** "Surrender every contested judgement … and the
   central still reaches only EGP 7.47" — below the study's own EGP 11.83, when every item
   conceded raises value. It is the stale `case_adversarial.json`, which also drives Table 7,
   Table 18 and Figure 4, where the study's own published central prints as **5.95**.
2. **Table 24 publishes two negative probabilities**, −74.6% and −16.0%, under a caption
   promising a genuine partition. `CUTS = [11.83, 7.50, 13.50, 11.00]` is not ascending and the
   only guard checks a sum that telescopes to 1.0 whatever the order.
3. **§3 mixes two anchors.** The touch ladder and Figure 5 put the 3-September price of 13.50
   in front of a 6-August cone struck at 9.10, embedding a −32.1% one-month drift the engine
   never produced, and the ladder's 99.5% chance of "reaching this study's fair value" is read
   aloud in the caption. The cone's anchor price is stated nowhere.
4. **The base-year account contradicts itself**, four lines apart in §1.2 and again across the
   workbook's READ FIRST sheet and page 1 of the bibliography, which both still tell the reader
   the half's gross profit was rejected and solved. The model uses the filed figure.
5. **The lens architecture in the documents is not the one in the record.** The relative lens is
   marked withdrawn and circular in `lens_record` and published as a live 20%-weighted
   cross-check in Table 1, Figure 1 and the workbook; the retired 45/20/20/15 blend is printed
   as a Weight column and a WEIGHTED RANGE bar beside a caption saying it carries no weight.
6. **The expert appendix is half-built**: no cross-examination, no three-in-one-room, no
   divergence table, no falsifiers — and two of three expert centrals sit outside their own
   published ranges, under a caption claiming all three land below a price one of them exceeds.
7. **Appendix A omits the latest reviewed period** and carries eighteen months of history against
   a required three years, which is also what hides the [R-ANCHOR-01] breach from the reader.
8. **The central contested judgement is not published.** EGP 18.35 (+55%) appears in no document,
   no table and no workbook cell.
9. **Three delivered date labels are wrong**: the masthead and Table 23 date the 3-September price
   6 August; the balance sheet is dated 31 December 2025 in three places while its figures are
   30 June 2026.
10. **The bibliography has no judgements table**, while the Disclosure tells readers it does; and
    its Method note asserts a driver-propagation test that `driver_test.py` refuses to run.

Everything above is a document defect. **No committed number needs to move to fix any of them**,
with two exceptions worth stating: the adversarial stack must be recomputed on the current model
before it can be published at all, and Appendix A must gain the periods it is missing.

## What is in good order, and should not be disturbed

The beta is conforming and reproduces exactly from `own_stock_beta()`. The ground-up record
covers 100% of revenue with the cost-disclosure gap stated rather than papered over. The numbers
file rebuilds byte-identically from `compute.py`. The delivered workbook recalculates 6,069 of
6,069 formula cells with zero disagreements and differs from its own builder in one text cell.
The bridge, the macro record and the cost-of-capital construction are right in the record, the
Kd-below-sovereign defect is corrected, and the correction that moved the central +19.4% is the
right correction: the forecast margin path is flat at 9.68–9.74% instead of declining, the
escalation now runs one driver per class, and the bear and bull are the company's own filed span
with the macro path held still.

---

*This gate was produced from outside the study. Nothing in `engine/amoc_study/` was edited to
produce it; every recomputation was run against an isolated copy. The only file written is this
one.*
