# GBCO — QC gate, 17-Sep-2026

GB Corp S.A.E. (formerly GB Auto / Ghabbour Auto) · The Egyptian Exchange: GBCO · reporting
currency EGP · edition **17-09-2026**

Filled from OUTSIDE the study by an auditor who did not build it. Nothing here is
self-certified: every row names the artefact, the command or the number that carries it.
Where a row is met with a gap the gap is stated in the same breath, not passed over. No row
is carried forward from `QC_GATE_07-09-2026.md`; every one was re-verified against the
documents, records and code as they stand today.

Neither `gate_result.json` nor any table the study prints about itself is evidence anywhere
below. Both delivered PDFs were read page by page as rendered images — 33 study pages and 11
bibliography pages — and all nine committed figures were inspected as images and measured
programmatically.

---

## VERDICT — NOT DELIVERABLE

**Three of the previous gate's defects: two closed, one open and now priced rather than
silent.** The external-reader scrub now exists and returns zero hits (prior defect 1,
CLOSED). The asset-conversion cycle is built, four periods, every table footed (prior defect
2, CLOSED). Management guidance is still the first-year capital-expenditure driver (prior
defect 3, OPEN — now disclosed and priced at 30.1% of the lower branch, which is disclosure,
not compliance).

**Six defects block delivery, every one verified at source and four of them found only by
reading the rendered pages.** Five are the same species: *the rebuild reached the model and
not the paper.* The model was corrected on 17-09 and the sentence describing it was not.

1. **A working-capital glide the model does not run, stated four times** — study §1.2 (p6),
   §1.7 (p11), §7 (p20) and bibliography p6. The driver is a constant.
2. **"Debt is entirely local-currency on the disclosed facility note"** (study p12) — the
   exact assertion this study's own audit response accepted as WRONG and lever L16 records
   as *retired*. It is also the field that switches off a house hard refusal.
3. **The bibliography translates the associate mark at EGP 47.5** (p7) while the study says
   50.2 twice and the printed mark is arithmetic on 50.25. Same page, four rows apart: 50.2.
4. **The same bibliography row states the filing figure as EGP 15,733,523 thousand** beside
   its own mark of 15,723.5 mn — EGP 10.0 mn apart, the wrong one attributed to note 34.
5. **Capital expenditure: guidance consumed as the first-year input, a 49.0% decline over
   the window with no mechanism named, and the study contradicting itself about whether the
   guidance exists at all.**
6. **The cost side is not built** — one typed gross-margin array stands in for every cost
   line, so there is no cost per unit and no escalator on anything.

Nothing in the valuation arithmetic is wrong. The workbook and both documents rebuild from
their own generators with zero differences. Every defect above is what the page says about
figures that are individually correct — which is precisely the class no programmatic gate in
this repository can see, and why the rendered read is a gate.

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates called in the study's own committed code | **FAIL, one of three** | `assert_beta_provenance()` is called at `beta_reissue.py:59`. **`assert_sigcm()` and `assert_model_study()` are called nowhere in this directory** — `grep -rn "assert_sigcm\|assert_model_study" engine/gbco_study/*.py` returns no call site, only the string "SIGCM" inside comments and the scrub's own banned-pattern list. The study does call `assert_bridge()` (`compute.py:924`), `assert_ground_up()` (`compute.py:1318`) and `assert_lens_design()` (`compute.py:1416`). `python3 scripts/check_study_provenance.py` passes GBCO because its bar is "at least one of the three"; two of the three named gates have never executed on this study. |
| 2 | The repo-level provenance gate, run from outside | **PASS** | `python3 scripts/check_study_provenance.py` → "study directories: 24 · registered indices: 7 · known outstanding: 11 · OK — no new violations", exit 0. GBCO appears on neither the failing list nor the outstanding list. |
| 3 | Standard version stamped = live `STANDARD_VERSION` | **PASS** | `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` → **2026.09.07**. `study_numbers.json → ground_up.standard_version` = **"2026.09.07"**. `python3 scripts/check_standard_claim.py` → "GBCO 2026.09.07 · OK — no study claims a standard it is listed as not meeting". |
| 4 | Beta on its record, not on a boolean | **PASS** | `beta_result.json`: `index_file` = **`raw_indices/EG/EGX30.csv`** — under `raw_indices/`, the published index of the exchange the `code` prefix in `assets/data.js` gives as **`EGX:GBCO`**, not inferred from the `raw_ohlc/EG/` folder. β **0.8907**, n **251** weekly (W-THU, Dimson-corrected, 2021-09-09 → 2026-07-16), R² **24.30%** against a 5% floor, SE **0.2041** against \|β\| 0.8907 — all three usability conditions cleared, so no fall to a peer or 1.0 is owed. `conforming: true`, `interim_note: null`, `index_asof` 2026-07-22. Not a constituent composite: the superseded tier-3 assumed 1.0 is recorded beside it (`superseded_beta`, `delta_beta` −0.1093) rather than deleted. No DFM interim and no Qatari caveat apply. |
| 5 | Step 0.0 data-quality gate | **PASS, with one gap named** | Evidenced on the beta path: `beta_regression.own_stock_beta()` runs `clean_ohlc()` on BOTH series (`engine/beta_regression.py:71-72`) and the record carries the result — `"index_dq": []`, `"stock_dq": []`, i.e. both the GBCO series and EGX30 screened against EGX's own ±20% limit with nothing repaired. **The gap:** `compute.py:52` loads the same library through `primitives.load_ohlc()`, which does no screening — the screened loader is `panel_refresh.load_ohlc()` ("Every series entering a panel passes the data-quality gate first"). So the cone, the technical read and the Step 0 panel in this study run on a series this study never put through the gate itself; the series is screened upstream by the unattended EG refit, which is how the published band record exists. |
| 6 | Step 0 calibration under the band record | **PASS** | The document publishes the BAND RECORD, which reproduces exactly from the live panel. `node` load of `assets/data.js` → `BANDS.GBCO` = `{n:57, hits:54, c50:0.5965, c80:0.9123, c90:0.9474, width:1.423, strength:"long", flag:null}`. Study §3 (p18): "Over **57** resolved three-month forecasts … inside the 90% band **95%** of the time, against a 90% target, and inside the middle 50% band **60%** of the time against a 50% target. THE COUNT IS PRINTED BESIDE THE PERCENTAGE". 54/57 = 94.74% → 95%; 0.5965 → 60%. Width ratio **1.42×** published with "It is NOT a pass mark and nothing here is graded against it" — disclosed, never gated. `flag: null` and the document says nothing about narrow or wide, which is the honest silence the two-sided binomial earns. The study's own `step0` block (n=56, cov90 92.86%) is the Step 0 sample and is deliberately a different sample; it is not what the reader is shown. |
| 7 | No verdict tokens anywhere a reader can see | **PASS** | `band_record.assert_no_verdict_tokens()` run by me on both delivered documents — returns without raising on each. `python3 scripts/check_band_vocabulary.py` clean. No PASS/PARITY/FAIL and no CRPS beside the company name; CRPS appears in neither document. |
| 8 | SIGCM clause 1 — historicals from official sources only | **PASS** | Bibliography §1 (p2–p3) lists **30 primary documents**, every one committed under `engine/gbco_study/src/` (directory holds 30 PDFs + SOURCES.md) and every one downloaded from `ir.gb-corporation.com/en/filings`. Six audited fiscal-year statements (FY2020–FY2025), two reviewed interims (31-Mar-2026, 30-Jun-2026), seven earnings releases, one investor presentation, twelve annual reports. `python3 scripts/check_source_integrity.py` → no GBCO entry on either the breaching or the unreadable list. The escalation `GBCO-audited-statements-not-reachable-from-this-container` (`engine/escalations.json`) is **resolved 2026-09-07**, answer written to `engine/gbco_study/src/SOURCES.md` — the ladder climbed and the answer written where the next session reads it. |
| 9 | SIGCM clause 2 — ground up, on a driver record | **PASS, with the split reported** | `assert_ground_up()` is called at `compute.py:1318` and the record is committed. **Share by level: unit 73.585% · derived 0% · segment 26.399% · topdown 0.016%**, 7 lines, 100.000% of revenue covered. The three unit lines each name the unit and its source: passenger cars 62.75% ("vehicles sold", 26,994 / 42,043 / 56,548 units on EGP 16,544.3 / 36,533.4 / 52,827.3 mn across three disclosed years), commercial vehicles 7.82% (3,404 units on 5,956.8), motorcycles and three-wheelers 3.01% (33,906 units on 2,203.8). Every line below `unit` carries a gap note — tyres/parts/trading 5.01%: "NO UNIT IS DISCLOSED … the build stops at the segment and says so rather than inventing a unit". `python3 scripts/check_ground_up.py` → OK. |
| 10 | SIGCM clause 3 — debt split local vs foreign currency | **FAIL — the record splits it and the delivered document tells the reader it does not** | `cost_of_capital_record.kd_integrity.pct_local_currency` = **0.8610683799581039**, derived by identity from note 26's disclosed EGP **21.91%** and USD **8.30%** average rates and GB Auto's own measured effective rate, and labelled derived. **Study p12 prints: "After-tax cost of debt \| 13.11% \| _debt is entirely local-currency on the disclosed facility note_"** (`docx_A.py:717`). That is the assertion `CRITIQUE_RESPONSE_17-09-2026.md` finding 4 records as **RIGHT / RIGHT / ACCEPT** ("the assertion switched off a whole branch of the cost-of-debt test") and lever L16 records as "**the delivered edition's claim that the book was entirely local-currency is retired**". It is not retired on the page. It matters arithmetically: `scripts/check_cost_of_capital.py` fires its Kd-above-sovereign refusal only at `pct_local_currency >= 0.999`, and adopted Kd **18.20%** sits **480bp BELOW** the 23.00% observed sovereign — the dollar tranche is what puts it there, and it is carried at its own dollar cost inside a pound-nominal weighted rate with no local-equivalent gross-up stated anywhere. |
| 11 | SIGCM clause 4 — asset-conversion cycle studied and projected from | **PASS — the previous gate's defect 2 is CLOSED** | `python3 engine/gbco_study/asset_cycle.py` → DSO / DIO / DPO / CCC for FY2023, FY2024, FY2025 and 1H2026: DSO 27.2 / 21.4 / 25.0 / 25.6; DIO 131.9 / 133.7 / 149.0 / 127.1; DPO 126.5 / 121.6 / 112.7 / **83.3**; CCC 32.6 / 33.5 / 61.3 / **69.3**; "every disclosed table foots to its own stated total: **True**". The cycle is what lever L15 used to refuse the glide. **One gap:** the artefact computes TTM working capital / revenue = **22.78%** on revenue of 75,046.7 while the model's `working_capital_ttm_revenue` is **75,707.1**, giving 22.58% — two TTM revenue denominators for one period inside one study, 0.2pp on the anchor. |
| 12 | SIGCM clause 5 — peers studied for KPIs and multiples | **PASS** | Appendix B.1 (p25–p26) splits the peer frame by leg — auto assembly/distribution, non-bank consumer and corporate finance, fintech associate — each with how it is typically valued and a named caution, and states in terms "no peer is a source for GB Corp's own reported figures, and no peer multiple is applied to the group as a whole". The relative lens takes its multiple from GB Corp's **own** three year-end closes rather than from a peer set, and says why. |
| 13 | SIGCM clause 7 — formula-based model, driver → IS → BS → CF → DCF | **FAIL on the balance-sheet leg** | The workbook is live: 16 sheets, 4,236 populated cells, `recalc.py` reconciles **859 formula cells** against the model. **But there is no property-plant-and-equipment roll-forward.** Capital expenditure is a typed ladder `[3000, 2400, 2500, 2600, 2800]` (`compute.py:275`) and depreciation a flat 1.1% of revenue (`dna_pct`), with no balance sheet joining them. `engine/Fundamental_Driver_Ledger.md`, GBCO section, states it: "Capex, depreciation and working capital are three independent typed ratios with no balance sheet joining them … **SIGCM clause 7 asks for driver → IS → BS → CF → DCF as a live formula model, and for property, plant and equipment it is not met.**" Confirmed live: `grep -n "ppe\|property, plant" compute.py` returns no roll-forward. |
| 14 | SIGCM clause 8 — gaps flagged before issue, stop-and-inform honoured | **PASS** | Bibliography §4 (p9) carries six negative results, each a search actually run: no disclosed useful life (both routes run, both failed, no life chosen); no audited statements for the two earliest intended years (items recorded MISSING, never estimated); no usable beta from the first regression (refused rather than used); no sourced regional revenue split (NAMED AND NOT PRICED); no audited statements for the associate (the reviewers had none either); two ownership percentages (both printed). The terminal refusal is printed in the study body at §1.2 (p7) rather than resolved by inventing a life. |
| 15 | Sweep Register — shared module, four rings, primary access logged | **PASS, with one gap named** | `python3 engine/gbco_study/sweep.py` → "PASS — **4/4 rings, 25 findings** (2 B · 8 S · 11 D · 1 C · 3 NEG), driver gate 11 rows (9 bottom-up / 2 top-down) \| primary access: attempted (1) \| FS years: 3 \| IR findings: 1 \| warnings: 1". `python3 scripts/check_sweep_module.py` → "NOW CONFORMING — remove from the list (1): **GBCO**". Company site attempted **and logged either way**: `primary_access[0]` records `ir.gb-corporation.com/en/filings`, `reachable: true`, HTTP 200, 2026-09-17. Study-year quarters both swept before the build: `study_quarters_disclosed: ["Q1-2026","Q2-2026"]`. `COMPANY_IR` tagged distinctly: source_type counts `{COMPANY_OFFICIAL: 22, REGULATOR_OFFICIAL: 2, COMPANY_IR: 1}`. **The gap:** the register carries **3 distinct fiscal years** (FY2023–FY2025) against the target of 4 — its own warning — while `src/` holds six audited fiscal-year statements (FY2020–FY2025). Above the floor of 2, below the target, and the documents to close it are already committed. |
| 16 | One escalator per driver class, with a sourced near-term cost anchor | **FAIL** | There is no per-unit cost stack, so there is no escalator on anything. `disclosed_drivers.cost_stack` holds `gross_margin: [0.138, 0.142, 0.145, 0.145, 0.145]`, `gsa_pct`, `other_income_pct`, `provisions_pct`, `dna_pct`, `capex`, `working_capital_pct`, `tax_rate` — **not one physically distinct direct-cost line**. A single typed margin path stands in for materials, imported content, assembly labour and freight together, which is the one-blended-index failure in its strongest form. The study's own §1.8 (p12) tells a reader "**The pound sets assembly input costs**" and its risk register names a step devaluation as "the compound risk — assembly costs, rates, and the pound value of the round-price mark at once", while no line in the cash-flow model responds to the pound at all. |
| 17 | Margins as OUTPUTS wherever the filings disclose enough to build cost per unit | **FAIL** | The gross margin is an **input**, not an output: `forecast_anchor.forecast_path = [0.138, 0.142, 0.145, 0.145, 0.145]`, applied to a revenue line built bottom-up from units × price. Every one of the seven `driver_lines` records `cost_basis` = "the auto leg's gross margin" — cost is the residual of the margin rather than the margin being the residual of cost. The study does **not** establish that a finer build is impossible: the six negative-results rows do not include one for cost per unit, so the coarser level is taken without the evidenced absence the rule requires. The near-term anchor itself is sound and sourced — 1H2026 filed 14.2976% on GB Auto's own segment revenue 40,021.5 and gross profit 5,722.1, with the forecast opening 3.5% relatively below it, inside the materiality line, so no mechanism is owed on the opening year. |
| 18 | 3y historical + 5y forecast, full FCFF waterfall inline to PV of FCFF | **PASS** | Appendix A.2 (p23) carries FY2023 / FY2024 / FY2025 as reported; A.3 (p23–24) FY2026E–FY2030E; A.4 (p24–25) the Auto leg's waterfall. Shown inline at §1.2 (p6–p7) and again in A.4, every line: Auto revenue → operating profit before depreciation → less D&A → operating profit → **operating profit after tax at 28%** → plus D&A → less capital expenditure → less increase in working capital → less the part of 2026 earned before the bridge date → **free cash flow to the firm** 160 / 295 / 1,762 / 3,011 / 4,005 → discount factor 0.837 / 0.713 / 0.614 / 0.534 / 0.468 → **present value of free cash flow** 134 / 210 / 1,082 / 1,609 / 1,875 → Σ 4,910 → terminal 59,842 → PV of terminal 28,014 → enterprise value 32,924. It does not stop at FCFF. `python3 scripts/check_waterfall_assertions.py` → "GBCO OK, 11 waterfall table(s)"; `python3 scripts/check_sign_convention.py` → 0 findings across 103 deduction tables book-wide. |
| 19 | v2 WACC — country risk once, marginal Kd, market-value weights, both ERP bases | **PASS on construction, FAIL on the disclosure of the debt book (row 10)** | `python3 scripts/check_cost_of_capital.py` → "GBCO EG, 19.40% gliding to 14.16% · OK — no new violations". Country risk once: rf\* **19.59%** = observed **23.00%** less this sovereign's own default spread **3.41%**, and the premium added back is on the same (market/CDS) basis — reproduced by me. Both bases published: market **9.41%** adopted and named central, rating **13.94%** printed beside it at study p12 and again as 19.86% first-year WACC. Market-value weights 42.4% / 57.6%. Ke reproduces: 19.59% + 0.8907 × 9.41% = **27.97%** explicit; 12.50% + 0.8907 × 7.00% = **18.73%** terminal under the declared `ke_terminal_construction: "same_beta"` — `python3 scripts/check_ke_reproduction.py` → "OK — no new breach", GBCO among the 8 records read. Kd is marginal and independently computed over two periods (FY2025 20.02%, 1H2026 annualised 18.20%) on the borrowings that actually bear the charge, with the denominator described. The 14-day sovereign staleness is 28 days and is **deliberately accepted and disclosed** (study p13, `macro.anchor_staleness_accepted`), which `python3 scripts/check_macro_anchor_age.py` passes. |
| 20 | Macro coherence — one house path, every rate on it | **FAIL on execution, PASS on substance** | The record is substantively right: terminal growth stored as a **real 0.0%** and recomputed to **7.00% nominal** on the house EG path (`macro.terminal_growth_real / _nominal`), terminal rf **12.50%** = terminal inflation 7.00% + the 5.50% real-rate convention, `inflation_inputs` declared (with `terminal_growth` mapped `terminal_flat`) even though nearly empty, and a stated reason that no domestic CPI escalates any line. **But the assertion has never run on it.** The record keys the market as `path: "EG"`; `assert_macro_coherence()` reads `record.get("market")`, so `python3 scripts/check_macro_coherence.py` reports GBCO as `MacroPathError: '' is not a covered market` and the study is excused only by `macro_outstanding.json`. I confirmed it directly: `rp.assert_macro_coherence(d['macro'],'GBCO')` raises. A record written under a rule, in a shape the rule's own instrument cannot read, is declaration without execution. |
| 21 | Forecast anchor [R-ANCHOR-01] | **FAIL — clause two, on the capital-expenditure path** | Clause one passes and the gate confirms it: `python3 scripts/check_forecast_anchor.py` → "GBCO GB Auto gross margin · latest 0.1430 · forecast 0.1380 · −0.50pp", inside the 5% relative line. **Clause two is breached on a different rate the record does not carry.** Capital expenditure runs **3.765% → 2.478% → 2.201% → 2.002% → 1.919%** of Auto revenue, a **49.0% relative fall from its own opening year**, and coverage of book depreciation falls 3.42× → 1.74× against a filed record of 3.87× / 2.79× / 3.67× (FY2023–FY2025). No mechanism is named anywhere. `forecast_anchor.forecast_path` carries the **gross margin only**, so the gate never sees the capex path. `engine/Fundamental_Driver_Ledger.md`, corrected **17-09-2026** — the same day as this edition — states it: "What is unsourced is the DECLINE to 1.76×, below every year the company has filed, which is **[R-ANCHOR-01 CLAUSE TWO]** … **None is named.**" |
| 22 | Company guidance scored, never consumed | **FAIL — the previous gate's defect 3 is OPEN, and the study contradicts itself about it** | Study §1.2 (p6): "capital expenditure **follows the EGP 3,000 mn guided for the first year** and then normalises". Bibliography p6 judgements table: "a ladder falling from 3.77% of Auto revenue in the first forecast year to 1.92% by the last, **whose first year is the figure management guided**", worth **30.1%** — the largest single priced exposure in the study. `sweep_register.json` driver row 8 states the breach in terms: "**The ladder rests on the company's described investment plans, which [R-FCAL-01] forbids as an INPUT, and that is registered as outstanding rather than repaired**". **And the same row also says "No capital-expenditure guidance, maintenance-capex disclosure or costed investment plan is published anywhere"** — which cannot be true at the same time as two delivered documents telling a reader the first year is the guided figure. One of those two statements is wrong and both reach the reader. Pricing an alternative is disclosure; the rule forbids consumption. |
| 23 | Every non-sourced driver logged to the Driver Ledger | **PASS** | `engine/Fundamental_Driver_Ledger.md` carries "GBCO — GB Corp S.A.E (EGX) — fundamental walk-forward, 07-09-2026", "GBCO — the study's own forward drivers, 08-09-2026" and a **CORRECTED 17-09-2026** block appended rather than rewritten, which prices the capex correction at −40.34 a share on the terminal (not the −8.98 the earlier entry claimed), strikes a mitigation that ran backwards, and publishes the filed capex/D&A coverage table. The ledger's working-capital entry is explicitly marked superseded by `asset_cycle.json`. |
| 24 | Walk-forward scope stated in the study | **PASS** | `walkforward_scope`: **FULL**, 14 sourceable fiscal years (FY2012–FY2025), status **run** — nine origins FY2016–FY2024, horizons 1–5, 225 scored cells, **no correction promoted** (the finance-cost candidate flips sign at four of six admissible cuts and is the denominator trap the rule names). `python3 scripts/check_walkforward_scope.py` → "GBCO FULL, 14 years, run". Stated to the reader in Appendix A.1 (p21): "the driver model was rebuilt as it stood at a series of past origins, projected forward, and every driver scored against what the company actually went on to report." |
| 25 | Far forecast years published as ranges, with basis, orientation and count | **PASS — and it is the strongest row in this gate** | Appendix A.1 Table A0 (p22) carries FY2028/FY2029/FY2030 as Low / Point / High with the **band applied**, the **basis** and the **observation count** in their own columns — e.g. group revenue FY2028 86,799 / 146,694 / 327,112 on 0.59–2.23×, basis **span**, **7** observations; group net profit FY2030 on **1** observation, printed as such. The **orientation is declared** in the header sentence: "The bands are stated as the ratio of what the company REPORTED to what the method FORECAST, so a figure above one is a year the method came in low", and again in bibliography §5. Twelve prose lines below the table state, per record length, the arithmetic expectation of a span and the house's own tested coverage beside it. `python3 scripts/check_forward_ranges.py` → "GBCO prints a far-year range, shape A". |
| 26 | Depth bar 1 — standalone bibliography document | **PASS, with one gap named** | `GBCO_Bibliography_17-09-2026.docx` / `.pdf`, **11 pages, 12 tables, 149 lines**. §1 primary documents (30 rows, each naming what it provided); §2 input register grouped by layer; §3 judgements, each with what would overturn it (8 rows) plus the unresolved fork stated separately; §4 negative results (6 rows); §5 figures derived rather than sourced (6 rows); §6 aggregator discrepancies and figures deliberately not used (4 rows). `python3 scripts/check_bibliography.py` → "DELIVERING A STUDY WITH NO BIBLIOGRAPHY: 0". **The gap:** page 11 is blank but for the footer line, and page 1 is roughly 60% white — both are the near-blank-page class the rendered read exists to catch. |
| 27 | Depth bar 2 — four-field provenance on every input, counted | **PASS, with the population named as the gap** | `python3 scripts/check_four_field.py` → "OK — every committed input carries all four fields". I counted them myself off `study_numbers.json → inputs`: **19 inputs, 0 missing-field instances**, grouped **Company 8 · Company (investor relations) 11**. **The gap is the population, not the completeness.** The register carries no Country, Market, Industry or Global layer at all, while the study demonstrably consumes country-layer inputs that appear in no register row: observed rf 23.00%, sovereign default spread 3.41%, ERP 9.41% and 13.94%, terminal rf 12.50%, terminal Kd 15.00%, terminal ERP 7.00% and EGP/USD 50.25. The bar asks for the FULL input register — every input, grouped by layer — and this one is a register of dated historicals. §5 (derived figures) and §3 (judgements) carry some of the remainder, but the macro anchors are in neither. |
| 28 | Depth bar 3 — numeric traceability and an independent recalculation | **PASS** | No financial numeral is typed into a builder that is not first committed: `python3 engine/gbco_study/prose_check.py` → "**prose figures checked: 276; unmatched: 0**", and `python3 scripts/check_prose_figures.py` → "NOW CARRYING ONE — remove from the list (2): **GBCO**, SWDY". `python3 engine/gbco_study/recalc.py` → "**859 of 854 formula cells** reproduce the model, 0 unresolvable, 0 unchecked; **37** headline reconciliations passed; the two branches are published side by side and no answer between them is published anywhere", plus its own gate 4: 0 answer cells strictly between the branches, 0 formulas averaging them. `python3 scripts/check_workbook_values.py` → GBCO in CLEAN (22). Nothing was unparseable and nothing was skipped. *(One cosmetic blemish in the instrument's own message: it prints 859 checks against 854 formula cells because some cells are reconciled twice; the `uncovered` set is empty either way.)* |
| 29 | Script-built files verified by a cell-by-cell diff against the delivered file | **PASS — and this went further than a clean recalc** | I copied the engine, the study, `gbco_walkforward/`, `valuation_calibration/` and `assets/data.js` into a sandbox and rebuilt everything from the committed generators. **Workbook:** `build_xlsx.py` → `build_xlsx4.py`, then a cell-by-cell diff against the delivered file — **16 sheets in the model order, 4,236 cells compared, 0 differ.** **Study document:** `build_docx.py` → 559 extracted lines, **0 differing**. **Bibliography:** `docx_biblio.py` → 149 lines, **0 differing**. So the documents built at 18:43 still reproduce from the numbers file as it stands at 19:37 — today's fix moved no printed figure, verified rather than asserted. *(My first sandbox pass omitted `engine/valuation_calibration/` and reported three false differences in Appendix A.1; re-run with the record present, they vanished. Recorded because a probe that did not run reads exactly like a finding.)* |
| 30 | Depth bar 4 — external-reader scrub | **PASS on the scrub, FAIL on what it does not cover** | `python3 engine/gbco_study/scrub.py` → "documents scanned 2 · patterns 15 banned, 3 ordinary senses declared · **hits 0**", exit 0 — the previous gate's blocking defect 1 is **CLOSED**, and the three declared ordinary senses (panel, register, gate) each name where the model report itself puts them in front of a reader. `python3 scripts/check_delivered_vocabulary.py` → GBCO in CLEAN (22). Calibration evidence sits in §3 as plain-language sentences with the statistics inline and there is no calibration appendix — the document's three appendices are A, B, C. **What the scrub does not catch, counted by me:** "superseded edition / the last edition / an earlier delivered edition" appears **13 times in the study and 10 in the bibliography** — including a whole bibliography section, "Constructions the superseded edition carried, and why they are not judgements" (p8) — which is the section the model report is defined as ADNOCLS **minus**, edition history being internal QC evidence and never in a delivered document. And Appendix C opens (p28) "Every study closes with a panel of standing expert **personas** … we **cast** the industrial trio", naming the machinery behind the labels; the labels themselves are correct (row 33). |
| 31 | Depth bar 5 — figure discipline, verified and inspected | **PASS** | Measured by me with PIL across all nine committed PNGs: **min alpha 255 on every one** (zero transparency) and **corner luminance 241 on every one** (solid light canvas). `python3 scripts/check_figure_opacity.py` → "DELIVERING A TRANSLUCENT FIGURE: 0"; `python3 scripts/check_figure_axes.py` → no GBCO entry. **All seven delivered figures inspected as rendered images**, not generated: Fig 1 football field (p10) — the primary drawn as a span between its branches with a brass tick on each, cross-checks as single marks, the gold band computed from `ENV['low']/['high']` = 18.08–44.12; Fig 2 heatmap (p13) — **the bold cells the title promises do exist**, 32 and 44, which is the DU defect not repeated; Fig 3 moving-average stack (p15); Fig 4 forward cone (p16) with both clocks drawn and labelled; Figs 5 and 6 distributions (p17); Fig C-1 expert ranges (p32), whose gold band is `sorted(bases)[1] × 0.95–1.05` = the median expert, computed rather than typed, and whose row labels resolve correctly against the reversed tick order. No label collision anywhere. Two committed figures (`calibration_GBCO.png`, `figB1_calibration.png`) are embedded in nothing, consistent with there being no calibration appendix. |
| 32 | Depth bar 6 — table discipline, three counts per document | **PASS, with the grid hazard named** | Measured by me. **Study document: 37 tables — over-wide 0 (no table's grid exceeds the 10,080-twip text block), starved 0, bloated 0, fixed layout on 35 of 37.** **Bibliography: 12 tables — over-wide 0, starved 0, bloated 0, fixed layout on 11 of 12.** `python3 scripts/check_column_widths.py` → "documents examined 46; tables 1,040; columns too narrow for a figure they print: 23" with **no GBCO entry**, and "NOW WIDE ENOUGH — remove from the list (4)". **The gap, which is L-014's own hazard:** in **35 of the study's 37 tables the `tblGrid` disagrees with the declared cell widths** — e.g. the §2 lens table's grid is 2520/2520/2520/2520 against declared cells 3096/3384/2088/1656. It does not bite here: every such table declares fixed layout with per-cell `tcW`, and reading the rendered pages confirms the printed proportions are the declared ones, not the equal grid. But the study's own width instrument reads cells, so if it ever did bite nothing here would see it. |
| 33 | Depth bar 7 — expert appendix at maximum detail, labels only | **PASS, with one gap named** | Each of the three carries all five required parts. Worldview and when-it-works/when-it-fails: C.1 p28, C.2 p29, C.3 p30. **Worked table with every intermediate line** — Expert 1: 17,629 + 6,267 + 29,780 = 53,676, less 8% wrapper discount (4,294) = 49,382, ÷1,085.5 = **45.49**; Expert 2: 33,454.3 ÷ 1,085.5 = 30.82 × justified multiple 0.2560 = **7.89**; Expert 3: 28,513 × 0.90 = 25,662 − 15,084 = 10,577 + 5,641 + 25,313 = 41,531 ÷ 1,085.5 = **38.26**. I reproduced all three. **Named sensitivity with numbers:** E1 "at three quarters of the round's valuation … EGP 39.29; at half, EGP 33.09"; E2 "a return one percentage point lower takes him to EGP 5.26; two points higher, EGP 13.14"; E3 "0.85× to 0.60× costs EGP 6.86 a share; 1.00× capital employed adds EGP 2.63". **Falsifier stated in advance** on each ("HE IS FALSIFIED BY …"). Every range brackets its own base (21.75–75.03 / 5.26–13.14 / 31.40–40.89). C.4 cross-examination: six challenges, each marked CONCEDED, CONCEDED IN PART or REJECTED with a reason. C.5 the three in one room. C.6 divergence table isolating the single swing assumption per expert. **Labels are "Expert 1/2/3" only and no persona name appears anywhere** — but the word "personas" does (row 30). |
| 34 | Depth bar 8 — the central contested judgement computed both ways | **PASS, with one gap named** | The basis of the MNT-Halan mark, worth EGP **13,549.1 mn**, 39.5% of the lower answer and 28.3% of the higher. Published side by side and never averaged in: the **summary table** (p2, two rows, 31.64 +9% and 44.12 +52%); the **body** (§1.1 p4–p5 leg-by-leg on both branches, plus a six-rung mark ladder pricing every level of the associate mark against the whole sum of the parts); the **workbook** (`SOTP Bridge!C11` and `!D11`, with `recalc.py` gate 4 asserting 0 answer cells between them and 0 formulas averaging them); and §1.5's envelope, which is the range of the present-value reads rather than a spread invented around a central. `study_numbers.central` is **null** by construction and `central_two_sided` carries both. **The gap:** no single expert publishes both branches — E1 takes the round at face, E2 prices it at nothing, E3 at 0.85× — so the dual framing reaches an expert's range only through C.6's divergence table, which does isolate the same line. |
| 35 | Lens design [R-LENS-03] — one class primary, the rest cross-checks | **PASS** | `python3 scripts/check_lens_design.py` → "GBCO two-sided primary sotp, branches [31.636760647, 44.118669444], 2 cross-checks · OK". Class **"automotive assembler and distributor with a captive lender"**, the row added 07-09-2026, with the cash-flow lens **inside** the sum of the parts as a part. Cross-checks published beside and weighted at zero: relative multiple 18.08, disclosed book floor 30.82. `lenses.weights` is **null**; §1.5 states "There is no weighted blend in this study and no central estimate". `python3 scripts/check_lens_vocabulary.py` and `check_published_lens_vocabulary.py` → no delivered document publishes the retired blend. |
| 36 | The relative lens's circularity operand — today's fix | **PASS, verified independently, and one residual named** | `check_output_sanity` caught the circularity block committing net profit **attributable** while the lens multiplies the **EPS-basis** figure. I re-derived the fix rather than accepting it: `metric_value` **2,989.0288889697918** = `net_profit_attributable` 3,009.373180632814 × (1 − `EPS_DEDUCTION_FY25` 0.006760308689513918) — difference **exactly 0.0**. The lens reproduces: (6.565733997700268 × 2,989.0289 − 0) ÷ 1,085.5 = **18.07938147988689** against a committed 18.079381479886894. `_traded_multiple` **10.524418186818643** = 28.98 ÷ 2.753596397024221 = market capitalisation ÷ metric_value, and **the document prints 10.52×** (§1.3 p8 and the memo row) — on the wrong basis it would read 10.4533. `python3 scripts/check_output_sanity.py` → "OK — … every relative lens either reproduces from its own operands or names what separates them". No printed figure moved, as the commit claims. **The residual:** `lens_record.cross_checks[0].multiple_source` (`compute.py:1371`) still reads "**4.53x (2023), 6.35x (2024) and 10.18x (2025), computed from its own reported net profit attributable**" — the pre-fix trio on the pre-fix basis — while the document prints the computed **4.70× · 6.57× · 10.25×** on the published basic basis (`compute.py:960-963` says so in terms). Three superseded figures and a wrong numerator description left in a committed record's justification text. It reaches no delivered document. |
| 37 | The bridge [R-BRIDGE-01] | **PASS** | `python3 scripts/check_bridge.py` → "GBCO stands on the 2026-06-30 sheet; minority on the value_share basis · OK". Latest disclosed sheet named with the register establishing it (`sweep_register.json`, Company ring). Minority at its **share of value** (4.347% × the leg's equity value = 801.1), deducted from **equity** value, with book 590.7, profit share 246.8 and proportional all published beside it and the reason the book basis was rejected stated. Cash **added at face beside GROSS-debt weights**, and the record names the prohibited pair it is not. Associates at the reviewed carrying value, unlisted, with the qualified review conclusion disclosed. No dividend deducted, with note 10's reason. The bridge foots and divides: 4,910 + 28,014 = 32,924 − 14,494 = 18,430 − 801 = 17,629; + 482 + 15,724 + 507 = 34,342 ÷ 1,085.5 = **31.64**. `python3 scripts/check_bridge_reaches_answer.py` → no GBCO entry. |
| 38 | The terminal [R-TERM-01] | **PASS on the refusal, ratcheted on the record** | No disclosed useful life is sourceable — the policy note gives rate RANGES by class and the identity route depends on an undisclosed land split — so **none was chosen**, the refusal is printed at §1.2 (p7) and recorded in the negative results. `python3 scripts/check_terminal_floor.py` → GBCO listed **unscoreable** ("no nopat_term, no ic"), so the 1/g test has never run on it; `python3 scripts/check_terminal_record_shape.py` → "RECORDS SHORT OF THE CURRENT SHAPE: 0"; `check_terminal_basis.py` and `check_terminal_spread.py` → OK. The charge is not hidden: §1.8 (p13) states "the terminal spends **1.74 times** its own book depreciation on capital expenditure — EGP 2,800 mn against 1,605 mn", against a 10.9-to-17.1-year composite the filings support, and says the direction of that inference. **The gap that is row 21's:** the path reaching that terminal falls 49% with no mechanism named. |
| 39 | The answer, against the price it was struck at [R-GAP-01] | **PASS, both branches, with the review at 8/8 headings** | Committed: `central` **null** (two-sided), branches **31.636760646545103** (+9.17%) and **44.11866944433414** (+52.24%), against `spot` **28.98** dated **2026-09-03**, read from `engine/prices/SUPPLIED_*.json` rather than typed. Both branches sit ABOVE the price, so the below-price trigger does not fire; the above-price trigger does, and the review exists. `GAP_REVIEW_17-09-2026.md` carries **all eight headings** — LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK — plus a VERDICT, and declares `AUDITED CENTRAL: 31.6368`, `AUDITED CENTRAL: 44.1187` and `AUDITED GAP: +9.2% (the nearer branch; the far branch is +52.2%)`. `python3 scripts/check_valuation_gap.py` → "GBCO +52.2% GAP_REVIEW_17-09-2026.md (audits 31.6368) · OK — no new violations". |
| 40 | The publish block [R-GAP-02] and what a reader currently sees | **HELD, correctly — and the page is a full edition behind** | `python3 scripts/check_publish_block.py` → "**HELD GBCO** inside the band at +52.2% of 28.98, but Phase 1 is not proven — 2 of 6 acceptance criteria open". Held on the method, not on the gap: both branches sit above the price and the one-sided block bites only below it. **What a reader sees today:** a real JavaScript load of `assets/data.js` gives `TICKERS.GBCO.fair = {bear: 23.3, base: 35.7, full: 51}` against `spot 29.51` — a single central of **35.7**, which is this rebuild's own **start** value (`rebuild_ledger.start_value` 35.746804894778066), not either published branch. `published_gap_outstanding.json` carries GBCO at "+21.0% (fair 35.7 vs spot 29.51) — study directory, no usable review", magnitude 0.209759. Ratcheted, not a new breach, and it closes when the book publishes. |
| 41 | The rebuild ledger [R-REBUILD-01] | **PASS** | `python3 scripts/check_rebuild_ledger.py` → "GBCO **19 lever(s), 14 rule(s), cumulative −11.5%** · OK — every ledger walks from its start to its answer". Start 35.7468 → 31.6367, each lever naming the rule it serves, each walking from where the last ended, with **three audit points declared in advance** in `REBUILD_PLAN_17-09-2026.md` and committed before any recomputed figure — including the standing one that caught the carrying branch crossing back inside the band **at L15, during the rebuild rather than at the end of it**. The route is visible: L1 +106% on cost of capital, L2 −40% on the macro path, L15 −27% on the working-capital anchor and the bridge date, L16 +6% on one capital structure. |
| 42 | Output records [R-ENF-05] — reverse read and sign test | **PASS** | `python3 scripts/check_output_records.py` → "GBCO price implies the whole-company valuation of MNT-Halan of **614.07** against the study's 1,400.00; **8 judgements, 3 material, sign test p=0.25** · OK". The reverse read lives in `diagnostics.json` and **no builder reads it back** — `grep -rn "diagnostics.json" engine/gbco_study/*.py` returns only `diagnostics_gbco.py`, which writes it. Published to the reader at §1.7 (p11) as a rate as well as a mark: the price implies 25.49% on the round branch and 20.02% on the carrying branch against the study's 19.40%, "Neither figure is used anywhere in the valuation; both are solved from it." The sign test is printed in the bibliography (p8) and **all three material judgements went the same way**, p=0.25, flagged rather than failed, with the unresolved fork named as deliberately outside it. |
| 43 | Artefact currency [R-ENF-06] | **PASS** | `python3 scripts/check_artefact_currency.py` → "GBCO asset_cycle.json built against 31.6368 · GBCO contested_judgements.json built against 31.6368 · GBCO diagnostics.json built against 44.1187 · OK — no new violations". Each declares `published_central`, `published_spot` and `published_central_two_sided`, and the one built against the other branch says so. |
| 44 | The document reproduces the model, everywhere a reader looks | **FAIL — five sentences the model no longer supports** | Found only by reading the rendered pages. **(a)** "net working capital — **glides from 22.6% of revenue to 22.6%** as the payables re-extension completes and the Sadat stock build unwinds" (§1.2, p6); "modelled **from 22.6% of revenue down to 22.6%**" (§1.7, p11); "the operating leg's value lives in the **glide of working-capital intensity from 22.6% of revenue to 22.6%**. **If the higher figure is** the new structural cost…" (§7, p20) — there is no higher figure; `auto_working_capital_pct` is `0.22577406874652442` five times, and lever L15's own evidence says "the intensity is **held flat** at 22.58%" and that the named mechanism is half contradicted by this company's own filings (DPO 112.7 → **83.3** days, the cycle got **worse**, 61.3 → 69.3). The bibliography's judgements table (p6) then records the adopted construction as "gliding from 22.6% … to 22.6% … as payables re-extend" against an alternative of "the first forecast year held flat across the window", which is what the model does — the two sides of that row are the same construction, priced at 2.4%. **(b)** "debt is entirely local-currency on the disclosed facility note" (§1.8, p12) — row 10. |
| 45 | The bibliography reproduces the study | **FAIL — two contradictions inside one table row** | Bibliography p7, judgements table, MNT-Halan round-price row: "**41.61% of the USD 1.4bn primary round … translated at EGP 47.5**" beside its own printed mark of **29,272.6**. At 47.5 that row's own arithmetic gives 27,670.6; 29,272.6 is 0.4161 × 1,400 × **50.25**. Study p4 and the currency note on p33 both say **EGP/USD 50.2**, and `audit_2026_09_17.egp_usd_typed_before` records **47.5** as the superseded typed rate lever L19 replaced. **Four rows below on the same page** the same document says "the round is translated at the house macro path's own **EGP/USD 50.2**, the rate at the strike". Source: `contested_judgements.json → two_sided_judgement.basis_a.what` and `compute.py:1103`. **Second contradiction, same row:** "note 34 … **EGP 15,733,523 thousand**" beside a mark column reading **15,723.5** — EGP 10.0 mn apart, with the wrong figure attributed to the filing. The operative value everywhere else is 15,723.523 (`lens_record.primary.range_basis.low`, `bridge_record.associates.note`). |
| 46 | EPS bases, stated once | **FAIL, small, and the gate already names it** | The study prints two earnings-per-share figures for the same year with only one naming its basis. FY2026E: **2.77** in Appendix A.3 (p24, attributable 3,009 ÷ 1,085.5) against **2.75** in §1.3 (p8, "on the published basic basis"). FY2025: **2.65** in Appendix A.2 (p23) against the **2.635** the relative lens uses as GB Corp's own published basic figure. `python3 scripts/check_eps_reconciliation.py` → "GBCO registers no reported earnings per share beside its attributable profit, so the gap cannot be seen" — listed unreadable on that ratchet. §1.3's own waterfall does reconcile it (3,009 − 20 = 2,989 ÷ 1,085.5 = 2.75); the statements appendix does not. |
| 47 | Table footing and printed totals | **PASS** | `python3 engine/gbco_study/footing_check.py` → "**tables examined: 46; unreconciled totals: 0**". `python3 scripts/check_table_footing.py` → "[ok] GBCO tables examined: 46; unreconciled totals: 0". I re-derived the load-bearing ones by hand off the page: the enterprise-to-equity bridge (p7), the sum of the parts (p4–p5), the relative lens (p8), the book floor (p9) and all three expert tables (p28–p30) each reproduce from the rows printed above them. |
| 48 | Structure, sheets and depth against the model report | **PASS** | `python3 scripts/check_document_structure.py` → "MODEL-REPORT SECTION LIST — 36 sections, read off `MODEL_REPORT_09-08-2026.docx`" and "MATCHES THE MODEL REPORT (22): … **GBCO** …", with "NOW PASSING — remove from the list (1): GBCO" (it was on that ratchet at 9 missing sections). `python3 scripts/check_workbook_structure.py` → GBCO among the 22 matching; my own sandbox rebuild printed the sheet list in order: READ FIRST, Summary, Fundamental Valuation, Assumptions, SOTP Bridge, Segments, Relative & Normalized, DCF, Income Statement, Balance Sheet, Cash Flow, Summary Financials, Monte Carlo, Sensitivity, Per-Share & Ratios, Peer & Sector. The class fits none of the three reference names; it adapts the operating-company pattern inside ADNOCLS's skeleton with a sum-of-the-parts primary, and §1 says which and why. |
| 49 | Records survive their own rebuild; generators reproduce | **PASS** | `python3 scripts/check_record_survives_rebuild.py` → "GBCO compute.py survives"; `python3 scripts/check_numbers_generators.py` → no GBCO entry; `python3 scripts/check_source_rebinding.py` → "no study rebinds a source constant"; `python3 scripts/check_delivered_pdf_currency.py` → "CLEARED: **GBCO**, STC". |
| 50 | No rating, no price target | **PASS** | Read in the rendered documents. READ FIRST (p1): "It is NOT investment advice, NOT a recommendation…". Disclosure (p33): "It contains **no price target and no rating**." §4 (p18) is headed "A fair-value read, not a recommendation". Nothing in either document names a target. |
| 51 | The closing question: what do the filings disclose that the model does not consume? | **Answered — three things, and two of them are why rows 16 and 17 fail** | Asked against the Sweep Register rather than against the work already done. **(i) Segment gross profit by leg.** The 2Q26 release Table 11 discloses GB Auto's and GB Capital's gross profit separately and the register consumes the blended auto figure as a single margin path — no line of the model is built from a cost. **(ii) Note 26's two disclosed borrowing rates, EGP 21.91% and USD 8.30%.** They are consumed only to *derive* a currency weight; neither rate escalates, and the dollar tranche never acquires a local-equivalent cost, which is what leaves Kd 480bp below the sovereign. **(iii) The property-plant-and-equipment note.** It is read for the asset-life search, found to disclose ranges, and then consumed by nothing — there is no roll-forward, so the capital expenditure the model spends never becomes an asset and never depreciates (row 13). Two further disclosures are consumed correctly and are named here because they were the previous gate's open items: the quarterly conversion-cycle tables now feed `asset_cycle.json` and through it the working-capital anchor, and the segmented balance sheet now feeds the bridge on the 30-June-2026 sheet. |

---

## What moved since 07-09-2026, verified rather than accepted

Five ratchets report GBCO as **now passing**, each confirmed by running the gate myself:
`check_document_structure` (was 9 sections short), `check_sweep_module` (was "no sweep script
in the study directory"), `check_prose_figures` (was "no script reconciles the delivered
documents' figures"), `check_delivered_pdf_currency`, and `check_column_widths`. The
external-reader scrub and the asset-conversion cycle did not exist in the previous edition
and both now do. The rebuild ledger walks nineteen levers against fourteen rules with its
audit points declared before any figure moved.

The five stale entries still on the ratchets — `pdf_outstanding` ("no delivered study PDF"),
`prose_outstanding`, `sweep_outstanding`, `document_outstanding`, `coc_state` (edition
"08-07-2026") and `terminal_outstanding`'s claim that "this study additionally does not RUN"
— all describe the superseded edition. `compute.py` runs; `check_record_survives_rebuild`
confirms it. Those lists need `--prune`; that is a repository housekeeping matter and is not
a defect in this study.

---

## What blocks delivery, in one list

1. **Row 44** — the working-capital glide the model does not run, in four places, with a
   mechanism this study's own conversion-cycle record contradicts, and a §7 caveat that has
   lost its referent.
2. **Row 10 / 44(b)** — "debt is entirely local-currency", retired in the model on the
   study's own audit finding 4 and still printed to the reader, on the field that switches
   off the Kd-above-sovereign refusal.
3. **Row 45** — the bibliography's EGP 47.5 against the study's 50.2, in a row whose own
   printed mark is arithmetic on 50.25 and four rows above the same document saying 50.2.
4. **Row 45** — EGP 15,733,523 thousand attributed to note 34 beside a mark of 15,723.5.
5. **Rows 21 and 22** — capital expenditure: the guided figure consumed as the first-year
   input, a 49.0% unmechanised decline over the window, and the study telling a reader the
   guidance exists in one document while its own register says it is published nowhere.
6. **Rows 16 and 17** — no cost-per-unit build, no escalator on any cost line, and no
   negative-result establishing that a finer build is impossible.

Rows 1, 13, 20, 26, 27, 32 and 46 are named gaps rather than blockers, and each is stated
above with the number that carries it.

Every one of items 1 to 4 was found by reading the rendered pages as images. None of them
is a modelling error; the workbook and both documents rebuild from their own generators with
zero differences, and the valuation arithmetic reconciles to the last cell. What is wrong is
what the page says about figures that are individually correct — which is the class the
rendered read exists to catch, and the reason it is a gate.

---

*Every check above ran read-only. `python3 scripts/check_tree_unmodified.py` after the run:
"0 tracked file(s) modified before the run, 0 after — OK, every check that ran left the
committed tree exactly as it found it." The rebuilds were performed in a sandbox copy under
`/tmp`, never in the repository. This file is the only thing this audit wrote.*
