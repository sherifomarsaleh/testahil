# GBCO — QC gate, 07-Sep-2026

GB Corp S.A.E. (formerly GB Auto / Ghabbour Auto) · The Egyptian Exchange: GBCO · reporting
currency EGP · edition 07-09-2026

Filled from OUTSIDE the study by an auditor who did not build it. Nothing here is
self-certified: every row names the artefact, the command or the number that carries it.
Where a row is met with a gap the gap is stated in the same breath, not passed over.

**This gate replaces three earlier ones of the same date.** The first audited the SUPERSEDED
document — a four-lens weighted blend and a single central of EGP 44.3295, neither of which
the study publishes. The second and third were written against the study as it stood before
the rebuilds of `49e3b096` and `a25862b4`. **This edition re-verifies every row against the
current 33-page document rather than carrying any row forward on trust**, and both delivered
PDFs were re-read page by page as rendered images after the rebuild.

---

## VERDICT — NOT DELIVERABLE

Five of the eight defects raised across the earlier editions are now **closed**, every one of
them verified at source rather than accepted from a summary. Three remain, and two further
FAIL rows stand that were never in dispute.

**What blocks delivery:**

1. **No external-reader scrub exists and the delivered bibliography still leaks** (row 10):
   `walk-forward` ×5, `valuation-input block`, "a hard gate rather than a preference". The
   bibliography is byte-identical to the previous pass and the six hits are unchanged.
2. **The asset-conversion cycle is not built** (row 21) — no DSO, DIO or DPO anywhere — and
   the study's own largest priced risk, worth 64.5% of the lower branch, is the five typed
   numbers that stand in for it.
3. **Management guidance is consumed as a driver, and the case is now stronger than it was**
   (row 30). First-year capital expenditure is the figure the company guided. Measured here:
   the forward ladder runs **3.82% → 1.93% of auto revenue** against **5.52%** on the FY2025
   filing, and holding it at the filed intensity is worth **EGP −6.14 to −8.98 a share on
   BOTH branches**. This rebuild added a **risk register** (row 57) that prices nine risks
   from the study's own record — and **capital expenditure is not one of them**. On my
   measurement it would rank second in that register, above the cost of capital at 6.8% and
   the margin path at 3.2%, behind only working capital. The phrase "capital expenditure"
   occurs **once in the whole 33-page document**, in the sentence that calls it guided.

**Two further FAIL rows stand** and are not new: peers are characterised but never studied
(row 22), and the study's own non-sourced forward drivers are absent from the driver ledger
(row 44).

**Closed since the previous edition, each re-derived rather than accepted:** the calibration
appendix is deleted rather than renamed and §3 now carries the band record alone (rows 8,
56a); §6's dangling reference is rewritten and now says in terms that the upper-zone mass is
evidence about the cone's WIDTH and not its median's direction (row 56b); the appendix
lettering matches the model report and `check_document_structure --show GBCO` reads
`status: ok` (row 2); and Figures 5 and 6 no longer label a 29.51-struck histogram with the
other clock's price (row 11).

**What the rebuild added, and it is not a formality** (row 57): Appendix B now carries a
**risk register** whose nine entries are each PRICED or NAMED-AND-NOT-PRICED with the figure
read straight from the committed contested-judgement record, and a **research register**
whose six negative results include the useful-life refusal that §1.2 points at. Both are new,
both reconcile, and neither existed in any earlier edition of this study.

Nothing in any of this moved either published branch. The valuation arithmetic reconciles on
the rebuilt files: 851 of 851 formula cells, 37 headline reconciliations, 270 prose figures,
46 tables, 11 instructing waterfalls, 7 opaque figures — all clean.

---

## Derivations stated before the build, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker, market, exchange | GBCO · EG · **EGX** | `assets/data.js` carries the code prefix `EGX:`, which is what decides the regressor. Not inferred from the `raw_ohlc/EG/` folder, which groups by market code. |
| Reporting and valuation currency | **EGP** | Every consolidated statement in `engine/gbco_study/src/` is headed EGP thousand. One dollar input — the MNT-Halan round — is translated at a flagged EGP 47.5 and labelled an estimate in the Disclosure. |
| Company class | **automotive assembler and distributor with a captive lender** | Registered 07-09-2026 in `research_protocol.LENS_REGISTRY` and in `lessons_register.CLASSES`; the two agree by import or the import fails. `python3 engine/lessons.py GBCO --class "automotive assembler and distributor with a captive lender"` returns 260 lessons. The class was opened through an escalation, not assumed: `lens-registry-has-no-row-for-an-auto-assembler-with-a-captive-lender`, now resolved. |
| Reference pattern adapted | **ADNOCLS** (operating company), inside ADNOCLS's skeleton | `REFERENCE_SET` is closed at ADNOCLS / ADCB / ALPHADHABI and asserts at import. GB Corp fits none: it is not a bank (the lender is a segment, not the firm), and it is not a holdco (a holdco IS its stakes; here 91% of the traded capitalisation is covered by the two operating legs). The study says which and why in §1. |
| Primary lens | **sum of the parts**, two-sided | `study_numbers.json:lens_record.primary.kind == "sotp"`, `two_sided: true`. `python3 scripts/check_lens_design.py` → `GBCO two-sided primary sotp, branches [41.348367298, 52.345259974], 2 cross-checks`. |
| The answer | **EGP 41.3484 and EGP 52.3453 — no central** | `central: null`; `central_two_sided.branches`. The two differ in ONE input: the associate carried at its reviewed value (EGP 15,733.5mn) or at the June-2026 round (EGP 27,670.7mn). Unmoved by the rebuild. |
| Latest known price | **EGP 28.98, close of 3 September 2026** | `engine/prices/SUPPLIED_03-09-2026.json`. Read by the resolver in `compute.py`, never typed. Note `SUPPLIED_07-09-2026.json` is a NEWER FILE carrying an OLDER close (29.51 of 23 August); `gap_today.latest_price_per_ticker()` merges on each price's own date, so 28.98 wins. Correct. |
| Cone anchor | **EGP 29.51, 23 August 2026** — the exchange library's last session | `engine/raw_ohlc/EG/GBCO.csv`, 3,637 sessions. As of this rebuild this is genuinely the number the paths start on (row 53), which it was not before. |
| Standard version | **2026.09.07** | `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` → `2026.09.07`. The study stamps `ground_up.standard_version = "2026.09.07"`. `python3 scripts/check_standard_claim.py` → `GBCO 2026.09.07 · OK`. |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | Deliverables present and on one edition date | **PASS** | `GBCO_Valuation_Study_07-09-2026_public.docx` + `.pdf` (31 pp, rebuilt 00:41), `GBCO_Valuation_Model_07092026_public.xlsx`, `GBCO_Bibliography_07-09-2026.docx` + `.pdf` (11 pp, rebuilt 00:37), `GAP_REVIEW_07-09-2026.md`, this gate. `python3 scripts/check_calibration_deliverables.py` → `GBCO edition 2026-09-07`, complete and self-consistent. The filename and edition date of this gate are unchanged, so the edition does not break. |
| 2 | Word structure matches the model report's 16-section skeleton | **PASS — the previous edition's FAIL is CLOSED** | `python3 scripts/check_document_structure.py --show GBCO` → **`status: ok`**, and the book-wide run prints **`NOW PASSING — remove from the list (1): GBCO`** and lists GBCO under `MATCHES THE MODEL REPORT (22)`. The appendices were **re-lettered, not renamed around the problem**: the calibration appendix is deleted outright (row 8), the peer frame moved B→**Appendix B** and gained the two sections it was missing, and the expert panel moved D→**Appendix C** as C.1–C.6. The document now carries `Appendix B, B.1, B.2, B.3, Appendix C, C.1, C.2, C.3, C.4, C.5, C.6` against a wanted list that is identical; A.4 and A.5 are extra, which the gate permits. **The cross-references were enumerated and read in context, not search-replaced** — all seven resolve: the masthead and §7 point at Appendix A for the far-year ranges; the summary table's expert block reads "(Appendix C)"; §1.2's driver table points at §1.6 and Appendix A; **§1.2's useful-life refusal now reads "Appendix B's research register carries that search among this study's negative results", and B.3's negative-results list carries it as its first entry** — verified by reading B.3, not by trusting the pointer; §3's drift sentence and §6 and §7's drift bullet now point at §3 rather than at a deleted appendix. `Appendix D` occurs **0** times, `D.1`–`D.6` **0** times, `calibration appendix` **0** times. `document_outstanding.json` still lists GBCO and is now a stale entry (row 55). |
| 3 | Excel: 16 sheets, model names, model order | **PASS** | `wb.sheetnames == research_protocol.MODEL_STUDY['excel_sheets']` returns **True**, 16 sheets. `python3 scripts/check_workbook_structure.py` lists GBCO under `MATCHES THE MODEL REPORT (22)`. |
| 4 | §1 carries 1.1–1.9 in the model's order | **PASS, with one adaptation argued** | 1.1 sum of the parts and both branches · 1.2 the Auto-leg cash-flow model · 1.3 relative multiples · **1.4 "Earnings power — why this class carries no normalised-earnings lens, and the book floor instead"** · 1.5 synthesis · 1.6 legs and the units × price driver table · 1.7 the crux · 1.8 macro and country · 1.9 sensitivity. The model's 1.4 is normalised earnings power; GBCO replaces it and gives the issuer-specific reason on p.8 — associate income was 56% / 30% / 34% of profit attributable in FY2023/24/25 and swings on marks rather than on an operating cycle, so "normalising earnings that swing on associate marks normalises noise". Argued, not dropped. |
| 5 | **Depth 1** — standalone bibliography at the required depth | **PASS, with one gap named** | `python3 scripts/check_bibliography.py` → clean. `GBCO_Bibliography_07-09-2026.pdf`, 11 pp, **re-read as rendered images after the rebuild**: sourcing position · primary-documents table of **30 documents** all committed under `src/` · input register of **19 inputs** grouped by layer (Company 8, Company-investor-relations 11) each with value, unit, source, date and route · judgements table of 7 · a "named and deliberately NOT priced" table of 3 · a removed-constructions table of 5 · negative results table of 6 · derived-figures table of 6 · aggregator-discrepancy table of 4. **The gap, unchanged by the rebuild: the judgements table has no what-would-overturn-it column.** Its own introductory sentence says "The last column is what would overturn the choice" and the last column is headed **"Direction taken"** — the sentence is false of the table it introduces. The overturn conditions are supplied instead as one paragraph after the table, per judgement but not per row. |
| 6 | **Depth 2** — four-field provenance on every input, validated by assertion | **PASS on the fields; the register's SCOPE is the gap, and it is named** | `python3 scripts/check_four_field.py` → `OK — every committed input carries all four fields`. All **19** inputs in `study_numbers.json:inputs` carry value + source + date + layer, plus unit, tier and extraction route. **The gap: 19 is the whole register, and the study's largest inputs are not in it.** The MNT-Halan carrying value (15,733.5), the round valuation (USD 1,400mn), the stake (41.61%), the EGP/USD 47.5, the other associates (496.9) live in `study_numbers.json:sotp` and are sourced in prose and in the bibliography's judgements and negative-results tables rather than in the four-field register — and between them they are 35% to 49% of the answer. **The forward capex path (row 30) is not in it either.** The model report's own register carries 594 inputs. |
| 7 | **Depth 3** — numeric traceability and an independent recalculation | **PASS** | `cd engine/gbco_study && python3 recalc.py` **re-run on the rebuilt workbook** → **"851 of 851 formula cells reproduce the model, 0 unresolvable, 0 unchecked; 37 headline reconciliations passed; the two branches are published side by side and no answer between them is published anywhere."** Two extra gates inside it: `answer cells lying strictly between the two branches: 0` and `formulas averaging the two branches: 0`. From outside: `python3 scripts/check_workbook_values.py` lists GBCO under `CLEAN (22)`. Nothing was skipped and nothing was unparseable. `python3 scripts/check_prose_figures.py` → `GBCO prose figures checked: 270; unmatched: 0` — four more figures than before the rebuild, all matched — and the gate reports `NOW CARRYING ONE — remove from the list (2): GBCO, SWDY`. |
| 8 | **Depth 4a** — no calibration appendix | **PASS — the previous edition's FAIL is CLOSED, and by deletion rather than by relabelling** | `research_protocol.MODEL_STUDY['word_skeleton']` §3 requires "calibration evidence as plain-language sentences with the statistics inline — **no calibration appendix**". The appendix is **gone**: `calibration appendix` and `Calibration —` return **0** occurrences in the delivered text; the 56-window coverage table, its distribution-centre column and Figure B-1's embed are all absent; the document now embeds **7** images (Figures 1–6 and C-1) against 9 before, with no gap in the numbering and no stale "Figure B-1" reference. **The re-scored replay is still generated and committed and reaches no reader** — `figures.py:257-259` still writes `figB1_calibration.png` and copies it to `calibration_GBCO.png`, and no builder references either — which is exactly [R-REC-01]'s settled disposition for that record rather than an orphan. **Checked for residue rather than assumed:** searching the delivered text for every figure the replay carried returns `56 windows` 0, `56 non-overlapping` 0, `80% band` 0, `Distribution centre` 0, `calibration replay` 0, `zero drift` 0, `monthly origins` 0, `158` 0. Three apparent `93%` hits and one each of `0.57` and `0.59` were read in context and are the ownership percentage **42.93%**, a discount factor of 0.579 and a forward band of 0.59–2.23× — false positives of a substring probe, not survivors. |
| 9 | **Depth 4b** — no verdict tokens; CRPS only on the methodology page | **PASS** | `band_record.assert_no_verdict_tokens(text)` re-run over the extracted text of both rebuilt PDFs → **no verdict tokens** in either. `PASS`/`PARITY`/`FAIL` beside the company name: none. Occurrences of the string `CRPS`: **0** in both documents. `python3 scripts/check_band_vocabulary.py` → `OK — no verdict vocabulary in any page text; every published band record agrees with its panel`. |
| 10 | **Depth 4c** — external-reader scrub, zero hits for internal-procedure vocabulary | **FAIL** | **The study commits no scrub script at all.** `ls engine/gbco_study/*.py` returns 19 files — `recalc.py`, `prose_check.py`, `footing_check.py`, builders — and nothing that scans the delivered documents against a banned-vocabulary list; the model report's own study carries `qc_checks.py` for exactly this. Scanning from outside, word-boundary matched over `pdftotext` output of the **rebuilt** files: the study PDF is **clean**; **the delivered bibliography still carries `walk-forward` five times** (extract lines 8, 176, 191, 347, 349 — "in the walk-forward behind it", "the walk-forward panel", "The panel behind the walk-forward", "The walk-forward targets as long a…", "walk-forward's **valuation-input block**"), plus "which is a **hard gate** rather than a preference" (line 406). Six hits, unchanged. The shared shape gate `python3 scripts/check_delivered_vocabulary.py` lists GBCO under `CLEAN (22)` and is right to — it matches rule identifiers and repository paths by shape and says of itself that it "does NOT replace the per-study scrubs, which catch procedure NOUNS". This is that hole, open. |
| 11 | **Depth 5** — figure discipline, zero transparency, every figure inspected as an image | **PASS — the previous edition's named residual is CLOSED** | Seven figures now reach a reader. Programmatic, re-run on the images **extracted from the delivered `.docx` itself** rather than from the build directory: every one is RGBA with **minimum alpha 255** across all pixels and a corner colour of (246, 241, 230) — solid light canvas, zero transparency. `python3 scripts/check_figure_opacity.py` → `OK — every delivered figure is opaque to the pixel`. `check_figure_axes.py` → no GBCO entry. **All seven re-inspected as rendered images.** Figure 4 draws and names **both clocks** — a solid line labelled "cone struck on 29.51 — the library's last session" and a dotted one labelled "latest known price 28.98" — with the labels spaced clear of the lines they describe. **Figures 5 and 6 previously labelled their reference line "spot 28.98" beside a histogram struck from 29.51; both now read "latest known price 28.98"**, matching the fan chart, so no figure in the study names one clock while drawing the other. Figure 2's heatmap marks the two published branches ‡ and prints the two published answers in bold, and the bold cells its title promises do exist. |
| 12 | **Depth 6** — table discipline, fixed layout, starved/bloated/over-wide across every table | **PASS, with one gap named** | Re-measured on the rebuilt `.docx`: **study — 37 tables, 140 columns, over-wide 0, starved 0, bloated 0**; **bibliography — 12 tables, 53 columns, over-wide 0, starved 0, bloated 0**. The study gained a table on net: the calibration coverage table left, and the risk register, the research register's source table and the peer table's new caution column arrived. From outside: `python3 scripts/check_column_widths.py` → 46 documents, 1,039 tables, 27 columns too narrow book-wide, **none of them GBCO's**. `python3 engine/gbco_study/footing_check.py` → `tables examined: 46; unreconciled totals: 0`. **The gap: 3 of the 49 tables (2 in the study, 1 in the bibliography) do not declare fixed layout**, though all 49 carry an explicit grid, so none is at risk of the renderer-versus-declared-width defect. |
| 13 | **Depth 7** — expert appendix at maximum detail | **PASS** | Appendix D, pp.26–30, re-read as images. Expert 1 (split-legs net asset value): worldview; when-it-works / when-it-fails; a worked table of 7 intermediate lines from the auto leg to EGP 52.07 per share; a named sensitivity with numbers (mark at three-quarters of the round → EGP 46.20; at half → EGP 40.34); a falsifier stated in advance (a real secondary transaction at a materially different price, or a second closing repricing the round). Expert 2 (residual income on the whole group): 7-line table to EGP 7.89, sensitivity ±1pp of return → EGP 5.26 / 13.14, falsifier, plus two self-stated contaminations. Expert 3 (cash returns): 9-line table to EGP 36.88, two ranked levers (0.85× → 0.60× worth EGP 6.49; 1.00× capital employed worth EGP 2.63), falsifier. D.4 cross-examination — **6 challenges, each recorded CONCEDED, CONCEDED IN PART or REJECTED with its reason**. D.5 the three in one room. D.6 a divergence table isolating the single swing assumption per expert, plus Figure D-1. |
| 14 | **Depth 7b** — experts labelled "Expert 1/2/3", no persona name anywhere | **PASS** | Headings read "D.1 Expert 1 — the split-legs net asset value and the marks", "D.2 Expert 2 — residual income on the whole group", "D.3 Expert 3 — cash returns: return on capital against its cost". No persona name appears anywhere in either document. The appendix additionally records that the previous edition's mid-cycle-earnings expert was retired **as a method**, not renamed as a persona. |
| 15 | **Depth 8** — the central contested judgement computed both ways, published side by side, never averaged | **PASS — and this is the study's strongest row** | The contested judgement IS the answer. Both readings appear in the valuation summary table (p.2, two rows), in §1.1's leg table (two full columns), in §1.5's synthesis table, in the mark ladder (six rungs from EGP 2,308mn to 27,671mn with the sum of the parts recomputed at each), in Figure 1, in Figure 2 (both marked ‡), in Figure 4's two brass dashed lines, in §4, in §7 and in the Disclosure. In the workbook: `Sensitivity` carries both branch columns and `recalc.py` asserts each reaches its own branch to six decimals. Two structural refusals are asserted rather than promised: **0 answer cells lie strictly between the branches** and **0 formulas average them**. Expert 1 and Expert 3 both take the round-price side and Expert 2 prices the associate at nothing, so the panel spans the same fork. |
| 16 | Step 0 — calibration published as a band record, coverage against target, count beside the percentage, width ratio disclosed and never gated | **PASS, with one gap named** | Now published in §3 as plain sentences with the statistics inline, under "How often these bands have actually held", which is where the model report asks for it. Verified against the live record `assets/data.js:BANDS.GBCO` = `{n:57, hits:54, c50:0.5965, c90:0.9474, width:1.423, strength:"long", flag:null}`: the page reads "Over **57** resolved three-month forecasts … the price finished inside the 90% band **95%** of the time, against a 90% target, and inside the middle 50% band **60%** of the time against a 50% target" — 54/57 = 94.7% ✓, c50 0.5965 ✓ — and states in capitals that **THE COUNT IS PRINTED BESIDE THE PERCENTAGE**, with the record's own strength read out ("57 resolved windows is a long record for a single name", against `strength:"long"`). **The width ratio is disclosed with no threshold**: "this cone's 90% band is **1.42** times as wide as a naive carry-anchored one … It is NOT a pass mark and nothing here is graded against it." **And it is read, not typed** — `figures.py:37-59` defines `_band_record()`, which parses the `BANDS` block out of `assets/data.js`, refuses under `[R-ENF-04]` if the block or the record is absent, and requires `n`, `hits`, `c50`, `c90`, `width`. The published flag is `null` so nothing is said about narrow or wide, which is the rule's own "silence is the honest response". **Gap — the binomial is still not printed.** Computed here on the published record: 90% band 54/57, two-sided **p = 0.372**; 50% band 34/57 against 50%, **p = 0.185**. Neither earns a flag, so the page's wording is right, but it is asserted rather than shown. **An observation, not a defect:** the 80% band, 52/57 = 91.2% against 80%, gives **p = 0.031** and would earn a flag — but `band_record.py:357-359` computes the flag on the 90% band alone, which is what [R-CAL-02] specifies, and the study does not publish the 80% figure. |
| 17 | Step 0.0 — data-quality gate on every series before anything else | **PASS** | Both series pass and both records are committed empty-of-findings: `beta_result.json` carries `"index_dq": []` and `"stock_dq": []`, i.e. `beta_regression.own_stock_beta()` ran `data_quality` on the stock and on the regressor and neither raised. `engine/raw_ohlc/EG/GBCO.csv` 3,637 sessions to 23-Aug-2026 against the EGX ±20% daily limit; `engine/raw_indices/EG/EGX30.csv` 3,744 sessions, as-of 2026-07-22. The cone reads the persistent library and not the study-local extract, and `compute.py` records why — the study-local copy stops at 7 July 2026. |
| 18 | SIGCM 1 — historicals from official sources only, no vendor / broker / press | **PASS** | `python3 scripts/check_source_integrity.py` → no GBCO entry among the 1 breaching and 5 unreadable. All **30** primary documents are GB Corp's own and are committed under `engine/gbco_study/src/`: 12 annual reports (2013–2024), 6 audited consolidated statements (GB Auto FY2020–22, GB Corp FY2023–25), 2 reviewed interims (31-Mar-2026, 30-Jun-2026), 9 earnings releases (4Q20–4Q25, 1Q26, 2Q26), 1 investor presentation. Every one of the 19 registered inputs names a GB Corp document and its extraction route. The bibliography's own first section states the position and records the earlier failure honestly: an earlier attempt ran six routes and concluded the filings unreachable, having assembled the host list from the company's FORMER name and never tried its current domain. Escalation `GBCO-audited-statements-not-reachable-from-thi` is `resolved (answer on HEAD)` per `python3 scripts/check_escalations.py`. |
| 19 | SIGCM 2 / [R-SIGCM-02] — ground up, on a driver record, 100% of revenue, gaps noted | **PASS, with the split reported not ticked** | `compute.py:823` calls `research_protocol.assert_ground_up(_DL, 'GBCO')` in the study's own code, and `python3 scripts/check_ground_up.py` re-runs it from outside → OK, with GBCO no longer on that ratchet. **6 driver lines summing to exactly 1.000000 of first-forecast-year revenue.** Share by level: **unit 74.42% · derived 0% · segment 26.70% · topdown −1.11%.** Unit lines: passenger cars 63.46% (26,994 / 42,043 / 56,548 vehicles on EGP 16,544.3 / 36,533.4 / 52,827.3mn across three disclosed years), commercial vehicles 7.91% (3,404 units, one disclosed year, and the record says the level is observable and the trend is not), motorcycles and three-wheelers 3.04% (33,906 units, one year). Segment lines both carry gap notes: tyres/parts/trading 5.07% ("NO UNIT IS DISCLOSED"), GB Capital 21.63% ("BUILT ON THE SEGMENT TOTAL WHILE A FINER SPLIT IS DISCLOSED" — Table 13 breaks it into six companies). The topdown line is the intersegment elimination, negative, and **the one line whose cost side rests on a typed proportion says so in its own record**: "the eliminated sales are reversed WITH their own cost, at a stated 20% gross margin … THAT PROPORTION IS TYPED RATHER THAN DISCLOSED", 1.1% of group revenue. |
| 20 | SIGCM 3 — debt studied in full, local-currency vs foreign-currency split | **PASS** | `cost_of_capital_record.kd_integrity.pct_local_currency = 1.0`, sourced: "GB Corp FY2025 audited consolidated statements, notes 26 (loans) and 38 (bonds), and the segmented balance sheet in the 2Q26 earnings release as at 30 June 2026; **every disclosed facility is EGP-denominated**." Group borrowings EGP 42,476.0mn at 30-Jun-2026, of which the GB Auto segment carries 22,733.1mn — both registered with four fields. No FX tranche exists, so no local-equivalent carry is owed. |
| 21 | SIGCM 4 — asset-conversion cycle studied from the statements, balance sheet and cash flow projected from it | **FAIL** | **No DSO, DIO, DPO or cash-conversion cycle appears anywhere** in `compute.py` or in any builder — grep for `dso`, `dio`, `dpo`, `days sales`, `days inventory`, `days payable`, `receivable days` returns nothing but the phrase "cash conversion" used in prose. Working capital is projected from **one typed intensity ratio on revenue**: `compute.py:214` `wc_pct = [0.265, 0.250, 0.235, 0.225, 0.215]`. Receivables, inventory and payables are never separated, and GB Corp's statements disclose all three. This is not a small gap: the bibliography's own judgements table prices the glide at **64.5% of the answer** — the largest single judgement in the study, larger than the beta, the equity-risk-premium basis and the margin path put together — and it rests on five numbers with no disclosed-cycle build behind them. The study registers it as a contested judgement with both framings, which is what depth standard 8 asks; SIGCM clause 4 is a separate requirement and it is not met. |
| 22 | SIGCM 5 — competitors studied for operating KPIs and valuation multiples | **FAIL, narrowly, and improved but not closed** | B.1 names peers — Al Watania, Saudi Co. for Vehicles, Turk Traktor, Astra Industrial; listed Egyptian finance companies; Fawry, e-Finance and MNT-Halan's own rounds — with valuation conventions, and **this rebuild added a fourth "Caution" column** saying why each set travels badly ("None of them assembles under the same localisation regime or carries a captive lender"; "Funding mix and provisioning policy differ enough that the multiple travels badly"; "A listed comparator has a price and this holding has none"). It also now states in terms that "the absence of a single clean comparable is itself a finding rather than a gap in the research". That is a real improvement and it is the honest reading of the disclosure. **It is still not what the clause asks.** The `Peer & Sector` sheet carries **0 formulas and 0 numeric cells**; not one peer multiple, operating KPI or dated figure is computed anywhere in the workbook or printed anywhere in the document. Peers are characterised, not studied. The relative cross-check deliberately uses GB Corp's OWN trailing multiple instead (row 35), which is defensible and is NOT the gap; the gap is that no peer figure was sourced, dated or registered. |
| 23 | SIGCM 6 / [R-BETA-04] — beta from own history against the exchange's PUBLISHED index, inspected on the record | **PASS** | `beta_reissue.py:59` calls `assert_beta_provenance(rec)` in the study's own code, and `python3 scripts/check_study_provenance.py` re-runs the record test from outside — GBCO does **not** appear in the outstanding list. The record, inspected rather than trusted: **`index_file = "raw_indices/EG/EGX30.csv"`** — under `raw_indices/`, not a constituent composite. GBCO is EGX-listed per the `EGX:` code prefix in `assets/data.js`, and EGX30 is the registered index for (EG, EGX), so no interim note is owed and `interim_note` is correctly `null`; not a DFM name, so `wacc_builder.index_interim_note('AE','DFM')` does not apply; not Qatari, so the QATAR10 weekly-only caveat does not apply. **Usability gate cleared on its own terms, so no fallback was owed:** n = **251** weekly observations (≥24), R² = **0.2430** (≥5%), SE(β) = **0.2041**, below the absolute beta of **0.8907**; `usable: true`, `gate_msg: "passes minimum usability gate"`, `tier: 1`, `conforming: true`. Window 2021-09-09 to 2026-07-16 (4.85 years), W-THU to match the EGX trading week, Dimson-corrected, Blume cross-check 0.9271. Index as-of **2026-07-22**, quoted in §1.8 and in the bibliography. The record also carries what it replaced and prices it: `superseded_beta 1.0` ("assumed"), `delta_beta −0.1093`, worth −1.03pp of cost of equity on the market basis and −1.52pp on the rating basis. |
| 24 | SIGCM 7 — every constructed statement is a live formula model | **PASS, with the split reported** | 851 formulas against 916 pasted cells, 48% live. Per sheet: Income Statement 144 formulas / 28 pasted, Balance Sheet 133 / 49, Cash Flow **65 / 0**, Summary Financials **64 / 0**, DCF 65 / 6, SOTP Bridge **35 / 0**, Per-Share & Ratios **84 / 0**, Segments 113 / 49, Sensitivity 85 / 15. The driver → IS → BS → CF → DCF chain is live end to end. The 916 pasted are concentrated where they should be: `Assumptions` 192 (inputs by definition) and `Monte Carlo` 35 (whole-model re-runs). |
| 25 | SIGCM 8 — flag before issue; STOP AND INFORM where official data could not be obtained | **PASS — and it fired twice, correctly** | (i) **The terminal useful life.** `useful_lives.json` and rebuild lever L3 record that both admissible routes were run and both failed — the accounting-policy note discloses rate RANGES (2%–33%, a 3-to-50-year span) and the identity route returns 10.9–17.1 years depending on an undisclosed land split, with per-class rates contradicting the disclosed bands. **A life this desk chose is not a disclosed life, so none was chosen**, the terminal is carried on the construction the study describes, and the refusal is printed in §1.2 and §7 and called "the largest single piece of unfinished business in this study". (ii) **The regional revenue split** for Iraq and Jordan is not disclosed for the reviewed half; the exposure is NAMED in §1.7 and §5 and NOT priced. Both are also in the bibliography's negative-results table with what was sought, why, what was found and what the study did. |
| 26 | Sweep Register — primary-source depth, run through the shared module | **OUTSTANDING, ratcheted — and the facts are held where no checker can point at them** | `python3 scripts/check_sweep_module.py` → **`GBCO no sweep script in the study directory`**; on `sweep_outstanding.json`. There is no `sweep.py`, no `sweep_register.json` and no import of `engine/research_sweep.py`, so none of its eight invariants ever ran. **What the register would have found, verified here by hand:** company site attempted and **logged either way** — the earlier six-route failure and the current first-attempt success at `ir.gb-corporation.com`, both recorded in the bibliography and in `engine/escalations.json` ✓; **six audited fiscal years from the filing itself** against a floor of two and a target of four (GB Auto FY2020–22, GB Corp FY2023–25), plus twelve annual reports reproducing earlier audited statements ✓; **every study-year quarter already disclosed swept in before the build** — 1Q26 (reviewed statements + release + investor presentation) and 2Q/1H26 (reviewed statements + release), both ✓; **results releases swept with their statements** — nine releases beside six statement sets ✓; **≥1 COMPANY_IR source tagged distinctly** — 11 of the 19 registered inputs carry `layer: "Company (investor relations)"` and the investor presentation is a separate document row ✓. Every fact the invariants look for exists; not one of them is in a form a checker can test. That is precisely the failure the shared-register rule was adopted on. |
| 27 | Cost stack — one escalator per driver class, and a sourced near-term cost anchor | **FAIL on the escalators; PASS on the anchor** | **There is no cost stack.** `compute.py:210-215`: the entire cost side of the auto leg is `gpm = [0.138, 0.142, 0.145, 0.145, 0.145]` — a gross-margin path — plus `gsa`, `oth`, `prov` and `dna_pct` as ratios of revenue; the lender's is `cap_gm = [0.188, 0.190, 0.192, 0.194, 0.196]`. No physically distinct cost line (imported content, local assembly, freight, wages) carries its own driver, so the "one escalator per driver class" requirement cannot be met and is not — though it also cannot be breached by a single blended index, because there is no index either. **The near-term anchor is sourced and foots:** `forecast_anchor` anchors on GB Auto's own segment gross margin for the filed half — revenue 40,021.5, gross profit 5,722.1, **14.30%** — from the 2Q/1H26 release of 13 August 2026, Table 11, with the segment table asserted to foot (39,627.0 + 8,847.4 = 48,474.4 revenue; 5,722.1 + 1,762.2 − 62.4 = 7,421.9 gross profit). |
| 28 | Margins are OUTPUTS wherever the filings disclose enough to build cost per unit | **PASS on the arithmetic; the gap is disclosure-bounded and is named** | The margin is an INPUT on both legs (row 27). Whether that is a fail turns on whether the filings disclose enough to build cost per unit, and they do not: GB Corp publishes GB Auto's revenue and gross profit **in aggregate for the leg**, never a cost of sales per product line and never a cost per vehicle, so a per-unit cost build stops at the leg. The driver record says exactly this for every unit line — `cost_basis: "the auto leg's gross margin, held at the level the latest reviewed half filed"`. The revenue side IS built as volume × price on disclosed units for 74.4% of revenue. **The gap the record does not carry:** the three unit lines are marked `level: "unit"` with `gap_note: null`, and `assert_ground_up()` accepts that because it tests the naming of the unit, its source and the price basis — the cost side of a unit line is outside what it inspects. So a real gap on the cost side is invisible to the assertion that passed. |
| 29 | 3 years historical + 5 years forecast, with the FULL FCFF waterfall shown inline to PV of FCFF | **PASS** | Appendix A.2 carries FY2023 / FY2024 / FY2025 **as reported**, each footed against its own arithmetic and each naming its source document and date. A.3 carries FY2026E–FY2030E. The waterfall is shown inline in §1.2 and again in A.4 and on the `DCF` sheet, every line and **it reaches present value**: Auto revenue → operating profit before depreciation → less depreciation and amortisation → operating profit → operating profit after tax at 28% → plus depreciation and amortisation → less capital expenditure → less increase in working capital → **free cash flow to the firm** → discount factor → **present value of free cash flow**, then Σ PV of explicit 6,915 → terminal 80,285 → PV of terminal 35,297 → enterprise value 42,212 → less net debt 14,624 → less non-controlling interests 591 → Auto equity value 26,998. It does not stop at FCFF. `python3 scripts/check_waterfall_assertions.py` → `GBCO OK, 11 waterfall table(s)` — the builder calls `table_residual.waterfall()`, so every instructing table was asserted to reach its own printed answer at build time. |
| 30 | Guidance is scored, never consumed | **FAIL — priced, and the case is stronger after this rebuild than before it** | `docx_A.py:411`, and the delivered §1.2, read: *"capital expenditure follows the EGP 3,000 mn **guided** for the first year and then normalises"*. `compute.py:213` `capex = [3000, 2400, 2500, 2600, 2800]`. The standing clause is "GUIDANCE IS SCORED AND NEVER CONSUMED". **Measured here rather than asserted.** Against the company's own filed FY2025 — group capital expenditure EGP 3,664.2mn — the intensity is **5.52%** of auto revenue (4.57% of group revenue). The forward ladder runs **3.82 · 2.51 · 2.22 · 2.02 · 1.93%** of auto revenue: it opens a third below the filed year and **falls by two thirds across the window**, unsourced, raising free cash flow every year. Holding it at the filed intensity, every other driver untouched, takes the present value of explicit free cash flow from **+6,915 to −2,830** (5.52% auto base) or **to +248** (4.57% group base) — **EGP −8.98 or −6.14 a share on BOTH branches**, 14.9% to 21.7% of the lower one. Two honest mitigations: capital expenditure still runs **3.5× down to 1.8× depreciation** across the window, so it stays above replacement; and the terminal is 84% of the auto leg, so the explicit window carries about a sixth of it. **What this rebuild changed is the compounding defect, and it made it worse.** The rebuild added a **risk register** (row 57) that prices nine risks straight from the committed contested-judgement record. Capital expenditure is not among them. So the assumption now sits outside **three** registers — no four-field input entry (row 6), none of the seven contested judgements, and no risk-register row — while an instrument built to catch exactly this was added beside it. On my measurement it would rank **second** in that register, above the cost of capital (6.8%), the lender's return (5.2%) and the margin path (3.2%), behind only working capital (64.5%). The phrase "capital expenditure" occurs **once in the entire 33-page document**: the sentence that calls it guided. |
| 31 | v2 cost of capital — country risk once, marginal Kd above the sovereign, market-value weights, both ERP bases | **PASS** | `python3 scripts/check_cost_of_capital.py` → `GBCO EG, 22.88% gliding to 14.18%`, no new violation. Inspecting the record: **rf normalised by Egypt's OWN default spread** — rf 23.00% − 3.41% = **rf\* 19.59%**, and the premium adds the country risk back exactly once. **Both ERP bases published** — market/credit-default-swap basis 9.41% named CENTRAL (`erp_basis: "market"`), rating basis 13.94% published beside it in §1.8 and priced in the judgements table at 6.8%. **Kd marginal and above its sovereign** — 26.53% against a 23.00% sovereign yield, and computed independently over two periods with its denominator described: interest per audited note 7 **plus** GB Capital's cost of funds (which GB Corp books inside that segment's cost of revenue, not in the group finance line) over **average interest-bearing borrowings only**, customer balances, trade payables and lease liabilities excluded — FY2024 29.06%, FY2025 26.53%, adopted at the latest, `within_tolerance_bp: 0.0` against the 150bp bound and below the 29.06% peak. **Market-value weights** — 42.55% / 57.45% on market capitalisation against disclosed borrowings, not book. **The schedule is a glide** — five forward rates 22.88 / 19.77 / 17.28 / 15.42 / 14.18%, fractions inherited from the policy-rate path, terminal norm-built, and the terminal brought home on the last explicit year's own factor (0.43965 in both places). |
| 32 | [R-COC-02] — Ke reproduces from its own committed inputs under a NAMED construction | **PASS** | `python3 scripts/check_ke_reproduction.py` → `cost-of-capital records read: 8 (… GBCO …)`, `OK — no new breach`; GBCO is not on `ke_outstanding.json`. The record declares `ke_terminal_construction: "same_beta"`, and the arithmetic reproduces: explicit 0.1959 + 0.8907 × 0.0941 = **0.27971** = `ke_exp`; terminal 0.1250 + 0.8907 × 0.0700 = **0.18735** = `ke_terminal`. No relevering, so no tax rate is owed. |
| 33 | [R-MACRO-01] — one house macro path, no study-local inflation number, inflation_inputs declared | **OUTSTANDING, ratcheted** | `python3 scripts/check_macro_coherence.py` → **`GBCO MacroPathError: '' is not a covered market`** — the committed record does not expose the market key the assertion reads, so the study is held on `macro_outstanding.json` and its coherence has never actually been tested from outside. **What the record itself holds, read by hand and consistent:** `macro.path = "EG"`, `path_asof 2026-09-02`, terminal inflation 7.00% from `engine/macro_paths/EG.json`, terminal growth stored as **real 0.0%** and recomputed to **7.00% nominal** — not typed. `inflation_inputs` IS declared, with one mapped entry (`terminal_growth`, mapping `terminal_flat`) and a note explaining the near-emptiness: the auto leg is units × ASP on company-disclosed volumes, so no domestic CPI series escalates any line. `anchor_staleness_accepted` is declared with a reason and is not empty: the path's sovereign quote and currency spot are both anchored 2026-08-06 against a strike of 2026-09-03, 28 days, disclosed in §1.8 as an italic note. `python3 scripts/check_macro_anchor_age.py` → no new breach. |
| 34 | [R-ANCHOR-01] — the forecast is anchored on the latest reviewed period; a material decline names its mechanism | **PASS, and the record is printed whether or not it fires** | `python3 scripts/check_forecast_anchor.py` → `GBCO GB Auto gross margin, latest 0.1430, forecast 0.1380, −0.50pp`. Relative gap **−3.5%**, inside the 5% materiality line, so no mechanism is owed and none is claimed. The path then RISES (13.80 → 14.20 → 14.50 → 14.50 → 14.50), so clause two does not fire either; the rise from the opening year is +5.1% relative. The record names its own supersession honestly: the previous edition anchored on a 12.4% 1Q26 margin the study asserted and held no document for, in a directory that carried no filings at all before this run. |
| 35 | [R-LENS-03] — one class primary IS the answer; the others are cross-checks; no weighted blend | **PASS** | `python3 scripts/check_lens_design.py` → conforming, `two-sided primary sotp, 2 cross-checks`. The blend is gone and its removal is recorded as rebuild lever L5 with the retired weights named (0.40 / 0.15 / 0.20 / 0.25, two of the four lenses being the same sum of the parts at two discount levels), worth +20.7%. **Cross-checks, published beside the answer and weighted into nothing:** relative multiple **EGP 19.29** on FY2026E earnings per share of 3.04 × **6.350×**, the MEDIAN of GB Corp's own trailing price-to-earnings at its last three year-end closes (4.535× / 6.350× / 10.177×) — **non-circular by construction, and the traded multiple on the same forward earnings, 9.540×, is committed beside it** so a reader can divide the claim rather than believe it, with the count of three published because a median of three is thin; and **disclosed book value EGP 30.82** (shareholders' equity before minority interests 33,454.3 ÷ 1,085.5 shares) published as a FLOOR and never weighted, against which the shares trade −6%. The envelope is the RANGE of the present-value reads, 19.29–52.35, and the book floor is explicitly excluded from it. `python3 scripts/check_lens_vocabulary.py` lists GBCO under **`clean`** — no delivered document prints the retired blend. |
| 36 | [R-BRIDGE-01] — the enterprise-to-equity bridge as a checked record | **OUTSTANDING, ratcheted** | `python3 scripts/check_bridge.py` → **`GBCO carries no bridge record`**; on `bridge_outstanding.json`, one of 15. So none of the construction clauses — which sheet, the minority basis, cash charged once, the lines footing to the stated equity value — has ever been tested from outside on this study. **What the document does show, read by hand:** the bridge stands on the **latest disclosed balance sheet**, the reviewed statements to 30 June 2026 and not the prior year end (rebuild lever L4, +0.9%), with A.5 tabulating cash 10,951.5, group borrowings 42,476.0, GB Auto segment borrowings 22,733.1, group total equity 35,127.9, each dated 30 June 2026 and each naming where it was read; the deduction is the **AUTO leg's** own net debt (14,623.7) and the **AUTO leg's** own non-controlling interest (590.7), because the lender's borrowings are its raw material rather than its leverage; and the printed lines foot to the printed equity value — asserted at build time by `table_residual.waterfall()` (row 29). Sound on inspection, unrecorded as a record. |
| 37 | [R-TERM-01] — the terminal built on a disclosed asset life, through the sanctioned module | **OUTSTANDING, ratcheted — and the refusal is the right one** | `python3 scripts/check_terminal_floor.py` → **`GBCO no nopat_term, no ic`**, listed under `unscored` in `terminal_outstanding.json`: the terminal resolves but its charge does not, so the 1/g test has never run on it. The ratchet's stated reason is now partly stale — it says "This study additionally does not RUN — compute.py passes rf= to a v2 WaccInputs"; it runs, and `python3 scripts/check_record_survives_rebuild.py` → `GBCO compute.py survives`. **The substantive position is honest and is row 25's stop:** no disclosed life could be sourced, so `engine/terminal_value.py` was NOT used and the study says so in §1.2 and §7. Terminal growth is nevertheless stored as a real rate on the house path and recomputed (7.00% nominal, replacing a typed 11.5%, rebuild lever L2, worth −40.4%), and the terminal is **84%** of the auto leg's enterprise value (`dcf.tv_pct = 0.836`), stated in the bridge, in §1.9 and in §7. `check_terminal_spread.py`, `check_terminal_basis.py` and `check_terminal_record_shape.py` → all clean. |
| 38 | [R-ENF-05] — the reverse read outside the numbers file, and the sign test | **PASS on the structure; the sign test's SET is qualified by row 30** | `python3 scripts/check_output_records.py` → `GBCO price implies the whole-company valuation of MNT-Halan of 116.76 against the study's 1400.00; 7 judgements, 4 material, sign test p=0.62`. The reverse read lives in `diagnostics.json`, is computed by `diagnostics_gbco.py`, and **no builder reads it** — the file's own `why_this_file` states the prohibition and the script asserts the committed numbers file is byte-identical before and after it runs. It is expressed in the crux's own observable unit: the price implies MNT-Halan is worth USD 117mn as a whole against USD 796mn at carrying value and USD 1,400mn at the round — 8.3% of the round, 14.7% of the carrying value. §1.7 also states the same gap read as a rate: the price implies 31.28% cost of capital on the round branch and **25.71%** on the carrying branch against the study's 22.88%. Sign test: 4 material judgements, 3 up and 1 down, **p = 0.625**, not flagged — and the record states in terms that the one fork the study does NOT resolve is deliberately outside the test. **The qualification:** the capital-expenditure path (row 30) clears the materiality line by a wide margin and is absent from the seven, so the set the test runs on is incomplete in the value-raising direction. |
| 39 | [R-ENF-06] — every artefact a builder reads declares the answer it was built against | **PASS** | `python3 scripts/check_artefact_currency.py` → `GBCO contested_judgements.json built against 41.3484` and `GBCO diagnostics.json built against 52.3453`, no violation. Both additionally declare `published_central_two_sided` carrying **both** branches and a note explaining that the study publishes no central, so the currency check holds each file to an answer the study actually publishes rather than to an average of two it does not. |
| 40 | [R-REBUILD-01] — the route recorded, not only the destination | **PASS** | `python3 scripts/check_rebuild_ledger.py` → `GBCO 9 lever(s), 8 rule(s), cumulative +46.4%`, and the ledger walks: 35.7468 → 73.7116 (L1 cost of capital, +106.2%) → 43.9555 (L2 terminal growth as a real rate, −40.4%) → 43.9555 (L3 the sanctioned terminal NOT APPLIED, 0.0%, with its refusal recorded) → 44.3295 (L4 the bridge onto the reviewed sheet) → 53.5259 (L5 the blend retired) → 59.4732 (L6 the typed conglomerate discount removed) → … → the published branch. Two audit points were declared **in advance** (after L4 and again after L9) and the ledger says why: the running total is read once the rule-driven corrections have landed and **before** the price is consulted. L1 and L2 pull hard in opposite directions — read as nine levers this is a landslide; read as rules in contest it is a contest, which is what it was. |
| 41 | [R-FCAL-01] — the fundamental walk-forward, scope decided first and stated | **PASS** | `python3 scripts/check_walkforward_scope.py` → `GBCO FULL, 14 years, run`, with GBCO no longer on that ratchet. `engine/gbco_walkforward/` holds the pre-registration, the training record, the panel, and the scored output: `scores.json` → **origins FY2016–FY2024 (nine), horizons 1–5, cells 225**, `errors.json` carrying 245 rows, 7 drivers. **No correction was promoted** — `check_walkforward_actuation.py` → `GBCO 7 drivers -> adopt 0, watch 5, none 2`, and `check_corrections_applied.py` → `GBCO declares NO correction adopted`. The finance-cost candidate is named rather than buried: a large bias whose sign flips at four of six admissible cuts, and the exact trap the rule names, where a finance charge over a broader liabilities total manufactures a bias that looks like evidence (7.05% over total liabilities, 11.27% over group borrowings, 26.53% over the interest actually incurred). `check_valuation_inputs.py` → GBCO conforming: the valuation-input block is committed per origin. `check_driver_coverage.py` and `check_frozen_escalator.py` → no GBCO finding. |
| 42 | Far forecast years published as RANGES with their basis, orientation and count | **PASS** | `python3 scripts/check_forward_ranges.py` → `GBCO prints a far-year range, shape A`. Appendix A.1 carries FY2028/29/30 as Low / Point / High for four aggregates with a **Basis** column reading `span` and an **Observations** column printed beside every band (7 / 6 / 5 on revenue, gross profit and operating profit; 3 / 2 / 1 on net profit). The orientation is declared — "stated as the ratio of what the company REPORTED to what the method FORECAST, so a figure above one is a year the method came in low" — and the bibliography's derived-figures table repeats it in capitals. The text then says at each record length what a span of that many readings can honestly promise, tested: a span of 5 contained the outcome 51 times in 100 against 43 resolved readings, a span of 6 fifty times against 30, a span of 7 forty-eight times against 21 — "narrower than a confidence interval rather than wider". The one-observation net-profit band is called out in its own paragraph as not a span at all. |
| 43 | The crux sensitised in real observable units | **PASS** | §1.9 prices the crux on its own axes and nothing else: the MNT-Halan mark in EGP million (6,918 / 13,835 / 15,734‡ / 20,753 / 27,671‡) against the first-year cost of capital (20.9% → 24.9%), spanning EGP 23.7 to 69.4 per share, with the two published branches marked ‡ and the two published answers in bold. A second grid holds the associate at the round and moves the rate path against terminal growth. §1.1's mark ladder does the same in a third unit — the mark, the implied whole-company valuation in USD, and the sum of the parts at each rung — and its top row reproduces the traded price exactly, which is the check that the ladder is arithmetic rather than illustration. `recalc.py` asserts both grids reach their own branch to six decimals. |
| 44 | Every non-sourced driver logged to `engine/Fundamental_Driver_Ledger.md` | **FAIL** | The ledger's GBCO section (line 840) is a **walk-forward** entry: it logs the mechanical rule and outcome for the seven scored drivers, the corrections declined, the macro split and one not-built driver. **It logs none of the study's own non-sourced forward drivers** — the working-capital intensity glide (five typed values, worth 64.5% of the answer, row 21), **the capital-expenditure path (five typed values, worth EGP 6.14–8.98 a share, row 30)**, GB Capital's revenue growth and margin paths, the group operating-expense and other-income ratios, the associate-income path, the finance-cost path, the minority ratio, the 20% margin on eliminations, and the passenger-car volume and average-selling-price growth rates. Two of them ARE priced with both framings in `contested_judgements.json`; the capex path is in neither that record nor the ledger. |
| 45 | Script-built files verified by a cell-by-cell diff against the DELIVERED file | **PASS** | `recalc.py` opens the delivered workbook **by name** — `GBCO_Valuation_Model_07092026_public.xlsx`, the 07-09-2026 edition, not the superseded `GBCO_Valuation_Model_08072026_public.xlsx` that still sits beside it — and evaluates every formula cell against the model: **851 of 851**, cell by cell, 0 unresolvable, 0 unchecked, plus 37 headline reconciliations naming the quantity each time. Re-run on the rebuilt file for this edition of the gate. A clean recalculation alone would not be sufficient and is not what this row rests on; the diff is per cell. |
| 46 | **THE ANSWER** — the branches against the latest known price, and the gap review [R-GAP-01] | **PASS** | `python3 scripts/check_valuation_gap.py` → `GBCO +42.7% GAP_REVIEW_07-09-2026.md (audits 41.3484)`, `23 of 23 readable studies compared`, `OK — no new violations`. **Both branches sit ABOVE the price: EGP 41.3484 = +42.7% and EGP 52.3453 = +80.6% against EGP 28.98 of 3 September 2026.** Neither moved in the rebuild. The trigger is two-sided, so a review is owed and is delivered: `GAP_REVIEW_07-09-2026.md`, 301 lines, **all eight headings present and none a stub** — LATEST FILINGS (l.43) · BASE YEAR (74) · MACRO COHERENCE (93) · DISCOUNT RATE (115) · TERMINAL (148) · BALANCE SHEET (175) · CLAIMS AGAINST THE RECORD (187) · MULTIPLE CROSS-CHECK (206) — plus a verdict. It carries the AUDITED CENTRAL marker for **both** branches and the AUDITED GAP for both, and states the rule it invented for a two-sided answer: every branch is audited. The standard the study is stamped with, 2026.09.07, is the live `research_protocol.STANDARD_VERSION`, which names this artefact. |
| 47 | [R-GAP-02] — the publish block | **HELD, correctly, and for a reason that is not this study's** | `python3 scripts/check_publish_block.py` → `HELD GBCO inside the band at +80.6% of 28.98 (2026-09-03), but Phase 1 is not proven — 2 of 6 acceptance criteria open`. The deviation half does **not** hold this study: [R-GAP-02] is one-sided and fires only where a branch sits more than 10% BELOW the price, and every branch here is above it. What holds it is clause three, the method proof, which holds the whole book. No `MARKET_DISSENT` is owed and none is filed. |
| 48 | [R-ENF-02] — the three standing gates called in the study's OWN committed code | **PARTIAL — two of three are absent** | `python3 scripts/check_study_provenance.py` → GBCO does **not** appear in the outstanding list, because that gate fails a study calling *none* of the three and GBCO calls some. Grepping the directory: `beta_reissue.py:59` calls **`assert_beta_provenance(rec)`** ✓; `compute.py:823` calls **`assert_ground_up(_DL,'GBCO')`** ✓; `compute.py:890` calls **`assert_lens_design(_LENS_RECORD,'GBCO')`** ✓. **`assert_sigcm()` and `assert_model_study()` appear nowhere in `engine/gbco_study/` — not in any `.py`, not as `SIGCMChecklist` or `ModelStudyChecklist`.** Unchanged by the rebuild. |
| 49 | Delivered-document vocabulary, by shape | **PASS** | `python3 scripts/check_delivered_vocabulary.py` → GBCO under `CLEAN (22)`: no standing-rule identifier and no repository path in any delivered document. (This is the shape gate, and it does not substitute for row 10.) |
| 50 | Table totals, sign convention, and figures against the model | **PASS** | `python3 scripts/check_table_footing.py` → `GBCO tables examined: 46; unreconciled totals: 0`. `python3 scripts/check_sign_convention.py` → 46 documents, 102 tables carrying a deduction row, **0 findings** — GBCO prints deductions as magnitudes in brackets with the operation in the label, one convention throughout, and says so in two captions. `python3 scripts/check_prose_figures.py` → 270 figures, 0 unmatched. |
| 51 | Reported earnings per share reconciled against attributable profit | **OUTSTANDING, ratcheted** | `python3 scripts/check_eps_reconciliation.py` → **`GBCO registers no reported earnings per share beside its attributable profit, so the gap cannot be seen`**; listed under `unreadable` in `eps_outstanding.json`. Appendix A.2 prints EPS of 1.74 / 2.70 / 2.65 for FY2023–25 and A.3 prints 3.04 → 10.33 forward, all computed on 1,085.5 shares; **the company's own reported EPS is registered nowhere**, so no comparison against the figure GB Corp itself publishes is possible from the record. |
| 52 | **THE RENDERED PDFs READ AS IMAGES** — both documents, after this rebuild | **DONE — and it is what verified rows 2, 8, 11, 56 and 57** | `GBCO_Valuation_Study_07-09-2026_public.pdf`, **33 pages, re-read as rendered images** after the 01:19 rebuild; `GBCO_Bibliography_07-09-2026.pdf`, **11 pages, re-read**. This pass confirmed by eye what the greps implied: §3's new "How often these bands have actually held" subsection carries one sample and one set of figures; §6's zone paragraph now ends "That is evidence about the WIDTH of the cone and says nothing about the direction of its median"; Figures 5 and 6 read "latest known price 28.98"; Appendix B is the peer frame, a new risk register and a new research register; Appendix C is the expert panel at C.1–C.6; no page carries a calibration table, a Figure B-1 or an Appendix D reference. It also confirmed the two rows that did NOT move: the bibliography's six leaked terms are on the same lines as before, and its judgements table is still introduced by a sentence describing a column it does not have (row 5). Minor and unchanged: the bibliography's last page is blank but for its footer. |
| 53 | **The committed cone and the document's account of it** | **PASS — the previous edition's FAIL is CLOSED, and in the direction the evidence pointed** | The previous edition recorded that the document said four times that the cone was struck on EGP 29.51 while every path started at 28.98. **The code was the deviation and the code was corrected.** Verified at source and in the record, not accepted: `compute.py:133-134` now reads `paths[:,0] = mc_anchor` / `paths[:,1:] = mc_anchor*np.exp(logp)`, followed by an assertion that **refuses any other start** — `assert abs(paths[0,0]-mc_anchor) < 1e-12 and paths[:,0].min() == paths[:,0].max() == mc_anchor`, with the comment "nothing about a wrong anchor looks wrong: every percentile is a plausible price and the only witness is the number the paths started on". In the committed record **all five percentile paths in `mc.fan` now begin at 29.51**. The consequences reconcile: the §3 probability table's "50% band EGP 26.80 – 35.18 (−9% / +19% of the anchor)" and "Median EGP 30.69 (+4.0%)" now solve to an implied base of **29.5100** exactly, where they solved to 28.98 before; the level-touch ladder's distance column was always against 29.51 and its caption — "measured against the session the cone was struck on" — is now **true**; and §3's opening sentence, the p.1 anchor paragraph, §6 and the Disclosure are all true of the arithmetic. **The move is a pure rescale and was checked as one:** every three-month percentile is the old one times 29.51/28.98 to fourteen decimal places (21.00 / 26.80 / 30.69 / 35.18 / 44.89), and the scale-invariant statistics are byte-identical (probability above the anchor 58.1%, touch ±10% 64.8% / 48.9%). **Neither branch moved.** One independent corroboration that the new anchor is the real one: the ladder's one-month touch probabilities are now 81% for EGP 30 (+1.7% away) against 53% for EGP 28 (−5.1% away) — the nearer level touched far more often — where the old pair, 70% against 63%, was the incoherence that first exposed the defect. |
| 54 | **What do the filings disclose that the model does not consume?** — asked explicitly, against the sweep | **ANSWERED, with six items named** | **(1) GB Capital's revenue by company.** Table 13 of every release breaks the lending leg into GB Lease, Drive, GB Auto Rental, GBBR, Capital Securitization and Kredit; the model consumes the total on one growth path. The driver record names this itself. **(2) GB Capital's cost of funds, disclosed separately from its cost of sales.** The model reads it for the cost-of-debt derivation (FY2025 EGP 3,756.9mn) and then forecasts the segment on a typed blended margin path, so a disclosed cost line is consumed for the discount rate and not for the forecast. **(3) The working-capital components.** Receivables, inventory and payables are all disclosed and none is separated (row 21). **(4) The disclosed capital-expenditure history.** FY2025's EGP 3,664.2mn is registered with four fields and is consumed by nothing — the forward path is guidance, not a build off the filed intensity (row 30). This is the item this question was invented to find. **(5) Commercial-vehicle and light-mobility volumes for FY2023 and FY2024.** The unit build reaches back three years on passenger cars and one year on the other two lines; the annual-report business reviews carry earlier volume disclosure the study does not reconstruct. **(6) The disclosed useful-life rate ranges** in the accounting-policy note: read, tested by both admissible routes, and correctly refused (row 25) — the one item where NOT consuming is the right answer. |
| 55 | Live ratchet inventory — re-read from disk for this edition | **5 LIVE · 6 STALE · 2 PRUNED EARLIER** | Re-read from `engine/build_depth_audit/*.json` on the current tree, not carried forward. **Live:** `bridge_outstanding` (no bridge record — row 36) · `eps_outstanding`/unreadable (row 51) · `macro_outstanding` (record exposes no market key — row 33) · `sweep_outstanding` (no sweep script — row 26) · `terminal_outstanding`/unscored (row 37). Plus `coc_state`, `queue` and `queue_raw`, which still describe the 08-07-2026 edition as needing a cost-of-capital rebuild since done. **Stale — the gate now passes GBCO while the list still names it, and this edition adds one to the count rather than removing any:** `document_outstanding` is **newly stale**, its gate printing `NOW PASSING — remove from the list (1): GBCO` · `pdf_outstanding` says "no delivered study PDF" and one ships (`CLEARED: GBCO`) · `prose_outstanding` says "no script reconciles the delivered documents' figures" while `prose_check.py` passes and its gate prints `NOW CARRYING ONE — remove from the list: GBCO` · `lens_vocabulary_outstanding` — GBCO is `clean` · `waterfall_outstanding` — GBCO is `OK, 11 waterfall table(s)` · `published_gap_outstanding` records "+21.0% (fair 35.7 vs spot 29.51)", both figures superseded. **Measured, not assumed: none of the five previously-flagged stale entries has been pruned**, and a sixth has joined them. **Pruned in an earlier rebuild, both stale when pruned:** `ground_up_outstanding` and `walkforward_scope_outstanding`. **The correction this row still owes:** the first edition of this gate reported the inventory from a `grep -l GBCO` over that directory and **under-reported by two** — `git show a7f2e94d:…` returns a match in each of those two files and the probe did not list them. The substantive rows were right, because they rested on running the gates rather than reading the lists; the inventory was not. Recorded rather than silently corrected, because a probe that comes back short reads exactly like a finding. |
| 56 | Defects introduced by the previous calibration fix | **PASS — both CLOSED** | **(a) The two-coverage-figures defect is gone with the appendix.** The previous edition recorded that Appendix B published 93% on 56 windows in its table and 95% on 57 forecasts in its prose, three sentences apart, with nothing saying they were different samples — the defect [R-CAL-02] was written to close. The 56-window replay no longer reaches a reader at all (row 8), so **one sample and one set of figures survive**: 57 / 95% / 60% / 1.42×, in §3, in §6 and in §7's drift bullet, identical in all three and all reconciling to `BANDS.GBCO`. **(b) The dangling cross-reference is rewritten, not deleted.** §6 previously closed "Read that with Appendix B open … that appendix states the margin rather than flattering it", pointing at a quantity the same rebuild had removed. It now reads: "Read that against the band record earlier in §3: over 57 resolved three-month forecasts this cone's 90% band has contained the close 95% of the time against a 90% target. **That is evidence about the WIDTH of the cone and says nothing about the direction of its median.**" `states the margin` and `Appendix B open` both return **0** occurrences. §7's drift bullet took the same correction independently and now points at §3 with the same width-not-direction sentence. |
| 57 | **What this rebuild ADDED** — the risk register and the research register | **PASS — new, and both reconcile to the committed record** | Neither existed in any earlier edition of this study, and the model report requires both. **B.2 Risk register:** nine risks, each either PRICED or NAMED AND NOT PRICED, with the register stating its own rule — "Nothing is left as an adjective." Every priced figure was checked against `contested_judgements.json` and reproduces: the associate basis at **EGP 11.00 a share, 21.0% of the higher branch** (52.3453 − 41.3484 = 10.9969; 10.9969/52.3453 = 21.0%) · working-capital intensity **64.5% of the lower branch, EGP 25.13 against EGP 41.35** (`value_alternative` 25.129582) · cost of capital **6.8% on the premium basis and 6.5% on the beta** · the lender's return **5.2%** · the Auto margin **3.2%**. Four are NAMED AND NOT PRICED with the reason given — the associate's unverifiable accounts, the terminal-value dependency on a life that could not be sourced, the Iraq/Jordan exposure, and the pound on the round branch only. **B.3 Research register:** the four research layers, a dated source table whose counts foot to the input register (8 Company + 11 Company-investor-relations = the 19 registered inputs), and **six negative results**, each a search that was actually run — the useful life, the associate's audited statements, the regional revenue split, the first beta attempt, the second closing's terms, and the two earliest fiscal years. **The one the rest of the gate turns on:** "No disclosed useful life for the operating asset base" is B.3's first entry and is exactly what §1.2's refusal now points at, so the pointer added in row 2 resolves to real content rather than to a heading. **What neither register carries is the capital-expenditure path** (row 30). |

---

## Gates run from outside for this edition

Re-run against the 33-page rebuild, not quoted from an earlier pass. All exit 0.

`check_document_structure` (and `--show GBCO` → `status: ok`) · `check_valuation_gap` ·
`check_publish_block` · `check_prose_figures` · `check_table_footing` · `check_workbook_values` ·
`check_workbook_structure` · `check_waterfall_assertions` · `check_sign_convention` ·
`check_figure_opacity` · `check_figure_axes` · `check_delivered_pdf_currency` ·
`check_band_vocabulary` · `check_lens_vocabulary` · `check_published_lens_vocabulary` ·
`check_delivered_vocabulary` · `check_calibration_deliverables` · `check_bibliography` ·
`check_four_field` · `check_ground_up` · `check_column_widths` · `check_edition_date` ·
`check_output_records` · `check_output_sanity` · `check_study_provenance` ·
`check_walkforward_scope` · `check_walkforward_actuation` · `check_valuation_inputs` ·
`check_eps_reconciliation` · `check_driver_coverage` · `check_standard_claim` ·
`check_numbers_generators` · `check_record_survives_rebuild` · `check_frozen_escalator` ·
`check_lens_independence` · `check_harness_outputs` · `check_corrections_applied` ·
`check_published_gap` · `check_terminal_floor` · `check_terminal_basis` · `check_terminal_spread` ·
`check_terminal_record_shape` · `check_bridge` · `check_bridge_reaches_answer` ·
`check_lens_design` · `check_cost_of_capital` · `check_ke_reproduction` · `check_macro_coherence` ·
`check_macro_anchor_age` · `check_forecast_anchor` · `check_anchor_ordering` · `check_asset_base` ·
`check_asset_base_wired` · `check_source_integrity` · `check_source_rebinding` ·
`check_sweep_module` · `check_forward_ranges` · `check_rebuild_ledger` · `check_artefact_currency` ·
`check_exemplar_debt` · `check_escalations` · `check_protocol_sync`

**No gate came back RED.** Every remaining GBCO finding is either a ratcheted entry the gate is
designed to allow, or a defect no gate in this repository inspects — which is why the documents
are read. The three blocking rows are all of the second kind: no gate reads a per-study
vocabulary list, none asks whether a working-capital ratio was built from disclosed days, and
none asks whether a driver that moves the answer by a fifth was ever written into a register.

Plus the study's own instruments, re-run against the REBUILT files:
`recalc.py` (851/851 + 37 reconciliations), `footing_check.py` (46 tables, 0 unreconciled),
`prose_check.py` (270 figures, 0 unmatched).

---

## The claims this gate refused to take on trust

**The beta was inspected on its record, never on a boolean.** `conforming: true` was ignored.
What was read instead: `index_file` is `raw_indices/EG/EGX30.csv`, under `raw_indices/` and not a
constituent composite; the exchange was resolved from the `EGX:` prefix in `assets/data.js` and not
from the `raw_ohlc/EG/` folder; the usability gate was recomputed from the record's own n, R² and
standard error and clears on all three, so no fallback was owed; no interim note applies because
EGX30 is registered for (EG, EGX); and the number it replaced — an assumed 1.0 — is committed
beside it with what the change was worth.

**The ground-up clause was inspected on the driver record, never on a flag.** The share-by-level
split is reported in row 19 rather than ticked: unit 74.42%, segment 26.70%, topdown −1.11%,
summing to exactly 1.000000. Row 28 records what `assert_ground_up()` structurally cannot see —
that every unit line's COST side is a leg-level margin, so a passing assertion coexists with a real
cost-side gap.

**Every closure was re-derived, and one probe of mine had to be re-run before it could be
believed.** The appendix deletion was confirmed by searching the delivered text for each figure
the replay carried, and three `93%` hits and one each of `0.57` and `0.59` came back — all four
read in context and all four false positives of a substring probe (the ownership percentage
42.93%, a discount factor, a forward band). Reporting those as survivors would have been the
mirror image of the previous edition's own error, where a probe that came back short was believed.
An earlier pass on this study had already been caught that way twice: a line-broken
"carry- anchored" that defeated a grep, and a `grep -l` over the ratchet directory that
under-reported by two (row 55). **When a probe's result is the finding, the probe is the thing to
check first.**

---

## What a re-issue would need to close, in order of cost

1. Print the two-sided binomial beside the coverage figures (row 16): 90% band p = 0.372,
   50% band p = 0.185, neither flagged. One sentence.
2. Register the capital-expenditure path — as a four-field input, as a contested judgement with
   both framings, and as a row in the risk register that now sits beside it — and either source
   the first year or stop calling it guided (row 30). Registering it also completes the set the
   sign test runs on (row 38). This is a day's work and it is worth EGP 6.14–8.98 a share.
3. Write a per-study external-reader scrub and clear the six bibliography hits (row 10).
4. Give the bibliography's judgements table the what-would-overturn-it column its own
   introductory sentence promises, or fix the sentence (row 5).
5. Compute a peer figure — one multiple or one operating KPI, sourced and dated — or state in
   the study that none was obtainable and why (row 22).
6. Build the working-capital glide from disclosed receivable, inventory and payable days
   (row 21) — the largest piece of work on this list, and the one standing behind the largest
   priced risk in the study's own new register.
7. Log the study's own non-sourced forward drivers to the driver ledger (row 44).
8. Commit a `bridge_record`, expose the market key the macro assertion reads, register the
   company's own reported earnings per share, and add a sweep script through
   `engine/research_sweep.py` (rows 36, 33, 51, 26); call `assert_sigcm()` and
   `assert_model_study()` in the study's own code (row 48).
9. Prune the six stale ratchet entries (row 55) — not this study's work to do, but countable.
