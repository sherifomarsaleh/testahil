# GBCO — QC gate, 07-09-2026

GB Corp S.A.E. · Egyptian Exchange: GBCO · EGP · edition of 07-09-2026

Every row names the artefact, the command or the number that carries it. Nothing here is
self-certified. Where a row fails it says so plainly, and where it is excused by a
standing allowance the allowance is named rather than the failure being dressed as a pass.

**VERDICT: DELIVERABLE TO THE PRINCIPAL, NOT PUBLISHABLE.** The re-issue is complete and
self-consistent at one edition, and the workbook now reaches the same answer as the study
— which it did not before. Publication is blocked for a reason that has nothing to do
with the quality of this edition: the fair value sits 53.0% ABOVE the traded price, which
is the ordinary shape of finding something cheap and is NOT held by the one-sided publish
block, but the method-proof clause holds every study in the book until Phase 1 acceptance
closes. Six items below are OUTSTANDING with their reasons, every one of them a debt this
study already carried before this pass and none of them created by it.

---

## The edition audited, and how it was resolved

Every deliverable was resolved by the DATE IN ITS FILENAME, never by string sort. That
distinction is not pedantry here: `"08-07-2026"` sorts ABOVE `"07-09-2026"` as a string, so
a checker that sorts filenames reads the superseded July edition and reports its defects as
current. One repository gate does exactly that today and is named in the outstanding list.

| Deliverable | File | Also on disk |
|---|---|---|
| Study, Word | `GBCO_Valuation_Study_07-09-2026_public.docx` — 34 tables, 8 figures | 08-07-2026 |
| Study, PDF | `GBCO_Valuation_Study_07-09-2026_public.pdf` — 26 pages | — |
| Workbook | `GBCO_Valuation_Model_07092026_public.xlsx` — 16 sheets, 736 formula cells, 1,598 populated cells | 08072026 |
| Bibliography | `GBCO_Bibliography_07-09-2026.docx` / `.pdf` — 8 pages, 9 tables | — (this artefact did not exist before this pass) |
| Gap review | `GAP_REVIEW_07-09-2026.md` — audits central 44.3295 at +53.0% | — |

`python3 scripts/check_calibration_deliverables.py` resolves the same five and reports
GBCO complete and self-consistent at edition 2026-09-07.

## What this pass changed, and what it did not

**THE ANSWER DID NOT MOVE BY A BASIS POINT.** The committed central was 44.329496406267786
before this pass and is 44.329496406267786 after it. `compute.py` was re-run five times
during the pass and reproduced its own committed output byte-for-byte on the first run
before any edit; every subsequent change was ADDITIVE (fields the document needed to read
rather than type) or a correction to a TYPED SENTENCE that disagreed with the record beside
it. No lever was applied, no driver moved, and the rebuild ledger is unchanged at four
levers and +24.0%.

| Changed | Evidence |
|---|---|
| The document, rebuilt at edition 07-09-2026 from the current record | `build_docx.py` — a runner that did not exist; the three content parts shared one document object and nothing saved it, so the delivered edition could not be rebuilt from the study's own code |
| The workbook, rebuilt at the same edition | `build_xlsx{,2,3,4}.py`; verified below |
| The bibliography, built for the first time | `docx_biblio.py` |
| The far-year ranges, published for the first time | Appendix A.1, Table A0 |
| Figures, rebuilt opaque and on the exchange library | `figures.py` |
| Five committed records corrected where a TYPED sentence contradicted the record beside it | listed under "What was found while doing this" |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | Structure matches the model report — 16-section Word, 16-sheet Excel, standalone bibliography | **PASS** | `python3 scripts/check_workbook_structure.py` → GBCO in "MATCHES THE MODEL REPORT (22)" and **"NOW PASSING — remove from the list (1): GBCO"**. The workbook's fifth sheet was named `SOTP` against the model's `SOTP Bridge`; it is renamed and every cross-sheet reference moved with it. `check_bibliography.py` → **"RATCHET ENTRIES NOW CLEAN: GBCO"**. |
| 2 | Step 0.0 data-quality gate | **PASS** | `compute.py` loads the persistent library `raw_ohlc/EG/GBCO.csv` through `primitives.load_ohlc`, the same reader every cone in this book is struck on. The study-local price extract stops at 7 July 2026 and is no longer read by anything: `figures.py` was still drawing its chart from it and now draws from the library, so the chart and the numbers are one series. |
| 3 | Step 0 calibration, published as a band record | **PASS, and thin — the document says so** | Appendix B prints coverage with the count beside every percentage: on 56 non-overlapping windows the 90% band held 92.9% against a 90% target, the 80% band 80.4%, the 50% band 55.4%, distribution centre 0.59. The proper score against the naive benchmark is **−1.8%** and Appendix B states that plainly rather than burying it: this cone does not beat a carry-anchored random walk on that score and the case for the drift rests on coverage and centring. |
| 4 | SIGCM clause 1 — historicals from official sources only | **PASS** | `python3 scripts/check_source_integrity.py` → GBCO not among the breaching or unreadable. All 19 committed inputs name a GB Corp document held under this study's own `src/` directory; `src/SOURCES.md` records the route each came by and that the four most recent audited statements carry NO text layer, so the annual report is the ROUTE and the audited statement is the DOCUMENT. |
| 5 | SIGCM clause 2 — ground-up forecast | **PASS on the auto leg, and the gap is flagged** | The passenger-car line is volume × price on disclosed units; the price is disclosed revenue divided by disclosed volume, which is arithmetic on two published figures. The other three auto lines are built at segment level. §1.6's driver table prints a DASH rather than a figure for the three lines the record does not carry for FY2023 and FY2024, and the caption says a dash marks a line this study did not source rather than a nil. |
| 6 | Beta produced by the sanctioned resolver and attested | **PASS** | `beta_result.json`: 0.8906822450333004, weekly, Dimson-corrected, 251 observations over 4.85 years to 2026-07-16, R² 0.243, SE 0.204, `conforming: true`, `index_file: raw_indices/EG/EGX30.csv`, `tier: 1`. It replaces the assumed 1.0 the superseded edition carried. |
| 7 | Cost of capital under the v2 method [R-COC-01] | **PASS** | `python3 scripts/check_cost_of_capital.py` → `GBCO EG, 22.88% gliding to 14.18%`. Country risk enters once: rf* = 23.00% − 3.41% = 19.59%. Market-value weights. Both premium bases published, market named central. |
| 8 | Ke reproduces from its own inputs [R-COC-02] | **PASS** | `python3 scripts/check_ke_reproduction.py` → GBCO among the 8 records read, no new breach. Reproduced by hand: 0.1959 + 0.8906822450333004 × 0.0941 = 0.279713199, against a committed `ke_exp` of 0.2797131992576336. Construction declared `same_beta`. |
| 9 | Macro coherence [R-MACRO-01] | **OUTSTANDING, ratcheted** | `python3 scripts/check_macro_coherence.py` → `GBCO MacroPathError: '' is not a covered market`. The study commits no `market` field in the shape that gate reads. It DOES commit the path (`macro.path: EG`), the terminal inflation, and a terminal growth stored as (real 0.0%, path EG) that recomputes to 7.00% nominal — the substance of the rule is met and the RECORD SHAPE is not. On the ratchet; not fixed in this pass because the fix is a record-shape change that belongs with the macro record rather than with a document re-issue. |
| 10 | The enterprise-to-equity bridge [R-BRIDGE-01] | **OUTSTANDING, ratcheted** | `python3 scripts/check_bridge.py` → `GBCO carries no bridge record`. The bridge itself is correct and stands on the LATEST disclosed sheet: auto net debt 14,623.7 and auto minority 590.7, both at 30 June 2026, printed in §1.2 and in Appendix A.5 with their source and date. What is missing is the machine-readable `bridge_record` the gate reads. Substance met, record absent. |
| 11 | The terminal on a disclosed asset life [R-TERM-01] | **REFUSED, and the refusal is the finding** | `useful_lives.json`: both admissible routes were run and both failed — the policy note discloses RATE RANGES only (a 3-to-50-year span) and the derived identity returns 10.9–17.1 years depending on an undisclosed land split, with per-class rates contradicting the disclosed bands. A life this desk chose is not a disclosed life, so none was chosen. Printed in §1.2 as a third honesty note and in §7 as the largest single piece of unfinished business. `check_terminal_floor.py` → no new breach. |
| 12 | Numeric traceability — no financial numeral typed into a builder | **PASS on the study document; the workbook is verified but unchecked in CI** | `python3 engine/gbco_study/prose_check.py` → **259 figures checked, 0 unmatched**, against 305 checked and 15 unmatched on the superseded edition. Every one of the 15 was a superseded figure the rebuild had already moved — an old risk-free rate, an old cost of capital, a pre-transaction stake. See row 13 for the workbook. |
| 13 | An independent recalculation of the delivered workbook | **VERIFIED BY HAND, AND STILL ON THE `no_check` RATCHET — deliberately** | The workbook's own formula chain was evaluated cell by cell against the study's record: 43 cells, **0 disagreements**, ending at a weighted central of 44.329496 against a committed 44.329496. That evaluation was run from OUTSIDE the study directory on purpose: `python3 scripts/check_workbook_values.py` lists GBCO under `no_check`, and that gate goes RED for a study that MOVES between its two ratchet groups — adding a `recalc.py` here would report a move rather than a disagreement. The instrument belongs in this directory and putting it there is its own pass, recorded rather than done in passing. |
| 14 | The delivered workbook publishes the answer the study publishes | **PASS — and it did not before this pass** | The superseded workbook carried a price of 31.25 (three months older than the study), an assumed beta of 1.0, a risk-free rate from a data vendor, a typed nominal terminal growth of 11.5%, a bridge on the prior year end, and **one rate compounded five times as the discount factor with the terminal discounted at the first year's rate**. All six are corrected: the Assumptions sheet reads the committed record, and the DCF sheet now carries one forward rate per explicit year with the terminal brought home on the same cumulative factor as the last explicit year. |
| 15 | External-reader scrub | **PASS** | `python3 scripts/check_delivered_vocabulary.py` → GBCO clean. One leak was introduced during this pass and caught by that gate before delivery: the macro staleness note carried a standing-rule identifier verbatim into §1.8. Fixed at the source — the note is now computed and the shared stripper renders it — rather than by deleting the disclosure. |
| 16 | Table discipline — every printed total reproduces from the rows above it | **PASS, with one declared exception** | `python3 engine/gbco_study/footing_check.py` → **40 tables examined, 0 unreconciled** across both delivered documents. One exception is declared with its reason: "Total equity" in Appendix A.5 is a separately disclosed balance-sheet line listed beside cash and borrowings, not a roll-up of them. `python3 scripts/check_table_footing.py` → no new violations. |
| 17 | Figure discipline — solid canvas, zero transparency, verified programmatically | **PASS on the delivered edition; the repository gate still reads the superseded one** | All 8 images embedded in `GBCO_Valuation_Study_07-09-2026_public.docx` carry a MINIMUM ALPHA OF 255 — fully opaque, verified by reading the pixels out of the `.docx` archive. `figures.py` had `savefig.transparent: True` and now paints a solid canvas. `scripts/check_figure_opacity.py` still reports `GBCO 8 of 8 images, minimum alpha 0` because its `latest_study_doc()` sorts filenames as strings and therefore opens the 08-07-2026 edition. That is a defect in the gate's resolver, not in this edition, and it is NOT fixed here because a checker is not this study's to edit. |
| 18 | Every figure inspected as a rendered image, collisions fixed in-pass | **PASS, and two real collisions were found this way** | Figure 1's title was CLIPPED off the left of the canvas and its "spot" label overprinted it, because the axis limits were typed at 8–64 while the widest lens's bull case had moved to 90.7. Figure D-1 printed Expert 1's brass tick OUTSIDE its own bar and outside the axis, because his base came from the model and his range was typed. Both fixed at the source: the axes are fitted to the data and the expert ranges are read from the record. |
| 19 | A caption that promises what the figure shows | **PASS, and one false promise was removed** | Figure 2's title read "bold ≈ spot 31.25" — a typed price, and after the rebuild NO cell in that grid is within EGP 1.60 of the price, so the caption promised bold cells that do not exist. It now states the computed condition, and where no cell reaches the price it says so and names the nearest. |
| 20 | Years three to five published as RANGES [R-FCAL-01] | **PASS — published for the first time** | `python3 scripts/check_forward_ranges.py` → `GBCO prints a far-year range shape A`. Appendix A.1, Table A0: four drivers × three far years, each row labelled with its forecast year and carrying Low / Point / High, the band applied, the BASIS ("span") and the OBSERVATION COUNT. The bands are read from `engine/gbco_walkforward/forward_ranges.json` and honoured exactly as declared — orientation `actual_over_forecast`, applied by MULTIPLYING the point projection. Nothing was converted, inverted or relabelled: a span is printed as a span. |
| 21 | The reader is told what a span of that length can promise | **PASS** | The disclosure sentence is generated by the shared module rather than typed, once per distinct record length in the table. The net-profit band at the fifth forecast year rests on ONE observation and the table prints the count; the study adds a sentence saying a span of one reading is not a span at all, and why net profit is the most geared line here. |
| 22 | The forecast anchor [R-ANCHOR-01] | **PASS** | `python3 scripts/check_forecast_anchor.py` → no new violation. The anchor is GB Auto's own segment gross margin for the reviewed half to 30 June 2026, 14.30%, footed from the release's own segment table. The forecast opens at 13.80% — 3.5% relatively BELOW it, inside the 5% materiality line — and rises to 14.50%. The record is printed whether or not it fires. |
| 23 | The reverse read and the sign test [R-ENF-05] | **PASS, and it reports no lean** | `python3 scripts/check_output_records.py` → `GBCO price implies the whole-company valuation of MNT-Halan of −252.96 against the study's 1400.00; 10 judgements, 6 material, sign test p=1.00`. Ten contested judgements, six material, three taken upward — a two-sided p of 1.00, which is the instrument reporting NO systematic lean. The reverse read lives in `diagnostics.json` and no builder reads it. |
| 24 | The artefacts a builder reads declare the answer they were built against [R-ENF-06] | **PASS** | `python3 scripts/check_artefact_currency.py` → `GBCO contested_judgements.json built against 44.3295` and `diagnostics.json built against 44.3295`. |
| 25 | The rebuild ledger walks from its start to its answer [R-REBUILD-01] | **PASS** | `python3 scripts/check_rebuild_ledger.py` → `GBCO 4 lever(s), 4 rule(s), cumulative +24.0%`. The audit point was declared IN ADVANCE and the four rules pull in opposite directions: cost of capital +106.2%, the macro path −40.4%, the terminal 0.0% (refused), the bridge +0.9%. |
| 26 | The gap review [R-GAP-01] | **PASS** | `python3 scripts/check_valuation_gap.py` → `GBCO +53.0% GAP_REVIEW_07-09-2026.md (audits 44.3295)`. The review states both the central it audited and the GAP it audited, and the gap it states is the gap the study now carries. |
| 27 | The publish block [R-GAP-02] | **HELD, and not by the gap** | The central is 53.0% ABOVE the price. The deviation block is one-sided and holds only a study BELOW the price, so it does not hold this one. What holds it is the method clause: every study in the book is held until Phase 1 acceptance closes. Nothing in this pass changes that and no fair value moved toward any price. |
| 28 | Four fields on every input | **PASS** | `python3 scripts/check_four_field.py` → every committed input carries all four fields. All 19 inputs carry value, source, date and research layer; the layer is DERIVED in the generator from which source constant built the string, so it cannot drift from the source it describes. |
| 29 | Standing rules called in the study's own committed code [R-ENF-02] | **OUTSTANDING, ratcheted** | The study calls none of `assert_sigcm()`, `assert_beta_provenance()` or `assert_model_study()` in its own code. On `engine/build_depth_audit/outstanding.json`. A ratchet entry excuses a recorded failure; it does not make the study conforming, and this row says so. |
| 30 | The currency anchor the macro path derives from [R-MACRO-01 AMENDED] | **DISCLOSED, and NOT EXAMINED BY THE GATE** | The house Egyptian path's currency anchor is dated 2026-08-06 and this study is struck against a price of 2026-09-03 — **28 days**, against a 14-day bound. The study DECLARES that staleness and accepts it deliberately, and the sentence is now COMPUTED from the path file and the resolved price date rather than typed: it previously stated a strike date of 23 August and a staleness of 17 days, both wrong. `python3 scripts/check_macro_anchor_age.py` reports 12 studies that pair a strike date with an anchor and GBCO is not among them — it commits no strike date in the shape that gate reads, so it is neither listed nor examined. Four other Egyptian studies sit at 27–28 days on that gate's ratchet, so the age is the house path's rather than this study's. |
| 31 | Prose figures reconciled by a shared instrument | **PASS** | `python3 scripts/check_prose_figures.py` → **"NOW CARRYING ONE — remove from the list (2): GBCO, SWDY"**, and the sign-word check reads 44 delivered documents with 0 contradictions. The rendering set was WIDENED once, to admit a margin — a ratio of two committed figures, which is not itself committed — rather than any figure being deleted from the page. |

---

## What was found while doing this, that nobody had asked for

Five committed records carried a TYPED SENTENCE that disagreed with the computed record
beside it. Each was found by reading the record rather than by any gate, and each is
corrected at its source so it cannot go stale again.

| Found | What it said | What the record said |
|---|---|---|
| `valuation_gap.price_note` | that NO supplied price existed for GBCO and the study was struck on the library's own close of 29.51 on 23 August | the resolver twelve lines above it had already found EGP 28.98 as at 3 September, which is the committed spot in the same file |
| `macro.anchor_staleness_accepted` | a strike date of 23 August and a staleness of 17 days | 3 September, and 28 days. Both are now DERIVED from the path file and the resolved price date |
| `contested_judgements` — terminal growth | that the study ADOPTED a typed nominal 11.5% and the house terminal was the alternative | the rebuild had already taken the house terminal; the entry's own arithmetic returned a movement of exactly zero, which was the record saying so |
| `contested_judgements` — the equity beta | that the study ADOPTED an assumed 1.00 | the rebuild had already taken the conforming 0.8907 regression; again a movement of exactly zero |
| The workbook's cost-of-capital detail block | a risk-free rate from a named data vendor, a paragraph titled "Beta — why 1.0", weights struck on a price of 31.25, and two repository filenames | none of it true of this edition |

And one defect of a different kind, reported because it is not this study's to fix:
`scripts/check_figure_opacity.py` resolves "the latest study document" by SORTING FILENAMES
AS STRINGS, so on any study whose editions cross a month boundary it opens the wrong file.
On GBCO it opens the 08-07-2026 edition and reports its eight translucent figures as
current, while the eight figures actually delivered are opaque to the pixel.

## Outstanding, with reasons

1. **The macro record shape** — the substance of the macro rule is met and the record
   does not carry the field the gate reads. Ratcheted.
2. **The bridge record** — the bridge is correct, current and printed; the
   machine-readable record the gate reads is absent. Ratcheted.
3. **A disclosed useful life** — refused rather than invented. Stop and inform stands.
4. **A recalculation script in this directory** — the workbook is verified against the
   study by hand at 43 cells with zero disagreements, and the instrument is deliberately
   NOT added here because that would move the study between two ratchet groups and report
   the move rather than a disagreement.
5. **The three assertions in the study's own code** — ratcheted.
6. **The published page** — the site carries the fair value from before this rebuild.
   Nothing in this pass touches it, and publishing is a separate explicitly-requested step.
