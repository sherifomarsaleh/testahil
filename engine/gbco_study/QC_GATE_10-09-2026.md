# GBCO — QC gate, 10-Sep-2026

GB Corp S.A.E. (formerly GB Auto / Ghabbour Auto) · The Egyptian Exchange: GBCO · reporting
currency EGP · edition 10-09-2026 · documents rebuilt 13-09-2026

Filled from OUTSIDE the study by an auditor who did not build it. Nothing here is
self-certified: every row names the artefact, the command or the number that carries it.
Where a row is met with a gap the gap is stated in the same breath, not passed over.

**This gate exists because `scripts/check_calibration_deliverables.py` was RED on GBCO** —
the newest gate on disk was `QC_GATE_07-09-2026.md` against an edition of 2026-09-10. It is
not a re-dating of that gate. Every row was re-derived against the current files; both
delivered PDFs were re-read page by page as rendered images after the 13-09 rebuild; and the
two records added on 13-09-2026 (the MNT-Halan listing application and the plant-capacity
test) were traced to their own sources and tested against the study's own arithmetic.

---

## VERDICT — NOT DELIVERABLE

The 07-09 gate's three blocking rows are **all closed**: the study now commits an
external-reader scrub and it returns zero hits; the capital-expenditure path is now a
four-field-adjacent contested judgement, a priced risk-register row and a driver-ledger
entry; the bibliography's judgements table now carries the overturn column its own
introductory sentence promised. That is real work and it is recorded below.

**Nine rows block delivery. Five of them are new at this edition and four were never in
dispute.**

1. **The headline gives GB Auto a value that is not GB Auto's** (row 8). p.1–2: "GB Auto at
   its cash-flow value of EGP **33.5** bn". `docx_A.py:116` sets `OPERATING_EQ = auto_eq +
   cap_val` = 31,531.0 + 1,935.5 = **33,466.5**. GB Auto alone is **31,531**, which §1.1's
   own table prints four pages later, and the masthead on the same page prints the whole
   company's market capitalisation as EGP **31.5** bn. A reader sees one leg worth more than
   the whole company and cannot reconcile the 106% the sentence then claims.
2. **Figure 2 prints `nan` five times** (row 22). The whole 21.1% column of the delivered
   heatmap reads `nan` where the table immediately above it prints `n.m.`. **New at this
   edition** — the 07-09 figure carried all 25 cells; the terminal rate falling to 13.45%
   pushed the −2pp column inside `figures.py:113`'s `wt − g ≤ 0.045` guard, and nobody looked
   at the figure the rebuild redrew.
3. **§1.8 prints the retired cost-of-equity identity beside a number it does not produce**
   (row 34). The source cell reads Ke "reproduces from the normalised rate plus beta times
   the premium". That identity is 0.1959 + 0.8907 × 0.0941 = **27.97%**. The cell beside it
   prints **28.54%**. The committed record is `split_premium` and reproduces exactly; the
   delivered document describes `total_premium`, which this edition's own gap review calls
   retired and names at 27.97%. The sentence is a **typed string literal** at `docx_A.py:696`,
   not read from `ke_construction`. And the split's two components — mature premium
   **4.2268%**, country premium **5.1832%** — occur **zero times in either delivered
   document**, which [R-COC-03], named in the standard version this study stamps itself with,
   requires to be published.
4. **A document issued 10 September contains a search run on 13 September** (row 3). p.6:
   "Its investor news room, read on **13 September 2026**", against §1's own "ISSUE DATE **10
   September 2026** — the day this edition was issued". Both new records were added in place
   to the 10-09-2026 document three days after its stated issue date.
5. **The capacity test is run on the one line that cannot be tested and skipped on the two
   that can — and one of those breaches the disclosed figure** (row 21). The record registers
   GB Corp's published capacity for the bus plant (**5,000** bodies a year) and the two- and
   three-wheeler plant (**80,000** units) and then tests neither. Recomputed from
   `compute.py:194`'s own `cv_vg = [0.25, 0.18, 0.12, 0.10, 0.08]` on the filed 3,404 units:
   commercial vehicles run **4,255 → 5,021 → 5,623 → 6,186 → 6,681**, reaching **100% of the
   5,000 at FY2027E and 134% by FY2030E**. Light mobility runs 44,078 → **74,939** against
   80,000. Neither comparison appears anywhere in the study.
6. **The listing applicant is never pinned to the entity GB Corp owns** (row 9). GB Corp's
   associate of record, in the audit opinion the study quotes, is **MNT-BV / MNT Investment
   B.V.** The applicant is **MNT Tech Holding for Financial Investments** — a third name, and
   the study's own §1.1 says the two disclosed stake percentages differ "most likely at a
   different level of the structure". No primary source in this study links the two. The
   study then says both "**A listing would put a quote on the asset this study values two
   ways**" and, four paragraphs later, "**It is a different perimeter** … A smaller number on
   a smaller business is not a lower valuation of the same business." Those cannot both hold.
7. **SIGCM clause 4 — the asset-conversion cycle is still not built** (row 27). No DSO, DIO,
   DPO or cycle anywhere in the directory. Five typed intensity ratios stand in for it, worth
   **63.8% of the lower branch** on the study's own contested-judgement record.
8. **SIGCM clause 5 — peers are characterised, never studied** (row 28). The `Peer & Sector`
   sheet carries **0 formulas and 0 numeric cells**; not one dated peer multiple or operating
   KPI exists anywhere in the delivered set.
9. **[R-ENF-02] — `assert_sigcm()` and `assert_model_study()` appear nowhere in the study's
   own code** (row 5). Unchanged across four editions.

**Two further findings that do not block but are named rather than absorbed:** eight financial
numerals in the new content are **typed into `docx_A.py` while the numbers file holds them**
(row 12), and the standard stamp **2026.09.10** carries clauses this study does not meet on
delivery — [R-COC-03]'s published components (row 34), [R-NEWS-01]'s sweep category (row 30),
[R-EPS-01]'s reported earnings per share (row 45) (row 2).

**The two new records are, on their own terms, sound and honestly sourced** — see rows 9 and
21 for what each gets right and what each is missing. Neither moves either branch, both say
so, and both `recalc.py` gate 4 and `check_lens_design` confirm no answer is published
between the branches.

Nothing in this audit moved either published branch.

---

## Derivations stated before the build, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker, market, exchange | GBCO · EG · **EGX** | `assets/data.js:1189` carries `code: "EGX:GBCO"`, which is what decides the regressor. Not inferred from the `raw_ohlc/EG/` folder, which groups by market code. |
| Reporting and valuation currency | **EGP** | Every consolidated statement in `engine/gbco_study/src/` is headed EGP thousand. One dollar input — the MNT-Halan round — is translated at a flagged EGP 47.5 and labelled an estimate in the Disclosure. |
| Company class | **automotive assembler and distributor with a captive lender** | Registered in `research_protocol.LENS_REGISTRY` and in `lessons_register.CLASSES`; the two agree by import or the import fails. `python3 engine/lessons.py GBCO --class "automotive assembler and distributor with a captive lender"` returns **265 lessons**. |
| Reference pattern adapted | **ADNOCLS** (operating company), inside ADNOCLS's skeleton | `REFERENCE_SET` is closed at ADNOCLS / ADCB / ALPHADHABI and asserts at import. GB Corp fits none: not a bank (the lender is a segment, not the firm), not a holdco (a holdco IS its stakes; here the two operating legs cover 106% of the traded capitalisation). §1 says which and why. |
| Primary lens | **sum of the parts**, two-sided | `python3 scripts/check_lens_design.py` → `GBCO two-sided primary sotp, branches [45.782581859, 56.779474535], 2 cross-checks`. |
| The answer | **EGP 45.78 and EGP 56.78 — no central** | `study_numbers.json:central = null`; `central_two_sided.branches`. Recomputed independently from the study's own parts: (31,530.985 + 1,935.543 + 15,733.523 + 496.942) / 1,085.5 = **45.782582**; (…+ 27,670.650 …) / 1,085.5 = **56.779475**. Both reproduce to six decimals. |
| Latest known price | **EGP 28.98, close of 3 September 2026** | `engine/prices/SUPPLIED_03-09-2026.json`, read by the resolver in `compute.py`, never typed. `spot × shares` = 28.98 × 1,085.5 = **31,457.8**, which is the committed `mktcap` of 31,457.79. |
| Cone anchor | **EGP 29.51, 23 August 2026** — the exchange library's last session | `engine/raw_ohlc/EG/GBCO.csv`. All five percentile paths in `mc.fan` begin at 29.51; Figure 4 draws and names both clocks. |
| Standard version | **2026.09.10** | `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` → `2026.09.10`. The study stamps `standard_version = "2026.09.10"` and `ground_up.standard_version = "2026.09.10"`. `python3 scripts/check_standard_claim.py` → `GBCO 2026.09.10 · OK`. **Row 2 tests what that stamp now promises.** |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | Deliverables present and on one edition date | **PASS — the RED gate that called this audit is CLOSED by this file** | `GBCO_Valuation_Study_10-09-2026_public.docx` + `.pdf` (33 pp), `GBCO_Valuation_Model_10092026_public.xlsx` + `.pdf`, `GBCO_Bibliography_10-09-2026.docx` + `.pdf` (11 pp), `GAP_REVIEW_10-09-2026.md`, and this gate. Before it, `python3 scripts/check_calibration_deliverables.py` printed `GBCO the QC gate is dated 2026-09-07 while the edition is 2026-09-10`. **One gap named:** three superseded deliverables still sit in the directory — `GBCO_Valuation_Study_08-07-2026_public.docx/.pdf`, `GBCO_Valuation_Model_08072026_public.xlsx`, `GBCO_Valuation_Study_07-09-2026_public.docx/.pdf`, `GBCO_Valuation_Model_07092026_public.xlsx`, `GBCO_Bibliography_07-09-2026.docx/.pdf` — and `check_edition_date` holds the 08-07 study on `edition_outstanding.json` for exactly that reason. |
| 2 | The standard the study is stamped with is the live one, and the study meets what that stamp now names | **FAIL on three clauses of the stamp, PASS on the version string** | The version matches: `research_protocol.STANDARD_VERSION == "2026.09.10"` and the study stamps the same. `STANDARD_VERSION_NOTE` names what 2026.09.10 requires. Measured against it: **[R-COC-03]** "its components published" — the split's two components appear **0 times** in either delivered document (row 34) · **[R-NEWS-01]** a dated announced-plans category in the Company ring of the sweep — this study has **no sweep register at all** (row 30), so the category could not be opened, and the one announced plan it holds has an entity that is not established (row 9) · **[R-EPS-01]** "EVERY STUDY REGISTERS THE COMPANY'S OWN REPORTED EARNINGS PER SHARE … AN UNNAMED ONE REFUSES THE BUILD" — GBCO registers none and is on `eps_outstanding.json` (row 45). **`check_standard_claim.py` passes GBCO because it compares a stamp against a list, not against the clauses.** |
| 3 | [R-DOC-03] — two dates, labelled, and a recalibrated document is a new one | **FAIL** | §1 carries both labels: "PRICE DATE 3 September 2026 … ISSUE DATE 10 September 2026 — the day this edition was issued", and the masthead carries the edition. **But p.6 reads "Its investor news room, read on 13 September 2026"**, and the two records added on 13-09 were written into the document that says it issued on 10 September. `ls -la` on the directory: `docx_A.py`, the `.docx`, the `.xlsx` and both PDFs all carry mtimes of **13-09-2026 07:00–07:01**. The rule's own words: "an existing document is never amended in place to carry a newer answer". No *answer* moved, so the narrow clause holds; the *document* was amended in place and now contradicts its own stated issue date on the page. |
| 4 | Word structure matches the model report's 16-section skeleton | **PASS** | `python3 scripts/check_document_structure.py` → GBCO under `MATCHES THE MODEL REPORT (23)`, and the book-wide run prints `NOW PASSING — remove from the list (1): GBCO`. Read as images: masthead + READ FIRST · Headline · Valuation summary · Company overview · §1.1–1.9 · §2 · §3 · §4 · §5 · §6 · §7 · Appendix A (A.1–A.5) · Appendix B (B.1–B.3) · Appendix C (C.1–C.6) · About this series · Disclosure & Disclaimer. `document_outstanding.json` still names GBCO and is a stale entry (row 50). |
| 5 | [R-ENF-02] — the three standing gates called in the study's OWN committed code | **FAIL — two of three are absent, unchanged** | `python3 scripts/check_study_provenance.py` → 25 study directories, GBCO **not** in the outstanding list, because that gate fails a study calling *none* of the three. Grepping the directory: `beta_reissue.py:59` calls **`assert_beta_provenance(rec)`** ✓; `compute.py:959` calls **`_RP.assert_ground_up(_DL, 'GBCO')`** ✓; `compute.py:1026` calls `assert_lens_design(...)` ✓. **`assert_sigcm()`, `SIGCMChecklist`, `assert_model_study()` and `ModelStudyChecklist` return zero hits across every `.py` in `engine/gbco_study/`.** |
| 6 | Excel: 16 sheets, model names, model order | **PASS** | `wb.sheetnames == research_protocol.MODEL_STUDY['excel_sheets']` returns **True**, 16 sheets. `python3 scripts/check_workbook_structure.py` → GBCO under `MATCHES THE MODEL REPORT (23)`. |
| 7 | §1 carries 1.1–1.9 in the model's order | **PASS, with one adaptation argued** | 1.1 sum of the parts and both branches · 1.2 the Auto-leg cash-flow model · 1.3 relative multiples · **1.4 "Earnings power — why this class carries no normalised-earnings lens, and the book floor instead"** · 1.5 synthesis · 1.6 legs and the units × price driver table · 1.7 the crux · 1.8 macro and country · 1.9 sensitivity. The model's 1.4 is normalised earnings power; GBCO replaces it and argues the issuer-specific reason on p.9 — associate income was 56% / 30% / 34% of profit attributable in FY2023/24/25 and swings on marks, so "normalising earnings that swing on associate marks normalises noise". Argued, not dropped. |
| 8 | **THE RENDERED PDFs READ AS IMAGES** — both documents, after the 13-09 rebuild | **DONE — and it is what caught rows 3, 9, 21, 22 and 34** | `GBCO_Valuation_Study_10-09-2026_public.pdf`, **33 pages, every page read as a rendered image**; `GBCO_Bibliography_10-09-2026.pdf`, **11 pages, every page read**. What the read caught that no gate in this repository sees: **(a) p.1–2 — "GB Auto at its cash-flow value of EGP 33.5 bn" against §1.1's own table printing GB Auto at 31,531 and the masthead printing the whole company's capitalisation at EGP 31.5 bn** (`docx_A.py:116` `OPERATING_EQ = sotp['auto_eq'] + sotp['cap_val']`; the 106% that follows is right for the pair and the label is wrong for the leg); **(b) Figure 2's five `nan` cells** (row 22); **(c) §1.8's retired cost-of-equity identity** (row 34); **(d) the 13-September read inside a 10-September document** (row 3); **(e) §1.1's listing block asserting both that a listing tests a branch and that it is a different asset** (row 9). It also confirmed what is right: Figure 2's title promises bold cells for the two published answers and **46 and 57 are bold**; Figures 5 and 6 read "latest known price 28.98" beside a cone struck on 29.51 and say so; the mark ladder's top row reproduces the traded price exactly at 28.98. Minor and unchanged: bibliography p.3 is two-thirds blank below its table. |
| 9 | **NEW RECORD 1 — the MNT-Halan EGX listing application**, independently scrutinised | **SOUND ON ITS FACTS · FAIL ON THE ENTITY AND ON AN INTERNAL CONTRADICTION** | **What is right, and it is most of it.** The arithmetic is self-consistent: 1.6bn shares × EGP 0.10 par = **EGP 160mn issued capital** ✓. `mnt_listing.consequence` reads "none — registered, not priced. Neither branch moves", and neither did: `recalc.py` reaches 45.782582 / 56.779475, identical to the 10-09 workbook to the cell (row 14). The record marks its own limits without being asked: the source is named as press (`Arab Finance 9 September 2026 09:43`) reporting an exchange disclosure, and it records that **"the EGX news page itself is behind a bot wall and could not be read directly on 13-09-2026; that is recorded rather than papered over"**. The US$0.9–1.0bn figure is registered as `UNATTRIBUTED PRESS REPORT OF SOURCES … not a disclosure, not a transaction`, and the document says so to a reader. GB Corp's silence is logged as a dated negative search with the URL and the date. **THE ENTITY IS NOT PINNED, AND THIS IS THE defect.** The applicant is `MNT Tech Holding for Financial Investments`. GB Corp's associate of record, in the audit opinion the study itself quotes on p.20 and in the bibliography, is **"MNT - BV"**; `compute.py:519` names the structure as "the Dutch holding vehicle **MNT Investment B.V.** against the operating group MNT-Halan". Three names, and **no primary source in this study links the applicant to the entity GB Corp holds 41.61% of.** The study's own §1.1 has already established that the two disclosed stake percentages (41.61%/42.58% versus 42.93%/44.01%) differ "most likely at a different level of the structure" — so which level a listing applies to is precisely the open question, and it is not asked. **AND THE BLOCK CONTRADICTS ITSELF FOUR PARAGRAPHS APART**: "A listing would put a quote on the asset this study values two ways, and one of the two branches would then be tested against it", then "**It is a different perimeter.** The reported listing covers the Egyptian operating activities; Turkey, Pakistan and the United Arab Emirates stay outside it … A smaller number on a smaller business is not a lower valuation of the same business." If it is a different perimeter it is a different asset and it tests neither branch. **One further gap:** the perimeter claim itself — the load-bearing sentence that defuses the lower number — rests on the same press chain the study has just told the reader is not a disclosure. |
| 10 | **NEW RECORD 1 — does it reach the registers the study built to catch this class?** | **FAIL** | The listing appears in **§1.1 only**. It is **not** in the risk register (B.2 carries nine rows and this is not one, priced or named), **not** in the research register's negative results (B.3 carries six and the bot-walled EGX page and GB Corp's silence are not among them), **not** in the bibliography at all (`grep -i "listing\|MNT Tech"` over `GBCO_Bibliography_10-09-2026.pdf` → **0 hits**; `docx_biblio.py` does not read `mnt_listing`), **not** in the four-field input register (19 inputs, unchanged), and **not** in §5's catalysts to watch — which carries ten bullets including "A secondary transaction in the associate's shares" and "An audited MNT-Halan financial statement" while the one dated, live, filed event the study calls "the event that would settle the branch" is absent. This is the same shape the 07-09 gate found on capital expenditure, on new content, in the same edition that closed it. |
| 11 | **NEW RECORD 2 — the plant-capacity test**, independently scrutinised | **SOUND IN CONCEPT AND SOURCING · FAIL ON WHAT IT TESTED** | **What is right.** The record separates disclosure from press by construction and names the separation as the finding. Company-published, read live from `gb-corporation.com/gb-auto/manufacturing-facilities` on 13-09-2026: bus **5,000** bodies a year, two/three-wheeler **80,000** units, and **`passenger_disclosed: null` — "NOT DISCLOSED. Stated, not guessed."** Press is registered as press and its weakest link is named: the 110,000 "traces to the State Information Service reporting GB Corp's leadership at an April 2026 meeting … a government press office reporting a company statement, which is second hand". The forecast peak is **computed, not asserted** — `pc_vol_path` recomputes from the filed FY2025 base of 56,548 at 12/14/10/8/6%: 63,334 → 72,200 → 79,421 → 85,774 → **90,921**, matching `pc_vol_peak` exactly. The record's own framing is right: the study "carried NO capacity figure of any kind" and could "have run to two hundred thousand units". **What is wrong is what it tested — see row 21.** |
| 12 | **Depth 3 — numeric traceability: do the new figures resolve to the numbers file, or are they typed?** | **FAIL on eight numerals in the new content** | The load-bearing figures ARE read: `_LST["shares_bn"]`, `_LST["par_egp"]`, `_LST["issued_capital_egp_mn"]`, `_LST["applicant"]`, `_CAP["pc_vol_peak"]`, `_CAP["disclosed"]["bus_bodies"]`, `_CAP["disclosed"]["two_three_wheelers"]`, `V_LO`, `V_HI` — all resolved from `study_numbers.json`. **Eight are typed into `docx_A.py` while the record holds them:** `US$0.9–1.0bn` (l.431) against `mnt_listing.press_valuation_usd_mn = [900.0, 1000.0]`; `70,000–80,000` (l.466) against `capacity.press.prima_pc = [70000, 80000]`; `50,000` (l.467) against `sadat_pc = 50000`; `110,000` (l.467) against `group_total = 110000`; `41.61%` (l.440) against `sotp.mnt_halan_stake` (which the same builder reads correctly as `pc(STAKE,2)` three lines earlier); `five working days` (l.424) against `mnt_listing.window`; `roughly three quarters` (l.468) against the record's own `70-76%`; and `from FY2028E` (l.469), a year derived rather than solved. **`check_prose_figures.py` reports `GBCO prose figures checked: 272; unmatched: 0` and is right to** — it tests that a printed value exists in the record, not that the builder read it. That is the L-014 species: the check inspects a different object from the rule. |
| 13 | **A raw ISO date reaches a reader** | **FAIL, minor** | p.6: "On **2026-09-08** the Egyptian Exchange published an application by MNT Tech Holding…". `docx_A.py:422` prints `{_LST["filed"]}` unformatted. Every other date in the 33 pages is written out — "9 June 2026", "13 September 2026", "3 September 2026", "30 June 2026". One machine-format date in a document for somebody who does not work here. |
| 14 | **Depth 3 — independent recalculation, cell by cell, against the DELIVERED file** | **PASS** | `cd engine/gbco_study && python3 recalc.py` → opens **`GBCO_Valuation_Model_10092026_public.xlsx`** by date inside the filename (not by sort, not by mtime) and prints **"851 of 851 formula cells reproduce the model, 0 unresolvable, 0 unchecked; 37 headline reconciliations passed; the two branches are published side by side and no answer between them is published anywhere."** Two structural gates inside it: **`answer cells lying strictly between the two branches (EGP 45.7826 to 56.7795): 0`** and **`formulas averaging the two branches: 0`**. From outside: `python3 scripts/check_workbook_values.py` → GBCO under `CLEAN (24)`. **Nothing was skipped and nothing was unparseable.** Independently, I diffed the workbook the 13-09 rebuild produced against the copy committed on 10-09 **cell by cell across all 16 sheets: 0 cells differ**, which is why the workbook PDF dated 10-09 is still a true rendering of it. |
| 15 | **Depth 1 — standalone bibliography at the required depth** | **PASS, with two gaps named — and the 07-09 gap is CLOSED** | `python3 scripts/check_bibliography.py` → clean. `GBCO_Bibliography_10-09-2026.pdf`, 11 pp, **re-read as rendered images**: sourcing position · primary-documents table of **30 documents** all committed under `src/` · input register of **19 inputs** grouped by layer (Company 8, Company-investor-relations 11) each with value, unit, source, source date and extraction route · **judgements table of 8, now WITH a "What would overturn the choice" column** — the 07-09 gate recorded that the table's own introductory sentence promised a column it did not have, and the column is now there and populated per row · negative results of 6 · aggregator-discrepancy table. **Gap one: the two new records reach none of it** (row 10, and the capacity search is not among B.3's negative results either). **Gap two: the input register's SCOPE, unchanged.** 19 is the whole register and the study's largest inputs are outside it — the MNT-Halan carrying value (15,733.5), the round (USD 1,400mn), the stake (41.61%), EGP/USD 47.5, the other associates (496.9), the capital-expenditure path, and now the listing and capacity figures. The model report's own register carries 594 inputs. |
| 16 | **Depth 2 — four-field provenance on every input, validated by assertion** | **PASS on the fields; the scope is row 15's gap** | `python3 scripts/check_four_field.py` → `OK — every committed input carries all four fields`. Counted independently from `study_numbers.json:inputs`: **19 inputs, 0 missing any of value / source / date / layer**, plus unit and extraction route on every one. Layer split **Company 8 · Company (investor relations) 11**, which foots to the count B.3 prints. |
| 17 | **Depth 4a — no calibration appendix** | **PASS** | `calibration appendix`, `Calibration —`, `Appendix D`, `56 windows`, `Distribution centre` — **0 occurrences each** in the delivered text. The document embeds **7** images (Figures 1–6 and C-1), no gap in the numbering. Calibration evidence lives in §3 under "How often these bands have actually held", which is where `MODEL_STUDY['word_skeleton']` puts it. The re-scored replay is still generated and committed (`calibration_GBCO.png`, rewritten 13-09 07:21) and **is read by no builder**, which is the settled disposition for that record rather than an orphan. |
| 18 | **Depth 4b — no verdict tokens; CRPS only on the methodology page** | **PASS** | `band_record.assert_no_verdict_tokens(text)` run over `pdftotext` output of **both** rebuilt PDFs → raises on neither. `grep -E "\bPASS\b\|\bFAIL\b\|\bPARITY\b"` over both extracts → **0 hits**. `CRPS` → **0** in both. `python3 scripts/check_band_vocabulary.py` → `OK — no verdict vocabulary in any page text; every published band record agrees with its panel`. |
| 19 | **Depth 4c — external-reader scrub** | **PASS — the 07-09 FAIL is CLOSED, and verified independently** | The study now commits `scrub.py`. `python3 engine/gbco_study/scrub.py` → **`hits 0`** over both delivered documents, 15 banned patterns, 3 ordinary senses declared with their reasons. It reads the **delivered PDF** through `pdftotext`, not the builder, which is the right object. **Checked from outside rather than accepted:** my own 34-term scan over both extracts returns `walk-forward` **0** (it was 5 at 07-09), `valuation-input block` 0, `hard gate` 0, `SIGCM` 0, `ratchet` 0, `QC gate` 0, `four-field` 0, `Step 0` 0, `sweep register` 0, `research_protocol` 0, `compute.py` 0, `engine/` 0, `[R-` 0. Three hits read in context and cleared: "escalation" ×3 is **regional** escalation, a geopolitical risk; "the register errs strict" is the risk register, which the standard requires a reader to read; "lens" ×18/×8 is the model report's own reader-facing word. |
| 20 | **Depth 5 — figure discipline, zero transparency, every figure inspected as an image** | **FAIL — see row 22; PASS on the canvas** | Programmatic, run on the images **extracted from the delivered `.docx` itself** rather than the build directory: **all 7 are RGBA with minimum alpha 255 across every pixel and a corner of (246, 241, 230)** — solid light canvas, zero transparency. `python3 scripts/check_figure_opacity.py` → `OK — every delivered figure is opaque to the pixel`. `check_figure_axes.py` → no GBCO entry. **All 7 inspected as rendered images.** Figure 4 draws and names both clocks with the labels clear of their lines; Figures 5 and 6 read "latest known price 28.98" against a 29.51-struck histogram, which is the correct label. **Figure 2 fails on content — row 22.** |
| 21 | **The capacity test tested the wrong lines** | **FAIL** | The record registers two disclosed capacities and tests neither. **Commercial vehicles.** Base 3,404 units (FY2025, `compute.py:193`), growth `cv_vg = [0.25, 0.18, 0.12, 0.10, 0.08]`: **4,255 · 5,021 · 5,623 · 6,186 · 6,681**. Against the **5,000** bus bodies a year the record itself registers as GB Corp's own published figure, that is **100% at FY2027E and 134% at FY2030E** — a forecast running past a capacity the company publishes, in the edition whose new work is a capacity test. **Light mobility.** Base 33,906, growth `lm_vg = [0.30, 0.20, 0.15, 0.12, 0.10]`: 44,078 → **74,939** against a published **80,000** — inside, at 94% by FY2030E, and untested. Neither number appears anywhere in the study. **Stated fairly:** the GB Bus figure is bus *bodies* while the reported segment is "commercial vehicles & construction equipment", which includes distributed trucks and equipment, so the two may not be the same perimeter — **and the study neither makes the comparison nor states the perimeter difference**, while its own record calls 5,000 the disclosed capacity of one of its three plants. **Two further imprecisions in the same two paragraphs.** "It passes the Prima complex on its own **from FY2028E**" — FY2027E is already 72,200 against a Prima low bound of 70,000, and it does not pass the high bound of 80,000 until FY2029E at 85,774; FY2028E is true of neither. And "roughly **three quarters** of nameplate" sits immediately after a sentence naming 110,000 as the group figure, against which 90,921 is **83%**; the 70–76% the record computes is against Prima+Sadat of 120,000–130,000, and a reader cannot tell which denominator is meant. |
| 22 | **Figure 2 prints a computational artefact to a reader** | **FAIL — new at this edition** | The delivered `fig2_sens.png` prints **`nan` in all five cells of the 21.1% column**, while the identical grid tabulated immediately above it prints **`n.m.`** The cause is in the study's own code: `figures.py:113` returns `float('nan')` where `wt − g ≤ 0.045`, and `figures.py:129` formats every cell with `f'{tab[i, j]:.0f}'`, which renders NaN as the string `nan`. It is new: the same figure in `GBCO_Valuation_Study_07-09-2026_public.pdf` page 13 carries **all 25 cells populated** (50/40/33/28/24 … 69/59/52/47/43), because the terminal rate was higher then; this edition's terminal weighted rate of 13.45% less 7.00% growth is 6.45%, and a −2pp shift takes it to 4.45%, inside the guard. **The rebuild redrew the figure and nobody looked at it.** No programmatic gate in this repository reads the pixels of a heatmap's annotations. |
| 23 | **Depth 6 — table discipline: fixed layout, and the three counts per document** | **PASS on two counts, with one gap named and one not measurable from the shipped tooling** | Measured on the rebuilt `.docx` files using the house function `engine/col_width.token_cm` on data tokens, the same measure the shipped gate uses. **Study — 37 tables, 140 columns, over-wide 0, starved 0.** **Bibliography — 12 tables, 54 columns, over-wide 0, starved 0.** From outside: `python3 scripts/check_column_widths.py` → 48 documents, 1,081 tables, 14 columns too narrow book-wide, **none of them GBCO's** (the 4 NEW ones are all PHDC). `python3 engine/gbco_study/footing_check.py` → `tables examined: 46; unreconciled totals: 0`. **Gap one:** **3 of the 49 tables do not declare fixed layout** — 35 of 37 in the study and 11 of 12 in the bibliography carry `autofit=False` — though **all 49 carry an explicit grid**, so none is at risk of the renderer-versus-declared-width defect. **Gap two, stated rather than passed:** the repository ships **no bloat test**; `check_column_widths.py` measures only the narrow direction and `col_width.py` exposes no bloat function, so the third count cannot be produced from the house tooling and is not invented here. |
| 24 | **Depth 7 — expert appendix at maximum detail** | **PASS** | Appendix C, pp.27–32, re-read as images. **Expert 1** (split-legs net asset value): worldview; when-it-works / when-it-fails; a worked table of 7 lines from the auto leg at 31,531 to **EGP 55.91** per share, arithmetic verified independently (31,531 + 6,267 + 28,168 = 65,966; less 8% = 60,689; / 1,085.5 = 55.91); a named sensitivity with numbers (mark at three quarters → 50.05; at half → 44.18); a falsifier stated in advance. **Expert 2** (residual income on the whole group): table to **EGP 9.22** at 0.2993× book, sensitivity ±1pp of return → 6.15 / 15.36, falsifier, plus **two self-stated contaminations** he raises before anyone puts them to him. **Expert 3** (cash returns): table to **EGP 36.88**, two ranked levers (0.85× → 0.60× worth 6.49; 1.00× capital employed worth 2.63), falsifier. **C.4 cross-examination — 6 challenges, each recorded CONCEDED, CONCEDED IN PART or REJECTED with its reason.** C.5 the three in one room. C.6 a divergence table isolating the single swing assumption per expert, plus Figure C-1. |
| 25 | **Depth 7b — experts labelled "Expert 1/2/3", no persona name anywhere** | **PASS, with one wording note** | Headings read "C.1 Expert 1 — the split-legs net asset value and the marks", "C.2 Expert 2 — residual income on the whole group", "C.3 Expert 3 — cash returns: return on capital against its cost". **No persona name appears anywhere in either document.** The note: C's opening calls them "a panel of standing expert **personas**" and glosses Expert 1 as "the accountant". Those are method labels rather than names, and the appendix uses the word precisely — "a method being retired rather than a persona being renamed" — so the rule is met on its own terms and the vocabulary is recorded here rather than passed over. |
| 26 | **Depth 8 — the central contested judgement computed both ways, published side by side, never averaged** | **PASS — and it remains the study's strongest row** | The contested judgement IS the answer. Both readings appear in the valuation summary table (p.2, two rows), §1.1's leg table (two full columns), the associate-footprint table (p.5), the **mark ladder** (six rungs from −2,506 to 27,671 with the whole sum of the parts recomputed at each), §1.5's synthesis, Figure 1, Figure 2 (both marked ‡), Figure 4's two brass dashed lines, §4, §7 and the Disclosure. In the workbook: `recalc.py` asserts each sensitivity grid reaches its own branch to six decimals, and two structural refusals are **asserted rather than promised** — 0 answer cells strictly between, 0 formulas averaging. `python3 scripts/check_lens_design.py` → conforming. Expert 1 and Expert 3 take the round side and Expert 2 prices the associate at nothing, so the panel spans the same fork. |
| 27 | SIGCM 4 — asset-conversion cycle studied from the statements | **FAIL — unchanged** | **No DSO, DIO, DPO or cash-conversion cycle appears anywhere** in `compute.py` or in any builder: `grep -in "\bdso\b\|\bdio\b\|\bdpo\b\|days sales\|days inventory\|days payable\|receivable days"` over `engine/gbco_study/*.py` returns exactly one line, and it is the *overturn condition* in `diagnostics_gbco.py:599` saying the glide "is currently five figures standing in for a cycle the filings would measure directly". Working capital is projected from one typed intensity ladder on revenue — 26.5 · 25.0 · 23.5 · 22.5 · 21.5%. Receivables, inventory and payables are all disclosed and none is separated. `contested_judgements.json` prices the glide at **63.8% of the lower branch** (45.78 against 27.94 held flat) — the largest single judgement in the study. |
| 28 | SIGCM 5 — competitors studied for operating KPIs and valuation multiples | **FAIL — unchanged** | B.1 names peer *sets* — Gulf and regional auto distributors, listed Egyptian finance companies, listed payments and financial-technology names — with valuation conventions and a Caution column saying why each travels badly, and states in terms that "the absence of a single clean comparable is itself a finding". That is the honest reading of the disclosure and it is not the clause. Measured: the **`Peer & Sector` sheet carries 0 formulas and 0 numeric cells**; not one peer multiple, operating KPI or dated figure is computed in the workbook or printed in the document. The relative cross-check deliberately uses GB Corp's own trailing multiple instead (row 39), which is defensible and is not the gap; the gap is that no peer figure was sourced, dated or registered. |
| 29 | SIGCM 1 — historicals from official sources only | **PASS, with the extraction route named** | `python3 scripts/check_source_integrity.py` → no GBCO entry among the 1 breaching and 5 unreadable. All **30** primary documents are GB Corp's own and committed under `src/`. `src/SOURCES.md` records the route honestly, including that the earlier six-route failure assembled its host list from the company's FORMER name and never tried its current domain. **The route is stated rather than smoothed:** the reviewed statements to 30-Jun-2026 and 31-Mar-2026 and the FY2022–FY2025 audited sets carry **no text layer** (I confirmed: `pdftotext` on `GB_Corp_Consolidation_English_30_June-2026.pdf` returns 51 bytes across 51 pages, producer "Xerox D125 Copier-Printer"), and the study says so, names the annual report as the ROUTE and the audited statement as the DOCUMENT, and records a **ten-thousand OCR ambiguity in one associate-note cell** — resolving the other associates by identity off the note's own total rather than by summing the rows. |
| 30 | Sweep Register — primary-source depth, run through the shared module | **FAIL, ratcheted — and [R-NEWS-01] now depends on it** | `python3 scripts/check_sweep_module.py` → **`GBCO no sweep script in the study directory`**; on `sweep_outstanding.json`. `ls engine/gbco_study/sweep*` → nothing; `grep -rn research_sweep engine/gbco_study/*.py` → nothing. **None of the eight invariants has ever run.** **What the register would have found, verified here by hand:** company site attempted and **logged either way** ✓ (the six-route failure and the first-attempt success at `ir.gb-corporation.com`, both in `src/SOURCES.md` and in `engine/escalations.json`, whose entry `GBCO-audited-statements-not-reachable-from-thi` reads `resolved (answer on HEAD)`); **six audited fiscal years from the filing itself** against a floor of two and a target of four ✓; **every study-year quarter disclosed swept in** — 1Q26 and 2Q/1H26, statements and releases both ✓; **results releases swept with their statements** — 9 releases beside 6 statement sets ✓; **≥1 COMPANY_IR source tagged distinctly** — 11 of 19 inputs carry `layer: "Company (investor relations)"` ✓. **What is NEW and now blocked by the absence:** [R-NEWS-01], adopted at the standard this study stamps, makes a dated announced-plans category MANDATORY on the Company ring and requires every item to carry `entity` and `entity_is_issuer`, refusing an item whose issuer status is unestablished. This study's one announced-plan item is the listing, its entity is unestablished (row 9), and there is no register to refuse it — so it drives narrative instead. |
| 31 | SIGCM 6 / [R-BETA-04] — beta inspected on its record, not on a boolean | **PASS** | `beta_reissue.py:59` calls `assert_beta_provenance(rec)` in the study's own code, and `python3 scripts/check_study_provenance.py` re-runs the record test from outside — GBCO is **not** in the outstanding list of 8. The record, inspected line by line rather than trusted: **`index_file = "raw_indices/EG/EGX30.csv"`** — under `raw_indices/`, **not a constituent composite**. GBCO is EGX-listed per the `EGX:` prefix in `assets/data.js`, and EGX30 is the registered index for (EG, EGX), so no interim note is owed and `interim_note` is correctly `null`. Not a DFM name, so `wacc_builder.index_interim_note('AE','DFM')` does not apply; not Qatari, so the QATAR10 weekly-only caveat does not apply. **Usability gate cleared on its own terms, so no fallback was owed:** n = **251** weekly observations (≥24), R² = **0.2430** (≥5%), SE(β) = **0.2041** below |β| = **0.8907**; `usable: true`, `tier: 1`, `conforming: true`. Window 2021-09-09 to 2026-07-16 (4.85 years), W-THU to match the EGX trading week, Dimson-corrected, Blume cross-check 0.9271, index as-of 2026-07-22. The record also carries what it replaced and prices it: `superseded_beta 1.0` ("assumed"), `delta_beta −0.1093`. |
| 32 | SIGCM 2 / [R-SIGCM-02] — ground up on a driver record, 100% of revenue, gaps noted | **PASS, with the split reported not ticked** | `compute.py:959` calls `research_protocol.assert_ground_up(_DL, 'GBCO')` in the study's own code; `python3 scripts/check_ground_up.py` re-runs it from outside and the only failure is ADIB. **6 driver lines summing to exactly 1.000000** of first-forecast-year revenue. **Share by level: unit 74.42% · derived 0% · segment 26.70% · topdown −1.11%.** Unit lines: passenger cars 63.46% (26,994 / 42,043 / 56,548 vehicles on EGP 16,544.3 / 36,533.4 / 52,827.3mn across three disclosed years), commercial vehicles 7.91% (3,404 units, one disclosed year, and the record says the level is observable and the trend is not), motorcycles and three-wheelers 3.04% (33,906 units, one year). Both segment lines carry gap notes: tyres/parts/trading 5.07% ("NO UNIT IS DISCLOSED"), GB Capital 21.63% ("BUILT ON THE SEGMENT TOTAL WHILE A FINER SPLIT IS DISCLOSED" — Table 13 breaks it into six companies). The topdown line is the intersegment elimination and **the one line whose cost side rests on a typed proportion says so in its own record**: "at a stated 20% gross margin … THAT PROPORTION IS TYPED RATHER THAN DISCLOSED", 1.1% of group revenue. |
| 33 | SIGCM 3 — debt studied in full, local vs foreign currency split | **PASS** | `cost_of_capital_record.kd_integrity.pct_local_currency = 1.0`, sourced to "GB Corp FY2025 audited consolidated statements, notes 26 (loans) and 38 (bonds), and the segmented balance sheet in the 2Q26 earnings release as at 30 June 2026; **every disclosed facility is EGP-denominated**." Group borrowings EGP 42,476.0mn at 30-Jun-2026, GB Auto segment 22,733.1mn, both four-field registered. No FX tranche exists, so no local-equivalent carry is owed. |
| 34 | v2 cost of capital — country once, marginal Kd, market-value weights, both ERP bases, and **the construction as the reader receives it** | **FAIL on delivery; PASS on the record** | **The record is right and I verified it arithmetically.** rf normalised by Egypt's own default spread: 23.00% − 3.41% = **rf\* 19.59%**. `ke_construction: "split_premium"` and it reproduces to ten decimals: 0.1959 + 0.8906822 × **0.042268** + **0.051832** = **0.2853794** = `ke_exp`. Terminal: 0.1050 + 0.8906822 × 0.042268 + 0.027732 = **0.1703794** = `ke_terminal`, with rf\*\_terminal 10.50% = 7.00% inflation + 3.50% real. WACC checks: 0.425486 × 0.285379 + 0.574514 × 0.191016 = **0.231166** = `wacc_exp`. Kd marginal and above its sovereign, 26.53% against 23.00%, computed over average interest-bearing borrowings only with GB Capital's cost of funds added because GB Corp books it in that segment's cost of revenue. Market-value weights 42.55% / 57.45%. Both ERP bases published in the record (market 9.41% adopted, rating 13.94%). `python3 scripts/check_cost_of_capital.py` → `GBCO EG, 23.12% gliding to 13.45%`, no violation; `python3 scripts/check_ke_reproduction.py` → GBCO among the 10 reproducing, the one breach being FERTIGLOBE. **THE DELIVERED DOCUMENT IS NOT.** §1.8's Ke row reads, verbatim: *"reproduces from the normalised rate plus beta times the premium; 30.10% on the rating basis"*. That identity gives **0.1959 + 0.8907 × 0.0941 = 27.9713%**. The cell beside it prints **28.54%**. `docx_A.py:696` types that sentence as a string literal rather than reading `ke_construction`. The gap review for this very edition names 27.97% as the **retired** construction. And the two components the split rests on — **4.23%** and **5.18%** — return **0 hits** in a `grep` over both delivered PDFs. [R-COC-03] requires them published, `ke_reproduction.py` enforces publication **in the record only**, and the record passes while the page states the identity the rule retired. |
| 35 | [R-COC-02] — Ke reproduces under a NAMED construction from the closed list | **PASS** | `ke_reproduction.TERMINAL_CONSTRUCTIONS` = `("same_beta", "relevered", "split_premium", …)`; the record declares `split_premium` on both the explicit and the terminal and both reproduce (row 34). No relevering, so no tax rate is owed. `python3 scripts/check_ke_reproduction.py` → `cost-of-capital records read: 11`, `reproducing: 10`, GBCO not among the breaches and not on `ke_outstanding.json`. |
| 36 | [R-MACRO-01] — one house macro path, no study-local inflation number | **OUTSTANDING, ratcheted** | `python3 scripts/check_macro_coherence.py` → **`GBCO MacroPathError: '' is not a covered market`**, listed among the 15 allowed on `macro_outstanding.json`; the single new failure is ADIB. The cause is unchanged and I confirmed it by reading the record: `macro` carries `path: "EG"` but no `market` key, which is what the assertion reads, **so this study's macro coherence has never been tested from outside.** By hand the record is consistent: `path_asof 2026-09-02`, terminal inflation 7.00% from `engine/macro_paths/EG.json`, terminal growth stored as **real 0.0%** and recomputed to **7.00% nominal** — the exact Fisher identity, (1+0.00)(1+0.07)−1 = 0.07, not the first-order approximation. `inflation_inputs` IS declared with one mapped entry and a note explaining the near-emptiness: the auto leg is units × ASP, so no domestic CPI series escalates any line. `anchor_staleness_accepted` is declared with a reason — the path's sovereign quote is anchored 2026-08-06 against a strike of 2026-09-03, 28 days, disclosed in §1.8 as an italic note. `check_macro_anchor_age.py` → no new breach. |
| 37 | [R-ANCHOR-01] — anchored on the latest reviewed period; a material decline names its mechanism | **PASS** | `python3 scripts/check_forecast_anchor.py` → GBCO listed under `CONFORMING (22)`: `GB Auto gross margin, latest 0.1430, forecast 0.1380, −0.50pp`. Relative gap **−3.5%**, inside the 5% materiality line, so no mechanism is owed and none is claimed. The path then RISES (13.80 → 14.20 → 14.50 → 14.50 → 14.50), so clause two does not fire either. The single new violation in the book is ADIB. |
| 38 | [R-SENS-01] — sensitivity grids centred on the struck values | **PASS on substance, with the module gap named** | Both grids are centred on the model's own build rather than on round numbers: grid 1's rate axis is 21.1 / 22.1 / **23.1** / 24.1 / 25.1% around a struck 23.12%, and its centre row is the carrying-value mark, so the centre cell is **45.8** = the lower branch; grid 2's centre cell, 23.1% against 7.0%, is **56.8** = the upper branch. `recalc.py` asserts both — "the sensitivity grid reaches branch B / branch A on its own branch column", each to six decimals. **The gap:** the study calls neither `dcf_table.sensitivity_grid()` nor any sanctioned grid helper (`grep -rn "sensitivity_grid" engine/gbco_study/*.py` → nothing), so the assertion is the study's own rather than the shared module's, and there is no repository gate for this rule. |
| 39 | [R-LENS-03] — one class primary IS the answer; the others are cross-checks; no weighted blend | **PASS** | `python3 scripts/check_lens_design.py` → `GBCO two-sided primary sotp, branches [45.782581859, 56.779474535], 2 cross-checks`. **Cross-checks published beside the answer and weighted into nothing:** relative multiple **EGP 19.29** on FY2026E earnings per share of 3.04 × **6.350×**, the MEDIAN of GB Corp's own trailing price-to-earnings at its last three year-end closes (4.54× / 6.35× / 10.18×) — non-circular by construction, with the traded multiple on the same forward earnings, **9.540×**, printed beside it so a reader can divide the claim rather than believe it, and the count of three published because a median of three is thin; and **disclosed book value EGP 30.82** (33,454.3 ÷ 1,085.5) published as a FLOOR, explicitly excluded from the envelope, against which the shares trade −6%. `check_lens_vocabulary.py` → `OK — no delivered document newly publishes the retired blend`. |
| 40 | [R-STAR-01] — the traded price as the northern star, and the asymmetric burden | **PASS, and the module says so rather than this gate** | `python3 -c "import northern_star as ns; ns.from_numbers('GBCO')"` → **`two_sided: true`**, both branches assessed and never collapsed, each `direction: "above"`, `tier: "none"`, `verdict: "PASS"`, with the note "The central sits ABOVE the traded price, which needs no case". **Both branches sit above the price, so no air-tight case is owed and none is claimed.** One inconsistency worth naming: `northern_star.from_numbers` reads spot **29.51** from `assets/data.js` while the study is struck on **28.98**; the gaps it computes (+55.1% / +92.4%) therefore differ from the study's own (+58.0% / +95.9%). It changes neither the direction nor the verdict. |
| 41 | 3 years historical + 5 years forecast, with the FULL FCFF waterfall shown inline to PV | **PASS** | Appendix A.2 carries FY2023 / FY2024 / FY2025 **as reported**, each naming its source document and date (`GB_Corp_ER_4Q23/4Q24/4Q25`, dated 2024-03-01 / 2025-02-01 / 2026-02-26). A.3 carries FY2026E–FY2030E. **The waterfall is shown inline in §1.2 and again in A.4 and on the `DCF` sheet, every line, and it reaches present value:** Auto revenue 78,550 → operating profit before depreciation 6,677 → less D&A (864) → operating profit 5,813 → after tax at 28% 4,185 → plus D&A 864 → less capital expenditure (3,000) → less increase in working capital (1,899) → **free cash flow to the firm 151** → discount factor 0.812 → **present value 122**; then Σ PV 6,965 → terminal 89,266 → PV of terminal 39,781 → enterprise value 46,745 → less net debt (14,624) → less minorities (591) → **Auto equity 31,531**. It does not stop at FCFF. `python3 scripts/check_waterfall_assertions.py` → `GBCO OK, 11 waterfall table(s)`. |
| 42 | [R-DCF-01] — the valuation on ONE page | **NOT OWED by class; the number is stated rather than skipped** | `dcf_table.probe(numbers)` → **`renders: false`, `missing_total: 11`** — missing forecast keys `years, rev, ebitda, capex, dnwc, dna, fcff` and bridge keys `pv_explicit, nd, eq_attr, ps`; `dcf_table(numbers)` raises `DCFTableError the numbers file carries no fcst/dcf block`. **The module's own docstring carves this out**: "NOT EVERY CLASS OWES THIS TABLE. The contract describes a cash-flow lens, and the class primary is not a cash-flow lens everywhere — a bank is valued on equity flows and a holdco on a sum of its parts." GBCO's class primary is `sotp`, so the probe is asking about a lens the study does not run as its primary. **Named as a gap rather than closed:** the Auto leg IS a full cash-flow lens and already publishes every line of that table, so eleven renamed keys would render it, and the reader currently assembles the valuation across §1.1's leg table, §1.2's waterfall and p.8's bridge — three tables on three pages. No gate exists for this rule. |
| 43 | Cost stack — one escalator per driver class, and a sourced near-term cost anchor | **FAIL on the escalators; PASS on the anchor** | **There is no cost stack.** The entire cost side of the auto leg is a gross-margin path (`gpm = [0.138, 0.142, 0.145, 0.145, 0.145]`) plus `gsa`, `oth`, `prov` and `dna_pct` as ratios of revenue; the lender's is `cap_gm = [0.188, …, 0.196]`. No physically distinct cost line — imported content, local assembly, freight, wages — carries its own driver, so "one escalator per driver class" cannot be met and is not, though it also cannot be breached by a single blended index because there is no index either. **The near-term anchor is sourced and foots:** `forecast_anchor` anchors on GB Auto's own segment gross margin for the filed half — revenue 40,021.5, gross profit 5,722.1, **14.30%** — from the 2Q/1H26 release of 13 August 2026, Table 11. |
| 44 | Margins as OUTPUTS wherever the filings disclose enough to build cost per unit | **PASS on the arithmetic; the gap is disclosure-bounded, named, and invisible to the assertion that passed** | The margin is an INPUT on both legs (row 43). Whether that is a fail turns on whether the filings disclose enough to build cost per unit, and they do not: GB Corp publishes GB Auto's revenue and gross profit **in aggregate for the leg**, never a cost of sales per product line and never a cost per vehicle. The driver record says exactly this for every unit line — `cost_basis: "the auto leg's gross margin, held at the level the latest reviewed half filed"`. The revenue side IS built as volume × price on disclosed units for 74.4% of revenue. **The gap the record does not carry:** the three unit lines are `level: "unit"` with `gap_note: null`, and `assert_ground_up()` accepts that because it tests the naming of the unit, its source and the price basis — **the cost side of a unit line is outside what it inspects**, so a real gap is invisible to the assertion that passed it. |
| 45 | [R-EPS-01] — the company's own reported earnings per share registered beside the profit divided | **OUTSTANDING, ratcheted, and now a clause of the stamp** | `python3 scripts/check_eps_reconciliation.py` → **`GBCO registers no reported earnings per share beside its attributable profit, so the gap cannot be seen`**, under `unreadable` in `eps_outstanding.json`. A.2 prints EPS of 1.74 / 2.70 / 2.65 for FY2023–25 and A.3 prints 3.04 → 10.33 forward, all computed on 1,085.5 shares; **GB Corp's own reported figure is registered nowhere**, so no comparison is possible from the record. [R-EPS-01], adopted at the standard this study stamps, says an unnamed difference "REFUSES THE BUILD AND DOES NOT WARN" (row 2). |
| 46 | Every non-sourced driver logged to `engine/Fundamental_Driver_Ledger.md` | **PASS — the 07-09 FAIL is CLOSED** | The ledger now carries a second GBCO section at line 1083, **"GBCO — the study's own forward drivers, 08-09-2026"**, which opens by saying the entry above it logged the walk-forward's drivers and not the study's own, "which is a different requirement and was not met". It prices the two that matter in a table — capital expenditure (3,000 · 2,400 · 2,500 · 2,600 · 2,800, i.e. 3.82% → 1.93% of auto revenue against FY2025's filed **5.52%**, worth **EGP 8.98 a share off both branches**) and working-capital intensity — and then **names all sixteen of the rest**: passenger-car, commercial-vehicle and light-mobility volume and price growth, trading growth, the gross-margin path, the SG&A / other-income / provisions / depreciation ratios, GB Capital's revenue and margin paths, the group ratios, the associate-income path, the finance-cost path, the minority ratio, the elimination ratio and its 20% margin, and GB Capital's depreciation. It also records a correction made while writing it — a field named `working_capital_fy2025` that held FY2030E's closing balance — rather than fixing it quietly. |
| 47 | Guidance is scored, never consumed | **FAIL on the substance; the 07-09 register gap is CLOSED** | `compute.py:283` `capex = [3000, 2400, 2500, 2600, 2800]` and the delivered §1.2 still reads *"capital expenditure follows the EGP 3,000 mn **guided** for the first year"*. The standing clause is that guidance is scored and never consumed, and the first forecast year is still the figure management guided. **What has changed, and it is the whole of the 07-09 finding except this:** the path is now the **first row of `contested_judgements.json`**, priced at **23.3% of the lower branch** (45.78 against 37.14 at the filed intensity, and 56.78 against 48.14 on the round branch), with an `overturned_by` naming two further filed years or a disclosed capital plan; it is the **fourth row of B.2's risk register**, printed as "PRICED: 23.3% of the lower branch"; and it is the **first entry of the driver ledger's new section**. The judgement's own `why` field states the finding against itself: "THIS JUDGEMENT WAS IN NO REGISTER UNTIL IT WAS ASKED FOR". Two mitigations are recorded rather than left to a reader: capex still runs 3.47× down to 1.76× depreciation, so the asset base grows; and the terminal is 85% of the auto leg. |
| 48 | [R-ENF-05] — the reverse read outside the numbers file, and the sign test | **PASS, and the reverse read is the sharpest thing in the study** | `python3 scripts/check_output_records.py` → `GBCO price implies the whole-company valuation of MNT-Halan of **−126.78** against the study's 1400.00; 8 judgements, 4 material, sign test p=0.62`; the one new failure in the book is ADIB. The reverse read lives in `diagnostics.json`, is computed by `diagnostics_gbco.py`, and **no builder reads it** — the file's own `why_this_file` states the prohibition and the script asserts the committed numbers file is byte-identical before and after it runs. **The number went negative at this edition and the study handles it honestly rather than hiding it:** the market capitalisation leaves **EGP −2,009mn** for ALL the associates once the two operating legs are taken at the study's marks, against EGP 16,230mn carried on the reviewed sheet — printed on p.5, in the mark ladder's top row, in §1.7 and in §4. The record states in terms that "THE ARITHMETIC CAN BE READ TWO WAYS" and that it does not settle who is right. §1.7 also gives the milder reading in the same breath: solved as a rate instead, the price implies 32.26% on the round branch and **26.62%** on the carrying branch against the study's 23.12%. Judgement count rose 7 → 8 with capex added, so the sign test's set is no longer missing it. |
| 49 | [R-ENF-06] — every artefact a builder reads declares the answer it was built against | **PASS** | `python3 scripts/check_artefact_currency.py` → `GBCO contested_judgements.json built against 45.7826` and `GBCO diagnostics.json built against 56.7795`, `OK - no new violations`. Both additionally declare `published_central_two_sided` carrying **both** branches and a note explaining that the study publishes no central, so the check holds each file to an answer the study actually publishes rather than to an average of two it does not. |
| 50 | Live ratchet inventory — re-read from disk for this edition | **5 LIVE · 7 STALE** | Re-read from `engine/build_depth_audit/*.json` on the current tree by loading each file and searching its JSON, not by `grep -l`, which under-reported by two on an earlier pass. **Live:** `bridge_outstanding` (no bridge record — row 51) · `eps_outstanding`/unreadable (row 45) · `macro_outstanding` (record exposes no market key — row 36) · `sweep_outstanding` (no sweep script — row 30) · `terminal_outstanding`/unscored (row 52). **Stale — the gate now passes GBCO while the list still names it:** `document_outstanding` (`check_document_structure` prints `NOW PASSING — remove from the list (1): GBCO`) · `lens_vocabulary_outstanding` (GBCO clean) · `waterfall_outstanding` (`GBCO OK, 11 waterfall table(s)`) · `published_gap_outstanding` (records "+21.0% fair 35.7 vs spot 29.51", both figures two editions superseded) · `coc_state`, `queue`, `queue_raw` (all still describing the 08-07-2026 edition as needing a cost-of-capital rebuild since done). **Two entries are correct and not stale:** `band_outstanding` and `edition_outstanding` both name `GBCO_Valuation_Study_08-07-2026_public.docx`, which is still sitting in the directory (row 1). |
| 51 | [R-BRIDGE-01] — the enterprise-to-equity bridge as a checked record | **OUTSTANDING, ratcheted** | `python3 scripts/check_bridge.py` → `GBCO carries no bridge record`, among the 15 allowed on `bridge_outstanding.json`; the one new failure is ADIB. **None of the construction clauses has been tested from outside.** By hand: the bridge stands on the **latest disclosed balance sheet**, the reviewed statements to 30 June 2026, with A.5 tabulating cash 10,951.5, group borrowings 42,476.0, GB Auto segment borrowings 22,733.1, group total equity 35,127.9, each dated 30 June 2026 and each naming where it was read; the deduction is the **AUTO leg's** own net debt (14,623.7) and minority (590.7), because the lender's borrowings are its raw material rather than its leverage; and the printed lines foot to the printed equity value, asserted at build time by `table_residual.waterfall()`. Sound on inspection, unrecorded as a record. |
| 52 | [R-TERM-01] — the terminal on a disclosed asset life, and the refusal | **OUTSTANDING, ratcheted — and the refusal is the right one** | `python3 scripts/check_terminal_floor.py` → `GBCO no nopat_term, no ic`, under `unscored`: the terminal resolves but its charge does not, so the 1/g test has never run. `check_terminal_spread.py`, `check_terminal_basis.py`, `check_terminal_record_shape.py` → all clean. **The substantive position is a STOP AND INFORM and it is honoured:** `useful_lives.json` records that both admissible routes were run and both failed — the policy note discloses rate RANGES (2%–33%, a 3-to-50-year span) and the identity route returns 10.9–17.1 years on an undisclosed land split, with per-class rates contradicting the disclosed bands. **No life was chosen**, `engine/terminal_value.py` was not used, and the refusal is printed in §1.2, in §7 ("the largest single piece of unfinished business in this study"), in B.2 as a NAMED AND NOT PRICED risk and as B.3's first negative result. Terminal growth is nevertheless stored as a real rate and recomputed (0.0% real → 7.00% nominal), and **the terminal is 85.1% of the auto leg's enterprise value** (39,780.8 / 46,745.4, verified), stated in the bridge, in §1.9, in §7 and given its own heading in the gap review. |
| 53 | Step 0 — calibration under the band record: coverage against target, the count beside the percentage, width disclosed and never gated | **PASS, with the gap named in the same breath** | Published in §3 under "How often these bands have actually held" as plain sentences with the statistics inline, where the model report asks for it. Verified against the live record `assets/data.js:3270` `BANDS.GBCO = {n:57, hits:54, c50:0.5965, c80:0.9123, c90:0.9474, width:1.423, strength:"long", flag:null}`: the page reads "Over **57** resolved three-month forecasts … inside the 90% band **95%** of the time, against a 90% target, and inside the middle 50% band **60%** of the time against a 50% target" — 54/57 = 94.7% ✓, 0.5965 ✓ — with the record's own strength read out. **The width ratio is disclosed with no threshold**: "this cone's 90% band is **1.42** times as wide as a naive carry-anchored one … It is NOT a pass mark and nothing here is graded against it." `flag` is `null` so nothing is said about narrow or wide. **The gap, and it is two-part.** (i) The page says in capitals "**THE COUNT IS PRINTED BESIDE THE PERCENTAGE**" and prints the **denominator** (57) but not the numerators — 54 and 34 appear nowhere, so a reader cannot recover them. (ii) **The binomial is not printed.** Computed here on the published record: 90% band 54/57, two-sided **p = 0.372**; 50% band 34/57 against 50%, **p = 0.185**. Neither earns a flag, so the wording is right, but it is asserted rather than shown. **An observation, not a defect:** the 80% band is now carried in the record at `c80: 0.9123`, i.e. 52/57, which gives **p = 0.031** and would earn a flag — but `band_record` computes the flag on the 90% band alone, which is what the rule specifies, and the study publishes no 80% figure. |
| 54 | Step 0.0 — data-quality gate on every series before anything else | **PASS** | Both series pass and both records are committed **empty-of-findings rather than absent**: `beta_result.json` carries `"index_dq": []` and `"stock_dq": []`, i.e. `beta_regression.own_stock_beta()` ran `data_quality` on the stock and on the regressor and neither raised. `engine/raw_ohlc/EG/GBCO.csv` to 23-Aug-2026 against the EGX ±20% daily limit; `engine/raw_indices/EG/EGX30.csv` as-of 2026-07-22. The cone reads the persistent library and not the study-local extract, and `compute.py` records why — the study-local copy stops at 7 July 2026. |
| 55 | [R-FCAL-01] — the fundamental walk-forward, scope decided first and stated | **PASS** | `python3 scripts/check_walkforward_scope.py` → `GBCO FULL, 14 years, run`. `engine/gbco_walkforward/` holds the pre-registration, the training record, the panel and the scored output: origins FY2016–FY2024 (nine), horizons 1–5, **225 scored cells**, 7 drivers. **No correction was promoted** — `check_walkforward_actuation.py` → `GBCO 7 drivers -> adopt 0, watch 5, none 2` (GBCO is not the new failure; ADIB is), and `check_corrections_applied.py` → `GBCO declares NO correction adopted` (again ADIB is the failure, for silence). The finance-cost candidate is named rather than buried: a large bias whose sign flips at four of six admissible cuts, and the exact trap the rule names — 7.05% over total liabilities, 11.27% over group borrowings, **26.53%** over the interest actually incurred. `check_valuation_inputs.py` → GBCO conforming. `check_driver_coverage.py`, `check_frozen_escalator.py` → no GBCO finding. |
| 56 | Far forecast years published as RANGES with their basis, orientation and count | **PASS, with one presentational gap named** | `python3 scripts/check_forward_ranges.py` → `GBCO prints a far-year range, shape A`. Appendix A.1 carries FY2028/29/30 as Low / Point / High for four aggregates with a **Basis** column reading `span` and an **Observations** column printed beside every band (7 / 6 / 5 on revenue, gross profit and operating profit; 3 / 2 / 1 on net profit). The orientation is declared. The text then says at each record length what a span of that many readings can honestly promise, tested against resolved readings. **The gap:** the FY2030 net-profit row prints **Low 31,265 · Point 11,219 · High 31,265** — the two bounds equal and the point outside them. It is arithmetically what a one-observation span is (11,219 × 2.79), and the study calls it out in its own paragraph — "a span of one reading is not a span at all, and it is printed with its count rather than dressed up" — but the table as printed reads to a reader as an error, and a one-observation row would be better left blank with its reason than printed as a range. |
| 57 | The crux sensitised in real observable units | **PASS** | §1.9 prices the crux on its own axes and nothing else: the MNT-Halan mark in EGP million (6,918 / 13,835 / 15,734‡ / 20,753 / 27,671‡) against the first-year cost of capital (21.1% → 25.1%), spanning EGP 26.3 to 65.6 per share, with the two published branches marked ‡ and the two published answers in bold. A second grid holds the associate at the round and moves the rate path against terminal growth. **§1.1's mark ladder does the same in a third unit** — the mark, the implied whole-company valuation in USD, and the sum of the parts at each rung — and **its top row reproduces the traded price exactly (28.98)**, which is the check that the ladder is arithmetic rather than illustration. `recalc.py` asserts both grids reach their own branch to six decimals. |
| 58 | **THE ANSWER** — the branches against the latest known price, and the gap review [R-GAP-01] | **PASS** | `python3 scripts/check_valuation_gap.py` → `GBCO +58.0% GAP_REVIEW_10-09-2026.md (audits 45.7826)`, `OK — no new violations`. **Both branches sit ABOVE the price: EGP 45.7826 = +58.0% and EGP 56.7795 = +95.9% against EGP 28.98 of 3 September 2026**, recomputed independently. The trigger is two-sided, so a review is owed and is delivered: `GAP_REVIEW_10-09-2026.md`, 180 lines, **all eight headings present and none a stub** — LATEST FILINGS (l.40) · BASE YEAR (52) · MACRO COHERENCE (59) · DISCOUNT RATE (72) · TERMINAL (90) · BALANCE SHEET (108) · CLAIMS AGAINST THE RECORD (126) · MULTIPLE CROSS-CHECK (139) — plus a verdict naming three things that could make this the study that is wrong. It carries the AUDITED CENTRAL and AUDITED GAP markers for **both** branches and states the rule it applies to a two-sided answer: every branch is audited. **It also names its own mover in one line:** the terminal risk-free moved 12.50% → 10.50% because the house structural real rate moved 5.5% → 3.5%, and the review says "THE DIRECTION IS NOT THE REASON … It would have been taken had it done the opposite." |
| 59 | [R-GAP-02] — the publish block, and what a reader receives today | **HELD, correctly — and the live site carries a retired answer** | `python3 scripts/check_publish_block.py` → `HELD GBCO inside the band at +95.9% of 28.98 (2026-09-03), but Phase 1 is not proven — 2 of 6 acceptance criteria open`. The deviation half does not hold this study: [R-GAP-02] fires only where a branch sits more than 10% BELOW the price, and both are above. What holds it is the method proof, which holds the whole book. **The consequence, stated because it is a fact about the reader:** `assets/data.js:1186` still publishes `fair: {bear: 23.3, base: 35.7, full: 51.0}` with `fairAsof: "2026-07-07"`, the retired four-lens blend and the retired 10% complexity discount in its note, and links `GBCO_Valuation_Study_08-07-2026_public.docx/.pdf` and `GBCO_Valuation_Model_08072026_public.xlsx`. `check_published_gap.py` ratchets it at `GBCO +21.0% fair 35.7 spot 29.51 listed`, and `published_lens_outstanding.json` names `GBCO/study/index.html` for the retired blend. The study directory is two editions ahead of the site, and that is the block working rather than failing. |
| 60 | Delivered-document vocabulary by shape, sign convention, table totals | **PASS** | `python3 scripts/check_delivered_vocabulary.py` → GBCO under `CLEAN (23)`: no standing-rule identifier and no repository path in any delivered document (this is the shape gate and does not substitute for row 19). `python3 scripts/check_table_footing.py` → `GBCO tables examined: 46; unreconciled totals: 0`. `python3 scripts/check_sign_convention.py` → `OK — no new violations`, 0 studies on the ratchet; GBCO prints deductions as magnitudes in brackets with the operation in the label, one convention throughout, and says so in two captions. `python3 scripts/check_delivered_pdf_currency.py` → `OK — every delivered PDF carries the answer its study publishes`. |
| 61 | The study directory's own integrity while this audit ran | **PASS, with the movement recorded** | `git status --short engine/gbco_study/` returns two modified files: `compute.py` and `study_numbers.json`. **The directory changed under the audit** — `study_numbers.json` was rewritten at **07:31:28** on 13-09-2026, after the delivered documents were built at 07:00–07:01. I diffed rather than assumed: the **only** change is the addition of a top-level `"standard_version": "2026.09.10"`, added by an uncommitted ten-line block at the end of `compute.py` so that `engine/campaign_queue.py` can read the stamp. `git diff --numstat` → **2 insertions, 1 deletion**. No valuation figure moved and no delivered figure is affected. Recorded because a generator and a numbers file out of step with the documents built from them is the state in which a silent divergence hides, and because a probe that finds a directory moving should say so rather than average over it. `check_record_survives_rebuild.py` → `GBCO compute.py survives`; `check_numbers_generators.py` → `OK`. |
| 62 | **What do the filings disclose that the model does not consume?** — asked explicitly, against the sweep | **ANSWERED, with seven items named, one CLOSED since 07-09 and one NEW** | **(1) GB Capital's revenue by company.** Table 13 of every release breaks the lending leg into GB Lease, Drive, GB Auto Rental, GBBR, Capital Securitization and Kredit; the model consumes the total on one growth path. The driver record names this itself. **(2) GB Capital's cost of funds, disclosed separately from its cost of sales.** Read for the cost-of-debt derivation (FY2025 EGP 3,756.9mn) and then the segment is forecast on a typed blended margin path — a disclosed cost line consumed for the discount rate and not for the forecast. **(3) The working-capital components.** Receivables, inventory and payables are all disclosed and none is separated (row 27). **(4) The disclosed capital-expenditure history — CLOSED.** FY2025's EGP 3,664.2mn is now the alternative framing of a priced contested judgement, a risk-register row and a driver-ledger entry (row 47). This is the item the question was invented to find and it took two editions. **(5) Commercial-vehicle and light-mobility volumes for FY2023 and FY2024.** The unit build reaches back three years on passenger cars and one year on the other two; the annual-report business reviews carry earlier volume disclosure the study does not reconstruct. **(6) NEW — the plant capacities GB Corp DOES publish.** The 5,000 bus bodies and 80,000 two/three-wheelers are now read, registered and **consumed by nothing**: no line compares them with the two forecasts they govern, and one of those forecasts runs past its disclosed capacity (row 21). A registered input no line consumes is the defect [L-018] names, and this edition created a fresh instance of it while closing an older one. **(7) The disclosed useful-life rate ranges** — read, tested by both admissible routes, and correctly refused (row 52), the one item where NOT consuming is the right answer. |

---

## Gates run from outside for this edition

Every one re-run against the 13-09 rebuild, not quoted from an earlier pass.

`check_calibration_deliverables` · `check_study_provenance` · `check_standard_claim` ·
`check_edition_date` · `check_document_structure` · `check_workbook_structure` ·
`check_workbook_values` · `check_waterfall_assertions` · `check_table_footing` ·
`check_sign_convention` · `check_figure_opacity` · `check_figure_axes` ·
`check_column_widths` · `check_band_vocabulary` · `check_lens_vocabulary` ·
`check_published_lens_vocabulary` · `check_delivered_vocabulary` ·
`check_delivered_pdf_currency` · `check_bibliography` · `check_four_field` ·
`check_ground_up` · `check_prose_figures` · `check_lens_design` · `check_lens_independence` ·
`check_output_records` · `check_output_sanity` · `check_valuation_gap` ·
`check_publish_block` · `check_published_gap` · `check_cost_of_capital` ·
`check_ke_reproduction` · `check_macro_coherence` · `check_macro_anchor_age` ·
`check_forecast_anchor` · `check_anchor_ordering` · `check_asset_base` ·
`check_asset_base_wired` · `check_bridge` · `check_bridge_reaches_answer` ·
`check_terminal_floor` · `check_terminal_basis` · `check_terminal_spread` ·
`check_terminal_record_shape` · `check_source_integrity` · `check_source_rebinding` ·
`check_sweep_module` · `check_forward_ranges` · `check_rebuild_ledger` ·
`check_artefact_currency` · `check_eps_reconciliation` · `check_walkforward_scope` ·
`check_walkforward_actuation` · `check_valuation_inputs` · `check_driver_coverage` ·
`check_corrections_applied` · `check_frozen_escalator` · `check_numbers_generators` ·
`check_record_survives_rebuild` · `check_harness_outputs` · `check_escalations` ·
`check_exemplar_debt` · `check_protocol_sync`

**Seven of these came back RED book-wide, and not one of them names GBCO:**
`check_bridge`, `check_macro_coherence`, `check_output_records`, `check_forecast_anchor`,
`check_rebuild_ledger`, `check_walkforward_actuation`, `check_corrections_applied` all fail
on **ADIB**; `check_column_widths` on **PHDC**; `check_ke_reproduction` on **FERTIGLOBE**;
`check_published_gap` on **ELEC**; `check_edition_date` on GBCO's own **superseded 08-07**
document, ratcheted. `check_calibration_deliverables` was RED on GBCO and this file closes it.

**Every one of the nine blocking rows above is a defect no gate in this repository
inspects.** No gate reads the pixels of a figure's annotations; none compares a forecast
volume with a capacity the company publishes; none asks whether a sentence describing an
identity produces the number beside it; none pins a news item's legal entity; none notices
that a document issued on one date cites a search run on another. That is why the pages are
read.

Plus the study's own instruments, re-run against the rebuilt files:
`recalc.py` (851/851 formula cells + 37 reconciliations + 2 structural gates),
`scrub.py` (0 hits), `prose_check.py` (272 figures, 0 unmatched),
`footing_check.py` (46 tables, 0 unreconciled).

---

## What would close this gate

Named so the work is countable rather than remembered. Nothing below is done here: an
auditor who repairs the study becomes its author.

1. `docx_A.py:218` — print GB Auto's own equity value, or relabel the sentence as the two
   operating legs together. One word or one variable.
2. `figures.py:129` — format a NaN cell as the `n.m.` the table beside it already prints.
3. `docx_A.py:696` — read the construction from `ke_construction` and publish the two
   components the record already holds (4.23% and 5.18%).
4. Re-issue the document with an issue date that is the day its content was made, or move
   the two new blocks to a new edition.
5. Run the capacity test against the two lines GB Corp publishes capacity for, and either
   carry the commercial-vehicle breach as a priced risk or state the perimeter difference
   that makes 5,000 the wrong comparator.
6. Establish whether `MNT Tech Holding for Financial Investments` is the entity GB Corp
   holds 41.61% of, or record the item as UNVERIFIED and delete the sentence that says a
   listing would test a branch.
7. Build the asset-conversion cycle from the disclosed receivables, inventory and payables.
8. Source, date and register one peer figure.
9. Call `assert_sigcm()` and `assert_model_study()` in `compute.py`.
