# ADNOCLS — QC gate, 07-09-2026

ADNOC Logistics & Services plc · Abu Dhabi Securities Exchange: ADNOCLS · ISIN AEE01268A239

Edition of 9 August 2026, re-struck 7 September 2026 on the reviewed six months to
30 June 2026. Audited from outside the study, by a reader who did not build it.

**VERDICT: NOT DELIVERABLE.** Five rows are RED and four more pass only with a gap named.
The model itself is in good order — the workbook rebuilds cell for cell, the document text
rebuilds block for block, the beta conforms, the bridge stands on the latest sheet, SIGCM
holds clause by clause and every repository gate is green. What fails is the PAPER. Eight of
the nine figures a reader receives are a vintage behind the numbers beside them and
contradict their own captions; three passages of prose still describe the retired weighted
blend as though it built the central and one of them contradicts itself inside a single
sentence; the primary-documents table in both delivered documents names none of the four
half-year filings this whole re-strike is built on and still tells a reader the valuation
stands on the first quarter; and the Driver Ledger carries no entry for this name at all.

This is the exemplar. Every study written afterwards is built by opening it, so each of
these travels by imitation whether or not anybody decides to take it on.

---

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker · market · exchange | ADNOCLS · AE · ADX | `assets/data.js` carries the entry under the `ADX:` code prefix, read through a real JavaScript load rather than a regex. The exchange is read from that prefix and never from the `raw_ohlc/AE/` folder, which groups by market code and spans ADX and DFM. |
| Class | marine logistics and shipping, chartered fleet on global day rates | `study_numbers.json → lens_record.class`, which resolves in `research_protocol.LENS_REGISTRY` by import. `python3 scripts/check_lens_design.py` prints `ADNOCLS primary dcf, 3 cross-checks` and passes. |
| Reference pattern | ADNOCLS is itself the model report | `REFERENCE_SET` asserts at import on exactly ADNOCLS, ADCB, ALPHADHABI. The comparison document opened beside this audit is `engine/model_report/MODEL_REPORT_09-08-2026.docx`. |
| Standard version claimed | `2026.09.07` | `study_numbers.json → meta…standard_version` = `2026.09.07`; `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` prints `2026.09.07`. The stamp is the live standard, which is the one naming `GAP_REVIEW_{DD-MM-YYYY}.md`. |
| Committed central · strike | AED 5.3385 against AED 6.80 as at 7 September 2026 | `study_numbers.json → central` = 5.338494934992969; `meta.spot` = 6.8, `meta.spot_date` = 2026-09-07. The price is a committed artefact at `engine/prices/SUPPLIED_07-09-2026.json`, supplied by the principal for this name on that day. |
| Gap | **−21.5%** | `python3 engine/prices/gap_today.py` prints `ADNOCLS 5.338 6.80 6.80 −21.5% −21.5% BREACH`. |
| Route to the answer | nine levers, 5.6054 → 5.3385 by way of 6.6100 | `rebuild_ledger.json`, walked by `python3 scripts/check_rebuild_ledger.py`: `ADNOCLS 9 lever(s), 6 rule(s), cumulative −4.8%`. |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | Structure matches the model report — 16 Word sections, in order | **PASS** | Extracted heading list from the delivered `.docx`: masthead + READ FIRST · Headline · Valuation summary — every read at a glance · Company overview · 1 Fundamental valuation (1.1–1.9) · 2 Technical and price structure · 3 A probabilistic price map · 4 Comparison of the lenses · 5 Catalysts to watch · 6 Reading the probability zones · 7 Caveats and what would change our mind · Appendix A · Appendix B · Appendix C · About this series · Disclosure and disclaimer. Sixteen top-level blocks in `MODEL_STUDY['word_skeleton']` order. |
| 2 | Structure matches the model report — 16 Excel sheets, in order | **PASS** | `wb.sheetnames == research_protocol.MODEL_STUDY['excel_sheets']` returns `True` — READ FIRST, Summary, Fundamental Valuation, Assumptions, SOTP Bridge, Segments, Relative & Normalized, DCF, Income Statement, Balance Sheet, Cash Flow, Summary Financials, Monte Carlo, Sensitivity, Per-Share & Ratios, Peer & Sector. `python3 scripts/check_workbook_structure.py` lists ADNOCLS in `MATCHES THE MODEL REPORT (21)`. |
| 3 | Depth-bar 1 — standalone bibliography document | **FAIL** | The document ships (`ADNOCLS_Bibliography_09-08-2026.docx` / `.pdf`, 45 pages, 17 tables) and `python3 scripts/check_bibliography.py` prints `OK — every delivered study ships the standalone bibliography the bar requires`. **Its primary-documents table is a vintage behind.** Table 3 of the bibliography lists 17 documents and NOT ONE of the four half-year filings this re-strike stands on; it names `Condensed consolidated interim financial information · 14 May 2026 · FS_Q1_2026.pdf` and stops there. All four are on disk in `engine/adnocls_study/filings/` (`ADNOCLS_Q2_2026_FS_EN.pdf`, `ADNOCLS_MDA_Q2_2026_EN.pdf`, `ADNOCLS_ER_Q2_2026_EN.pdf`, `ADNOCLS_Presentation_1H_2026.pdf`) and 144 of the 778 registered inputs cite them in their source field. The register beside the table is current and the table naming what was READ is not. The same list is reprinted in the study's own Appendix B.3, where the Q1 row still reads "the balance sheet the valuation is built on" — false since lever three moved the bridge to 30 June 2026. And bibliography table 15 is headed **"At 31 Mar 2026"** over the 30-June figures (USD 257.3m net debt, USD 1,861.6m as the study uses it). The list is typed at `docx_biblio.py:497–570`, not read from the sweep register, which is why the sweep could move and the table could not. |
| 4 | Depth-bar 2 — four-field provenance on every input, validated by assertion | **PASS** | 778 inputs in `study_numbers.json`. Counted from outside the study: `missing value/source/date: 0`, `missing layer: 0`. Layers: Company 744 · Country 17 · Market 9 · Industry 6 · Global 2. Rendered in the bibliography's input register at 745 + 7 + 18 + 3 + 10 rows across five tables. `python3 scripts/check_input_provenance.py` does not exist under that name; the field is validated inside `research_protocol` and re-counted here independently. |
| 5 | Depth-bar 3 — numeric traceability, no financial numeral typed into a builder | **PASS** | `python3 scripts/check_prose_figures.py` → `ADNOCLS prose figures checked: 814; unmatched: 0`. Every builder reads `study_numbers.json`; the one hand-typed list found is the primary-documents table at `docx_biblio.py:497`, which carries no financial numeral and is failed under row 3 for currency rather than here. |
| 6 | Depth-bar 3 — the delivered workbook recalculated independently | **PASS** | `python3 recalc.py` on the delivered file: **"2,244 of 2,244 formula cells reproduce the model, 0 unresolvable, 0 unchecked"** plus **121 headline reconciliations** against `study_numbers.json`. `python3 lo_recalc_gate.py`: 16 sheets recalculated through a second, independent engine, **0 error cells of any kind**. `python3 driver_test.py`: **113 drivers each reprice the workbook in the asserted direction, 0 dead inputs**. `python3 label_gate.py`: **170 labels checked, every one describes its own row**. `python3 scripts/check_workbook_values.py` lists ADNOCLS in `CLEAN (21)`. Nothing was unparseable and nothing was skipped. |
| 7 | Script-built files verified by a cell-by-cell diff against the delivered file | **PASS** | The repository was copied to a sandbox and `build_xlsx_adnocls.py` re-run there from the committed numbers. Cell-by-cell comparison of the rebuilt workbook against the delivered one: **10,074 cells compared, 0 differing**. The same for the documents: `docx_adnocls.py` rebuilt, **2,339 text blocks, 0 differing**; `docx_biblio.py` rebuilt, **3,556 text blocks, 0 differing**. A clean recalc is necessary and not sufficient, and the diff was run as well. |
| 8 | Depth-bar 4 — external-reader scrub, zero hits | **PASS, with one gap named** | `python3 qc_checks.py` scans the delivered documents against 51 patterns: **study 0 hits, bibliography 0 hits, workbook 0 hits**; `python3 label_check.py ADNOCLS_Valuation_Study_09-08-2026_public.docx` → `scrub clean: 0 hits across 262 paragraphs and 51 tables`. `python3 scripts/check_delivered_vocabulary.py` lists ADNOCLS in `CLEAN (22)` on the shape test for rule identifiers and repository paths. `research_protocol.band_record`-class verdict tokens: `python3 scripts/check_band_vocabulary.py` → `OK — no verdict vocabulary in any page text`; PASS/PARITY/FAIL and CRPS appear nowhere beside the company name, and there is no calibration appendix — the document has exactly three appendices and §3 carries the statistics inline in plain sentences. **The gap:** `compute.py`'s `_scrub_attestation()` refuses a scrub result that names the wrong FILENAME but not one measured on a different EDITION of the same file. The committed `scrub_result.json` records `chars: 475086`; re-running the scrub on the delivered documents now measures **475,682** — 596 characters more, so the attestation the study reads back was struck on a shorter edition than the one that shipped. No leak resulted (the re-run is still 0 hits) and the hole is real. |
| 9 | Depth-bar 5 — figure discipline; every figure inspected as a rendered image | **FAIL** | Opacity is clean: `qc_checks.py` reports `transparent=False, corner luminance=[248,248,248,248]` on all nine, and `python3 scripts/check_figure_opacity.py` → `OK — every delivered figure is opaque to the pixel`. **The figures are a vintage behind the study.** `figures.py` was re-run in the sandbox from the committed `study_numbers.json` and diffed against the delivered PNGs: **8 of 9 differ** (only `fig3_ma.png`, the moving-average chart, is identical). Read as rendered images, the delivered figures contradict the tables and captions printed beside them: **Figure 1** draws the field at 5.61 / 8.02 / 8.64 / 8.92 / 3.66 with a market-price line labelled **6.16**, against a table two inches below giving 5.34 / 7.37 / 12.21 / 11.07 / 3.91 and a caption reading "against the market price of AED **6.80**" — the regenerated figure draws exactly the table's numbers and labels the line 6.80. **Figure 2** draws forecast earnings 2,086 / 2,518 / 2,507 / 2,312 / 2,059 and a group margin rising to 44%, against §1.6's own table of 2,716 / 3,140 / 2,924 / 2,426 / 1,880 and 38.9% falling to 31.9%. **Figure 3** draws an implied first-quarter very-large-crude rate of 199,838 where the table under it gives 151,440, and a second-quarter published rate of 260,000 where §1.7 gives 291,145. **Figure 4** carries the title "bold = within 0.35 of the market price of **6.16**" under a caption naming 6.80, and its grid prints 5.61 at the adopted beta and growth where the table beneath prints 5.34. **Figure 6** draws its two fundamental lines at 5.61 and 8.02 under a caption reading "the two fundamental centrals: AED **5.34** … and AED **7.37**". **Figure 9** draws expert bases of 7.33 / 6.25 / 4.58 and a panel centre of 6.06 against tables giving 9.89 / 9.35 / 4.67 and a caption naming a panel centre of 7.97. The cause is mechanical and the fix is one command: `docx_adnocls.py` embeds the PNGs on disk, and the re-strike ran compute → workbook → documents without re-running `figures.py` (PNGs stamped 12:24–12:32, numbers file 19:19). |
| 10 | Depth-bar 6 — table discipline, three counts per document | **PASS** | `python3 qc_checks.py`: study **51 tables — over-wide 0, starved 0, bloated 0**; bibliography **17 tables — over-wide 0, starved 0, bloated 0**. `python3 scripts/check_table_footing.py` → `[ok] ADNOCLS tables examined: 63; unreconciled totals: 0`. `python3 scripts/check_waterfall_assertions.py` → `ADNOCLS OK … 8 waterfall table(s)`. `python3 scripts/check_sign_convention.py` → 45 documents read, 98 tables carrying a deduction row, **0 findings**. |
| 11 | Depth-bar 7 — expert appendix at maximum detail | **PASS, with the stale figure named at row 9** | C.1 Expert 1 (mid-cycle earnings power), C.2 Expert 2 (owner cash earnings), C.3 Expert 3 (cash returns against the cost of capital). Each carries a worldview; a when-it-works / when-it-fails paragraph; a worked table with every intermediate line (Expert 1 runs 13 lines from mid-cycle EBITDA to AED 9.89; Expert 3 runs invested capital → economic profit → terminal → bridge to AED 4.67); a named sensitivity with numbers ("each single turn of the multiple is worth AED 0.82 a share"; "capitalise the same owner cash earnings at 7.42% and the answer is roughly AED 12.81, a rise of about 37%"); and a falsifier stated in advance ("two consecutive years in which group EBITDA falls below USD 1.4 billion"; "a year in which free cash flow falls below the ordinary distribution plus the perpetual coupon"; "a return on invested capital settling below the cost of capital for two consecutive years"). C.4 cross-examines four challenges, each conceded or rejected with the receipt. C.5 puts the three in one room. C.6 is a six-row divergence table isolating which assumption drives which gap — and it names an inconsistency against the panel rather than smoothing it (Expert 2 runs a different bridge, worth AED −0.16, stated in C.2). |
| 12 | Experts labelled "Expert 1/2/3" only — no persona name anywhere | **PASS** | Headings read "C.1 Expert 1 — mid-cycle earnings power", "C.2 Expert 2 — owner cash earnings", "C.3 Expert 3 — cash returns against the cost of capital". Full-text scan of both delivered documents returns no persona name; the scrub's own pattern set covers it and reports 0 hits. |
| 13 | Depth-bar 8 — the central contested judgement computed both ways, published side by side | **PASS** | The judgement is what stands for "the market" in the beta. Both constructions are carried at full size in all four required places: the **summary table** (a separately banded ALTERNATIVE READINGS block giving AED 7.37 on the equal-weight composite against the central 5.34); the **body** (§1.1 prints the two constructions as parallel columns down to fair value per share, and §1.8 prints the regression evidence for both — 1.103 against 0.690, R² 0.181 against 0.167); the **workbook** (25 cells name the alternative, including `Summary!A12–A13`, `Fundamental Valuation!A20–A24`, and a whole second cost-of-capital and DCF column); and an **expert's range** (C.2's named sensitivity runs the same owner cash earnings at 7.42% and publishes AED 12.81). Never averaged: `python3 scripts/check_lens_vocabulary.py` lists ADNOCLS among the 11 clean documents. |
| 14 | Step 0.0 data-quality gate | **PASS, with one gap named** | `step0_result.json`: **795 clean rows**, `dq_log: []`, density **249.89 sessions a year** against the ADX calendar, span 3.18 years; the index passed the same screen with `dropped 1 interior stale/no-trade rows` (`beta_result.json → index_dq`). **The gap:** the gate was run on the library as it stood at 2026-08-07 (795 rows). `engine/raw_ohlc/AE/ADNOCLS.csv` now carries 814 rows to 2026-09-07 — the twenty sessions that produced the 6.80 the study is struck against have not themselves been through Step 0.0. |
| 15 | Step 0 calibration under the band record | **PASS, with one gap named** | `step0_result.json`, three-month horizon, 8 independent non-overlapping windows, origins 2024-06-21 to 2026-03-24: coverage **37.5% / 75.0% / 87.5%** against 50 / 80 / 90 targets — **the count is printed beside every percentage**, in §3 and here. Two-sided binomial at the 5% level does not reject on any band at n = 8. One-month horizon: **24 windows**, coverage 67% / 88% / 96%, uniformity p = 0.48. Width ratio **w90/w90_b = 1.18** — disclosed in §3 ("the bands are about 18% wider than the benchmark's, which is a real cost"), never gated. The share listed in June 2023 so a five-year set of origins does not exist for it; the width setting rests on the 18-name Abu Dhabi panel over 261 windows and §3 says so in plain language. **The gap:** as row 14 — the record was struck on the 2026-08-07 library and the one-month check date it publishes (2026-09-07) has arrived today, so §3's shorter horizon is a claim that has already resolved and is not graded in the document. |
| 16 | SIGCM clause 1 — historicals from official sources only | **PASS** | `python3 scripts/check_source_integrity.py` → ADNOCLS neither breaching nor unreadable; no named commercial vendor or news outlet appears in the source of any dated historical without a company document named beside it. Four complete audited fiscal years held and read from the filings themselves (`FS_FY2022.pdf` … `FS_FY2025.pdf`, with `AR2024`/`AR2025` for the machine-readable text), plus the reviewed interim to 31 March 2026 and the reviewed interim to 30 June 2026. The three comparator multiples in §1.3 come from aggregator statistics pages and are labelled at the table, in Appendix B.3 and in the negative-results list as third-party cross-checks, never as the subject's own history. |
| 17 | SIGCM clause 2 — forecast built ground-up, on a driver record | **PASS, with the gap the record itself states** | `compute.py:2834` calls `RP.assert_ground_up(_gu, ticker='ADNOCLS')` on seven `DriverLine` records, and `python3 scripts/check_ground_up.py` re-runs the assertion from outside: `OK — every committed driver-line record passes the ground-up assertion when it is actually run`. Committed split, reported rather than ticked: **`unit` 64.79% of revenue · `derived` 0% · `segment` 35.21% · `topdown` 0%**, covering 100.0%. The two unit lines name their unit, its source and the price basis (tankers: vessel-days × the company's own published time-charter-equivalent rate by class by quarter, cost as a fixed base plus a variable component per unit of fleet charter-equivalent revenue; gas: contracted vessel-years × day rate). Each of the five `segment` lines carries a gap note naming what the disclosure stops at — "the company discloses segment revenue and the contracted backlog but no unit — no vessel-days, no tonne-miles, no day rate" — and each records that the margin is the input with cost as the residual. |
| 18 | SIGCM clause 3 — debt split local-currency / foreign-currency | **PASS** | `cost_of_capital_record.kd_integrity.currency_composition`: "the drawn book is entirely US-dollar denominated, which under the peg IS the local currency for this purpose: a shareholder facility, third-party bank borrowings and lease liabilities, no other-currency tranche disclosed". §1.8's cost-of-debt table sets out six instruments individually with their bases and rates. |
| 19 | SIGCM clause 4 — asset-conversion cycle studied, balance sheet and cash flow projected from it | **PASS** | Working capital is receivables at 110.1 days on revenue, inventory at 14 days on operating cost, payables at 132 days on operating cost, all measured from the statements; §1.1's second table sets out why the receivable day count was re-based from 108.4 to 110.1 when the revenue gross-up moved. Appendix A.3 carries the forecast balance-sheet and cash-flow markers with net working capital rolling 732 → 485 and the change in working capital appearing as a live line in the free-cash-flow waterfall. |
| 20 | SIGCM clause 5 — competitors studied for KPIs and multiples | **PASS** | §1.3 and Appendix B.1 carry Qatar Gas Transport (Nakilat), Frontline plc and International Seaways with market, business model, EV/earnings, price/earnings forward and trailing, and price/book, each with a stated caution. The absence of a single clean comparable is itself recorded as the reason the relative lens is built as a blend and cross-checked by the sum of the parts. |
| 21 | SIGCM clause 6 — the beta, inspected on its record and not on a boolean | **PASS, with one gap named** | `beta_reg.py:70` calls `own_stock_beta('ADNOCLS','AE','ADX')` — the only sanctioned producer — and `compute.py:2901` calls `RP.assert_beta_provenance(beta_res, tier2_fallback_documented=False)`. The record: `index_file = raw_indices/AE/FADGI.csv`, which is the registered `EXCHANGE_INDEX[('AE','ADX')]` and sits under `engine/raw_indices/`, not in any panel. The exchange was read from the `ADX:` prefix in `assets/data.js`, not inferred from the folder. Not a DFM name, so `wacc_builder.index_interim_note('AE','DFM')` does not apply and `interim_note` is correctly `null`; not Qatari, so no QATAR10 caveat applies. Usability gate: **n = 159 ≥ 24 · R² = 0.1814 ≥ 0.05 · SE(β) = 0.3147 < |β| = 1.1032** — the fit passes, so no fall to a peer beta or 1.0 is owed. The equal-weight composite (0.690, 12 names) is computed and published as the study's largest contested judgement and is never the adopted number. `python3 scripts/check_study_provenance.py` passes ADNOCLS. **The gap:** the record's own disclosure is one vintage behind. It states "the last stock session used elsewhere in the study (2026-08-07). 2 weekly stock observations therefore fall outside the regression"; the study is now struck on the 2026-09-07 close and the bibliography says so in the same sentence ("the stock's own history runs to 7 Sep 2026"). Against a last paired observation of 2026-07-17, roughly **seven** weekly observations now fall outside, not two. |
| 22 | SIGCM clause 7 — every constructed statement is a live formula model | **PASS** | The delivered workbook carries **2,244 formulas** against **439 pasted numeric cells**; every formula cell is covered by the builder's own expected map and every one reproduces (row 6). The driver test moves 113 inputs and every one reprices the workbook in the asserted direction, with 0 dead inputs — driver → income statement → balance sheet → cash flow → DCF propagates. |
| 23 | SIGCM clause 8 — gaps flagged before issue; stop-and-inform honoured | **PASS** | Nine negative results are published in the bibliography and repeated in §7 and Appendix B.3: no independent broker vessel valuations reachable (so no net-asset-value lens, and the book lens stands in as a disclosed floor at no weight); no per-vessel gas charter rates disclosed; medium-range quarterly rates absent for FY2024 and handysize not broken out at all, both substitutions labelled wherever they appear; vessel-level running costs not disclosed, so the cost stack is SOLVED and labelled as solved; the comparators' own filings not used and their multiples not recomputed, with the consequence stated at the table; no UAE sovereign credit-default-swap entry, so only one premium basis exists and the study says so rather than omitting the comparison. The gas fleet's contracted vessel-year path is the sharpest case: the first-half deck's table is ambiguous on its face, so the coarser reading is kept, the finer one is published as a contested judgement worth −14.6%, and neither is invented. |
| 24 | The terminal — is the STOP properly recorded? | **PASS on the recording; the construction stays outstanding, named** | `python3 scripts/check_terminal_floor.py` → `ADNOCLS implied cycle 50.0 years against 1/g of 50.0, charge 20.5% of terminal profit`, and `OK — no new terminal carries the 1/g construction … and the list has not grown`. The stop is recorded in three places and each is dated: `terminal_outstanding.json` carries the ratchet entry with its measurement; `TERMINAL_EVIDENCE_05-09-2026.md` records the closed routes so the next session does not re-derive them; and `compute.py:2002–2072` carries the reasoning inline beside the arithmetic it refuses. `terminal_value.build()` was re-run on this re-strike's own figures — terminal NOPAT 1,081,231, book D&A 746,550, disclosed 25-year vessel life against a FY2025 gross base ex capital work in progress of 7,595,070, zero real growth — and refused with `implied payout of terminal NOPAT is 140.1%, outside [0, 1]`. The reason is a disclosure gap and not a parameter: dry-docking is maintenance capitalised and amortised over 2–5 years (60 months for a major dry dock, from the policy note), the filings split no vessel line by component, and the four annual accounting-policy notes were all re-opened to confirm it. A life this desk chose is not a disclosed life and none was chosen. The refusal is sharper than at the last test (140.1% against 117%) because the rate reversion carries 2030 profit down while the fleet keeps growing, and the file says so. |
| 25 | v2 cost of capital — country risk once, marginal Kd, market-value weights, both ERP bases | **PASS** | `python3 scripts/check_cost_of_capital.py` → `ADNOCLS AE, 8.71% gliding to 8.68% [cost-of-debt check unavailable on this disclosure, stated]`, no new violation. The record: rf observed 4.48% (dirham Treasury tranche maturing Jan 2031, July 2026 auction) **less** the UAE's own adjusted default spread 0.42% = rf\* 4.06%, with the country charge re-entering once through the 4.87% premium — the double count is named in §1.8's contested table and priced. Kd pre-tax 6.35%, built as the plain average of three constructions from six disclosed instruments and sitting **above** the 4.48% sovereign as a same-currency corporate must. Weights are market-value throughout — ordinary equity 83.1% on a market capitalisation of USD 13,699mn, debt 4.9%, perpetual capital securities 12.0% at their own 4.90% coupon; book equity is never used. Terminal rf 3.98% is DERIVED from the house path (2.0% inflation objective plus a 1.25% real convention), never quoted. Both premium bases are addressed: the rating basis is central and the swap basis is stated as unavailable for this sovereign rather than omitted, with the ~70bp gap Gulf comparators show given as the construction uncertainty. `python3 scripts/check_ke_reproduction.py`: ADNOCLS is one of the three records that reproduce, declaring `beta_source: own_stock_regression` and `ke_terminal_construction: same_beta`. |
| 26 | 3y historical + 5y forecast, full FCFF waterfall shown inline to PV of FCFF | **PASS** | Appendix A.1 and A.2 carry FY2023 / FY2024 / FY2025 actual and FY2026E–FY2030E forecast; four complete audited years were obtained against a floor of two. §1.1 prints the waterfall inline, every line: revenue → less operating costs → EBITDA → less depreciation and amortisation → EBIT → net operating profit after tax → add back depreciation and amortisation → less capital expenditure → less the movement in working capital → **free cash flow to the firm** → less the first half already inside the balance sheet → cost of capital that year → discount factor → **present value of free cash flow** (293 / 1,512 / 1,368 / 1,357 / 919), then the terminal block and the enterprise-to-equity bridge to AED 5.34. It does not stop at FCFF. |
| 27 | One escalator per driver class, with a sourced near-term cost anchor | **PASS** | The tanker cost stack is two legs on two different drivers, and §1.6 says so: a fixed base of USD 530mn a year escalated at **2% on wages and technical management** — a services escalator, taken from the house path, not a commodity index — and a variable component of **1.05× every dollar of owned-fleet charter-equivalent revenue**, which carries voyage cost, charter-in hire and the relet book and therefore moves with the global freight market rather than with domestic inflation. `macro_record.growth_lines` registers charter and freight rates with an explicit exemption ("US-dollar day rates set by the global tanker and offshore market … no inflation rate is applied to them at all") and `opex_escalation_path` states it is "applied to every dirham cost line". The near-term anchor is sourced and measured, not a macro assertion: both cost parameters are solved from the audited FY2025 year and the reviewed six months to 30 June 2026 **together**, with the reviewed six months to 30 June 2025 **held out** and reproduced to within 10.7%, understating. |
| 28 | Margins as OUTPUTS wherever the filings disclose enough to build cost per unit | **PASS** | §1.6's own table is headed "What the build produces — margins as outputs" and the group margin moves 38.9% → 31.9% as an arithmetic consequence of the mix. The re-strike replaced four typed segment margins with four measured ones and they moved in both directions (Offshore Contracting 0.385 typed against 0.316 measured; Offshore Services 0.287 against 0.241; Dry-Bulk 0.184 against 0.355; Services 0.200 against 0.275), and the gas margin moved from a typed 0.70 "near the 2025 outcome" to a measured 0.4966. Where no unit is disclosed the margin is the input and the record says so at the line rather than in prose (row 17). |
| 29 | Sweep register — primary-source depth | **PASS** | `python3 sweep.py` → `register valid — 30 findings {B:3, S:8, D:14, C:2, NEG:3}, 12 drivers`, `PASS — 4/4 rings … primary access: attempted (4) | FS years: 4 ['FY2022','FY2023','FY2024','FY2025'] | IR findings: 10`. It runs through the shared module: `sweep.py:13` imports `research_sweep` and calls `validate()`; `python3 scripts/check_sweep_module.py` lists ADNOCLS in `RUNS THROUGH THE SHARED REGISTER (16)`. The company's own site is attempted and **logged either way** — four attempts, three reachable, one (`/investors/reports-and-presentations`) recorded as a 503 from this environment with the note that the same documents were reached through two other pages. Study year FY2026 with `study_quarters_disclosed: ["Q1 2026","Q2 2026"]`, both swept before the build. Results releases are swept with their statements (the FY2025 release and the Q2 2026 release both registered). Ten findings are tagged `COMPANY_IR`, distinct from the 12 `COMPANY_OFFICIAL`. |
| 30 | Every non-sourced driver logged to the Driver Ledger | **FAIL** | `grep -n -i adnocls engine/Fundamental_Driver_Ledger.md` returns **one line, 703**, and it is a passing mention of "the ADNOCLS skeleton" inside the ELEC entry. **There is no ADNOCLS entry.** The ledger carries PHDC, TMGH, EMFD, AMOC, EGCH, ARCC, STC, ELEC, PHAR, GBCO and SCEM — including four written on 7 September 2026, the same day as this re-strike. This re-strike introduced three drivers the study itself labels solved rather than sourced, named in its own caveats: the tanker fixed cost base of USD 530mn a year, the variable component of 1.05×, and the gas-carrier day rate of USD 61,573 — plus two labelled substitutions (handysize at 0.79× the medium-range rate, medium-range FY2024 standing in on the FY2025 average). None is recorded where a later study of this class would find it. |
| 31 | The walk-forward scope decision stated in the study | **FAIL** | `study_numbers.json → walkforward_scope` commits `scope: LIGHT`, `sourceable_fiscal_years: 5`, `status: pending`, with the reasoning that the company listed in June 2023 and no pre-listing audited year was established as reachable. **Nothing reaches either delivered document.** Full-text scan of the study and the bibliography finds no statement of the scope decision and no pending note; the only occurrences of "own history" are about the beta regression and the source-integrity rule. The rule requires the decision to be stated in the study and a first edition to carry a one-line pending note, and neither is present. |
| 32 | The answer — the gap, and the review behind it | **PASS** | The committed central is AED 5.3385 against a spot of AED 6.80 struck 7 September 2026, a gap of **−21.5%**, more than 10% BELOW the price. `GAP_REVIEW_07-09-2026.md` exists in the study directory and covers all eight headings — LATEST FILINGS (§1) · BASE YEAR (§2) · MACRO COHERENCE (§3) · DISCOUNT RATE (§4) · TERMINAL (§5) · BALANCE SHEET (§6) · CLAIMS AGAINST THE RECORD (§7) · MULTIPLE CROSS-CHECK (§8) — and carries both markers, `AUDITED CENTRAL: 5.34` and `AUDITED GAP: −21.5 per cent`, so the review audits the current gap and not only the current answer. `python3 scripts/check_valuation_gap.py` → `ADNOCLS −21.5% GAP_REVIEW_07-09-2026.md (audits 5.3400)`, and `OK — no new violations`. The review is what produced the re-strike rather than following it: it records that the previous review of 4 September wrote "This study holds no such document" of a half-year filing that had been public for twenty-seven days. `python3 scripts/check_publish_block.py` → `HELD ADNOCLS the central is −21.5% below the price of 6.80 (2026-09-07), past the 10% publication limit — and no market dissent is filed`. That is the correct state and it is not a defect: the study may not publish, and it does not. |
| 33 | The bridge stands on the latest disclosed sheet | **PASS, with one label named** | `python3 scripts/check_bridge.py` → `ADNOCLS stands on the 2026-06-30 sheet; minority on the value_share basis`. The record names the register that establishes what "latest" means (`sweep_register.json`), the lines foot to the stated equity value and the equity divides to the stated per share, the minority is deducted from EQUITY at a value share with book, profit share and proportional published beside it, and the interim dividend declared 20 August 2026 is deducted because it was declared AFTER the balance-sheet date. Cash is charged exactly once — the operations are discounted at the weighted rate and the balance sheet is added in the bridge, never both. **The label:** the delivered bridge still describes joint ventures and associates as "carried at the reviewed **31 March 2026** book value with no uplift", while the rebuild ledger records that line among those "re-read at 30 June". The value may be unchanged; the note names the superseded quarter to a reader. |
| 34 | Macro coherence — one house path, every inflation-class input mapped | **PASS** | `python3 scripts/check_macro_coherence.py` passes ADNOCLS with no new violation. `macro_record` declares `market: AE`, `path_as_of: 2026-09-03`, `regime: pegged`, and an `inflation_inputs` block naming both inflation-class inputs with a closed-list mapping (`inflation_26` calendar; `opex_escalation` calendar) plus the charter-rate line with its exemption reason. Terminal growth 2.0% = terminal inflation 2.0% plus a stated real growth of zero; terminal rf 3.975% derived from the same path. `python3 scripts/check_macro_anchor_age.py` → `OK — no study newly breaches the bound`; ADNOCLS is not listed, the AE peg making a currency-anchor age immaterial by construction. |
| 35 | Forecast anchor — the latest reviewed period, with a mechanism if it steps down | **PASS** | `python3 scripts/check_forecast_anchor.py` → `ADNOCLS EBITDA margin, group, on t latest 0.4022 forecast 0.3890 −1.32pp`, no new violation. The forecast opens 3.3% below the reviewed half in relative terms, inside the 5% materiality line, and the record still names its mechanism and measures it: `contracted_price_step_down`, evidenced by the company's own published very-large-crude rate falling from 291,145 a day in 2Q2026 to 159,518 in 3Q2026 to 11 August, and by eleven of its own vessels fixed out at 19,750–72,500 with disclosed expiries to July 2028. `forecast_path` is committed and every later year is within reach of the opening year. |
| 36 | Asset base — the operating asset base is as at the latest disclosure the study read, and is consumed | **PASS** | `python3 scripts/check_asset_base.py` → 11 studies in scope, **conforming: 1**, and ADNOCLS is that one. `asset_base_record` gives 172 owned vessels as at 2026-08-07, footed from the 30 June 2026 fleet disclosure (52 tankers, 22 gas, 11 dry-bulk, 3 container, subtotals to a stated 88) carried forward by the 7 August purchase of eleven, plus 33 jack-up barges and 40 support vessels. The base is newer than the information set, which is the right direction for an ordering test. `python3 scripts/check_asset_base_wired.py` → `OK — no new decorative asset quantity`; the five fleet quantities are read by arithmetic in `compute.py`, not merely printed. |
| 37 | Output records — the reverse read, the sign test, and artefact currency | **PASS** | `python3 scripts/check_output_records.py` → `ADNOCLS price implies the single flat discount rate that repro of 7.21% against the study's 8.41%; 7 judgements, 7 material, sign test p=1.00`. The reverse read lives in `diagnostics.json`, outside the numbers file every builder reads, and no builder imports it. `python3 scripts/check_output_sanity.py` passes. `python3 scripts/check_artefact_currency.py` → `ADNOCLS contested_judgements.json built against 5.3385` and `diagnostics.json built against 5.3385`, both current. `python3 scripts/check_terminal_record_shape.py` → `RECORDS SHORT OF THE CURRENT SHAPE: 0`. `python3 scripts/check_numbers_generators.py` and `python3 scripts/check_eps_reconciliation.py` (`ADNOCLS FY2025 0.1133 computed against 0.1100 reported`) both pass. |
| 38 | The rendered PDFs read as images, page by page | **FAIL — and it is what caught everything above** | All 53 pages of `ADNOCLS_Valuation_Study_09-08-2026_public.pdf` were opened and read as rendered images, not as extracted text. That is what found the eight stale figures at row 9 — every one of which is individually opaque, individually well-labelled and individually invisible to `prose_figures`, `figure_opacity`, `figure_axes`, `table_footing` and the recalculation, because each of those instruments points from the page back to the model and none of them looks at what the image draws. It also found the §1.5 contradictions at row 39, the "within a few per cent" claim at row 40, and the Figure 4 heatmap title promising bold cells against a market price the study no longer uses. |
| 39 | Prose that matches the construction it describes | **FAIL** | §1.5's last two paragraphs still describe the RETIRED weighted blend as though it built the central, two paragraphs after the same section says in capitals that it does not. Verbatim: *"Together they carry 45% of the central. So 45% of the headline rests on one method presented as two reads … dropping the normalised lens and redistributing its weight would move the central toward the cash-flow and asset lenses."* Under `lens_record` the central IS the cash-flow lens and the normalised lens carries **zero** weight; §7 says so correctly on the same page-set ("neither carries any of the answer now"). The next paragraph then states *"**Weighted together, the four centre at AED 5.34**"* — false: weighting the four at the committed `lens_weights` (0.40 / 0.25 / 0.20 / 0.15) gives **7.9879**, which is exactly the figure the study itself commits as `retired_blend` and prints as a MEMO row two inches above. And that same sentence contradicts itself inside its own length: *"AED 5.34, **21% below** the market price … on the adopted construction the centre of the evidence sits about **21% above** the screen."* 5.3385 against 6.80 is −21.49%. Every other occurrence in the document — eleven of them, including seven table cells reading "−21%" — has the sign right. |
| 40 | Claims about the answer that survive being recomputed | **FAIL** | Two survive that should not. §4: *"Two constructions that share no machinery land within AED 2.63 of each other and both within a few per cent of the screen."* The two are AED 5.34 and AED 7.97 against a screen of 6.80 — **−21.5% and +17.2%**. Page 3 carries the same framing: *"the two constructions that stay closest to the company's own reported cash … sit within a few per cent of the market. This is not a study that finds a mispricing"* — written when the central was 6.60 against 6.16 and not rewritten when it became 5.34 against 6.80, at which point [R-GAP-02] holds the study from publishing for exactly that disagreement. §1.5 closes on the same claim: *"the share is close to fairly priced."* The study's own gap review reaches the opposite verdict on the same numbers and the publish gate agrees with the review. |
| 41 | The crux sensitised in real observable units | **PASS** | §1.7 is built on the observable the industry actually quotes — time-charter equivalent per vessel per day by size class by quarter — and §1.9 sensitises it in those units: the mid-cycle rate anchor is moved across 80%–120% of its base and the fair-value span is 3.92–6.75, a swing of AED 2.83 a share, with the caption stating that the market price is not reached anywhere in the range tested. The five-row sensitivity table ranks every driver by its own swing (beta 3.51 · rate anchor 2.83 · capital expenditure 1.15 · tax 0.98 · terminal growth 0.20) and §1.9 reads the ranking out loud, distinguishing the beta's width as measured statistical uncertainty from the rate anchor's as a judgement no data settles. |
| 42 | What the filings disclose that the model does not consume | **FAIL — one material item** | Asked explicitly against the Sweep Register rather than assumed. Note 11(b) of the reviewed statements to 30 June 2026 discloses **capital commitments in relation to the construction of vessels of USD 2,855,988 thousand** (31 December 2025: USD 2,897,077 thousand). That figure appears **nowhere** in `study_numbers.json`, in the sweep register, or in either delivered document, and no input key matches it. The model forecasts capital expenditure of 1,375 / 1,245 / 1,245 / 700 / 650 — USD 5,215mn over five years — sourced to "the guided programme", and §1.9 prices that path at a swing of AED 1.15 a share. A contracted, filed, dated USD 2.86bn of it is the hardest available evidence on the near-term half of that path and it is not registered. Note 11(a) bank guarantees of USD 16,366 thousand and 11(c) purchase commitments of USD 53,734 thousand are likewise absent. Consumed correctly and recorded here for contrast: the half's USD 14,880 thousand impairment on property, plant and equipment is registered as `h1_26_impair` and appears in the bibliography. |
| 43 | The exemplar's own debt — has it grown? | **PASS — it has SHORTENED sharply** | `python3 scripts/check_exemplar_debt.py` → `4 outstanding entries`, of which **one is ADNOCLS**: `ADNOCLS:terminal_outstanding.json:signature`. `exemplar_outstanding.json` lists twelve ADNOCLS entries; walking each into the ratchet it names shows **eleven now clear and one live** — anchor ordering, asset base, band vocabulary, edition date, earnings per share, ground-up, cost of equity, lens vocabulary, output sanity, published gap and rebuild ledger have all been met, and `asset_base_outstanding.json`, `published_gap_outstanding.json` and `ke_outstanding.json` each record ADNOCLS under `pruned_on.removed` today. The one live entry is the terminal, and row 24 establishes that its stop is properly recorded rather than worked around. The gate's own verdict: `OK — the exemplar has fallen onto no new standard.` |
| 44 | The delivered edition history in a delivered document | **PASS, with a standing tension named** | The model report is defined as this study MINUS "What changed in these editions, and why", and `engine/model_report/build_model_report_docx.py` strips it, so the artefact every new study is told to open conforms. The study PDF a reader receives carries that section in full at pages 39–43, which is the rule's own "never in a delivered document" read literally. This is the documented construction rather than a new defect, and it is recorded here because a reader of the study, not of the model report, receives it. |
| 45 | The gap a READER sees, against the gap the house holds | **PASS on the gate, with the divergence named** | `python3 scripts/check_published_gap.py` → `OK — no new unaudited breach`, and ADNOCLS is not listed: the site publishes `fair{bear:5.02, base:7.05, full:10.80}` against a spot of 6.80, **+3.7%**, inside the trigger. But 7.05 is the pre-re-strike answer. The house now holds 5.3385, **−21.5%**, and [R-GAP-02] holds the corrected number from reaching the page. The reader's gap and the gate's gap differ by 25 percentage points and the published one is inside the band only because it is stale. That is a property of the hold rather than of this study, and it is named rather than passed over. |

---

## What is RED, in the order it blocks delivery

1. **Eight of nine figures are a vintage behind and contradict the tables and captions beside
   them** (row 9). Figures 1, 2, 3, 4, 6 and 9 print superseded values a reader can read off
   the page; Figure 1's own caption names 6.80 while the image draws 6.16, and Figure 6's
   caption names 5.34 and 7.37 while the image draws 5.61 and 8.02. Measured by re-running
   `figures.py` from the committed numbers in a sandbox and diffing: `fig3_ma.png` identical,
   the other eight differ. The regenerated Figure 1 draws exactly the table's own numbers,
   so the module is right and only the run was missed.
2. **§1.5 describes the retired weighted blend as building the central and contradicts
   itself inside one sentence** (row 39) — "Weighted together, the four centre at AED 5.34"
   against a committed blend of 7.9879, and "21% below the market price … about 21% above
   the screen" in consecutive clauses.
3. **The primary-documents table in both delivered documents omits every one of the four
   half-year filings the re-strike is built on**, and still names the first quarter as "the
   balance sheet the valuation is built on" (row 3). The bibliography's net-debt table is
   headed "At 31 Mar 2026" over 30-June figures.
4. **No Driver Ledger entry for ADNOCLS**, while this re-strike introduced three solved
   drivers and two labelled substitutions (row 30).
5. **The walk-forward scope decision reaches neither delivered document** (row 31), and two
   published claims about the answer are false when recomputed (row 40).

## What passes with a gap named

- The external-reader scrub attestation was measured on an edition 596 characters shorter
  than the one delivered (row 8) — the currency check compares filenames, not content.
- Step 0.0, Step 0, the technical read and the price map all stand on the library as at
  2026-08-07 (795 rows) while the library the spot is read from now runs to 2026-09-07
  (814 rows), and §3's one-month check date arrives today (rows 14, 15).
- The beta record's own disclosure still says two weekly observations fall outside the
  regression against a last session of 2026-08-07; on a history now running to 7 September
  the figure is about seven (row 21).
- The bridge's joint-venture line is labelled at the superseded 31 March quarter (row 33).
- The site publishes an answer a vintage old, and passes the published-gap gate only because
  the stale figure happens to sit near the price (row 45).

## What is not a defect, and is recorded so it is not re-opened

- **The terminal.** It carries the retired reinvestment identity and it is right that it
  does, for now: the filings disclose no vessel component split and no residual values, the
  sanctioned module refuses at a 140.1% implied payout, and a life this desk chose is not a
  disclosed life. The stop is recorded in the ratchet with its measurement, in
  `TERMINAL_EVIDENCE_05-09-2026.md` with its closed routes, and inline in `compute.py`
  beside the arithmetic. Row 24.
- **The hold.** −21.5% below the price with no market dissent filed is the correct state
  under [R-GAP-02] and the study does not publish. Nothing in the re-strike moved toward the
  price: every parameter is solved from the audited FY2025 year or the reviewed half, and
  the two largest levers pulled in opposite directions (+16.8% on the tanker cost stack,
  −14.8% on the gas margin) off the same document.

## Repository gates run, and what each printed

| Gate | Result |
|---|---|
| `check_valuation_gap.py` | `ADNOCLS −21.5% GAP_REVIEW_07-09-2026.md (audits 5.3400)` · OK — no new violations |
| `check_publish_block.py` | `HELD ADNOCLS … −21.5% below the price of 6.80 (2026-09-07) … no market dissent is filed` |
| `check_lens_design.py` | `ADNOCLS primary dcf, 3 cross-checks` · OK |
| `check_bridge.py` | `ADNOCLS stands on the 2026-06-30 sheet; minority on the value_share basis` · OK |
| `check_cost_of_capital.py` | `ADNOCLS AE, 8.71% gliding to 8.68%` · OK |
| `check_ke_reproduction.py` | ADNOCLS among the 3 records that reproduce · OK — no new breach |
| `check_terminal_floor.py` | `ADNOCLS implied cycle 50.0 y against 1/g of 50.0, charge 20.5%` · list has not grown |
| `check_terminal_record_shape.py` | `RECORDS SHORT OF THE CURRENT SHAPE: 0` |
| `check_forecast_anchor.py` | `ADNOCLS EBITDA margin … latest 0.4022 forecast 0.3890 −1.32pp` · OK |
| `check_macro_coherence.py` | OK — no new violations |
| `check_macro_anchor_age.py` | OK — no study newly breaches the bound |
| `check_asset_base.py` | 11 in scope, **conforming: 1 — ADNOCLS** · OK |
| `check_asset_base_wired.py` | 5 fleet quantities read by arithmetic · OK |
| `check_output_records.py` | `ADNOCLS … 7 judgements, 7 material, sign test p=1.00` · OK |
| `check_output_sanity.py` | OK |
| `check_rebuild_ledger.py` | `ADNOCLS 9 lever(s), 6 rule(s), cumulative −4.8%` · OK |
| `check_source_integrity.py` | ADNOCLS neither breaching nor unreadable · OK |
| `check_sweep_module.py` | ADNOCLS in `RUNS THROUGH THE SHARED REGISTER (16)` · OK |
| `check_ground_up.py` | OK — every committed driver-line record passes when actually run |
| `check_prose_figures.py` | `ADNOCLS prose figures checked: 814; unmatched: 0` · OK |
| `check_table_footing.py` | `ADNOCLS tables examined: 63; unreconciled totals: 0` · OK |
| `check_waterfall_assertions.py` | `ADNOCLS OK … 8 waterfall table(s)` · OK |
| `check_sign_convention.py` | 45 documents, 98 deduction tables, 0 findings |
| `check_delivered_vocabulary.py` | ADNOCLS in `CLEAN (22)` · OK |
| `check_band_vocabulary.py` | OK — no verdict vocabulary in any page text |
| `check_lens_vocabulary.py` | ADNOCLS clean · OK |
| `check_workbook_values.py` | ADNOCLS in `CLEAN (21)` · OK |
| `check_workbook_structure.py` | ADNOCLS in `MATCHES THE MODEL REPORT (21)` · OK |
| `check_numbers_generators.py` | OK — no new violations |
| `check_study_provenance.py` | OK — no new violations |
| `check_bibliography.py` | OK — every delivered study ships the standalone bibliography |
| `check_artefact_currency.py` | both ADNOCLS artefacts built against 5.3385 · OK |
| `check_eps_reconciliation.py` | `ADNOCLS FY2025 0.1133 computed against 0.1100 reported` · OK |
| `check_figure_opacity.py` | OK — every delivered figure is opaque to the pixel |
| `check_figure_axes.py` | OK — no new violations |
| `check_anchor_ordering.py` | OK |
| `check_edition_date.py` | OK — no new violations |
| `check_published_gap.py` | OK — no new unaudited breach (ADNOCLS not listed; see row 45) |
| `check_exemplar_debt.py` | 4 outstanding, 1 on ADNOCLS (terminal) · OK — no new standard |
| `check_technical_read.py` | 93 entries against 93 libraries, 0 failures |
| `check_forward_ranges.py` | FAILS on GBCO, not ADNOCLS — no calibration run exists for this name, so it is out of scope |
| `check_calibration_deliverables.py` | FAILS on GBCO, not ADNOCLS — same reason |

## Study-local checks run, and what each printed

| Check | Result |
|---|---|
| `qc_checks.py` | scrub 0/0/0 hits · study 51 tables 0/0/0 · bibliography 17 tables 0/0/0 · 9 figures opaque · ALL AUTOMATED QC CHECKS PASSED |
| `recalc.py` | 2,244 of 2,244 formula cells reproduce, 0 unresolvable, 0 unchecked · 121 headline reconciliations |
| `lo_recalc_gate.py` | 16 sheets, 0 error cells of any kind |
| `driver_test.py` | 113 drivers reprice in the asserted direction, 0 dead inputs |
| `label_gate.py` | 170 labels checked, every one describes its own row |
| `label_check.py` | scrub clean: 0 hits across 262 paragraphs and 51 tables |
| `prose_check.py` | 814 figures checked, 0 unmatched |
| `footing_check.py` | 63 tables, 0 unreconciled totals |
| `doc_freshness.py` | 47 headline figures found, 3 alternative-construction figures carried, 16 superseded phrasings absent |
| `diagnostics_adnocls.py` | reverse read 7.21% against 8.41% · sign test 7 judgements, 4 resolved upward |
| `sweep.py` | 30 findings, 4/4 rings, 12 drivers, 4 primary-access attempts, 4 FS years, 10 IR findings |
| `beta_reg.py` | own_stock_beta against FADGI · 1.1032, R² 0.1814, SE 0.3147, n 159, conforming |

Note that `doc_freshness.py` reports the documents current while eight of their nine figures
are not: it ties the rendered TEXT to the numbers file in both directions and does not look
at what an embedded image draws. That is the same shape as every other instrument that
missed row 9, and it is why the rendered PDF has to be read as images.

## What was written by this audit

Only this file. The study, its builders, its numbers file, its figures and its delivered
documents were not edited. `qc_checks.py` writes `scrub_result.json` as a side effect of
running; that file was restored to its committed state and `git status` on the study
directory is clean.
