# STC — QC gate, 5 September 2026

Saudi Telecom Company (stc Group) · Tadawul: 7010 · edition 05-09-2026

Filled from outside the study by an auditor who did not build it. Nothing here is
self-certified: every row names the artefact, the command or the number that carries it.
"Checked" is not evidence.

**VERDICT: NOT DELIVERABLE.** Seven items block it; they are listed under the table.

---

## What was examined, and the population it was held against

| | |
|---|---|
| Commit audited | `ad30f463` (`git rev-parse HEAD`), 5 September 2026, 12:14 UTC |
| Delivered artefacts read | `STC_Valuation_Study_05-09-2026_public.docx` + `.pdf` (25 pages, 35 tables, 197 paragraphs) · `STC_Valuation_Model_05092026_public.xlsx` (16 sheets, 686 formula cells, 821 pasted/label cells) · `STC_Bibliography_05-09-2026.docx` + `.pdf` (19 pages, 7 tables) · `GAP_REVIEW_05-09-2026.md` (385 lines) |
| Repository gates run | all 48 non-negative-control `scripts/check_*.py`, over the whole book |
| Population held against | 24 study directories · 45 delivered documents · 1,022 tables · 43 documents for the sign-word check · 7 registered indices · 24 workbooks |
| Rendered pages read as images | 44 (25 study + 19 bibliography), rendered at 110 dpi with `pymupdf` |
| Figures inspected as rendered images | 7 of 7 |
| Independent recalculation engine | LibreOffice headless (`soffice --convert-to xlsx`), i.e. an evaluator that is not this repository's code |
| **Caution on timing** | The delivered `.docx`, `.pdf` and `.xlsx` were **rebuilt during this audit** by commit `323ab306` (12:07–12:14 UTC), which closed [R-ENF-05]. Every finding below was **re-measured against the current files at `ad30f463`** after that rebuild. A check that opens a delivered file by name moves with the re-issue, and this one did. |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates are called in the study's own committed code, and a job outside the study verifies it | **FAIL, partially met** | `grep -rn` over `engine/stc_study/**.py`: `assert_beta_provenance()` is called at `beta_reissue.py:59`, and `assert_macro_coherence()` at `stc_compute.py:1118`. **`assert_sigcm()` is called nowhere. `assert_model_study()` is called nowhere. `assert_ground_up()` is called nowhere.** `python3 scripts/check_study_provenance.py` reports `OK — no new violations` because its clause 4 requires *at least one* of the three, and the beta call satisfies it: 24 study directories, 11 known outstanding, STC not among them. Two of the three named gates do not execute on this study. |
| 2 | `research_protocol.STANDARD_VERSION` is live and the study is stamped with it | **FAIL** | `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` → **`2026.09.01`**, whose own `STANDARD_VERSION_NOTE` names the `GAP_REVIEW` artefact. **No file in `engine/stc_study/` carries a `standard_version` key** — checked across all 23 committed JSONs. The provenance gate counts the book as `stamped: 5, unstamped: 19`; STC is one of the 19. A study built to the current standard that does not say so is not countable. |
| 3 | Beta — inspected on its record, never on a boolean | **PASS** | `assert_beta_provenance(json.load(open('stc_study/beta_result.json')))` returns without raising. `index_file` = **`raw_indices/SA/TASI.csv`** — under `raw_indices/`, not a constituent composite. The listing exchange is read from `assets/data.js`: `STC: { code: "TADAWUL:7010" }`, so the regressor is the published index of the exchange the stock is listed on; `wacc_builder.EXCHANGE_INDEX[('SA','TADAWUL')]` → `TASI`, `market_index_path('SA','TADAWUL')` resolves and the file exists. `index_interim_note('SA','TADAWUL')` → `None`: TASI is fully registered, so no interim disclosure is owed and none is claimed. Not Qatar, so no QATAR10 weekly-only caveat applies. Tier 1, usability gate cleared on all three conditions: n=254, R²=30.25% (floor 5%), SE(β)=0.1032 < \|β\|=0.7107. The superseded 40-session daily stopgap (β 0.4753) is recorded with the reason it is not a tier. |
| 3a | The beta artefact is current with the beta the study publishes | **FAIL** | Committed `beta_result.json` records **β 0.7107, n=254, last_obs 2026-07-23**. The delivered study, the bibliography and the workbook all publish **β 0.7078, n=252, 4.91 years** (`Assumptions!B11` = 0.7078292485042049; §1.8 table; bibliography derived-figures table). `coc_run.py:164` calls `own_stock_beta()` **live at build time**, so the model uses 0.7078 while the artefact the repo-level gate and `assert_beta_provenance()` read carries 0.7107. Re-running `beta_reissue.py` reproduces 0.7078 and **overwrites the committed artefact** — the file was restored to its committed state immediately (`git checkout --`). Δβ = 0.0029 ⇒ ΔKe ≈ 1.7bp. Small in value; the record is nonetheless not the record the study stands on. `check_artefact_currency.py` cannot see it because the file carries no valuation figure. |
| 4 | Ground-up clause on a driver record, not a flag [R-SIGCM-02] — share-by-level split | **FAIL, and the split is reported** | **No `DriverLine` record exists and `assert_ground_up()` is never called.** Reconstructed from the study's own committed `seg_hist` and `sweep_register.json` against FY2025 gross segment revenue of SAR 89,317.3mn: **`derived` 57.2%** — the `stc` KSA segment, volume × price on subscriber counts from the earnings presentations (30.0mn mobile, 6.0mn fixed at FY25; 30.3 / 6.1 at H1-2026) with revenue-per-subscriber **back-solved** against audited segment revenue, and `units.json` itself records the counts are "chart labels from presentations whose own footnote says the figures are not audited"; **`segment` 42.8%** — the other eleven lines, each grown on its own measured two-year real rate from note 9, gap stated; **`unit` 0.0%**. Coverage is 100% of revenue. The cost side is **not** built to unit level: `stc_compute.py:428` computes gross profit as `sum(seg_fc[y][k] * SEG_MARGIN[k])`, a held per-segment margin, and `Assumptions!C55` carries `Gross margin (% of revenue) = 0.485` as a forecast driver. The study's own sweep register says so: *"The model currently holds each segment's disclosed gross margin, and the seven-line build is registered as the next correction rather than claimed here."* Note 35 discloses all seven cost lines by nature for three years, so the filings support the build — which makes this the QC FAIL the margins-are-outputs rule names, correctly diagnosed by the study and not yet done. |
| 4a | The bibliography's driver table agrees with the model on that level | **FAIL** | Bibliography §5, row "cost of revenues": **Level built at = "bottom up"**, while the same row's own justification cell says the model holds the disclosed gross margin. The bibliography judgements table further tells a reader the margin path is *"An OUTPUT of the cost build"* — it is an output of an SG&A share applied to a **held** margin, which is not a cost build. Two delivered statements about the model that the model does not support. |
| 5 | SIGCM clause 1 — historicals from official sources only | **PASS** | `inputs_register.py` `check()` run from outside: **176 inputs, 174 of them dated historicals, 0 problems**, and `source_integrity.audit()` returns no finding — no vendor, broker or press appears in any source field. `python3 scripts/check_source_integrity.py`: 18 registers read, 1 study in breach (ELEC), 6 unreadable; **STC is in neither list**. Sources are the FY2023/24/25 audited consolidated statements and the reviewed 30-June-2026 interim, held as text in `src/`. |
| 6 | SIGCM clause 2 — forecast from the ground up | **FAIL** | See item 4. Revenue reaches `derived`/`segment`; cost stops at a held margin where note 35 supports per-unit cost. |
| 7 | SIGCM clause 3 — debt split local vs foreign currency | **PASS** | `coc_record.kd_integrity.currency_source` reproduces note 26 of the FY2025 statements facility by facility: dollar 19,401,578 · ringgit 1,310,218 · riyal 1,105,020 · dinar 141,875 · 732,737 subsidiary borrowings of unstated currency, **footing exactly to the stated 15,191,428**. `pct_local_currency = 4.87%`. Every non-riyal leg is carried at local-equivalent cost, which under a peg at 3.75 equals its own coupon, and that is stated rather than assumed. |
| 8 | SIGCM clause 4 — asset-conversion cycle studied and BS/CF projected from it | **FAIL, one half done** | `working_capital.json` is thorough and sourced to notes 17/18/32: DSO 108.8 / 106.9 / **125.4**, DIO 63.1 / 57.8 / 59.0, DPO(trade) 161.4 / 184.9 / 228.6, contract assets and liabilities, cash cycle −29.3 / −32.7 / −0.03 days, the disclosed 90–107 day settlement range with the note that all three measured years sit outside it, and the government share of the receivable book at 75.0%. **The projection half is not done**: the model drives working capital off `Working-capital movement (% of revenue)` on `Assumptions` row 61, and **there is no forecast balance sheet at all** (item 21). |
| 9 | SIGCM clause 5 — competitors studied for KPIs and multiples | **PASS, with the gap named** | Sweep driver row 4, mode `TOP_DOWN`: *"No named competitor, market share or peer figure appears in any document this study holds, so no relative multiple is taken from peers."* The relative lens therefore uses this company's **own** trailing EV/EBITDA at three year ends (9.131× / 8.316× / 8.839×, mean 8.762×) and the record prints the non-circularity test — the traded multiple today is 9.292×, **above all three**, so the lens cannot be anchored on the price. The peer table (T27, 7 names) is present with P/E and dividend yield. The gap — no sourced Saudi peer operating KPIs — is stated rather than filled. |
| 10 | SIGCM clause 6 — beta | **PASS** | Item 3. |
| 11 | SIGCM clause 7 — every constructed statement is a live formula model | **FAIL** | The workbook is formula-linked (686 formula cells; `Assumptions` is the single input sheet; driver→IS→BS→CF→DCF chains resolve). **But the chain is mis-wired and the model does not reprice correctly** — item 15. A formula model that recomputes the wrong number is worse than a pasted one, because it looks live. |
| 12 | SIGCM clause 8 — flag before issue, stop and inform | **PASS** | Three gaps are named and stopped on rather than filled: the forecast balance sheet (`balance_sheet.json.reason`, A.2, and a judgement row with its unblocking condition); the BGSM look-through — *"a market cross-check on that look-through is NOT performed because the price is not held, and that is recorded as a gap rather than estimated (SIGCM clause 8)"*; and stc Bank's own equity value, excluded from the bridge with the direction of the error stated (*"which understates the answer, and it is left as a stated gap"*). |
| 13 | Depth standard 1 — standalone bibliography document | **PASS, with two gaps named** | `STC_Bibliography_05-09-2026.docx` + `.pdf`, 19 pages, 7 tables. Primary-documents table (T1, 6 documents: three audited fiscal years, the reviewed 30-June-2026 interim, two earnings presentations tagged `EARNINGS PRESENTATION (investor-relations)`), with inputs drawn per document. Full input register (T0, **176 rows**, four fields each, grouped by layer). Judgements table (T5, **7 judgements, every one with what would overturn it**). Negative results (T3, **4 of 19 findings are dated negative searches**, printed rather than left out). **Gap 1: no aggregator-discrepancy note** — legitimate here, since no aggregator was consulted and the register says so, but the element is absent. **Gap 2: the register carries no Country, Industry or Global layer at all** (Company 166, Company_IR 9, Market 1). The observed sovereign yield 5.52%, the default spread 0.98% and both ERP bases 5.72% / 5.01% are sourced inputs that drive the valuation and appear only inside the *derived-figures* table's prose, with no source field and no source date. |
| 13a | The bibliography carries no edition history | **FAIL** | `research_protocol.MODEL_STUDY['excluded_sections']` = `["What changed in these editions, and why"]` — edition history is internal QC evidence and never goes in a document an external reader receives. The delivered bibliography's **§7, "What moved this edition, and in what order"**, is a 16-row table of the 14 rebuild levers with before/after values and a cumulative −19.04%. That is the rebuild ledger, in a delivered document. No gate covers the bibliography for excluded sections. |
| 14 | Depth standard 2 — four-field provenance on every input, validated by assertion | **PASS** | `python3 inputs_register.py` run from outside → **"176 inputs, 174 of them dated historicals of this company"**, layers Company 166 · Company_IR 9 · Market 1, **0 FAIL lines**. Independently recounted: value 176/176, source 176/176, date 176/176, layer 176/176; 0 incomplete. Every row's source resolves to a document on the closed catalogue, asserted by the register's own `check()`. |
| 15 | Depth standard 3 — numeric traceability; the delivered workbook recalculated independently | **FAIL — the largest defect in this delivery** | **The delivered workbook does not reproduce the study's answer.** Recalculated in LibreOffice (an evaluator outside this repository): 686 formula cells, **0 unparseable, 0 error results** — nothing is skipped — and the computed values are: `Summary!C12` "THE CENTRAL" = **SAR 28.9443** against a published **38.1426**; `Summary!B16` "Central against spot" = **−34.01%** against a published **−13.04%**; `DCF!B27` enterprise value **135,399.7** against **176,163.4**; `DCF!B28` terminal share **92.65%** against a published **71%**; `Relative & Normalized!C6` **24.98** against **47.97**. **Root cause, exact:** `_asm_rows.json` maps two labels to Assumptions row 55 — `"First-year scale onto the reviewed half (annualised)"` **and** `"Gross margin (% of revenue)"` — and only the gross-margin row exists on the sheet. `stc_xlsx_b.py:97` therefore emits, for all twelve segments × five years, `=D6*(1+Assumptions!$C$54)*(1+Assumptions!$C$56)*Assumptions!$C$55`, whose last factor is **0.485**. Group revenue halves every year (38,920 → 19,402 → 9,645 → 4,784 → 2,366 against a model path of 80,737 → 88,722; the ratio 38,920.5/80,736.6 = **0.4821**). It propagates through Income Statement, Balance Sheet, Cash Flow, Summary Financials, Per-Share & Ratios, DCF, SOTP Bridge, Relative & Normalized and Sensitivity: profit turns **negative from FY28E**, closing cash reaches **−SAR 33.5bn**, P/E prints **−1,482×**. The builder's own `an()` docstring warns about precisely this class of failure; referencing by name did not prevent it because the **name map itself collides**. Secondary defect in the same line: the comment says the scale applies "in the FIRST year", the formula applies it in **all five**. |
| 15a | Numeric traceability — the numbers file, and financial numerals in builders | **PASS on the model, FAIL on the workbook builder** | `stc_compute.py` re-run in a sandbox copy reproduces `study_numbers.json` **exactly: 2,422 leaf values compared, 0 differing, 0 keys added or removed.** Every document builder reads that file. **But `stc_xlsx_a.py` types financial numerals into the workbook's own prose**: line 69 `"Spot SAR 43.58 (7 Jul 2026 close…)"` against a delivered spot of **43.86 on 2026-09-03**; line 71 `"balance-sheet detail from the Q1-2026 interim FS (31-Dec-25 comparatives)"` against a bridge standing on **30 June 2026**; line 386 `"Market cap = 43.58 × 4,989.8mn = SAR 217,455mn; total debt = SAR 22,475mn … E/(D+E) = 90.6%"` — printed beside the cell `=23536.554/(B5*B6+23536.554)`, which uses 43.86, 4,993.024, 23,536.554 and gives **90.3%**. `stc_xlsx_c.py:154` types `"Four-lens fair value vs spot"` onto `Summary!A2`. None of this is reachable by the study's own prose instrument, because `prose_check.py` declares `DOCS = [study.docx, bibliography.docx]` — **the workbook is not in its population**. |
| 15b | Script-built files verified by a cell-by-cell diff against the delivered file | **PASS on fidelity** | Rebuilt in a sandbox from `stc_xlsx_a/b/c/fix1.py` and diffed against the delivered file: sheet names identical, **1,507 populated cells compared, 0 differences**. The delivered workbook is exactly what its builders produce — which is why item 15 is a builder defect and not a stale delivery. |
| 15c | An independent evaluator exists to recalculate the workbook | **FAIL** | `engine/stc_study/` carries **no `recalc.py`, no `driver_test.py`, no `label_gate.py` and no `qc_checks.py`.** 22 of the 24 study directories carry `recalc.py`; STC is one of two that do not (the other, GBCO, predates the standard, and XPT has no workbook). That absence is why item 15 shipped. |
| 16 | Depth standard 4 — external-reader scrub | **FAIL** | Verdict tokens: `band_record.assert_no_verdict_tokens(text)` run over both delivered PDFs → **PASS on both** (no PASS/PARITY/FAIL, no CRPS beside the company name; CRPS appears nowhere). `python3 scripts/check_delivered_vocabulary.py` (45 documents, shape-matched rule ids and repository paths) → **STC CLEAN**. **But an independent scrub of the rendered PDFs finds internal-procedure vocabulary reaching the reader**: the study carries **6 hits** — §B.3 *"the research ran in four rings — the world, the country, the industry, and the company"*, a table column headed `Ring`, and the headings *"Global ring —"*, *"Country ring —"*, *"Industry ring —"* (twice) — and Table 16's note cell reads *"the house fallback, had the regression failed its usability **gate**"*; the bibliography carries **2 hits** — §4 is titled *"Research trail — the four rings, and what came back empty"*. Depth standard 4 names sweep/ring vocabulary explicitly and requires zero hits. The study carries **no study-local scrub script**, so nothing in the build looks for this class. |
| 16a | Calibration evidence in §3 as plain-language sentences, no calibration appendix | **PASS** | The document has exactly three appendices — A financial statements, B peer frame/risk/research register, C expert panel. The calibration evidence sits in §3 under the heading "How often these bands have actually held", in sentences with the statistics inline. |
| 17 | Depth standard 5 — figure discipline | **PASS, with two gaps named** | 7 figures, all inspected as rendered images and all verified programmatically: **alpha minimum 255 on every one (zero transparency)** and corner luminance **241** (solid light canvas). `python3 scripts/check_figure_axes.py`: 26 figure scripts, 24 ran under the guard, 2 drawing outside their own axis — **STC in neither**. Figure 2's title promises "bold is the base case, SAR 38.1" and the centre cell **is** bold. Figure D1 labels are "Expert 1/2/3" only. **Gap 1: Figure 4's x-axis reads "sessions ahead — the 3 months to 2026-11-30"**, a session-counted unit on a figure whose own §3 text insists *"Both horizons are CALENDAR … graded on a stated date rather than after a count of trading sessions"*; the retired session-counted unit in an axis label is the documented DU defect. **Gap 2: two label collisions** — Figure 1's "37–39 · 38.1" annotation overlaps the discounted-cash-flow bar it labels, and Figures 5 and 6 draw "median 44.92" / "median 45.30" in pale gold **over** the tan histogram bars, at low contrast. |
| 18 | Depth standard 6 — table discipline, three counts per document | **FAIL** | Fixed layout with explicit widths: study **33 of 35 tables carry `<w:tblLayout type="fixed">`**, 0 columns missing an explicit width — the two exceptions (T0 masthead band, T1 READ FIRST panel) are single-cell layout containers, not data tables; bibliography **7 of 7 fixed**, 0 missing widths. `python3 scripts/check_column_widths.py` over 45 documents and 1,022 tables: **over-wide 0 · bloated 0 · starved (too narrow for a figure they print) — study 6, bibliography 1**, and all 7 are **NEW**, so the gate goes red: `STC_Valuation_Study…docx::FY25/FY26E/FY27E/FY28E/FY29E/FY30E declared 1.56cm, needs 1.63cm` (Appendix A.1) and `STC_Bibliography…docx::# declared 0.76cm, needs 0.84cm`. `python3 scripts/check_table_footing.py`: 22 of 24 studies check their totals; **STC is one of two with NO FOOTING CHECK**, on the ratchet. |
| 19 | Depth standard 7 — expert appendix at maximum detail | **PASS** | Per expert: worldview and tradition; when it works / when it fails; a worked table with every intermediate line (Expert 1's economic-profit test runs 7 lines from invested capital SAR 93.4bn to SAR 36.9 per share; Expert 2's normalisation runs 7 lines from FY2025 profit before zakat 14,723 to SAR 39.8 — and it foots: 14,723 − 1,217 − 273 = 13,233, ÷ 4,993.0 = 2.65, × 15.0 = 39.8); a named sensitivity with numbers (*"every turn of the price-to-earnings ratio is worth exactly his normalised earnings per share, SAR 2.65 — so at 13.5× he reads SAR 35.8 and at 16.5× SAR 43.7"*); and a falsifier stated in advance. C.4 carries **4 challenges, each marked CONCEDED or REJECTED** with reasons. C.5 puts the three in one room. C.6 is a divergence table isolating each expert's single swing assumption against his base. Labels are "Expert 1/2/3" throughout the text, the table and Figure D-1 — **no persona name appears anywhere**. |
| 20 | Depth standard 8 — the central contested judgement computed both ways | **PASS, with one gap named** | The contested judgement is capital intensity, and it is computed three ways from the company's own filed record — 13.6% / 15.0% / 16.1% of revenue, equivalently 1.054× / 1.161× / 1.252× the depreciation of the base being renewed — and published side by side in all four required places: the summary table (37.4 / 38.1 / 39.0), the body (§1.7's cover table, 1.24× / 1.14× / 1.05× dividend cover), the workbook (`Summary!B6:D6`, `Summary!B13/D13`) and Expert 1's range. It is **never averaged into one number**: `Summary!A12` reads "THE CENTRAL — the class primary, not an average of the rows above". **Gap:** the study names management's own guided band of 15.0–17.5% of revenue and rejects it under the guidance-is-scored-never-consumed rule, but does not price the whole band as the alternative framing; only its upper bound is priced, and then only inside Expert 3's bear scenario (17.5% → SAR 28). |
| 21 | Appendix A.2 / A.3 — forecast balance sheet | **FAIL — recorded as knowingly outstanding, not hidden** | A.2 is titled *"Balance sheet — the filed years, and why there is no forecast column"* and carries FY23/FY24/FY25 only; there is no forecast balance sheet and A.3 carries no forecast balance-sheet markers. The reason is established rather than asserted: `balance_sheet.py` `check()` run from outside prints residuals for the reviewed 30-June-2026 interim of **non-current assets +201 · current assets −900 · non-current liabilities +1,000,030 · current liabilities −9,900 · assets against the stated total +990 · liabilities against the stated total +10,001**, while **every 31-Dec-2025 column foots to exactly 0**, on the text layer and on OCR off the page rendered at 300 dpi alike; `check()` fails both ways. The lines the bridge does use are corroborated against a second statement: cash 12,940,389 + 6,000,384 = 18,940,773, the interim cash-flow statement's own closing balance **to the riyal**; borrowings 23,536,554 rolls from the audited FY2025 book through that statement's financing movements **to twelve basis points**. The model report requires A.2 and A.3 to carry it; they do not. |
| 22 | Step 0.0 data-quality gate | **PASS** | `data_quality.clean_ohlc(df, ticker='STC', market='SA')` run independently on `engine/raw_ohlc/SA/STC.csv`: **3,907 raw rows → 3,906 clean**, note *"dropped 1 interior stale/no-trade rows"*, span **2011-01-01 to 2026-08-31**, density **249.4 sessions a year** against the Tadawul calendar, largest single-session move **9.94%** against that exchange's own **±10%** daily limit — inside it, so no corporate action needed back-adjusting. The library's last session, 2026-08-31, is exactly the study's published cone anchor date. The TASI index series passed the same screen (`index_dq`: 7 interior stale rows dropped) and lives outside `raw_ohlc/`. |
| 23 | Step 0 calibration under the band record [R-CAL-03] — coverage, two-sided binomial, count beside the percentage, width disclosed never gated | **PASS, with one gap named** | Reproduced independently from the committed panel `engine/panels/SA_STC_3m.csv`: **n = 58**, inside the 90% band **51 (87.9%)**, the 80% band 45 (77.6%), the 50% band 27 (46.6%); **two-sided binomial against the 90% target p = 0.5154** — no flag earned at the 5% level; width ratio median w90/w90_b = **1.0445**. The committed `band_record` and the delivered §3 match to the rounding: *"Over 58 resolved three-month forecasts, the price finished inside the 90% band 88% of the time … 78% and 47%"*, *"p = 0.52"*, *"The band is 1.04 times the width of a simple no-forecast rule's … published beside the record rather than turned into a pass mark"*, strength `long`, flag `null`. **The count is always printed beside the percentage.** No skill verdict, no PASS/PARITY/FAIL, no CRPS anywhere. **Gap:** origins are one calendar quarter apart, so **24 of 57 consecutive gaps are shorter than the 62-session horizon** (54 to 61 sessions) and those window pairs share 1–8 trading sessions; the 58 observations are not fully independent and the document does not say so. |
| 24 | Sweep Register — runs through the shared module and its invariants execute | **PASS** | `python3 scripts/check_sweep_module.py`: STC is one of **16 studies that run through the shared register**, and it imports `research_sweep` **and calls `validate()`**. `python3 sweep.py` from outside → `PASS — 4/4 rings, 19 findings (6 B · 6 S · 3 D · 0 C · 4 NEG), driver gate 5 rows (3 bottom-up / 2 top-down)`. |
| 25 | Sweep Register — primary-source depth | **PASS, with one gap named** | Company site attempted and **logged either way**: *"Reached through www.stc.com.sa/sitemap.xml after four direct URL guesses had each returned the site's own 404 page under an HTTP 200"* — `primary_access` carries 1 attempt with its outcome. Audited fiscal years cited to the filing itself: **3 (FY2023, FY2024, FY2025)** — above the floor of 2, **below the target of 4**, and the register warns in its own words: `warning FS DEPTH: 3 distinct fiscal years — target is 4`. Every study-year quarter already disclosed is swept in **before the build**: `R.declare_study_year("FY2026", ["Q1-2026", "H1-2026"])`, both registered with their statements. Results releases swept with their statements: the FY2025 and H1-2026 earnings presentations are registered, and **`COMPANY_IR` is tagged distinctly** — 6 IR findings and 9 register inputs on the `Company_IR` layer, separate from the audited-statements tag. |
| 26 | One escalator per driver class, with a sourced near-term cost anchor | **FAIL** | `cost_stack.json` does the work: note 35's seven cost lines by nature for three years, **each with its own `driver_class`** (imported handsets on world equipment prices under a pegged riyal; the provisioning levy on its own disclosed base; wages domestic), the two disclosed one-off reversals identified and put back (reported gross margin 48.40/49.18/48.45 → underlying 47.39/47.20/48.45), and `cost_decomposition.json` prices putting each line on the base the filings name at **−0.30% of the central**. **But the model consumes none of it**: cost enters as one held gross margin (0.485) plus one SG&A share of revenue. One blended rate across physically distinct cost lines is exactly what the rule forbids, and the decomposition proving the lines differ sits beside a model that does not use them. |
| 27 | Margins as OUTPUTS wherever the filings disclose enough to build cost per unit | **FAIL** | Item 26. Note 35 discloses the seven lines; the model holds the margin. The study's own register says the seven-line build "is registered as the next correction". |
| 28 | 3 years historical + 5 years forecast, full FCFF waterfall shown inline to PV of FCFF | **PASS** | Appendix A.1 carries FY23/FY24/FY25 actual and FY26E–FY30E forecast on 13 lines. §1.1's Table 4 shows the waterfall inline and **reaches PV of FCFF**, every line: Group revenue → EBITDA → D&A → EBIT → NOPAT = EBIT × (1 − 8.03%) → + D&A → − Capex → − Δ working capital → **FCFF** → Discount factor → **PV of FCFF** (11,563 / 10,601 / 10,047 / 9,510 / 8,997). It does not stop at FCFF. |
| 29 | The enterprise-to-equity bridge a reader is asked to follow reaches the answer it prints | **FAIL** | Delivered §1.1 "DCF bridge" table, read off the rendered page: Enterprise value 176,163 · **+ Associates & JVs 12,910** · **+ Telefónica 8,513** · **less: Net debt (8,375) · NCI (3,928)** · **Equity value 190,447**. A reader following the page's own instructions reaches **185,283** — **SAR 5,164mn away, SAR 1.03 a share**. The missing line is `bridge_record.lines[3]`, *"plus investment funds and unlisted equity investments, at fair value" = 5,163.516*, which the model includes (`DCF!B34` adds `Assumptions!$B$22`) and the delivered table prints **nowhere**. `check_waterfall_assertions.py` reports STC `OK — 1 waterfall table(s)` because `docx_stc_C.py:316` calls `table_residual.waterfall()` once, on the **normalised-earnings** table; the bridge is not asserted. The same defect is in the workbook in a second form: `SOTP Bridge!C12` omits `Assumptions!$B$22` while `DCF!B34` includes it, so one workbook prints two DCF values per share — **28.9443** and **29.9785**. |
| 29a | Bridge construction as a record [R-BRIDGE-01] | **PASS on the record, FAIL on the label** | `python3 scripts/check_bridge.py` → `STC stands on the 2026-06-30 sheet; minority on the value_share basis`, no new violations. The record is strong: latest disclosed sheet named with the register establishing it; the minority deducted from **equity** value at its **value share** (2.021%, the disclosed profit share from note 25) with book 2,726.349 published beside it and named as not the adopted basis; cash added at face **once**, against **gross** discount-rate weights so it is not netted twice, with stc Bank's cash excluded and the direction of that omission stated; no dividend deducted, with the reason (the interim sheet is struck after the SAR 6,486.411mn paid in the half); the lines foot to the stated equity value and divide to the stated per share. **But the delivered table and the workbook both label the net-debt line "IR basis, Q1-26" and the minority "Book, 31-Mar-26"** — two labels naming the superseded quarter on figures taken from the June sheet, and the minority is not book at all. |
| 30 | [R-LENS-03] one class primary is the central, the rest cross-checks, never a weighted blend | **FAIL** | `python3 scripts/check_lens_design.py` → `STC primary dcf, 2 cross-checks` — the **record** conforms (`LENS_REGISTRY['telecom operator']` = dcf primary; ev_ebitda_own_history and book_value as cross-checks). **The delivered documents do not.** `python3 scripts/check_lens_vocabulary.py` → **`NEW 1 · STC · 2 assertion(s)` and the gate goes red.** Read off the rendered page: §1.5 "Synthesis" **opens** with *"We weight the DCF most heavily because it is the only lens that prices the full capex-and-recovery arc; the DDM carries the pre-committed cash; the relative lens anchors to what the market pays for GCC telecom cash flows today; normalized earnings is the ballast"*, and the table beneath it carries a **Weight column of 35% / 25% / 20% / 20%**. §4, four sections later, says *"No weights are applied anywhere"* and *"Three lenses, read side by side and never averaged"*. **The study contradicts itself.** In the workbook, `Fundamental Valuation!A10` is labelled **"Weighted central"** and points at `Summary!C10`, which is the **book-value floor of 17.02** — a retired label on a row that is not what it says; `Summary!A2` reads "Four-lens fair value vs spot". The second gate hit, Expert 3's "Probability-weighted fair value", is a probability weighting of policy scenarios inside one lens and reads as a false positive of the shape match — the substantive hit is §1.5 and the workbook. |
| 31 | [R-COC-01] cost-of-capital schedule through the sanctioned module | **PASS, with one printed claim wrong** | `python3 scripts/check_cost_of_capital.py` → `STC SA, 8.13% gliding to 8.13%`, no new violations. Country risk counted **once**: rf* = 5.52% observed − 0.98% this sovereign's own default spread = 4.54%, and the premium added back is on the same (market) basis; the alternative rating basis is published beside it. Market-value weights 90.3% / 9.7%, never book. Flat ladder with the reason — *"SA runs a pegged regime, so the risk-free rate already sits at its long-run level … applying a glide here would manufacture movement the peg forbids (+0.0% where it was tried)"*. Terminal norm-built and derived, no quote. The three-assert Kd gate clears: currency composition sourced to note 26 and footing exactly; effective rates computed independently over FY2024 and FY2025 (5.226% and 3.890%) **with the denominator described** — borrowings only, leases and non-interest-bearing balances excluded, closing rather than average balances with the re-basing reason; adopted 4.860% sits **97.0bp** from the latest effective, inside the 150bp bound, and below the peak. The 36-day sovereign quote is **disclosed** as accepted deliberately, in the record and on the page. **The printed claim is wrong:** §1.8's source cell for "Cost of debt, before tax 4.86%" reads *"and above this sovereign's own yield, as a same-currency corporate borrower must be"* — two rows below "Risk-free rate, as observed **5.52%**". The construction is defensible only because the book is **95% foreign-currency under a peg**, which the module's own refusal encodes (`pct_local_currency >= 0.999`), and that reason is not on the page. |
| 32 | [R-MACRO-01] one house macro path, every inflation-class input mapped | **PASS** | `python3 scripts/check_macro_coherence.py`: 24 studies examined, 9 conforming, 15 outstanding — **STC is not among the outstanding**, so it conforms. `stc_compute.py:1118` calls `assert_macro_coherence(macro_record, market='SA', ticker='STC')` in the study's own code. Terminal growth 2.00% = terminal inflation 2.0% + a **stated** real growth of 0.0%, derived rather than typed; terminal risk-free 3.9751% = 2.00% + the 1.98% real-rate convention, derived. |
| 33 | [R-TERM-01] terminal built on a disclosed asset life through the sanctioned module | **PASS** | `python3 scripts/check_terminal_floor.py` → `CLEARED since the list was written: STC`, and STC is listed among those *"built on something else (the cycle does not equal 1/g)"* — the retired construction is gone. Scored at **−0.4% (38.14 → 38.24 there)**. The delivered terminal block is the module's: terminal NOPAT 15,445.7 · + book depreciation 11,436.6 · − maintenance at current cost on a **20.86-year life derived from the company's own property, plant and equipment note** 15,461.0 · − growth capital 0 (real growth stated at zero) · − inflation on working capital 271.1 = terminal free cash flow 11,150.1. The bibliography's derived-figures table names the life and the identity that produces it. |
| 34 | [R-ANCHOR-01] forecast anchored on the latest reviewed period | **PASS, with one gap named** | `python3 scripts/check_forecast_anchor.py` → `STC gross margin latest 0.4896 forecast 0.4915 +0.19pp`, no new violations — the forecast opens **above** the latest reviewed period, so no mechanism is owed, and the record is printed either way. The ledger's lever 11 is *"the first forecast year anchored on the latest reviewed period"*, +6.49%. **Gap:** the record covers the **gross margin only**. Capital intensity, the study's own stated crux, is anchored on three **full filed years** (14.96% of revenue, held flat) while the H1-2026 earnings presentation the study registers discloses a half-year capital intensity of **11.9%** (against 9.4% a year earlier) — a near-term disclosed actual that the anchor rule's own logic reaches and that the record does not test. |
| 35 | [R-REBUILD-01] the route recorded, not only the destination | **PASS** | `python3 scripts/check_rebuild_ledger.py` → `STC 14 lever(s), 10 rule(s), cumulative −19.0%`, no new violations. The ledger walks continuously from 47.1108 to 38.1426, every lever naming the rule it serves, grouped so several levers serving one rule read as one piece of evidence (R-SIGCM-02 four levers −7.3%; R-BRIDGE-01 two levers −8.1%). The **audit point was declared in advance** — *"AFTER LEVER 3 … Declared in REBUILD_PLAN_05-09-2026.md before any lever was touched, and unchanged since"*, with the reason (levers 2 and 3 pull opposite ways) and the statement *"THE NET WAS NOT PREDICTED and the plan said so in advance"* — and the audit was **taken and recorded**, including the discipline that matters: at the audit the answer sat at +0.43% of the latest price and the record says *"that is reported as an observation and NOT as corroboration — a rebuild that stopped here because the number looked comfortable would be fitting to the price by choosing when to stop."* |
| 36 | [R-ENF-05] the study audits its own answer — reverse read and sign test | **PASS as of `323ab306`, closed during this audit** | At the commit this audit opened on (`14dfa3dc`) STC was on `output_outstanding.json` with no reverse read and no sign test, exactly as flagged. Commit **`323ab306`, landed at 12:07–12:14 UTC while this audit was running**, closed it: `python3 scripts/check_output_records.py` → `STC price implies the equity beta the traded price implies of 53.33% against the study's 70.78%; 7 judgements, 2 material, sign test p=1.00`, conforming 4 of 24. The reverse read is **published** in §4 — *"Read backwards, this model reproduces the market price of SAR 43.86 at an equity beta of about 0.54 — read straight off the grid in §1.9, between its 0.50 and 0.71 rows, so a reader can check it against what is printed there rather than taking it on trust"* — and `diagnostics.json` records it is solved by bisection on the cost of capital holding every other input at its published value. `assert_reverse_dcf()`'s structural rule holds: `diagnostics.json` is read only by `diagnostics_stc.py`, never by a document builder. **One gap:** the sign test (p = 1.00) is recorded in `contested_judgements.json` and is **printed nowhere in the delivered documents** — "sign test" returns 0 hits across both PDFs. |
| 37 | [R-ENF-06] every artefact a builder reads declares the answer it was built against | **PASS** | `python3 scripts/check_artefact_currency.py` → 30 artefacts carrying a valuation figure across 24 directories; STC's three all **CURRENT**: `contested_judgements.json`, `cost_decomposition.json` and `diagnostics.json` each declare `published_central 38.1426` / `published_spot 43.86`. 0 stale, 0 new undeclared. (The stale artefact this study does carry, `beta_result.json`, sits outside this gate's reach because it holds no valuation figure — item 3a.) |
| 38 | **[R-GAP-01] the answer, not only the process** — the gap, and the eight-heading review | **PASS, with one gap named** | Committed `central = 38.142610789530956` against `spot = 43.86` at `spot_date = 2026-09-03`; gap **−13.04%**, more than 10% **below** the price. `python3 scripts/check_valuation_gap.py` → `STC −13.0% GAP_REVIEW_05-09-2026.md (audits 38.1426)`, 19 studies outside the band and reviewed, **no new violations**. The spot is **not stale**: `python3 engine/prices/gap_today.py` reads `SUPPLIED_03-09-2026.json` and returns `STC 38.14 / 43.86 / 43.86 / −13.0% / −13.0% BREACH` — the price the study is struck against **is** the latest known price. All eight headings present and substantive: LATEST FILINGS (l.19) · BASE YEAR (61) · MACRO COHERENCE (116) · DISCOUNT RATE (139) · TERMINAL (166) · BALANCE SHEET (211) · CLAIMS AGAINST THE RECORD (294) · MULTIPLE CROSS-CHECK (306), plus a verdict, `AUDITED CENTRAL: 38.1426` and `AUDITED GAP: -13.0%`. **Gap: the review states three different gaps.** Its opening sentence says the central sits **"20.7% below"** the price; its header table says central **SAR 37.8396**, gap **−13.73%**, envelope **37.14–48.71**; the study publishes **38.1426**, **−13.04%**, **37.37–47.97**. Only the machine-read markers at the foot match. The gate passes because it reads the markers — which is the check reading what a process declares rather than what it says. |
| 39 | [R-GAP-02] the publication block | **HELD, correctly** | `python3 scripts/check_publish_block.py` → `HELD STC — the central is −13.0% below the price of 43.86 (2026-09-03), past the 10% publication limit — and no market dissent is filed`; book-wide `0 may publish, 11 HELD on the gap, 11 HELD on the method`. Both conditions bind: the one-sided gap block, and Phase 1 of the method reassessment being unproven (2 of 6 acceptance criteria open). The study says so on its own first page — *"THAT GAP IS WHY THIS STUDY IS HELD"* — and `assets/data.js` still carries the superseded 09-Jul-2026 `fair{}`, so nothing has reached the live site. |
| 40 | Structure matches the model report — 16 Word sections | **PASS** | `python3 scripts/check_document_structure.py --show STC` → `status: ok`, carries = wanted, all **36 section markers** read off the delivered `.docx` and compared against markers derived from `engine/model_report/MODEL_REPORT_09-08-2026.docx`: Headline · Valuation summary · Company overview · 1 · 1.1–1.9 · 2 · 3 · 4 · 5 · 6 · 7 · Appendix A · A.1–A.3 · Appendix B · B.1–B.3 · Appendix C · C.1–C.6 · About this series · Disclosure. STC appears in `NOW PASSING — remove from the list (1)`. |
| 41 | Structure matches the model report — 16 Excel sheets | **PASS** | `python3 scripts/check_workbook_structure.py` → STC among the 21 that `MATCH THE MODEL REPORT`. Read off the delivered file: `wb.sheetnames == research_protocol.MODEL_STUDY['excel_sheets']` — READ FIRST · Summary · Fundamental Valuation · Assumptions · SOTP Bridge · Segments · Relative & Normalized · DCF · Income Statement · Balance Sheet · Cash Flow · Summary Financials · Monte Carlo · Sensitivity · Per-Share & Ratios · Peer & Sector, in that order. |
| 42 | Every figure a reader sees is reconciled against the model | **PASS on the two documents, FAIL on the workbook** | `python3 scripts/check_prose_figures.py` → `STC prose figures checked: 377; unmatched: 0`. `python3 scripts/check_sign_convention.py` → 45 documents, 98 tables carrying a deduction row, **0 findings**. **But `prose_check.py` declares only the study and the bibliography**; the workbook is outside its population, which is how the stale spot, the stale balance-sheet date and the stale market-cap arithmetic of item 15a survived. |
| 43 | **Read the rendered PDF as images** | **DONE — and it is what caught the most** | All 44 pages of both PDFs rendered and read as images, not as extracted text. It caught: the **enterprise-to-equity bridge that does not foot by SAR 5,164mn** (item 29); §1.5's **"We weight the DCF most heavily"** and the 35/25/20/20 weight column contradicting §4's "No weights are applied anywhere" (item 30); the §1.8 cost-of-debt cell claiming a rate **above** a sovereign yield printed two rows above it and larger (item 31); the **retired session-counted unit** in Figure 4's axis against §3's own calendar-only sentence (item 17); the label collision on Figure 1 and the low-contrast median labels on Figures 5 and 6; and the stale-prose cluster in item 44. |
| 44 | The delivered prose describes the model that was actually built | **FAIL** | Three passages carried over from the previous edition, all naming Q1-2026 as the latest period while the bridge, the base year and the first forecast year stand on the reviewed **30 June 2026** interim: the **READ FIRST panel** — *"Q1-2026 release, 28 Apr 2026; Q1-2026 interim financial statements"*; **Appendix A's source line** — *"Q1-2026 release and interim FS"*; and **Company overview**, whose "Balance sheet" row reads *"SAR 7.1 bn at Q1-26"* against a model net debt of 8,375.4 and whose "Corporate events" row says *"2Q26 results due ~late Jul 2026"* — results the study itself has read and used. The workbook's READ FIRST repeats it and adds a **two-month-stale spot of SAR 43.58 (7 Jul 2026)**. Separately, §1.6 and the Segments sheet title both say **eleven** disclosed operating segments while the model carries **twelve** lines before eliminations (eleven named plus note 9's own "Other operating segments" residual). |
| 45 | Delivered documents carry their own edition date | **FAIL** | `python3 scripts/check_edition_date.py` → **NEW**: *"STC_Valuation_Study_05-09-2026_public.docx — the edition date 2026-09-05 appears only at paragraph 53, not in the masthead"*, and the gate goes red on it. Confirmed by reading the masthead: paragraphs 1–4 carry the title, the entity, the lens line and the anchor, and **no date**; 2026-09-05 first appears inside §1.8's honesty note. Population: 34 `.docx` + 33 `.pdf` delivered studies, 18 stating their own edition. |
| 46 | Delivered documents free of rule identifiers and repository paths | **PASS** | `python3 scripts/check_delivered_vocabulary.py` — 45 documents across 24 study directories, matched by shape → **STC CLEAN**, `OK — no new leaks`. |
| 47 | Every non-sourced driver logged to the Driver Ledger | **FAIL** | `engine/Fundamental_Driver_Ledger.md` contains **no STC entry** — the file's own header states that entries for studies delivered before 30-Aug-2026 were never compiled, but STC is a 5-Sep-2026 rebuild and its judgement drivers are named in its own bibliography (the justified through-cycle P/E of 15.0×, *"a stated judgement rather than a measurement"*; the DDM terminal dividend growth of 3.0%; the capital-intensity path). None is logged. No gate covers this, which is why it is easy to miss. |
| 48 | The walk-forward scope decision stated in the study | **FAIL** | [R-FCAL-01] requires the scope decision to be **stated in the study**, in the protocol's own words when it was skipped — *"walk-forward not run — insufficient sourceable history (N years)"* — and it is a standing step of every new study **and every update**. Neither delivered document, nor `study_numbers.json`, nor the QC-relevant records carry any scope decision, in those words or any others. `python3 scripts/check_valuation_calibration.py` and `check_lessons_register.py` both pass, and neither reaches this: `check_valuation_inputs.py` lists **5 walk-forward runs on disk (AMOC, ARCC, EGCH, PHDC, TMGH)** and STC is not one of them. |
| 49 | **What do the filings disclose that the model does not consume?** | **Answered — and the answer is material** | Asked explicitly against the Sweep Register and the eight source documents in `src/`. **From the H1-2026 earnings presentation, a document this study already registers and draws exactly 2 inputs from:** capital expenditure of **SAR 4.8bn at a capex intensity of 11.9%** (against 9.4% in H1-2025), unconsumed while the model holds capex intensity flat at 14.96% measured off three full years — the study's own crux driver; **free cash flow of SAR 3.6bn, +264.5%**; the company's **own net-debt definition and value, SAR 9.534bn at 30-Jun-2026** (total debt less non-banking cash and short-term murabahas) against the model's 8,375.4 — the MODON precedent says a newer release's anchors supersede or the study says why not, and it does not; **total assets SAR 166.997bn**; **net debt / EBITDA 0.38× (LTM)**; and four credit ratings (Fitch A+, S&P A+, Moody's Aa3, Tassnief AAA) directly relevant to the cost of debt built from the sukuk. **Unit data for segments the study says have none:** the driver row asserts *"No unit data is disclosed for any of them"*, while the same presentation discloses **fibre-optic connections 3.87mn (from 3.68mn)**, **stc tv 6.34mn subscribers (from 5.39mn)** and **STC Bank 8.5mn+ customers**, and the mobile base is disclosed split **prepaid / postpaid / M2M** with fixed-wireless separately — none consumed, and no ARPU by category built. **From the audited statements:** note 35's seven cost lines by nature × three years, plus the government-charges, selling-and-marketing and general-and-administrative sub-notes — all decomposed in `cost_stack.json` and `cost_decomposition.json`, **none consumed by the model**; note 32's disclosed 90–107 day settlement range, measured against and not used; and the reviewed 30-June-2026 balance sheet, used for the bridge but printed nowhere in Appendix A.2. **Named and priced-away rather than unconsumed:** the Group Efficiency Programme, correctly not taken as guidance — but not scored either. |

---

## What blocks delivery — the shortest list

1. **The delivered workbook does not reproduce the study's answer.** Independently recalculated it publishes a central of **SAR 28.94** and a gap of **−34.0%** against a document publishing 38.14 and −13.0%. Root cause is a label collision in `_asm_rows.json` that puts the gross margin (0.485) into every segment's revenue formula, halving revenue every forecast year and turning profit negative from FY28E. *(items 15, 15c, 11)*
2. **Three CI gates go red with NEW violations, all of them STC's**: `check_lens_vocabulary`, `check_edition_date`, `check_column_widths`. *(items 30, 45, 18)*
3. **§1.5 publishes the retired four-lens blend at typed weights** (35/25/20/20) under the sentence "We weight the DCF most heavily", and §4 says "No weights are applied anywhere". *(item 30)*
4. **The §1.1 enterprise-to-equity bridge does not foot**: the printed rows reach 185,283 against a printed 190,447, the SAR 5,164mn investment-funds line printed nowhere; and the workbook prints two different DCF values per share from the same model. *(item 29)*
5. **Stale prose across READ FIRST, Company overview, Appendix A and the workbook's own front sheet** names Q1-2026 as the latest period, and the workbook a spot of SAR 43.58 from 7 July. *(items 44, 15a)*
6. **Sweep/ring vocabulary reaches the reader in both documents**, and the study carries no scrub of its own. *(item 16)*
7. **Two of the three named gates never execute** on this study, the standard-version stamp is absent, the ground-up clause has no driver record, no walk-forward scope decision is stated, and no driver is logged to the ledger. *(items 1, 2, 4, 48, 47)*

## Knowingly outstanding, recorded rather than passed or hidden

- **No forecast balance sheet in A.2/A.3.** The reviewed 30-June-2026 interim does not foot in its own current column — four subtotals and both totals — while every prior-year column foots exactly. Established by re-fetch, re-extraction and OCR off the rendered pixels at 300 dpi, not assumed; `balance_sheet.py`'s `check()` fails both ways and prints the six residuals. Unblocked by the FY2026 audited statements or a corrected interim. *(item 21)*
- **[R-ENF-05] was outstanding when this audit opened and was closed while it ran.** At `14dfa3dc` the study published no reverse read and no contested-judgement sign test. Commit `323ab306` (12:07–12:14 UTC) added both and rebuilt the delivered documents; the reverse read is now in §4 and the gate reports STC conforming. The sign test is recorded but printed in neither delivered document. *(item 36)*

## What was left alone

Nothing in this study was edited. The only file written is this one. `beta_result.json` was modified by running the study's own `beta_reissue.py` from outside and was restored to its committed state in the same pass (`git checkout --`); the diff it produced is reported as evidence in item 3a. `inputs_register.py`, `sweep.py` and `balance_sheet.py` were re-run and rewrote their own outputs byte-identically. The working tree is clean of this audit's changes.
