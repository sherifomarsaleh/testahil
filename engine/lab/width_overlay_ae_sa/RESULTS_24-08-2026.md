# Per-name width overlay — AE/SA promotion gate — 24-Aug-2026 — BOTH DECLINED

Run per instruction ("stop looking at stocks in a country as a bulk"), replaying the
adopted EG mechanism (`adaptive_width` constants imported, walk-forward, MIN_WINDOWS=28)
on every AE and SA name's own library, under the same gate the EG adoption cleared on
23-Jul-2026. Full numbers: `RESULTS_24-08-2026.json`; harness: `validate.py`.

| gate | AE (28 names) | SA (13 names) |
|---|---|---|
| proper score parity, blocks {2,3,4} | PARITY ✓ | PARITY ✓ |
| pooled \|std_u − 1\| improves | 0.0361 → 0.0215 ✓ | 0.0514 → 0.0533 ✗ |
| majority of moved names closer | 7 of 14 ✗ | 7 of 9 ✓ |
| cov90 stays in band | 0.912 → 0.910 ✓ | 0.909 → 0.905 ✓ |
| **verdict** | **DECLINED (breadth)** | **DECLINED (pooled)** |

Why the GCC is not Egypt yet: EG passed with 24/30 names improving on long histories and
a big pooled dispersion to fix (0.096 → 0.069). AE's pooled dispersion is already small
(0.036 — the pooled fit serves it well), and 16 of its 28 names are 2021-plus listings
still below the history gate, so the overlay barely acts; SA's miss is its under-covered
names (ALINMA/SABIC/ELM std_u 1.20-1.27) pulling the pooled measure the wrong way when
the gated subset moves. The mechanism is not wrong — the evidence is thin where the
histories are short. Per the promotion rule, a mechanism that does not beat the pooled
cone on a market's own panel does not ship there on anyone's say-so — the same rule that
declined EG's mid-band reshape and SA's on 24-Aug.

What GO looks like (a gate with no release is a stall): re-run this harness at any
roll-forward — one command, `python3 engine/lab/width_overlay_ae_sa/validate.py` — and
adopt per market when all four rows pass. The natural re-run trigger is the young AE
names crossing 28 resolved windows as their libraries lengthen; nothing else needs to
change first. EG's overlay is untouched by this result and remains live.
