# PHAR — QC gate, 09-09-2026

Egyptian International Pharmaceutical Industries Company (EIPICO) · The Egyptian Exchange:
PHAR · EGP · edition of 09-09-2026

Filled from OUTSIDE the study per [R-ENF-02] by an auditor who did not build it. Nothing
here is self-certified: every row names the artefact, the command or the number that
carries it. Where a row is met with a gap, the gap is stated in the same breath.

**VERDICT: NOT DELIVERABLE.** Ten hard fails, listed at the foot. The MODEL underneath is
in very good order and should not be lost — the workbook reproduces cell for cell against
a sandbox rebuild across 2,077 populated cells, all 883 formula cells recalculate, the
free-cash-flow waterfall foots by hand on all five years and runs inline to present value,
the bridge foots to the published per-share, the beta is the sanctioned EGX30 regression
with its withdrawn composite kept beside it, margins are outputs of a disclosed six-line
cost stack, and the bibliography carries all 278 inputs four-field complete. What fails is
the PAPER. **Six of the eight figures in a 26-page document publish the withdrawn
pre-correction numbers** — the headline figure on page 2 contradicts the summary table on
page 3 on all five readings. The masthead states the wrong edition date and the wrong price
date, which is the exact defect this edition was built to close. The struck spot of 127.30
is the 1-September close in this repository's own library, not the 3-September close of
129.70 it is registered as. And the cost of debt is still built on a risk-free print the
same study withdrew as stale.

---

## The edition audited, and how it was resolved

Resolved through `engine/phar_study/edition.py` (`EDITION = 2026-09-09`,
`SUPERSEDES = (2026-08-09,)`), not by globbing a date. Every study script that opens a
deliverable — `qc_checks.py`, `recalc.py`, `driver_test.py`, `footing_check.py`,
`prose_check.py` — imports `edition` and resolves the name from it. Confirmed by reading
the source, not by trusting the report. That instrument works; the problem is that the
DOCUMENT's own masthead does not read it.

| Deliverable | File | Built |
|---|---|---|
| Study, Word | `EIPICO_Valuation_Study_09-09-2026.docx` | 2026-09-09 11:50 |
| Study, PDF | `EIPICO_Valuation_Study_09-09-2026.pdf` — **26 pages, 25 tables, 8 figures** | 2026-09-09 12:00 |
| Workbook | `EIPICO_Valuation_Model_09092026.xlsx` — 16 sheets, 883 formula cells, 2,077 populated | 2026-09-09 11:43 |
| Workbook, PDF | **ABSENT for this edition** — only `EIPICO_Valuation_Model_09082026.pdf` (2026-09-08 22:09) is on disk | — |
| Bibliography | `EIPICO_Bibliography_09-09-2026.docx` / `.pdf` — 25 pages, 13 tables, 278-row input register | 2026-09-09 11:43 / 12:00 |
| Gap review | `GAP_REVIEW_08-09-2026.md` — 156 lines, eight headings + verdict, dated ONE DAY before the edition | 2026-09-08 |
| Figures | `fig1…fig8.png` — **all eight last written 2026-09-07 19:37**, against `study_numbers.json` at 2026-09-09 11:42 | 2026-09-07 |

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker and exchange | PHAR · EGX · market code EG | `assets/data.js` `code` prefix `EGX:PHAR`, read through a real `node` parse of the object the page renders, never a regex or a folder name [R-ENF-03]. |
| Regressor the beta rule demands | **EGX30**, `engine/raw_indices/EG/EGX30.csv` | `wacc_builder.EXCHANGE_INDEX[('EG','EGX')]` → `EGX30`; file present and registered. Not a DFM name, so `index_interim_note('AE','DFM')` does not apply; not Qatari, so no QATAR10 weekly-only caveat applies. |
| Class and pattern | Pharmaceutical manufacturer, generic and branded · **operating company** | `meta.company_class` = "Operating company (vertically integrated pharmaceutical manufacturer and exporter)", `meta.reference_pattern` = "Operating company". The nearest of the three closed references is ADNOCLS and the study adapts its skeleton. `LENS_REGISTRY['pharmaceutical manufacturer, generic and branded']` = `('dcf', ('ev_ebitda_own_history','relative_multiple','book_value'))`. |
| Committed central | **TWO, never one: Frame A EGP 31.373799 · Frame B EGP 47.603463** | `study_numbers.json.central_two_sided.branches`; `lens_record.primary.two_sided = true`, `value = null`. Deliberate and correct — the provision charge is the contested judgement and averaging the frames would state a number neither supports. |
| Committed spot | **EGP 127.30, registered as the close of 03-Sep-2026** | `meta.spot` / `spot_date`; `inputs.spot.date = "2026-09-03"`. **See row 4 — that date does not match this repository's own price library.** |
| Gap on the committed spot | **−75.4% (A) · −62.6% (B)** | `python3 engine/prices/gap_today.py` → `PHAR 31.37 127.30 127.30 −75.4% BREACH`. |
| Gap on the latest known price | **−75.5% (A)** against 128.00 of 06-Sep-2026 | `scripts/check_valuation_gap.py`. Both readings far past the 10% trigger, same side. |
| Routing | **HELD under [R-GAP-02], correctly** | `scripts/check_publish_block.py`: `HELD PHAR every branch is more than 10% BELOW the price (highest Frame B … at −62.8%) — and no market dissent is filed`. This is routing, not a defect, and it is not counted against the study anywhere below. |
| What the site publishes today | fair `{58.04, 61.21, 73.03}`, spot 128.00 of 06-Sep-2026 | Real `node` parse of `assets/data.js`. `scripts/check_published_gap.py`: `PHAR −52.2% … the site publishes 61.21`. |

---

## Run as CI runs it, not as a sweep of the check scripts

`python3 scripts/run_ci_gates.py` was run over CI's own step list rather than over a
hand-picked set of checkers [R-ENF-04]. It ends `CI would be red`. **PHAR is named in three
of the RED steps**, and two of them are NEW rather than ratcheted:

- `Every delivered study states its own edition date` — `[NEW] EIPICO_Valuation_Study_09-09-2026.docx the masthead states '9 August 2026' and the file is dated 2026-09-09` (row 5)
- `Column widths — every figure a reader sees fits its own column` — eleven NEW columns in `EIPICO_Bibliography_09-09-2026.docx` (row 8)
- `a calibrated name ships a report, its PDF, a workbook, a standalone bibliography and a QC gate, all of one edition` — `NEW failures 1: PHAR the QC gate is dated 2026-08-09 while the edition is 2026-09-09`. **This document closes that one.** The other two survive it, and so does row 13's missing model PDF, which that step does not test for.

The remaining RED steps belong to ARCC, SWDY and the tree-baseline gate, not to this study.

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates called in the study's own committed code [R-ENF-02] | **FAIL** | `grep -rn "assert_sigcm\|assert_beta_provenance\|assert_model_study\|assert_ground_up" engine/phar_study/*.py` across **25 Python files** returns exactly **one** line, and it is prose: `beta_reg.py:31: assert_beta_provenance() can inspect the record rather than trust a boolean the study`. That is a sentence inside a docstring. Not one line of this study EXECUTES any of the three. `check_study_provenance.audit()` returns `(True, [])` for this directory **because its test is a substring match over the file text** — a docstring satisfies it. The repo gate is green on PHAR for a reason that is not the reason it exists. `bridge_record.py:203` does call `RP.assert_bridge(record,'PHAR')`, which is real and is the only protocol assertion this study runs. |
| 2 | Beta on its record, never on a boolean — SIGCM clause 6, [R-BETA-04] | **PASS** | `beta_result.json` carries the full record: `index_file: "raw_indices/EG/EGX30.csv"`, `index_asof: 2026-09-08`, `market EG`, `exchange EGX`, `conforming: true`, `week_rule W-THU`, `dimson: true`, β **0.6658**, R² **0.1427**, n **256**, SE **0.1355**, window **4.94 years** 2021-09-23 → 2026-09-03, `usable: true` (`gate_msg: "passes minimum usability gate"`). Produced by `beta_reg.py`, which imports `beta_regression.own_stock_beta('PHAR','EG','EGX')` and hand-rolls nothing (`grep -n "lstsq\|polyfit\|cov(" beta_reg.py` → no matches outside the docstring). Usability gate cleared on all three legs: n 256 ≥ 24, R² 14.3% ≥ 5%, SE 0.1355 < |β| 0.666, so no fall to a peer beta or 1.0 is owed. The withdrawn composite is kept, not deleted: `withdrawn_composite` records β 0.6295 / R² 0.235 / n 257 / SE 0.0711 with the reason. `check_study_provenance.py` names no PHAR violation. |
| 2a | The beta as the delivered documents state it | **PASS** | Study Table 10, Beta row, verbatim: *"0.666 … Own-stock weekly regression against the published EGX30 index of the exchange this share is listed on, 4.94 years; R-squared 0.143, n = 256, standard error 0.135."* The live figures, not the withdrawn ones. Table 19's caveat and §7 both describe the composite as a PREVIOUS edition's construction and explain why a better fit against the wrong regressor is not evidence for it. **Searched both delivered documents for the withdrawn statistics** `0.235`, `0.071`, `257` and for any present-tense description of a composite as the method in force: none. |
| 2b | Bibliography §6.2 | **PASS** | Read as a rendered image on page 24: *"The beta of 0.666 is a weekly regression of this company's own returns against the published EGX30 index of the exchange this share is listed on, over 4.94 years and 256 weekly observations… The index series was read as at 2026-09-08."* And: *"THE REGRESSOR IS THE PUBLISHED INDEX AND NOT A BASKET OF THIS HOUSE'S OWN COVERAGE."* The rewrite landed. |
| 2c | The one arithmetic claim §6.2 makes about itself | **FAIL** | Same paragraph: *"The standard error implied by the reported coefficient, R-squared and sample size is **0.10240**, against the **0.13548** reported — **they agree**, so the three statistics are mutually consistent rather than separately asserted."* They do not agree: 0.13548 / 0.10240 = **1.32**, a 32% difference. The OLS identity β·√((1−R²)/(R²(n−2))) = 0.6658·√(0.85731/36.243) = 0.10240 is right; the reported SE is larger because it is Dimson-corrected, which is a real and defensible reason the paragraph never gives. A cross-check that FAILED is printed as one that passed, in the document whose whole job is letting a reader check. |
| 3 | Ground-up clause on a driver record, not a flag [R-SIGCM-02] | **FAIL — no `DriverLine` record exists** | `study_numbers.json` carries no `ground_up` key of any kind; `assert_ground_up()` cannot be run because there is no `DriverLine` list to run it on. `scripts/check_ground_up.py`: `known PHAR commits no driver-line record at all; [R-SIGCM-02] retired the boolean that used to stand in for one`. **The share-by-level split, reconstructed by hand from `study_numbers.json.unit_build` because the study commits none:** revenue is built from three named lines covering **100%** of the company-only book of EGP 9,302mn — domestic own preparations EGP 6,192.7mn (**66.6%**), export EGP 2,967.5mn (**31.9%**), contract manufacturing fee + resale EGP 142.3mn (**1.5%**) — reaching consolidated EGP 9,441mn through a disclosed 1.0149 subsidiary uplift. On every line the **unit is the pack and it is sourced** (291.81mn domestic and 60.0mn export packs, board of directors' report FY2025 and the investor presentation), and the **price basis is DERIVED**, revenue ÷ packs, not quoted. So the honest level is `unit` on volume and `derived` on price, on 100% of revenue. That is a good build. It is attested by nothing the rule can read. |
| 3a | The gap flag SIGCM clause 2 requires | **PASS** | The negative-results table in the bibliography, row 2, verbatim: *"Volume, price or utilisation guidance for the biologicals plant — sought in all four annual reports, the investor presentation, and every 2026 press release. The plant is charged but not credited, and the required revenue is solved for and published."* Repeated in the READ FIRST panel (*"What this study deliberately does NOT do"*) and in Table 19. The largest gap in the build is stated on page 1. |
| 4 | The struck spot is the price the study says it is | **HARD FAIL** | `inputs.spot` = **127.30**, `date` **2026-09-03**, source *"Egyptian Exchange close, 3 September 2026."* This repository's own library says otherwise. `engine/raw_ohlc/EG/PHAR.csv` (MM/DD/YYYY, confirmed against the Jan-2011 tail): `"09/03/2026","129.70"` and `"09/01/2026","127.30"`. **127.30 is the close of 1 September 2026. The 3 September close was 129.70.** The number came from `engine/prices/SUPPLIED_03-09-2026.json` (`"PHAR": {"price": 127.3, "date": "2026-09-03"}`), whose own header warns *"These are CLOSES on the date each row carries, not all the same day."* So two committed records in this repository disagree by **1.89%** on the same date, and neither delivered document records the discrepancy — in a bibliography that has a whole table for exactly that (§5, six rows). `scripts/check_spot_currency.py` reports `PHAR 127.3 as at 2026-09-03, the latest supplied price` and is clean, because it reads the supplied file and never crosses it against the OHLC library. Everything struck on the spot inherits it: market capitalisation 21,483, the 14.90x and 11.6x trailing multiples, the −75.4% gap, the reverse-valuation crux (EGP 95.93 a share, EGP 16,188mn, 1.33x asset turn) and the market-price line on three figures. On the library's 3-September close the Frame A gap is −75.8%, so no conclusion moves; the DATE claim is what is false, and making the date claim true is what this edition existed to do. |
| 5 | The masthead states this edition's own date and its own price date | **HARD FAIL** | Page 1, read as a rendered image: *"**Prepared 9 August 2026** · share price EGP 127.30 at **the close of 6 August 2026** · 168.756 million shares in issue."* Against `meta.study_date = "2026-09-09"` and `meta.price_date = "2026-09-03"`. Both dates are **typed literals** — `docx_phar.py:63`: `P(f'Prepared 9 August 2026 · share price EGP {n2(SPOT)} at the close of 6 August 2026 · '`. Only the FILENAME is derived (`docx_phar.py:1563` reads `_ed.STUDY_DOCX`). `scripts/check_edition_date.py` **exits 1** with this as a NEW violation: `[NEW] EIPICO_Valuation_Study_09-09-2026.docx the masthead states '9 August 2026' and the file is dated 2026-09-09`. The bibliography masthead repeats it: *"the companion to the valuation study of **9 August 2026**"*, and its §1 adds *"All were downloaded directly from the company's own website on 9 August 2026."* The document then contradicts itself four pages later: Table 1's last row reads *"Market price 127.30 · **Close of 3 September 2026**."* A reader is given three dates for one price. |
| 6 | Figures carry the numbers the study publishes | **HARD FAIL — six of eight** | The whole study directory was copied to a sandbox and `figures.py` re-run there against the committed `study_numbers.json`, so the real tree was never written to. **Six of the eight PNGs differ:** fig1_field, fig4_multiple, fig5_cone, fig6_tornado, fig7_crux, fig8_experts. Only fig2 and fig3 reproduce. Cause: all eight delivered PNGs were last written **2026-09-07 19:37**, `figures.py` itself **2026-09-04**, while `study_numbers.json` was rebuilt **2026-09-09 11:42** and the documents at 11:43–12:00. **The beta correction reached the model and the prose and never reached the figures.** What a reader actually receives: <br>• **Figure 1**, the headline, page 2: bars **33.00 / 49.68 / 43.91 / 48.65 / 56.78** and centre lines *"centre A 33.0"*, *"centre B 49.7"*. The rebuilt figure prints **31.37 / 47.60 / 42.80 / 47.89 / 55.34** and *"centre A 31.4"*, *"centre B 47.6"*. Every one of the five readings is a withdrawn number, overstated by **+5.18% / +4.36% / +2.60% / +1.59% / +2.60%** — and Table 1 on the very next page prints the correct five. <br>• **Figure 5** (crux): floor label *"the model without a revenue line for the new plant **33**"* and *"**EGP 8,871m** required (**USD 127m** a year)"*, against the body text on the facing page: *"an additional **EGP 9,322 million** of revenue by FY2030 … about **USD 133 million** a year"*, and against Expert 3's own table on page 24 which prints 9,322 and USD 133m. <br>• **Figure 6** (tornado): title *"Frame A base **33.0** per share"*, printed directly beneath Table 11 whose adopted row reads **31.37**. <br>• **Figure 7** (cone): *"fundamental centre, Frame B **50**"* and *"fundamental centre, Frame A **33**"*, against 47.60 and 31.37. <br>• **Figure 8** (experts): *"Expert 2 … 37–53 · worked value **43.9**"* and *"Expert 1 … 15–51 · worked value **33.0**"*, against C.1's table ending 31.37 and C.2's ending 42.80 — and against C.5's own prose on the same page: *"Expert 2 says … so EGP **42.80**."* The rebuilt figure gives 36–51 / 42.8 and 13–49 / 31.4. <br>No programmatic check in this repository looks at the numerals inside a figure. This was found by reading the rendered PDF. |
| 7 | Figure discipline — opacity and canvas [standard 5] | **PASS** | `scripts/check_figure_opacity.py`: *"every delivered figure is opaque to the pixel"* — measured on the alpha channel, not on the colour mode. `qc_checks.py`: `figure discipline: 8 figures checked, 0 problem(s)`, corner luminance on a solid light canvas. All eight inspected as rendered images in the PDF. Two label collisions survive and are named rather than passed over: on **Figure 2** the *"four-year mean 6.6"* annotation overprints the 6.6x bar label and *"multiple this model justifies 6.3x"* overprints the 6.2x label; on **Figure 4** the FY23 value label 105 is overprinted by the line marker. The CONTENT failure in six figures is row 6, not this row. |
| 8 | Table discipline — the three counts, per document [standard 6] | **FAIL** | Measured independently against a 7.00in text block. **Study:** 33 tables · starved 0 · bloated 0 · **over-wide 5** · non-fixed layout 0 · no explicit width 0. **Bibliography:** 13 tables · starved 0 · bloated 0 · over-wide 0 · non-fixed 0 · no explicit width 0. The five over-wide tables and how far past the text block each runs: Table 9 (Q1 comparison) 7.05in, Table 16 (lens comparison) 7.40in, **Table 20 (income statement) 7.48in**, **Table 21 (balance sheet) 7.48in**, Table 22 (cash-flow markers) 7.30in — visibly spilling past the body-text right margin on pages 10, 15, 19 and 20. The study's own `qc_checks.py` reports `46 tables checked, 0 problem(s)` because **it tests starved and bloated and never tests over-wide** (`qc_checks.py` lines 96–130 loop over columns only; no `sum(widths) > text_width` test exists). Separately `scripts/check_column_widths.py` **exits 1** with **11 NEW violations, all in the PHAR bibliography** — the ten PIT-histogram columns `0-10%`…`90-100%` declared 1.35cm against 1.55–1.75cm needed, and `Date` declared 1.68cm against 1.83cm. |
| 9 | Numeric traceability — standard 3 | **PASS** | `python3 recalc.py` on the delivered file: **"RECALCULATION PASSED — 883 of 883 formula cells reproduce the model, 0 unresolvable, 0 unchecked"**, plus 17 headline reconciliations including `value per share, Frame A 31.373799`, `weighted centre — Frame B 47.603463`, `forecast balance sheet balances FY2030E −0.000000`. `python3 driver_test.py`: **"30 asserted directions all correct, 138 further inputs live against at least one headline, 0 dead"**, with 4 rows explicitly declared DISCLOSURE rather than driver. `qc_checks.py`: `numeric traceability: 0 typed financial numeral(s) in builders`. `scripts/check_workbook_values.py` lists PHAR among the 22 CLEAN. Nothing was unparseable, so nothing was skipped. |
| 10 | Script-built file verified by a cell-by-cell diff against the delivered file | **PASS on the workbook and both documents** | The directory was copied to a sandbox and rebuilt there. **Workbook:** `build_xlsx_phar.py` re-run, then every populated cell of all 16 sheets compared — **2,077 cells, 0 differences**, sheet names and order identical. **Documents:** `docx_phar.py` and `docx_biblio.py` re-run — **1,449 / 1,449 paragraph-and-cell strings identical in the study, 1,454 / 1,454 in the bibliography, 0 text differences.** The delivered files ARE what the builders produce; nothing was hand-edited. Which is why row 6 matters: the builders are faithful and the FIGURES they embed were never rebuilt. |
| 11 | Sixteen-sheet Excel structure | **PASS** | `wb.sheetnames == research_protocol.MODEL_STUDY['excel_sheets']` → **True**. Sixteen sheets in the model order, none renamed. `scripts/check_workbook_structure.py` lists PHAR among the 22 MATCHES THE MODEL REPORT. |
| 12 | Word structure against the model report | **PASS, with one gap named** | `scripts/check_document_structure.py` lists PHAR among the 22 MATCHES THE MODEL REPORT. Read page by page: masthead + READ FIRST, Headline, Valuation summary, Company overview, §1.1–1.9, §2–§7, Appendix A (A.1 income statement 3y+5y, A.2 balance sheet, A.3 cash-flow markers, A.4 far-year ranges), Appendix B (B.1 peers, B.2 risks, B.3 research record), Appendix C (C.1–C.6), About this study, Disclosure. **The gap, and it is a rule breach:** §7.1 is *"What changed in this edition, and by how much"* with Table 18, *"every change from the first edition, and its direction"* — 18 rows of edition history. `MODEL_STUDY['excluded_sections'] = ["What changed in these editions, and why"]`. Edition history is internal QC evidence and the model report strips it; this document ships it. It also carries the study's only statement of its terminal return (row 20). |
| 13 | Model deliverable set | **FAIL** | `MODEL_STUDY['deliverables']` requires *"Excel model + PDF"*. `ls EIPICO_Valuation_Model_090*.pdf` returns **one file and it is the superseded one**: `EIPICO_Valuation_Model_09082026.pdf`, written 2026-09-08 22:09. Searched: `31.37` appears **0 times** in it and `47.60` appears **0 times**; `33.00` appears 16 times. A reader who opens the only model PDF on disk gets neither of the answers this edition publishes. |
| 14 | Cost of capital under the v2 method [R-COC-01] | **FAIL on one input, PASS on the construction** | The construction is right and reproduces by hand off Table 10. Country risk **once**: rf* = 23.00% − 3.42% Egypt's own sovereign CDS spread = **19.58%**, and the premium added back is from the same row of the same dataset (`erp_cds` 9.5164% TOTAL, not the 5.32% country column). The retired double-count is priced beside it: `ke_double_counted_retired = 0.29336` and §1.8 prints *"would give 29.34% and would count sovereign risk twice."* Both bases published (swap 25.92% / rating 26.01%, `crp_cds` 5.32% and `crp_rating` 9.28% shown as memoranda). Market-value equity weights (74% / 26%), both net and gross published (22.97% / 22.56%). Forward rate per explicit year with the terminal on the year-five factor — verified: 1/1.2297 = 0.8132, 0.8132/1.2089 = 0.6727, /1.1862 = 0.5671, /1.1673 = 0.4858, /1.1522 = 0.4216, all matching `wacc.df`. **THE FAILURE:** `inputs.kd_egp = 0.2481` and its own registered source reads *"the ten-year sovereign yield of **22.31%** plus a 250 basis-point corporate credit spread."* 22.31% is the print this same study **withdrew** — `inputs.rf` says it *"was both stale against the pricing date and 39 basis points below the observable level on its own date."* So the cost of equity stands on 23.00% and the cost of debt on the number the study calls wrong, in one discount rate. Table 10 prints *"Cost of debt, local currency 24.81% — Sovereign yield plus 250 basis points"* two rows under a 23.00% row: 23.00 + 2.50 = **25.50**, not 24.81, a **69 basis point** shortfall the table's own stated construction does not reproduce. Worth ≈ +7bp on WACC (69bp × 49.8% local share × 0.765 after tax × 25.5% debt weight) — small in value and a stale, self-contradicted input in the one place the study most insists is built rather than asserted. `22.31` appears **0 times** in the delivered document, so a reader cannot find the reason. |
| 15 | Ke reproduces from its own inputs [R-COC-02] | **FAIL — the record the rule reads does not exist** | `scripts/check_ke_reproduction.py` reads **10** cost-of-capital records and **PHAR is not among them**; `scripts/check_cost_of_capital.py`: `PHAR carries no cost-of-capital schedule record`. The assertion therefore cannot run. **Reproduced by hand instead, and it holds:** explicit Ke = 19.58% + 0.6658 × 9.5164% = **25.92%** = `wacc.ke` 0.259162 ✓; rating basis 17.03% + 0.6658 × 13.4806% = **26.01%** = `ke_rating` 0.260054 ✓; terminal Ke = 12.50% + 0.6658 × 7.00% = **17.16%** = `ke_term` 0.171607 ✓; terminal WACC = 0.7447 × 17.161% + 0.2553 × 9.561% = **15.221%** = `wacc_term` ✓. The terminal uses the SAME beta, so the construction is `same_beta` — **and the record names no construction at all**, which is exactly what [R-COC-02]'s closed list exists to stop. |
| 16 | Terminal basis and the spread it reinvests at | **FAIL** | `scripts/check_terminal_floor.py` puts PHAR among those *"built on something else (the cycle does not equal 1/g)"* — good; terminal growth 7.0% is DERIVED from the house terminal inflation at zero real growth, and `macro_record.terminal.note` records that the previous edition typed 5% nominal, which against a 7% long-run rate is a 1.87% real decline in perpetuity nothing argued for. The terminal is fed the last forecast year's cash flow grown one year (verified: 1,401 × 1.07 / 0.082206 = 18,236 against a printed 18,229; × 0.4216 = 7,685 against a printed 7,686). **The failure:** `scripts/check_terminal_spread.py`: `PHAR terminal return roic_term **0.1273** sits BELOW terminal cost of capital wacc_term **0.1522** (**−249bp**) while reinvest_rate reinvests **55.0%** of terminal profit — value destroyed in perpetuity to buy growth, and no terminal_spread_reason declares it`. The number is not hidden — but the ONLY place it appears in the delivered document is inside Table 18, the edition-history table row 12 says should not be there, and it is never set beside the 15.22% terminal rate printed six pages earlier. Searched the document: `12.73` appears once, `destroy` appears **0 times**. A terminal reinvesting more than half its profit below its cost of capital is a first-order claim about the answer and the reader is never told. |
| 17 | 3y historical + 5y forecast, full FCFF waterfall inline to PV of FCFF | **PASS** | Appendix A.1 runs FY2023/24/25 actual and FY2026E–FY2030E. Table 4 in §1.1 carries every line to the end: Revenue → EBITDA → EBITDA margin → D&A → EBIT → NOPAT = EBIT×(1−t) → add back D&A → capital expenditure → change in working capital → **free cash flow to the firm** → discount rate → discount factor → **present value of free cash flow**. **Footed by hand on all five years off the rendered page:** FY2026 1,615+304−1,337−910 = −328 ✓; FY2027 1,724+425−568−1,020 = 561 vs 562 (rounding); FY2028 1,982+446−512−869 = 1,047 vs 1,048; FY2029 2,262+437−532−877 = 1,290 ✓; FY2030 2,517+423−558−829 = 1,553 ✓. Present values −267/378/594/627/655 sum to 1,987 against a printed 1,986. Stopping at FCFF would be a hard fail; it does not stop. |
| 18 | The enterprise-to-equity bridge [R-BRIDGE-01] | **FAIL — it stands on a superseded sheet the study itself has open** | `scripts/check_bridge.py`: `BRIDGE FAIL -- PHAR: the bridge stands on the **2025-12-31** balance sheet while the latest disclosed is **2026-03-31**`. This is not a filing the study lacks: **25 registered inputs come out of the 31-Mar-2026 reviewed interim**, including `q1_ppe`, `q1_cip`, `q1_debt` 9,428.0, `q1_cash` 362.8, `q1_ar` and `q1_nci`. The study knows what it costs and says so — Table 19: *"net debt was EGP 9,065m at 31 March against EGP 7,364m at 31 December … On the March net debt alone the value would be about **EGP 10 a share lower**."* EGP 10 against a Frame A central of 31.37 is **32% of the answer**. The stated reason — that using the March sheet with a full forecast year would charge the same quarter twice — is a real argument, and it is an argument for restating the forecast, not for standing on the older sheet. On the arithmetic the bridge itself is clean: 1,986 + 7,686 = 9,672 core, + 2,750 associates + 228 + 12 = 12,663 total, − 7,364 net debt − 4 minority = 5,295, ÷ 168.755750mn = **31.37** ✓, terminal published on BOTH bases (79% of core, 61% of total) as a line of the bridge, and the two consolidation perimeters disclosed with the EGP 3.04/share they are worth. `scripts/check_bridge_reaches_answer.py` is green. |
| 19 | Macro coherence [R-MACRO-01] | **PASS, with one registered tension** | `scripts/check_macro_coherence.py` green with no PHAR entry. `macro_record` carries the house EG path at `path_as_of 2026-09-02` and states *"the house calendar ladder exactly, at zero real: this study carries no inflation rate of its own."* Terminal growth 7.0% DERIVED from terminal inflation 7.0% at zero real. The currency path's own first-year defect is registered rather than smoothed: *"The derivation compounds a full year of purchasing-power depreciation onto a quote taken EIGHT MONTHS INTO that year, so the FY2026 average comes out at 56.87 while the same house path reads 50.25 on 6 August 2026."* `scripts/check_macro_anchor_age.py`: `listed PHAR EG struck 28 days after the EG currency anchor it derives from` — on the ratchet. |
| 20 | One escalator per driver class, with a sourced near-term cost anchor | **PASS on the split, FAIL on the anchor** | Five distinct escalators on physically distinct lines, no blended index anywhere: `esc_materials_usd` (hard-currency API path through the model's own FX path), `esc_packaging_import_share` 55%, `esc_labour` 14/12/10/8.5/7.5, `esc_energy` 18/15/12/10/8, `esc_domestic_cpi` on the house ladder. Table 8 in §1.6 publishes the six cost lines with their shares (54.9 / 24.6 / 9.8 / 3.4 / 5.7 / 1.7) and the escalator each carries. **The failure:** only ONE of the five carries a sourced near-term anchor — `esc_labour` anchors on the company's own FY2025 disclosure (*"Headcount grew 2% in FY2025 while the wage bill in cost of sales grew 27%"*). `esc_energy` gives reasoning and **no tariff print, no publisher, no date**; `esc_materials_usd` at 2.0% gives reasoning and **no price index** — and that one escalator governs **54.9% of the production cost base**. Both are ring `House` dated 2026-08-09. |
| 21 | Margins as OUTPUTS wherever the filings disclose enough | **PASS** | Table 7's own stub row reads **"Gross margin (an output)"** and §1.6 closes *"Margins are an OUTPUT of this build, never an input."* The cost side is built as a cash cost per pack off the audited cost-of-sales note's own line items (`unit_build.unit_cost_fy25` = 14.536 EGP/pack). The build is falsifiable and §1.6 shows it being tested: the two disclosures reconcile *"to the thousand pound in both years"* and the channel-versus-pack reconciliation is stated with the 1.50% error it would otherwise carry. `scripts/check_forecast_anchor.py`: `PHAR gross margin latest 0.4239 forecast 0.3919 −3.20pp (mechanism: input_cost_outpacing_price)` — the direction is named and mechanised, not left to drift. |
| 22 | The central contested judgement computed both ways [standard 8] | **PASS** | The provision charge is carried at 5.25% (Frame A) and 2.5% (Frame B) of revenue and published side by side in **all four required places**: the **summary table** (Table 1's two bold CENTRE rows, with *"The two centres are NEVER averaged"*), the **body** (§1.5, §6, §7), the **workbook** (`recalc.py`: `summary sheet centre — Frame A 31.373799`, `Frame B 47.603463`, both live), and an **expert's range** (C.6's divergence table: `Frame A less Frame B −16.23`, `Frame B centre less market price −79.70`). Never averaged: `lens_record.primary.value = null`, and the retired single centre is printed as a labelled MEMO at 39.73/47.85 with Table 18 stating *"weighting both frames inside one number IS averaging them."* Table 11 prices the judgement on **every** reading the statements support — 5.25% → 31.37, 6.52% → 23.14, 3.03% → 45.84, 5.15% → 32.02. |
| 23 | Expert appendix at maximum detail [standard 7] | **PASS, with one gap named** | C.1–C.6 all present. Per expert: worldview, when-it-works / when-it-fails, a worked table with intermediate lines, a named sensitivity WITH numbers (E1: *"every 100 basis points on the terminal rate is worth roughly EGP 5 a share"*; E2: *"at 20% rather than 18.8% the multiple goes to 1.28x and the value to EGP 47.33"*; E3: *"at 35% rather than 45% contribution the required revenue rises to EGP 11,985m, USD 171m a year"*), and a falsifier stated in advance. C.4 cross-examination runs five challenges each explicitly CONCEDED or REJECTED with a qualification. C.5 puts the three in one room. C.6 is a six-row divergence table isolating the assumption behind each gap; its arithmetic checks (31.37 − 42.80 = −11.43 against a printed −11.42; 31.37 − 47.60 = −16.23 ✓). Labels are *"Expert 1 — the cash-flow analyst"*, *"Expert 2 — the asset-and-return analyst"*, *"Expert 3 — the market-implied analyst"* — method descriptors; **no persona name anywhere in either document**. **The gap:** Expert 1's worked table jumps *"Terminal free cash flow 1,401"* → *"rate less growth 8.22%"* → *"Terminal value 18,229"*, and a reader dividing gets 17,044, 6.5% out, because the one-year growth step is the intermediate line the table omits. And Figure 8, which is this appendix's own ranges figure, is stale — row 6. |
| 24 | External-reader scrub [standard 4] | **PASS** | `qc_checks.py`: `external-reader scrub, study: 0 hit(s) · bibliography: 0 hit(s) · workbook: 0 hit(s)`. Re-run independently over both documents' full paragraph and table text for `PASS`, `FAIL`, `PARITY`, `BOUNDARY`, `CRPS`, `VERDICT`, `SIGCM`, `sweep`, `register`, `gate`, `step 0`, `ring`, `mc_v3`, `study_numbers` and `[R-XXX-NN]` — **zero hits on every one**. `band_record.assert_no_verdict_tokens('EG')` passes. `scripts/check_band_vocabulary.py`: `[R-CAL-02] OK — no verdict vocabulary in any page text; every published band record agrees with its panel`, **exit 0** — the two retired-skill passages are gone. `scripts/check_delivered_vocabulary.py` lists PHAR among the 22 CLEAN. `calibration.step0.verdict = "PARITY"` sits in the numbers file and reaches no reader. Calibration evidence lives in §3 as plain-language sentences with the statistics inline; there is no calibration appendix — the document has exactly three, A, B and C. |
| 25 | Step 0.0 data-quality gate | **PASS** | `step0_result.json`: **3,225 clean rows of 3,228**, `dq_log` carries its one repair rather than an empty list (*"dropped 3 rows carrying a non-positive or non-finite OHLC value (2011-06-15..2011-09-21) — vendor fills, not tradeable prices"*), span **15.59 years**, `max_abs_log` **0.1823** = 18.2% against the EGX ±20% daily limit — a per-market threshold, not a global one. Density 206.8 sessions a year, and the thin pre-2020 tail (162–195 a year against ~245) is disclosed in Table 19 with its consequence stated: *"it affects only the longest calibration window, not the live bands."* |
| 26 | Step 0 calibration under the band record [R-CAL-02] | **PASS, with one gap named** | §3 publishes three window sets with the COUNT in its own column and the percentages beside it, and the bibliography's Table B7 repeats all of it: last five years **19** windows 47% / 89% / 100%, full history **56** windows 52% / 93% / 100%, post-break **16** windows 50% / 94% / 100%, each with chi-square and Kolmogorov–Smirnov p. Every figure reproduces from `calibration.backtest` (five_year cov50 0.47368, cov90 1.0, width 1.4686, chi2 p 0.447, ks p 0.676) — checked cell by cell. Width **1.47×** disclosed and explicitly not gated: *"the bands are about 1.47 times as wide as the benchmark's, which means they are conservative rather than flattering."* Published band record `BANDS.PHAR` = `{n:56, hits:56, c50:0.5536, c90:1.0, width:1.447, flag:"wide"}` — consistent with the full-history set. **The gap:** the counts sit one paragraph above the percentages rather than beside them, and the flag `"wide"` the panel carries is described in the study's own words rather than named. |
| 27 | Four-field provenance on every input [standard 2] | **PASS** | **278** registered inputs in `study_numbers.json.inputs`; a programmatic sweep for any of value / source / date / layer missing or empty returns **0**. `scripts/check_four_field.py`: *"every committed input carries all four fields."* By layer: **Company 218, House 46, Country 8, Market 6**. The bibliography reproduces **all 278** across four layer-grouped tables (219 + 9 + 7 + 47 rows less headers) with the same four fields — counted, not assumed. |
| 28 | Standalone bibliography document [standard 1] | **PASS** | `EIPICO_Bibliography_09-09-2026.docx`, 25 pages, 13 tables, read as rendered images. Primary-documents table **P1–P14** with publisher, what was taken from each, and the address. Full 278-row input register grouped by layer. Judgements table, **12 rows**, each with what-would-overturn-it. Negative-results table, **5 rows**, each naming what was sought and where. Aggregator-discrepancy table, **6 rows**, real ones — including *"FY2023 total assets: the FY2024 filing prints the FY2023 comparative four pounds below the sum of its own subtotals"* and the FY2024 tablet-production restatement between two of the company's own reports. Plus the cleaned price series, the beta regressor, and the window outcomes behind every coverage statistic. `scripts/check_bibliography.py` green. |
| 29 | Sweep Register primary-source depth | **PASS on the invariants, FAIL on currency** | `python3 sweep.py` → **`QC(m) — Information Sweep [PHAR, 2026-08-09]: PASS — 4/4 rings, 27 findings (10 B · 12 S · 0 D · 2 C · 3 NEG), driver gate 10 rows (7 bottom-up / 3 top-down) | primary access: attempted (7) | FS years: 4 ['FY2022','FY2023','FY2024','FY2025'] | IR findings: 7`**. Company site attempted and **logged either way**: three eipico.com.eg URLs reachable with what each yielded, and three refused with the reason recorded rather than substituted (EGX portal returns a 48KB bot-defence page; the EFSA sub-domain 502s; the central bank auction page is blocked by its own firewall — which is why the risk-free is carried from a dated print and sensitised). **4 audited fiscal years** from the filings themselves, meeting the target of 4 rather than the floor of 2. Results releases swept with their statements (F06 for FY2025 beside F02/F13). **7** findings tagged `COMPANY_IR`, distinct from `COMPANY_OFFICIAL`. `scripts/check_sweep_module.py` lists PHAR among the 16 that run through the shared register. **The failure:** `sweep_date` is **2026-08-09**, a month before this edition, and `study_quarters_disclosed = ['Q1-2026']` only. See the closing section — H1-2026 was never sought. |
| 30 | Standard version stamp [R-STD-01] | **FAIL** | `research_protocol.STANDARD_VERSION` is **`2026.09.07`**. `check_study_provenance.check_standard_version()` counts PHAR among the **18 unstamped** against 6 stamped; `grep -rn "standard_version" engine/phar_study/*.json` returns nothing. The live standard is the one that names `GAP_REVIEW_{DD-MM-YYYY}.md` as a required artefact, `assert_ground_up()` on a driver record and the three gates called in the study's own code — this edition meets one of the three, so the absent stamp is at least not a false claim. `scripts/check_standard_claim.py` is green for that reason. |
| 31 | [R-GAP-01] the answer, not only the process | **PASS, with one gap named** | Frame A **31.3738** against a struck spot of **127.30** = **−75.4%**; Frame B **47.6035** = **−62.6%**. Far past the 10% trigger and BELOW the price, so the review is owed. `GAP_REVIEW_08-09-2026.md` exists, is 156 lines, and covers **all eight headings** — LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK — plus a verdict. It audits **both** branches (*"A two-sided answer is audited on EVERY branch … a review naming one of two answers has audited half the study"*), carries `AUDITED CENTRAL: 31.3738` and `47.6035` and `AUDITED GAP: −75.4% (Frame A) · Frame B −62.6%`, and every one of those numbers is the number this edition publishes. `python3 scripts/check_valuation_gap.py` → **exit 0, "OK — no new violations"**, with `PHAR −75.5% GAP_REVIEW_08-09-2026.md (audits 31.3738)`. **The gap:** the review is dated **08-09-2026** and the edition is **09-09-2026**. The substance is current — the beta lever is the review's own subject and the numbers did not move afterwards — but the artefact does not carry this edition's date. |
| 32 | [R-GAP-02] publish block | **HELD, correctly — routing, not a defect** | `scripts/check_publish_block.py`: `HELD PHAR every branch is more than 10% BELOW the price (highest Frame B … at −62.8%) — and no market dissent is filed`. The gap review reaches the same conclusion in its own words: *"THE ANSWER IS AUDITED AND IT MOVED FURTHER FROM THE PRICE … the gap is not explained."* Referring this to the principal rather than publishing it is the rule working. |
| 33 | [R-GAP-03] the gap a reader sees | **FAIL, ratcheted, and not this edition's doing** | `scripts/check_published_gap.py`: `PHAR −52.2% review GAP_REVIEW_08-09-2026.md audits 31.3738; **the site publishes 61.21**`. A real `node` parse of `assets/data.js` gives fair `{58.04, 61.21, 73.03}` with `fairAsof: "2026-08-06"`. The page a reader receives carries roughly **double** the Frame A answer this study now holds, and its distribution block still resolves to 2026-10-06 / 2026-12-06 off the August anchor. Named here because the debt is countable, not because this edition created it. |
| 34 | The price map and the technical read are struck on this edition's clock | **HARD FAIL — a month behind, and one horizon has already expired** | `strike_result.json`: `anchor_date "2026-08-06"`, `spot 130.05`, and the one-month horizon's `target_date`/`grade_date` is **2026-09-06** — three days BEFORE this edition's own date. A study issued on 9 September publishes a one-month forward distribution whose window closed before it shipped. `technicals.json`: `close 130.05`, `data_date 2026-08-06`, `computed_on 2026-08-09`. On the page: §2 opens *"The price closed **130.05** above a rising 20-day (101.58)…"* and *"the last close sits **17%** below that high and **198%** above that low"* — both computed off 130.05 — over **Table 13, four lines below, whose Last close row reads 127.30** and whose distances (+22.5% to 156.00, −27.4% to 92.40) are computed off 127.30. Two last closes, one page. Table 14's caption is honest — *"The anchor is the EGP 130.05 close of 2026-08-06"* — which makes the disclosure good and the artefact stale. The study's own price file `PHAR_Stock_Price_History.csv` and `EIPICO_cleaned_price_series.csv` both stop at 2026-08-06, and the bibliography's Table B6 prints that series ending *"06 Aug 2026 · 130.05"* while P11 describes it as running *"2 January 2011 to 6 August 2026"* — a month short of the price the valuation is struck at. |
| 35 | Prose figures, table footing, waterfalls, sign convention | **PASS as instruments; and see rows 4–6, 14, 34** | `prose_check.py`: **497 figures checked, 7 unmatched** — reviewed one by one and all seven are benign (the 51.34% parent holding ×2, an ownership 17.03%, Table 13's own −27.4%, and three superseded January-2026 dataset vintages the bibliography publishes for the audit trail: 6.3725% / 13.9377% / 9.7077%). `footing_check.py`: **42 tables examined, 0 unreconciled totals**; `scripts/check_table_footing.py` agrees. `scripts/check_sign_convention.py` green. `scripts/check_waterfall_assertions.py`: `PHAR NO IMPORT (ratchet) … 4 waterfall table(s)` — the tables are there and the shared assertion module is not imported. **All four instruments run clean and not one of them can see the defects in rows 4, 5, 6, 14 or 34**, because every number in those places was computed correctly and it is the labels, the dates and the un-rebuilt images that are wrong. |
| 36 | Lens design [R-LENS-03] | **PASS, with two gaps named** | `scripts/check_lens_design.py`: `PHAR two-sided primary dcf, branches [31.373799027, 47.60346288], 2 cross-checks` — green. The class primary IS the central: both branches are pure cash-flow reads, weight 1.0, and the other lenses carry `weight: 0.0, role: cross_check`. The retired four-lens blend is published as a labelled MEMO at 39.73 / 47.85 and `scripts/check_lens_vocabulary.py` reports `PHAR 3 assertion(s)` with no study newly publishing the retired blend. **Gap one:** §1.5 still narrates the retired construction — *"the cash-flow weight of 50% is carried in full on ONE frame at a time, beside … book value against sustainable return at 20%, relative multiples at 15%, and normalised earnings power at 15%. That gives EGP 31.37 a share on Frame A"* — and the arithmetic it describes gives 39.73, the MEMO, not 31.37. Typed weights in a document whose central is not a blend. **Gap two:** `lens_record.cross_checks` holds two entries, `relative_multiple` 47.891 and `book_value` 42.799. The registry names three for this class (`ev_ebitda_own_history` is not committed), and **normalised earnings power at 55.342 — the FIELD HIGH of the range the study publishes as 31.37–55.34 — is in no lens record at all**, so the top of the published range lives outside anything a gate reads. |
| 37 | Asset base [R-ASSET-01] | **PASS — the class resolves, and out of scope** | `asset_base.normalise_class('pharmaceutical manufacturer, generic and branded')` resolves in `LENS_REGISTRY`, and its lens set `('dcf', ('ev_ebitda_own_history','relative_multiple','book_value'))` contains none of `ASSET_BASED_LENSES = ('rnav','replacement_cost','ev_per_tonne')`. `scripts/check_asset_base.py` names 11 studies in scope and PHAR is not one of them. Resolved, not skipped. For the record the operating quantity the study does print — 3,383mn units of disclosed capacity at 65.3% utilisation — is dated 2026-03-28, INSIDE an information set ending 2026-03-31, so the ordering would hold if it were in scope. |
| 38 | Far-year forecasts published as ranges [R-FCAL-01] | **PASS** | `scripts/check_forward_ranges.py`: `PHAR prints a far-year range shape B`. Appendix A.4 / Table 23 gives the third forecast year as a span on six lines with the **count printed in its own column** (3 readings on revenue, cost of sales, gross profit and profit; 2 on D&A and capital expenditure) and the **basis named** — *"it is the widest and narrowest of the readings behind it, not a percentile, because a percentile needs more readings than this record holds."* And it refuses to extend: FY2029E and FY2030E are marked *"no measured range"* with *"No range is invented for them: an invented one would corrupt the very error it is scored against."* |
| 39 | Walk-forward scope stated in the study | **FAIL** | `scripts/check_walkforward_scope.py` lists PHAR among the **15 with NO DECISION**: `study_numbers.json` carries no walk-forward scope record of any kind. The run itself EXISTS and is good — `engine/Fundamental_Driver_Ledger.md` line 771 holds `## PHAR … fundamental walk-forward, 07-09-2026 · **LIGHT**`, five origins FY2020–FY2024, 150 scored cells — and the delivered study CONSUMES it: Appendix A.4's whole range table rests on *"3 past observations of this method's own error."* The study uses the walk-forward's output and commits neither its scope nor its status. |
| 40 | Non-sourced drivers logged to the Driver Ledger | **PASS, with one gap named** | `engine/Fundamental_Driver_Ledger.md` line 771 carries a dated PHAR entry with a 13-row driver table, the mechanical rule each was scored under, and six decisions written down so a later study does not rediscover them (the interest denominator; the three distinct cost-of-debt quantities on this book; capex sitting in construction for years; the disclosed scalar life table; the packs-series hole and redefinition; six printed cells in the filings that do not foot). **The gap:** it is the walk-forward's driver record, dated 07-09-2026, and it does not list this edition's own 46 `House`-ring inputs — `esc_energy`, `esc_materials_usd`, `esc_packaging_import_share`, `dep_rate` 6.2%, `assoc_norm` 250 and the rest. `scripts/check_lessons_register.py`: `[ok] phar_walkforward — 17 harvested finding(s), all resolved (2 registered, 15 declined with a reason)`. |
| 41 | Rebuild ledger [R-REBUILD-01] | **PASS** | `scripts/check_rebuild_ledger.py`: `PHAR 8 lever(s), 5 rule(s), cumulative −45.9%` — the ledger walks from its start to the published answer and every lever names the rule it serves. The beta lever's own entry records the trade honestly: *"THE WITHDRAWN COMPOSITE FIT BETTER — 0.6295 at an R-squared of 0.235 … a better fit against the wrong regressor is not evidence for the wrong regressor. Frame A 32.9975 → 31.3738, Frame B 49.6789 → 47.6035."* The correction runs AGAINST the value and further from the price, which is the only direction that proves the discipline is not fitting. |
| 42 | Output records, artefact currency, source integrity | **PASS** | `scripts/check_output_records.py` green. `scripts/check_artefact_currency.py` green with no PHAR violation. `scripts/check_source_integrity.py`: no new breach, PHAR not named. `scripts/check_record_survives_rebuild.py`: `PHAR compute.py survives`. `scripts/check_source_rebinding.py` clean across 2,362 registration reads. `scripts/check_numbers_generators.py`, `check_output_sanity.py`, `check_anchor_ordering.py`, `check_terminal_record_shape.py`, `check_valuation_inputs.py` (PHAR conforming), `check_fetch_authenticity.py`, `check_figure_axes.py` — all green. |
| 43 | Deliverables of one edition | **FAIL** | `scripts/check_calibration_deliverables.py` **exits 1**: `PHAR the QC gate is dated 2026-08-09 while the edition is 2026-09-09 (QC_GATE_09-08-2026.md)`. This document closes that half. The other half, row 13's missing model PDF, it does not. |
| 44 | No rating, no price target | **PASS** | The masthead band reads *"Educational analysis · not investment advice"*; the READ FIRST panel states *"It contains no rating, no recommendation and no price target"*; the Disclosure repeats it. Every reading is published with its range and the summary table's Note column carries method descriptions, never a recommendation. |

---

## What the filings disclose that the model does not consume

Asked explicitly against the Sweep Register, because a self-audit that only re-checks the
work it did will keep missing the work it never did [L-006]. Four answers, and the register
can help with none of them because it was last swept on 9 August, a month before this
edition.

**1. The 31 March 2026 balance sheet is read for 25 inputs and the bridge stands on
December.** `q1_ppe`, `q1_cip`, `q1_debt` 9,428.0, `q1_cash` 362.8, `q1_ar` and `q1_nci`
all come out of the reviewed interim, and the study prices what ignoring it costs — *"about
EGP 10 a share lower"*, which is **32% of the Frame A answer**. It then publishes the
December bridge anyway. `scripts/check_bridge.py` calls it a FAIL and it is the largest
single unconsumed disclosure in this study.

**2. Note 35, TAX POSITION, is not in the register.** The 278-row input register cites
notes 3–13, 17–31, 33, 34 and 36. Note 35 of the audited consolidated statements discloses
that corporate tax is **unexamined for 2020–2022** with the 2023 return filed April 2024,
salary tax **unexamined 2020–2023**, stamp tax **unexamined 2022–2023**, VAT **unexamined
2020–2022**, and that EGP 20 million was paid on account for 2016–2019 with *"the rest will
be paid during the next period"* — an acknowledged, unquantified liability. Nothing of this
reaches the bridge, which deducts net debt and minorities and stops, or the risk register
in Table B2, whose seven rows carry no tax exposure at all.

**3. Note 33 splits the associate income and the model normalises the total.** The FY2024
note discloses that EGP 151.6mn of associate income is built from *"Re-evaluation of the
Batterjee Company in KSA"* +69.9mn and *"Re-evaluation of the medical union Company"*
−45.2mn alongside the operating shares — so **a net EGP 24.7mn, 16% of that year's line, is
revaluation rather than earnings**. The study normalises *"the WHOLE associate stream to EGP
250 million against a three-year mean of EGP 246.1 million"* and then capitalises it at an
earnings multiple into the bridge at **EGP 2,750mn, 16.30 a share — 52% of the Frame A
value per share**. The split the filing gives it is available and unused.

**4. The half-year to 30 June 2026 was never sought.** `sweep_register.json` carries
`study_quarters_disclosed = ['Q1-2026']` and `sweep_date = "2026-08-09"`. The
negative-results table records searches for the Q1-2026 interim (since closed) and for
plant guidance; it records no search for an H1-2026 filing, and the gap review's LATEST
FILINGS heading answers the question about the company's general practice rather than about
this period. On the EGX's own 45-day rule an H1 would have been due around 14 August 2026,
before this edition was built. Whether one exists has not been established, and *"not
established"* is a different sentence from the one currently on the page.

---

## The ten hard fails, in the order they would have to be cleared

1. **Six of the eight figures publish the withdrawn pre-correction numbers.** Figure 1, the
   headline, prints 33.00 / 49.68 / 43.91 / 48.65 / 56.78 against the committed 31.37 /
   47.60 / 42.80 / 47.89 / 55.34 — every reading overstated, by up to 5.18% — and the
   summary table on the facing page prints the correct five. Figures 5, 6, 7 and 8 carry the
   same withdrawn centres, and Figure 5 also carries a required revenue of EGP 8,871m /
   USD 127m against the body's 9,322m / USD 133m. All eight PNGs are dated 2026-09-07 19:37
   against a numbers file of 2026-09-09 11:42; a sandbox re-run of `figures.py` reproduces
   two and changes six. (row 6)
2. **The masthead states the wrong edition date and the wrong price date** — *"Prepared 9
   August 2026 · share price EGP 127.30 at the close of 6 August 2026"* — both typed as
   literals at `docx_phar.py:63` while `meta.study_date` is 2026-09-09 and `meta.price_date`
   is 2026-09-03. `scripts/check_edition_date.py` exits 1 on it as a NEW violation. The
   bibliography repeats it. Table 1 contradicts it four pages later with *"Close of 3
   September 2026."* This is the defect the edition existed to close. (row 5)
3. **The struck spot of 127.30 is the 1-September close, not the 3-September close.**
   `raw_ohlc/EG/PHAR.csv` gives 129.70 on 09/03/2026 and 127.30 on 09/01/2026. Two committed
   records disagree by 1.89% on one date and no delivered document says so. (row 4)
4. **The cost of debt is built on a risk-free print this study withdrew.** `kd_egp` 24.81% =
   22.31% + 250bp, where 22.31% is the yield `inputs.rf` calls stale and 39bp low. Table 10
   prints 23.00% two rows above and calls 24.81% *"sovereign yield plus 250 basis points"* —
   which is 25.50%. A 69bp shortfall whose cause appears nowhere in the document. (row 14)
5. **§2 and §3 are anchored a month behind the strike**, at the 130.05 close of 2026-08-06,
   and the one-month horizon's own check date of 2026-09-06 had already passed when the
   edition shipped. §2's opening sentence and Table 13's Last close row give two different
   closes four lines apart. (row 34)
6. **No study code calls any of the three gates.** The only occurrence across 25 Python
   files is a sentence in `beta_reg.py`'s docstring, which is enough to satisfy
   `check_study_provenance`'s substring test. (row 1)
7. **No `assert_ground_up()` driver record exists**, so the clause is attested by nothing.
   The build is in fact `unit` on volume and `derived` on price across 100% of revenue, and
   that had to be reconstructed by hand from `unit_build`. (row 3)
8. **Eleven bibliography columns are too narrow and five study tables exceed the text
   block.** `scripts/check_column_widths.py` exits 1 with 11 NEW PHAR violations; the
   over-wide count is 5 and the study's own `qc_checks.py` has no test for it. (row 8)
9. **The bridge stands on the 31 December 2025 sheet** while the 31 March 2026 sheet is read
   for 25 inputs and is worth EGP 10 a share, 32% of the Frame A answer. (row 18)
10. **No model PDF exists for this edition**, and the only one on disk publishes neither
    31.37 nor 47.60. (row 13)

Beneath those: the bibliography states that two standard errors 32% apart *"agree"* (row
2c); the terminal reinvests 55% of its profit at a return 249bp below its cost of capital
and the document never says so outside an edition-history table that should not be in a
delivered document at all (rows 16, 12); no cost-of-capital schedule record, so [R-COC-02]
cannot run (row 15); no walk-forward scope record on a study whose Appendix A.4 depends on
the walk-forward (row 39); the field high of the published range, 55.34, is in no lens
record (row 36); no standard version stamp (row 30); and the two largest cost escalators
carry reasoning without a sourced price print (row 20).

## What is genuinely strong here, and should not be lost in a rebuild

The workbook reproduces on all 883 formula cells and reproduces **byte for byte against a
sandbox rebuild across 2,077 populated cells**, on the 16 model sheets in the model order.
Both delivered documents reproduce **text for text** from their builders — 1,449 and 1,454
strings, zero differences — so nothing was hand-touched. The free-cash-flow waterfall foots
by hand on every year and reaches present value inline. The beta is the sanctioned EGX30
regression through `own_stock_beta()`, conforming, usable on all three legs, with the
withdrawn composite kept beside it and the uncomfortable fact that the composite fit BETTER
written down rather than left out. The bibliography is the best artefact in this study: 278
inputs four-field complete and all 278 reproduced, a primary-access log recording three
refusals with their reasons rather than substituting weaker sources, four audited fiscal
years out of the company's own site, five negative results and six real aggregator
discrepancies including a four-pound inconsistency inside a filing's own subtotals. The
contested judgement is carried both ways in all four required places and priced on every
reading the statements support. The expert appendix runs C.1 to C.6 at full depth with
falsifiers stated in advance and challenges explicitly conceded or rejected. The far
forecast years are published as spans with the count and basis named, and the study refuses
to extend them. §3's calibration reproduces cell by cell from three window sets. And the
routing is right: two centres, never averaged, far below the price, HELD and referred.

The defects above are almost all in the images, the dates and the labels — not in the
arithmetic. That is what makes them dangerous, and it is why reading the rendered pages and
auditing from outside the study are gates rather than formalities.
