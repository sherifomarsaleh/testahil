# PHAR — QC gate, 10-09-2026

Egyptian International Pharmaceutical Industries Company (EIPICO) · The Egyptian Exchange:
PHAR · EGP · edition of 10-09-2026

Filled from OUTSIDE the study per [R-ENF-02] by an auditor who did not build it, did not
edit it, and wrote exactly this one file. The 87 files in `engine/phar_study/` were
hashed before and after every command below and are byte-identical; every study script
that writes a result file was run in a sandbox copy at
`/tmp/.../scratchpad/sb/phar_study/`, never in place. Every row names the artefact, the
command or the number that carries it. Where a row is met with a gap, the gap is stated
in the same breath.

**VERDICT: NOT DELIVERABLE.** Fifteen hard fails, listed at the foot.

**The model is in good order and none of what follows should be read as doubting it.**
The workbook reproduces cell for cell against an independent sandbox rebuild across 2,080
populated cells with ZERO differences; all 883 formula cells recalculate; the free-cash-flow
waterfall runs inline to present value and the enterprise-to-equity bridge foots by hand to
the published EGP 85.25; the beta is the sanctioned EGX30 regression with its withdrawn
composite kept beside it; the cost of equity reproduces exactly from rf* + β × ERP on both
published bases; margins are outputs of a disclosed six-line cost stack; 282 inputs are
four-field complete and all 282 print in the bibliography's register; 30 asserted driver
directions all move the right way with 0 dead inputs.

**What fails is the PAPER, and it fails in a pattern.** Wherever a number was READ from the
model the study is right. Wherever a SENTENCE ABOUT a number was typed, it has gone stale or
inverted, and nothing checks sentences. The bibliography's judgements table is a hard-coded
Python literal in `docx_biblio.py` and SIX of its twelve rows now contradict the delivered
study of the same edition — including the withdrawn 22.31% risk-free print the commit of
10-09-2026 said it had removed. The study tells the reader it is 5% BELOW a benchmark it is
5.4% above. Section 2's technical read and section 3's whole price cone are struck on the
withdrawn 6-August close of 130.05, four lines above a table computed on 127.30, and the
cone's one-month check date of 6 September had already passed when the edition issued on
10 September. Two prose claims about where the centres sit are arithmetically false against
figures printed on the same page. And until the audit was under way no code in this study called ANY of the three standing
assertions: it passed `check_study_provenance.py` on a DOCSTRING that mentions one. One of
the three was wired in on 13-09-2026 while this gate was being filled; two still are not.

---

## The edition audited, and how it was resolved — including the EIPICO/PHAR alias

Resolved through `engine/phar_study/edition.py` (`EDITION = 2026-09-10`,
`SUPERSEDES = (2026-08-09, 2026-09-09)`), not by globbing a date. That module derives
`STUDY_DOCX`, `MODEL_XLSX`, `BIBLIO_DOCX` and all three PDF names from one date, and every
study script that opens a deliverable imports it.

**THE ALIAS RESOLVES CORRECTLY EVERYWHERE I COULD TEST IT, AND IT IS NOT MISLEADING THE
DELIVERABLES GATE.** The directory is `phar_study` while the artefacts are named EIPICO.
`check_calibration_deliverables.py` matches on `*Valuation_Study*.docx`, `*Valuation_Model*.xlsx`
and `check_bibliography.BIBLIO`, none of which contains the ticker, so the EIPICO stem is
found. Called directly on this directory it resolves:

```
report        EIPICO_Valuation_Study_10-09-2026.docx    (2026, 9, 10)
report PDF    EIPICO_Valuation_Study_10-09-2026.pdf     (2026, 9, 10)
workbook      EIPICO_Valuation_Model_10092026.xlsx      (2026, 9, 10)
bibliography  EIPICO_Bibliography_10-09-2026.docx       (2026, 9, 10)
QC gate       QC_GATE_09-09-2026.md                     (2026, 9,  9)
```

The compressed `10092026` stem parses to the same date as `10-09-2026` through the gate's own
`_date()` regex, so the workbook is not being read as a different edition. `check_bibliography.py`,
`check_document_structure.py` (which lists PHAR among the 23 that MATCH THE MODEL REPORT),
`check_delivered_pdf_currency.py` and `check_valuation_gap.py` all locate the EIPICO artefacts
under the PHAR key. **The RED is real and is not an alias artefact: the newest QC gate on
disk was one edition behind the study.** This file closes that.

The site is a separate matter and is recorded below: `assets/data.js` still points PHAR at
`files/PHAR_Valuation_Study_09-08-2026.pdf` and publishes `fair: {bear 58.04, base 61.21,
full 73.03}` — a fair value this study has withdrawn twice.

---

## The three gates, run from outside

| Gate | Result |
|---|---|
| `python3 scripts/check_study_provenance.py` | 25 study directories, 7 registered indices. PHAR is NOT among the 8 outstanding. **"OK — no new violations."** Re-run at the close of the audit, after the gate itself was re-pointed from a substring search to an `ast` parse: still clean. See row 1 for what that clean now means and what it still does not cover. |
| `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` | **2026.09.10**. `study_numbers.json["standard_version"] = "2026.09.10"` — matches, and is READ from the protocol at build time (`compute.py:2750`, added 13-09-2026) rather than typed. |
| `python3 scripts/check_calibration_deliverables.py` | **FAILED.** `PHAR   the QC gate is dated 2026-09-09 while the edition is 2026-09-10 (QC_GATE_09-09-2026.md)`. Closed by this file. |
| The study's own entry points (sandbox) | `recalc.py` PASS · `driver_test.py` PASS · `footing_check.py` PASS · `qc_checks.py` PASS · `prose_check.py` PASS · `sweep.py` PASS. **None of them calls `assert_sigcm()` or `assert_model_study()`; `beta_reg.py` calls `assert_beta_provenance()` as of 13-09-2026.** |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The study's own committed code calls the three standing assertions | **HARD FAIL — one of three, and only since 13-09-2026** | **At the start of this audit, `grep -rn "assert_sigcm\|assert_beta_provenance\|assert_model_study" engine/phar_study/*.py` returned exactly ONE hit: `beta_reg.py:31`, inside the module docstring** — *"so assert_beta_provenance() can inspect the record rather than trust a boolean the study set on itself."* A sentence about the assertion, not a call. `check_study_provenance.py:102` tested `any(g in t for g in GATE_CALLS)`, a substring match over the file text, so the docstring satisfied it and PHAR reported clean while calling nothing. **This was found and fixed independently while this gate was being filled** — commit `8f14c0cc1`, *"A mention is not a call: three studies passed the provenance gate on a docstring"*, re-points the gate to an `ast.walk` for a real `Call` node and adds `_RP.assert_beta_provenance(res)` at `beta_reg.py:86` in PHAR, SCEM and SWDY. I re-ran the assertion myself against the delivered `beta_result.json`: **clean**. **WHAT IS STILL NOT DONE:** `assert_sigcm()` and `assert_model_study()` have **ZERO occurrences anywhere in this study** — not as a call, not as a mention. The eight SIGCM clauses in rows 10–17 and the model-study conformance in row 44 are evidenced by me from outside; no committed code in this directory refuses to issue when one of them fails. The only other protocol assertion the study invokes is `RP.assert_bridge(record, 'PHAR')` at `bridge_record.py:203`. **The edition under audit was built on 10-09-2026, when none of the three was called.** |
| 2 | Deliverables complete and self-consistent at one edition [R-FCAL-01] | **HARD FAIL, now closed by this file** | `check_calibration_deliverables.py`: report, PDF, workbook and bibliography all 2026-09-10; QC gate 2026-09-09. PHAR was one of 10 NEW failures of the same shape across the book. |
| 3 | Standard-version stamp is the live one | **PASS** | `rp.STANDARD_VERSION` = `2026.09.10`; `study_numbers.json["standard_version"]` = `2026.09.10`. Read at build time from `research_protocol`, not typed. Verified the 13-09-2026 re-run changed nothing else: a full flatten-and-compare of `study_numbers.json` before and after commit `124d559c5` gives **0 changed values**, one added key (`/standard_version`). So the delivered documents of 10-09 are still consistent with the numbers file. |
| 4 | **The answer, and the gap [R-GAP-01]** | **PASS on the record; the gap itself is the study** | The study publishes a TWO-CENTRE field, never one number: **bear/relative 55.4481 · Frame A 85.2465 · Frame B 102.8915 · field high 102.8915**, against a struck spot of **127.30** (close of 3 Sep 2026). Gap on Frame A **−33.03%** against the struck spot and **−33.4%** against the latest known 128.00; Frame B **−19.17%** and **−19.6%**. Both centres are more than 10% BELOW the price, so a gap review is required on both. |
| 5 | The answer is READABLE by `check_valuation_gap.py` — an unreadable answer is held as a breaching one [R-ENF-04] | **PASS** | `read_answer()` returns `(None, 127.3, 'study_numbers.json publishes a TWO-SIDED answer: 2 named branches, no single central')` and `read_branches()` returns both branches with labels and values. The `central: null` named in the brief is `lens_record.central`, the deliberate two-sided marker; the answer lives at `central_two_sided.branches`. The gate reads it: the live run prints `PHAR  -33.4%  GAP_REVIEW_10-09-2026.md  (audits 85.2465)` and returns **"OK — no new violations."** |
| 6 | `GAP_REVIEW_10-09-2026.md` exists and covers all eight headings | **PASS** | Present, dated the edition. It audits **BOTH** branches on their own lines (`AUDITED CENTRAL: 85.2465` and `AUDITED CENTRAL: 102.8900`) and states both gaps (`−33.4% / −19.6% against 128.00`). All eight headings carried and each answered rather than referred: **LATEST FILINGS** (Q1-2026 remains the last period consumed; its PDF is image-only and the ECL and associate notes have not been re-read from source — named, not cleared) · **BASE YEAR** · **MACRO COHERENCE** · **DISCOUNT RATE** · **TERMINAL** · **BALANCE SHEET** (the December bridge under-charges by ~EGP 7.33 a share; **not corrected**) · **CLAIMS AGAINST THE RECORD** (associates at 11× plus the API plant at cost, EGP 2,978.5m against a 31-March carrying value of 904.4; worth ~−EGP 12.29; unresolved) · **MULTIPLE CROSS-CHECK**. It also records what is NOT closed: the explicit-window cost of capital of 23.04% against EFG's 20.6% for this company, 2.4 points unexplained. |
| 7 | Step 0.0 data-quality gate, on both series | **PASS** | `calibration.step0`: raw rows **3,228 → clean 3,225**, three dropped and named (`non-positive or non-finite OHLC value, 2011-06-15..2011-09-21 — vendor fills, not tradeable prices`); density **206.8 rows/yr** over **15.59 years** with the sessions-per-year table printed 2011–2026; largest single-session |log| **0.1823** against an artefact threshold of **0.2901**; flat fraction 17.7%. Run on the INDEX too: `beta_result.json` carries `index_dq: []` and the same `stock_dq` line, so both legs of the regression were screened. |
| 8 | Step 0 calibration under the band record — coverage against target, count beside the percentage, width ratio disclosed not gated | **PASS, with one gap named** | Three window sets committed and all three published in §3. Five-year set: **19 non-overlapping three-month windows**, inside the 90% band **100%**, inside the 50% band **47.4%**, χ² p = **0.447**, KS p = **0.676**, width ratio **1.469×** the benchmark — disclosed in plain language ("about 1.47 times as wide as the benchmark's, which means they are conservative rather than flattering") and NOT gated. Full history 56 windows; production set 16 windows post-break. The count is stated ("19 non-overlapping three-month windows") one paragraph before the percentages rather than beside them. **THE GAP: no two-sided binomial on coverage against target appears anywhere** — χ² and KS test UNIFORMITY, not coverage. 100% inside a 90% band on 19 windows would not have been rejected (0.9¹⁹ = 0.135), but the test is not run and not shown. |
| 9 | No verdict tokens, no calibration appendix, CRPS only on the methodology page [R-CAL-02] | **PASS** | `band_record.assert_no_verdict_tokens()` run from outside on the extracted text of both delivered documents: **PASS on the study, PASS on the bibliography**. Independent scan for `\bPASS\b|\bFAIL\b|\bPARITY\b|\bCRPS\b`: **0 hits in each**. `PARITY` and `skill_norm = −0.0109` live in `study_numbers.json` and reach no delivered page. The document has three appendices — A statements, B peers/risks/research record, C expert panel — and no calibration appendix; the evidence is §3 prose. `check_delivered_vocabulary.py` lists PHAR as CLEAN. |
| 10 | SIGCM 1 — historicals from official disclosure only | **PASS** | 219 of 282 register inputs are layer Company and every one cites an audited or reviewed filing, the board report inside an annual report, or the investor presentation. READ FIRST states it: *"No data vendor, broker note or press report is used to build any number about the company itself."* `sweep_register.json` carries 15 COMPANY_OFFICIAL findings and four consecutive audited years FY2022–FY2025. The three peer multiples (26.7×, ~16×, 21.35×) are labelled market data, cross-check only, in Table 24. |
| 11 | **SIGCM 2 — the ground-up clause, inspected on a driver record** | **HARD FAIL — the record does not exist** | `python3 scripts/check_ground_up.py`: *"24 study directories examined · assertions actually RUN 4 · unreadable 20"*, with `PHAR  commits no driver-line record at all; [R-SIGCM-02] retired the boolean that used to stand in for one`. PHAR is on `ground_up_outstanding.json` so the repo gate stays green, but **nothing outside this study can run `assert_ground_up()` against it**, and I cannot report a share-by-level split off a committed record because there is none. **Computed by hand from `forecast` instead, FY2026E:** `rev_dom` 7,578.59 (**66.23%**), `rev_exp` 3,505.35 (**30.64%**), `rev_resale` 124.00 (**1.08%**), `toll` 65.85 (**0.58%**) — **four named lines, each a volume × a price, covering 98.53% of revenue**; the residual **1.47%** is `consol_uplift = 1.0149`, a scalar with no unit behind it, identical in all five years. The substance is genuinely unit-level — §1.6 and Table 7 build three product lines from packs and price per pack, and Table 3's caption names the 1.47% step as the subsidiary's external sales — but under `assert_ground_up()` the lines must cover 100% and a sub-`unit` line must carry a gap note on the record, and the record is absent. |
| 12 | SIGCM 3 — debt split local currency / hard currency | **PASS** | `wacc`: `kd_egp` 25.50%, `kd_fx_coupon` 7.50% carried at a local equivalent of 12.34%, `w_fx` **50.20%**, blended marginal 18.89%, after tax 14.64%. §1.8 Table 10 prints all of it and cites borrowings note (17). |
| 13 | SIGCM 4 — asset conversion cycle | **PASS** | `working_capital`: DIO **268.35**, DSO **128.54**, DPO **53.88**, cash conversion cycle **343.01** days on FY2025. Working capital is projected from those day ratios (Table 21 caption). |
| 14 | SIGCM 5 — competitors | **PASS** | Appendix B.1 Table 24: a listed Saudi generics manufacturer at 26.7×, regional/international mid-caps at ~16×, the struck reference at 21.35×, and the company's own history at 6.57× and 14.90×. Plus the named competing product found by the research pass: Alvotech's AVT02 adalimumab, approved in Egypt 29 Aug 2023 — a competitor in the one molecule that matters. |
| 15 | **SIGCM 6 — the beta, inspected on its RECORD, not on a boolean** | **PASS** | `beta_result.json`: `index_file` = **`raw_indices/EG/EGX30.csv`** — under `engine/raw_indices/`, registered in `EXCHANGE_INDEX`, not a constituent composite. The exchange is read from `assets/data.js` `code: "EGX:PHAR"` → EGX → EGX30, which is the published index of the exchange this share is listed on; it is NOT inferred from the `raw_ohlc/EG/` folder. `index_asof` 2026-09-08, `week_rule` W-THU, `dimson: true`, `interim_note: null`, `conforming: true` — no interim note is required because this is not a DFM name on FADGI. **Usability gate passed on all three conditions: n = 256 (≥24), R² = 0.1427 (≥5%), SE(β) = 0.1355 < |β| = 0.6658.** CI90 0.443–0.889; Blume cross-check 0.777. `research_protocol.assert_beta_provenance(rec, 'PHAR')` run from outside: **returns clean**. The withdrawn composite is KEPT beside it (β 0.6295, R² 0.2350, `why_withdrawn` stated) with `delta_vs_withdrawn` 0.0577, so the correction's size is visible. Produced by `beta_regression.own_stock_beta('PHAR','EG','EGX')` — `beta_reg.py` hand-rolls nothing. |
| 16 | SIGCM 7 — formula-based model | **PASS** | `recalc.py` on the delivered workbook: **883 formula cells against 483 pasted**, and `RECALCULATION PASSED — 883 of 883 formula cells reproduce the model, 0 unresolvable, 0 unchecked`, plus the gate-3 headline reconciliations (core EV 18,763.32, terminal share 0.7852, equity 14,385.83, Frame A 85.2465, Frame B 102.8915, both forecast balance-sheet checks 0.000000). Pasted cells are declared by class: audited 321, unit_build 6, grid 136, measured_error 20, label 616. |
| 17 | SIGCM 8 — gaps flagged before issue | **PASS** | Table 19 caveats (the qualified Q1 review, no plant revenue, the December-vs-March bridge, the two consolidation perimeters, the interest path); Table 25 risk register with severities; five dated negative searches in the sweep register; Table 6's caption stating that the third relative leg cannot be rebuilt from peer filings. |
| 18 | Sweep Register — primary-source depth | **PASS, with two gaps named** | `python3 sweep.py` (sandbox): `PASS — 4/4 rings, 33 findings (10 B · 15 S · 0 D · 3 C · 5 NEG), driver gate 10 rows (7 bottom-up / 3 top-down) | primary access: attempted (7) | FS years: 4 ['FY2022','FY2023','FY2024','FY2025'] | IR findings: 8`. Company site attempted and **logged either way**: eipico.com.eg reachable (three URLs, seven annual reports, the investor presentation), the EGX disclosure portal NOT reachable and the failure recorded verbatim (48KB bot-defence page, headless render also attempted). Four audited years against a floor of two and a target of four. **≥1 COMPANY_IR tagged distinctly: 8 of 33, and 7 of the 8 sit on eipico.com.eg.** **GAP ONE:** the eighth — the December-2025 EIPICO 3 announcement that CLOSED the previously-failing "announced projects, ventures and capacity" category — is tagged `COMPANY_IR` on a third-party URL (`meobserver.news`), not the company's own site. **GAP TWO is row 19.** |
| 19 | **Every study-year quarter already disclosed swept in before the build** | **FAIL** | `sweep_register.json`: `sweep_date: "2026-08-09"`, `study_quarters_disclosed: ["Q1-2026"]`. **The build is 10-09-2026 and the sweep has not been re-dated since 09-08-2026** (one primary-access attempt is logged at 2026-08-11 and two negative searches at 2026-09-10, but the quarter list was fixed on 9 August). Nothing in the register asks, at the build date, whether the half-year to 30 June 2026 has been filed — and the study itself names *"a half-year balance sheet"* or *"an unqualified half-year review"* THREE times, in Table 19, as the thing that would settle its three largest open items. A disclosed H1 would be invisible to this sweep. Neither a positive nor a negative search for it exists. |
| 20 | Results releases swept with their statements | **PASS** | The Q1-2026 reviewed interim is in the register with its numbers (net sales 2,532.834, +10.1%; gross margin 42.4% vs 45.7%) AND the board report / general-assembly statements alongside (production value EGP 10.8bn, sales EGP 9.8bn, the chairman's March-2026 remarks, the FY2025 dividend of EGP 3.50). The annual reports' board reports are registered as a separate finding because they carry an operating dataset that appears in no financial statement. |
| 21 | One escalator per driver class, with a sourced near-term cost anchor | **PASS on the escalators; FAIL on three of five anchors** | Table 8 gives six physically distinct cost lines each on its OWN escalator: imported active ingredients 54.9% on a hard-currency path, packaging 24.6% at a 55/45 import split, labour 9.8% on wage growth, energy 3.4% on the regulated tariff schedule, other consumables 5.7% on consumer prices, depreciation 1.7% excluded and taken from the property roll-forward. No blended index anywhere. **The anchors are uneven.** `esc_domestic_cpi` [16/12/9/7.5/7%] is sourced to the central bank's published baseline; `esc_labour` [14/12/10/8.5/7.5%] is anchored on the company's own filing (wage bill +27% on headcount +2% in FY2025). **`esc_energy` [18/15/12/10/8%] names no tariff, no schedule and no date** — its whole source string is a reason, not an anchor. **`esc_materials_usd` 2.0%** and **`esc_packaging_import_share` 0.55** carry no external anchor either. All three are layer House. |
| 22 | Margins as OUTPUTS wherever the filings disclose cost per unit | **PASS** | The cost-of-sales note (26) splits FY2025 production cost of EGP 5,625.990m into six lines with their own percentages, and the model escalates each separately; gross margin falls out at **43.2 / 42.8 / 42.9 / 43.2 / 43.5%** and is printed as "Gross margin (an output)" in Table 7. Unit cash cost is built per pack (`unit_cost_fy25` 14.536). §1.6 states it: *"Margins are an OUTPUT of this build, never an input."* |
| 23 | 3 years historical + 5 years forecast, with the FULL FCFF waterfall inline to PV of FCFF | **PASS** | Appendix A.1 and A.2 carry FY2023/24/25 actual and FY2026E–FY2030E. Table 4 runs the waterfall inline, every line: Revenue → EBITDA → EBITDA margin → less D&A → EBIT → tax rate → NOPAT = EBIT × (1−t) → add back D&A → less capex → less increase in working capital → **free cash flow to the firm** → **discount rate** → **discount factor** → **present value of free cash flow**. It does not stop at FCFF. Table 5 then bridges PV of 5 years 4,030 + PV of terminal 14,733 = core EV 18,763 → + associates 2,750 + API at cost 228 + held for sale 12 = total EV 21,754 → − net debt 7,364 − NCI 4 = **equity 14,386 = EGP 85.25 a share**. I re-footed FY2026E and the bridge by hand; both foot. |
| 24 | v2 WACC — country risk once, marginal Kd above the sovereign, market-value weights, both ERP bases | **PASS on the construction; FAIL on the record the standing gate reads** | §1.8 Table 10 publishes both bases side by side. **Country risk once:** quoted ten-year EGP yield **23.00%** less Egypt's OWN default spread **3.42%** (swap) / **5.97%** (rating) → normalised rf* **19.58% / 17.03%**; the country-risk premium is shown as a MEMORANDUM row with the explicit note *"It is NOT added to the cost of equity — the total premium above already contains it"*. Cost of equity **25.92% / 26.01%**, agreeing to 9 basis points. **Marginal Kd above its own sovereign:** 25.50% = 23.00% + 250bp, floored by construction. **Market-value weights:** 74% / 26% on net debt, `we_net` 0.7447 / `wd_net` 0.2553, "Market-value equity, never book equity". Glide 23.04 → 21.01 → 18.79 → 16.95 → **15.47%**. **THE GAP:** `python3 scripts/check_cost_of_capital.py` reports `PHAR  carries no cost-of-capital schedule record` and holds it on `coc_outstanding.json`. Everything above I verified by hand from `study_numbers.json["wacc"]`; the standing gate cannot. `coc_state.json` still describes PHAR as `"cls": "composite-cohort", "d": "no regressor recorded", "ed": "09-08-2026"` — three facts that stopped being true when the beta was re-derived. |
| 25 | Ke reproduces from rf* + β × ERP, and the terminal under a NAMED construction [R-COC-02] | **PASS on the explicit window; FAIL on the terminal** | Explicit: 0.1958 + 0.6658 × 0.095164 = **0.259162** against `ke` = **0.259162**, difference 0.00e+00. Rating basis reproduces to 4e-6. Terminal: 0.105 + 1.00 × 0.070 = **0.175** = `ke_term`. **But the terminal construction is NOT named from `ke_reproduction.TERMINAL_CONSTRUCTIONS` = `('same_beta','relevered','split_premium','beta_to_one_split')` anywhere in the committed record, and the record carries none of the keys `ke_terminal()` needs — no `rf_terminal`, no `erp_terminal`, no `erp_mature`, no `crp_effective_terminal`.** It is the `beta_to_one_split` shape and it says so nowhere, so the check that exists for exactly this cannot be run. |
| 26 | **The terminal beta is provenanced** | **HARD FAIL** | `BETA_TERM = 1.0` is a bare module constant at `compute.py:1540`. It is **not in the 282-input register**: `'beta_terminal' in study_numbers['inputs']` is False, and `grep -i "terminal beta" ` over the delivered bibliography text returns **0 hits**. It carries no value/source/date/layer and appears nowhere a reader can check it. By `GAP_REVIEW_10-09-2026.md`'s own arithmetic it is worth **EGP 27.55 a share on Frame A** (112.80 without it, 85.25 with it) — the single largest driver in the study, and the only one with no provenance at all. The four-field standard says "no orphan numbers"; this is the orphan. |
| 27 | **The terminal risk-free rate's register entry reproduces** | **HARD FAIL** | Delivered bibliography, register row: `rf term | 0.105 | 2026-09-03 | Terminal risk-free rate, DERIVED and never quoted: the long-run inflation this valuation carries throughout, 7.0%, plus the standard 5.5-point emerging-market real-rate convention. The previous edition used 10.5%, built the same way but on a 5% inflation assumption...` **7.0% + 5.5 = 12.5%, not the 10.5% the row states as its own value.** And "the previous edition used 10.5%" — 10.5% IS the current value. `GAP_REVIEW_10-09-2026.md` records that the real rate was cut from **5.50% to 3.50%** at this very edition; 7.0% + 3.5% = 10.5% is the live construction and the register still describes the retired one. The row's own closing sentence is *"the single most terminal-value-sensitive number in the model cannot be typed"* — and the sentence about it was. |
| 28 | **The cost of debt is the rate the study actually uses** (the defect commit `4eae44ec5` closed) | **PASS on the cost of debt; the same species survives elsewhere** | Verified from outside: `grep "24.81\|18.55"` over both delivered documents returns **0 hits**. The study now prints `Cost of debt, local currency | 25.50% | 25.50% | Sovereign yield plus 250 basis points — above the sovereign by construction` and, in Table 19, `a blended MARGINAL cost of debt of 18.89%`. Those are exactly `wacc.kd_egp = 0.2550` and `wacc.kd_blend = 0.18892`. The withdrawn 22.31% print now appears only in the `rf` and `kd_egp` register entries, correctly labelled as withdrawn. **BUT the same species is alive in four other places** — rows 27, 29 and 32 — so the fix was made in one file and not the class. |
| 29 | **The bibliography's judgements table reproduces from the model** | **HARD FAIL — 6 of 12 rows contradict the delivered study of the same edition** | The table is a hard-coded Python literal, `docx_biblio.py:184–256`. Row by row against `study_numbers.json`: **(a)** "The risk-free rate is normalised... The quoted **22.31%** ten-year yield... subtracting the **3.41%** credit-default-swap spread leaves **18.90%**" — the model holds **23.00% / 3.42% / 19.58%**, and the study's own §1.8 says so in the same edition. 22.31% is the print this study formally WITHDREW. **(b)** "The terminal debt weight is **20%**, below today's 25%" — `wd_term` = **25.53%**, identical to today's; the study's own Table 10 says *"neither is 20%"* and §7.1 says *"it was 20%... The market reading is used and both are shown."* **(c)** "Terminal growth is **5%**... a terminal risk-free rate that itself embeds the central bank's **5% inflation target**... the sensitivity table runs 3% to 7% and the value ranges from **EGP 78 to EGP 98**" — `macro_record.terminal.g_nominal` = **7.0%** on **7.0%** inflation, and `sensitivity.g` runs **EGP 62.88 to EGP 85.25**. The study's own `g_term_real` register entry two tables earlier says the 5% figure was retired as *"a real DECLINE of 1.87% a year, in perpetuity."* **(d)** "The associate contribution is normalised to **EGP 320 million**... the stream is **74.5m, 147.1m and 495.5m**... the three-year average is **239.0**" — `assoc_norm` = **250.0**, the stream is **74.508 / 151.581 / 512.085**, the three-year mean is **246.06**; §7.1 states the cut *"from EGP 320m to EGP 250m"* as a change this edition made. **(e)** "They contributed **EGP 495.5 million**" — `assoc_fy25` = **512.085**. **(f)** "The relative lens uses three multiples... (**8.1x**), ... (**6.7x**), and a **regional peer median** adjusted for the cost-of-equity gap (**9.8x**)" — the committed legs are **7.065× / 6.572× / 6.100×**, and the study's own Table 24 and `peer_pe_regional` register entry both state it is *"NOT a median of a disclosed peer set"*. A seventh row quotes imported inputs at **79%** of the cost stack where the study's own risk register uses **68.4%** and says the 79.4% variant *"the group's own primary-packaging manufacture rules out."* |
| 30 | Numeric traceability — every builder reads the committed numbers file, no financial numeral typed into a builder | **HARD FAIL, and the check that should catch it cannot** | `qc_checks.py` prints `numeric traceability: 0 typed financial numeral(s) in builders`. Its `MONEY` regex matches only thousands-separated numerals (`1,234,567`) and long decimals (`\d{4,}\.\d{3,}`). It cannot match `22.31%`, `320`, `8.1x`, `495.5`, `675.9`, `20%`, `5%` or `79%` — **every figure in row 29**. Zero hits here is not evidence of traceability; it is evidence of a regex that excludes the class of numeral that actually went stale. |
| 31 | The delivered workbook recalculates, independently | **PASS** | `python3 recalc.py` in a sandbox copy on the delivered file: **883 of 883 formula cells reproduce, 0 unresolvable, 0 unchecked, 0 mismatched, 0 headline failures.** Nothing was reported as a skip. |
| 32 | Script-built files verified by a **cell-by-cell diff** against the delivered file | **PASS** | Rebuilt `build_xlsx_phar.py` in the sandbox from the committed `study_numbers.json` and diffed cell by cell against the delivered workbook: **16 sheets, identical names and order, 2,080 populated cells compared, 0 DIFFERENCES.** `wb.sheetnames == research_protocol.MODEL_STUDY['excel_sheets']` returns **True**. Extended to the documents: `docx_phar.py` and `docx_biblio.py` rebuilt in the sandbox produce text **byte-identical** to both delivered `.docx` files (`diff` empty on 443 and 393 extracted lines). So the delivered documents ARE what the committed builders produce — which means the defects in rows 29, 33 and 34 are in the code, not in a stale file. |
| 33 | **Rendered PDF read as images, page by page** | **HARD FAIL — five defects no programmatic check saw** | All 27 pages of `EIPICO_Valuation_Study_10-09-2026.pdf` rendered at 125 dpi and read as images. **(i) §2 is struck on a withdrawn price.** *"The price closed **130.05** above a rising 20-day (101.58), a rising 50-day (92.49) and a rising 200-day (82.58)... it has ranged 43.60–156.00; the last close sits 17% below that high"* — four lines above a table whose "Last close" row reads **127.30** and whose distances are all computed on 127.30. `technicals.json` carries `close: 130.05, data_date: "2026-08-06"`. The whole paragraph is a month stale and contradicts the table beneath it. **(ii) §3's price cone is expired on the day it shipped.** Table 14's caption: *"The anchor is the EGP **130.05** close of **2026-08-06**"*, with `Check date **2026-09-06**` for the one-month band. The edition issued **10 September**. `strike_result.json` confirms `anchor_date 2026-08-06, spot 130.05, 1M grade_date 2026-09-06`. **(iii) §6 states a falsehood two bullets below its own evidence.** *"the three-month 90% band runs from EGP **94.40** to EGP 193.82"*, then *"BOTH fundamental centres — EGP 85.25 and EGP **102.89** — sit below the entire three-month band."* 102.89 > 94.40; centre B is inside the band. **(iv) C.5 states a second falsehood.** *"their own worked values run from EGP 70.85 to EGP 85.25 a share, and the study's own two centres, EGP 85.25 and EGP **102.89**, sit inside that."* 102.89 is outside 70.85–85.25. Both are typed claims about numbers the builder reads correctly (`docx_phar.py:972` and `:1529`). **(v) Table 9 inverts a comparison.** *"Attributable profit... 284 | 1,285 | 1,355 | the study is 5% **below** the quarter's seasonal read."* `docx_phar.py:608-611` computes `FC['parent'][0] / (q1_parent / q1_share_of_year_profit) - 1` = 1354.7 / 1285.0 − 1 = **+5.42%**. The number is read; the word "below" is typed, and it is the wrong word. |
| 34 | **Table 1's caption describes a table that does not exist** | **FAIL** | The summary valuation table's caption promises *"Terminal value is stated beside each cash-flow reading on BOTH bases — as a share of the core operating value and of the total."* The Note cells for Frame A and Frame B both read **"three-year average margin 22.9%"**. No terminal share appears in the table. (The figures do exist — 78.52% of core and 67.72% of total — but in Table 5, not here.) Worse, the Note itself is wrong for those two rows: **22.9% is the three-year mean OPERATING margin used by the normalised-earnings-power lens.** The DCF's own forecast operating margin runs **23.75 / 23.44 / 23.86 / 24.27 / 24.73%** and is never 22.9%. One note is pasted across three lenses and describes the construction of only one of them. |
| 35 | Figure discipline — solid light canvas, zero transparency, verified programmatically | **PASS** | All 8 figures: mode RGB, minimum alpha 255 (no alpha channel), corner luminance **248** on every one. `qc_checks.py`: `figure discipline: 8 figures checked, 0 problem(s)`. |
| 36 | **Every figure inspected as a RENDERED IMAGE** | **FAIL — label collisions on three of eight, including the headline figure** | All eight opened and read. **Figure 1 (`fig1_field.png`), the headline figure on page 2:** the labels *"centre B 102.9"* and *"market price 127.30"* are drawn on top of each other and neither is legible. **Figure 2 in the document (`fig4_multiple.png`):** *"four-year mean 6.6x"* overprints the 2023 bar's own *"6.6x"* label, rendering as `six.6.6x`; the 2024 bar's *"6.2x"* is struck through by the dashed mean line. **Figure 7 in the document (`fig5_cone.png`):** *"fundamental centre, Frame A 85"* is overprinted by the price series itself; *"1M 5th 104"* collides with the Frame B line at 103; **and the x-axis carries no tick labels at all**, so a 13-year price history is shown with no time scale. Figures 3, 7 and 8 have lighter collisions (the FY23 "105" label crossed by the balance line; the crux line through "the model without a revenue line..."; the dashed line through "market price 127.30"). Figures 2 and 6 are clean. |
| 37 | Table discipline — fixed layout, explicit widths, three counts per document | **FAIL on one count, and the study's own check does not measure it** | Independent audit, text block 7.00in both documents. **Study: 33 tables — OVER-WIDE 5 · STARVED 0 · BLOATED 0.** **Bibliography: 13 tables — OVER-WIDE 0 · STARVED 0 · BLOATED 0.** All 46 use fixed layout with explicit per-column widths, and the `tblGrid` agrees with the stated widths on every one (the L-014 species is absent). The five over-wide are T11 7.05in, T18 7.40in, T22 7.48in, T23 7.48in, T24 7.30in — Appendix A.1 and A.2 among them. They render inside the margin in the PDF, so the practical harm is nil, but **`qc_checks.py` reports `46 tables checked, 0 problem(s)` because it never tests total width against the text block at all.** |
| 38 | Standalone bibliography document, at depth | **PASS on structure; the content failure is row 29** | `EIPICO_Bibliography_10-09-2026.docx` / `.pdf`, 25 pages: READ FIRST and research-layer guide; primary-documents table (**14 documents**, publisher, date, what was taken); a source-reachability table (**7 sources, reachable and not**); the **FULL input register of all 282 inputs grouped by layer** across four tables (Company 219, Country 11, Market 6, House 46) each with value, date and source-and-construction; a judgements table (**12 rows**, each with what would overturn it); a negative-results table (**5 dated searches**); an aggregator-discrepancy table (**6 items**, "What the secondary source says / What this study does"); a price table; and the calibration window tables. `python3 scripts/check_bibliography.py`: **"OK — every delivered study ships the standalone bibliography the bar requires."** |
| 39 | Four-field provenance on every input, validated, counted | **PASS** | `study_numbers.json["inputs"]` holds **282** entries; checking `value`/`source`/`date`/`layer` all present and non-empty on each gives **0 incomplete**. All **282** print in the bibliography register (219 + 11 + 6 + 46 rows counted directly out of the delivered `.docx`). One orphan sits OUTSIDE the register and is row 26. |
| 40 | External-reader scrub | **PASS** | `qc_checks.py`: `external-reader scrub, study: 0 hit(s) · bibliography: 0 hit(s) · workbook: 0 hit(s)` against 40 banned patterns plus a case-sensitive `PARITY`. Independently confirmed with `band_record.assert_no_verdict_tokens()` (row 9) and `scripts/check_delivered_vocabulary.py`, which lists PHAR as CLEAN. |
| 41 | Expert appendix at maximum detail | **PASS** | C.1 Expert 1 (cash-flow), C.2 Expert 2 (asset-and-return), C.3 Expert 3 (market-implied). Each carries worldview, when-it-works / when-it-fails, a worked table with every intermediate line (Expert 1 runs PV of five years → terminal FCF → terminal rate less growth → terminal value → PV of terminal → core EV → associates and held for sale → less net debt and minorities → per share; Expert 3 runs gap to be explained → equity value of that gap → required FY2030 revenue at a 45% contribution margin → share of revenue → USD a year → asset turn → implied value), a NAMED sensitivity with numbers (*"At 35% rather than 45%, the required revenue rises to about EGP 5,450 million, or USD 78 million a year"*), and a falsifier stated in advance (*"If the company discloses biosimilar revenue below USD 20 million a year in its third full year of operation..."*). C.4 cross-examination: **5 challenges, each explicitly CONCEDED or REJECTED**. C.5 the three in one room. C.6 divergence table. |
| 42 | Experts labelled "Expert 1 / 2 / 3" only | **PASS** | Headings read *"C.1 Expert 1 — the cash-flow analyst"*, *"C.2 Expert 2 — the asset-and-return analyst"*, *"C.3 Expert 3 — the market-implied analyst"*. No persona name anywhere in the extracted text of either document. |
| 43 | The central contested judgement computed BOTH ways and published side by side, never averaged | **PASS — and this is the study's best feature** | The judgement is the credit-loss and provision charge: Frame A permanent at 5.25% of revenue, Frame B normalising to 2.5%. Published side by side in **the summary table** (two CENTRE rows, plus a banded memo row showing the withdrawn blend 75.95 / 84.78 "printed so the change is visible"), **the body** (§1.1 Table 5's bridge ends in both equity values; §1.5; §1.9 Table 11 prices every reading of the charge at 5.25 / 6.52 / 3.03 / 5.15% → 85.25 / 76.29 / 100.98 / 85.95), **the workbook** (`recalc.py` reconciles *"weighted centre — Frame A"*, *"weighted centre — Frame B"*, *"summary sheet centre — Frame A/B"* and the identity that each centre IS its own cash-flow lens), and **an expert's range** (C.1's 66–105). `lens_record.primary` carries `two_sided: true, value: null` with both branches and a `range_basis` naming the single driver that separates them. Never averaged: §1.5 states *"weighting both of them inside one number averages them"* and the prior single centre is explicitly withdrawn. |
| 44 | Sections and sheets match the model report | **PASS** | 16 Word sections in the model order — masthead + Read first · Headline · Valuation summary · Company overview · §1 with 1.1–1.9 · §2 · §3 · §4 · §5 · §6 · §7 · Appendix A (A.1–A.4) · Appendix B (B.1–B.3) · Appendix C (C.1–C.6) · About · Disclosure — checked against `MODEL_REPORT_09-08-2026.docx`'s own heading list. `scripts/check_document_structure.py` lists PHAR among the 23 that **MATCH THE MODEL REPORT**. 16 Excel sheets in the model order, asserted True against `MODEL_STUDY['excel_sheets']`. A.4 ("the far forecast years as ranges") is an addition the model report does not carry; it is required by the walk-forward and is not a departure. |
| 45 | **The excluded section is excluded** | **FAIL** | `research_protocol.MODEL_STUDY['excluded_sections'] = ('What changed in these editions, and why',)` — edition history is internal QC evidence, never in a delivered document, which is why the model report is ADNOCLS's study MINUS that section. PHAR ships **§7.1 "What changed in this edition, and by how much"**, an 18-row Change/Effect table describing defects in its own previous edition (*"the model conceded a 23.5% effective burden in one place and applied 22.5% in the cash-flow engine"*, *"no balance check was ever computed"*, *"its finance-cost row was a first-edition artefact"*). Measured across the book: of 24 current delivered studies, **only PHAR and SAVOLA carry a section of this class**, and ADNOCLS's own study carries it precisely because the model report subtracts it. The title differs from the excluded string by three words, so no string-matching gate sees it. |
| 46 | Terminal value as a share of enterprise value visible to the reader | **PASS** | **78.52% of core enterprise value and 67.72% of total**, both on their own lines inside the bridge (Table 5), rounded to 79% and 68%. Repeated in Table 16, C.1's "when it fails", and §1.9. Computed, not typed. (The summary table's caption claims them too and does not carry them — row 34.) |
| 47 | Summary valuation table present | **PASS, with row 34's gap** | "Valuation summary": one row per lens with EGP/share, distance from market and a basis note; two banded CENTRE rows; a banded memo row for the withdrawn blend; a field-low-to-field-high row; the market price. |
| 48 | Non-sourced drivers logged to `engine/Fundamental_Driver_Ledger.md` | **FAIL** | The ledger carries a PHAR entry (line 771, `fundamental walk-forward, 07-09-2026, LIGHT`), but it records the **walk-forward's** mechanical driver rules, not the delivered study's own House-layer inputs. **46 of the 282 register inputs are layer House** — `erp_term` 7.0%, `kd_term_lc` 15.0%, `kd_term_fx` 6.5%, `esc_energy`, `esc_materials_usd`, `esc_packaging_import_share`, `assoc_norm` 250, `assoc_multiple` 11×, `prov_pct_permanent` 5.25%, `prov_pct_normalising`, `wd_term_basis` — and none of them appears in the ledger. `BETA_TERM = 1.0` (row 26) is not even a House input. Separately, `kd_term_lc`'s source string reads *"roughly 10 points above the **5% inflation target**"* against a terminal inflation of **7.0%** — the same stale-string species as row 27. |
| 49 | The walk-forward scope decision stated in the study | **PASS on the study; FAIL on the record the gate reads** | The study states it in A.4 in plain language: *"It covers one, two and three years ahead, so the third forecast year carries a measured range and the fourth and fifth carry NONE. No range is invented for them."* Table 23's caption states the basis is a SPAN over 3 (or 2) past observations, oriented actual-over-forecast, and that none of it is a percentile or a confidence interval. `engine/phar_walkforward/` holds the run and the ledger entry declares **Scope: LIGHT**, seven sourceable years FY2019–FY2025, five origins, 150 scored cells. **But `scripts/check_walkforward_scope.py` lists PHAR among the 15 with NO DECISION**, on `walkforward_scope_outstanding.json` with the note *"no scope decision stated... this study predates the gate."* The decision exists in prose and in the ledger and is invisible to the gate. |
| 50 | What the study publishes vs what a reader receives from the site | **FAIL, on the ratchet** | `python3 scripts/check_published_gap.py`: `PHAR  **−52.2%**  review GAP_REVIEW_10-09-2026.md audits 85.2465; the site publishes **61.21**`. `assets/data.js` carries `fair: {bear: 58.04, base: 61.21, full: 73.03}` dated "9 Aug 2026" with `fairAsof: "2026-08-06"`, and `files:` pointing at `PHAR_Valuation_Study_09-08-2026.pdf`. The study has moved twice since. Held on `published_gap_outstanding.json` because the book is HELD, so this is not a new breach — but the answer a reader gets from the live page is **28% below** the study's own Frame A. |
| 51 | Ratchet entries carried by this name | **RECORDED** | Eleven, and each is a real open item rather than a formality: `anchor_age` (struck 28 days after the EG currency anchor) · `bridge` (the bridge stands on 2025-12-31 while the latest disclosed is 2026-03-31) · `coc` (no cost-of-capital schedule record) · `eps` (registers no reported EPS beside attributable profit, so the gap cannot be seen) · `ground_up` (row 11) · `lens_vocabulary` · `output` · `published_gap` (row 50) · `terminal_spread` · `walkforward_scope` (row 49) · `waterfall_assertions` (NO IMPORT). `prose_outstanding.json` records PHAR as REMOVED on 2026-09-10 with a clean check — correctly, on its own terms. |

---

## The fifteen hard fails

1. **`assert_sigcm()` and `assert_model_study()` are called nowhere in this study** — zero occurrences, not even as a mention. The edition under audit was built with none of the three standing assertions running; `assert_beta_provenance()` was wired in on 13-09-2026, after delivery and during this audit, by commit `8f14c0cc1`. (Row 1)
2. **The delivered QC gate was one edition behind the study** — `check_calibration_deliverables.py` RED. Closed by this file. (Row 2)
3. **The bibliography's judgements table contradicts the delivered study of the same edition on six of twelve rows**, including the withdrawn 22.31% risk-free print, a retired 20% terminal debt weight, a retired 5% terminal growth, and an associate normalisation of EGP 320m the study's own §7.1 says was cut to 250m. It is a hard-coded literal in `docx_biblio.py:184–256`. (Row 29)
4. **`assert_ground_up()` cannot be run: no driver-line record is committed.** The substance is unit-level on 98.53% of revenue, but nothing outside the study can verify that. (Row 11)
5. **The terminal beta of 1.00 has no provenance at all** — a bare constant at `compute.py:1540`, absent from the 282-input register, worth EGP 27.55 a share on Frame A. (Row 26)
6. **The terminal risk-free rate's register entry does not reproduce**: it derives 10.5% as "7.0% plus 5.5 points" (= 12.5%) and calls 10.5% the previous edition's number. (Row 27)
7. **§2's technical read is struck on the withdrawn EGP 130.05 close of 6 August**, four lines above a table computed on 127.30. (Row 33 i)
8. **§3's price cone is anchored on 130.05 / 6 August with a one-month check date of 6 September** — expired before the 10 September issue. (Row 33 ii)
9. **§6 says both centres sit below the entire three-month band**; centre B at 102.89 is inside a band whose floor of 94.40 is printed two bullets above. (Row 33 iii)
10. **C.5 says both centres "sit inside" EGP 70.85–85.25**; 102.89 is outside it. (Row 33 iv)
11. **Table 9 says the study is 5% BELOW the quarter's seasonal read when its own arithmetic makes it 5.4% ABOVE.** (Row 33 v)
12. **Table 1's caption promises a terminal-value share the table does not contain, and the note it does carry describes the wrong lens** — 22.9% is the normalised-earnings margin, while the DCF's own margin runs 23.75–24.73%. (Row 34)
13. **Label collisions on three of eight figures, including the headline figure**, where "centre B 102.9" and "market price 127.30" are drawn on top of each other; and the cone figure has no x-axis labels. (Row 36)
14. **§7.1 is the edition-history section the model report exists to exclude.** (Row 45)
15. **The sweep register is stamped 09-08-2026 against a 10-09-2026 build and carries no search, positive or negative, for the half-year to 30 June 2026** — the filing the study names three times as what would settle its three largest open items. (Row 19)

Four further items are met with a gap rather than failing: no two-sided binomial on coverage (row 8); three of five escalators with no sourced near-term anchor (row 21); the v2 WACC correct by hand but invisible to `check_cost_of_capital.py` (row 24); the terminal Ke construction unnamed from the closed list (row 25).

---

## What the two research passes actually changed

Commit `f71227aeb`, *"two research passes, and most of what they carried did not survive tracing"*. Traced from outside against `sweep_register.json` and the delivered documents.

**The fair value did not move.** Frame A 85.2465 and Frame B 102.8915 before and after, confirmed by the recalc reconciliations.

**What was kept — two items, and neither came from either report's own claims.**

- **The lead molecule is not first into its own market.** The Egyptian Drug Authority approved Alvotech's AVT02 adalimumab biosimilar (marketed as Adalimumab-EVA, commercialised by Bioventure) on 29 August 2023 — twenty-seven months before this company launched its own, and adalimumab is the one molecule of fourteen in the pipeline that has reached the market. It is in the delivered study in bold on page 9 and it changes the crux's reading: the 45% contribution margin behind the required-revenue solve is now stated as a **ceiling**, not a base case. **Primary-source trace: one step short.** The register cites *"Alvotech investor newsroom announcement, 29 August 2023"*, tagged `REPUTABLE_PRESS`, at `alvotech.com/newsroom/...`. That is the counterparty's own press release. The primary source for an Egyptian marketing approval is the Drug Authority's register, and it is not cited. The study states the fact as an act of the Egyptian regulator.
- **The company's own investor relations says repricing does not restore margin.** Against the October 2024 round of 35–40% increases, gross margin FELL over the nine months to September 2025, to 44.7% from 46.3%; the IR manager says price rises reflect cost rises up to a year earlier. Sourced to *Al Borsa*, 20 December 2025, tagged `REPUTABLE_PRESS`. **It reached the register and not the study**, which is the right outcome: it corroborates a forecast path the model already ran (gross margin down from FY2025 and never back), and the study's READ FIRST promises no press report builds any number about the company. Confirmed: `grep "46.3\|44.7\|repricing"` over the delivered study returns 0 hits.

**What did not survive — three of four written back as dated negative searches.**

- **A throughput of 100,000 vials a day.** One pass reported it; the other, on the same question, said explicitly that no standalone throughput is disclosed and that the 327 million packs a year in circulation is the THREE-FACTORY group figure. The company's December 2025 announcement, its investor presentation and the Drug Authority's factory-licence record were re-read: the licence enumerates one combination sterile line and two primary-biological lines and **states no capacity at all**. Logged as `[INDUSTRY] Negative search — nothing found`, dated **2026-09-10**, searched in English and Arabic, with the reasoning that two independent passes disagreeing on a number IS the finding.
- **A dynamic drug-pricing formula "operationalised 2025" with a 10% index threshold, and pharmacy margins raised to 23% / 27%.** Searched in Arabic, which is the language the decree register uses. The formula is reported on 28 July 2026 as a proposal the authority is **studying**; the standing framework remains the 2012 decision at 25% local / 18% imported with an essential list at 20%, and the reported 2024–25 direction of travel was to **reduce** pharmacists' margins. Logged as one `[COUNTRY]` negative search dated **2026-09-10**. Neither number enters.
- **First commercial revenue "booked late Q4 2025".** Contradicted — the company's own release says the licences pave the way for the operational phase in the **first quarter of 2026** — but **NOT written back as a dated negative search.** It is answered only inside a positive `COMPANY_IR` finding. The commit presents all four as having been recorded as negative searches; three were.

**What the research actually bought, and it is not a number.** The company ring's "announced projects, ventures and capacity" category was unclosed and had been failing the sweep gate. It is closed by the company's own December 2025 announcement in its chairman's name — the EIPICO 3 licences, the operational phase from Q1 2026, the Unified Procurement Authority award for ADALIMAB, the Namibian export request — with the entity established as the listed issuer. The sweep went from red to `PASS — 4/4 rings, 33 findings, 5 negative searches`. **One flaw in that close:** the finding is tagged `COMPANY_IR` but its URL is `meobserver.news`, a third-party carriage, not eipico.com.eg. The other seven COMPANY_IR findings are on the company's own site.

---

## What the filings disclose that the model does not consume [L-006]

A self-audit that only re-checks the work it did keeps missing the work it never did. Asked explicitly against the Sweep Register:

1. **The half-year to 30 June 2026.** The sharpest answer, and it is a question the study never asks. The sweep is stamped 9 August; the edition is 10 September. Whether EIPICO has filed H1-2026 is recorded nowhere, positive or negative, while §7's caveats name a half-year balance sheet three times as what would move the bridge anchor, settle the perimeter mismatch and lift the qualified review. **Until that search is logged either way, the study cannot claim its information set is current.**
2. **Capacity per shift by dosage form.** The investor presentation discloses 54 production lines with capacity per shift per year BY DOSAGE FORM, 414 products across 27 therapeutic groups and 1,238 products registered in export markets. The model carries a single blended domestic price per pack, a single export price and one utilisation number (65.3%). No mix, no dosage-form economics, no therapeutic-group concentration is built, although the disclosure supports it.
3. **Production value against sales value.** The board report discloses production value (EGP 10.8bn) separately from sales (EGP 9.8bn). The model consumes sales and packs; the production-sales wedge — inventory build, and therefore part of the working-capital story that drives a 343-day conversion cycle — is not built from it.
4. **Seven years of unit history.** `engine/phar_walkforward/filings/` holds FY2019–FY2025 annual reports and the walk-forward scores a seven-year packs and price-per-pack series. The delivered study's own history is three years in Appendix A and four for the traded multiple. The longer unit series exists, is primary, and is not in the study.
5. **The disclosed useful-life table.** `useful_lives.json` holds the scalar table (buildings 50, production machines 15, transport 5, furniture 10) that the ledger says weights to 13.80 years. The depreciation rate enters the model as an input sensitised 8.0%→4.5%, not as a roll-forward built from the disclosed lives per asset class.
6. **Note 8.2's three-way associate split.** Al-Batterjee at book, MUP at market because it is listed, Arab API at book. The gap review says the market leg cannot be produced because no MUP price series is held in this repository — which makes it nameable work rather than an unknown. The whole stream is normalised to EGP 250m at 11× instead.
7. **The FY2019 board report's operating KPIs without accounts.** Named in the ledger as the reason the walk-forward window stops at FY2019. Unit data exists for FY2018 with no statements beside it; it is left out rather than fabricated, which is right, and it is a disclosed dataset the model does not reach.

---

## Independence

I did not build this study and I have not edited it. `engine/phar_study/` was hashed before and
after (87 files, `md5sum`, identical). Every study script was run against a sandbox copy at
`/tmp/.../scratchpad/sb/phar_study/` with `engine/` mirrored beside it, so `qc_result.json`,
`recalc_result.json`, `driver_test_result.json`, `sweep_register.json` and the rebuilt
workbook and documents were written there and compared, never in place. `git status` is clean.
The defects above are handed back, not repaired.

**One thing moved under me and is recorded rather than smoothed over.** Between the start
and the end of this audit the branch advanced four commits, one of which —
`8f14c0cc1` — touched `engine/phar_study/beta_reg.py`. Everything above was re-checked
against the tree at `8f14c0cc1`; the only file in this directory that differs from my
opening snapshot is `beta_reg.py`, the delivered documents, workbook, figures and
`study_numbers.json` are byte-identical throughout, and `git diff` on the directory is
empty. Row 1 states the before and the after rather than only the after, because a defect
that was live when the edition shipped is a fact about the edition.
