# GBCO — QC gate, fundamental refresh of 19-08-2026 (second issue, same day)

Refresh of `GBCO_Valuation_Study_08-07-2026` (amended 09-07) on the H1-2026 disclosure set
(KPMG-reviewed consolidated interim FS 30-Jun-2026 + 2Q/1H26 earnings release, both
13-Aug-2026). Fundamentals only: NO recalibration, NO cone re-strike, NO ledger rows, NO
technical pass. **Second issue of the same day, on instruction:** the workbook rebuilt
bottom-up and formula-pure (every derived number a live formula; only disclosed/sourced/
judgement inputs are value cells), and §1 rewritten to the depth pattern of the delivered
ADNOCLS study (challenged-lines tables, a noted EV→equity bridge, one-way priced
alternatives, and terminal-value discipline — terminal growth paid for via reinvestment
= g/ROIC). ADNOCLS served as the **pattern for §1's depth on explicit instruction**; the
closed reference set (SWDY / ADCB / ALPHADHABI) is unchanged and the study still sits in
SWDY's 16-section skeleton. The terminal-value discipline moved the base fair value
34.2 → 33.6; published range now **19.9 / 33.6 / 44.6**. Everything below was produced by
`qc_gate.py` and the scripts it re-runs — never self-certified; run it to reproduce.

| # | Item | Verdict | Evidence |
|---|------|---------|----------|
| a | Structure, content, format AND depth match the model study | **PASS** | 16-section Word (150 paragraphs, 27 tables, 5 figures) and 16-sheet Excel with the exact sheet names/order from the machine-readable spec, verified by assertion in `build_xlsx.py`; `assert_model_study()` passes on a fully-True checklist; standalone bibliography ships (10-page PDF). §1 depth raised to the ADNOCLS pattern per instruction (see header); skeleton and reference set unchanged. |
| b | Step 0.0 / Step 0 | **N/A — by design** | This trigger does not touch the price side. No series entered any panel; no fit was re-derived. The published cone's own gate evidence (skill +3.2% non-overlapping / +9.6% monthly, drift-on) is quoted verbatim from the July strike with its `asof` stamps. |
| c | SIGCM clause 1 — historicals official-only | **PASS** | Every H1-26 figure from the reviewed FS/ER (both read page-by-page; 51-page scanned FS rendered and read as images); FY23–25 from the issued statements as extracted in the delivered July workbook (cell-verified then; FY25 cross-checked against the ER's own ND/LTM-EBITDA 2.39x → 6,364 ≈ 6,363.3). FY25 restatement (note 39) applied. No vendor/broker/press number anywhere in the subject's historicals. |
| d | SIGCM clause 2 — forecast ground-up | **PASS** | Rebuilt at sub-LOB level: locally-assembled (CKD) cars are the driver and imports (CBU) the residual; buses the driver, construction equipment planned units, trucks the residual; every line volume × price with separate growth in each; FY26E is **derived, never typed** — disclosed H1 + an H2 built on FY25's measured seasonal split with one visible tempering judgement per line; unit costs anchored on the FS segment note (5-B) and escalated ONE class per physical driver (imported share on the FX path, domestic on CPI); margins are outputs (`gpm` computed in the workbook as a residual). |
| e | SIGCM clause 3 — debt LC/FX | **PASS** | Note 26 (EGP avg 20.82%, USD 7.78%), note 29 (variable book 43,480.1; USD liabilities 7,274.4 incl. payables; net USD −1,853.6; USD deposits 5,687.0). Per-tranche split not disclosed → bounded ≤17%, carried 90/10, FX tranche at local-equivalent cost, flagged in study, workbook and bibliography. |
| f | SIGCM clause 4 — asset-conversion cycle | **PASS** | DIO 122 / DSO 27 / DPO 82 / CCC 67 days computed from the statements; WC projected on the measured LTM ratio (23.0%→21.0%) anchored on the ER's five quarterly snapshots; BS/CF rolls (ND cash-sweep, equity roll, associates roll) live as formulas in the workbook's Balance Sheet / Cash Flow sheets. |
| g | SIGCM clause 5 — competitors | **PASS** | CNFN (Egypt NBFS) 9.41x, DOAS (Turkey) 12.55x, AutoNation (US) 9.11x, Bajaj Auto (India) 27.56x — all dated 19-Aug-26, context only; aggregator-discrepancy notes recorded (CNFN mktcap 4.5–6.6bn across services). |
| h | SIGCM clause 6 — beta | **PASS** | `own_stock_beta('GBCO','EG','EGX')`: 0.893, SE 0.202, n=255 weekly, R² 0.244, window 2021-08→2026-07, regressor `raw_indices/EG/EGX30.csv` (as-of 2026-07-22), usable, conforming, no interim note. `assert_beta_provenance()` passes on the actual record. Replaces the July study's assumed 1.0. |
| i | SIGCM clause 7 — formula-based model | **PASS** | Purity audit inside `build_xlsx.py`: **638 formula cells, 326 blue input cells, 0 non-input value cells** — every derived number is a live formula (driver → Segments → IS → BS → CF → DCF → bridge → published range). Independent evaluator recalculates all 638 (0 unparseable) and reconciles **32** outputs to the committed numbers; driver-nudge tests: discount 10→20% reprices the central 33.56→32.06, round 1.4→1.8bn → 36.44, terminal ROIC 22.5→15% → 30.91, CKD volume growth 25% lifts FY30E CKD units 47,926→53,489 through the whole chain. |
| j | SIGCM clause 8 — flag-before-issue | **PASS** | Flags in the delivered documents: debt-split bound; LOB-vs-segment perimeter note; MNT-Halan unverifiable carrying (qualified review, quoted); import-share of unit cost constructed and flagged; TV 85% of auto EV; stale price side with dates; FY-level tires split absent. Stop-and-inform: nothing blocking was inaccessible (IR site reachable, both primary documents supplied). |
| k | Sweep register | **PASS** | `sweep.py`: 4/4 rings, 24 findings (1 B · 7 S · 7 D · 7 C · 2 NEG), 13 driver-gate rows (12 bottom-up / 1 top-down with its negative search), primary access logged (ir.gb-corporation.com REACHABLE), study-year quarters Q1+Q2-2026 both swept, release AND statements registered (COMPANY_IR distinct), FS years 3 (warning vs target 4 — disclosed; FY22 not re-pulled for a refresh). |
| l | Bibliography document | **PASS** | Standalone: 15 primary documents, the full **187-input** four-field register grouped by layer (169 company / 11 country / 7 industry), 10 judgements each with what-would-overturn-it, 6 negative results, 3 aggregator discrepancies. |
| m | External-reader scrub | **PASS** | `qc_checks.py`: 0 hits across both delivered documents on the internal-vocabulary pattern list, **extended this issue by 9 tokens** after the rendered read caught one register source string carrying an internal acronym (reworded to plain language; the scrub now catches that class). Calibration evidence appears in §3 as plain-language sentences with the statistics inline; no calibration appendix. |
| n | Figures | **PASS** | 5 figures, all opaque on light canvases (programmatic), every one inspected as a rendered image on the r2 numbers (football field, sensitivity heatmap with the bolded joint-judgement cell, driver stack, variance scorecard, expert panel). |
| o | Tables | **PASS** | Fixed layout, explicit widths; programmatic check across all 33 delivered tables (27 study + 6 bibliography): 0 over-wide, 0 starved, 0 bloated. |
| p | Expert appendix at maximum detail | **PASS** | Three experts by method: worldview, when-works/when-fails, worked tables with every intermediate line (E1 sum 40,635 → −8% → 34.44; E2 walk → 4.75 × 8.5x → 40.37; E3 CE × return/cost → 30.56), named sensitivities with numbers (2.58 / 2.64 / 2.29 per share), falsifiers in advance; cross-examination with concessions/rejections; three-in-one-room figure; divergence table pricing each gap (12.23 / 9.80 / 0.75 / 1.13). |
| q | Contested judgement BOTH WAYS | **PASS** | MNT-Halan at the round mark (42.93% × USD 1.4bn × 50.71 = 30,478) vs the company's balance-sheet carrying (15,723.5): SOTP **33.69 vs 21.46**, weighted central **33.56 vs 28.66**, side by side in the summary table, the football field, the bridge sheet's parallel columns, §1.5/1.7, and the crux ladder. Never averaged; bear/full of the published range live in the two worlds. |
| r | Rendered-PDF read | **PASS** | All 16 study pages, bibliography pages (title, register, judgements) and model-PDF pages (19, each content-bearing) read as images on this issue. Defects caught and fixed in-pass: §1.5 quoted the centrals' gap from unrounded values (4.89) against displayed 33.56/28.66 → now display-consistent (4.90); one register source string carried an internal acronym → reworded (see item m). |
| s | WACC v2 | **PASS** | rf* = 22.92 − 6.37/3.41; ERP 13.94/9.41 (both bases published); beta tier-1 regression; Kd marginal from the filing (20.82% EGP variable book; tenor-vs-sovereign tension disclosed and explained, not suppressed); MV equity weights (IR-page quote 29.70 × 1,085.5); WACC 22.72%/23.33%. The v1 double-count (raw yield + CRP-loaded ERP) that reached the July study is retired here — this refresh IS the re-issue the open-items list called for. |
| t | Variance vs the prior study (>5% escalations) | **PASS** | 9 escalations, each traced to the driver at fault (table in study §1.6 and `study_numbers.json['variance']`): CV&CE +37% (under-forecast cycle), trading −11% (one-off as run-rate), group NP −34% pace (flat 28% tax + no finance-cost line), ASP +7%, MNT stake 41.61→42.93 (press superseded by FS), FX 47.5→50.71. |
| u | Terminal-value discipline (adopted from the §1 model) | **PASS** | TV = NOPAT_FY30E × (1+g) × (1 − g/ROIC_t) / (WACC − g) with ROIC_t 22.5% anchored on the realized LTM ROCE 22.9% (release table 10) — growth is paid for with 51.1% reinvestment of terminal NOPAT. The July construction (Gordon on year-five FCFF) is computed and published as the **ALTERNATIVE**: +EGP 1,853mn (+1.71/share on the auto leg), and shown to imply ROIC 25.5% with no year ever reinvesting at that rate. Both appear in the bridge table, the DCF sheet (rows 45–46) and §1.9; the TV-ROIC ladder (15%→uncapped) is priced in Sensitivity rows 21–26. This is the change that moved base 34.2 → 33.6. |
| v | data.js integrity | **PASS** | `node --check` both files; loaded and asserted vs HEAD: 89 tickers and 201 ledger rows, LEDGER byte-identical, every ticker except GBCO byte-identical, GBCO `spot/spotDate/ccy/dist/hz/touch/levels/tech/asof/name/nameAr/code` byte-equal to HEAD; only `fair` (→ 19.9/33.6/44.6) and the four GBCO `files` links (cache-bust `?v=0819b`, scoped per-filename — RIYADHCABLE's `?v=0819a` untouched, verified) changed. |
| w | Verify-by-import | **PASS** | 10 engine modules imported (not parsed) before commit. |
| x | Factor-stack slider constants | **N/A — with evidence** | gbco.html carries no bespoke factor-stack slider: CONT_FIXED/EV_FIXED/GEO_MEAN/LNCH_MEAN exist only in `build_adh_page.py` (ADCB/ALPHADHABI calculator pages) — repo-wide grep 19-08-2026. Nothing to re-fit; recorded in `study_numbers.json['slider_constants']`. |
| y | Driver Ledger | **Recorded absent** | `engine/Fundamental_Driver_Ledger.md` does not exist in the repo (as CLAUDE.md already warns). Same-class-prior check therefore ran against the July GBCO study's own driver stack (the only same-class prior); this refresh's driver decisions are logged in `sweep_register.json` (gate table) + the 187-row input register, compilable into the ledger when it is rebuilt. |

## What was deliberately left untouched, and why
- `spot` (31.31), `spotDate`, `dist`, `hz`, `touch`, `levels`, `tech`, `asof` in
  `assets/data.js` — the price side is on its own clock; no fresh OHLC was posted, so no
  roll-forward ran. The published cone's staleness (price data 2026-07-22 vs refresh date
  2026-08-19; 1M window resolves 2026-08-23) is stated plainly in study §3, not reconciled.
- `LEDGER` — append-only, and a fundamental refresh never writes ledger rows.
- The pooled EG (ν, width_cal) fit, drift, signal state — untouched; not even read into the
  deliverables beyond the quoted July-strike statistics.
- The five-year backtest PNG (`assets/calibration_GBCO.png`) — a fundamentals-only refresh
  moves nothing it measures.

## Observed defects NOT fixed here (out of mandate — for the next technicals pass)
1. `assets/data.js` GBCO carries **two `levels:` keys** (19-Jul values then 17-Aug values);
   JS last-key-wins so the page renders the 17-Aug set, but the dead first key should be
   removed by the next technical pass.
2. The published `tech.bull/bear` sentences reference the dead first `levels` set (31.61 /
   28.64 / 34.20 / 23.99) while the rendered `levels` are 31.70/32.30/33.40 ·
   29.96/28.20/26.73 — an internal inconsistency of the published technical block,
   reproduced verbatim in study §2 as the mandate requires, and flagged here.

## Deliverables
`files/GBCO_Valuation_Study_19-08-2026_public.{docx,pdf}` (16 pp),
`files/GBCO_Valuation_Model_19082026_public.{xlsx,pdf}` (16 sheets / 19 pp),
`files/GBCO_Bibliography_19-08-2026.{docx,pdf}` (10 pp), mirrored in `engine/gbco_refresh/`
with every build script, `sweep_register.json`, `study_numbers.json`, and this gate.
Publishing to testahil.com is a separate, explicit ask and has NOT been done.
