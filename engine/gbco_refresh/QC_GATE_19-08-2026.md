# GBCO — QC gate, fundamental refresh of 19-08-2026

Refresh of `GBCO_Valuation_Study_08-07-2026` (amended 09-07) on the H1-2026 disclosure set
(KPMG-reviewed consolidated interim FS 30-Jun-2026 + 2Q/1H26 earnings release, both
13-Aug-2026). Fundamentals only: NO recalibration, NO cone re-strike, NO ledger rows, NO
technical pass. Everything below was produced by `qc_gate.py` and the scripts it re-runs —
never self-certified; run it to reproduce.

| # | Item | Verdict | Evidence |
|---|------|---------|----------|
| a | Structure, content, format AND depth match the model study | **PASS** | 16-section Word (141 paragraphs, 21 tables, 5 figures) and 16-sheet Excel with the exact sheet names/order from the machine-readable spec, verified by assertion in `build_xlsx.py`; `assert_model_study()` passes on a fully-True checklist; standalone bibliography ships (10-page PDF). |
| b | Step 0.0 / Step 0 | **N/A — by design** | This trigger does not touch the price side. No series entered any panel; no fit was re-derived. The published cone's own gate evidence (skill +3.2% non-overlapping / +9.6% monthly, drift-on) is quoted verbatim from the July strike with its `asof` stamps. |
| c | SIGCM clause 1 — historicals official-only | **PASS** | Every H1-26 figure from the reviewed FS/ER (both read page-by-page; 51-page scanned FS rendered and read as images); FY23–25 from the issued statements as extracted in the delivered July workbook (cell-verified then; FY25 cross-checked against the ER's own ND/LTM-EBITDA 2.39x → 6,364 ≈ 6,363.3). FY25 restatement (note 39) applied. No vendor/broker/press number anywhere in the subject's historicals. |
| d | SIGCM clause 2 — forecast ground-up | **PASS** | PC/CV&CE/LM built volume × price with separate growth in each; trading and other at the finest disclosed level (FY-level tires-vs-parts split not disclosed — flagged); unit costs anchored on the FS segment note (5-B) and escalated ONE class per physical driver (imported share on the FX path, domestic on CPI); margins are outputs (`gpm_path` computed, never input). |
| e | SIGCM clause 3 — debt LC/FX | **PASS** | Note 26 (EGP avg 20.82%, USD 7.78%), note 29 (variable book 43,480.1; USD liabilities 7,274.4 incl. payables; net USD −1,853.6; USD deposits 5,687.0). Per-tranche split not disclosed → bounded ≤17%, carried 90/10, FX tranche at local-equivalent cost, flagged in study, workbook and bibliography. |
| f | SIGCM clause 4 — asset-conversion cycle | **PASS** | DIO 122 / DSO 27 / DPO 82 / CCC 67 days computed from the statements; WC projected on the measured LTM ratio (23.0%→21.0%) anchored on the ER's five quarterly snapshots; BS/CF rolls (ND cash-sweep, equity roll, associates roll) in compute + as live formulas in the workbook. |
| g | SIGCM clause 5 — competitors | **PASS** | CNFN (Egypt NBFS) 9.41x, DOAS (Turkey) 12.55x, AutoNation (US) 9.11x, Bajaj Auto (India) 27.56x — all dated 19-Aug-26, context only; aggregator-discrepancy note recorded (CNFN mktcap 4.5–6.6bn across services). |
| h | SIGCM clause 6 — beta | **PASS** | `own_stock_beta('GBCO','EG','EGX')`: 0.893, SE 0.202, n=255 weekly, R² 0.244, window 2021-08→2026-07, regressor `raw_indices/EG/EGX30.csv` (as-of 2026-07-22), usable, conforming, no interim note. `assert_beta_provenance()` passes on the actual record. Replaces the July study's assumed 1.0. |
| i | SIGCM clause 7 — formula-based model | **PASS** | 446 formula cells; the independent evaluator recalculates all of them (0 unparseable) and reconciles 16 key outputs to the committed numbers exactly; driver-nudge test: discount 10→20% reprices central 34.16→32.59, round 1.4→1.8bn reprices to 37.04. |
| j | SIGCM clause 8 — flag-before-issue | **PASS** | Flags in the delivered documents: debt-split bound; LOB-vs-segment perimeter note; MNT-Halan unverifiable carrying (qualified review, quoted); TV 86% of auto EV; stale price side with dates; FY-level tires split absent. Stop-and-inform: nothing blocking was inaccessible (IR site reachable, both primary documents supplied). |
| k | Sweep register | **PASS** | `sweep.py`: 4/4 rings, 24 findings (1 B · 7 S · 7 D · 7 C · 2 NEG), 13 driver-gate rows (12 bottom-up / 1 top-down with its negative search), primary access logged (ir.gb-corporation.com REACHABLE), study-year quarters Q1+Q2-2026 both swept, release AND statements registered (COMPANY_IR distinct), FS years 3 (warning vs target 4 — disclosed; FY22 not re-pulled for a refresh). |
| l | Bibliography document | **PASS** | Standalone: 15 primary documents, the full 174-input four-field register grouped by layer (155 company / 12 country / 6 industry / 1 global), 10 judgements each with what-would-overturn-it, 6 negative results, 3 aggregator discrepancies. |
| m | External-reader scrub | **PASS** | `qc_checks.py`: 0 hits on 34+ internal-vocabulary patterns across both delivered documents; calibration evidence appears in §3 as plain-language sentences with the statistics inline; no calibration appendix. |
| n | Figures | **PASS** | 5 figures, all opaque on light canvases (programmatic), every one inspected as a rendered image. Defects found and fixed in-pass: fig1 label/title collision + wrong shared ranges on the two SOTP rows; fig2 annotation collision + ink-on-dark contrast; fig4 bar clipped at the axis; figD1 label/title collision. |
| o | Tables | **PASS** | Fixed layout, explicit widths; programmatic check across all 27 delivered tables (21 study + 6 bibliography): 0 over-wide, 0 starved, 0 bloated (after fixing 2 over-wide + 4 starved found on the first run). |
| p | Expert appendix at maximum detail | **PASS** | Three experts by method: worldview, when-works/when-fails, worked tables with every intermediate line (E1 42,478→−8%→36.00; E2 walk→40.34; E3 CE×return/cost→30.56), named sensitivities with numbers, falsifiers in advance; cross-examination with concessions/rejections; three-in-one-room figure; divergence table pricing each gap. |
| q | Contested judgement BOTH WAYS | **PASS** | MNT-Halan at the round mark (42.93% × USD 1.4bn × 50.71 = 30,478) vs the company's balance-sheet carrying (15,723.5): SOTP 35.22 vs 22.99, central 34.16 vs 29.26, side by side in the summary table, the football field, the bridge sheet, §1.5/1.7, and the workbook's parallel columns. Never averaged; bear/full of the published range live in the two worlds. |
| r | Rendered-PDF read | **PASS** | All 14 study pages + bibliography and model spot pages read as images. Defects found and fixed in-pass: duplicated "Group net profit" row in the variance table; register headline count off by one (174 vs 173 enumerated); model PDF pagination (48→17 pages via print scaling). |
| s | WACC v2 | **PASS** | rf* = 22.92 − 6.37/3.41; ERP 13.94/9.41 (both bases published); beta tier-1 regression; Kd marginal from the filing (20.82% EGP variable book; tenor-vs-sovereign tension disclosed and explained, not suppressed); MV equity weights (IR-page quote 29.70 × 1,085.5); WACC 22.72%/23.33%. The v1 double-count (raw yield + CRP-loaded ERP) that reached the July study is retired here — this refresh IS the re-issue the open-items list called for. |
| t | Variance vs the prior study (>5% escalations) | **PASS** | 9 escalations, each traced to the driver at fault (see the table in the study §1.6 and `study_numbers.json['variance']`): CV&CE +37% (under-forecast cycle), trading −11% (one-off as run-rate), group NP −34% pace (flat 28% tax + no finance-cost line), ASP +7%, MNT stake 41.61→42.93 (press superseded by FS), FX 47.5→50.71. |
| u | data.js integrity | **PASS** | `node --check` both files; loaded and asserted vs HEAD: 89 tickers and 201 ledger rows byte-identical except GBCO `fair` + `files`; spot/spotDate/dist/hz/touch/levels/tech/asof byte-equal to HEAD; LEDGER untouched. |
| v | Verify-by-import | **PASS** | 10 engine modules imported (not parsed) before commit. |
| w | Factor-stack slider constants | **N/A — with evidence** | gbco.html carries no bespoke factor-stack slider: CONT_FIXED/EV_FIXED/GEO_MEAN/LNCH_MEAN exist only in `build_adh_page.py` (ADCB/ALPHADHABI calculator pages) — repo-wide grep 19-08-2026. Nothing to re-fit; recorded in `study_numbers.json['slider_constants']`. |
| x | Driver Ledger | **Recorded absent** | `engine/Fundamental_Driver_Ledger.md` does not exist in the repo (as CLAUDE.md already warns). Same-class-prior check therefore ran against the July GBCO study's own driver stack (the only same-class prior); this refresh's driver decisions are logged in `sweep_register.json` (gate table) + the 174-row input register, compilable into the ledger when it is rebuilt. |

## What was deliberately left untouched, and why
- `spot` (31.31), `spotDate`, `dist`, `hz`, `touch`, `levels`, `tech`, `asof` in
  `assets/data.js` — the price side is on its own clock; no fresh OHLC was posted, so no
  roll-forward ran. The published cone's staleness (price data 2026-07-22 vs refresh date
  2026-08-19; 1M window resolves 2026-08-23) is stated plainly in study §3, not reconciled.
- `LEDGER` — append-only, and a fundamental refresh never writes ledger rows.
- The pooled EG (ν, width_cal) fit, drift, signal state — untouched; not even read into the
  deliverables beyond the quoted July-strike statistics.
- The five-year backtest PNG (`assets/calibration_GBCO.png`) — a fundamentals-only refresh
  moves nothing it measures.

## Observed defects NOT fixed here (out of mandate — for the next technicals pass)
1. `assets/data.js` GBCO carries **two `levels:` keys** (19-Jul values then 17-Aug values);
   JS last-key-wins so the page renders the 17-Aug set, but the dead first key should be
   removed by the next technical pass.
2. The published `tech.bull/bear` sentences reference the dead first `levels` set (31.61 /
   28.64 / 34.20 / 23.99) while the rendered `levels` are 31.70/32.30/33.40 ·
   29.96/28.20/26.73 — an internal inconsistency of the published technical block,
   reproduced verbatim in study §2 as the mandate requires, and flagged here.

## Deliverables
`files/GBCO_Valuation_Study_19-08-2026_public.{docx,pdf}` (14 pp),
`files/GBCO_Valuation_Model_19082026_public.{xlsx,pdf}` (16 sheets / 17 pp),
`files/GBCO_Bibliography_19-08-2026.{docx,pdf}` (10 pp), mirrored in `engine/gbco_refresh/`
with every build script, `sweep_register.json`, `study_numbers.json`, and this gate.
Publishing to testahil.com is a separate, explicit ask and has NOT been done.
