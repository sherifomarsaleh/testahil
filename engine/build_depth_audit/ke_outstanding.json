{
 "why": "[R-COC-02] with the ratchet of [R-ENF-02]. Every study carrying a cost-of-capital record on the day the rule was adopted (07-09-2026) predates it and declares no terminal construction. Each entry names WHAT THE RECORD ACTUALLY REPRODUCES UNDER, because the fix is a single declared field rather than a rebuild and an entry that names the fix is an entry somebody can close. NONE of these is a wrong number: all five reproduce exactly, three under same_beta and two under a Hamada relevering at 22.50%. What is outstanding is that the record does not SAY WHICH, so a reader cannot tell a relevered beta from a typo and neither could any instrument. The list may only ever get SHORTER (--prune). [R-ENF-08, 07-09-2026]: each entry now carries the SIGNATURE of the failure it excuses. A ratchet entry is a ticker, so before this a listed study was excused from the whole gate and could acquire a SECOND, DIFFERENT defect of the same class unseen — which the injection harness demonstrated by planting a stale land base in a study already listed and being ignored. Comparison is on the failure's SHAPE with live figures stripped, because an exact-text match would go red every time a date moved. [RE-SEEDED 07-09-2026] The signatures were rewritten when [R-COC-02] gained the weights and beta-provenance clauses: [R-ENF-08] correctly refused to excuse a failure description that no longer described the failure, so every listed study read as a NEW breach. THE DEBT DID NOT CHANGE, THE DESCRIPTION DID — re-seeding records what is actually wrong, which is what a ratchet entry is; leaving the stale signatures would have made the build permanently red, which [R-ENF-02] forbids.",
 "adopted": "2026-09-07",
 "outstanding": {
  "PHDC": {
   "reason": "reproduces exactly under 'same_beta'; declare ke_terminal_construction.",
   "signature": "the record names no beta_source, so nothing distinguishes a measured regression from a priced tier-3 fallback from a number somebody typed. One of ['own_stock_regression', 'peer_relevered', 'tier3_fallback', 'shrunk']; ke_terminal names no construction. The record must declare one of ['same_beta', 'relevered']; it reproduces under 'same_beta'"
  },
  "TMGH": {
   "reason": "reproduces exactly under 'same_beta'; declare ke_terminal_construction.",
   "signature": "the record names no beta_source, so nothing distinguishes a measured regression from a priced tier-3 fallback from a number somebody typed. One of ['own_stock_regression', 'peer_relevered', 'tier3_fallback', 'shrunk']; ke_terminal names no construction. The record must declare one of ['same_beta', 'relevered']; it reproduces under 'same_beta'"
  },
  "ARCC": {
   "reason": "reproduces exactly under 'relevered' at an implied 22.50% — beta 0.927522 unlevers to 0.900090 at a debt weight of 0.0378 and relevers to 1.074483 at a terminal 0.20. Declare ke_terminal_construction and relevering_tax_rate; the rate must be STATED, never solved out of the answer it explains.",
   "signature": "the record names no beta_source, so nothing distinguishes a measured regression from a priced tier-3 fallback from a number somebody typed. One of ['own_stock_regression', 'peer_relevered', 'tier3_fallback', 'shrunk']; ke_terminal names no construction. The record must declare one of ['same_beta', 'relevered']; it reproduces under 'relevered' at an implied tax rate of 22.50%, which the record does not state — declare it, do not let it be solved out of the answer"
  },
  "SCEM": {
   "reason": "reproduces exactly under 'relevered' at an implied 22.50% — beta 1.000000 unlevers to 0.995949 and relevers to 1.188914 at a terminal debt weight of 0.20. Same fix as ARCC.",
   "signature": "the record names no beta_source, so nothing distinguishes a measured regression from a priced tier-3 fallback from a number somebody typed. One of ['own_stock_regression', 'peer_relevered', 'tier3_fallback', 'shrunk']; ke_terminal names no construction. The record must declare one of ['same_beta', 'relevered']; it reproduces under 'relevered' at an implied tax rate of 22.50%, which the record does not state — declare it, do not let it be solved out of the answer"
  },
  "FERTIGLOBE": {
   "reason": "UNREADABLE rather than clean: commits a cost-of-capital record carrying no rf_star, beta, erp or ke_exp, so the explicit cost of equity cannot be reproduced at all.",
   "signature": "record carries no rf_star, beta, erp or ke_exp, so the explicit cost of equity cannot be reproduced at all; record carries no market-value weights; the record names no beta_source, so nothing distinguishes a measured regression from a priced tier-3 fallback from a number somebody typed. One of ['own_stock_regression', 'peer_relevered', 'tier3_fallback', 'shrunk']"
  },
  "STC": {
   "reason": "reproduces exactly under 'same_beta'; declare ke_terminal_construction. FOUND BY THE GATE AND NOT BY THE MEASUREMENT THAT PRECEDED IT: the hand-written probe looked for cost_of_capital_record alone and reported six studies; the gate searches three spellings and reads seven. L-355 exactly, caught by the instrument rather than by a person.",
   "signature": "the record names no beta_source, so nothing distinguishes a measured regression from a priced tier-3 fallback from a number somebody typed. One of ['own_stock_regression', 'peer_relevered', 'tier3_fallback', 'shrunk']; ke_terminal names no construction. The record must declare one of ['same_beta', 'relevered']; it reproduces under 'same_beta'"
  },
  "ADNOCLS": {
   "reason": "ADDED 07-09-2026 when [R-COC-02] was extended to the WEIGHTS. Its committed weight_equity 0.800444 and weight_debt 0.071933 SUM TO 0.872377, not one, and its wacc_exp of 8.5484% reproduces from neither the raw weights (7.9230%, -62.5bp) nor the renormalised ones (9.0821%). This is THE EXEMPLAR — the document every new study is built by copying — so the construction propagates looking exactly like the house standard. Fixing it is a re-issue of a delivered study, not a declaration.",
   "signature": "weight_equity 0.800444 + weight_debt 0.071933 = 0.872377, not one — a weighted average over weights that do not sum to one is not an average of anything; wacc_exp 0.085484 does not reproduce from its own weights and rates (0.079230, +62.5 bp); the record names no beta_source, so nothing distinguishes a measured regression from a priced tier-3 fallback from a number somebody typed. One of ['own_stock_regression', 'peer_relevered', 'tier3_fallback', 'shrunk']"
  }
 },
 "pruned_on": [
  {
   "removed": [
    "ADNOCLS"
   ]
  }
 ]
}