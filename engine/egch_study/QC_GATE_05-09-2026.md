# EGCH — QC gate, 5 September 2026

Egyptian Chemical Industries "KIMA" · Egyptian Exchange: EGCH · reporting and valuation
currency Egyptian pounds · edition `EGCH_Valuation_Study_05-09-2026` (Word + PDF),
`EGCH_Valuation_Model_05092026` (Excel + PDF), `EGCH_Bibliography_05-09-2026` (Word + PDF).

Filled from **outside** the study by an auditor who did not build it. Every gate below was
**re-run**, not read off its last recorded output; every repository gate was run from the
repository root. Nothing here is self-certified: each row names the artefact, the command or
the number that carries it, and where an item is met with a gap the gap is stated in the same
breath rather than passed over.

The 08-08-2026, 01-09-2026 and 03-09-2026 editions are superseded and were not opened as
current. Where a figure below is compared against an earlier edition it is named as such.

---

## VERDICT

**NOT DELIVERABLE.** Eight defects reach a reader and block delivery; the model itself
recalculates and reproduces. The blockers are listed at the end of this file.

The study is also **HELD** under [R-GAP-02] independently of anything in this gate — both
branches sit more than 10% below the price — and that hold is correct and is not a defect.

---

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker, exchange, market code | EGCH · EGX · EG | `assets/data.js` line 502 carries `code: "EGX:EGCH"`. The exchange is read from the code prefix, never inferred from the `raw_ohlc/EG/` folder. |
| Regressor the beta owes | **EGX30** | `wacc_builder.market_index_path('EG','EGX')` resolves to `engine/raw_indices/EG/EGX30.csv`. EG spans one exchange, so no ambiguity and no interim note is owed; this is not a DFM-listed name and not a Qatari name. |
| Company class | Gas-fed nitrogen fertiliser — **operating company, single site, one operating segment** | FY2024/25 revenue EGP 8,603mn from one segment; PPE and construction in progress are EGP 18,712mn of EGP 30,048mn total assets; no lending book, no rental business, no holding structure. `lens_record.class = "petrochemical"`, which `research_protocol.LENS_REGISTRY` registers with DCF as the primary. |
| Reference pattern | **ADNOCLS** (operating company), inside the closed three-name set | `research_protocol.REFERENCE_SET == ('ADNOCLS','ADCB','ALPHADHABI')`, asserted at import. Neither the bank nor the holdco pattern applies; the nearest pattern is the operating company and the study uses ADNOCLS's skeleton unchanged. |
| Standard the study is stamped to | **2026.09.01** | `attestation.json` and `study_numbers.json` both carry `standard_version: "2026.09.01"`; `python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` returns `2026.09.01`. They agree. |
| Price the answer is struck against | **EGP 14.41, 2026-09-03** | `engine/prices/SUPPLIED_03-09-2026.json` carries `{"EGCH": {"price": 14.41, "date": "2026-09-03"}}`. `scripts/check_valuation_gap.py` does **not** list EGCH among the four studies whose struck spot diverges from the latest known price, so [R-GAP-01]'s latest-price clause is satisfied. |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | Structure, sections and sheets match the model report | **PASS** | `python3 attest.py` re-run: "51 headings, 0 model-study sections missing"; "16 sheets in the model order, 0 mismatches". `python3 scripts/check_workbook_structure.py` from the repo root opens the latest workbook in every study directory and lists EGCH among the 20 that MATCH THE MODEL REPORT. The 16 Word sections are present in order (masthead + READ FIRST · Headline · Valuation summary · Company overview · §1.1–1.9 · §2 · §3 · §4 · §5 · §6 · §7 · App A · App B · App C · About · Disclosure), read page by page off the rendered PDF. The excluded section "What changed in these editions, and why" does not appear. |
| 2 | Depth bar 1 — standalone bibliography document | **PASS** | `EGCH_Bibliography_05-09-2026.docx/.pdf`, 21 pages, read as rendered images. Carries all five required parts: a primary-documents table naming each filing with period, report date, auditor and retrieval date; the FULL input register of all **304** inputs with value / unit / date / source-and-construction grouped by layer (L1 192 · L2 14 · L3 12 · L4 21 · L5 65); a judgements table with what-would-overturn-it on each of 7; a negative-results table of 6 searches that failed; and a third-party-discrepancy table of 3. **Two defects in it are recorded at rows 21 and 22 below.** |
| 3 | Depth bar 2 — four-field provenance on every input, validated by assertion | **PASS, with two gaps named** | `attest.py` builds `four_field` from `input_register.json` itself and reports "304 inputs, all four-field complete"; `assert_model_study()` refuses otherwise. **Gap (a):** `python3 source_audit.py` (in the directory, NOT in `run_all.py`'s pipeline) exits 1 — "31 input(s) carry a source that describes a source rather than citing one", among them `rf_observed` ("Egypt ten-year government bond yield, market quote"), `urea_fob_egypt`, `usd_egp_spot` and `subsidised_price`. A four-field check that only tests for a non-empty string is not a provenance check. Pre-existing; named here rather than rediscovered. **Gap (b), and it is sharper because it is a source that cites a document not carrying the number:** see row 22. |
| 4 | Depth bar 3 — numeric traceability, builders read the numbers file only | **PASS, with one gap named** | `python3 qc_checks.py`: "4 builders, 0 typed financial numerals" across `docx_egch.py`, `docx_biblio.py`, `build_xlsx.py`, `figures.py`. `python3 formula_audit.py`: "638 formulas across 16 sheets, embedded constants that are not structural: 0". `python3 scripts/check_prose_figures.py` from the repo root: EGCH "prose figures checked: 346; unmatched: 0". **Gap:** `compute.py:745` prints `"reverse DCF: EGP 13.98 implies…"` with the price **typed and stale** — the solver correctly uses `_V('spot_price')` = 14.41, only the operator-facing console line is hardcoded at the superseded 13.98. It reaches no reader and the scan's builder list legitimately excludes the model, so this is a gap and not a blocker. |
| 5 | Depth bar 3 — the delivered workbook recalculated independently | **PASS** | `python3 recalc.py` on `EGCH_Valuation_Model_05092026.xlsx`: **"638 of 638 formula cells reproduce the model, 0 unresolvable, 0 unchecked"**, plus **16/16** headline reconciliations. Nothing was unparseable, so nothing is reported as a skip. `python3 cross_check.py`: "the model, the delivered workbook and the delivered study state the same 16 headline figures". The workbook is 638 formula cells against 1,248 populated non-formula cells (34% live); the three permitted pasted classes are named on the READ FIRST sheet. |
| 6 | Script-built files verified by a **cell-by-cell diff** against the delivered file | **PASS** | The study was rebuilt from source in a clean sandbox (`beta_record → inputs → compute → lenses → experts → alternatives → sensitivity → flat_rate_ladder → band_record → figures → build_xlsx → docx_egch → docx_biblio`) and diffed cell by cell against the delivered files. Workbook: **2,917 cells compared across all 16 sheets, 0 differences.** Study Word: 638 paragraph-and-table rows, **0 differences**. Bibliography Word: 414 rows, **0 differences**. A clean recalculation is necessary and not sufficient; this is the sufficient half. |
| 7 | Depth bar 4 — external-reader scrub, zero internal vocabulary, zero verdict tokens | **PASS** | `python3 qc_checks.py`: "73 patterns, 0 hits" across both delivered Word documents. Independently, `band_record.assert_no_verdict_tokens()` was run over **all five** delivered artefacts (study .docx and .pdf, bibliography .docx and .pdf, workbook .pdf) — clean on every one; PASS/PARITY/FAIL and CRPS appear beside no company name anywhere. `python3 scripts/check_delivered_vocabulary.py` from the repo root lists EGCH among the 21 CLEAN studies on both shape patterns (rule identifiers, repository paths). Calibration evidence lives in §3 as plain-language sentences with the statistics inline; the document has exactly three appendices (A statements, B peers/risk/research register, C expert panel) and **no calibration appendix**. |
| 8 | Depth bar 5 — figure discipline, and every figure inspected as a rendered image | **PASS, with one gap named** | `qc_checks.py`: "15 figures, 0 with transparency" (solid light canvas verified programmatically; `figures.py` sets `savefig.transparent: False` and `savefig.facecolor: BG`). **All 14 figures that reach a reader were opened and read as rendered images** at 125 dpi off the delivered PDF, plus `fig2_bridge.png` and `fig9_field.png` at native resolution: no label collisions, no bar outside its own axis (the bridge figure's earlier defect is fixed — all six bars sit inside the plot), axis units current, every callout legible. **Gap:** the count of 15 includes `fig8_price.png`, which `figures.py:273` builds and **no builder inserts into any delivered document** — 15 checked, 14 delivered. |
| 9 | Depth bar 6 — table discipline, three counts per document | **PASS** | `qc_checks.py`: "56 tables checked, 0 problems". Measured independently from outside, per document and **both ways** — off the cell widths the study's own checker reads AND off the `tblGrid` the renderer actually reads, which is the L-014 defect class: **study document, text block 7.00in, 43 tables — over-wide 0, starved 0, bloated 0; bibliography, text block 9.80in, 13 tables — over-wide 0, starved 0, bloated 0; grid-versus-cell disagreements 0 in both.** Fixed layout with explicit widths on every table. |
| 10 | Depth bar 7 — expert appendix at maximum detail, "Expert 1/2/3" only | **PASS** | Each of the three carries worldview / when-it-works / when-it-fails / a worked table with every intermediate line / a named sensitivity with numbers / a falsifier stated in advance. Expert 1 (replacement cost) runs 20 lines from gross fixed assets at cost to per share and foots at every step (17,022 − 3,435 = 13,587; 17,889 + 3,392 + 1,383 + 2,155 = 24,819; − 10,032 = 14,787; ÷ 1,986.579mn = 7.44; × 0.60 = 4.47). Expert 2 runs 27 lines and **prints the tax row and the bridge rows the body table omits** (see row 19). Expert 3 runs 11 lines with a 3 × 5 volatility/maturity grid. C.4 cross-examination carries four challenges each conceded or rejected with the mechanism given; C.5 the three in one room; C.6 a divergence table isolating six pairs and the single assumption that drives each. Headings read "C.1 Expert 1 — …", "C.2 Expert 2 — …", "C.3 Expert 3 — …"; **no persona name appears anywhere** in either document (checked by full-text search of both PDFs). |
| 11 | Depth bar 8 — the central contested judgement computed both ways, published side by side | **PASS** | The judgement is binary — is the ANNA capital programme carried through or stopped — and it is published in all four required places and averaged in none: the **summary table** (two "Cash flow" rows, 3.90 and 7.88, and an answer row that says "TWO-SIDED … an average would describe a half-built plant"); the **body** (§1.1 Table 5 runs both columns from PV of the explicit window to value per share; §1.7 prices the gap at EGP 3.98 a share / EGP 7,915mn of equity); the **workbook** (Summary and SOTP Bridge sheets both carry "Programme carried through" and "Programme stopped" columns); and **an expert's range** (C.3: "with the programme carried through the claim is worth EGP 4.87 … with the programme stopped … EGP 7.77"). `lenses.json` `contested` carries side_a 3.8967, side_b 7.8807, gap 3.9840. |
| 12 | Step 0.0 — data-quality gate | **PASS on the data, FAIL on the study's own script** | Re-run independently through `data_quality.clean_ohlc`: stock **3,756 raw → 3,755 clean rows**, 2011-01-02 to 2026-08-06, 15.6 years at **240.8 rows/yr** against the EGX calendar, max abs log move **0.1990** against the EG artefact threshold of **0.2901**, flat fraction 0.0495, one row dropped (2011-08-03, non-positive OHLC). EGX30: **3,745 raw → 3,745 clean**, to 2026-07-22, 240.8 rows/yr, max abs log move 0.1112, nothing dropped. Both series clear the gate. **FAIL on the script:** `python3 step0.py` **aborts** at line 32, `AssertionError: PARITY` — it asserts `reg['market_verdict'] == 'PASS'` on machinery [R-CAL-03] retired outright on 25-Aug-2026, and the live EG registry now prints PARITY. Its next assertion, `'EGCH' not in reg['panel_names']`, would also fail: EGCH is now in the 37-name EG panel. The committed `step0_result.json` records `nu: 6.0` against a live `fitted_configs.json` EG fit of **nu 5.0**. The gate is not in `run_all.py`'s pipeline, so the build never runs it. |
| 13 | Step 0 — calibration under the band record | **PASS** | `python3 band_record.py` re-run, which hands `assets/data.js` to **node** and reads the object the page renders rather than regexing the file: **n=57, c50 0.5614, c80 0.8772, c90 0.9474, width 1.446, strength long, flag None.** The delivered §3 states exactly that in plain language with **the count printed beside every percentage** — "Over 57 resolved three-month forecasts … inside the ninety-per-cent band 94.7% of the time, inside the eighty-per-cent band 87.7% and inside the fifty-per-cent band 56.1% — against the ninety, eighty and fifty they promise. The record earns no flag either way." The two-sided binomial at 5% returns no flag, and none is claimed. The width ratio is **disclosed and explicitly not gated** — "1.45 times the width of a naive band anchored on the carry alone — a width disclosed beside the record and never used as a threshold, because a wider band is not automatically a wrong one." |
| 14 | The published price map reproduces the committed engine fit | **FAIL** | `strike_result.json` records `nu: 6.0` and `signal_active: false`. The live `market_profiles.PROFILES['EG']` carries **nu 5.0, width_cal 0.951, signal_active True**; `strike_egch.py` reads all three off the profile, so the committed strike was last run against a fit that has since moved and was not re-run for this edition. Re-striking in a sandbox on the live profile moves **every published percentile** and the headline probability: 1M p5 12.04→12.12, p50 14.19→14.26, p95 16.73→16.77, **P(above anchor) 56.7%→59.1%**; 3M p5 10.69→10.84, p50 14.64→14.77, p95 20.04→20.09, **P(above anchor) 60.7%→63.1%**. Table 22, Table 23 and Figures 11–13 all carry the superseded numbers. The 90% edge moves 3.7% on `width_cal × q95(t(ν))`, inside [R-CAL-01]'s 5% materiality bar — but the drift changed too, and the study's §3 describes the drift as the carry alone while the live profile runs the committed momentum tilt. |
| 15 | SIGCM clause 1 — historicals from official sources only | **PASS, with the outside check unable to see it** | Every historical figure in `input_register.json`'s L1 layer (192 inputs) cites a named filing with its auditor and report date — four audited years (FY2021/22 comparatives, FY2022/23, FY2023/24, FY2024/25, Central Auditing Organization with PKF Rashed Badr or alone) and three reviewed interims (Q1, H1, 9M FY2025/26, Nasr Abou El Abbas / Morison Global). All seven PDFs are held at `engine/egch_study/filings/` and both the annual (48 pages) and the 9M interim (37 pages) carry **zero text-layer characters**, consistent with the study's claim that every figure was read by OCR off the rendered pixels. The bibliography's cross-check table names aggregators and press explicitly as "used to test and never to build". **Named gap:** `python3 scripts/check_source_integrity.py` from the repo root reports EGCH as **UNREADABLE** — "the input register is not in the committed numbers file, so no source can be read from it" — so this clause cannot currently be verified from outside the study at all. On the ratchet with six others. |
| 16 | SIGCM clause 2 — the ground-up clause, on the driver record | **PASS** | `compute.py:806-826` builds five `_rp.DriverLine` records and calls `_rp.assert_ground_up(DRIVER_LINES, ticker='EGCH')`, which refuses on anything short of 100% of revenue. The share-by-level split, read from `study_numbers.json → gates.ground_up`: **unit 74.71% · derived 24.09% · segment 1.20% · topdown 0%**, five lines, 100.0% of revenue covered. Export urea is at `unit` with the tonne named, the auditor's own product-cost table as its source, world urea f.o.b. Egypt net of the 10% duty as the price basis and a per-tonne cost stack as the cost basis. **All four lines below `unit` carry a gap note** — subsidised urea ("the auditor's 147kt over fourteen months annualised"), free-market urea ("the residual of the note-20 local revenue … the filings do not split the three domestic channels"), nitrates ("implied from the revenue note, not disclosed per tonne") and other ("disclosed in total only"). |
| 17 | SIGCM clause 6 — the beta, inspected on its record | **PASS** | `beta_result.json` read directly, not a boolean: **`index_file: "raw_indices/EG/EGX30.csv"`** — under `engine/raw_indices/`, so not a constituent composite; `index_asof 2026-07-22`; market EG, exchange EGX, matching the `EGX:` prefix in `assets/data.js`; frequency weekly on `W-THU`, the EGX's own Sunday-to-Thursday week; **β 1.0302, R² 0.2504, SE 0.1907, n 253** over 4.89 years — the usability gate is met on all three legs (n ≥ 24, R² ≥ 5%, SE < \|β\|), so no fall to a peer beta or 1.0 is owed and the diagnostics are published in §1.9. `interim_note: null` and this is not a DFM-listed name, so no interim disclosure is owed; not Qatari, so no QATAR10 weekly-only caveat is owed. The withdrawn 35-name equal-weight composite (β 1.0527) is recorded in the same file **as withdrawn**, with the reason. `assert_beta_provenance()` is called in the study's own code twice — `beta_record.py:30` and `compute.py:797` — and again in `attest.py:101`; all pass. `scripts/check_study_provenance.py` from the repo root does **not** list EGCH among the 11 outstanding. |
| 18 | SIGCM clauses 3, 4, 5, 7, 8 | **PASS, with the attestation's own shape named** | **(3) Debt split LC/FX:** §1.8 and Table 18 — 99.7% dollar, 0.3% local; the dollar leg carried at local-equivalent cost `(1+11.7%)(1+wedge)−1` on the same purchasing-power wedge the revenue build uses, 24.35% in year one gliding to 13.90%. **(4) Asset-conversion cycle:** Table 16 gives receivable, inventory and payable days from the audited statements (26.8 / 165.2 / 83.2, cycle 108.9) and A.3 projects the balance sheet and cash flow from those day counts — no plug. **(5) Competitors:** Appendix B.1 carries Abu Qir, MOPCO, Helwan/NCIC with capacity and role, used only in the relative lens and labelled as cross-checks. **(7) Formula-based:** 638 live formula cells, driver→IS→BS→CF→DCF, verified by `recalc.py` and by `driver_test.py` (17/17 drivers reprice in the asserted direction). **(8) Flag-before-issue / stop-and-inform:** the bibliography's access notes log four reached and three not-reached URLs with what each failure cost, and the negative-results table records six searches that found nothing. **Named gap:** `attest.py:96` constructs `SIGCMChecklist(**{f: True for f in …})` — every clause set True by the study on itself. Clauses 1, 2 and 6 are separately evidenced above from records rather than booleans; the other six rest on that blanket attestation, which is the exact shape [R-ENF-01] closes. |
| 19 | Sweep Register — primary-source depth | **PASS, with two gaps named** | `python3 sweep.py` re-run through the shared `research_sweep.SweepRegister`, and `python3 scripts/check_sweep_module.py` from the repo root lists EGCH among the 15 studies that RUN THROUGH THE SHARED REGISTER. Result line: **"4/4 rings, 27 findings (1 B · 12 S · 7 D · 4 C · 3 NEG), driver gate 9 rows (9 bottom-up / 0 top-down), primary access: attempted (7), FS years: 4 ['FY2021/22','FY2022/23','FY2023/24','FY2024/25'], IR findings: 1"**. Company site attempted and **logged either way** — `kimaegypt.com/InvestorsRelations.aspx` reached (with the earlier proxy refusal recorded), the Mist IR portal reached, `egx.com.eg` NOT reached (bot challenge, and the consequence named: the share count comes from note 14 instead), `cbe.org.eg` NOT reached, `kima.com.eg` refused. **Four audited fiscal years, which is the target rather than the floor of two**, cited to the filings themselves. **Every study-year quarter already disclosed is swept in** — `study_quarters_disclosed: ['Q1-2025/26','H1-2025/26','9M-2025/26']`, all three registered and the 9M consumed by the model. **Gap (a):** exactly **one** COMPANY_IR-tagged finding — the floor, not depth, and the bibliography's own negative-results table explains why ("None exists. The company publishes filings and disclosure reports only"), which is an honest answer rather than a fix. **Gap (b):** `sweep.py:408` calls `R.check_freshness(SWEEP_DATE)`, comparing the sweep date with itself, so the freshness check can never fire; the real gap is 2026-09-01 to 2026-09-05, four days, inside the 14-day bound, so nothing is concealed — but nothing is tested either. |
| 20 | Cost stack — one escalator per driver class, with a sourced near-term anchor | **PASS** | Table 11 and its caption: gas escalates on its **administered dollar price through the currency path**; the export price on **its own commodity path** (held flat in nominal dollars, with the convention stated); the subsidised price on **its own administered path**; and the domestic lines — other materials, wages, purchased services, inland haulage, other selling and administration — share **one domestic inflation path**, with the reason printed ("that is the index they are paid in; a globally traded input is never put on it"). No blended index runs across physically distinct classes. **Sourced near-term anchor:** gas at **US$4.68/mmbtu**, derived from the company's own Q1 loss disclosure (31,313,235 m³ valued at EGP 251mn = EGP 8.016/m³), reviewed 13-Nov-2025 — not a macro-convergence assertion; the contract formula price of US$5.75 is carried as the priced downside (EGP 2.67 a share). Margins fall out: Table 12 labels the gross and EBITDA rows **"Falls out of the above"** and `qc_checks`/`driver_test` confirm no margin is an input. |
| 21 | Margins as OUTPUTS wherever the filings disclose enough to build cost per unit | **PASS** | Table 12 prints revenue, cost of sales and gross profit for all five forecast years with the margin as the residual (45.7% → 42.3%), and Figure 6 decomposes the FY2026/27 cost per tonne of urea into its six physical drivers (gas EGP 7,336/t, other materials 2,442, depreciation 1,697, inland freight 1,248, administration and other 1,450, wages and services 598). `scripts/check_forecast_anchor.py` from the repo root lists EGCH as **CONFORMING**: gross margin, latest reviewed FY2024/25 audited 0.3839 against a first forecast year of 0.4572, **+7.33pp — above the latest period, so the opening-year clause does not fire**, and the record is printed anyway. Clause two (the path) does fire — 0.4572 falling to 0.4228, 7.5% relative — and is answered with a mechanism from the closed list (`input_cost_outpacing_price`), the cost-stack disclosure, and a like-for-like measurement in the company's own audited pair (cost per unit of revenue 54.059% in FY2022/23 to 61.613% in FY2024/25, rising, which is the direction the forecast carries forward). The mechanism and the filings agree. |
| 22 | 3y historical + 5y forecast, full FCFF waterfall shown inline to PV of FCFF | **PASS** | Appendix A.1 carries FY2022/23, FY2023/24, FY2024/25 actual, FY2025/26E and FY2026/27–FY2030/31 forecast; A.2 carries four balance-sheet dates; A.3 the forecast balance-sheet and cash-flow markers; A.4 years three to five as ranges from the walk-forward's own error distribution. Tables 4 and 5 in §1.1 show the waterfall inline and **do not stop at FCFF**: Revenue → EBITDA → less D&A → EBIT → NOPAT → add back D&A → less capital expenditure → less change in working capital → **free cash flow to the firm** → discount rate from the glide → **discount factor** → **present value of free cash flow**, then the terminal block and the enterprise-to-equity bridge. Re-added by hand off the printed page: 3,039 + 891 − 2,729 + 788 = 1,989 ✓ in FY2026/27 and correct in all five years; PV row 1,581 / 512 / 459 / 521 / 764 sums to 3,837, the figure the bridge then uses. Signed-cash-effect convention throughout, and `scripts/check_sign_convention.py` is clean book-wide with nothing on its ratchet. |
| 23 | WACC built under the v2 cost-of-capital method | **PASS, with one construction disclosed and priced** | Table 17: local ten-year government yield **23.00%** (market quote 6-Aug-2026, corroborated by a listed treasury coupon of 23.10%), **less the sovereign's own default spread** — 6.37% on the rating basis, 3.41% on the market/CDS basis — giving normalised risk-free rates of 16.63% and 19.59%, "Built, not quoted". Country risk therefore enters **once**. **Both ERP bases are published** (13.94% rating / 9.41% CDS, each mature-market 4.23% plus the Egypt country premium) and the study states which is central and why, and that neither is mixed with the other's risk-free rate. Ke 30.99% / 29.28%; **market-value weights, 66.2% / 33.8%, never book** (market capitalisation EGP 28,627mn); WACC 26.88% / 25.76%. **Marginal Kd above the sovereign:** the blended pre-tax cost of debt is **24.34%** against the 23.00% sovereign — above it, unlike the AMOC precedent. The 19.40% local facility does sit below the sovereign; the book is 99.7% dollar so the rule's all-local-currency condition does not bite, and the study **discloses the exception in §1.8 and prices the floored alternative** (EGP 2.80 against 3.90). **Named gap:** `scripts/check_cost_of_capital.py` reports EGCH "carries no cost-of-capital schedule record" — outstanding on [R-COC-01]'s ratchet, so the glide is not machine-checkable from outside. |
| 24 | The terminal, rebuilt through the sanctioned module | **PASS** | `compute.py:37` imports `terminal_value as TV` and line 600 calls `TV.build(TV.TerminalInputs(...))`; the retired reinvestment identity is kept in two lines beside it, labelled retired, and does not produce the answer. The committed `terminal_record`: rule R-TERM-01, real growth **0.0** with nominal 7.0% **derived** from the house path, `maintenance_basis: book_dna_escalated`, `average_age_years: 4.4544` with `maintenance_age_basis: "measured"` and a source naming note 6 of the audited FY2024/25 statements read by OCR, `useful_life_years: 22.0702` with its own note-6 source, growth capex 0.0 (real growth is zero), TV 28,867 against a labelled NOPAT-perpetuity floor of 21,193 (+36.2%), **implied payout of terminal NOPAT 79.4%**, inside [0,1]. Arithmetic re-derived by hand: escalator 1.07^4.4544 = 1.3517 ✓; maintenance 1,703.219 × 1.3517 = 2,302.28 ✓; FCFF 3,939.29 + 1,703.22 − 2,302.28 − 213.92 = 3,126.31 ✓; TV 3,126.31 × 1.07 / (0.18588 − 0.07) = 28,867 ✓. `implied_cycle_years: null` — this terminal does **not** carry the 1/g construction, and `scripts/check_terminal_floor.py` from the repo root confirms it, listing EGCH under "built on something else" and recording it as **CLEARED since the list was written**. |
| 25 | Macro coherence — one house path, every inflation-class input declared | **FAIL, on one field, and the gate cannot see it** | `scripts/check_macro_coherence.py` passes EGCH (one of 8 conforming), and the parts it reads are genuinely right: the `cpi_path` block declares `mapping: "fiscal_june"` and reproduces the house ladder onto a 30-June year to the basis point (16/12 → 0.140, 12/9 → 0.105, 9/7.5 → 0.0825, 7.5/7 → 0.0725, beyond the ladder → the house terminal 0.070); the currency path is derived from that ladder by relative purchasing-power parity and is not hand-set; terminal growth 7.0% = terminal inflation 7.0% + a stated real 0.0. **The defect is `terminal.rf`.** The committed record declares **0.1250** — exactly `macro_path.load('EG').terminal_rf`, which is additive (0.07 + 0.055) — while the model computes **`rf_star_terminal = (1+0.07)(1+0.055) − 1 = 0.12885`** at `compute.py:205` and 220, and the delivered §1.8 prints that number in words: "7.0% inflation … **compounded** with a 5.5% real rate gives a normalised risk-free rate of **12.88%**". The record therefore states a terminal risk-free rate the model does not use and the document contradicts; the gate reads the record and passes. It is not a rounding matter — `MACRO_TOLERANCE` is 0.0005 and the gap is 0.00385, so a record stating the model's own figure would go **red**. Measured in a sandbox: rebuilding on the house additive convention moves the central **3.8967 → 4.0396 (+3.7%)** and the alternative branch **7.8807 → 8.0388 (+2.0%)**. ARCC, the worked case for this rule, builds `_RF_TERM = _PI_T + _MACRO.real_rate_convention` — additive — so EGCH is alone in the book on this. Direction is conservative, which is not a defence. |
| 26 | Enterprise-to-equity bridge as a record | **PASS** | `scripts/check_bridge.py` from the repo root: EGCH conforming — "stands on the 2026-03-31 sheet; minority on the none_disclosed basis". The committed `bridge_record` names the reviewed nine-month statements to 31 March 2026 (limited review 20 May 2026) as the **latest disclosed** sheet and `sweep_register.json` as the register establishing it, with the reason no later filing exists. Cash is `added_at_face` on `gross` weights with `cash_charged_once: true` and the note saying which construction was chosen. No non-controlling interest exists (evidenced from the face of the balance sheet and the equity note). No dividend was declared after the balance-sheet date. The bridge foots on the printed page: 3,837 + 10,398 = 14,236; − 10,032 + 1,383 + 2,155 = 7,741; ÷ 1,986.579mn = 3.90 ✓, and again on the stopped branch to 7.88 ✓. |
| 27 | Lens design — one class primary, the rest as cross-checks | **PASS** | `scripts/check_lens_design.py`: EGCH conforming, "primary dcf, 2 cross-checks". `lens_record` carries the DCF as the primary with a range basis in an observable unit (the dollar export price, US$530 to US$560 f.o.b. Egypt, both levels the market has printed, macro held still across both) and a note that the primary is two-sided and never averaged. The relative multiple is **non-circular** — forward EBITDA times a multiple from the company's own history and regional peers, with the circularity inputs recorded so a reader can check it was not read off the price. Book value is published as a **disclosed floor and never weighted**. The retired 45/20/20/15 blend is recorded as retired, published unused so a reader of the previous edition can see what changed, and reaches the central nowhere. |
| 28 | Every non-sourced driver logged to the Driver Ledger | **FAIL** | `engine/Fundamental_Driver_Ledger.md:447` carries an EGCH section, and it describes the **01-09-2026 edition**, not this one. It states "Central EGP 3.76 (field 0.00–15.47) against a 13.98 close, audited under `GAP_REVIEW_01-09-2026.md`" against a delivered 3.8967 / 7.8807 against 14.41 audited under `GAP_REVIEW_05-09-2026.md`. Item 6 states "The terminal inflation equals the terminal growth (**5%/5%**…)" against a delivered 7%/7%. Its L5 table names `real_rate_lt` at **3.5%** building "(1+5%)(1+3.5%)−1 = 8.67%" against a delivered 5.5% and 12.885%; `fx_terminal_wedge` at 2.54% against a delivered 4.5%; `kd_usd_lt` "carried at … = 11.77%" against a delivered 13.90%; item 9's Kd path "19.99% in year one gliding to 14.54%" against a delivered 24.35% gliding to 13.90%. Most consequentially it names **`roc_terminal` at 18% as "The largest unsourced input in the study", "Sets terminal reinvestment (g / ROIC = 27.8% of NOPAT)"** — a driver [R-TERM-01] removed from the terminal in this very edition, as the study's own Table 18 says in terms ("THIS ROW REPLACED THE TERMINAL REINVESTMENT ROW, which priced a return on capital that no longer enters the terminal at all"). Nothing has been added for this edition's new construction (`fa_avg_age_years`, `fa_life_implied_years`, `maintenance_basis`). The next petrochemical study reading this ledger would inherit a retired terminal. |
| 29 | The rebuild ledger — the route, not only the answer | **PASS** | `scripts/check_rebuild_ledger.py` from the repo root walks it: "EGCH 2 lever(s), 1 rule(s), cumulative +68.6% — R-TERM-01 +68.6% 2 lever(s)". `rebuild_ledger.json` records both levers in the order applied, each naming the rule it serves and carrying its before and after (2.3109 → 2.9877 → 3.8967), the audit point declared **in advance** with the reason the two are one pass, and a note that the alternative branch runs 6.2591 → 7.8807 and that both branches remain far below the price so nothing about the held status changes. It also records a **third correction priced and not taken** — the 4-September evidence that would have put lever one at 3.2905 by handing the sanctioned module figures already grown by (1+g), corrected before it was built [L-329]. That is the honest shape: a lever that never shipped belongs in the evidence file rather than in the route. |
| 30 | The answer against the price, and the gap review [R-GAP-01] | **PASS** | `scripts/check_valuation_gap.py` from the repo root: "EGCH **−73.0%** GAP_REVIEW_05-09-2026.md (audits 3.8967)", inside the OUTSIDE THE BAND, AND REVIEWED list; **OK — no new violations**. The study's committed central is EGP 3.8967 (carried through) and 7.8807 (stopped) against a spot of EGP 14.41 at 2026-09-03 — **−73.0%** and **−45.3%**, both more than 10% BELOW the price. `GAP_REVIEW_05-09-2026.md` exists in the study directory, is 247 lines, and covers **all eight headings** — LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK — and carries the AUDITED CENTRAL markers for both branches and an AUDITED GAP of −73.0%, so it cannot go stale silently against either. |
| 31 | The publication block [R-GAP-02] | **PASS — the study is correctly HELD** | `scripts/check_publish_block.py` from the repo root: "**HELD EGCH** every branch is more than 10% BELOW the price (highest Cash-flow lens, capital programme stopped at −45.3%) — and no market dissent is filed", listing both branches at 3.90/−73.0% and 7.88/−45.3%. A two-sided answer is held only if every branch breaches, and both do. No `MARKET_DISSENT` file exists and none is claimed. Phase 1 is separately unproven (2 of 6 acceptance criteria open), which holds the whole book. Nothing in this edition may reach the live site, and the study does not claim otherwise. |
| 32 | Artefact currency [R-ENF-06] | **PASS** | `scripts/check_artefact_currency.py` from the repo root lists "EGCH `contested_judgements.json` built against **3.8967**" among the 16 CURRENT artefacts and nothing for EGCH among the 7 undeclared — the stale-at-1.7854-against-2.3109 condition that this rule was extended to catch on this very study is closed. `diagnostics.json` carries `spot 14.41`, `as_of 2026-09-03` and the reverse read solved on both branches (13.0% carried through, 14.3% stopped), and it lives outside `study_numbers.json`; no builder reads it back in. `scripts/check_delivered_pdf_currency.py`: "every delivered PDF carries the answer its study publishes", with EGCH not among the exceptions. |
| 33 | Output records [R-ENF-05] — the reverse read and the sign test | **PASS on the reverse read, FAIL on the sign test** | The reverse read is published, and well: §1.1 Table 7 solves the flat nominal rate that reproduces the traded price on the study's own drivers — 13.0% carried through, 14.3% stopped, against a sovereign ten-year yield of 23.0% — and the headline and §4 both state it as the disagreement rather than asserting the market is wrong. **But** `scripts/check_output_records.py` from the repo root reports "conforming: 3 … still outstanding, allowed for now (21)" and EGCH is among the 21: no committed output record, therefore **no binomial sign test over the material contested judgements**. Ten judgements are priced in Table 18 and eight of the ten resolve downward, which is exactly the pattern the sign test exists to measure and report — flagged, never failed. It is on the ratchet, so this is a named debt rather than a new breach. |
| 34 | Table totals and instructing waterfalls | **PASS programmatically, FAIL on one table read as a reader** | `python3 footing_check.py`: "tables examined: 51; unreconciled totals: 0", and `scripts/check_table_footing.py` from the repo root reports the same. `scripts/check_waterfall_assertions.py`: EGCH **OK**, 6 waterfall tables, the builder importing `table_residual` and calling it. **The gate's own design is why it passes:** `docx_egch.py` imports `signed_column, waterfall` at line 12 and makes **exactly one** call, at line 230, on the FCFF column; the other five instructing tables are not asserted, and the gate deliberately does not re-derive their arithmetic. Read as a reader, five of the six foot and one does not — §1.4's Table 9, recorded at row 39 below. |
| 35 | The rendered PDFs read as images, page by page | **FAIL — and it is what caught the most** | All 34 pages of `EGCH_Valuation_Study_05-09-2026.pdf`, all 21 of the bibliography and four of the workbook PDF were rendered and read as images, not as extracted text. Nine defects were found this way and none of them is visible to any programmatic check in this repository; every one is recorded individually at rows 36–44 below. Every quoted sentence was then confirmed present in the delivered PDF's own text layer. |
| 36 | *(defect)* A headline claim typed rather than computed, and false | **FAIL** | Page 1, Headline, in bold: *"The multiple lens … reaches that answer only by never asking what the capital programme does to cash. **Every lens that does ask lands below EGP 4.29.**"* The study's own cash-flow lens with the programme **stopped** — the lens defined by asking exactly that question — reads **EGP 7.88**, and it is printed four lines above in the READ FIRST panel on the same page. The number 4.29 is computed (`LN['normalised']['value_per_share']`) so `prose_check.py` passes it; the **claim around it** is typed at `docx_egch.py:97` and nothing asserts it. This is the AMOC "above the best single quarter this company has ever filed" defect in another costume. |
| 37 | *(defect)* A superlative asserted twice and contradicted by its own table's caption | **FAIL** | §7, page 25: *"on this base that assumption is worth EGP 2.16 a share — **the largest of the priced alternatives**"* (`docx_egch.py:1186`, number computed, claim typed). Table 18's own row for the same alternative: *"This is the **largest single contested number in the study**"* (`alternatives.py:153`, carried into `alternatives.json`). Table 18's **computed** caption, on the same page: *"The two that move the answer most are gas (−2.67) and glide (−2.46)."* `alternatives.json` deltas, read directly: −2.67 gas, −2.46 glide, **−2.16 asset age**, then −1.67, −1.53, −1.47, −1.10, −0.91, +0.41, −0.21. The asset-age alternative is **third**. `compute.py:596` carries the same false comment. |
| 38 | *(defect)* Two different costs of equity for one company, the second unlabelled | **FAIL** | §6, Table 26, "Above the upper quartile": *"a business whose sustainable return on equity is 6.9% against a **cost of equity of 31.0%**"* — `docx_egch.py:1124` passes `WC['ke_rating']` = 30.986%. Every other place in the document uses the **adopted** CDS basis: §1.2 Table 8 "Cost of equity 29.28%", §1.5, §4 Table 24 "a cost of equity of 29.28%", and the summary table's own basis cell. §1.8 states in terms that the swap basis "is carried into the valuation" and that "neither is mixed with the other's risk-free rate". §6 quotes the basis the study declined, with no label saying so. |
| 39 | *(defect)* A body table whose printed rows do not reach its printed answer | **FAIL** | §1.4, Table 9 (`docx` table index 10). Printed: MID-CYCLE EBITDA 2,828 · Depreciation and amortisation −891 · **MID-CYCLE OPERATING PROFIT AFTER TAX 1,502**. A reader adding the column reaches **1,937**; the EGP **436mn** tax step is not printed. Two rows later: "At ten times — enterprise value 15,016" · **"Value per share at ten times (EGP) 4.29"**, with net debt (−10,032), non-operating assets (+3,538) and the share count (1,986.579mn) printed **nowhere** — a reader dividing the printed enterprise value by the share count reaches **7.56**. Appendix C's Table 32, which is the same build, **prints all three rows** ("Tax at the statutory rate −436", "Less net debt −10,032", "Plus non-operating assets 3,538"), so the omission is the body table's alone. No row label declares itself a total, so `table_footing` cannot see it; no operator word appears, so the waterfall detector does not classify it. |
| 40 | *(defect)* The Disclosure block names the wrong date and the wrong price | **FAIL** | Page 34: *"**Point in time.** It reflects information available on **1 September 2026** and the closing price of **2026-08-06**."* The masthead on page 1 reads "Valuation study — **5 September 2026** · Anchor price **EGP 14.41** at the close of **2026-09-03**", and the terminal rebuilt in this edition rests on evidence gathered on 4 September (`TERMINAL_EVIDENCE_04-09-2026.md`). `docx_egch.py:1543` types the information date and resolves the price date from `ST['anchor_date']` — the price-map anchor, not the valuation spot. The study's own §3 is careful to keep those two clocks apart; the Disclosure conflates them and tells a reader the study is struck against a price a month old. |
| 41 | *(defect)* The bibliography's masthead is four days stale — the identical defect the study builder fixed | **FAIL** | `EGCH_Bibliography_05-09-2026.docx/.pdf`, masthead: *"Egyptian Chemical Industries (KIMA), EGX: EGCH — **1 September 2026**"*, hardcoded at `docx_biblio.py:103`, on a file named for 5 September and shipped beside a study dated 5 September. `docx_egch.py:49-55` carries the fix and the reason for it in a comment — *"THE EDITION DATE WAS TYPED AND WENT STALE. This read '1 September 2026'…"* — and derives the date from the edition filename. The lesson was learned in one builder and left unapplied in the other beside it. |
| 42 | *(defect)* A delivered bibliography row describing a driver this edition retired | **FAIL** | Bibliography, "Negative results", row 3: *"Maintenance capital expenditure **is set at a mature-plant standard** and sensitised."* The study beside it, §7: *"It was set at three per cent of revenue on the assertion that the company's own observed spending was abnormally low; the cash-flow statements show two pre-project years at 1.1% of revenue, and **that is now the central**. The correction is worth EGP 0.34 a share on its own."* `maint_capex_pct_rev` = 0.011157. The bibliography states as current a construction the study states as retired. |
| 43 | *(defect)* Two register inputs cite a filing that does not carry them | **FAIL** | `input_register.json`: `bs_gross_fixed_M9FY2526` = 17,022.493 and `bs_acc_dep_M9FY2526` = 3,435.300, both sourced to *"Interim statements, nine months ended 31 March 2026, limited review dated 20 May 2026, note 6"*, dated 2026-05-20. Their values are identical to `fa_cost_gross_FY2425` (17,022.493238) and `fa_accum_dep_FY2425` (3,435.299807), both sourced to the **audited year to 30 June 2025**. The study's own arithmetic settles it: 17,022 − 3,435 = **13,587**, which A.2 prints as net fixed assets at **30 June 2025**; at 31 March 2026 A.2 prints **13,058**. The study's own `terminal_record` says so explicitly — *"cost less accumulated reproduces the balance-sheet net book value of 13,587.193"*. Both keys are consumed by `experts.py:21-22` and printed in Appendix C's Table 31, whose first three rows therefore carry 30-June-2025 figures under a "note 6" label in a table whose other rows are dated 31 March 2026. Nine months of depreciation on a base charging EGP 771mn a year cannot be zero. |
| 44 | *(defect)* The delivered workbook PDF is not readable at its own margins | **FAIL** | Rendered and read as images. Page 1 (READ FIRST): every long line is cut at the right edge — the title ends "VALUA", and the body runs off at "built product by pro", "sheets can", "would be un", "WHEN A DRIVER C". Page 2 (Summary): row labels truncated mid-word — "Discount rate implied by the traded p", "Sovereign ten-year yield the same da", "Cost of capital built in this model, ye", "RETIRED — the 45/20/20/15 weighte" — and the section heading ends "SIDE BY SIDE AND NEV". Page 20 (Assumptions): **an entire page of orphaned values with no label column at all** — 7.0%, 5.5%, 12.89%, 9.00%, 13.91%, 18.59%, 4.45, 22.07, 771.2, 119.4, 2,599, 3,100 — a reader cannot tell what any of them is. Page 30 (SOTP Bridge): the **"Programme stopped" column is missing entirely**, so the study's own two-sided answer loses one of its two sides in the workbook a reader receives. |
| 45 | *(defect)* The field floor the document states sits above a lens central the same document prints | **FAIL, small** | The summary table and Figure 1 both print "Book value and sustainable return · central **0.00**". The same table's answer row says the range shown *"is the floor and ceiling of every read"* and gives **3.90 to 15.99**; §1.5 repeats it. `lenses.json` shows why: the synthesis field uses the **disclosed book of 8.16** for that lens while the table and the figure use the **justified-multiple value of 0.00**, and §4's Table 24 uses 8.16 again ("Book value — the disclosed floor 8.16"). Three numbers under two names for one lens, with the stated floor sitting above one of them on the same page. |
| 46 | *(gap)* The sensitivity grid does not contain the study's own central | **PASS, with the gap named** | §1.9's crux is sensitised in real observable units — dollars a tonne and a terminal cost of capital, not percentage-point abstractions — which is what the item asks, and Figure 9 and Table 19 agree cell for cell. `sensitivity.py:20` builds each column as `[p+90, p+60, p+30, p+10, p]`, so the axis is the **last-year** price on a glide starting US$90 above it, while the base case holds the price **flat** at US$530. Verified by re-running the model: the grid's published cells reproduce exactly (500 → 3.36, 540 → 5.40 at 18.5%), the base reproduces at **3.8967** on a flat 530 and a terminal 18.588%, and the grid's own shape at p = 530 gives **4.8862**. A reader reading the axis as the study's own price driver lands about EGP 1.00 a share away from the published answer, and nothing on the page says the two are built differently. |
| 47 | *(gap)* Expert 3's published low is a literal, described as something else | **PASS, with the gap named** | C.3 states *"Range: EGP 0.00 to EGP 5.03 a share"* and *"the claim is worth EGP 4.87 a share (**EGP 0.00 to 5.03 across the volatility range**)"*. `experts.py:188` sets `low=0.0` as a hardcoded literal. Table 34, the expert's own grid, has a minimum of **3.74** anywhere and runs **4.71 to 5.12** across the volatility range at the published five-year maturity. The range does bracket its own base, which is the assertion the model report requires; the parenthetical attributing the 0.00 to the volatility range does not describe what produced it. |
| 48 | The walk-forward scope decision, stated in the study | **PASS** | `study_numbers.json → walkforward`: `ran: true`, `scope: "FULL"`, 13 origins, horizons 1–5, 55 cells, window "FY2008–FY2025, KIMA's own audited statements (18 fiscal years)". §7 states it to a reader in plain language, including the result that goes against the study — *"it did **NOT** forecast net profit better than assuming no change: on the profit line its average miss was 1.6 times the miss of carrying last year's figure forward, and it came in below the outturn in 73.9% of cases"* — and the consequence, which is A.4 publishing years three to five as ranges from the record's own error distribution rather than as points. `corrections_adopted: 0`, with the reason recorded. |
| 49 | The Sweep-Register question: **what do the filings disclose that the model does not consume?** | **FAIL — one material item** | Asked mechanically first: `prose_check.py` reports **269 of 304** register inputs consumed by a builder and names the **35** that are not. Most are legitimate — Q1 and H1 gross and net superseded by the 9M reviewed figures the model does use; `budget_net_9M`, the company's own budget, deliberately excluded because guidance is scored and never consumed; peer figures used only in the relative lens; context items. **One is not.** `gas_standard_m3_t` = **1,200 m³ per tonne of ammonia**, an **L1 disclosure** from the auditor's report on the reviewed Q1 interim, is registered and consumed by nothing. The model instead runs `gas_m3_per_t_ammonia` = **1,292**, constructed by setting gas at 75% of the single disclosed materials line and flagged as such — §7 calls it "the largest modelled allocation". The study prices the gas **price** alternative (EGP 2.67 a share) and does not price the disclosed **consumption** rate. Measured in a sandbox, holding every other driver: substituting the auditor's own 1,200 m³/t moves the central **EGP 3.8967 → 4.7285, +21.3%** — larger than eight of the ten alternatives Table 18 does price. A disclosed figure sits unused beside a constructed one, and the constructed one is worth a fifth of the answer. |
| 50 | The study calls the three gates in its own committed code | **PASS** | `assert_beta_provenance()` at `beta_record.py:30`, `compute.py:797` and `attest.py:101`; `assert_sigcm()` at `attest.py:97`; `assert_model_study()` at `attest.py:89`; plus `assert_ground_up()` at `compute.py:826`. `python3 attest.py` re-run end to end: all four pass. `python3 scripts/check_study_provenance.py` from the repo root does not list EGCH among the 11 outstanding, and no study-local regression script survives — the beta comes from `beta_regression.own_stock_beta()` through `beta_record.py`. |
| 51 | The study's committed gate outputs are current, not stale | **PASS** | Every gate in `run_all.py`'s verification tail was re-run **inside the study directory** — `recalc.py`, `formula_audit.py`, `cross_check.py`, `prose_check.py`, `driver_test.py`, `qc_checks.py`, `attest.py` — plus `footing_check.py`, `band_record.py`, `sweep.py`, `footing_audit.py` and `source_audit.py`. `git status --porcelain engine/egch_study` returns **empty** afterwards: every result file reproduced byte-for-byte. The three workbook-reading gates open `EGCH_Valuation_Model_05092026.xlsx` **by name**, which is the delivered edition — the L-066/L-067 defect (a gate still opening a superseded workbook) is not present here. |
| 52 | Driver test and the dead-input sweep | **PASS** | `python3 driver_test.py` on the delivered workbook: baseline EGP 3.8967, **17 of 17 drivers move the headline in the asserted direction**, including the two this edition introduced (`Average age of the fixed-asset base — MEASURED` 1.20× → 3.657, down; `Project utilisation in the terminal year` 1.40× → 4.311, up). Dead-input sweep: **111 drivers swept, 0 dead**, with **4 cross-checks exempted by name rather than silently** ('Ammonia design capacity', 'Replacement cycle the accounts imply', 'Ammonia per tonne of nitrate', 'Cross-check: nameplate derived from the ammonia surplus'). |
| 53 | Balance-sheet footing | **FAIL, one period, EGP 0.055mn** | `python3 footing_audit.py` (in the directory, **not** in `run_all.py`'s pipeline) exits 1: "FY2223 assets 18,727.840 claims 18,727.785 **gap +0.055** ← DOES NOT FOOT"; FY2324 −0.001, FY2425 −0.000, M9FY2526 +0.010 all inside rounding. Pre-existing; named here rather than rediscovered. The A.2 balance sheet printed to the nearest million is unaffected in substance (18,728 against components summing to 18,727, one unit of printed rounding across eight rows) and `table_footing` passes it correctly. |

---

## What blocks delivery

Eight of these are reader-facing and were found only by reading the rendered documents as
images. None is a modelling error; all eight reach a reader.

1. **Row 36** — "Every lens that does ask lands below EGP 4.29", in the bold headline, false
   against the study's own EGP 7.88 printed four lines above it.
2. **Row 37** — "the largest of the priced alternatives" / "the largest single contested number
   in the study", asserted twice, contradicted by the same table's own computed caption and by
   `alternatives.json` (−2.67, −2.46, then −2.16).
3. **Row 38** — a cost of equity of 31.0% in §6 against the adopted 29.28% everywhere else, on
   the basis the study says it declined and says it never mixes.
4. **Row 39** — §1.4's Table 9: a reader following the printed rows reaches 1,937 against a
   printed 1,502, and 7.56 a share against a printed 4.29. Appendix C prints the missing rows.
5. **Row 40** — the Disclosure block dates the study 1 September and its price 6 August, against
   a masthead of 5 September and EGP 14.41 at 3 September.
6. **Row 41** — the bibliography masthead reads 1 September 2026 on a 5 September file: the
   exact defect the study builder fixed and recorded, left open in the builder beside it.
7. **Row 43** — two register inputs cite the 31-March-2026 interim and carry the 30-June-2025
   values, proved by the study's own A.2 balance sheet, and both are printed in Appendix C.
8. **Row 44** — the delivered workbook PDF truncates labels mid-word, orphans a whole page of
   values from their labels, and drops the "Programme stopped" column from the bridge sheet.

Three more are not reader-facing but are defects of record, and two of them would mislead the
next study rather than this one's reader:

9. **Row 25** — the committed macro record declares a terminal risk-free rate of 12.50% while
   the model runs 12.885% and the document prints 12.88%. Worth +3.7% on the central. The
   gate reads the record, so it passes; a record stating the model's own figure would go red.
10. **Row 28** — the Driver Ledger entry is a full edition stale and names a retired terminal
    construction as "the largest unsourced input in the study".
11. **Row 49** — the auditor's own disclosed gas consumption of 1,200 m³ per tonne of ammonia
    is registered, unused, and unpriced, while the model runs a constructed 1,292. Worth
    +21.3% on the central.

**Not blocking, and correct as they stand:** the study is HELD under [R-GAP-02] with both
branches more than 10% below the price and no dissent filed (row 31); its eight-heading gap
review is complete and marked with both audited centrals and the audited gap (row 30); the
workbook reproduces cell for cell and both Word documents reproduce row for row from a clean
rebuild (rows 5 and 6); the beta is conforming on its record against the published index of
the exchange the share is listed on (row 17); the terminal is built through the sanctioned
module on a measured asset age (row 24); and every one of the study's committed gate outputs
was reproduced byte-for-byte when re-run (row 51).

---

## What could not be verified, stated rather than implied

- **The OCR route on the filings.** Both the FY2024/25 annual (48 pages) and the 9M interim
  (37 pages) carry a zero-character text layer, which corroborates the study's claim that
  every figure was read off the rendered pixels. The filings are in Arabic and a
  reconstruction of the study's OCR pipeline from this environment did not recover note 6's
  figures cleanly, so the extraction itself was **not** independently re-read. Row 43's
  finding does not depend on it: it is proved by the study's own printed balance sheet.
- **SIGCM clause 1 from outside.** `scripts/check_source_integrity.py` cannot read this
  study's register at all, so the official-source-only clause is evidenced here from the
  register's own contents and from the filings on disk, not by an outside instrument.
- **The five SIGCM clauses that rest on a blanket boolean** (3, 4, 5, 7, 8) are evidenced in
  row 18 from the delivered documents, which is the strongest available substitute; the
  attestation itself proves nothing about them.
- **`fig8_price.png`** is built and delivered nowhere, so its content was checked as a file
  but has no rendered context to be judged in.
