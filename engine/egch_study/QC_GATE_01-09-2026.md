# EGCH — QC gate, 01-Sep-2026 (independent audit, THIRD PASS)

Egyptian Chemical Industries "KIMA" · Egyptian Exchange: EGCH · edition dated 1 September 2026,
rebuilt inside the fundamental walk-forward campaign [R-FCAL-01] (run directory
`engine/egch_walkforward/`, FULL scope, 13 origins FY2012–FY2024, h=1–5, 55 cells).
Audited at commit `38f124f` ("print the weighted central, read the bibliography from the
register"), working tree clean (`git status --short` → 0 lines).

Filled from outside the study by an auditor who did not build it. Every row names the artefact,
the command or the number that carries it. The study's own JSON outputs were NOT trusted: the
whole pipeline (`run_all.py`, 20 stages from `beta_record.py` to `attest.py`, plus `sweep.py`)
was re-run in a scratch mirror (engine symlinked, the study copied) and every output diffed
against the delivered files; the rendered PDFs were read as images on every page the second
pass failed (READ FIRST, the valuation summary, §1.5, §1.8, §3, bibliography page 2 and the
register page carrying `lens_weights`), and all fifteen figures were opened as images. The
study directory was not edited; this file is the only thing written.

**VERDICT: DELIVERABLE.** Both second-pass blockers are cleared and re-verified from the
artefacts: the weighted central EGP 3.76 now prints in READ FIRST (page 1), the valuation
summary's field row (page 2: range 0.00 to 15.47 · central 3.76 · −73% vs spot), §1.5 (page 8)
and the Summary sheet (`B38 = B34*B5+B35*B7+B36*B8+B37*B6`, recalculating to 3.7590), with the
weights stated in each place and the register row corrected; the bibliography's market table
reads the register exclusively and prints 9.41% (CDS) — `live_data.json` is read by no stage of
the pipeline and `erp_cds` is deleted. Nothing found on this pass blocks delivery. Seven gaps
are named below (rows 1, 2, 3, S16, S21 and the closing list) — the largest is a legibility
defect in the bibliography's full register, where four tonnage and price paths render in
scientific notation ("4e+04, 4.1e+04 …"). None moves a number.

---

## What each pass found and what changed

| Item | First pass (`607899d`) | Second pass (`56098e0`) | Third pass — verified at `38f124f` |
|---|---|---|---|
| Numeric traceability, §1.8 Table 17 | **FAIL:** `docx_egch.py` loaded `wacc_result.json` beside `study_numbers.json`; Table 17 printed 12.97% / 3.55% / 19.45% / 23.57% that the workbook and bibliography contradicted. | Cleared: `wacc.py` and `wacc_result.json` deleted; cost of capital built once through `wacc_builder.build_wacc()`. | **Holds.** Table 17 (page 14, read as image): Ke 30.99% / 29.28%, Kd after tax 15.49% / 15.49%, weights 65.5 / 34.5, WACC 25.64% / 24.52% = `study_numbers.json['wacc']` (0.30986, 0.29284, 0.15491, 0.65483, 0.25638, 0.24523). `docx_egch.py` `json.load` set unchanged from the second pass; no `wacc_result` anywhere in the directory. |
| Seven typed contradictions in the rendered PDF | **FAIL:** "above one hundred per cent", "negative before flooring", "book lens sits at zero", "38.9%", "16.73%" Kd, the 12.97 block, C.6 "whole study … 1.49". | All seven cleared as rendered images; `prose_check.py` introduced (396 figures, 0 unmatched). | **Holds.** Delivered-text greps this pass: "above one hundred" 0, "negative before flooring" 0, "sits at zero" 0, "38.9" 0, "12.97" 0, "19.45" 0, "23.57" 0. `prose_check.py` re-run in the mirror: **367 prose figures checked, 0 unmatched** (the count fell from 396 because the printed input-register table is now skipped as the source of truth rather than checked against itself). |
| External-reader scrub | **FAIL:** engine names in a bibliography entry; the retired skill figure and a break-filtered 17-window sample in §3. | Cleared: `beta_ci90_low` reworded; §3 carries the 57-window band record; `qc_checks.py` widened to 68 patterns. | **Holds.** Mirror re-run: 68 patterns, 0 hits in both documents; `band_record.assert_no_verdict_tokens()` on the full extracted text of both PDFs: clean; "skill" 0, "CRPS" 0, "PASS/FAIL/PARITY" 0, "gate" 0, "ring" 0 in the delivered text. "register" appears only as the share register, the model report's own section names (risk register, research register) and the bibliography's title. |
| v2 cost of debt (S15) | **FAIL:** 14.55% blended Kd below the 23.00% sovereign and the 16.63% rf*; `build_wacc()` bypassed; nothing disclosed or priced. | Cleared: FX leg built year by year on the study's own wedge path; builder warnings printed verbatim; sovereign floor priced (−1.06 vs −0.63). | **Holds, and the second pass's gap on it is closed.** The builder's two warnings are kept as a record (`wacc.builder_warnings`, two strings) and are no longer printed — "looks like a raw FX coupon" 0 hits, "not grossed up" 0 hits in the delivered text. §1.8 pages 14–15 (read as image) state both facts in the study's own words: the 19.40% local facility below the 23.00% sovereign; the dollar leg's local-equivalent cost below the 16.63% rf* in FY2028/29 (15.63%), FY2029/30 (14.54%), FY2030/31 (14.54%) "because the currency wedge in those years (3.5%, 2.5%, 2.5%) is smaller than the gap between the 11.7% coupon and that rate" — the sentence is computed in `compute.py` (`_fx_leg_sentence`) from `kd_fx_year(k) < rf_star_rating`. The floored construction is priced in the same paragraph: −1.06 against −0.63, a difference of −0.44, matching `alternatives.json['cost_of_debt_floor']` (−1.062) and `Sensitivity!C20/D20`. |
| Driver Ledger (S16) | **FAIL:** eight constructed terminal-rate and ANNA drivers unlogged. | Cleared: `Fundamental_Driver_Ledger.md` lines 447–560. | **Holds.** Ledger line 447 heading; lines 537–544 tabulate `roc_terminal`, `kd_usd_lt`, `real_rate_lt`, `anna_cash_margin`, `anna_util_base/bull`, `dep_escalation`, `fx_terminal_wedge`, `anna_winddown_cost`, each with what it does and what would overturn it. One nit named under S16. |
| First-pass gaps (bibliography page 1 contradiction; Expert 1's range; Expert 3 on both sides; escalator claim; `sweep_date`; budget column; stale artefacts; `recalc_ok` unused; EGCH in `outstanding.json`) | Named. | All closed and re-verified. | **Hold.** `sweep_register.json` `sweep_date` 2026-09-01 (re-run in the mirror, byte-identical); `experts.json['ranges']['e1']` 3.82–8.52 = Figure D1 (read as image); EGCH absent from `outstanding.json` and `gap_outstanding.json` (grep count 0 in both); `attest.py` binds on `recalc_ok`; `figures.py` no longer loads `backtest_5y.json` (removed in this commit's diff). |
| **Second-pass blocker 1:** the published central EGP 3.76 appeared in no delivered document; the `lens_weights` register row claimed the 08-08 edition published these weights. | — | **FAIL.** | **Cleared.** `\b3\.76\b`: 3 hits in the study (pages 1, 2, 8), "weighted central": 3 hits. Page 1 READ FIRST (image): "Four lenses give a field, and inside it one weighted central — EGP 3.76 a share on stated weights: cash flow with the programme carried through 45%, relative multiples 20%, normalised earnings 20%, book value 15%. The central is a reading inside the field, never a point target." Page 2 field row (image): "0.00 to 15.47 · 3.76 · −73%" with the weights and the carried-through-only rule in the basis cell. Page 8 §1.5 (image): "one weighted central, EGP 3.76 a share, 73% below the traded price … the stopped reading (EGP 3.43) is published beside it as the contested judgement rather than blended in." Workbook `Summary!A33:B40` (new): weights 0.45/0.20/0.20/0.15 as inputs, `B38 = B34*B5+B35*B7+B36*B8+B37*B6` (expect 3.7590), `B39 = B38/$B$14-1` (expect −0.7311); the workbook PDF page 2 prints 3.76 and −73.1%. Arithmetic: 0.45 × (−0.6254) + 0.20 × 15.4744 + 0.20 × 4.2896 + 0.15 × 0.5842 = 3.759 = `lenses.json['central']['base']` = `data.js` `fair.base` 3.76. Register row (bibliography page 17, image): "SET IN THIS EDITION (1 September 2026): the 08-08-2026 edition published the field alone, with no central and no weights", dated 2026-09-01, with the basis for each weight stated. |
| **Second-pass blocker 2:** bibliography page 2 printed "9.42% (CDS)" from `live_data.json` while the register, Table 17 and the workbook carry 9.41%; `erp_cds` (0.09425) registered and consumed by nothing. | — | **FAIL.** | **Cleared.** `docx_biblio.py` no longer opens `live_data.json` (the `LIVE =` line is deleted in the diff); every cell of the market table now reads `V(...)` — `spot_price`, `rf_observed`, `sovereign_bond_coupon`, `moodys_rating`, `sov_spread_rating`, `country_risk_premium_rating`, `erp_rating`, `erp_cds_damodaran`, `usd_egp_spot`, `urea_fob_egypt`, `cpi_latest`, `policy_rate`. Page 2 (image): "13.94% (rating) / 9.41% (CDS)", country premium 9.71% (= 13.94% − 4.23%, the new DERIVED register input `country_risk_premium_rating` 0.09708), policy rate "19.50% main operation rate, mid-corridor". `erp_cds` removed from `inputs.py` and `input_register.json` (diff); "9.42" has 0 hits in the study and exactly 1 in the bibliography — the `erp_cds_damodaran` row's own correction note on page 14 ("the study had 9.4247%"), which is provenance, not a value. `grep live_data engine/egch_study/*.py`: no stage of `run_all.py` reads it (see the gap in row 3). |
| Second-pass non-blocking gaps: builder warning wording; width ratio and record strength absent from §3; "11.79%" named as the dollar leg; seven string-literal cells in the bibliography's market table; `figures.py` loading `backtest_5y.json`. | — | Named. | **All five closed.** Warnings: above. §3 page 21 (image): "By the count ladder this house uses it is a long record, and the ninety-per-cent band ran 1.45 times the width of a naive band anchored on the carry alone — a width disclosed beside the record and never used as a threshold" — `band_record.json` now carries `width` 1.446 and `strength` "long", read from `data.js` through node and asserted `> 0`. §1.8 page 14 (image): "11.77% on the dollar leg (11.79% once the 0.3% local facility is blended in)" — new `drivers.kd_fx_terminal` 0.117676 = 1.09 × 1.0254 − 1 ✓, `kd_local_equiv_terminal` 0.117915 the blend ✓. Market-table literals: all computed from the register (above). `backtest_5y.json`: no longer loaded. |

---

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker, exchange, market code | EGCH · **EGX** · EG | `assets/data.js` line 502: `code: "EGX:EGCH"` — read from the code prefix, not inferred from `raw_ohlc/EG/`. |
| Reporting and valuation currency | EGP | Every statement in `filings/` is in Egyptian pounds; masthead page 1 "Reporting and valuation currency: Egyptian pounds". |
| Anchor price and date | EGP 13.98, close 2026-08-06 | `study_numbers.json` `spot` 13.98 / `spot_date` 2026-08-06; `strike_result.json` anchor 2026-08-06; `data.js` spot 13.98 / "close 6 Aug 2026". |
| Regressor for the beta | **EGX30**, `engine/raw_indices/EG/EGX30.csv`, as of 2026-07-22 | `beta_result.json` `index_file: "raw_indices/EG/EGX30.csv"`, `index_asof: "2026-07-22"`, `interim_note: null`, `conforming: true`; resolved by `beta_regression.own_stock_beta("EGCH","EG","EGX")` in `beta_record.py`. |
| Price history | 3,755 clean sessions, 240.8 sessions/yr | `step0_result.json`; one vendor-fill row dropped (2011-08-03), recorded in `step0_result.json` and `beta_result.json` `stock_dq`. |
| Company class | Gas-fed nitrogen-fertiliser producer — registered class **petrochemical**; single-site operating company | `lessons_register.CLASSES` holds no fertiliser class; the run and the Driver Ledger (line 447) register it as petrochemical. One operating segment, no lending book, no NAV business. |
| Reference pattern | **ADNOCLS** (operating company) | `research_protocol.REFERENCE_SET` = ('ADNOCLS', 'ADCB', 'ALPHADHABI'), read live; FCFF-DCF with a volume × price driver tree, cross-checked on book, relative and normalised lenses — the ADNOCLS shape. |
| Lessons read before starting | `python3 engine/lessons.py EGCH --class petrochemical` → **61 lessons**: 58 ALL + L-109 (CLASS) + L-206/L-207 (STOCK) | Read, not cited as authority — fundamental lessons are PROVISIONAL. L-048 (one macro path) is what the year-by-year Kd wedge honours; L-054 (cash charged once) and L-055 (terminal inflation = terminal growth) both met (S15). |
| Depth standards | Eight, read live | `research_protocol.MODEL_STUDY_DEPTH` printed from import this pass: bibliography_document · provenance_four_field · numeric_traceability · external_reader_scrub · figure_discipline · table_discipline · expert_appendix_max_detail · contested_judgement_both_ways. |

---

## The three gates, run from outside

| Command | Result |
|---|---|
| `python3 scripts/check_study_provenance.py` | "study directories: 24 · registered indices: 7 … OK — no new violations." EGCH appears on no line of the output; the only "NOW PASSING" entry is FERTIGLOBE, someone else's. No study-local regression script survives in `engine/egch_study/` (`beta_record.py` calls `own_stock_beta()`). |
| `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` | `2026.09.01`. Stamped identically in `study_numbers.json['standard_version']`, `study_numbers.json['gates']['ground_up']['standard_version']`, `attestation.json`; `attest.py` line 104 asserts the equality. |
| Gates called in the study's own committed code | `compute.py`: `assert_beta_provenance(BETA_REC)` and `assert_ground_up(DRIVER_LINES, ticker='EGCH')`; `beta_record.py`: `assert_beta_provenance(rec)`; `attest.py` lines 94–101: `assert_model_study(c)`, `assert_sigcm(s)`, `assert_beta_provenance(BETA)`. Entry point `run_all.py` re-run in the mirror: exit 0, "all stages completed"; log lines 261/272/273: "assert_model_study PASSED — every standard attested from evidence", "assert_sigcm PASSED", "assert_beta_provenance PASSED — 1.0302 vs raw_indices/EG/EGX30.csv (conforming=True)". |
| `python3 scripts/check_valuation_gap.py` | "BELOW THE LINE, AND REVIEWED (1): EGCH −73.1% GAP_REVIEW_01-09-2026.md · OK — no new violations." EGCH is in neither `gap_outstanding.json` list (grep count 0). |
| `python3 scripts/check_lessons_register.py` · `python3 engine/fv_movement.py check` · `python3 scripts/check_protocol_sync.py` · `python3 scripts/check_band_vocabulary.py` | "lessons register OK" (provisional 31 · adopted 51 · watch 0 · outstanding 2 — the two outstanding are not EGCH's; L-057/L-058/L-206/L-207 resolve) · "[ok] every run in the queue has a frozen baseline and a recorded fair value" · "OK — the two governing documents carry the same tagged rules" · "[R-CAL-02] OK — no verdict vocabulary in any page text; every published band record agrees with its panel". |

---

## The gate — model report and the eight depth standards

| # | Item | Verdict | Evidence |
|---|---|---|---|
| (a) | Structure, content, format and depth match the model report | **PASS** | `attest.py` in the mirror: "51 headings, 0 model-study sections missing"; `wb.sheetnames == MODEL_STUDY['excel_sheets']` — "16 sheets in the model order, 0 mismatches". Standalone bibliography ships (20 pages). PDFs: study 33 pages, bibliography 20, workbook 59. The summary table now carries the model report's weighted-central row (page 2, read as image) — the one structural element the second pass found absent. |
| 1 | Standalone bibliography document | **PASS, with one gap named** | 13 tables (`python-docx` count): primary documents (8 rows with report date, auditor, retrieval date, what it sources); market/sovereign/macro (12 rows, every value now read from the register through `V()`); external context (7); cross-checks (3); access notes (7 attempts, 3 NOT REACHED with reasons); the FULL input register — **297 inputs = 188 L1 + 14 L2 + 11 L3 + 21 L4 + 63 L5**, each with value / unit / date / source-and-construction; judgements (7, each with what-would-overturn-it); negative results (6); aggregator discrepancies (3). Page 2 read as image: 9.41% (CDS). **Gap (found reading page 17 as an image):** four register rows print their value in scientific notation — `an_path` "2.6e+04 …", `local_free_path` "4e+04, 4.1e+04, 4.2e+04, 4.3e+04, 4.4e+04", `subsidised_p_path` "… 1.011e+04, 1.092e+04", `subsidised_t_path` "1.55e+05 … 1.75e+05" — 17 such tokens across the register pages, from `docx_biblio.py` line 267 `f"{x:,.4g}"`, which switches to exponent form at five significant digits. The values are correct (they match `input_register.json`); the rendering is not what an external reader should be handed for a tonnage path. Non-blocking; fix the formatter in the next pass. |
| 2 | Four-field provenance on every input, validated by assertion | **PASS, with one gap named** | `inputs.py` in the mirror: "input register: 297 inputs, all four-field complete", asserted through `ModelStudyChecklist.provenance_four_field` in `attest.py`; About page and register total both print 297 (2 hits in the study, 1 in the bibliography). The second pass's `erp_cds` orphan is gone (deleted from `inputs.py` and `input_register.json`; 296 + 1 new DERIVED `country_risk_premium_rating` = 297). **Gap:** `prose_check.py` now measures consumption and reports **263 of 297 register inputs consumed by a builder; 34 consumed by none**, listed in `prose_check_result.json['not_consumed']` — quarterly income-statement lines (`is_gross_Q1`, `is_net_H1` …), the budget column (`budget_net_9M`), Abu Qir comparables, design capacities, the 2021 quota split, `gas_standard_m3_t`, `unitcost_urea_FY2324`. These are record inputs (scored, cross-checked or cited in prose) rather than drivers, and each is legitimately in a research register; but the register does not mark them as record-only, so a reader cannot tell a registered driver from a registered fact (L-018). Name the layer or a `role` field next edition. |
| 3 | Numeric traceability — builders read the committed numbers file exclusively; independent recalculation | **PASS, with one housekeeping gap named** | Mirror re-run: `recalc.py` on the delivered workbook → **628 of 628 formula cells reproduce the model, 0 mismatches, 0 unresolvable, 0 unchecked, 0 orphans, 16/16 headline reconciliations** (`recalc.py` line 4: "Anything the evaluator cannot parse is a FAILURE, never a skip"; exit 1 on any unresolvable); `formula_audit.py` "every formula is built from cell references and structural constants only"; `cross_check.py` "the model, the delivered workbook and the delivered study state the same 16 headline figures"; `qc_checks.py` 4 builders, 0 typed financial numerals; `prose_check.py` 367 figures, 0 unmatched. The four delivered-document builders (`docx_egch.py`, `docx_biblio.py`, `build_xlsx.py`, `figures.py`) open only pipeline outputs and the register. **Gap:** `live_data.json` (31-Aug snapshot carrying the superseded 9.4247% figure) is still in the directory, and the prior edition's builder `build_xlsx_egch.py` line 546 still reads it — that builder is not in `run_all.py` and produces no 01-09 deliverable, so nothing delivered depends on it, but "no builder reads live_data.json" is true of the pipeline, not of the directory. Delete the file or the retired builder. |
| 4 | External-reader scrub — zero internal vocabulary, zero verdict tokens | **PASS** | Mirror re-run `qc_checks.py`: **68 patterns, 0 hits** in both documents. `band_record.assert_no_verdict_tokens()` on the full extracted text of both PDFs this pass: clean. No persona name: "Expert 1/2/3" are the only `Expert …` forms in the study (regex over the extracted text); "Damodaran" appears once, in the bibliography, as the source workbook's name. §3 carries the band record as prose with the count beside every percentage; no calibration appendix (appendices A, B, C only). The second pass's gap — the builder's own diagnostic wording pasted into §1.8 — is closed (row S15). |
| 5 | Figure discipline — solid light canvas, zero transparency, every figure read as an image | **PASS** | PIL over all 15 `fig*.png` this pass: alpha minimum 255 on every file; `qc_checks.py` "15 figures, 0 with transparency". All 15 byte-identical to the mirror rebuild. **All fifteen opened and read as images this pass:** Figure 1 (cases: 3.43 / 1.84 / −0.63 / −2.37 against 13.98), Figure 2 (bridge 1,740 + 3,512 = 5,252 − 10,032 + 3,538 = −1,242), Figure 3 (revenue stacks 11,310 … 11,654 = A.1), Figure 4 (FCFF 1,963 / 298 / −156 / −219 / 450 = Table 4), Figure 5 (heatmap, 25 cells legible, ink switches on dark cells), Figure 6 (cost per tonne, gas 7,079/t), Figure 7 (glide 25.6 → 18.2 with the 7.8% implied line), Figure 8 (five years of price), Figure 9 (field: five lens spans, market price), Figure 10 (fan, axis "Trading sessions — past to the left of zero, simulated to the right", P5 10.69 / P95 20.04 = Table 22), Figure 11 (levels 16.00/15.37/14.98 and 12.49/11.13/9.83), Figures 12–13 (distributions, P5/P95 and medians match Table 22), Figure 14 (27.8% spent / 12.9% physical / 37.0% planned; −0.3% return vs 18.2%), Figure D1 (expert ranges 3.82–8.52 / 2.78–5.80 / 0.00–1.96). Solid cream canvas on every one; no label collision; no retired session-counted unit in any title or axis; every printed number traced to the numbers file. Design nit only: Figure 9 does not mark the weighted central 3.76 inside the field — the central is stated in the text and table beside it. |
| 6 | Table discipline — explicit widths, starved/bloated/over-wide check | **PASS** | `qc_checks.py` re-run: **study 43 tables, bibliography 13 tables — 56 checked, 0 over-wide, 0 starved, 0 bloated** (counts confirmed independently by `python-docx`: 43 and 13). |
| 7 | Expert appendix at maximum detail | **PASS** | `experts.json` e1/e2/e3 each carry worldview, works, fails, a worked table with every intermediate line (Tables 31–33), a named sensitivity with numbers, a falsifier; C.4 four challenges each CONCEDED / REJECTED / CONCEDED in part; C.5 the three in one room with Figure D1 (read as image); C.6 divergence table. Labels "Expert 1/2/3" only. Expert 1's range 3.82–8.52 equals its sensitivity's end-points; Expert 3 publishes both sides of the contested judgement (1.78 carried through / 4.54 stopped). |
| 8 | The central contested judgement computed both ways, never averaged | **PASS** | "Is the ANNA capital programme carried through, or stopped?" (`lenses.json['contested']`): **−0.63 carried through / 3.43 stopped, gap EGP 4.05 a share (EGP 8,051m of equity)**. Side by side: summary table page 2 rows 1–2; Table 5 page 5 (EV 5,252 / 13,304, equity −1,242 / 6,809); workbook `Summary!B20='DCF'!B44`, `B21='DCF'!D44`, `B22=B21-B20`; Expert 3's two readings; Figure 9. `attest.py` asserts `side_a != side_b`. The second pass's residual — that the weighted central then leaned on one side without saying so — is closed: page 2, page 8 and `Summary!A40` all state that only the carried-through side enters the weighting, why (the company's stated plan), and that the stopped reading is published beside it and never averaged in. |
| (i) | Summary valuation table present, with the weighted central and its weights inline | **PASS** (second-pass blocker 1, cleared) | Page 2 read as image: row "THE FIELD, all four lenses — and the weighted central inside it" · basis cell names the four weights and the carried-through-only rule · range 0.00 to 15.47 · central **3.76** · vs spot **−73%**; market price row 13.98; the alternative-readings block beneath. The same central and weights in READ FIRST (page 1), §1.5 (page 8), `Summary!B34:B39` and the workbook PDF page 2. The register row `lens_weights` (bibliography page 17, image) now says the weights were SET IN THIS EDITION and that the 08-08 edition published no central. |
| (l2) | Every deliverable ships as a PDF | **PASS** | PyMuPDF page counts: study 33, bibliography 20, workbook 59 (was 56: the Summary sheet's new block spilled the print to three further pages); all three regenerated at `38f124f` (commit stat). |
| (l3) | **Rendered PDF read as images, not extracted text** | **PASS** | Study pages 1, 2, 8, 14, 15, 16, 20, 21 and bibliography pages 2, 14, 17 read at 100 dpi this pass, plus every figure. Arithmetic checked on the page: page 2 field row −73% = 3.76 / 13.98 − 1 ✓; page 8 "73% below" ✓; page 14 Table 17 WACC 25.64% = 0.6548 × 30.99% + 0.3452 × 15.49% ✓, "7.4% … 19.99%" = 1.117 × 1.0742 − 1 ✓, "11.77% on the dollar leg" = 1.09 × 1.0254 − 1 ✓, "(11.79% … blended)" = `kd_local_equiv_terminal` ✓; page 15 "−1.06 … against −0.63, a difference of −0.44" ✓ (−1.062 − (−0.625)); page 21 "1.45 times" = `width` 1.446 ✓, "long" = `strength` ✓; page 1 "12.9% built against a 37% plan" = register `anna_progress_sep2025` 0.129 / `anna_plan_sep2025` 0.37 (both L1, the Q1 FY2025/26 auditor's report) ✓, distinct from the 27.8% money-spent share on page 3 and Figure 14 ✓; bibliography page 2 country premium 9.71% = 13.94% − 4.23% ✓. Found on this read: the scientific-notation rendering in the register (row 1). Nothing else. |
| (n) | Graph text readable, no overwriting | **PASS** | As row 5: every figure opened; Figure 5's 25 cells, Figure 7's six point labels, Figure 10's five percentile labels and Figure 11's six level labels all clear of the series. |
| (p) | Terminal value as a share of enterprise value visible to the reader | **PASS** | 66.9% carried through / 33.0% stopped: Table 2 (page 3), Table 5 (page 5), §7; workbook `SOTP Bridge` and `DCF!B38/D38` as formulas. |
| (q) | The workbook calculates | **PASS** | 628 formula cells across 16 sheets (`recalc.py`), two more than the second pass (`Summary!B38`, `B39`), both in `xlsx_expected.json` with expected values 3.7590 / −0.7311; `formula_audit.py` clean; `Sensitivity!D15='DCF'!B44` with every alternative row differenced from it by formula (`C17:C23` values, `D17:D23 = Cn-$D$15`). |
| (r) | Every formula cell reproduces the model, and drivers propagate | **PASS** | `recalc.py` 628/628, 0 mismatches; `driver_test.py` on the delivered file: **17/17 drivers move the committed-capital headline in the asserted direction; 110 drivers swept, 3 cross-checks exempted by name, 0 dead inputs.** |
| (s) | Script-built files verified by cell-by-cell diff against the delivered file | **PASS** | Full pipeline rebuilt in the mirror (`run_all.py`, 20 stages, exit 0) plus `sweep.py`, and diffed: **17 numbers files byte-identical** (`study_numbers`, `lenses`, `alternatives`, `experts`, `sensitivity_grid`, `input_register`, `beta_result`, `band_record`, `flat_rate_ladder`, `xlsx_expected`, `xlsx_addresses`, `sweep_register`, `driver_test_result`, `qc_checks`, `recalc_result`, `prose_check_result`, `attestation`); **workbook 16 sheets, 1,867 populated cells compared, 0 differ**; study docx 1,810 text units, 0 differ, 14 inline images both; bibliography docx 1,786 text units, 0 differ; **all 15 figure PNGs byte-identical**. The delivered files are what the committed builders produce. |

---

## The gate — standing items

| # | Item | Verdict | Evidence |
|---|---|---|---|
| S1 | Step 0.0 data-quality gate | **PASS** | `step0_result.json` `dq_log`: "dropped 1 rows carrying a non-positive or non-finite OHLC value (2011-08-03)"; 3,755 clean rows at 240.8 sessions/yr against the EGX calendar; the same screen on both regression series (`beta_result.json` `stock_dq` identical, `index_dq: []`). |
| S2 | Step 0 calibration under the band record | **PASS** (the two second-pass gaps closed) | `data.js` BANDS.EGCH read through node this pass: **n 57, hits 54, c50 56.1%, c80 87.7%, c90 94.7%, strength long, flag null, width 1.446**. Two-sided binomial of 54/57 against 0.90: **p = 0.372** (`scipy.stats.binomtest`) — no flag earned, and §3 page 21 says so with the count printed beside every percentage, and now states the record strength ("a long record") and the width ratio ("1.45 times the width of a naive band … never used as a threshold"). `band_record.py` asserts `width > 0 and strength` before writing. Note: the committed strike of 2026-08-06 (`strike_result.json`) predates the 01-Sep EG refit; §3 stands on the committed strike, correctly, with the two-part as-of stamp. |
| S3 | SIGCM (1) historicals official-only | **PASS** | `filings/`: seven company PDFs (FY2021/22–FY2024/25 annuals, Q1/H1/9M FY2025/26 interims) from the company's Mist-hosted portal via kimaegypt.com, plus ten older annuals for the calibration only; 188 L1 inputs each cite the filing, auditor and report date; `GAP_REVIEW` "LATEST FILINGS" table shows each period read and footed. Bibliography §4 lists the three third-party cross-checks and states none builds. |
| S4 | SIGCM (2) ground-up — on the driver record | **PASS** | `assert_ground_up()` on five `DriverLine`s in `compute.py` covering 100% of FY2026/27 revenue — `study_numbers.json['gates']['ground_up']['share_by_level']`: **unit 74.4% (export urea — tonne, auditor's product-cost table, world f.o.b. Egypt US$/t through the derived currency path net of the 10% duty, gas m³/t + other materials/wages/services per tonne) · derived 24.4% (subsidised urea, free-market urea, nitrates — each with a gap note) · segment 1.2% (other — gap note) · topdown 0%**. Margins are outputs (Table 12: gross 45.7% → 33.0%, EBITDA 41.4% → 31.6%). |
| S5 | SIGCM (3) debt split LC/FX | **PASS** | 31-Mar-2026: bank loans 14,386.1m + current 207.0m + holdco 45.9m = EGP 14,639m (`wacc.total_debt` 14,639,005,000); **99.7% US-dollar** (`wacc.pct_debt_local` 0.0031); FX leg carried at local-equivalent cost year by year (`kd_fx_path` 19.99% → 16.72% → 15.63% → 14.54% → 14.54% on `fx_wedge_path` 7.42% → 4.49% → 3.52% → 2.54% → 2.54%); the ANNA facility (US$82.9m + EGP 5,931m) in the cost-of-debt evidence table, page 14. |
| S6 | SIGCM (4) asset-conversion cycle → BS/CF | **PASS** | Table 16: DSO 26.8 / DIO 165.2 / DPO 83.2 from FY2024/25, CCC 108.9 days; A.3 projects receivables, inventory, payables and ΔNWC from those day counts (Table 4: ΔWC +858, −106, −92, −92, −108). |
| S7 | SIGCM (5) competitors | **PASS** | B.1 peer frame (Abu Qir, MOPCO, Helwan/NCIC with capacities); relative lens on the Egyptian listed range 6.0–9.9× (register `peer_ev_ebitda_low/high`); Abu Qir FY2025 figures used for comparison only (and, per row 2, registered without being consumed by a driver). |
| S8 | SIGCM (6) beta — inspected on the record | **PASS** | `beta_result.json`: **β 1.0302, R² 0.250, SE 0.191, n 253 weekly, 4.89 y (2021-08-26 → 2026-07-16), Dimson, 90% CI 0.72–1.34, usable true, conforming true, index_file `raw_indices/EG/EGX30.csv`, index_asof 2026-07-22, interim_note null** (EGX-listed: no DFM interim, no QATAR10 caveat applies). Usability: n ≥ 24 ✓, R² ≥ 5% ✓, SE 0.191 < 1.030 ✓. Produced by `own_stock_beta()`; `assert_beta_provenance()` passes in `compute.py`, `beta_record.py`, `attest.py`. Table 17 page 14 (image): "Own-stock weekly regression, 253 observations, R-squared 0.250, standard error 0.191". The withdrawn 35-name composite (1.053) is under `superseded_composite` and named as withdrawn in §1.8 and the bibliography. |
| S9 | SIGCM (7) formula-based model | **PASS** | Rows (q)/(r): driver → IS → BS → CF → DCF live; 17 drivers reprice the headline; 0 dead inputs; the new weighted-central block is formula off the lens cells. |
| S10 | SIGCM (8) flags before issue / stop-and-inform | **PASS, with one note** | Flagged inputs: derived ANNA nameplate, gas share of materials (75%, "FLAGGED"), Abu Qir D&A estimate, ANNA wind-down cost; three NOT-REACHED sources logged with consequences. **Note (stands from the first pass):** `attest.py` line 94 builds `SIGCMChecklist` with every field True by construction — a self-attested boolean; the clauses are evidenced by the beta record, the ground-up record, `formula_audit.py` and the register rather than by that call. |
| S11 | Sweep Register primary-source depth | **PASS** | `sweep.py` re-run in the mirror (register byte-identical): "PASS — 4/4 rings, 27 findings (1 B · 12 S · 7 D · 4 C · 3 NEG), driver gate 9 rows (9 bottom-up / 0 top-down) · primary access: attempted (7) · FS years: 4 [FY2021/22–FY2024/25] · IR findings: 1 · freshness: OK — sweep and delivery same day". `sweep_date` 2026-09-01. Seven access attempts logged: kimaegypt.com and the Mist portal reached (4 entries), egx.com.eg, cbe.org.eg, kima.com.eg NOT reached with reasons; `study_quarters_disclosed` Q1 / H1 / 9M FY2025/26 swept before the build; one finding tagged `source_type: COMPANY_IR`; results releases: the company publishes none — a negative result. |
| S12 | One escalator per driver class with a sourced near-term cost anchor | **PASS** | Gas on its administered dollar price × m³/t × the derived currency path; export price on the commodity path; subsidised price on its administered path; the domestic lines (other materials, wages, purchased services, inland haulage, other selling, administration) on the one domestic inflation path — `compute.py` lines 378–398 — with Table 11's caption stating the rule and its reason. Unit costs anchored on the FY2024/25 auditor's product-cost table and the Q3 FY2025/26 reviewed margin; gas at 4.68 US$/mmBtu from the Q1 loss disclosure, contract 5.75 priced as the downside (−1.88 on page 16). |
| S13 | Margins as outputs | **PASS** | Table 12 and `lenses.json`: gross and EBITDA margins fall out of tonnes × price less the cost stack; `driver_test.py` moves margins when gas, FX and price move. |
| S14 | 3y historical + 5y forecast, full FCFF waterfall to PV inline | **PASS** | A.1 page 25: FY2022/23–FY2024/25 actual + FY2025/26E + FY2026/27–FY2030/31 (revenue 6,612 / 6,532 / 8,603 / 10,379 / 11,310 … 11,654); Table 4 page 4: Revenue → EBITDA → D&A → EBIT → NOPAT (2,940 …) → +D&A → −capex (−2,725 …) → −ΔWC → FCFF (1,963 / 298 / −156 / −219 / 450) → "Discount rate from the glide" (25.64% … 20.02%) → discount factor (0.7959 … 0.3618) → **PRESENT VALUE OF FREE CASH FLOW (1,562 / 192 / −82 / −95 / 163)**; Table 5 the terminal block and EV → equity bridge, both cases. |
| S15 | v2 WACC | **PASS** | rf* = 23.00% − 6.37% (Egypt's own Damodaran adjusted default spread) = 16.63%; ERP 13.94% rating / 9.41% CDS, each with its own rf* (19.59% CDS); Ke 30.99% / 29.28%; market-value weights 65.5/34.5 on EGP 27.77bn market cap and gross debt EGP 14.64bn; both bases published (Table 17). Cost of debt through `wacc_builder.build_wacc()`: marginal (company's own facilities), FX leg at local-equivalent cost on the study's own wedge path (19.99% year one → 14.54%), local facility 19.40%; the two places it sits below the sovereign / rf* are stated in the study's own computed words (page 14–15) and the sovereign-floored construction priced (−0.44 on the carried-through lens) rather than argued. Terminal 18.24% from 5% inflation + 3.5% real rate (rf* 8.67%) + 9% long-run dollar coupon on the 2.54% terminal wedge (11.77% dollar leg, 11.79% blended). Cash charged once: gross-debt weights, net debt in the bridge (L-054). |
| S16 | Every non-sourced driver logged to `Fundamental_Driver_Ledger.md` | **PASS, with one nit** | Lines 447–560: nine driver decisions with falsifiers, the eight constructed L5 drivers tabulated with values. **Nit:** line 538 reads "carried at (1+9%)(1+2.5%)−1 = 11.79% local-equivalent" — that product is 11.73% (on 2.5%) or 11.77% (on the 2.54% wedge); 11.79% is the blend with the local leg. The delivered study now has this right; the internal ledger and `GAP_REVIEW` DISCOUNT RATE ("on the 2.5% terminal wedge (11.79% local-equivalent)") carry the older conflation. Append-only ledger: correct it in a dated line, not in place. |
| S17 | [R-FCAL-01] fundamental walk-forward | **PASS** | Scope stated in §7 in plain words ("Rebuilt as it would have stood at each of 13 year-ends from June 2012 to June 2024 … 55 tested cases"); `PRE_REGISTRATION_01-09-2026.md`, `TRAINING_RECORD_01-09-2026.md`, `BASIS_BREAKS_01-09-2026.md`, `corrections_log.json`, `scores.json`, `forward_ranges.json`, `egch_IS_projected_vs_actual_all_origins.md` in `engine/egch_walkforward/`; the study says it did NOT beat "no change" on net profit; years 3–5 published as ranges in A.4 from `forward_ranges.json['published_band']`; no correction adopted (all watch flags); lessons L-057, L-058, L-206, L-207 registered, 0 unruled; fair value frozen before the run (`fv_movement.py check` ok). |
| S18 | Expert appendix in full | **PASS** | As row 7. |
| S19 | Crux sensitised in real observable units | **PASS** | Figure 5 / Table 19: long-run export price US$380–540/t × terminal cost of capital 17–24%, every cell a full re-run (read as image: 3.31 at 540/17.0% to −3.02 at 380/24.0%); Table 20 ranks nine swings in EGP/share. |
| S20 | Sweep Register validated | **PASS** | As S11. |
| S21 | **The answer** [R-GAP-01] | **PASS** | `study_numbers.json`: central **3.759** (field 0.00–15.47) against spot **13.98** on 2026-08-06 → **−73.1%**, now printed in the study (row (i)). `GAP_REVIEW_01-09-2026.md` carries all eight headings verbatim as `##` lines — LATEST FILINGS (24) · BASE YEAR (42) · MACRO COHERENCE (57) · DISCOUNT RATE (74) · TERMINAL (130) · BALANCE SHEET (145) · CLAIMS AGAINST THE RECORD (156) · MULTIPLE CROSS-CHECK (173) — each with substance (the most recent filing named with its date; the base year footed to filed periods with the run-rated quarter named; one macro path; cash charged once; terminal g = terminal inflation; bridge on the 31-Mar-2026 balance sheet; two superlatives recomputed, one corrected; EV/EBITDA 11.2× / 8.4× / 7.3× and P/E 28× / 39× at the price against 1.1× at the central). `check_valuation_gap.py` lists EGCH as reviewed. The review's arithmetic matches the numbers file (25.64% / 18.24% / 1,740 / 3,512 / 66.9% / −1.06 / −0.44); the second pass's typed "about 3.0" is replaced by the statement that the floor is priced on the carried-through case only. Two internal wording nits: the review still says the builder's checks are "printed in section 1.8" (they are now restated in the study's words, which is better than what the review describes) and carries the 11.79% conflation named under S16. The study's stamp equals the live `STANDARD_VERSION` 2026.09.01, which names this artefact. |
| S22 | What the filings disclose that the model does not consume | **Answered** | (1) The deferred-tax note — loss carry-forwards and the accelerated first-year deduction — not read at line level; the model charges 22.5% from year one against a company that has booked no current tax since the complex started (priced as a bound, +0.97 on the lens, +0.44 on the central, carried in §7 and the gap review). (2) The 9M FY2025/26 auditor's flags (unmarked ABUK stake, Damietta stock overstatement, unissued electricity invoices, under-booked loan interest) listed in §7, not adjusted. (3) The company's FY2025/26 budget column — registered (`budget_net_9M`), scored, not consumed. (4) Board and shareholder-structure disclosure reports — colour only. (5) The cost-by-nature notes for three years that did not foot in the prior extraction — excluded rather than re-read from the rendered page, so the 75% gas share stays a flagged construction. (6) Electricity — named in the sweep, no line of its own in the cost stack. (7) The 34 register inputs no builder consumes (row 2) are the machine-readable form of this list: every one is a disclosed fact the model records and does not use. |

---

## The beta, on its record

| | Regressed against | β | R² | SE | n | 90% CI | Conforming |
|---|---|---|---|---|---|---|---|
| **Adopted** | EGX30, `raw_indices/EG/EGX30.csv`, as of 2026-07-22 | **1.030** | 0.250 | 0.191 | 253 weekly | 0.72–1.34 | yes |
| Withdrawn | 35-name equal-weight EGX composite (08-08 edition) | 1.053 | 0.283 | 0.105 | 257 | — | no — SIGCM clause 6 hard fail |

The lower confidence bound is priced as an alternative (+0.80 a share against −0.63, page 16). The
index series ends 2026-07-22 and the stock's last regression observation is 2026-07-16, while the
anchor close is 2026-08-06 — three weekly observations fall outside the regression; §1.8 quotes
the as-of.

## What blocks delivery

Nothing. Both second-pass blockers are cleared at the artefact, not at the claim.

## Named gaps, none blocking, to clear in the next pass

1. **Bibliography register rendering** (row 1): `an_path`, `local_free_path`, `subsidised_p_path`,
   `subsidised_t_path` print as "4e+04, 4.1e+04 …" — `docx_biblio.py` line 267 `f"{x:,.4g}"`;
   use a thousands-separated integer format for tonnage and EGP/t paths.
2. **Registered-but-unconsumed inputs** (row 2): 34 of 297, listed in `prose_check_result.json`;
   mark them as record inputs so a reader can tell a driver from a fact.
3. **`live_data.json`** (row 3): still in the directory with the superseded 9.4247% figure, read only
   by the retired 08-08 builder `build_xlsx_egch.py` line 546 — delete one or the other.
4. **Internal documents' 11.79%** (S16, S21): the Driver Ledger line 538 and the gap review's
   DISCOUNT RATE name the blended terminal Kd as the dollar leg; the delivered study is right.
5. **`attest.py` `SIGCMChecklist`** all-True by construction (S10) — stands from the first pass; the
   clauses are evidenced elsewhere in this table.
6. **Figure 9** does not mark the weighted central inside the field (row 5, design nit).
7. **Housekeeping:** the 08-08 deliverables, `build_xlsx_egch.py` and `QC_GATE_08-08-2026.md` remain
   as the prior edition's record, and `assets/data.js` lines 533–535 still link the 08-08 files —
   publishing is a separate, explicitly-requested step and not part of this gate.

No rating and no price target appears in any deliverable.
