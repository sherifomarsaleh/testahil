# EGCH — QC gate, 01-Sep-2026 (independent audit)

Egyptian Chemical Industries "KIMA" · Egyptian Exchange: EGCH · edition dated 1 September 2026,
rebuilt inside the fundamental walk-forward campaign [R-FCAL-01] (run directory
`engine/egch_walkforward/`, FULL scope, 13 origins FY2012–FY2024, h=1–5, 55 cells).

Filled from outside the study by an auditor who did not build it. Every row names the artefact,
the command or the number that carries it. The study's own JSON outputs were NOT trusted: every
programmatic check was re-run in a scratch mirror of the study directory (engine symlinked, the
study copied) and the outputs compared with the delivered files. The study directory was not
edited; this file is the only thing written.

**VERDICT: NOT DELIVERABLE.** The build machinery is sound — the workbook reproduces cell for
cell, the beta is conforming, the gap review exists and clears its gate — but the rendered
documents carry a contradictory cost-of-capital table sourced from a stale intermediate file, and
seven typed statements that the computed numbers beside them contradict. The blockers are listed
at the end.

---

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker, exchange, market code | EGCH · **EGX** · EG | `assets/data.js` line 502: `code: "EGX:EGCH"` — the exchange is read from the code prefix, not inferred from `raw_ohlc/EG/`. |
| Reporting and valuation currency | EGP | Every statement in `filings/` is in Egyptian pounds; the study discounts pound cash flows at a pound rate and says so (§7, "The currency of discounting"). |
| Anchor price and date | EGP 13.98, close 2026-08-06 | `study_numbers.json` `spot`/`spot_date`; `strike_result.json` anchor_date 2026-08-06; `data.js` spot 13.98. |
| Regressor for the beta | **EGX30**, `engine/raw_indices/EG/EGX30.csv`, as of 2026-07-22 | `beta_result.json` `index_file: "raw_indices/EG/EGX30.csv"`, `index_asof: "2026-07-22"`, `interim_note: null`, `conforming: true`. Resolved by `beta_regression.own_stock_beta("EGCH","EG","EGX")` in `beta_record.py`. |
| Price history | 3,755 clean sessions, 240.8 sessions/yr, 15.6 years | `step0_result.json` (`clean_rows`, `density_rows_per_yr`, `span_years`); one vendor-fill row dropped (2011-08-03), recorded in both `step0_result.json` and `beta_result.json` `stock_dq`. |
| Company class | Gas-fed nitrogen-fertiliser producer — registered class **petrochemical**; an asset-heavy single-site operating company | `lessons_register.CLASSES` holds no fertiliser class; the run and the Driver Ledger register it as petrochemical (`Fundamental_Driver_Ledger.md` line 447). One operating segment, no lending book, no NAV business (§Company overview). |
| Reference pattern | **ADNOCLS** (operating company) | `REFERENCE_SET` is closed at ADNOCLS/ADCB/ALPHADHABI; an operating company takes ADNOCLS's skeleton. The study's lens is FCFF-DCF with a volume × price driver tree, cross-checked on book, relative and normalised lenses — the ADNOCLS shape. |
| Lessons read before starting | `python3 engine/lessons.py EGCH --class petrochemical` → 56 ALL + 1 CLASS (L-109, BOROUGE: a forecast year must be producible from the disclosed half-year) + the run's own STOCK lessons L-206/L-207 | Fundamental lessons are PROVISIONAL and were read, not cited as authority. L-109 is met: the base year is nine reviewed months plus one run-rated quarter at the third quarter's own margin (GAP_REVIEW "BASE YEAR"). |

---

## The three gates, run from outside

| Command | Result |
|---|---|
| `python3 scripts/check_study_provenance.py` | "NOW PASSING — remove from the outstanding list: EGCH" · OK, no new violations. `beta_reg.py` (the composite regression) is deleted (`git status`: `D engine/egch_study/beta_reg.py`). **Caveat:** `engine/build_depth_audit/outstanding.json` still lists EGCH; `--prune` has not been run. |
| `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` | `2026.09.01`. Stamped in `study_numbers.json`, `beta_result.json` and `attestation.json` as `2026.09.01`; `attest.py` asserts the equality. |
| Gates called in the study's own committed code | `compute.py` line 612 `assert_beta_provenance(BETA_REC)` and line 640 `assert_ground_up(DRIVER_LINES, ticker='EGCH')`; `beta_record.py` line 29 `assert_beta_provenance(rec)`; `attest.py` calls `assert_model_study(c)`, `assert_sigcm(s)`, `assert_beta_provenance(BETA)`. Entry point `run_all.py` runs all 19 stages ending in `attest.py`. Re-run of `attest.py` in the scratch mirror: all four pass. |
| `python3 scripts/check_valuation_gap.py` | "BELOW THE LINE, AND REVIEWED (1): EGCH −72.9% GAP_REVIEW_01-09-2026.md" · OK, no new violations. EGCH is in neither `gap_outstanding.json` list. |
| `python3 scripts/check_lessons_register.py` · `python3 engine/fv_movement.py check` · `python3 scripts/check_protocol_sync.py` | lessons register OK (3 registered from this run: L-057, L-058, L-207 — the ledger text says L-206 and L-207; `lessons_draft.json` records L-057/L-058/L-207 registered and 23 declined, 0 unruled) · fv register: 4 runs, every one with a frozen baseline · protocol sync OK. |

---

## The gate — model report and the eight depth standards

| # | Item | Verdict | Evidence |
|---|---|---|---|
| (a) | Structure, content, format and depth match the model report | **PASS, with the contents defects of rows (l3) and (4) carried** | 51 headings; every one of the 16 `MODEL_STUDY['word_skeleton']` sections present in order (Masthead+Read first · Headline · Valuation summary · Company overview · 1.1–1.9 · 2 · 3 · 4 · 5 · 6 · 7 · App A (A.1–A.3, plus A.4 ranges) · App B (B.1–B.3) · App C (C.1–C.6) · About · Disclosure), verified from the extracted heading list. `wb.sheetnames == MODEL_STUDY['excel_sheets']` — 16 sheets in the model order, 0 mismatches. Standalone bibliography ships (`EGCH_Bibliography_01-09-2026.docx/.pdf`, 20 pages). |
| 1 | Standalone bibliography document | **PASS, with one gap named** | 13 tables: primary-documents table (8 rows, ten older annuals + FY2021/22–FY2024/25 + three FY2025/26 interims, each with report date, auditor, retrieval date, what it sources); market/sovereign table (12 rows); external-context table (7); cross-checks table (3); access notes (7 attempts, 3 NOT REACHED); the FULL input register — **296 inputs = 187 L1 + 14 L2 + 11 L3 + 21 L4 + 63 L5**, each with value / unit / date / source-and-construction; judgements table (7 rows, each with what-would-overturn-it); negative results (6); aggregator discrepancies (3). **Gap:** page 1 states "Also indexed on the same portal but not used: annual statements for 2009 to 2021" while the first row of the same table records the ten 2009–2021 annuals as retrieved 1 Sep 2026 and used for the calibration (`docx_biblio.py` line 155 — stale sentence from the 08-08 edition). |
| 2 | Four-field provenance on every input, validated by assertion | **PASS** | `attest.py`: `four_field = all(... value, source, date, layer ...)` over `input_register.json['inputs']` → "296 inputs, all four-field complete", asserted through `ModelStudyChecklist.provenance_four_field`. Independently counted: 296 entries, zero with an empty field. About-page count (296) matches. |
| 3 | Numeric traceability — builders read the committed numbers file exclusively; independent recalculation | **FAIL** | Recalculation is clean: `python3 recalc.py` on the delivered workbook → **615 of 615 formula cells reproduce the model, 0 unresolvable, 0 unchecked, 16/16 headline reconciliations**; `formula_audit.py` → 615 formulas, 0 non-structural embedded constants; `cross_check.py` → model, workbook and study agree on 16 headline figures; `qc_checks.py` → 4 builders, 0 typed financial numerals. **But `docx_egch.py` line 21 loads `wacc_result.json` as a second numbers source** (`WC = json.load(open('wacc_result.json'))`) beside `study_numbers.json`. `wacc_result.json` is the `wacc_builder` output on a 4.5% depreciation wedge and a 3.55% CDS spread; `compute.py` lines 230–252 then RE-DERIVE the cost of debt on the register's 2.54% wedge and 3.41% spread and write the result to `study_numbers.json`. The workbook (Assumptions C88 = 0.0341, C98 = 14.55% pre-tax, C102 = 24.18%, Summary B28 = 23.07%) and the bibliography carry the re-derived figures; the Word §1.8 table carries the stale ones. See row (l3). The three stale artefacts in the directory — `wacc_result.json`, `recalc_evidence.txt` (dated 31 Aug, "555 of 555", the 08-08 edition), `formula_count.json` (707) — are what the depth standard's "exclusively" exists to prevent. |
| 4 | External-reader scrub — zero internal vocabulary, zero verdict tokens | **FAIL, on three counts** | Re-run `qc_checks.py`: 49 patterns, 0 hits in both documents. `band_record.assert_no_verdict_tokens()` run on the full extracted text of both documents: clean (no PASS/PARITY/FAIL/CRPS). No persona name; "Expert 1/2/3" only. **(i)** The bibliography's `beta_ci90_low` entry (page 16) cites "`beta_result.json`, `beta_regression.own_stock_beta`" — engine module and file names in a delivered document; the study's pattern list does not include them. **(ii)** §3 page 20 publishes the RETIRED skill figure — "it beat that benchmark by 2.64 per cent on a proper scoring rule … Over the last five years of windows the advantage was 2.04 per cent" — the [R-CAL-03] diagnostic that "may never again … reach a reader". **(iii)** The same paragraph reports the break-filtered sample (17 windows, 94.1%) rather than the published band record; `data.js` BANDS for EGCH is **n=57, c90=94.7%, flag none**, and [R-CAL-02] says the reader is owed the whole record with its count and a two-sided binomial flag (or silence), not a uniformity p-value. Calibration evidence does sit in §3 as prose; there is no calibration appendix. |
| 5 | Figure discipline — solid light canvas, zero transparency, every figure read as an image | **PASS** | PIL over all 15 `fig*.png`: mode RGBA, alpha minimum 255 on every file, corner luminance 249 on every file. All 14 figures in the rendered PDF inspected as images (pages 3, 5, 6, 8, 10, 12, 15, 16, 18, 19, 20, 30): labels legible, no collisions, no retired session-counted unit, fan/distribution axes in calendar dates. |
| 6 | Table discipline — explicit widths, starved/bloated/over-wide check | **PASS** | Re-run `qc_checks.py` table block on both documents: **study 43 tables — over-wide 0, starved 0, bloated 0; bibliography 13 tables — over-wide 0, starved 0, bloated 0** (56 tables, 0 problems; STARVED threshold 0.55 in, bloated > 75% of the text block). Every table is built through `table(rows, widths, …)` with explicit per-column widths. One layout note: bibliography page 4 is two lines followed by a blank page (page break after the access notes). |
| 7 | Expert appendix at maximum detail | **PASS, with one gap named** | `experts.json`: e1/e2/e3 each carry `worldview`, `works`, `fails`, `rows` (21 / 27 / 11 intermediate lines, printed in full as Tables 31–33), `sensitivity` with numbers, `falsifier`; C.4 cross-examination with four challenges each CONCEDED / REJECTED / CONCEDED in part; C.5 the three in one room with Figure 14; C.6 divergence table. Labels "Expert 1/2/3" only. **Gap:** Expert 1's named sensitivity (US$550 → EGP 3.82; US$700 → EGP 5.11) lies partly below the expert's own published range of 4.47–7.44 (the range is spanned by the control discount, not by the build cost), so the "swing factor" moves the answer outside the stated range. And see row (l3) for the C.6 rows that do not compute what their label says. |
| 8 | The central contested judgement computed both ways, never averaged | **PASS, with one gap named** | Contested judgement = "Is the ANNA capital programme carried through, or stopped?" (`lenses.json['contested']`): **−0.56 carried through / 3.59 stopped, gap 4.14 a share (EGP 8,231m of equity)**. Side by side in the summary table (page 2, rows 1–2), the body (Table 5 bridge, both columns; Table 24), the workbook (`Summary!B5 = 'DCF'!B44`, `Summary!C5 = 'DCF'!D44`; SOTP Bridge B6/C6), the README, and `data.js` `fair` note. `attest.py` asserts `side_a != side_b`. **Gap:** no expert range carries the two readings — Expert 3's falsifier mentions the programme being stopped, but none of the three ranges is published on both sides. |
| (l2) | Every deliverable ships as a PDF | **PASS** | PyMuPDF page counts: study 32 pages, bibliography 20 pages, workbook 53 pages. |
| (l3) | **Rendered PDF read as images, not extracted text** | **FAIL — seven contradictions, none visible to any programmatic check** | Read page by page at 95 dpi. (1) **§1.8 Table 17, page 14:** "Cost of debt after tax 12.97% / 12.97%", "Less sovereign default spread … CDS 3.55%", "Normalised risk-free rate CDS 19.45%", "Cost of capital, year one CDS 23.57%" — against "Cost of capital, year one, rating 24.18%" two rows below. 65.5% × 30.99% + 34.5% × 12.97% = **24.77%**, not 24.18%; 24.18% needs the 11.28% after-tax Kd the model actually uses (`study_numbers.json['wacc']['kd_aftertax'] = 0.1128`, Assumptions C98 = 14.55% pre-tax). The workbook's CDS spread is 3.41% (C88) and its CDS WACC 23.07% (Summary B28); the bibliography's `sov_spread_cds` is 0.0341 and says the 3.55% was "CORRECTED 9 August 2026". (2) **Page 14 prose:** "The dollar cost of 11.7% is therefore grossed up by a 2.5% expected depreciation to a local-equivalent 16.73%" — 1.117 × 1.025 − 1 = **14.49%**; 16.73% is the 4.5%-wedge figure (`docx_egch.py` lines 625–626 print `V('expected_depreciation')` beside `WC['kd_fx_local_equiv']`). (3) **Table 2 caption, page 3, and §7 page 23:** "Above one hundred per cent means the five explicit years contribute negative present value … the explicit window is a construction window with negative free cash flow, so the terminal block carries the whole enterprise value and then some" — the printed shares are 67.3% and 33.2%, the PV of the explicit window is **+1,763** (Table 5) and FCFF is positive in three of five years (Table 4). Stale from the 08-08 edition (`docx_egch.py` line 123). (4) **Table 8 caption, page 7:** "The justified multiple of book is negative before flooring … a sustainable return of 6.9% does not cover even nominal maintenance growth of 5.0%" — the table two lines above prints "Justified price-to-book before flooring **0.072x**", and (6.86% − 5%)/(30.99% − 5%) = +0.072; 6.9% is above 5.0%. (5) **§1.5, page 8:** "The book lens sits at zero because a 6.9% sustainable return on equity does not clear a 30.99% cost of equity" — the book lens is **0.58** in the summary table, Table 8 and Figure 1. (6) **Summary table page 2 and Table 18 page 16:** "Terminal reinvestment of **38.9%** of operating profit after tax, from growth over an 18% return on capital" — 5%/18% = **27.8%**, which §7 (page 23) and the terminal block (`reinv_rate 0.2778`) both state; 38.9% is 7%/18%, the 08-08 edition's growth rate (`alternatives.py` line 119, typed). (7) **C.6 Table 35, page 32:** "The whole study against the market — 1.49 — The discount rate, and only the discount rate" is computed as `LN['synthesis']['high'] − SPOT` = 15.47 − 13.98 (`docx_egch.py` line 1299): the RELATIVE lens against the price, which has no discount rate in it; the study's central is 3.79 and the cash-flow lens −0.56. Plus the bibliography contradiction in row 1. |
| (n) | Graph text readable, no overwriting | **PASS** | Every figure inspected as rendered; Figure 9 (heatmap) prints every cell value legibly; Figure 8 (glide) labels each point; Figure 10 (levels) places each level label clear of the series. |
| (p) | Terminal value as a share of enterprise value visible to the reader | **PASS** | 67.3% carried through / 33.2% stopped: Table 2 (summary), Table 5 (bridge), §7; workbook SOTP Bridge row "Terminal value as a share of enterprise value" as a formula. Never typed — but see (l3)(3) for the stale caption around it. |
| (q) | The workbook calculates | **PASS** | 615 formula cells (recalc) across 16 sheets; `formula_audit.py`: every formula built from cell references and structural constants only; the two re-run grids (Monte Carlo, Sensitivity) labelled as re-runs on their own sheets and in bibliography §7. |
| (r) | Every formula cell reproduces the model, and drivers propagate | **PASS** | `recalc.py` 615/615, 0 mismatches; `driver_test.py` on the delivered file: **17/17 drivers move the committed-capital headline in the asserted direction; 103 drivers swept, 3 cross-checks exempted by name, 0 dead inputs.** |
| (s) | Script-built files verified by cell-by-cell diff against the delivered file | **PASS** | Rebuilt `build_xlsx.py`, `docx_egch.py`, `docx_biblio.py` in the scratch mirror and compared: **workbook 1,821 populated cells compared, 0 differences** (all 16 sheets); study docx 630 text units (paragraphs + table rows), 0 differ; bibliography docx 406 text units, 0 differ. The delivered files are what the committed builders produce — which is also why the defects in (l3) are in the builders, not in a hand-edited file. |

---

## The gate — standing items

| # | Item | Verdict | Evidence |
|---|---|---|---|
| S1 | Step 0.0 data-quality gate | **PASS** | `step0_result.json` `dq_log`: "dropped 1 rows carrying a non-positive or non-finite OHLC value (2011-08-03)"; 3,755 clean rows at 240.8 sessions/yr against the EGX calendar; the same screen ran on both regression series (`beta_result.json` `stock_dq` identical, `index_dq: []`). |
| S2 | Step 0 calibration under the band record | **PASS on the record; the study text is the [R-CAL-02]/[R-CAL-03] gap in row 4** | Band record (`data.js` BANDS, EGCH): **57 resolved three-month windows, 90% band held 94.7%, 50% band 56.1%, strength "long", flag none** (two-sided binomial at 5% earns no flag); width ratio 1.446 disclosed, not gated. The study's step0 (17 post-break windows, cov50/80/90 = 64.7/88.2/94.1%) is the internal fit sample and is what §3 prints. Live `market_profiles.EG` today: ν 5.0, width_cal 0.951, momentum signal active — the strike of 2026-08-06 (`strike_result.json`: ν 6.0, signal off, width overlay 0.9) predates the 01-Sep refit; §3 stands on the committed strike, correctly, and `adaptive_width.live_width_mult` for EGCH now returns 1.0. |
| S3 | SIGCM (1) historicals official-only | **PASS** | `filings/`: seven company PDFs (FY2021/22–FY2024/25 annuals, Q1/H1/9M FY2025/26 interims) from the company's own Mist-hosted portal via kimaegypt.com; 187 L1 inputs each cite the filing, its auditor and its report date; GAP_REVIEW "LATEST FILINGS" table shows each period read and footed (9M revenue 7,314,933k; PBT 520,569k; net 531,310k). No vendor, broker or press figure enters any historical (bibliography §4 lists the three cross-checks and why none builds). |
| S4 | SIGCM (2) ground-up — on the driver record | **PASS** | `assert_ground_up()` on five `DriverLine`s (`compute.py` 620–640) covering 100% of FY2026/27 revenue: **unit 74.4% (export urea — tonne, auditor's product-cost table, world f.o.b. Egypt US$/t through the derived currency path net of the 10% duty, gas × m³/t + other materials/wages/services per tonne) · derived 24.4% (subsidised urea, free-market urea, nitrates — each with a gap note) · segment 1.2% (other — gap note) · topdown 0%.** Margins are outputs (Table 12: gross 45.7% → 33.0%, EBITDA 41.4% → 31.6%, none set). |
| S5 | SIGCM (3) debt split LC/FX | **PASS** | 31-Mar-2026: bank loans 14,386.1m + current 207.0m + holdco 45.9m = EGP 14,639m; **99.7% US-dollar** (the EGP tranche of KIMA-2 repaid June 2024, note 18-1); FX leg carried at local-equivalent cost; the ANNA facility (US$82.9m + EGP 5,930.7m) registered. |
| S6 | SIGCM (4) asset-conversion cycle → BS/CF | **PASS** | Table 16: DSO 26.8 / DIO 165.2 / DPO 83.2 from FY2024/25 (register `dso`, `dio`, `dpo`), CCC 108.9 days; A.3 projects receivables, inventory, payables and ΔNWC from those day counts (FCFF ΔWC −858, +106, +92, +92, +108). |
| S7 | SIGCM (5) competitors | **PASS** | B.1 peer frame (Abu Qir, MOPCO, Helwan/NCIC with capacities); relative lens on the Egyptian listed range 6.0–9.9× (Ezz Steel 4.35×, MOPCO 5.92×, Abu Qir ~8.2×, EFIC 11.52×, SKPC 17.61×, register `peer_ev_ebitda_low/high`); Abu Qir FY2025 revenue/net from a named page, used for comparison only. |
| S8 | SIGCM (6) beta — inspected on the record | **PASS** | `beta_result.json`: **β 1.0302, R² 0.250, SE 0.191, n 253 weekly, window 4.89 y (2021-08-26 → 2026-07-16), Dimson, 90% CI 0.72–1.34, usable=true, conforming=true, index_file `raw_indices/EG/EGX30.csv` (under `raw_indices/`), index_asof 2026-07-22, interim_note null** (EGX-listed, so no DFM interim and no QATAR10 caveat apply). Usability gate: n ≥ 24 ✓, R² ≥ 5% ✓, SE 0.191 < 1.030 ✓. Produced by `own_stock_beta()`, not a study-local script; `assert_beta_provenance()` passes in `compute.py`, `beta_record.py` and `attest.py`. The withdrawn 35-name composite (1.0527, R² 0.283) is recorded under `superseded_composite` and named as withdrawn in the study (page 17) and bibliography. The Blume cross-check (1.020) is disclosed. |
| S9 | SIGCM (7) formula-based model | **PASS** | Rows (q)/(r): driver → IS → BS → CF → DCF live; 17 drivers reprice the headline. |
| S10 | SIGCM (8) flags before issue / stop-and-inform | **PASS, with one note** | Flagged inputs: derived ANNA nameplate (189,863 t, "FLAGGED"), gas share of materials (75%, "FLAGGED"), Abu Qir D&A estimate, ANNA wind-down cost; three NOT-REACHED sources logged with consequences (EGX share count → note 14; CBE auctions → secondary quotes; kima.com.eg not the company's domain). **Note:** `attest.py` builds `SIGCMChecklist` with every field set True by construction — a self-attested boolean; the clauses are evidenced by the beta record, the ground-up record, `formula_audit.py` and the register rather than by that call. |
| S11 | Sweep Register primary-source depth | **PASS, with two notes** | Re-run `sweep.py`: 4/4 rings, 27 findings (COMPANY 15 · INDUSTRY 5 · GLOBAL 4 · COUNTRY 3), driver gate 9 rows all bottom-up; **primary access: 7 attempts logged, success and failure alike** (kimaegypt.com reached 08-Aug and 01-Sep; Mist portal reached; egx.com.eg, cbe.org.eg, kima.com.eg NOT reached with reasons); **FS years 4 (FY2021/22–FY2024/25) from the filings** (target met); study-year quarters Q1/H1/9M FY2025/26 all swept before the build; COMPANY_IR tagged distinctly (F21, 1 finding). Results releases: the company publishes none — recorded as a negative result rather than assumed. Notes: `sweep_date` is still 2026-08-08 for a 01-09 delivery (the freshness line prints "same day" regardless); driver row 4 says the non-gas stack is "anchored to the company's own FY2025/26 budget column", but `compute.py` never reads `budget_net_9M` — guidance is scored, not consumed, which is the right outcome and the register should say so. |
| S12 | One escalator per driver class with a sourced near-term cost anchor | **PASS, with one gap named** | Gas: administered dollar price × m³/t × the derived currency path (`gas_realised_usd_mmbtu` 4.68 from the Q1 loss disclosure, contract 5.75 as downside); unit costs anchored on the FY2024/25 auditor's product-cost table and the Q3 FY2025/26 reviewed margin. **Gap:** `compute.py` lines 328–342 apply the single `cpi_cum` path to other materials, wages, services, freight, other selling and admin alike — five physically distinct lines on one domestic index; Table 11's caption says "never on one blended index". Wages and admin are domestic; other materials (catalysts, packaging) and freight (fuel) deserve their own justification or their own path. |
| S13 | Margins as outputs | **PASS** | Table 12 and `lenses.json`: gross and EBITDA margins fall out of tonnes × price less the cost stack; `driver_test.py` moves margins when gas, FX and price move. |
| S14 | 3y historical + 5y forecast, full FCFF waterfall to PV inline | **PASS** | A.1: FY2022/23–FY2024/25 actual + FY2025/26E + FY2026/27–FY2030/31; Table 4 (page 4) prints Revenue → EBITDA → D&A → EBIT → NOPAT → +D&A → −capex → −ΔWC → FCFF → discount rate from the glide → discount factor → **PV of FCFF (1,581 / 195 / −84 / −98 / 168)**; then the terminal block and the EV→equity bridge (Table 5). A.2 balance sheet 4 dates; A.3 forecast BS/CF markers. |
| S15 | v2 WACC | **FAIL, on disclosure of the cost of debt; the construction otherwise conforms** | rf* = 23.00% − 6.37% (Egypt's own Damodaran adjusted default spread, read from `ctryprem_snapshot.xlsx`) = 16.63%; ERP 13.94% rating basis / 9.42% CDS basis, both published with their OWN rf*; Ke 30.99%; market-value weights 65.5/34.5 on EGP 27.77bn market cap and gross debt; terminal rate rebuilt from 5% inflation + 3.5% real rate, gliding 24.18% → 18.23%. **The failure:** the marginal cost of debt the model uses — 14.55% pre-tax blended (99.7% FX at 1.117 × 1.0254 − 1 = 14.49%; 0.3% local at 19.4%) — sits **below the 23.00% sovereign yield AND below the 16.63% normalised risk-free rate**, and the local facility's 19.4% is itself below the sovereign. `wacc_builder.build_wacc()` carries an explicit sanity check for both conditions (lines 359–366) and would have warned on the FX leg; `compute.py` bypasses `build_wacc()` and re-derives Kd from the register, so no warning was raised and the study neither states nor prices the below-sovereign cost of debt. A same-currency corporate borrowing below its sovereign is the case the rule names; if the company's own facilities genuinely price there, the study must say so and show what a sovereign-plus-spread Kd does to the answer. |
| S16 | Every non-sourced driver logged to `Fundamental_Driver_Ledger.md` | **FAIL** | The EGCH entry (lines 447–530) logs eight driver decisions from the walk-forward (macro path, capitalised FX, interest during construction, tax, flat tonnes, terminal inflation, beta, cost-by-nature notes). **Not logged:** the constructed drivers that set the terminal rate and the programme's economics — `kd_usd_lt` 9%, `real_rate_lt` 3.5%, `roc_terminal` 18% (which the study itself calls "the largest unsourced input left in the model"), `anna_cash_margin` 32%, `anna_util_base/bull` 50%/70%, `dep_escalation` 2%, `fx_terminal_wedge` 2.5%, `anna_winddown_cost` EGP 1,000m. Each is in the bibliography's L5 table with "Constructed" and no disclosed anchor; none is in the ledger. |
| S17 | [R-FCAL-01] fundamental walk-forward | **PASS** | Scope decision stated in the study §7 (page 24) in plain words: "Rebuilt as it would have stood at each of 13 year-ends from June 2012 to June 2024 and projected one to five years ahead — 55 tested cases over eighteen fiscal years"; `study_numbers.json['walkforward']`: ran, FULL, 13 origins, h 1–5, 55 cells. `TRAINING_RECORD_01-09-2026.md` and `PRE_REGISTRATION_01-09-2026.md` exist; the record reports skill vs freeze: revenue +0.146, cost of sales +0.273, net **−0.559** — and the study says so (§7: "it did NOT forecast net profit better than assuming no change"). Years 3–5 published as ranges in A.4 (Table 29, e.g. FY2030/31 revenue 945–15,490 against a point of 11,654, with the tested-case counts beside each band) from `forward_ranges.json['published_band']`, read by `build_xlsx.py` line 1028 and by the docx through `study_numbers['walkforward']['bands']`. Corrections: none adopted — all five are watch flags in `corrections_log.json` (adjusted made the next origin worse on revenue 0/3, cost 0/3, interest 0/2), stated in the ledger. Lessons: 3 registered, 23 declined, 0 unruled. Fair value frozen before the run (`fv_movement.py check` ok). |
| S18 | Expert appendix in full | **PASS, with the row 7 gap** | As row 7. |
| S19 | Crux sensitised in real observable units | **PASS** | Figure 9 / Table 19: long-run export price US$380–540/t × terminal cost of capital 17–24%, every cell a full re-run; Table 20 ranks nine swings in EGP/share (programme 4.14, gas 1.93, terminal rate 0.88, beta 0.82, maintenance capex 0.81, CDS basis 0.75, ANNA utilisation 0.23, terminal growth 0.04). |
| S20 | Sweep Register validated | **PASS** | `sweep.py` re-run in the mirror: "PASS — 4/4 rings … freshness: OK"; the two notes in S11 stand. |
| S21 | **The answer** [R-GAP-01] | **PASS on the gate; the contradictions in (l3) are what a reader of this answer would find next** | `study_numbers.json`: central **3.79** (field 0.00–15.47) against spot **13.98** on 2026-08-06 → **−72.9%**. `GAP_REVIEW_01-09-2026.md` exists and carries all eight headings verbatim (LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK); `check_valuation_gap.py` lists EGCH as "BELOW THE LINE, AND REVIEWED". The review found and fixed two defects (terminal inflation 7% → 5% = terminal growth; a typed maintenance-capex superlative), priced one open construction (statutory tax on a company paying none: +EGP 0.99 a share, bound), and reads the remainder as a conclusion: EV 5.4bn = 1.2× first-year EBITDA because the explicit window's FCFF (1,963 / 298 / −156 / −219 / 450) is consumed by EGP 14.7bn of programme spend and the terminal capitalises an EBIT falling to 2,250m at 18.2%; the price implies a flat 7.8% nominal rate against a 23% sovereign. The audit trail is consistent with the numbers file (its 11.28% after-tax Kd and 24.18% WACC are the model's) — it is the Word document that disagrees with it. The weighted central puts 45% on a lens whose equity value is negative (−0.56); bear is floored at 0.00 in `data.js`. |
| S22 | What the filings disclose that the model does not consume | **Answered** | (1) The deferred-tax note — loss carry-forwards from FY2020–21 (EGP 1.70bn pre-tax) and the accelerated first-year deduction — is disclosed and not read at line level; the model charges 22.5% from year one against a company that has booked no current tax since the complex started (GAP_REVIEW prices the bound at EGP 0.99 a share). (2) The 9M FY2025/26 auditor's flags — unmarked ABUK stake (+112m), Damietta stock overstatement (116m), unissued electricity invoices (49.7m), under-booked loan interest (27.6m) — are listed in §7 and not adjusted (under EGP 0.10 a share together). (3) The company's own FY2025/26 budget column (`budget_net_9M` 1,022m) is registered and, correctly, not consumed. (4) The quarterly board and shareholder-structure disclosure reports on the portal (F21) are recorded as colour only. (5) The cost-by-nature notes for three years were extracted in the prior edition and found not to foot (misses of 247k, 36.3m, 63k EGP) — excluded rather than re-read from the rendered page, so the gas/materials split (75%, the study's largest allocation) remains a construction where a footing note might have pinned it. (6) Electricity is named in the sweep (transmission-tariff dispute) and has no line of its own in the cost stack. |

---

## The beta, on its record

| | Regressed against | β | R² | SE | n | 90% CI | Conforming |
|---|---|---|---|---|---|---|---|
| **Adopted** | EGX30, `raw_indices/EG/EGX30.csv`, as of 2026-07-22 | **1.030** | 0.250 | 0.191 | 253 weekly | 0.72–1.34 | yes |
| Withdrawn | 35-name equal-weight EGX composite (08-08 edition) | 1.053 | 0.283 | 0.105 | 257 | — | no — SIGCM clause 6 hard fail |

The move is −2.1% on beta and +EGP 0.15 on the carried-through lens together with the terminal fix. The lower confidence bound is priced as an alternative (+0.82 a share). The index series ends 2026-07-22 and the stock's last regression observation is 2026-07-16, while the anchor close is 2026-08-06 — three weekly observations fall outside the regression; the "as of" is quoted wherever the beta appears.

## What blocks delivery, shortest list

1. **§1.8 Table 17 and its prose** state a cost of debt (12.97% after tax; "2.5% … to 16.73%"), a CDS spread (3.55%), a CDS rf* (19.45%) and a CDS WACC (23.57%) that the workbook, the bibliography, the gap review and the year-one WACC in the same table (24.18%) all contradict. Cause: `docx_egch.py` line 21 reads `wacc_result.json`, a stale intermediate, beside the numbers file. Fix in the builder (read `study_numbers.json['wacc']` only; delete or regenerate `wacc_result.json` from the same inputs) and re-render.
2. **Six stale typed statements** contradicted by the computed figures beside them: Table 2 caption and §7 ("above one hundred per cent … negative free cash flow" against 67.3% and PV +1,763); Table 8 caption ("negative before flooring", "does not cover … 5.0%" against 0.072x and 6.9%); §1.5 ("book lens sits at zero" against 0.58); summary table and Table 18 ("38.9%" against 27.8%); C.6 ("whole study against the market 1.49" = field high − spot). Each is a typed sentence in `docx_egch.py` (lines 123, 283–285, 346) or `alternatives.py` (line 119) that must be computed from the numbers file and asserted, per the number-stated-in-prose rule.
3. **Bibliography page 1** says the 2009–2021 annuals were "not used" in the same table that records them as retrieved and used (`docx_biblio.py` line 155).
4. **External-reader scrub:** engine names in the bibliography's `beta_ci90_low` entry; the retired skill percentage and the break-filtered 17-window sample in §3 in place of the 57-window band record [R-CAL-02, R-CAL-03].
5. **Cost of debt below the sovereign** (14.55% blended, 19.4% local, against 23.00% and rf* 16.63%) is neither stated nor priced, because `compute.py` bypasses `wacc_builder`'s own check.
6. **Driver Ledger** does not log the constructed terminal-rate and ANNA-economics drivers.

Non-blocking, to clear in the same pass: run `check_study_provenance.py --prune` (EGCH now passes); remove or regenerate `recalc_evidence.txt`, `formula_count.json`, `wacc_result.json` so the directory holds one set of numbers; make `attest.py`'s `recalc_ok` bind on something (it is computed and unused); state in the sweep register that the budget column is registered and not consumed; give other materials and freight their own escalator or their own justification; note that the stock's 6-Aug strike predates the 01-Sep EG refit. `data.js` still links the 08-08 deliverables — publishing is a separate, explicitly-requested step and is not part of this gate.

No rating and no price target appears in any deliverable.
