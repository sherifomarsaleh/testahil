# EGCH — QC gate, 01-Sep-2026 (independent audit, SECOND PASS)

Egyptian Chemical Industries "KIMA" · Egyptian Exchange: EGCH · edition dated 1 September 2026,
rebuilt inside the fundamental walk-forward campaign [R-FCAL-01] (run directory
`engine/egch_walkforward/`, FULL scope, 13 origins FY2012–FY2024, h=1–5, 55 cells).
Audited at commit `1c8cb52` ("fix the first-pass QC findings and rebuild"), working tree clean.

Filled from outside the study by an auditor who did not build it. Every row names the artefact,
the command or the number that carries it. The study's own JSON outputs were NOT trusted: the
whole pipeline (`run_all.py`, 20 stages from `beta_record.py` to `attest.py`) was re-run in a
scratch mirror (engine symlinked, the study copied) and every output diffed against the delivered
files; the rendered PDFs were read as images on the pages the first pass failed and on every page
carrying a regenerated figure. The study directory was not edited; this file is the only thing
written.

**VERDICT: NOT DELIVERABLE — two blockers, both small, both new.** Every one of the first pass's
five FAIL rows and every named gap has been cleared and is re-verified below. What remains is (1)
the study's published central of EGP 3.76 — the number `assets/data.js` carries as `fair.base`
and the number `check_valuation_gap.py` audits — appears in **no delivered document** (the summary
table's central cell is "—" and the field is the only answer given), while the bibliography's own
register row says these lens weights are "the same weights the 08-08-2026 edition published",
which that edition did not do either; and (2) the bibliography's market-data table prints the
CDS-basis equity premium as **9.42%** against the **9.41%** the register, Table 17, the workbook
and the model all carry — a stale figure read from `live_data.json`, sitting beside a superseded
register input (`erp_cds` = 0.0942) that nothing consumes. Both are the class the first pass
failed on (a delivered document disagreeing with the numbers file), and both are under an hour.

---

## What the first pass found and what changed

| First pass (this file, as committed at `607899d`) | Second pass — verified from the artefacts |
|---|---|
| **FAIL row 3, numeric traceability:** `docx_egch.py` line 21 loaded `wacc_result.json` beside `study_numbers.json`; §1.8 Table 17 printed a 12.97% after-tax Kd, a 3.55% CDS spread, a 19.45% CDS rf* and a 23.57% CDS WACC that the workbook, bibliography and gap review contradicted. | **Cleared.** `wacc.py` and `wacc_result.json` deleted (`git show --stat HEAD`: `wacc.py | 134 ---`, `wacc_result.json | 38 ---`); `docx_egch.py` now opens only `study_numbers.json`, `lenses.json`, `experts.json`, `strike_result.json`, `technicals.json`, `beta_result.json`, `alternatives.json`, `sensitivity_grid.json`, `flat_rate_ladder.json`, `band_record.json`, `input_register.json` — all pipeline outputs (grep `json.load` lines 14–24, 826, 1200). `compute.py` builds the cost of capital once through `wacc_builder.build_wacc()` (one `WaccInputs` per explicit year, lines 216–297 of the diff) from the register alone. Table 17 as rendered (page 14): Ke 30.99% / 29.28%, Kd after tax 15.49% / 15.49%, weights 65.5 / 34.5, WACC 25.64% / 24.52% — every cell equals `study_numbers.json['wacc']` (0.30986, 0.29284, 0.15491, 0.65483, 0.25638, 0.24523). `recalc_evidence.txt` and `formula_count.json` also removed. |
| **FAIL row (l3), seven typed contradictions in the rendered PDF** (Table 2 caption "above one hundred per cent"; Table 8 "negative before flooring … does not cover 5.0%"; §1.5 "book lens sits at zero"; "38.9%" terminal reinvestment in the summary table and Table 18; "16.73%" local-equivalent Kd; the 12.97/3.55/19.45/23.57 block; C.6 "whole study against the market 1.49"). | **All seven cleared, read as rendered images.** Page 3 Table 2 caption now "The explicit years contribute a present value of EGP 1,740 million … the terminal block carries 66.9%"; page 7 Table 8 caption "0.072 times: a sustainable return of 6.9% clears nominal maintenance growth of 5.0% by only 1.9%"; page 8 §1.5 "The book lens sits at EGP 0.58"; pages 2 and 16 "Terminal reinvestment of 27.8%"; page 14 "7.4% in FY2026/27, giving a local-equivalent 19.99%"; page 32 C.6 row relabelled "The top of the field against the market — 1.49 — The relative lens … is the one reading that sits above the price". Delivered text greps: "above one hundred" 0, "negative before flooring" 0, "sits at zero" 0, "38.9" 0, "12.97" 0, "19.45" 0, "23.57" 0, "16.73" 1 (now the one-month 95th percentile, EGP 16.73, a different number). New `prose_check.py`: **396 percentages/ratios in prose checked against the numbers files, 0 unmatched** — but see blocker 2 for the one it cannot see. |
| **FAIL row 4, external-reader scrub** — engine names in the bibliography's `beta_ci90_low` entry; the retired skill figure ("2.64 per cent on a proper scoring rule") and the break-filtered 17-window sample in §3 in place of the 57-window band record. | **Cleared.** `beta_ci90_low` now reads "the same beta estimate — a five-year weekly regression of the stock's own returns on the EGX30 (253 weekly observations to 16 July 2026)" (bibliography page 16, read as image). §3 page 20: "Over 57 resolved three-month forecasts on this share the price finished inside the ninety-per-cent band 94.7% of the time, inside the eighty-per-cent band 87.7% and inside the fifty-per-cent band 56.1% … The record earns no flag either way." — read from `assets/data.js` through node by the new `band_record.py` (n 57, hits 54, c90 0.9474, flag null). "skill" 0 hits, "scoring rule" 0, "beta_result" 0, "own_stock_beta" 0, "wacc_result" 0 in both documents; `qc_checks.py` widened to 68 patterns, 0 hits; `band_record.assert_no_verdict_tokens()` clean on the full extracted text of both documents. |
| **FAIL S15, v2 cost of debt:** the 14.55% blended Kd sat below the 23.00% sovereign and the 16.63% rf*, `compute.py` bypassed `build_wacc()`, so no warning fired and nothing was disclosed or priced. | **Cleared.** FX leg built year by year on the study's own PPP wedge path (`wacc.fx_wedge_path` 7.42% → 4.49% → 3.52% → 2.54% → 2.54%; `kd_fx_path` 19.99% → 16.72% → 15.63% → 14.54% → 14.54%); the builder's warnings are stored (`warnings_by_year`: FY2028/29–FY2030/31 "FX(local-equiv) Kd … < rf* 16.63%") and printed verbatim in §1.8 page 14, with the study's own added disclosure that the 19.40% local facility sits below the 23.00% sovereign (the builder's local-below-sovereign check is gated on a ≥99.9%-local book and would not fire on a 0.3%-local one — `compute.py` says so). Sovereign-floored alternative priced: `alternatives.json['cost_of_debt_floor']` value −1.062 vs −0.625, delta −0.437 — summary table page 2 (−1.06, −108%), Table 18 page 15 (−1.06 / −0.44), `Sensitivity!C20`/`D20`. Central 3.79 → 3.76. |
| **FAIL S16, Driver Ledger:** the eight constructed terminal-rate and ANNA drivers were not logged. | **Cleared.** `Fundamental_Driver_Ledger.md` lines 447–560: item 9 records the cost-of-debt ruling, and a "Constructed drivers carried without a source" table lists `roc_terminal` 18%, `kd_usd_lt` 9%, `real_rate_lt` 3.5%, `anna_cash_margin` 32%, `anna_util_base/bull` 50%/70%, `dep_escalation` 2%, `fx_terminal_wedge` 2.54% (DERIVED), `anna_winddown_cost` EGP 1,000m — each with what it does and what would overturn it. |
| Gaps named: bibliography page 1 "not used" contradiction; Expert 1's sensitivity outside its own range; no expert range on both sides of the contested judgement; escalator claim vs a single `cpi_cum`; `sweep_date` 2026-08-08; budget-column row; stale artefacts; `recalc_ok` computed and unused; EGCH still in `outstanding.json`. | Page 1 now: "The ten annual statements for 2009 to 2021 … were retrieved on 1 September 2026 and used only to test the forecasting method on the company's own history; no forecast driver is taken from them. Also indexed on the same portal but not used: the quarterly board and shareholder-structure disclosure reports" (read as image). Expert 1 range now 3.82–8.52 = its own sensitivity's end-points (`experts.json['ranges']['e1']`). Expert 3 (page 30) "On both sides of the contested judgement. With the programme carried through the claim is worth EGP 1.78 a share (EGP 0.00 to 1.96 …); with the programme stopped … EGP 4.54 (EGP 4.41 to 4.70)". Table 11 caption (page 9) now states the two-class rule and names which lines share the domestic path and why ("because that is the index they are paid in"). `sweep_register.json` `sweep_date` 2026-09-01; driver row 4 "The company's own budget column in the 9M interim is registered and scored against the outturn; no driver consumes it." `attest.py` binds `numeric_traceability` on `recalc_ok`. `check_study_provenance.py`: EGCH absent from both lists (pruned at `cce3482`). |

---

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker, exchange, market code | EGCH · **EGX** · EG | `assets/data.js` line 502: `code: "EGX:EGCH"` — read from the code prefix, not inferred from `raw_ohlc/EG/`. |
| Reporting and valuation currency | EGP | Every statement in `filings/` is in Egyptian pounds; §7 page 24 "The study discounts pound cash flows at a pound rate throughout". |
| Anchor price and date | EGP 13.98, close 2026-08-06 | `study_numbers.json` `spot`/`spot_date`; `strike_result.json` anchor_date 2026-08-06; `data.js` spot 13.98 / "close 6 Aug 2026". |
| Regressor for the beta | **EGX30**, `engine/raw_indices/EG/EGX30.csv`, as of 2026-07-22 | `beta_result.json` `index_file: "raw_indices/EG/EGX30.csv"`, `index_asof: "2026-07-22"`, `interim_note: null`, `conforming: true`; resolved by `beta_regression.own_stock_beta("EGCH","EG","EGX")` in `beta_record.py`. |
| Price history | 3,755 clean sessions, 240.8 sessions/yr, 15.6 years | `step0_result.json`; one vendor-fill row dropped (2011-08-03), recorded in `step0_result.json` and `beta_result.json` `stock_dq`. |
| Company class | Gas-fed nitrogen-fertiliser producer — registered class **petrochemical**; single-site operating company | `lessons_register.CLASSES` holds no fertiliser class; the run and the Driver Ledger register it as petrochemical (ledger line 447). One operating segment, no lending book, no NAV business (Company overview, page 3). |
| Reference pattern | **ADNOCLS** (operating company) | `REFERENCE_SET` is closed at ADNOCLS/ADCB/ALPHADHABI; FCFF-DCF with a volume × price driver tree, cross-checked on book, relative and normalised lenses — the ADNOCLS shape. |
| Lessons read before starting | `python3 engine/lessons.py EGCH --class petrochemical` → 61 lessons: 58 ALL + L-109 (CLASS) + L-206/L-207 (STOCK) | Read, not cited as authority — fundamental lessons are PROVISIONAL. L-048 (one macro path) is what the year-by-year Kd wedge now honours; L-054 (cash charged once) and L-055 (terminal inflation = terminal growth) are both met (below). |

---

## The three gates, run from outside

| Command | Result |
|---|---|
| `python3 scripts/check_study_provenance.py` | "study directories: 24 … OK — no new violations." EGCH is in neither the outstanding nor the exempt list (pruned at `cce3482`); the only "NOW PASSING" line is FERTIGLOBE, someone else's. No study-local regression script survives (`beta_reg.py` deleted in the first-pass commit; `beta_record.py` calls `own_stock_beta()`). |
| `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` | `2026.09.01`. Stamped identically in `study_numbers.json`, `beta_result.json`, `attestation.json`; `attest.py` asserts the equality. |
| Gates called in the study's own committed code | `compute.py`: `assert_beta_provenance(BETA_REC)` and `assert_ground_up(DRIVER_LINES, ticker='EGCH')`; `beta_record.py`: `assert_beta_provenance(rec)`; `attest.py`: `assert_model_study(c)`, `assert_sigcm(s)`, `assert_beta_provenance(BETA)`. Entry point `run_all.py` (20 stages, ends in `attest.py`) re-run in the mirror: exit 0, "all stages completed", all three assertions print PASSED. |
| `python3 scripts/check_valuation_gap.py` | "BELOW THE LINE, AND REVIEWED (1): EGCH −73.1% GAP_REVIEW_01-09-2026.md · OK — no new violations." EGCH is in neither `gap_outstanding.json` list. |
| `python3 scripts/check_lessons_register.py` · `python3 engine/fv_movement.py check` · `python3 scripts/check_protocol_sync.py` · `python3 scripts/check_band_vocabulary.py` | lessons register OK (ALL 58 · CLASS 19 · STOCK 7; 0 unruled) · fv register: 4 runs, every one with a frozen baseline; EGCH row "3.64 → 3.76, +3.3%" · protocol sync OK · band vocabulary OK. |

---

## The gate — model report and the eight depth standards

| # | Item | Verdict | Evidence |
|---|---|---|---|
| (a) | Structure, content, format and depth match the model report | **PASS, with the row (i) defect carried** | 51 headings; every one of the 16 `MODEL_STUDY['word_skeleton']` sections present in order (`attest.py` check_sections → 0 missing). `wb.sheetnames == MODEL_STUDY['excel_sheets']` — 16 sheets, 0 mismatches. Standalone bibliography ships (20 pages). PDFs: study 33 pages, bibliography 20, workbook 56. The one structural element of the model report's summary table that is absent is the weighted-central row — row (i). |
| 1 | Standalone bibliography document | **PASS, with blocker 2 carried** | 13 tables: primary documents (8 rows, each with report date, auditor, retrieval date and what it sources); market/sovereign/macro (12 rows); external context (7); cross-checks (3); access notes (7 attempts, 3 NOT REACHED with reasons); the FULL input register — **297 inputs = 188 L1 + 14 L2 + 11 L3 + 21 L4 + 63 L5**, each with value / unit / date / source-and-construction; judgements (7, each with what-would-overturn-it); negative results (6); aggregator discrepancies (3). Page 1 contradiction from the first pass is gone (read as image). **Blocker 2 sits in table 2 of this document** (below). |
| 2 | Four-field provenance on every input, validated by assertion | **PASS, with one gap named** | `attest.py` `four_field` over `input_register.json['inputs']` → "297 inputs, all four-field complete", asserted through `ModelStudyChecklist.provenance_four_field`; About page and register total both print 297. **Gap:** the register carries two values for one input — `erp_cds` 0.09425 (L2, "CDS basis total equity risk premium", 2026-01-01) and `erp_cds_damodaran` 0.0941 (L2, "CORRECTED 9 August 2026"). The model consumes only the second (`compute.py` `_V('erp_cds_damodaran')`); `erp_cds` is consumed by nothing — a registered input no line uses (L-018) — and its stale value is what surfaces in blocker 2. |
| 3 | Numeric traceability — builders read the committed numbers file exclusively; independent recalculation | **PASS on the model and the workbook; FAIL on one bibliography cell (blocker 2)** | Mirror re-run: `recalc.py` on the delivered workbook → **626 of 626 formula cells reproduce the model, 0 unresolvable, 0 unchecked, 0 orphans, 16/16 headline reconciliations**; `formula_audit.py` 626 formulas, 0 non-structural constants; `cross_check.py` 16/16 model = workbook = study; `qc_checks.py` 4 builders, 0 typed financial numerals; `prose_check.py` 396 figures, 0 unmatched. `wacc_result.json` gone; `docx_egch.py` reads pipeline outputs only. **The failure:** `docx_biblio.py` lines 166–183 build the market-data table's sovereign rows from `LIVE['damodaran_egypt']` in `live_data.json` (a 31-Aug file the rebuild did not regenerate), whose `cds_based_total_equity_risk_premium` is 0.09425 — so bibliography page 2 prints "13.94% (rating) / **9.42%** (CDS)" while `input_register.json['erp_cds_damodaran']` = 0.0941, Table 17 prints 9.41%, `Assumptions` carries 0.0941 and `wacc.erp_cds` = 0.0941. `prose_check.py` passed it because the correction note's own text ("the study had 9.4247%") is harvested as a literal and 9.42 lands in the render set — the checker's exemption for register prose defeats it on exactly the figure the note says was corrected. The same table types "EGP 13.98", "23.00%", "23.098%", "EGP 49.79", "US$545", "14.3%", "19.00 / 20.00%" as string literals in the builder (below the numeral scan's four-digit floor); they agree with the register today and are named here so the next edition computes them. |
| 4 | External-reader scrub — zero internal vocabulary, zero verdict tokens | **PASS, with one gap named** | Mirror re-run `qc_checks.py`: **68 patterns, 0 hits** in both documents. `band_record.assert_no_verdict_tokens()` on the full extracted text of both: clean. No persona name ("Damodaran" appears once, in the bibliography, as the source workbook's name — not a persona). §3 carries the band record as prose with the count beside the percentage; no calibration appendix (appendices are A, B, C only). **Gap:** §1.8 page 14 prints the builder's warning verbatim — "FX(local-equiv) Kd 15.63% < rf* 16.63% — looks like a raw FX coupon not grossed up for expected depreciation" — and the very next sentence correctly explains that the coupon IS grossed up and the wedge is simply smaller than the coupon-to-rf* gap in those years. The fact is right and disclosed; the diagnostic's wording is the builder's, not the study's, and a reader is handed a sentence the study then contradicts. Restate the warning in the study's own words (it is already computed) rather than pasting the builder's guess. |
| 5 | Figure discipline — solid light canvas, zero transparency, every figure read as an image | **PASS** | PIL over all 15 `fig*.png`: alpha minimum 255 on every file (qc_checks, re-run). All 15 are byte-identical to the mirror rebuild. Inspected as rendered: Figures 1, 2, 4, 5, 6, 8, 10, 11, 12, 13, 14 on pages 3, 5, 8, 10, 15, 19, 20, 32, and the three regenerated figures not on those pages (`fig2_bridge.png` = Figure 3, `fig14_programme.png` = Figure 7, `fig5_crux.png` = Figure 9) opened directly. Legible, no collisions, no retired session-counted unit (fan axis is "Trading sessions — past to the left of zero, simulated to the right", distributions in calendar dates), heatmap title promises nothing it does not show. |
| 6 | Table discipline — explicit widths, starved/bloated/over-wide check | **PASS** | `qc_checks.py` re-run: **study 43 tables, bibliography 13 tables — 56 checked, 0 over-wide, 0 starved, 0 bloated** (STARVED 0.55 in, bloated > 75% of the text block). Layout note only: page 16 is half blank after Table 18 (a page break before §1.9). |
| 7 | Expert appendix at maximum detail | **PASS** | `experts.json` e1/e2/e3 each carry worldview, works, fails, rows (printed in full as Tables 31–33), a named sensitivity with numbers, a falsifier; C.4 four challenges each CONCEDED / REJECTED / CONCEDED in part (page 31); C.5 the three in one room with Figure 14 (page 32); C.6 divergence table. Labels "Expert 1/2/3" only. Both first-pass gaps closed: Expert 1's range (3.82–8.52) now equals its sensitivity's end-points; Expert 3 publishes both sides of the contested judgement (1.78 carried through / 4.54 stopped, page 30). |
| 8 | The central contested judgement computed both ways, never averaged | **PASS** | "Is the ANNA capital programme carried through, or stopped?" (`lenses.json['contested']`): **−0.63 carried through / 3.43 stopped, gap EGP 4.05 a share (EGP 8,051m of equity)**. Side by side: summary table page 2 rows 1–2; Table 5 page 5 (both columns, EV 5,252 / 13,304, equity −1,242 / 6,809); workbook `Summary!B20='DCF'!B44`, `B21='DCF'!D44`, `B22=B21-B20`; Expert 3's two readings; `data.js` fair note. `attest.py` asserts `side_a != side_b`. But see row (i): the published central then puts 45% on one side and 0% on the other without saying so anywhere a reader can see. |
| (i) | Summary valuation table present, with the weighted central and its weights inline | **FAIL (blocker 1)** | The summary table (pages 1–2) carries one row per lens with basis, range, central and distance from spot, the field, the market price and the alternative-readings block — but its "THE FIELD" row prints central "—", and **EGP 3.76 appears nowhere in the delivered study or bibliography** (`\b3\.76\b`: 0 hits; "weighted central" / "lens weights": 0 hits). Yet `study_numbers.json['central']` = 3.759 = 0.45 × (−0.625) + 0.20 × 15.474 + 0.20 × 4.29 + 0.15 × 0.58, `data.js` publishes `fair.base: 3.76` from it, the FV register records "3.64 → 3.76", and `check_valuation_gap.py` audits it. The only place the weights exist for a reader is bibliography register row `lens_weights` (0.45/0.2/0.2/0.15, "Weights of the four lenses in the published central … the same weights the 08-08-2026 edition published") — and the 08-08 document does not print a central or its weights either (`\b3\.64\b` 0 hits, "weighted" 0 relevant hits in `EGCH_Valuation_Study_08-08-2026.docx`), so that sentence is untrue on its face. READ FIRST says "Four lenses give a field, not a point" while the register speaks of "the published central". A reader with 3.76 on the site and this PDF open cannot reconcile them. Fix either way — print the weighted-central row with the weights inline as the model report does (computed from `lenses.json`, asserted by `prose_check.py`), or state in READ FIRST and §1.5 how the base figure on the site is built and why the carried-through side alone enters it — and correct the register sentence. |
| (l2) | Every deliverable ships as a PDF | **PASS** | PyMuPDF page counts: study 33, bibliography 20, workbook 56; all three regenerated in the rebuild commit. |
| (l3) | **Rendered PDF read as images, not extracted text** | **PASS on the seven first-pass contradictions; the two blockers are what this read found on the second pass** | Pages 1, 2, 3, 4, 5, 7, 8, 9, 10, 13, 14, 15, 16, 19, 20, 23, 24, 30, 31, 32 of the study and pages 1, 2, 16, 17 of the bibliography read at 100 dpi. Each of the seven first-pass contradictions confirmed gone at the page (table above). Arithmetic spot-checked on the page: Table 7 caption "about 7.8%, which is 15.2% below the sovereign" (23.0 − 7.8 = 15.2 ✓); Table 17 WACC 25.64% = 0.6548 × 30.99% + 0.3452 × 15.49% ✓; §1.8 "7.4% … 19.99%" = 1.117 × 1.0742 − 1 ✓; Figure 8 glide 25.6 / 23.7 / 22.4 / 21.1 / 20.0 / 18.2 = `drivers.wacc_path` and `wacc_terminal` ✓; Table 5 terminal share 66.9% = 3,512 / 5,252 ✓; §1.3 "1.1 times" = 5,252 / 4,684 ✓. Found on this read: bibliography page 2 "9.42% (CDS)" (blocker 2); the absent central (blocker 1). Nit: §1.8 says "the long-run dollar cost of 9.0% in the terminal year, 11.79%" — 11.79% is the blended terminal Kd including the 0.3% local leg (`drivers.kd_local_equiv_terminal` 0.11792); the pure dollar leg is 11.77%. Two basis points, but the sentence names the wrong quantity. |
| (n) | Graph text readable, no overwriting | **PASS** | As row 5. Figure 9 (heatmap) prints all 25 cell values legibly with ink contrast switching on the dark cells; Figure 8 labels each point; Figure 10 places level labels clear of the series. |
| (p) | Terminal value as a share of enterprise value visible to the reader | **PASS** | 66.9% carried through / 33.0% stopped: Table 2 (page 3), Table 5 (page 5), §7 (page 23); workbook `SOTP Bridge` and `DCF!B38/D38` as formulas. Caption around it now computed (row (l3)). |
| (q) | The workbook calculates | **PASS** | 626 formula cells across 16 sheets (`recalc.py`); `formula_audit.py`: every formula built from cell references and structural constants only; the two re-run grids (Monte Carlo, Sensitivity) labelled as re-runs; `Sensitivity!D15='DCF'!B44` and every alternative row differenced from it by formula. |
| (r) | Every formula cell reproduces the model, and drivers propagate | **PASS** | `recalc.py` 626/626, 0 mismatches; `driver_test.py` on the delivered file: **17/17 drivers move the committed-capital headline in the asserted direction; 110 drivers swept, 3 cross-checks exempted by name, 0 dead inputs.** |
| (s) | Script-built files verified by cell-by-cell diff against the delivered file | **PASS** | Full pipeline rebuilt in the mirror from `beta_record.py` through `attest.py` and diffed: **15 numbers files identical** (`study_numbers`, `lenses`, `alternatives`, `experts`, `sensitivity_grid`, `input_register`, `beta_result`, `band_record`, `flat_rate_ladder`, `xlsx_expected`, `sweep_register`, `driver_test_result`, `qc_checks`, `recalc_result`, `attestation`); **workbook 16 sheets, 1,853 populated cells compared, 0 differ**; study docx 636 text units, 0 differ, 14 inline images both; bibliography docx 407 text units, 0 differ; **all 15 figure PNGs byte-identical**. The delivered files are what the committed builders produce — which is also why both blockers live in the builders and the register, not in a hand-edited file. |

---

## The gate — standing items

| # | Item | Verdict | Evidence |
|---|---|---|---|
| S1 | Step 0.0 data-quality gate | **PASS** | `step0_result.json` `dq_log`: "dropped 1 rows carrying a non-positive or non-finite OHLC value (2011-08-03)"; 3,755 clean rows at 240.8 sessions/yr against the EGX calendar; the same screen on both regression series (`beta_result.json` `stock_dq` identical, `index_dq: []`). |
| S2 | Step 0 calibration under the band record | **PASS, with two gaps named** | `data.js` BANDS.EGCH read through node: **n 57, hits 54, c50 56.1%, c80 87.7%, c90 94.7%, strength long, flag null, width 1.446**. Two-sided binomial of 54/57 against 0.90: p = 0.372 — no flag earned, and §3 says exactly that ("The record earns no flag either way") with the count printed beside every percentage. **Gaps:** the width ratio (1.45× a naive carry-anchored band, "disclosed, never gated") and the record-strength label ("long") are not stated in §3 — the width is described qualitatively ("wide because this share is volatile — an annualised 46.8%"). Note: the committed strike of 2026-08-06 (`strike_result.json`: ν 6.0, signal off) predates the 01-Sep EG refit (live `market_profiles.EG`: ν 5.0, width_cal 0.951, momentum signal active); §3 stands on the committed strike, correctly, and the two-part as-of stamp on the page carries the dates. |
| S3 | SIGCM (1) historicals official-only | **PASS** | `filings/`: seven company PDFs (FY2021/22–FY2024/25 annuals, Q1/H1/9M FY2025/26 interims) from the company's Mist-hosted portal via kimaegypt.com, plus ten older annuals for the calibration only; 188 L1 inputs each cite the filing, auditor and report date; `GAP_REVIEW` "LATEST FILINGS" table shows each period read and footed. Bibliography §4 lists the three third-party cross-checks and states none builds. |
| S4 | SIGCM (2) ground-up — on the driver record | **PASS** | `assert_ground_up()` on five `DriverLine`s in `compute.py` covering 100% of FY2026/27 revenue: **unit 74.4% (export urea — tonne, auditor's product-cost table, world f.o.b. Egypt US$/t through the derived currency path net of the 10% duty, gas m³/t + other materials/wages/services per tonne) · derived 24.4% (subsidised urea, free-market urea, nitrates — each with a gap note) · segment 1.2% (other — gap note) · topdown 0%** (`study_numbers.json['gates']['ground_up']`). Margins are outputs (Table 12 page 10: gross 45.7% → 33.0%, EBITDA 41.4% → 31.6%, "Not one margin in this table is set"). |
| S5 | SIGCM (3) debt split LC/FX | **PASS** | 31-Mar-2026: bank loans 14,386.1m + current 207.0m + holdco 45.9m = EGP 14,639m; **99.7% US-dollar** (`wacc.pct_debt_local` 0.0031); FX leg carried at local-equivalent cost year by year; the ANNA facility (US$82.9m + EGP 5,931m) in the cost-of-debt evidence table, page 14. |
| S6 | SIGCM (4) asset-conversion cycle → BS/CF | **PASS** | Table 16 page 13: DSO 26.8 / DIO 165.2 / DPO 83.2 from FY2024/25, CCC 108.9 days; A.3 projects receivables, inventory, payables and ΔNWC from those day counts (Table 4: ΔWC +858, −106, −92, −92, −108). |
| S7 | SIGCM (5) competitors | **PASS** | B.1 peer frame (Abu Qir, MOPCO, Helwan/NCIC with capacities); relative lens on the Egyptian listed range 6.0–9.9× (register `peer_ev_ebitda_low/high`); Abu Qir FY2025 figures used for comparison only. |
| S8 | SIGCM (6) beta — inspected on the record | **PASS** | `beta_result.json`: **β 1.0302, R² 0.250, SE 0.191, n 253 weekly, 4.89 y (2021-08-26 → 2026-07-16), Dimson, 90% CI 0.72–1.34, usable true, conforming true, index_file `raw_indices/EG/EGX30.csv`, index_asof 2026-07-22, interim_note null** (EGX-listed: no DFM interim, no QATAR10 caveat). Usability: n ≥ 24 ✓, R² ≥ 5% ✓, SE 0.191 < 1.030 ✓. Produced by `own_stock_beta()`; `assert_beta_provenance()` passes in `compute.py`, `beta_record.py`, `attest.py`. The as-of is quoted where the beta is built (§1.8: "as of 2026-07-22"); the withdrawn 35-name composite (1.0527) is under `superseded_composite`, named as withdrawn in §1.8 ("an equal-weight basket … a coverage artefact and not a market") and in the bibliography. |
| S9 | SIGCM (7) formula-based model | **PASS** | Rows (q)/(r): driver → IS → BS → CF → DCF live; 17 drivers reprice the headline; 0 dead inputs. |
| S10 | SIGCM (8) flags before issue / stop-and-inform | **PASS, with one note** | Flagged inputs: derived ANNA nameplate, gas share of materials (75%, "FLAGGED"), Abu Qir D&A estimate, ANNA wind-down cost; three NOT-REACHED sources logged with consequences. **Note (stands from the first pass):** `attest.py` builds `SIGCMChecklist` with every field True by construction — a self-attested boolean; the clauses are evidenced by the beta record, the ground-up record, `formula_audit.py` and the register rather than by that call. |
| S11 | Sweep Register primary-source depth | **PASS** | `sweep.py` re-run in the mirror: "PASS — 4/4 rings, 27 findings (1 B · 12 S · 7 D · 4 C · 3 NEG), driver gate 9 rows (9 bottom-up / 0 top-down) · primary access: attempted (7) · FS years: 4 [FY2021/22–FY2024/25] · IR findings: 1 · freshness: OK — sweep and delivery same day". `sweep_date` 2026-09-01. kimaegypt.com reached; egx.com.eg, cbe.org.eg, kima.com.eg NOT reached with reasons and consequences; study-year quarters Q1/H1/9M FY2025/26 swept before the build; COMPANY_IR tagged distinctly; results releases: the company publishes none — recorded as a negative result. Budget column registered and not consumed, now stated in driver row 4. |
| S12 | One escalator per driver class with a sourced near-term cost anchor | **PASS** | Gas on its administered dollar price × m³/t × the derived currency path; export price on the commodity path; subsidised price on its administered path; the domestic lines (other materials, wages, purchased services, inland haulage, other selling, administration) on the one domestic inflation path — `compute.py` lines 378–398 — with Table 11's caption now stating that rule and its reason. Unit costs anchored on the FY2024/25 auditor's product-cost table and the Q3 FY2025/26 reviewed margin; gas at 4.68 US$/mmBtu from the Q1 loss disclosure, contract 5.75 priced as the downside (−1.88). |
| S13 | Margins as outputs | **PASS** | Table 12 and `lenses.json`: gross and EBITDA margins fall out of tonnes × price less the cost stack; `driver_test.py` moves margins when gas, FX and price move. |
| S14 | 3y historical + 5y forecast, full FCFF waterfall to PV inline | **PASS** | A.1: FY2022/23–FY2024/25 actual + FY2025/26E + FY2026/27–FY2030/31; Table 4 page 4: Revenue → EBITDA → D&A → EBIT → NOPAT → +D&A → −capex → −ΔWC → FCFF (1,963 / 298 / −156 / −219 / 450) → discount rate from the glide (25.64% … 20.02%) → discount factor → **PV of FCFF (1,562 / 192 / −82 / −95 / 163)**; Table 5 the terminal block and EV → equity bridge, both cases. |
| S15 | v2 WACC | **PASS** | rf* = 23.00% − 6.37% (Egypt's own Damodaran adjusted default spread) = 16.63%; ERP 13.94% rating / 9.41% CDS, each with its own rf* (19.59% CDS); Ke 30.99% / 29.28%; market-value weights 65.5/34.5 on EGP 27.77bn market cap and gross debt EGP 14.64bn; both bases published (Table 17). Cost of debt through `wacc_builder.build_wacc()`: marginal (company's own facilities), FX leg at local-equivalent cost on the study's own wedge path (19.99% year one → 14.54%), local facility 19.40%; the two places it sits below the sovereign/rf* are disclosed in §1.8 and the sovereign-floored construction is priced (−0.44 on the carried-through lens) rather than argued. Terminal 18.24% from 5% inflation + 3.5% real rate + 9% long-run dollar coupon on the 2.54% terminal wedge. Cash charged once: gross-debt weights, net debt in the bridge (L-054). |
| S16 | Every non-sourced driver logged to `Fundamental_Driver_Ledger.md` | **PASS** | Lines 447–560: nine driver decisions with falsifiers, the eight constructed L5 drivers tabulated with values, plus the findings recorded but not acted on. |
| S17 | [R-FCAL-01] fundamental walk-forward | **PASS** | Scope stated in §7 page 24 in plain words ("Rebuilt as it would have stood at each of 13 year-ends from June 2012 to June 2024 … 55 tested cases"); `PRE_REGISTRATION_01-09-2026.md`, `TRAINING_RECORD_01-09-2026.md`, `BASIS_BREAKS_01-09-2026.md`, `corrections_log.json`, `scores.json`, `forward_ranges.json` in `engine/egch_walkforward/`; the study says it did NOT beat "no change" on net profit; years 3–5 published as ranges in A.4 from `forward_ranges.json['published_band']`; no correction adopted (all watch flags); lessons L-057, L-058, L-206, L-207 registered, 0 unruled; fair value frozen before the run. |
| S18 | Expert appendix in full | **PASS** | As row 7. |
| S19 | Crux sensitised in real observable units | **PASS** | Figure 9 / Table 19: long-run export price US$380–540/t × terminal cost of capital 17–24%, every cell a full re-run; Table 20 ranks nine swings in EGP/share. |
| S20 | Sweep Register validated | **PASS** | As S11. |
| S21 | **The answer** [R-GAP-01] | **PASS on the gate; blocker 1 is the reader-facing hole in it** | `study_numbers.json`: central **3.759** (field 0.00–15.47) against spot **13.98** on 2026-08-06 → **−73.1%**. `GAP_REVIEW_01-09-2026.md` carries all eight headings verbatim (LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK), updated for the rebuild (DISCOUNT RATE now records the year-by-year Kd, the two warnings and the priced floor; the tax-shield bound corrected from "+0.99 on the central" to +0.97 on the lens / +0.44 on the weighted central). `check_valuation_gap.py` lists EGCH as reviewed. The review's arithmetic matches the numbers file (25.64% / 18.24% / 1,740 / 3,512 / 66.9%). Internal-doc nit: "the stopped case 3.43 → about 3.0" under the floor is typed; `alternatives.json` prices the floor on the carried-through case only. And the number this gate audits is the one the delivered study never prints (row (i)). |
| S22 | What the filings disclose that the model does not consume | **Answered** | (1) The deferred-tax note — loss carry-forwards and the accelerated first-year deduction — not read at line level; the model charges 22.5% from year one against a company that has booked no current tax since the complex started (priced as a bound, +0.97 on the lens). (2) The 9M FY2025/26 auditor's flags (unmarked ABUK stake, Damietta stock overstatement, unissued electricity invoices, under-booked loan interest) listed in §7, not adjusted. (3) The company's FY2025/26 budget column — registered, scored, not consumed. (4) Board and shareholder-structure disclosure reports — colour only. (5) The cost-by-nature notes for three years that did not foot in the prior extraction — excluded rather than re-read from the rendered page, so the 75% gas share stays a flagged construction. (6) Electricity — named in the sweep, no line of its own in the cost stack. (7) New this pass: `live_data.json` holds a Damodaran block the register has since corrected — a second copy of a corrected input that one builder still reads (blocker 2). |

---

## The beta, on its record

| | Regressed against | β | R² | SE | n | 90% CI | Conforming |
|---|---|---|---|---|---|---|---|
| **Adopted** | EGX30, `raw_indices/EG/EGX30.csv`, as of 2026-07-22 | **1.030** | 0.250 | 0.191 | 253 weekly | 0.72–1.34 | yes |
| Withdrawn | 35-name equal-weight EGX composite (08-08 edition) | 1.053 | 0.283 | 0.105 | 257 | — | no — SIGCM clause 6 hard fail |

The lower confidence bound is priced as an alternative (+0.80 a share against −0.63). The index series ends 2026-07-22 and the stock's last regression observation is 2026-07-16, while the anchor close is 2026-08-06 — three weekly observations fall outside the regression; §1.8 quotes the as-of.

## What blocks delivery, shortest list

1. **The published central is not in the study.** `data.js` `fair.base` = 3.76 and `study_numbers.json['central']` = 3.759 (= 0.45 / 0.20 / 0.20 / 0.15 across carried-through cash flow, relative, normalised, book) appear in no delivered document; the summary table's central cell is "—", and the bibliography's `lens_weights` register row says the 08-08 edition published these weights when it did not. Print the weighted-central row with the weights inline (computed from `lenses.json`, covered by `prose_check.py`) and say in READ FIRST / §1.5 that the base figure on the site is built that way and why only the carried-through side enters it — or drop the point from the site and the numbers file and say so. Correct the register sentence either way.
2. **Bibliography page 2, "9.42% (CDS)".** `docx_biblio.py` reads `live_data.json['damodaran_egypt']` — a stale copy of an input the register corrected to 0.0941 on 9 August; retire `erp_cds` (0.09425, consumed by nothing) from `inputs.py`, point the bibliography's sovereign rows at the register, and either regenerate `live_data.json` from the same source or stop reading it for figures the register already holds. Add the correction-note exemption's blind spot to `prose_check.py` (a figure named in a "CORRECTED … the study had X" note must not license X in prose).

Non-blocking, to clear in the same pass: restate the two §1.8 builder warnings in the study's own words (the "looks like a raw FX coupon" diagnosis is contradicted by the next sentence); state the width ratio (1.45×) and record strength ("long") in §3 beside the band record; "11.79%" in §1.8 is the blended terminal cost of debt, not the dollar leg (11.77%); compute the seven string-literal cells in the bibliography's market table from the register; `figures.py` loads `backtest_5y.json` and never uses it; the half-blank page 16. Housekeeping: the 08-08 deliverables, `build_xlsx_egch.py` and `QC_GATE_08-08-2026.md` remain in the directory as the prior edition's record, and `data.js` still links the 08-08 files — publishing is a separate, explicitly-requested step and not part of this gate.

No rating and no price target appears in any deliverable.
