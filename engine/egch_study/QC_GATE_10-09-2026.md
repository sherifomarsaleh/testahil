# EGCH — QC gate, 10 September 2026

Egyptian Chemical Industries "KIMA" · Egyptian Exchange: EGCH · reporting and valuation
currency Egyptian pounds · edition `EGCH_Valuation_Study_10-09-2026` (Word + PDF),
`EGCH_Valuation_Model_10092026` (Excel + PDF), `EGCH_Bibliography_10-09-2026` (Word + PDF).

Filled from **outside** the study by an auditor who did not build it and who changed
nothing in it. Every gate below was **re-run**, not read off its last recorded output;
every repository gate was run from the repository root and every study gate from inside
the study directory. Nothing here is self-certified: each row names the artefact, the
command or the number that carries it, and where an item is met with a gap the gap is
stated in the same breath rather than passed over.

The 08-08-2026, 01-09-2026, 03-09-2026 and 05-09-2026 editions are superseded and were
not opened as current. Where a figure is compared against an earlier edition it is named
as such. `QC_GATE_05-09-2026.md` was read first, and every defect it recorded was re-tested
individually; the closing tally is at the end of this file.

---

## VERDICT

**NOT DELIVERABLE.** Nine defects reach a reader and block delivery. The model itself is
in good order: it rebuilds byte-for-byte from source, the workbook recalculates completely,
the beta is conforming on its record, the terminal is built through the sanctioned module,
and the two-branch answer is honestly constructed and correctly explained.

The blockers are concentrated in three places — §1.9's sensitivity grid, which no longer
contains the study's own answer; four typed claims that the study's own computed numbers
contradict; and the delivered workbook PDF, which mixes the two price clocks the Word
document is careful to keep apart. The blockers are listed in full at the end.

The study is also **HELD** under [R-GAP-02] independently of anything in this gate — both
branches sit more than 10% below the price — and that hold is correct and is not a defect.

**The two-branch explanation added at this edition is sound and matches the model.** It is
checked line by line at row 12 below and it survives every test put to it.

---

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker, exchange, market code | EGCH · EGX · EG | `assets/data.js` carries `code: "EGX:EGCH"`. The exchange is read from the code prefix, never inferred from the `raw_ohlc/EG/` folder. |
| Regressor the beta owes | **EGX30** | `wacc_builder.market_index_path('EG','EGX')` resolves to `engine/raw_indices/EG/EGX30.csv`. EG spans one exchange, so no ambiguity and no interim note is owed; not a DFM-listed name, not Qatari. |
| Company class | Gas-fed nitrogen fertiliser — **operating company, single site, one operating segment** | One reported segment; PPE plus construction in progress is EGP 18.7bn of EGP 33.6bn total assets at 31-Mar-2026. `lens_record.class = "petrochemical"`, which `research_protocol.LENS_REGISTRY` registers with DCF as the primary. |
| Reference pattern | **ADNOCLS** (operating company), inside the closed three-name set | `research_protocol.REFERENCE_SET == ('ADNOCLS','ADCB','ALPHADHABI')`, asserted at import. Neither the bank nor the holdco pattern applies; the study uses ADNOCLS's skeleton unchanged and `scripts/check_document_structure.py` lists EGCH among the 23 that MATCH THE MODEL REPORT. |
| Standard the study is stamped to | **2026.09.10** | `attestation.json` and `study_numbers.json` both carry `standard_version: "2026.09.10"`; `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` returns **2026.09.10**. They agree, and `scripts/check_standard_claim.py` does **not** list EGCH among the four studies claiming a standard they do not meet. |
| Price the answer is struck against | **EGP 14.41, 2026-09-03** | `study_numbers.json → spot / spot_date`. `scripts/check_spot_currency.py`: "EGCH 14.41 as at 2026-09-03, the latest supplied price". |
| Price the answer is audited against | **EGP 14.23, 2026-09-06** | The latest close in `engine/raw_ohlc/EG/EGCH.csv`. `GAP_REVIEW_10-09-2026.md` audits against it and says so. |
| The answer | **EGP 8.0748 carried through · EGP 11.6065 stopped · `central: null` by design** | `study_numbers.json → central_two_sided`. There is no single central and none is claimed; `scripts/check_lens_design.py` reads it as "two-sided primary dcf, branches [8.074785424, 11.60654207], 2 cross-checks". |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The six-artefact set exists and is all of one edition | **PASS** | All six are present and dated 10-09-2026: `EGCH_Valuation_Study_10-09-2026.docx` / `.pdf` (38 pages), `EGCH_Valuation_Model_10092026.xlsx` / `.pdf` (25 pages), `EGCH_Bibliography_10-09-2026.docx` / `.pdf` (22 pages). The model PDF is **new at this edition** — `bake_model_pdf.py` was added on 10-09-2026 and delegates to the shared `engine/bake_model_pdf_shared.py`, carrying only this study's two facts (its directory and its edition module). `edition.py` owns every artefact name, so no builder types one. `scripts/check_calibration_deliverables.py` was RED on EGCH solely because this file did not exist; writing it closes that row and nothing else. |
| 2 | The study calls the three gates in its own committed code, and they run | **PASS** | `assert_model_study()` at `attest.py:89`; `assert_sigcm()` at `attest.py:101`; `assert_beta_provenance()` at `attest.py:105`, again at `beta_record.py:30` and again at `compute.py`; plus `assert_ground_up()` at `compute.py:944`. `python3 attest.py` re-run end to end from inside the directory: all four print PASSED. `python3 scripts/check_study_provenance.py` from the repo root: 25 study directories, 8 outstanding, **EGCH is not among them**; "OK — no new violations". |
| 3 | Structure, sections and sheets match the model report | **PASS** | `attest.py`: "51 headings, 0 model-study sections missing"; "16 sheets in the model order, 0 mismatches". `scripts/check_workbook_structure.py` and `scripts/check_document_structure.py` both list EGCH among the 23 that MATCH THE MODEL REPORT. All 38 pages of the rendered study PDF were read as images and the 16 Word sections appear in the model order; the excluded section "What changed in these editions, and why" does not appear. |
| 4 | Depth bar 1 — standalone bibliography document | **PASS, with one defect named** | `EGCH_Bibliography_10-09-2026.docx/.pdf`, 22 pages, read as rendered images and as extracted text. All five required parts: a primary-documents table naming every filing with period, report date, auditor and retrieval date (ten annuals FY2009–FY2021 plus four audited years and three reviewed interims); the FULL input register of all **310** inputs grouped by layer — **L1 193 · L2 15 · L3 14 · L4 23 · L5 65**, summing to 310; a judgements table with what-would-overturn-it on each; a negative-results table of 6; and a third-party-discrepancy table of 3. The masthead reads **10 September 2026** — the 05-09 stale-masthead defect is fixed. **Defect:** one negative-results row is a full edition stale; row 21 below. |
| 5 | Depth bar 2 — four-field provenance on every input, validated by assertion | **PASS, with two gaps named** | `attest.py` builds `four_field` from `input_register.json` and reports **"310 inputs, all four-field complete"**; `assert_model_study()` refuses otherwise. **Gap (a):** `python3 source_audit.py` (in the directory, NOT in `run_all.py`'s pipeline) exits 1 — **"33 input(s) carry a source that describes a source rather than citing one"**, up from 31 at the 05-09 edition. Named among them: `rf_observed` ("Egypt ten-year government bond yield, market quote"), `usd_egp_spot` ("Market quote"), `urea_fob_egypt`, `subsidised_price`, `real_rate_lt`, `peer_*_capacity` ("Industry capacity survey"). A four-field check that tests only for a non-empty string is not a provenance check. **Gap (b):** `scripts/check_four_field.py` lists EGCH under **NO READABLE REGISTER** — the register is not inside the committed numbers file, so nothing outside the study can re-run the assertion. |
| 6 | Depth bar 3 — numeric traceability, builders read the numbers file only | **PASS** | `python3 qc_checks.py`: "4 builders, 0 typed financial numerals" across `docx_egch.py`, `docx_biblio.py`, `build_xlsx.py`, `figures.py`. `python3 formula_audit.py`: **"645 formulas across 16 sheets, embedded constants that are not structural: 0"**. `scripts/check_prose_figures.py` from the repo root: EGCH "prose figures checked: **475** across 3 documents; **unmatched: 0**". `scripts/check_numbers_generators.py`: "EGCH compute.py declares: compute.py, lenses.py". |
| 7 | Depth bar 3 — the delivered workbook recalculated independently | **PASS** | `python3 recalc.py` on `EGCH_Valuation_Model_10092026.xlsx`: **"645 of 645 formula cells reproduce the model, 0 unresolvable, 0 unchecked"**, plus **16/16** headline reconciliations. Nothing was unparseable, so nothing is reported as a skip. `python3 cross_check.py`: "the model, the delivered workbook and the delivered study state the same 16 headline figures". The workbook is 645 formula cells against 1,269 populated non-formula cells — **34% live**; the three permitted pasted classes are named on the READ FIRST sheet. |
| 8 | Script-built files verified by a **cell-by-cell diff** against the delivered file | **PASS** | The whole repository was copied to a sandbox and the study rebuilt from source in `run_all.py`'s own order (`beta_record → inputs → compute → lenses → experts → alternatives → diagnostics_egch → flat_rate_ladder → band_record → figures → build_xlsx → docx_egch → docx_biblio`), then diffed against the delivered files. Workbook: **1,914 populated cells compared across all 16 sheets, 0 differences.** Study Word: **664 paragraph-and-table rows, 0 differences.** Bibliography Word: **420 rows, 0 differences.** All nine committed numbers files byte-identical: `study_numbers.json`, `lenses.json`, `alternatives.json`, `experts.json`, `input_register.json`, `contested_judgements.json`, `diagnostics.json`, `band_record.json`, `beta_result.json`. A clean recalculation is necessary and not sufficient; this is the sufficient half. |
| 9 | Depth bar 4 — external-reader scrub, zero internal vocabulary, zero verdict tokens | **PASS, with two ambiguous hits disclosed** | `python3 qc_checks.py`: "3 documents, 73 patterns, **0 hits**, 3 ambiguous". Measured independently on the **rendered PDFs**, which the study's own scan does not open: the 72 extractable patterns run over `EGCH_Valuation_Study_10-09-2026.pdf` and `EGCH_Bibliography_10-09-2026.pdf` return **0 hits each**; the workbook PDF returns **2**, both in their ordinary sense and both already disclosed by the study's own check — "forecast roll-forward" on a balance-sheet schedule and "non-overlapping three-month windows" describing the sampling scheme in the sentence that uses it. `band_record.assert_no_verdict_tokens()` was run over **all six** delivered artefacts (study .docx/.pdf, bibliography .docx/.pdf, workbook .xlsx/.pdf) — **clean on every one**. `scripts/check_delivered_vocabulary.py` lists EGCH among the 23 CLEAN studies. Calibration evidence lives in §3 as plain-language sentences with the statistics inline; the document has exactly three appendices (A statements, B peers and risk, C expert panel) and **no calibration appendix**. |
| 10 | Depth bar 5 — figure discipline, and every figure inspected as a rendered image | **PASS on the canvas, FAIL on one figure read as a reader** | Opacity verified independently, file by file: **15 PNGs, every one RGBA with `alpha_min = 255` and corner luminance 249** — solid light canvas, zero transparency. All 38 pages of the study PDF were rendered at 110 dpi and read as images, so every one of the 14 delivered figures was inspected in place. **Two findings.** (a) **Figure 14 carries a label collision** that reaches a reader: Expert 2's range label "EGP 5.25 – 9.48" is overprinted by the "market price EGP 14.41" callout, so the two strings run together on the page — recorded at row 27. (b) `fig8_price.png` is built by `figures.py` and **inserted into no delivered document** — 15 built, 14 delivered, unchanged from the 05-09 edition. `qc_checks`'s count of 15 therefore includes one figure no reader ever sees. |
| 11 | Depth bar 6 — table discipline, three counts per document | **PASS programmatically, with one reader-visible break named** | `qc_checks.py`: "57 tables checked, 0 problems". Measured independently from outside, per document and **both ways** — off the declared cell widths AND off the `tblGrid` the renderer actually reads (the L-014 defect class): **study document, text block 7.00in, 44 tables — over-wide 0, starved 0, bloated 0; bibliography, text block 9.80in, 13 tables — over-wide 0, starved 0, bloated 0; grid-versus-cell disagreements 0 in both.** Every table carries a fixed grid. **Named gap:** Table 19's third column header is too narrow for its own text and breaks mid-word on every page it spans — "On the alternati / ve". No width check in this repository measures a header against the word it must hold, so it passes all three counts and still reaches a reader. |
| 12 | Depth bar 8 — the central contested judgement computed both ways, and **does the two-branch explanation match the model?** | **PASS — checked line by line against the model, and it holds** | Published in all four required places and averaged in none: the **summary table** (two cash-flow rows, 8.07 and 11.61, and an answer row saying "an average would describe a half-built plant"); the **body** (§1.1 Table 5 runs both columns from PV of the explicit window to value per share; §1.7 prices the gap at EGP 3.53 a share / EGP 7,016m of equity); the **workbook** (Summary, Fundamental Valuation and SOTP Bridge each carry both columns); and **an expert's range** (C.3: 7.91 carried through, 10.52 stopped). `study_numbers.json` carries `central: null` by design and `central_two_sided` with both branches, and `scripts/check_lens_design.py` reads it correctly as two-sided. **§1.7's new explanation was tested claim by claim against the model and every claim reproduces.** The gap: 11.6065 − 8.0748 = **3.5318** ✓, × 1,986,578,999 = **EGP 7,016m** ✓. "Every driver outside this table is the same in both": base and halt explicit-window revenue are **identical to the pound in all five years** (12,643.0 / 13,736.2 / 14,610.7 / 15,424.3 / 16,239.0) and both branches carry the same terminal WACC of 16.877% and the same base EBIT of 6,666.58 ✓. "The plant earns nothing until after the window closes": no project revenue appears in any explicit year ✓. "The remaining EGP 14,688m": base total capex 15,498.7 less halt total 1,810.6 is 13,688.2, and Table 14 discloses the missing EGP 1,000m as the stopped branch's own cost of walking away, so 13,688 + 1,000 = **14,688** ✓. "Earns 1.69% after tax on the money sunk into it": base terminal NOPAT 5,509.79 less halt 5,166.60 = **343.19**, over the approved EGP 20,342m = **1.687%** ✓, and Table 15 labels it "at half nameplate" so the 50% utilisation the figure rests on is disclosed, not hidden. "Stopping is worth more because the money leaves early and the earnings arrive late": the whole of the 7,016 is the explicit window (halt PV 12,214.7 against base 4,970.6, +7,244.1) net of the plant's terminal contribution (base PV of terminal 17,439.0 against halt 17,210.9, only **+228.1**) — the plant buys EGP 228m of present value for EGP 13,688m of spend, which is a stronger statement than the one the study makes ✓. "Not a range to average" and "three things this comparison is NOT" are both accurate. **One imprecision, not a defect:** the same sentence offers the EGP 14.7bn as the reason stopping wins when EGP 1.0bn of it is spent in both worlds; the difference that does the work is EGP 13.7bn, and Table 14 directly above supplies the correction. |
| 13 | Step 0.0 — data-quality gate | **PASS on the gate, FAIL on the series it was run over** | `python3 step0.py` now **runs** — the 05-09 abort at the retired PARITY assertion is fixed. Re-derived independently through `data_quality.clean_ohlc`: stock **3,756 raw → 3,755 clean rows**, 2011-01-02 to **2026-08-06**, 15.6 years at 240.8 rows/yr, max abs log move 0.199 against the EG artefact threshold of 0.290, flat fraction 0.050, one row dropped (2011-08-03, non-positive OHLC). The committed `step0_result.json` reproduces exactly. **FAIL on the input:** the series is the study's own local copy `EGCH_Stock_Price_History.csv`, which stops at **2026-08-06** while `engine/raw_ohlc/EG/EGCH.csv` holds **20 more sessions to 2026-09-06** (close 14.23). Recorded at row 22. |
| 14 | Step 0 — calibration under the band record | **PASS** | `python3 band_record.py` re-run, which hands `assets/data.js` to **node** and reads the object the page renders rather than regexing the file: **n=57, c50 0.5614, c80 0.8772, c90 0.9474, width 1.446, strength long, flag None.** §3 states exactly that in plain language with **the count printed beside every percentage**. The two-sided binomial at 5% returns no flag and none is claimed. The width ratio is **disclosed and explicitly not gated**. Separately, `step0.py`'s own skill read reproduces: 17 post-break windows, scale-normalised skill **+0.0233**, verdict BOUNDARY(PARITY-flagged), coverage 0.65 / 0.88 / 0.94, PIT mean 0.520, w90 ratio 1.106. |
| 15 | The published price map reproduces the committed engine fit | **PASS** | The 05-09 FAIL is fixed. `python3 strike_egch.py` re-run reproduces `strike_result.json` exactly — 1M p5 12.12 / p50 14.26 / p95 16.77, P(above anchor) **59.1%**; 3M p5 10.84 / p50 14.77 / p95 20.09, P **63.1%** — and Table 23 and Figures 11–13 carry those same numbers. The committed `nu 5.0`, `width_cal 0.951` and `signal_active true` all match the live `market_profiles.PROFILES['EG']` read at audit time. |
| 16 | SIGCM clause 1 — historicals from official sources only | **PASS, with the outside check unable to see it** | Every historical figure in the L1 layer (**193** inputs) cites a named filing with its auditor and report date — four audited years (FY2021/22 comparatives through FY2024/25, Central Auditing Organization with PKF Rashed Badr or alone) and three reviewed interims (Q1, H1, 9M FY2025/26, Nasr Abou El Abbas / Morison Global). The filings are held at `engine/egch_study/filings/`. The bibliography names aggregators and press explicitly as cross-checks that inform no figure. **Named gap:** `scripts/check_source_integrity.py` reports EGCH as **UNREADABLE** — "the input register is not in the committed numbers file, so no source can be read from it" — so this clause cannot be verified from outside the study by any instrument. On the ratchet with five others. |
| 17 | SIGCM clause 2 — the ground-up clause, on the driver record | **PASS on the split, FAIL on one line's record** | `compute.py:924-943` builds five `_rp.DriverLine` records and line 944 calls `_rp.assert_ground_up(DRIVER_LINES, ticker='EGCH')`, which refuses on anything short of 100% of revenue. The share-by-level split, read from `study_numbers.json → gates.ground_up`: **unit 76.65% · derived 22.24% · segment 1.11% · topdown 0%**, five lines, 100.0% of revenue. All four lines below `unit` carry a gap note, each naming what the filings do not disclose. **Defect:** the `unit` line — export urea, 76.6% of revenue and the largest single line in the study — declares `price_basis="world urea f.o.b. Egypt in US$/t through the derived currency path, **net of the 10% ad-valorem duty**"`. That duty was repealed and this edition runs it at nil. The SIGCM record that documents how the biggest revenue line is built describes a levy the model no longer charges. Separately, `scripts/check_ground_up.py` records EGCH as "commits the assertion's OUTPUT (share_by_level) and not the driver lines it was computed from, so nothing outside the study can re-run it". |
| 18 | SIGCM clause 6 — the beta, inspected on its record, never on a boolean | **PASS** | `beta_result.json` read directly: **`index_file: "raw_indices/EG/EGX30.csv"`** — under `engine/raw_indices/`, so not a constituent composite; `index_asof 2026-09-08`; market EG, exchange EGX, matching the `EGX:` prefix in `assets/data.js`; frequency weekly on `W-THU`, the EGX's own Sunday-to-Thursday week; **β 1.0198, R² 0.2477, SE 0.1905, n 256** over 4.94 years, 2021-09-23 to 2026-09-03. The usability gate is met on all three legs (n ≥ 24, R² ≥ 5%, SE < \|β\|), so no fall to a peer beta or 1.0 is owed; the 90% interval 0.706–1.333 and the Blume cross-check 1.0132 are published in §1.9 and the lower bound is priced in Table 19 (EGP 9.29). `interim_note: null` and this is not a DFM-listed name, so no interim note is owed; not Qatari, so no QATAR10 caveat is owed. The withdrawn 35-name equal-weight composite (β 1.0527) is recorded in the same file **as withdrawn**, with the reason. No study-local regression script survives; the beta comes from `beta_regression.own_stock_beta()` through `beta_record.py`. |
| 19 | SIGCM clauses 3, 4, 5, 7, 8 | **PASS, with the attestation's own shape named** | **(3) Debt split LC/FX:** `cost_of_capital_record.kd_integrity` — 0.31% local, 99.69% foreign, established facility by facility from notes 18-1, 18-2 and 18-3 **before** the rate was set; the dollar leg carried at local-equivalent cost, 24.35% in year one gliding to 13.90%. **(4) Asset-conversion cycle:** Table 17 gives receivable, inventory and payable days from the audited statements (26.8 / 165.2 / 83.2) and the balance sheet and cash flow are projected from those day counts — no plug. **(5) Competitors:** Appendix B carries Abu Qir, MOPCO and NCIC with capacity and role, used only in the relative lens and labelled as cross-checks. **(7) Formula-based:** 645 live formula cells, driver→IS→BS→CF→DCF, verified by `recalc.py` and by `driver_test.py`. **(8) Flag-before-issue:** the bibliography logs four reached and three not-reached URLs with what each failure cost, and six negative searches. **Named gap, unchanged from 05-09:** `attest.py:98` constructs `SIGCMChecklist(**{f: True for f in ...})` — every clause set True by the study on itself. Clauses 1, 2 and 6 are separately evidenced above from records; the other five rest on that blanket attestation, which is the exact shape [R-ENF-01] closes. |
| 20 | Sweep Register — primary-source depth | **PASS on depth, FAIL on currency** | `sweep_register.json` re-read: **"4/4 rings, 27 findings (1 B · 12 S · 7 D · 4 C · 3 NEG), driver gate 9 rows (9 bottom-up / 0 top-down), primary access: attempted (7), FS years: 4 ['FY2021/22','FY2022/23','FY2023/24','FY2024/25'], IR findings: 1"**. `scripts/check_sweep_module.py` lists EGCH among the 17 that RUN THROUGH THE SHARED REGISTER. Company site attempted and **logged either way** — `kimaegypt.com` reached, the Mist IR portal reached, `egx.com.eg` NOT reached (bot challenge, and the consequence named: the share count comes from note 14 instead), `cbe.org.eg` NOT reached, `kima.com.eg` refused. **Four audited fiscal years**, the target rather than the floor. All three disclosed study-year quarters swept in. Exactly one COMPANY_IR finding, which is the floor, and the bibliography explains why ("None exists. The company publishes filings and disclosure reports only"). **FAIL:** `sweep_register.json` carries `sweep_date: 2026-09-01` and has not been re-run since; `sweep.py:408` still calls `R.check_freshness(SWEEP_DATE)`, comparing the sweep date with itself, so the freshness check cannot fire. The consequence is concrete rather than theoretical — recorded at row 23. |
| 21 | *(defect)* A delivered bibliography row describing a driver this study retired | **FAIL — unrepaired from the 05-09 edition** | Bibliography, "Negative results", row 3: *"A maintenance capital-expenditure guide or investment plan beyond the project — None found. **Maintenance capital expenditure is set at a mature-plant standard** and sensitised."* The study shipped beside it, §1.7: *"maintenance capital expenditure is now set at the 1.1% of revenue the company has actually paid, **not at a three per cent mature-plant standard**. That standard was an assertion, it was 2.7 times the pooled rate of the two pre-project years."* `maint_capex_pct` = **0.011157**. The bibliography states as current a construction the study states as retired, in two documents delivered in the same envelope. This was recorded at row 42 of the 05-09 gate and is unchanged. |
| 22 | *(defect)* One study, two price histories | **FAIL** | `engine/raw_ohlc/EG/EGCH.csv` holds 3,776 rows to **2026-09-06** (close **14.23**). `engine/egch_study/EGCH_Stock_Price_History.csv` holds 3,756 rows to **2026-08-06** (close **13.98**) — 20 sessions behind. The beta reads the repository library through `beta_regression.own_stock_beta()` and its `last_obs` is **2026-09-03**; the price map, Step 0.0, Step 0 and the whole technical section read the study's private copy and stop at **2026-08-06**. Two different price histories inside one document. Two consequences reach a reader: §2 opens *"The shares closed at EGP 13.98 on 2026-08-06"* and computes the 52-week range, the relative strength index, the average true range and the entire level ladder in Figure 10 on data a month older than the price on its own masthead; and Table 23 and Figure 12 publish a one-month probability map **"to 2026-09-06"**, a date that had already passed when this edition issued on 10 September and whose outcome (14.23) sits in this repository. The document discloses that the anchor and the valuation run on different clocks; it does not disclose that the anchor is older than the data available, or that one of its two horizons has closed. `scripts/check_escalations.py` records `EGCH-ohlc-refresh` as **resolved** — the library was refreshed and the consumer's private copy was not. |
| 23 | The Sweep-Register question: **what do the filings disclose that the model does not consume?** | **PASS, with two named and one structural gap** | Asked mechanically first: `prose_check.py` reports **277 of 310** register inputs consumed by a builder and names the **33** that are not. Most are legitimate — Q1 and H1 figures superseded by the 9M the model does use; `budget_net_9M`, the company's own budget, deliberately excluded because guidance is scored and never consumed; peer figures used only in the relative lens; design plates consumed by `compute.py` rather than by a builder. **Two are worth stating.** (a) `maint_capex_pct_observed` = 0.0111570 is byte-identical to `maint_capex_pct` = 0.0111570, the key the model actually reads — one fact registered under two names, which is the defect class this repository closed on PHAR's cost of debt and SWDY's share count four commits before this edition. (b) `anna_nameplate` = **189,863 t/yr**, the ammonia-surplus derivation, sits in the register consumed by nothing while the model runs **264,000 t/yr** from the EPC award; the two disagree by 39% and the entire "1.7% return on the approved cost" rests on the higher one. It is named in `driver_test.py` as an exempted cross-check rather than hidden, and the direction is against the study's own conclusion (the lower nameplate would make the programme look worse), so this is disclosed rather than concealed. **The structural gap is the one that matters:** the sweep has not been re-run since **2026-09-01** and its freshness check compares its own date with itself, so nothing in this study tests whether a filing appeared in the nine days before issue. The study's single largest fact change at this edition — the repeal of the export levy — entered through `inputs.py` and `alternatives.py` and **never entered the Sweep Register at all**. |
| 24 | The export levy — **verified against a primary source?** | **FAIL — NOT INDEPENDENTLY VERIFIED, and the study says so itself** | The study registers `export_duty_2026 = 0.0` sourced to *"Ministerial Decision 340 of 2026, effective 1 August 2026, cancelling Decisions 190, 203 and 258 of 2026 … The Egyptian Customs Authority issued Circular 46 of 2026 to govern shipments whose declarations were opened before the levy ceased"*, and retains the repealed 10% as `export_duty_2026_superseded` sourced to Decree 258 of 2026 with its Gazette date. The claim is worth **EGP 2.98 a share** — it is the reason the central moved from 5.0966 to 8.0748 at this edition. **This auditor could not verify it from a primary source and does not accept it on the study's citation.** Every route out of this environment failed: DuckDuckGo (HTML and lite) returned a bot challenge with code 4458; Mojeek returned HTTP 403; Bing returned generic Egypt results with the query stripped, in English and in Arabic; `mti.gov.eg` reset the connection; `customs.gov.eg`, `gafi.gov.eg`, `egx.com.eg`, `enterprise.press` and `zawya.com` all loaded but render their search results client-side and returned no decree text. **No copy of Decision 340, Decree 258 or Circular 46 is held anywhere in this repository** — `engine/egch_study/filings/` holds only the company's own statements. **The study itself concedes the point in a delivered document**, in Table 19's own cell: *"What would improve the citation: the Gazette text of Decision 340 itself rather than the decree register and the customs circular that implement it."* An empty result is a failure, not a skip: the largest single driver of this edition's answer rests on an uncorroborated secondary citation, and it must be closed with the Gazette text or an escalation before this study issues. |
| 25 | *(defect)* §1.9's sensitivity grid does not contain the study's own answer | **FAIL — and it is worse than at the 05-09 edition** | `sensitivity.py:11` hardcodes `WACCS = [0.17, 0.185, 0.204, 0.22, 0.24]`. The model's own terminal cost of capital is **16.877%** — **below every row in the grid**. Line 10 hardcodes `PRICES = [380, 420, 460, 500, 540]` and line 19 builds each column as the glide `[p+90, p+60, p+30, p+10, p]`, while the base case holds the export price **flat at US$530**. So Figure 9 and Table 20 — the study's own crux, on "the two observable inputs that decide it" — contain neither the discount rate the model uses nor the price path it runs, and the published central of **EGP 8.07 appears in no cell**. A reader locating the base on the grid at roughly 530 and roughly 17% reads the corner cell, **EGP 9.64** — 19% above the answer on the masthead. The contamination spreads into Table 21, which the document introduces with *"Each anchor below is varied independently around its own base"*: its top row, **"Long-run export price · US$380 to US$540 · −1.06 to 7.34 · swing 8.40"**, is read off the grid's middle row at a 20.4% terminal rate, and its sixth row, "Terminal cost of capital · 17.0% to 24.0% · 2.20 to 4.65", off the 460/t column. Neither span contains EGP 8.07, and Table 21 then **ranks those two spans against five that were measured at the base**, putting the export price at the top of the list. At the 05-09 edition this was a gap worth about EGP 1.00 a share; the terminal rate has since moved from 18.588% to 16.877% and walked off the axis entirely. |
| 26 | *(defect)* Four typed claims the study's own computed numbers contradict | **FAIL** | Each number below is computed; each claim around it is typed, and nothing asserts it. **(a)** Page 4, under Figure 1: *"The widest of them is the gas price … worth EGP 3.00 a share. The narrowest is terminal growth, worth EGP 2.81."* Hardcoded to `AL_BY['gas']` and `AL_BY['terminal_growth']` at `docx_egch.py:194-197`. `alternatives.json` read directly: **glide −4.93**, gas −3.00, export duty −2.98, terminal growth −2.81, … project profile **−0.20**. The widest is the glide and the narrowest is the project profile. The same document's Table 19 caption, which **is** computed, says so on page 19: *"The two that move the answer most are glide (−4.93) and gas (−3.00)."* **(b)** §1.7, page 14: *"The EGP 3.53 between them … is larger than either the multiple lens or the book-value lens moves for any other reason in this study"* (`docx_egch.py:701`). The summary table on page 2 prints the multiple lens at **14.25 to 25.60**, a span of **EGP 11.35**, moved by nothing but the multiple chosen; and the book lens at **8.16 disclosed against 0.00 justified**, a span of **8.16**. Both are larger than 3.53. **(c)** §1.8, page 17: *"7.0% inflation … **compounded** with a 3.5% real rate gives a normalised risk-free rate of **10.50%**"* (`docx_egch.py:907`). 1.07 × 1.035 − 1 = **10.745%**. The figure 10.50% is the **additive** sum, which is what the model uses (`drivers.rf_star_terminal = 0.105`) and what the house path returns, so the number is right and the word describing how it was built is wrong. A reader who checks the arithmetic finds it does not reproduce. **(d)** The delivered workbook's READ FIRST sheet (`build_xlsx.py:127`): *"Whether that programme is carried through or stopped is worth more than three pounds a share — **more than twice the central estimate itself**."* The gap is 3.53 and the central is 8.07; 3.53 is 44% of the central, not twice it. The sentence was true two editions ago and is carried in `lenses.py`'s module docstring as well. |
| 27 | *(defect)* The delivered workbook mixes the two price clocks the study keeps apart | **FAIL** | `build_xlsx.py:1232` writes the **valuation** spot into a cell labelled *"Spot at the anchor date"* — the Probabilistic Price Map sheet prints **"Spot at the anchor date 14.41"** with **"Anchor date 2026-08-06"** on the row below. The close on 2026-08-06 was **13.98**, and the Word document says so in terms: *"Read every percentile below against EGP 13.98, not against the price on the masthead."* Two further consequences on the same sheet: the "Against spot, 1M / 3M" columns (lines 1226-1227) measure a distribution simulated from 13.98 against 14.41; and line 1229 labels `p_above` — the probability of finishing above **13.98** — as *"Probability the price ends above today's"*, printing **59.1%** three rows above a median of 14.26 that the same sheet shows finishing **−1.0%** against 14.41. A reader is told the price has a 59% chance of ending above a number the median finishes below. |
| 28 | *(defect)* A figure caption that says the opposite of its own figure | **FAIL** | Page 6, Figure 2: *"Operating profit is healthy throughout and **free cash flow is not**, because the capital programme absorbs it. This is the study in one picture."* The figure plots free cash flow to the firm **above zero and rising in every one of the five years** — 1,251 / 1,589 / 1,709 / 2,075 / 3,032, each labelled on the bar. Table 6 on the facing page states it outright: *"Free cash flow is **positive in 5 of the five forecast years**."* The caption survived the export-levy repeal, which is what turned the line positive. Found only by reading the rendered page as an image; no programmatic check in this repository can see it. |
| 29 | *(defect)* The published field contradicts two numbers printed on the same page | **FAIL, small — unrepaired from the 05-09 edition** | The summary table's ANSWER row reads *"The range shown is **the floor and ceiling of every read**"* and gives **7.37 to 19.93**. The same table prints the Book value row at a central of **0.00** and the cash-flow carried-through row at a range low of **3.99**; Figure 1 on page 4 draws the book lens as a bar at **0.00**. `lenses.json` shows the cause: `synthesis.field` uses the **disclosed book of 8.1578** for that lens while the table and the figure use the **justified multiple of 0.00**. Three numbers under two names for one lens, with the stated floor sitting above two reads on the same page. §1.5 adds a third phrasing — *"The bear and full readings are the floor and ceiling of every read"* — which describes a different pair again, and carries "full" where it means "bull". |
| 30 | *(defect)* Expert 3's published low is a literal, described as something else | **FAIL, small — unrepaired from the 05-09 edition, and it now propagates** | C.3 states *"Range: EGP 0.00 to EGP 8.04 a share"* and *"the claim is worth EGP 7.91 a share (**EGP 0.00 to 8.04 across the volatility range**)"*. `experts.py:188` sets `low=0.0` as a hardcoded literal. Table 35, the expert's own grid, has a **minimum of 6.82 anywhere** and runs **7.82 to 8.12 across the volatility range** at the published five-year maturity. The 0.00 now propagates twice further than it did at 05-09: Figure 14 draws Expert 3's bar from 0.00; the text beneath reads *"the panel spans EGP 0.00 to EGP 9.48"*; and Table 36's row **"The panel against the cash-flow lens −8.07"** is computed at `docx_egch.py:1633` as `min(expert lows) − 8.0748` = 0.00 − 8.07. The sentence beside that −8.07 says *"Every expert method treats it more kindly than a year-by-year cash-flow model does"* — a negative gap says the opposite. |
| 31 | *(defect)* A register count typed into two delivered documents and gone stale | **FAIL, small** | `alternatives.py:126` types *"the weakest-sourced input in a **304-input register**"*, which reaches a reader twice — in the summary table's alternative-readings block on page 2 and again in Table 19 on page 18. The register holds **310** inputs, `attest.py` prints "310 inputs, all four-field complete", and the study's own **About** section on page 38 says *"Every input behind this study — **310** of them"*. One document, two counts, four pages apart. `prose_check.py` does not reconcile it because 304 is not a financial numeral it tracks. |
| 32 | Cost stack — one escalator per driver class, with a sourced near-term anchor | **PASS** | Table 11 and its caption: gas escalates on its **administered dollar price through the currency path**; the export price on **its own commodity path**, held flat in nominal dollars with the convention stated; the subsidised price on **its own administered path**; and the domestic lines — other materials, wages, purchased services, inland haulage, other selling and administration — share **one domestic inflation path**, with the reason printed ("that is the index they are paid in; a globally traded input is never put on it"). No blended index runs across physically distinct classes. **Sourced near-term anchor:** gas at **EGP 9.16/m³** in FY2026/27, derived from the company's own Q1 loss disclosure (31,313,235 m³ valued at EGP 251mn), reviewed 13-Nov-2025, with the contract formula price carried as the priced downside (EGP 3.00 a share). |
| 33 | Margins as OUTPUTS wherever the filings disclose enough to build cost per unit | **PASS on construction, FAIL on the opening level** | Table 12 prints revenue, cost of sales and gross profit for all five forecast years with the margin as the **residual**, labelled "an OUTPUT, never an input" in the workbook and "Falls out of the above" in Table 11; Figure 6 decomposes the FY2026/27 cost per tonne of urea into its six physical drivers (gas EGP 7,336/t, other materials 2,442, depreciation 1,697, inland freight 1,248, administration and other 1,450, wages and services 598). `driver_test.py` confirms no margin is an input. **FAIL on the level:** `scripts/check_forecast_anchor.py` records EGCH at **gross margin, latest 0.3839, forecast 0.4988, +11.49pp**. The gate does not fire because its opening-year clause only tests a forecast that opens BELOW the filed record. The forecast opens **11.5 points above the last audited year, 3.9 points above the best of the three audited years printed in the study's own Table 10 (45.9% / 32.7% / 38.4%), and 6.0 points above the company's own part-year FY2025/26 estimate of 43.87%** that `forecast_anchor.note` itself records. The repealed duty explains about 4 points of it; the remaining ~6 points has no mechanism named anywhere, and the record's declared mechanism (`input_cost_outpacing_price`) explains the **decline** across the path, not the step up into it. Nowhere in the delivered document are the forecast opening margin and the latest audited margin put on the same page. |
| 34 | 3y historical + 5y forecast, full FCFF waterfall shown inline to PV of FCFF | **PASS** | Appendix A carries FY2022/23, FY2023/24 and FY2024/25 actual plus four balance-sheet dates and FY2026/27–FY2030/31 forecast. Table 4 in §1.1 shows the waterfall inline and **does not stop at FCFF**: Revenue → EBITDA → less D&A → EBIT → NOPAT → add back D&A → less capital expenditure → less change in working capital → **free cash flow to the firm** → **discount rate from the glide** → **discount factor** → **present value of free cash flow**, then the terminal block and the enterprise-to-equity bridge in Table 5. Re-added by hand off the printed page: FY2026/27 3,790 + 891 − 2,740 − 690 = **1,251** ✓, and correct in all five years; 1,251 × 0.7960 = **995** ✓; the PV row 995 / 1,025 / 906 / 917 / 1,127 sums to **4,971**, the figure the bridge then uses ✓. Table 5 foots to 8.07 and 11.61 on both branches ✓. `scripts/check_sign_convention.py` is clean book-wide with nothing on its ratchet. |
| 35 | WACC built under the v2 cost-of-capital method | **PASS, with one construction disclosed and priced** | Table 18: local ten-year government yield **23.00%** (market quote 6-Aug-2026, corroborated by a listed treasury coupon of 23.10%), **less the sovereign's own default spread** — 6.37% on the rating basis, 3.41% on the market/CDS basis — giving normalised risk-free rates of **16.63%** and **19.59%**, labelled "Built, not quoted". Country risk therefore enters **once**. **Both ERP bases are published** (13.94% rating / 9.41% CDS) and the study states which is central, why, and that neither is mixed with the other's risk-free rate. Ke 30.65% / **29.08%**; **market-value weights 66.2% / 33.8%, never book** (market capitalisation EGP 28,627m against debt of EGP 14,639m); WACC 26.66% / **25.63%** gliding to **16.88%**. **Marginal Kd above the sovereign:** blended pre-tax cost of debt **24.34%** against the 23.00% sovereign. The 19.40% local facility does sit below the sovereign; the book is 99.69% dollar so the all-local-currency condition does not bite, and §1.8 **discloses the exception and prices the floored alternative** (EGP 6.03 against 8.07, and says the disclosed rates lean toward the traded price rather than away from it). `scripts/check_ke_reproduction.py` reads EGCH's record and it reproduces; `scripts/check_cost_of_capital.py`: "EGCH EG, 25.63% gliding to 16.88%". |
| 36 | The terminal, rebuilt through the sanctioned module | **PASS, with one record not readable from outside** | `compute.py` imports `terminal_value` and calls `TV.build(TV.TerminalInputs(...))`; the committed `terminal_record` carries rule R-TERM-01, real growth **0.0** with nominal **7.0%** derived from the house path, `maintenance_basis: book_dna_escalated`, `average_age_years` **measured** from the accounts, and `useful_life_years: 22.07` sourced to note 6 of the audited FY2024/25 statements. TV 46,909 carried through against a labelled floor of 30,510; implied terminal payout 84.1%, inside [0,1]. `scripts/check_terminal_basis.py`, `check_terminal_record_shape.py`, `check_terminal_spread.py` and `check_terminal_floor.py` are all clean and EGCH is listed under "built on something else (the cycle does not equal 1/g)". **Named gap:** `scripts/check_terminal_module_use.py` reports EGCH as "module — **no record committed**", because the record is nested at `cases.{base,bull,bear,halt}.terminal.terminal_record` rather than at the top level the gate reads. The record exists and is correct; the gate cannot see it. |
| 37 | Macro coherence — one house path, every inflation-class input declared | **PASS — the 05-09 defect is closed** | `scripts/check_macro_coherence.py` passes EGCH (FERTIGLOBE is the only failure). The 05-09 defect — a committed `terminal.rf` of 12.50% against a model running 12.885% and a document printing 12.88% — is **resolved**: `macro_record.terminal.rf` is now **0.105**, `drivers.rf_star_terminal` is **0.105**, and `macro_path.load('EG')` returns `terminal_rf = 0.105` from `terminal_inflation 0.07 + real_rate_convention 0.035`. All three agree, the study no longer holds a private view of the terminal risk-free rate, and `cost_of_capital_record.rf_terminal_note` says so. The **word** describing the construction is still wrong in the delivered document — row 26(c). |
| 38 | The currency anchor's age [R-COC-01] | **FAIL — and the gate cannot see it** | The EG currency anchor is dated **2026-08-06**; this study is struck at **2026-09-03**. That is **28 days against a 14-day bound** — the identical breach that `scripts/check_macro_anchor_age.py` lists for ADIB, AMOC, ARCC and PHAR, and one day worse than SCEM's. EGCH does not appear on that list because the gate reads `meta.spot_date` and `meta.currency` from the numbers file, and `study_numbers.json` commits **no `meta` block at all** — it carries `spot_date` and `spot` at the top level under different names. Measured by hand from the gate's own `paths()` function: `(date(2026,9,3) − date(2026,8,6)).days = 28`, `MAX_AGE_DAYS = 14`. No `anchor_staleness_accepted` declaration is committed, so this is not an accepted exception; it is an invisible one. |
| 39 | The anchor ordering — the profit anchor against the study's own sheet | **FAIL, recorded on the ratchet** | `scripts/check_anchor_ordering.py`: "EGCH — the bridge stands on `balance_sheet_date` **2026-03-31** while the forecast is anchored on `latest_reviewed_date` **2025-06-30** — **274 days BEHIND** its own balance sheet. Both come out of the same reviewed filing, so the study opened it and took half of it, and no `anchor_ordering_reason` declares why." EGCH's gap is the largest of the three studies on this list (ARCC 181 days, SCEM 90). The study reads the nine-month interim to 31 March 2026 for the equity bridge, the translation loss and the company's own budget column, and anchors its forecast on the audited year to 30 June 2025. |
| 40 | Every non-sourced driver logged to the Driver Ledger, and the rebuild ledger current | **FAIL** | **The rebuild ledger is a full edition behind the answer it claims to route.** `rebuild_ledger.json` records three levers ending at **`value: 4.0396`** with `cumulative_move: +74.8%`, and `scripts/check_rebuild_ledger.py` reports "EGCH 3 lever(s), 2 rule(s), cumulative +74.8%". The delivered central is **8.0748**. The two largest moves of all are missing from the route: the strike that took 4.0396 to 4.8990 (recorded only in `edition.py`'s docstring) and the export-levy repeal that took 5.0966 to 8.0748, worth **EGP 2.98 a share** — a +100% move on top of a ledger that stops at +74.8%. Its own `note` still reads "the programme-stopped branch runs 6.2591 → 7.8807" and "BOTH REMAIN FAR BELOW THE TRADED 14.41 — −73.0% and −45.3%", against delivered branches of 8.07 and 11.61 at −44% and −19%. Separately, `engine/Fundamental_Driver_Ledger.md` still carries the EGCH section written for the **01-09-2026** edition — the 05-09 gate recorded that at its row 28 and it is unchanged. |
| 41 | Lens design — one class primary, the rest as cross-checks | **PASS** | `scripts/check_lens_design.py`: EGCH conforming, "two-sided primary dcf, branches [8.0748, 11.6065], 2 cross-checks". The DCF is the primary with a range basis in an observable unit; the relative multiple is published as a **cross-check** and the disclosed book as a **floor never weighted**. The retired 45/20/20/15 blend is recorded as retired and published unused (EGP 9.09) so a reader of the previous edition can see what changed; `scripts/check_lens_vocabulary.py` lists EGCH among the 13 clean studies and `check_published_lens_vocabulary.py` is clean. |
| 42 | Output records [R-ENF-05] — the reverse read and the sign test | **PASS, with the record's own comparison named** | The reverse read is published and well: §1.1 Table 7 solves the flat nominal rate that reproduces the traded price on the study's own drivers — **15.0%** against a sovereign ten-year yield of **23.0%** — and the headline and §4 both state it as the disagreement rather than asserting the market is wrong. `scripts/check_output_records.py` lists EGCH among the 16 **conforming**, with "17 judgements, 15 material, sign test p=1.00". **Named gap:** the printed comparison in that gate's own line reads "the dollar export price of urea, free on **of 630.77 against the study's 8.07**" — it sets a reverse-solved export price in dollars a tonne against a value per share in pounds. The two operands are in different units and the comparison the gate prints is meaningless as written. |
| 43 | Table totals and instructing waterfalls | **PASS programmatically** | `python3 footing_check.py`: "tables examined: **52**; unreconciled totals: **0**", and `scripts/check_table_footing.py` reports the same. `scripts/check_waterfall_assertions.py`: EGCH **OK**, **7** waterfall tables, the builder importing `table_residual` and calling it. Read as a reader, the six instructing tables on pages 5–9 and 14 all foot — including Table 9, whose missing tax, net-debt, non-operating and share-count rows were the 05-09 gate's row 39 blocker and are now all printed (10,429 − 3,998 − 1,276 − 303 − 662 − 590 = 3,601 → −891 = 2,710 → −610 = 2,100 → ×10 = 21,003 → −10,032 + 3,664 = 14,635 → ÷1,986.6 = **7.37** ✓). |
| 44 | Balance-sheet footing | **FAIL, one period, EGP 0.055m** | `python3 footing_audit.py` (in the directory, **not** in `run_all.py`'s pipeline) reports: "FY2223 assets 18,727.840 claims 18,727.785 **gap +0.055** ← DOES NOT FOOT"; FY2324 −0.001, FY2425 −0.000, M9FY2526 +0.010, all inside rounding. Pre-existing and unchanged from 05-09; named here rather than rediscovered. The A.2 balance sheet printed to the nearest million is unaffected in substance. |
| 45 | The walk-forward scope decision, stated in the study | **PASS on scope, FAIL on actuation** | `study_numbers.json → walkforward_scope`: `scope: "FULL"`, 18 sourceable fiscal years, 13 origins FY2012–FY2024, 825 scored cells; `scripts/check_walkforward_scope.py`: "EGCH FULL, 18 years, run". §7 states it to a reader in plain language including the result that goes against the study — *"on the profit line its average miss was 1.6 times the miss of carrying last year's figure forward"* — and the consequence, which is far-year forecasts published as ranges; `scripts/check_forward_ranges.py` confirms the study prints one. **FAIL:** `scripts/check_walkforward_actuation.py` records EGCH — "the log applies corrections the pre-registered rule does not adopt: **admin, selling**. Under the previous rule these were legitimate; under this one they are not, and a correction the rule declines and a study applies anyway is fitting." On the outstanding list, so recorded debt rather than a new breach. |
| 46 | Depth bar 7 — expert appendix at maximum detail, "Expert 1/2/3" only | **PASS** | Each of the three carries worldview / when-it-works / when-it-fails / a worked table with every intermediate line / a named sensitivity with numbers / a falsifier stated in advance. Expert 3 runs 11 lines from enterprise value including non-operating assets through the strike, the volatility, the dilution adjustment and the per-share figure, plus a 3 × 5 maturity/volatility grid (Table 35) whose published cell is named. C.4 carries three challenges each conceded or rejected with the mechanism given; C.5 the three in one room; C.6 a divergence table isolating six pairs and the single assumption that drives each. Headings read "C.1 Expert 1 — …", "C.2 Expert 2 — …", "C.3 Expert 3 — …"; **no persona name appears anywhere** in either document, checked by full-text search of both PDFs. Both sides of the contested judgement appear in an expert's range (C.3: 7.91 and 10.52). The 0.00 low is a defect of that appendix and is recorded separately at row 30. |
| 47 | Driver test and the dead-input sweep | **PASS** | `python3 driver_test.py` on the delivered workbook: baseline **EGP 8.0748**, **17 of 17 drivers move the headline in the asserted direction**. The two changes at this edition are both sound. `class ABS(float)` (line 85) lets a bump be stated as an absolute level, and the export-duty assertion is **re-pointed rather than relaxed**: with the levy repealed to nil, a multiplicative bump of zero is zero and the old test "printed OK while touching a driver it no longer moved"; it now sets the duty to the repealed 10% the register still carries as superseded and asserts the same direction on the same mechanism, moving **−2.9782**. The terminal-growth assertion is corrected from "down" to "up" with the measured curve recorded (−1.87% real → −0.0575, zero → zero, +0.93% → +0.0217, +2.80% → +0.0407, +4.67% → −0.0062) and the honest finding stated — the whole span moves the answer by under seven piastres. Dead-input sweep: **116 drivers swept, 0 dead**, with **4 cross-checks exempted by name rather than silently**. **One gap:** `driver_test.py`'s own module docstring still argues at length for the retired "terminal growth has no first-order effect … the test therefore asserts the SECOND-order property" construction and quotes per-share values from an edition when the headline was negative (−1.3031). The file's header now contradicts the file's own test. |
| 48 | The study's committed gate outputs are current, not stale | **PASS** | Every gate in `run_all.py`'s verification tail was re-run **inside the study directory** — `recalc.py`, `formula_audit.py`, `cross_check.py`, `prose_check.py`, `driver_test.py`, `qc_checks.py`, `attest.py` — plus `footing_check.py`, `footing_audit.py`, `source_audit.py`, `band_record.py`, `step0.py` and `strike_egch.py`. `git status --porcelain engine/egch_study` returns **empty** afterwards: every committed result file reproduced byte-for-byte. `scripts/check_recalculator_target.py`: "egch_study DERIVED — target is derived, not typed — cannot go stale", because `edition.py` owns the filenames. |
| 49 | Artefact currency [R-ENF-06] and record survival | **PASS** | `scripts/check_artefact_currency.py`: "EGCH `contested_judgements.json` built against **8.0748**" and "EGCH `diagnostics.json` built against **8.0748**", both among the CURRENT artefacts and neither among the undeclared. `scripts/check_record_survives_rebuild.py`: "EGCH compute.py → lenses.py **survives**". `scripts/check_records_follow_the_answer.py` and `check_delivered_pdf_currency.py` both clean — "every delivered PDF carries the answer its study publishes". |
| 50 | The answer against the price, and the gap review [R-GAP-01] | **PASS** | `scripts/check_valuation_gap.py`: "EGCH **−43.3%** GAP_REVIEW_10-09-2026.md (audits 8.0748)", inside the OUTSIDE THE BAND, AND REVIEWED list; **OK — no new violations**. Both branches are more than 10% BELOW the price: 8.0748 against the struck 14.41 is **−44.0%** and against the latest known 14.23 is **−43.3%**; 11.6065 is **−19.5%** and **−18.4%**. `GAP_REVIEW_10-09-2026.md` exists, is **300 lines**, and covers **all eight headings** — LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK — and carries **AUDITED CENTRAL markers for both branches**, an AUDITED SPOT of EGP 14.23 at 2026-09-06 and an AUDITED GAP of −43.3%, so it cannot go stale silently against either branch. The standard the study is stamped to, 2026.09.10, is the live `research_protocol.STANDARD_VERSION`, which names this artefact. |
| 51 | The publication block [R-GAP-02] | **PASS — the study is correctly HELD** | `scripts/check_publish_block.py`: "**HELD EGCH** — every branch is more than 10% BELOW the price (highest Cash-flow lens, capital programme stopped at −18.4%) — and no market dissent is filed". A two-sided answer is held only if every branch breaches, and both do. No `MARKET_DISSENT` file exists and none is claimed. Nothing in this edition may reach the live site, and the study does not claim otherwise. **Related and worth stating:** `scripts/check_published_gap.py` records "EGCH −74.4% … the site publishes **3.64**" — the live site still carries a fair value from a superseded edition while the current study says 8.07/11.61. EGCH is on that gate's outstanding list rather than a new breach, and the hold is exactly why the site has not moved. |
| 52 | The operating asset base's vintage [R-ASSET-01] | **PASS** | `scripts/check_asset_base.py`: 11 studies in scope, **3 conforming, and EGCH is one of them** — it is not among the 8 outstanding. The committed `asset_base_record` states the urea nameplate of **574,875 t/yr as at 2025-06-30**, sourced to note 28 of the audited FY2024/25 statements (1,575 t/day × 365), with the ammonia plate at 438,000 t/yr and the ammonium-nitrate line under construction at 264,000 t/yr carried separately and labelled **"IT IS NOT IN A FILING"**. The base is not older than the information set. `scripts/check_asset_base_wired.py` is clean. |
| 53 | The sensitivity of the answer to the drivers, ranked and priced | **PASS on the alternatives, FAIL on the ranking table** | Thirteen alternatives are each a complete re-run of the model with one component moved, published in Table 19 with the choice made, the alternative, the value and the delta, and argued both ways. `alternatives.json` read directly reproduces every one. **FAIL:** Table 21, which ranks the swings, mixes two incommensurable measurements — five rows measured as full re-runs at the base, and two read off the §1.9 grid at a discount rate and a price path the model does not use — and then ranks them together. Recorded in full at row 25. |

---

## What blocks delivery

Nine defects reach a reader. Eight were found only by reading the rendered PDFs as images
or by reading the study's own numbers files against its printed pages; none is visible to
any programmatic check in this repository.

1. **Row 25** — §1.9's sensitivity grid contains neither the discount rate the model uses
   (16.877%, below every row of a 17.0%–24.0% axis) nor its price path (flat US$530 against
   a glide axis), so the published central of EGP 8.07 appears in no cell. Table 21 then
   ranks two spans read off that grid against five measured at the base.
2. **Row 26(a)** — "The widest of them is the gas price … the narrowest is terminal growth",
   under Figure 1, contradicted fifteen pages later by Table 19's own computed caption
   ("glide −4.93 and gas −3.00") and by `alternatives.json`.
3. **Row 26(b)** — "larger than either the multiple lens or the book-value lens moves for any
   other reason in this study", against a multiple lens printed at 14.25–25.60 on page 2.
4. **Row 26(c)** — "7.0% inflation compounded with a 3.5% real rate gives 10.50%". Compounding
   gives 10.745%; 10.50% is the additive sum the model actually uses.
5. **Row 26(d)** — the delivered workbook's READ FIRST sheet: the contested gap is "more than
   twice the central estimate itself". It is 44% of it.
6. **Row 27** — the workbook's price-map sheet prints "Spot at the anchor date 14.41" above
   "Anchor date 2026-08-06", whose close was 13.98; measures the whole distribution against
   the wrong one of the two prices; and reports a 59.1% probability of finishing above a
   number its own median finishes 1.0% below.
7. **Row 28** — Figure 2's caption says free cash flow "is not" healthy above a figure showing
   it positive and rising in all five years, and against Table 6 on the facing page saying
   "positive in 5 of the five forecast years".
8. **Row 22** — the study runs on two price histories: the beta on the repository library to
   3 September, the price map, Step 0 and the whole technical section on a private copy that
   stops on 6 August. §2's technical read and Figure 10 are a month stale, and the one-month
   probability map is published for a window that had already closed.
9. **Row 24** — the export-levy repeal, worth EGP 2.98 a share and the single largest driver
   of this edition's answer, is **not verified against a primary source**. No copy of
   Decision 340, Decree 258 or Circular 46 is held in this repository, every search route out
   of this environment was blocked, and the study concedes the gap in a delivered document.

Five more are defects of record rather than of the reader's page, and three of them would
mislead the next study rather than this one's reader:

10. **Row 40** — the rebuild ledger stops at 4.0396 while the study publishes 8.0748, so the
    two largest levers of this edition are not in the route at all; and the Driver Ledger
    entry is still the 01-09-2026 one.
11. **Row 38** — the study is struck 28 days after its own currency anchor against a 14-day
    bound, and is invisible to the gate because its numbers file commits no `meta` block.
12. **Row 17** — the SIGCM ground-up record still declares the 76.6% revenue line as priced
    "net of the 10% ad-valorem duty" that this edition repealed.
13. **Row 33** — the forecast gross margin opens 11.5 points above the last audited year and
    6.0 points above the company's own part-year estimate, with no mechanism named for the
    step up and no page putting the two side by side.
14. **Row 20 / 23** — the Sweep Register has not been re-run since 1 September, its freshness
    check compares its own date with itself, and the edition's largest fact change never
    entered it.

Three are small and reader-facing: **row 21** (the bibliography's negative-results row still
describing the retired mature-plant maintenance standard), **row 29** (the published field's
floor sitting above two reads printed on the same page), **row 30** (Expert 3's invented 0.00
low, now propagated into Figure 14, the panel span and Table 36's sign), and **row 31** (a
"304-input register" on two pages of a 310-input study).

**Not blocking, and correct as they stand:** the study is HELD under [R-GAP-02] with both
branches more than 10% below the price and no dissent filed (row 51); its eight-heading gap
review is complete and marked with both audited centrals (row 50); the study rebuilds
byte-for-byte from source — 1,914 workbook cells, 664 study rows, 420 bibliography rows, all
zero differences (row 8); the workbook recalculates completely, 645 of 645 (row 7); the beta
is conforming on its record against the published index of the exchange the share is listed
on (row 18); the terminal is built through the sanctioned module on a measured asset age
(row 36); the macro-coherence defect the 05-09 gate recorded is closed (row 37); the price
map now reproduces the live engine fit (row 15); Step 0.0 runs (row 13); the six-artefact
set exists at one edition (row 1); and **the two-branch explanation added at this edition is
accurate line by line against the model** (row 12).

---

## The 05-09-2026 gate's defects, re-tested one by one

| 05-09 row | What it recorded | Now |
|---|---|---|
| 36 | "Every lens that does ask lands below EGP 4.29", false | **FIXED** — the headline now reads "THE TWO LENSES THAT DO ASK SPLIT ON THE ANSWER", and both 8.07 and 11.61 are below the price |
| 37 | "the largest of the priced alternatives", contradicted by its own caption | **RECURRED in a new place** — row 26(a) |
| 38 | Two costs of equity, the second unlabelled | **FIXED** — §6 no longer quotes the rating basis unlabelled |
| 39 | Table 9's printed rows did not reach its printed answer | **FIXED** — tax, net debt, non-operating assets and the share count are all printed (row 43) |
| 40 | The Disclosure block named the wrong date and price | **FIXED** — 10 September 2026, price 2026-09-03, price-map anchor 2026-08-06, all three separated (page 38) |
| 41 | The bibliography masthead four days stale | **FIXED** — reads 10 September 2026; `edition.py` now owns every artefact name |
| 42 | A bibliography row describing a retired driver | **UNREPAIRED** — row 21 |
| 43 | Two register inputs citing a filing that does not carry them | **FIXED** — `bs_gross_fixed_M9FY2526` and `bs_acc_dep_M9FY2526` are gone from the register |
| 44 | The workbook PDF unreadable at its own margins | **PARTLY FIXED** — the Assumptions page now carries its labels and the SOTP Bridge now carries both columns; READ FIRST still truncates every long line at the right edge, and the price-map sheet has a worse defect (row 27) |
| 45 | The field floor above a lens central on the same page | **UNREPAIRED** — row 29 |
| 46 | The sensitivity grid did not contain the central | **WORSE** — row 25 |
| 47 | Expert 3's published low a literal | **UNREPAIRED and propagated** — row 30 |
| 25 | The macro record's terminal rf against the model's | **FIXED** — row 37 |
| 28 | The Driver Ledger a full edition stale | **UNREPAIRED**, and the rebuild ledger has joined it — row 40 |
| 49 | The auditor's disclosed 1,200 m³/t gas rate unused and unpriced | **PARTLY FIXED** — it is now priced as an alternative worth +EGP 0.94 a share (Table 19), though the model still runs the constructed 1,292 |
| 12 | `step0.py` aborted on a retired assertion | **FIXED** — row 13 |
| 14 | The strike ran against a superseded engine fit | **FIXED** — row 15 |
| 53 | FY2223 balance sheet does not foot by EGP 0.055m | **UNREPAIRED** — row 44 |

---

## What could not be verified, stated rather than implied

- **The export-levy repeal.** Row 24. Not verified; the attempts and their exact failures are
  listed there rather than summarised as an environment limitation. This is the largest open
  item in this gate.
- **The OCR route on the filings.** The audited and interim PDFs carry no text layer, which
  corroborates the study's claim that every figure was read off the rendered pixels. The
  filings are in Arabic and the extraction itself was **not** independently re-read.
- **SIGCM clause 1 from outside.** `scripts/check_source_integrity.py` cannot read this
  study's register at all, so the official-source-only clause is evidenced in row 16 from the
  register's own contents and from the filings on disk, not by an outside instrument.
- **The five SIGCM clauses that rest on a blanket boolean** (3, 4, 5, 7, 8) are evidenced in
  row 19 from the delivered documents, which is the strongest available substitute; the
  attestation itself proves nothing about them.
- **`fig8_price.png`** is built and delivered nowhere, so its content was checked as a file
  but has no rendered context to be judged in.
- **Whether a filing appeared between 1 and 10 September 2026.** The sweep has not been run
  since 1 September and its own freshness check cannot fire (rows 20 and 23), so this gate
  cannot say whether the FY2025/26 annual — the study year's own full-year statements — has
  been issued.
