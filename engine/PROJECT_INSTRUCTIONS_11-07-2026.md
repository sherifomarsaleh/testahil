DIGEST REVISION 2026-08-24f — [R-DOC-01] every copy of this block carries this line as its FIRST characters. If the copy you are holding does not, or carries an earlier revision, it is STALE: the current text is engine/PROJECT_INSTRUCTIONS_11-07-2026.md on the repo's default branch and nothing else is authoritative. Bump the revision on every edit, however small. TESTAHIL — Standing Research Protocol (condensed). Full detail lives in Standing_Research_Protocol.md, Rollforward_and_Grading_Protocol.md, and Source_Integrity_and_Ground_Up_Mandate.md in project files. THIS BLOCK CONTAINS RULES, NOT NUMBERS. Every calibration figure (ν, width_cal, panel sizes, verdicts, which names FAIL, signal state, stock counts) is VOLATILE — the unattended pipeline refits them whenever a stock is posted. NEVER quote a fit from this block, from memory, or from any document: all three go stale the moment a stock is added. ALWAYS read the live state first: curl -s https://raw.githubusercontent.com/sherifomarsaleh/testahil/main/engine/market_profiles.py curl -s https://raw.githubusercontent.com/sherifomarsaleh/testahil/main/engine/fitted_configs.json The repo is PUBLIC — no token needed. engine/market_profiles.py IS THE SINGLE SOURCE OF TRUTH (it is what production reads). fitted_configs.json is a DERIVED MIRROR and panel_hashes.json a cache — never hand-edit either. UPDATE THIS BLOCK ONLY WHEN A RULE CHANGES, NEVER WHEN A NUMBER CHANGES. HORIZONS ARE CALENDAR-ONLY, EVERYWHERE: 1 month / 3 months (and 12 months for metals). The retired session-counted horizon names never appear in any deliverable, page, code comment, doc, or conversation. A forecast's check date is a CALENDAR DATE resolved at strike by engine/horizons.py (anchor + N calendar months, month-end clamped, rolled forward to the exchange's first real trading session) and it is graded ON that date regardless of how many sessions the window held; session counts are projected only at strike time to size the cone (h1/h3 via the blend projection, never hardcoded 20/60). DEFAULT BEHAVIOR — TWO DIFFERENT TRIGGERS, SAME INPUT SHAPE, DO NOT CONFLATE THEM: (a) Ticker with NO existing coverage/study + OHLC → run the FULL study end-to-end without asking: Step 0.0 → Step 0 → sweep → financials → build → QC gate → deliver. Publishing to the live site is a separate, explicitly-requested step. [CHANGED 07-Aug-2026] "PUBLISH" MEANS LIVE ON testahil.com, NOT "prepared for publishing" — once the word is said, go all the way: build → verify → commit → push → PR → MERGE to main → confirm deploy-pages went green on the merge SHA, with no stop to ask for a token (that gate was removed, not relaxed — this instruction is the authorisation). Run `python3 scripts/publish_site.py --ticker TK --ship [--republish]`, which owns the whole mechanical tail and fails loudly at any step; full detail and the canonical prompt live in Publish_Protocol.md. (b) Already-covered ticker + fresh OHLC (or "roll forward {TICKER}" / "recalibrate and forecast {TICKER}") → the lighter ROLL-FORWARD workflow below, NOT a new study. If a ticker already has a study/ticker page/ledger cohorts, fresh OHLC for it means (b), never (a). STEP 0.0 — DATA-QUALITY GATE (engine/data_quality.py; mandatory before ANY calibration, fit, or study). No series enters a panel without it. Detection is principled, not a guessed threshold: each exchange's own DAILY PRICE LIMIT defines what one session can physically do, so a move beyond it can only be a corporate action or a data error. Thresholds are PER-MARKET (EGX ±20%, Tadawul ±10%, ADX ±15%, QE ±10%, KOSPI ±30%, NSE ±20%; US/metals no limit). A GLOBAL THRESHOLD IS WRONG — an EGX-calibrated cutoff would falsely "repair" a legitimate Korean limit-down. The gate drops pre-listing/non-trading placeholder rows and back-adjusts unadjusted corporate actions. STANDING RULE: screen a market's trading-day density against that exchange's REAL calendar before trusting any fit built on it. Vendor corruption is PER-EXPORT — never assume a file is clean because another from the same vendor was. This gate exists because real corruption was found INSIDE a live production fit. STEP 0 — CALIBRATION GATE: 5-year walk-forward on the 3-month calendar window, non-overlapping windows, scored against a CARRY-ANCHORED lognormal RW benchmark (carry = ln(1+rf) − ln(1+q)), so skill can never harvest the time-value of money. The gate is SCALE-NORMALIZED (crps/spot): raw CRPS is denominated in price, so pooling it weighted every panel by SHARE PRICE, not information — one name once carried ~58% of an entire panel. Verdict is three-way and pooled: the market-panel CI is the standing gate; a name-level FAIL must be ROBUST across bootstrap block sizes {2,3,4} (a block-dependent sign flip = BOUNDARY, recorded PARITY-flagged, never a silent proceed). Break filtering applies to the CALIBRATION SAMPLE (adopted on out-of-sample evidence, not assertion). MC ENGINE v3 (engine/mc_v3.py + engine/market_profiles.py), "carry-anchored YZ-HAR-t". 50,000 paths, seed 42. mc_v2.py is legacy reference only, never the production default. Raw secular drift and unshrunk trend drift stay RETIRED (do-not-revive). Per-market (ν, width_cal) are fitted by MLE on pooled LONO-cross-fitted residuals. SIGNAL STATE IS PER-MARKET AND IS SET BY ABLATION ON THE PANEL, NEVER BY ASSUMPTION — read the live profile; do not assume any market runs a signal, and do not assume any market doesn't. ν IS WEAKLY IDENTIFIED — NEVER quote it as precise. On some panels every ν from 5 to Gaussian sits inside the 95% likelihood interval. ν trades off against width_cal. The (ν, width_cal) PAIR is what is fitted; the honest object is the cone they jointly produce, not either coordinate alone. THREE-LENS INDEPENDENCE [R-LENS-01, ADOPTED 23-Aug-2026, per instruction — investor-critique session]: the FUNDAMENTAL study, the MC price engine, and the TECHNICAL read are INDEPENDENT lenses — no lens's output is ever an input to another; they are shown side by side so agreement between them is information, not an echo. Binding consequences: (1) MC DIRECTION, if any, comes ONLY from price-native signals fitted inside the engine's existing per-market signal socket (signal_type/ic/ic_by_h/signal_active), promotion-gated as ever — NEVER from a fair value. The Fundamental_MC_Integration_Protocol's Phase C engine hook (value-gap IC into profile.ic/drift) is PERMANENTLY RETIRED as a drift source; the value-gap IC may still be MEASURED as a diagnostic, and fv_overlay remains a downstream COMPARISON layer (it reads both lenses' outputs to compare them, feeds nothing back). (2) TECHNICAL-FAMILY constructions (moving-average distances, 52-week-high proximity, RSI and kin) are INELIGIBLE as MC drift signals even where they test well — they belong to the technical lens; the eligible pool for an MC lean is the momentum family (12-1, 6-1) and other constructions the technical read does not use. [R-DRIFT-01] COMMITTED DRIFT [ADOPTED 23-Aug-2026, per instruction — "we have to find a way to commit to a drift up or down" — and UPGRADED the same day per instruction — "the tilt is still very conservative"]: the momentum lean is ACTIVE through the engine's signal socket in the markets whose momentum cells survived the 23-Aug-2026 direction tournament's four joint tests — robust same-sign CI across bootstrap blocks {2,3,4}, LONO sign-stability, split-half consistency, cross-framing agreement (evidence base of record: engine/direction_tournament/RESULTS_23-08-2026.md and COMBO_MOMENTUM_23-08-2026; on 23-Aug-2026 that meant AE and EG on mom_combo — equal-weight 12-1 + 6-1 — and SA on mom_12_1, where the combo measured WEAKER and was not adopted: a newer candidate that tests worse does not ship. THE LIVE PROFILE IS THE TRUTH if this list has moved). ic is PER-HORIZON via profile.ic_by_h at each horizon's OWN measured value (a min-horizon shrink was tried and retired the same day); a carried-across-horizon ic (a 3M value carried from a 1M measurement) is DISCLOSED wherever quoted. Socket knobs: dead zone 0.25, z clip 2.5, alpha cap 0.75σ — the ICs were measured on raw z, so the knobs track evidence, not conservatism. THE TILT NEVER EXCEEDS ic × σ × z: beyond that point a bigger number is a deliberately worse forecast, not more commitment, and the client is told so plainly. EVERY COVERED NAME'S FORECAST STATES A DIRECTION CALL — the sign of its own momentum z — even inside the dead zone (call printed as WEAK, tilt 0); every strike records signal_z/signal_alpha (strike_cohorts), every call is graded at its maturity, and a sustained failed-direction record triggers the standing out-of-cycle review. Markets with no surviving cell stay carry-centered, call-only on product surfaces. Per [R-DOC-02]: verify against the repository whether the 23-Aug committed-drift engine commits have merged to main before describing production as tilted — the live profile is the truth. [R-DRIFT-02] PER-NAME DISCIPLINE ON THE TILT: the per-stock record for every covered name is maintained under the exact production construction (first sweep engine/direction_tournament/PER_STOCK_CAREFUL_23-08-2026.md; full seed-paired ON-vs-OFF production backtest TILT_BACKTEST_ALL93_23-08-2026 — read those files for numbers, never this block). The ONLY sanctioned per-name exception is the PRE-REGISTERED suppression bar: a name's tilt is suppressed iff, at either horizon, its own-history IC is a robust FAIL across blocks {2,3,4} AND split-half both-halves-negative AND n≥40. Anything weaker is a WATCH FLAG — recorded, graded live, revisited at every refit, never acted on. Per-name tilt exceptions outside this bar are curve-fitting and PROHIBITED; a suppressed name still prints its call, flagged low-confidence. [R-NEG-01] THE DOCUMENT-TECHNIQUES NEGATIVE CONTROL (engine/doc_techniques_backtest/, 23-Aug-2026): GBM-historical-drift, ARIMA-family, pooled neural nets, Markov/fuzzy state chains, Kalman adaptive drift, month-of-year seasonality and their ensemble ALL FAILED to beat the carry center out-of-sample on 15 years (most robustly worse) — none may return as a drift source without new evidence clearing the promotion rule; AE month-of-year seasonality is a flagged rank-signal candidate only. WIDTH FLOORS ARE MEASURED FACTS, not modelling choices: the middle half of real 3-month moves spans roughly 13% (AE), 19% (SA) and 26% (EG) of price, so an honest 50% band cannot average narrower at these horizons whatever the technique — the honest levers on an individual name's width are the per-name width overlay (its own gate, below — which moves that name's width in whichever direction its own history demands, not always down) and the horizon — and the honest lever on the MIDDLE of a market's cone is the guarded mid-band shape selection [R-SHAPE-01] (which calibrates the 25–75 band at a FIXED 90% edge) — and HORIZONS STAY 1M/3M by instruction (23-Aug-2026): a shorter-horizon product was offered and declined. [R-SHAPE-01] GUARDED MID-BAND SHAPE SELECTION [ADOPTED 24-Aug-2026, per instruction — investor session, "reshape UAE and Egypt to make it less conservative"]: ν IS WEAKLY IDENTIFIED, so the MLE breaks a tie the likelihood cannot see, and shapes on the SAME iso-90% ridge (width_cal × T95(ν) held EXACTLY constant — the very quantity R-CAL-01's materiality metric watches, so every ridge point publishes an IDENTICAL 90% edge) differ visibly in how wide the 25–75 band is. AT EVERY REFIT, panel_refresh.reshape_mid_band() (called inside refresh_market after the MLE+shrink fit) moves production to the ridge point whose 50% band catches closest to half IFF ALL FIVE PRE-REGISTERED GUARDS PASS, else silently keeps the MLE shape: G-flat (ΔLL ≤ 3.0 vs the unconstrained MLE — inside the 95% joint likelihood region — and cal inside the shrink legality clip); G-improve (closes ≥ 1 point of |cov50 − 50%| — SA's adoption-day decline: nothing to fix); G-split (BOTH calendar halves move strictly toward 50% — EG's adoption-day decline: its mid-band over-coverage lives only in the late half while the early half already UNDER-covers, so no single shape helps both); G-lono (pooled leave-one-name-out coverage improves, each name scored under a shape selected WITHOUT it); G-crps (proper score not ROBUSTLY worse across bootstrap blocks {2,3,4} — the house robustness bar mirrored). THE GUARDS ARE THE RELEASE, per the R-CAL-01 lesson: a market reshapes automatically at any refit where all five pass and reverts to MLE the refit they stop passing — no per-market flag to remember; READ fitted_configs.json's mid_band_reshape FIELD for live state, never this block. THE 90% EDGE NEVER MOVES BY CONSTRUCTION — this is CALIBRATION OF THE MIDDLE, never narrowing, and R-NEG-01's width floors stand (the objective itself pins the 50% band to catching half). NOT THE RETIRED CRPS-SELECTION MISTAKE: that chased an in-sample score across the whole parameter space; this chooses among likelihood-equivalent shapes on ONE ridge and releases only through out-of-sample guards. THE PROMOTION RULE BINDS INSTRUCTIONS TOO: on adoption day EG was NAMED in the instruction and still declined (G-split), and the decline stood — a reshape that fails its guards does not ship on anyone's say-so. Verdict machinery (per-name LONO fits, robust_verdict) untouched. Evidence of record: the dated adoption file under engine/PENDING_REVIEW/ and fitted_configs.json. ADAPTIVE PER-STOCK WIDTH OVERLAY (engine/adaptive_width.py) sits on top of the pooled fit, EG-ONLY, adopted 23-Jul-2026: an ONLINE per-name multiplier on cone width, learned from that name's own resolved 3-month-window residuals (gentled+dead-zoned, history-gated at MIN_WINDOWS=28 resolved windows — below that, forced to exact 1.0). It is an OVERLAY, NOT A REFIT: the pooled (ν, width_cal), the carry drift, and tail ν are all untouched by it. READ width_overlay_active PER PROFILE, never assume — today only EG carries it. IT MOVES WIDTH IN BOTH DIRECTIONS, NOT ONLY DOWN: the raw multiplier is clipped to (0.7, 1.5), so a name whose own volatility has sat below the panel average narrows toward its own history while a name running hotter than the panel WIDENS toward its own history. Calling it a narrowing device is a misreading, and it is one this project has already made out loud — the overlay answers "is this name's cone the right width", not "make the bands smaller". BEING LIVE IS NOT BEING ACTIVE, AND WHICH NAMES ARE ACTIVE IS NOT RECORDED HERE: the overlay is history-gated and forces exact 1.0 below MIN_WINDOWS, then switches on NAME BY NAME as each library lengthens past the gate, so the set changes without any edit to this block. [R-WIDTH-01] READ IT LIVE BY RECOMPUTING — adaptive_width.live_width_mult() on that name's own library, or re-strike and compare percentiles against the published row — AND NEVER INFER IT FROM THE LEDGER NOTE: the "PER-NAME WIDTH OVERLAY APPLIED" clause is emitted by engine/rollforward_one.py ALONE, so a cone struck through any other path carries the overlay silently and the clause's ABSENCE PROVES NOTHING. See TOP OPEN ITEM below for adoption evidence. TECHNICAL READ IS COMPUTED, AND EVERY BLOCK IS STAMPED (engine/technicals.py + engine/apply_technicals.py + engine/ta_chart.py, adopted 29-Jul-2026). The old carve-out — "technical S/R levels and narrative need a fresh chart read, leave them alone" — is RETIRED. It protected staleness, not judgement: on 28-Jul-2026 COMI's live page carried a 142.00 spot beside a narrative reading "the price closed 129.25", with all three published resistances BELOW spot, and SAMSUNG's three published SUPPORTS all sat ABOVE its spot. STANDING RULE: when the library moves, the technical read moves with it, IN THE SAME PASS — levels, narrative AND the chart underneath them. The read is computed from the same cleaned series mc_v3 runs on, through the same Step 0.0 gate: SMA 20/50/200 with slope state, Wilder RSI(14), Wilder ATR(14) on true range, MACD(12,26,9), 50/200 cross recency, 52-week range, and S/R from recency-weighted fractal-pivot clusters. Prose is templated — every clause is selected by a computed number. Nothing is fitted, so the PROMOTION RULE's out-of-sample test does not apply: there is no free parameter to overfit. The pass is IDEMPOTENT — re-running on an unchanged library is a no-op. R1/S1 ALWAYS mean NEAREST to the close (the retired hand-authored levels were inconsistent — TSLA ascending, COMI descending). NO FUNDAMENTAL ASSERTIONS in a technical block: a deterministic module cannot source "ROE vs cost of equity", so it does not say it. apply_technicals NEVER re-strikes a cone — it reads the published cone's anchor off the newest LEDGER row and its run date off that row's note, and stamps them. THE CHART IS PART OF THE READ, NOT SCENERY: engine/ta_chart.py regenerates the static <svg id="ta-chart-svg"> from the same library. Refreshing levels onto a frozen chart is WORSE than leaving both stale — COMI's axis topped out at 148 against a freshly computed resistance of 160 and injectLevels drew a line at y=-21, outside the viewBox, silently. The SVG is a CONTRACT: injectLevels() recovers price->y by regressing over the chart's own muted axis labels and renderZoomChart() re-reads the same element, so changing the label markup mis-scales both without throwing. The y-range is fitted to the union of the price window, both moving averages AND the published S/R ladder, so an overlay cannot escape the plot by construction. MANDATORY GATE: node scripts/check_ta_chart_overlay.js renders every page with a chart and fails if any level line leaves the viewBox — nothing else catches this, no exception is raised and the page looks fine. It is negative-controlled against the 29-Jul defect. AS-OF STAMPS — TWO DATES, NEVER ONE: every entry carries asof:{mc:{data,computed}, tech:{data,computed}}, where data is the last session the block was built on and computed is the day it was run. A single "as of" cannot distinguish a block recomputed today on last week's prices from one recomputed last week — exactly the failure being closed. app.js renders both off renderStaticFan, the one function every ticker page already calls, so no page template needs editing and a new page inherits the stamps. READ THE STAMPS AS A DIAGNOSTIC: when asof.mc.data is older than asof.tech.data the published cone is stale relative to its own library — report it, never reconcile it silently inside a technicals pass. PROMOTION RULE (standing): nothing enters the engine — from a human OR from the pipeline — without surviving the same out-of-sample test the forecasts must survive. Precedent: selecting (ν, width_cal) by maximising CRPS skill instead of MLE looked clearly better IN-SAMPLE and LOST under LONO in both markets tested. It overfits. REJECTED — do not revive. THE UNATTENDED LOOP: engine/raw_ohlc/{MARKET}/{TICKER}.csv is a PERSISTENT LIBRARY of every covered stock — NOT an inbox. Posting/refreshing ONE stock = ONE file; a GitHub Actions workflow refits that stock's WHOLE market against the full library (~12s; content-hashed panels + an exact closed-form fast_rescore verified bit-for-bit against the engine). Market and ticker are decided by FILE PLACEMENT, never inferred from a filename. MATERIALITY GATE [R-CAL-01, AMENDED 23-Aug-2026 — APPLIES, NEVER SILENTLY]: a refit is MATERIAL if an existing name's verdict changes, a NEW name arrives already FAILING, the market verdict changes, a panel carries a name with no raw data, or THE PUBLISHED 90% CONE MOVES >5% — measured on width_cal × q95(t(ν)), NOT on ν and width_cal separately (they trade off, so watching them individually both misses real changes and fires on noise). A NEW NAME IS NOT MATERIAL BY ITSELF (placing the file is the human decision). MATERIAL NO LONGER MEANS BLOCKED. It used to stop and open a PR for a human; it now APPLIES and ANNOUNCES IN THREE PLACES AT ONCE — the evidence file under engine/PENDING_REVIEW/, the reasons repeated verbatim in the commit message, and the config it replaced stored under `superseded` in fitted_configs.json. Reverting that ONE commit restores market_profiles.py and fitted_configs.json together, in step; never hand-edit either to undo a refit. THE DEFECT THE GATE GUARDED AGAINST IS SILENCE, NOT AUTOMATION, and blocking turned out to be its own form of silence: an unclearable gate produced 66 unmerged review PRs in seventeen days while production went on publishing a month-old fit and said nothing, and those PRs could not be accepted even in principle because they staged only PENDING_REVIEW and panels — merging one applied nothing. A GATE WITH NO RELEASE IS A STALL: whenever a rule can say STOP, the same commit must define what saying GO looks like, or the rule will be ignored rather than obeyed. Run auto_refresh.py --halt-on-material for the pre-amendment behaviour, and scripts/adopt_calibration.py to apply a specific market deliberately. WHAT STILL STOPS THE RUN: a market that RAISES — its production config is left untouched, a PENDING_REVIEW/{MARKET}_{date}-ERROR.md carries the traceback, the healthy markets still apply, and the run ends RED because an exception is not evidence. ONE BLOCKED MARKET MUST NEVER FREEZE THE OTHERS: the runner exits nonzero if ANY market fails, so a single-path "commit only on exit 0" discards every healthy market's applied fit — that bug once left a market sitting at 11 of its 13 names while its own verdict was non-material. Why gated: data cleaning ALONE once flipped a market's ν and two names' verdicts — a bare cron would have shipped both silently. METALS IS THE WEAKEST CALIBRATION IN THE SYSTEM — say so, don't soft-pedal it. Gold is a single-name SELF-FIT (calibrated on its own data, so its verdict is CIRCULAR), and SILVER IS PUBLISHED WITH NO FIT OF ITS OWN — it borrows gold's. Never present the metals cone with the confidence of an EGX/GCC name. Silver/copper/platinum history is what fixes it. ── ROLL-FORWARD WORKFLOW (trigger 'b' above — existing ticker, fresh OHLC) ── FORECAST LIFECYCLE (adopted 29-Jul-2026; canonical STEP 0 of Rollforward_and_Grading_Protocol.md v3): at most ONE current forecast per name per horizon — the latest strike, which alone feeds ticker pages, Trade and Portfolio (all read TICKERS, never LEDGER). The 1-month maturity is the monthly metronome: grade the matured 1M (plus any aging tail whose grade date has arrived), strike a fresh 1M AND a fresh 3M. The fresh 3M demotes the prior 3M to an aging calibration tail — open, untouched, graded at its own maturity, used for nothing else. Steady state is 4 open per name (2→3→4); yield 12 graded 1-month + 12 graded 3-month windows per name per year (the record before adoption: 11 and 0). Mid-cycle OHLC updates refresh the displayed cone + technical read ONLY — no ledger rows; ledger strikes stay on the monthly rhythm. Metals 12-month stays on its own annual clock (one open 12M, graded then re-struck). Routine updates never delete a ledger row; deletion is reserved for deliberate mid-flight engine corrections (the superseded ungraded row removed in the same commit as its replacement) — the one-time 29-Jul cleanup deleted the 143 legacy superseded rows (304→161 = 150 live + 11 graded). After ANY ledger write, assert the lifecycle invariant: exactly one open latest-anchor row per (instrument, horizon). MERGE, NEVER OVERWRITE: diagnose partial vs full export; splice new rows onto the persistent library, verify overlapping dates match to the 4th decimal, no back-adjustment. CALIBRATION: run STEP 0.0 + the materiality gate above on the FULL market panel (never just the touched name) before anything else — same infrastructure as any other library update, nothing special here. GRADING — the forecast is a DATE, not a session count: grade against the close on the stored calendar grade_date (horizons.resolve(): anchor + 1/3 calendar months, month-end clamped, rolled forward to the exchange's first real trading session), regardless of how many sessions the window held — never re-derive the target by counting rows. If an unscheduled closure or suspension pushed the first real session past it, grade the next actual session in the library, keep grade_date as the actual date graded, add grade_date_projected + a one-line grade_note. The ledger page's existing JS already renders pass/fail once realized_close is non-null — no template change needed, this is a data fix only. [R-GRADE-01] EARLY GRADING IS OPT-IN, BOUNDED AND ANNOTATED [ADOPTED 24-Aug-2026, per instruction]: a horizon is a CALENDAR commitment, so a row matures when its calendar grade date ARRIVES, not when N sessions have printed — and the two separate when the month is up but the exchange has not yet exported that last session. The DEFAULT IS UNCHANGED: grade ON the stored grade_date, or on the next real session if a closure pushed past it, else BLOCK. grade_ledger.grade_session() now reports HOW the session was reached (exact / rolled / early) and allow_early grades a matured row on the LAST session inside its window. TWO STRUCTURAL SAFEGUARDS, NOT REMEMBERED ONES: it is OFF by default (the replay negative control plus an unchanged default sweep prove every existing caller is byte-identical), and it is BOUNDED by EARLY_MAX_DAYS=7 calendar days AND SCOPED TO NAMED INSTRUMENTS. Both halves were earned the same day: on 24-Aug-2026 nineteen rows were matured-and-blocked — EAND's library reached 21-Aug against a 24-Aug grade date (ONE session short, a real export lag) while eighteen AE names stopped at 24-Jul (a month short, where early grading would score a cone against a window that mostly never ran), so an unbounded flag would have graded all nineteen alike; and even WITH the bound a blanket flag would still have swept in ADIBUAE and ADNOCGAS, turning a decision about ONE name's permanent record into a decision about every name whose export was lagging that week. A row inside the bound but not named stays BLOCKED and SAYS SO ('within the 7-day early-grade bound, but X was not named'), so the un-taken decision is visible rather than absent. AN EARLY GRADE IS ANNOTATED EXACTLY LIKE A ROLLED ONE: grade_date becomes the session actually graded, grade_date_projected keeps the original calendar date, grade_note states the gap in calendar days, and THE FROZEN PERCENTILES ARE UNTOUCHED — grading appends an outcome, it never revises the claim. The cost is stated, not hidden: a window graded a session short is marginally narrower than the one committed to, which very slightly favours the cone — which is why this is opt-in per name and bounded at a week rather than a new default. NEW COHORT (runs at the monthly metronome only — see FORECAST LIFECYCLE; a mid-cycle data arrival skips this step and goes straight to TICKER PAGE): new cycle (cycle_no = prior+1, reanchor_from = prior anchor_date) anchored at the latest close, via the ACTUAL production chain — fit_har_v3 → har_forecast_v3 → carry_log_h (profile rf_live) → simulate_paths_v3 (profile's live nu/width_cal — for EG, width_cal is first passed through adaptive_width.live_width_mult() per the overlay above, a no-op until the name clears MIN_WINDOWS — signal per signal_active, seed=42, 50k paths) — never an approximation. q_annual must be sourced; if genuinely disputed/unclear, default 0 and flag it, never split the difference. Same touch/percentile conventions as the existing cohort (±5/10/15/20%/±5/10% relative touch, p5/p25/p50/p75/p95 to 2dp). grade_date for the new cycle comes from horizons.resolve() — the calendar commitment the GRADING rule grades; sessions (h1/h3) are projected only to size the cone, never hardcoded 20/60. TICKER PAGE: update TICKERS.{TICKER}.spot/spotDate/dist/touch (touch at the SAME absolute levels already on the page, not re-picked), THEN refresh the technical read in the same pass: python3 engine/apply_technicals.py --write (levels, tech, asof) followed by python3 engine/ta_chart.py --write (the chart underneath them), gated by node scripts/check_ta_chart_overlay.js. NEVER touch fair{bear,base,full} (separate clock, needs a real study refresh) or the interactive slider's bespoke factor-stack constants (CONT_FIXED/EV_FIXED/GEO_MEAN/etc. — fit once to the fundamental driver stack, not the carry-anchored engine; re-fitting is a full study task). Say explicitly what was left alone and why. LEDGER/BACKTEST PNG: pass/fail is automatic from data once graded — don't add template logic. The five-year quarterly backtest PNG is a coarser construct that a single new cohort or a few weeks of tail data essentially never moves — state plainly whether a regen is actually warranted rather than doing it by default. ── The sections below (TEMPLATE through QC GATE) apply only to trigger (a), a full study. Roll-forward never touches them. ── TEMPLATE [CHANGED 19-Aug-2026, per instruction]: THE MODEL REPORT IS ADNOCLS_Valuation_Study_09-08-2026 + its Excel + its standalone bibliography document (engine/adnocls_study/), MINUS the section “What changed in these editions, and why” — edition history is internal QC evidence and belongs in the QC gate and the critique adjudication, never in a document an external reader receives. THE MODEL REPORT DOCUMENT ITSELF IS engine/model_report/MODEL_REPORT_09-08-2026.docx — the exemplar with that section removed, built and asserted by engine/model_report/build_model_report_docx.py. OPEN IT BESIDE THE STUDY BEING WRITTEN: EVERY REPORT IS MODELLED ON IT — its sections list, its sheet list, its content and its research depth are the standard, matched exactly. Machine-readable spec MODEL_STUDY/MODEL_STUDY_DEPTH/ModelStudyChecklist/assert_model_study() in engine/research_protocol.py, verified by IMPORT. ADNOCLS DISPLACES SWDY under one-in-one-out, chosen on depth and analysis. THE REFERENCE SET IS CLOSED AT EXACTLY THREE NAMES — ADNOCLS (model report + operating-co pattern), ADCB (bank), ALPHADHABI (holdco) — enforced by REFERENCE_SET in engine/research_protocol.py, which asserts at import. NO OTHER COMPANY IS A TEMPLATE, AN EXEMPLAR OR A REFERENCE STUDY, and none is carried as a retired entry either — SWDY included, removed outright. If a class fits none of the three, adapt the nearest pattern's lens inside ADNOCLS's skeleton and say which and why — never go looking for a fourth exemplar. (Company names elsewhere in this block are EVIDENCE for a rule or COVERAGE facts, not references.) 16-section Word, in order: Masthead + READ FIRST · Headline · Valuation summary · Company overview · §1 Fundamental valuation (1.1 cash-flow model with the full FCFF waterfall + EV→equity bridge; 1.2 book value & sustainable return; 1.3 relative multiples; 1.4 normalised earnings power; 1.5 synthesis — four lenses, one field; 1.6 drivers — each disclosed segment grown on its own driver, margins as OUTPUTS; 1.7 the crux; 1.8 macro & country — sourced cost of capital, cost-of-debt evidence table, every contested construction PRICED; 1.9 sensitivity) · §2 Technical & price structure · §3 Probabilistic price map (percentile map + level-touch ladder) · §4 Comparison of the lenses · §5 Catalysts · §6 Reading the probability zones · §7 Caveats & what would change our mind · App A statements (A.1 IS 3y hist + 5y fwd; A.2 BS; A.3 forecast BS/CF markers) · App B peers/risk register/research register · App C expert panel (C.1–C.3 by method; C.4 cross-examination; C.5 three in one room; C.6 divergence table) · About · Disclosure. 16-sheet Excel, same names, same order: READ FIRST, Summary, Fundamental Valuation, Assumptions, SOTP Bridge, Segments, Relative & Normalized, DCF, Income Statement, Balance Sheet, Cash Flow, Summary Financials, Monte Carlo, Sensitivity, Per-Share & Ratios, Peer & Sector. MODEL-REPORT DEPTH BAR (every study, every class — each a QC item, missing one = FAIL, not a noted limitation): (1) STANDALONE BIBLIOGRAPHY DOCUMENT — primary-documents table + the FULL input register (EVERY input with value/date/source-and-construction, grouped by research layer) + judgements table each with what-would-overturn-it + negative-results table + aggregator-discrepancy notes; (2) every input FOUR-FIELD complete (value, source, date, layer), validated by assertion — no orphan numbers; (3) NUMERIC TRACEABILITY — every builder reads the study's committed numbers file exclusively, no financial numeral typed into a builder; an independent evaluator recalculates the delivered workbook and reports anything unparseable as FAILURE, never a skip; (4) EXTERNAL-READER SCRUB — programmatic scan of every delivered document for internal-procedure vocabulary (step/gate/ring/register/engine names, verdict tokens), zero hits; calibration evidence appears in §3 as plain-language sentences with the statistics inline, NO calibration appendix; (5) FIGURE DISCIPLINE — solid light canvas, zero transparency verified programmatically, every figure inspected as a RENDERED IMAGE, label collisions fixed in-pass; (6) TABLE DISCIPLINE — fixed layout with explicit widths + programmatic starved/bloated-column check across every table in every document; (7) EXPERT APPENDIX AT MAXIMUM DETAIL — per expert: worldview, when-it-works/when-it-fails, worked valuation table with EVERY intermediate line, named sensitivity with numbers, falsifier stated in advance; plus cross-examination (each challenge conceded or rejected), the three in one room, and a divergence table isolating which assumption drives which gap; (8) the study's single most consequential CONTESTED JUDGEMENT is computed BOTH WAYS and published side by side (summary table, body, workbook, an expert's range), never averaged into one number — the dual-framing rule extended to the study's central judgement. Adapt market/currency/lens/indicators to the class, never structure or depth. Lens-pattern references by class: ADNOCLS = operating-co/diversified industrial; ADCB = bank; ALPHADHABI = holdco. ONE-IN-ONE-OUT: the set stays at three — a new exemplar must DISPLACE one of them (removed outright, not demoted to a retired or secondary entry), and REFERENCE_SET must be updated in the same commit or the import fails. LENS BY CLASS: RE developer→SOTP/RNAV; pure recurring-income RE→10yr DCF with explicit terminal cap rate; bank→DDM (+FCFE +Residual Income, ADCB pattern); holdco→disciplined-SOTP NAV; contractor→FCFF+SOTP; operating-co + captive lender→split legs; aggregator/software→DCF with class driver tree. Never blend legs that need different methods. DRIVER DISCIPLINE: the Step 2A Information Sweep (four mandatory rings — Global/Country/Industry/Company, classified B/S/D/C) runs BEFORE any forecast driver is set, on every study and every update. Build to the FINEST SOURCED LEVEL (see SIGCM below): full bottom-up — product/service, volume × price, cost-per-unit — whenever the unit inputs are disclosed and shown as a sourced driver table first; where only segment data is disclosed, build at segment level and FLAG the gap; a blended top-down is the floor of last resort, never the default, and any top-down step must still rest on a sourced driver table. Before any unsourced driver, check Fundamental_Driver_Ledger.md for a same-class prior. For any ownership/stake driver, search the specific named transaction — never default to "estimated." [ADDED 07-Aug-2026, per instruction — ARCC study] PRIMARY-SOURCE FINANCIAL RESEARCH (extends the Company ring, extends SIGCM clause 1): try the company's OWN website/investor-relations page FIRST for every Company-ring figure, before any aggregator — log the attempt and outcome (success or failure) in the Sweep Register even when the site is unreachable (real case: arabiancementcompany.com returned connect_rejected at the proxy this session), and ask the user to attach the primary document rather than silently substituting a weaker source; the user then supplying it IS the rule working, not a failure of it. Audited financial statements: minimum 2, target 4 complete past fiscal years, from the filing itself — never an aggregator's restated summary. The study year: EVERY quarter already disclosed, swept in BEFORE the build starts — not discovered after (ARCC's own Q1-2026 actual, 42.9% gross margin, sat unswept until the user asked about margins). [ADDED 09-Aug-2026, per critique — MODON study] RESULTS RELEASES ARE SWEPT WITH THEIR STATEMENTS: a period is not swept until BOTH its financial statements AND its results announcement are registered — the release carries the operating anchors the statements never do (backlog and composition, sales and geography, the company's own net-debt definition and value, adjusted EBITDA, current-perimeter portfolio counts), and a newer release's anchors supersede older ones in the driver set or the study states why not (real case: MODON's first edition swept the H1-2026 interim but not the 29-Jul-2026 release whose AED 65.4bn backlog and AED 26bn H1 sales superseded its core drivers; caught by external audit, restruck same day). Investor-relations presentations and earnings-call materials are MANDATORY, not optional, for volumes/prices/utilisation/segment data no financial statement carries — tag them distinctly in the Sweep Register (COMPANY_IR), separate from the audited-financial-statements tag, so a reviewer can see how much of the Company ring rests on the primary IR channel specifically. Full rationale: Standing_Research_Protocol.md. SOURCE-INTEGRITY & GROUND-UP CONSTRUCTION MANDATE (SIGCM — standing HARD GATE, every study AND every update, every ticker, every market; canonical text in Source_Integrity_and_Ground_Up_Mandate.md, machine-readable form + assert_sigcm() gate in engine/research_protocol.py). A violation is a HARD FAIL — do not issue. (1) HISTORICALS = OFFICIAL SOURCES ONLY: construct the past IS/BS/CF using ONLY the company's OWN issued financial statements and full disclosures — no data vendors, brokers, press-as-a-numbers-source, or third-party estimates for the subject's reported historicals (the Step 2A sweep's Global/Country/Industry rings remain fine for external context and forecast drivers, but never as the source of the company's own reported numbers). If required official data is inaccessible, STOP AND INFORM — never substitute unofficial data; NEVER issue a report based on unofficial company information. (2) FORECAST FROM THE GROUND UP: product-by-product / service-by-service wherever segments are disclosed; revenue as VOLUME × PRICE and cost as COST-PER-UNIT, with growth projected in BOTH volume and price; where unit/segment data isn't disclosed, drop to the finest sourced level and FLAG the gap (this is the same finest-sourced-level rule stated in DRIVER DISCIPLINE). (3) DEBT & FX: study balance-sheet debt in full and split local-currency vs foreign-currency tranches — consistent with the WACC rule that FX debt is carried at local-equivalent cost. (4) ASSET-CONVERSION CYCLE → BS/CF: study DSO/DIO/DPO and the cash-conversion cycle from the statements and PROJECT the balance-sheet and cash-flow items from them — no unexplained plugs where the drivers are disclosed. (5) COMPETITORS: study peers inside AND outside the country for operating KPIs and valuation multiples (cross-check / relative multiples only — never a source for the subject's historicals). (6) BETA: from the stock's OWN price history regressed against THE PUBLISHED INDEX OF THE EXCHANGE IT IS LISTED ON, resolved through engine/beta_regression.own_stock_beta() (which reads engine/raw_indices/{MARKET}/{INDEX}.csv via wacc_builder.market_index_path) — the SAME beta the BETA hierarchy in the WACC block produces, not a second method. A CONSTITUENT COMPOSITE IS A HARD FAIL, not a fallback; if the index is absent, STOP AND INFORM. NEVER hand-roll a study-local beta script — every study in this repo once did and every one regressed on a composite. (7) FORMULA-BASED ALWAYS: every constructed financial statement is a live formula model (driver → IS → BS → CF → DCF; blue = input / black = formula; fair value recomputes when a driver changes) — never hardcoded values. (8) FLAG-BEFORE-ISSUE / STOP: flag any missing input BEFORE the report is issued; if the website or disclosed statements can't be read and that blocks a detailed ground-up build, STOP AND INFORM — do not proceed on assumptions or unofficial substitutes. WACC (bottom-up; UNIVERSAL, PER-SOVEREIGN, PER-STOCK — see Cost_of_Capital_Reference.md + engine/wacc_builder.py, v2): EVERY country is priced off its OWN Damodaran row (Moody's/S&P rating, sovereign CDS, adjusted default spread, ERP), looked up FRESH in the ORIGINAL ctryprem.html file for that specific sovereign — Damodaran publishes every country; NEVER borrow one country's number for another or assume a region shares a figure. NORMALIZE rf — rf* = local govt-bond yield − that sovereign's OWN default spread (≈0 for mature AAA; e.g. 6.37% for Egypt Jan-2026). Country risk enters ONCE, via the CRP inside the ERP; using the raw local yield PLUS a CRP-loaded ERP DOUBLE-COUNTS sovereign risk (the systemic v1 bug that reached GBCO — re-issue affected studies). CONSISTENCY: strip the SAME basis of default spread as the CRP you add back (rating↔rating, CDS↔CDS); publish BOTH ERP-basis WACCs. rf from the local govt bond even for pegged currencies (never shortcut to USD). ERP from Damodaran's ORIGINAL file only. BETA'S REGRESSOR IS THE PUBLISHED INDEX OF THE EXCHANGE THE STOCK IS LISTED ON, read from engine/raw_indices/{MARKET}/{INDEX}.csv (EGX30 for EGX, FTSE ADX General for ADX, TASI for Tadawul) — [AMENDED 10-Aug-2026, per instruction]. A CONSTITUENT COMPOSITE IS NOT A SUBSTITUTE AND NOT A TIER: an equal- or turnover-weighted basket of the names this engine happens to cover is a coverage artefact, not a market — it changes whenever a stock is posted, it mixes exchanges inside one market code (ADX with DFM), and it shares constituents with the panel it prices. It may be shown as a labelled cross-check, never as the regressor. MATCH THE EXCHANGE, NOT THE COUNTRY — AND THE RESOLVER IS KEYED ON EXCHANGE, NOT ON MARKET CODE [RE-KEYED 10-Aug-2026, same day, per instruction]: the first cut mapped one index per MARKET and so contradicted this very clause — market AE spans BOTH ADX and DFM, so every DFM-listed name resolved to an ADX index (at the time: six of them — DEWA, DIB, EMAAR, EMAARDEV, ENBD, SALIK; the live count belongs in assets/data.js, never here). wacc_builder.EXCHANGE_INDEX is now keyed (market, exchange) and market_index_path(market, exchange) REFUSES to resolve a market that spans two exchanges unless told which — unknown market, ambiguous market, unregistered exchange and missing file all RAISE. Registered today: AE/ADX→FADGI, EG/EGX→EGX30, IN/NSE→NIFTY50, KR/KRX→KOSPI100, QA/QSE→QATAR10, SA/TADAWUL→TASI, US/NASDAQ→NASDAQCOMP. AE/DFM RUNS AN INTERIM [10-Aug-2026, per instruction; HELD OPEN 23-Aug-2026, per instruction]: the nine DFM-listed names (DEWA, DIB, EMAAR, EMAARDEV, ENBD, SALIK, AIRARABIA, DU, EMPOWER) stand on FTSE ADX General — a LABELLED exception to "match the exchange", not a quiet fallback. wacc_builder.INTERIM_INDEX holds the disclosure and index_interim_note() returns it; ANY BETA BUILT ON IT MUST QUOTE THAT NOTE and is not a conforming regressor. It is the better-evidenced half of the substitution, against intuition: over five years of weekly returns FADGI explains the DFM names BETTER than the ADX names it actually covers (median R² 0.240 vs 0.127, median beta 1.067 vs 1.037), all six then-covered DFM names cleared the R²≥5% gate (the measurement is the 10-Aug one, on the six DFM names covered at that date — AIRARABIA, DU and EMPOWER were added after), and several ADX names barely do (AGTHIA 0.037 fails it, IHC 0.088, FERTIGLB 0.091) — the big Dubai names co-move with the whole UAE market while Abu Dhabi carries more low-float and ADNOC-family idiosyncrasy. A DFM GENERAL SERIES IS NOW HELD AND THE INTERIM STANDS ANYWAY [23-Aug-2026, per instruction]: engine/raw_indices/AE/DFMGI.csv (2015-01-05 to 2026-07-16) is in the repo, so the 10-Aug clause’s own replacement condition is MET and has been explicitly DECLINED for now — the nine DFM names stay on FTSE ADX General. DFMGI IS HELD BUT NOT REGISTERED: EXCHANGE_INDEX keeps mapping ("AE","DFM") to FADGI, and a later session MUST NOT register it on the reasoning that the file exists — registration needs its own instruction. The disclosure obligation gets STRONGER, not weaker, because a stopgap that persists by choice is a standing methodological position: quote index_interim_note() verbatim wherever the beta is quoted (body, bibliography, cost-of-capital table, workbook) and never describe such a beta as conforming. The cost is measured and not nil — on the two names run both ways DFMGI has the higher explanatory power and moves beta by a quarter to a third in OPPOSITE directions, so it is not a uniform bias a reader can correct for. Under the dual-framing rule a DFM study holding both numbers publishes the declined one as a LABELLED cross-check beside the adopted one (airarabia_study/beta_reg.py is the worked precedent: runs both, adopts the registered regressor, prints the difference, says why). Revisit only on a further instruction. STILL NOT REGISTERED, so no conforming beta is possible there: BR, GB. THE EXCHANGE OF A COVERED NAME IS RECORDED IN assets/data.js AS THE code PREFIX (ADX:, DFM:, EGX:, TADAWUL:, QSE:, KRX:, NSE:, NASDAQ:) — read it, never infer the exchange from the raw_ohlc/{MARKET}/ folder, which deliberately groups by market code. DUAL-LISTED NAMES ARE A LIVE TRAP: Orascom Construction trades on BOTH ADX and EGX, so the same issuer has two legitimate regressors and only the SERIES tells you which — verify the series' currency and price magnitude against the exchange it is filed under before regressing (an EGP series filed under EG regresses on the EGX index; a dirham series of the same company would regress on the ADX index), and flag dual listings explicitly in the Sweep Register. BROAD-VS-SUBSET IS SETTLED [10-Aug-2026, per instruction]: the user supplied all seven series explicitly as "the indices to use", so THESE ARE THE REGRESSORS and the preference clause is satisfied by that instruction — only SA/TASI is genuinely a broad all-share, while EGX30 (a 30-name blue-chip index against a broader covered panel that carries small caps), NIFTY50, KOSPI100, QATAR10 and NASDAQCOMP are subsets, adopted deliberately rather than by drift. All seven uploads were byte-identical to the copies already in raw_indices/, so no registration changed. Revisit only on a further instruction. ALL SEVEN PASSED STEP 0.0 on 10-Aug-2026 (density screened against each exchange's real calendar, max one-day move against that exchange's own limit). QATAR10 CARRIES A STANDING CAVEAT — WEEKLY ONLY: it publishes ~198 sessions/yr against QSE stocks' 248–250, a systematic ~20% shortfall stable at 188–201 every year for fourteen years, so it is a property of the published series, not corruption. It does NOT impair the mandated weekly regression (255 weekly points against a stock's 259 over five years, 255 aligned = 98.5%), but it WOULD impair any daily use, which is therefore not permitted without re-screening. Quote this caveat wherever a Qatari beta is quoted. The index passes STEP 0.0 like any other series and its as-of date is quoted wherever the beta is quoted. IF THE INDEX FILE IS ABSENT, STOP AND ASK FOR IT — same stop-and-inform discipline as missing primary financials; do not build a composite and proceed. Why this is a rule: every study in the repo (AMOC, ARCC, EGCH, ELEC, PHAR, SCEM, SWDY — and SWDY was the model study at the time) was regressed against an equal-weight composite, because the CLHO precedent that established the BETA procedure used one and each study copied the last. On FERTIGLB, the first name run both ways, the composite gave beta 0.492/R²6.2% against the real index's 0.931/R²10.0% — a ~40% understatement that carried WACC from 11.90% to 8.53% and overstated the centre by 21.6%. EVERY BETA BUILT ON A COMPOSITE IS NON-CONFORMING and must be re-derived before that study is re-issued or rolled forward. Markets with no index file as of 10-Aug-2026: BR, GB (SA/TASI supplied 10-Aug-2026; STC and every other Tadawul beta must be re-derived against it) — no conforming beta is possible there until one is supplied. BETA is PER STOCK, STRICT PREFERENCE ORDER — take the first that is available: (1) OWN-STOCK 2–5yr WEEKLY/monthly regression vs that published index whenever that much usable history exists (use the longest window up to 5yr; usable = passes the gate n≥24, R²≥5%, SE(β)<|β|) — FIRST CHOICE; (2) if a 2–5yr own-stock regression is NOT available (short listing, too illiquid/thin, or fails the gate), a SAME-COUNTRY peer beta (median unlevered peer β re-levered to target structure; multi-segment names → business-value-weighted unlevered β first) — never foreign peers; (3) β = 1.0 only if neither the own-stock history nor a same-country peer set is available. A daily/short-window regression is NOT one of these tiers — if ever used as a stopgap, flag it as interim and replace with tier (1) before a study is audit-clean. Kd is MARGINAL, forward-looking, and in the CASH-FLOW CURRENCY — the company's OWN latest debt issue, else local sovereign yield + corporate credit spread — and MUST sit ABOVE the local sovereign (a same-currency corporate cannot borrow below its sovereign). FX debt carried at LOCAL-EQUIVALENT cost (FX coupon + expected local depreciation), NEVER a raw FX coupon in a local-nominal WACC; NEVER a historical/accounting capitalization rate. Weights: MARKET-VALUE equity only (never book equity), book≈market for debt; normalized target structure when leverage is changing. Ke = rf* + β×ERP. Match nominal-to-nominal (real-to-real) with the cash flows; for high-inflation EM also run a USD/real valuation cross-check. [ADDED 07-Aug-2026, per instruction — ARCC study] [ADDED 17-Aug-2026, per instruction — DU study] MARGINS ARE OUTPUTS OR THE COST SIDE IS NOT BUILT: a contribution or gross margin set as an INPUT is a QC FAIL wherever the filings disclose enough to build cost PER UNIT instead — each physically distinct direct-cost line on its own driver with its own escalator, margin as the residual. WHERE A COMPANY DISCLOSES THE SAME TOTAL TWO WAYS (by nature and by segment) RECOVER THE JOINT AND TEST IT: two disclosed marginals plus a defensible structural assumption often pin the cross-tabulation the company never publishes, but the residual must be economically possible and must foot EXACTLY to the independently disclosed line in EVERY disclosed period, asserted in code — if it comes out negative or fails to foot the assumption set is wrong and the joint must not be used. A NEAR-TERM REVIEWED ACTUAL OUTRANKS A STALE FULL-YEAR RATE: anchor every unit rate on the most recent reviewed period and let it DRIFT only where a named structural mechanism has a MEASURED like-for-like direction in the company's own period pair; hold everything else flat INCLUDING observed improvements, and where a first-half rate is carried into the second half prove with the prior year's actual halves that this OVERSTATES cost. Delete forecast stories the disclosed series cannot measure. AN UNIDENTIFIED SPLIT MUST BE DEMONSTRATED UNIDENTIFIED, NEVER ASSERTED AND NEVER FILLED WITH AN IMPORTED RATIO: solve for the implied parameter across EVERY available period pair and publish the range; such splits are typically MIX-PRESERVING, so building one adds an unsourced driver and NO information while looking more precise — instead DECOMPOSE and PRICE what the coarse figure hides (a flat blended ARPU proved to be a mix tailwind offsetting per-leg erosion, worth -17% on the cash-flow lens when the tailwind exhausts, invisible until taken apart). A NUMBER STATED IN PROSE MUST BE COMPUTED, NOT TYPED — extended from builders to the four-field register's OWN justification text: deltas quoted in a note are computed in a pre-pass from the raw disclosed figures and asserted against the route the model computes independently. READING THE RENDERED PDF AND INSPECTING EVERY FIGURE AS AN IMAGE ARE GATES, NOT FORMALITIES, and a delivery that skipped them must NEVER be described as verified — on one DU delivery they caught a study contradicting itself four lines apart, two typed percentages disagreeing with the computed values, a retired session-counted unit in an axis label, and a heatmap title promising bold cells that did not exist, none of which any programmatic check could see. A SELF-AUDIT THAT ONLY RE-CHECKS THE WORK IT DID WILL KEEP MISSING THE WORK IT NEVER DID: ask explicitly, against the Sweep Register, WHAT THE FILINGS DISCLOSE THAT THE MODEL DOES NOT CONSUME before declaring a self-audit complete. Full rationale: Standing_Research_Protocol.md. COST-STACK ESCALATION: any per-unit cost stack allocated to its own physical driver (materials/fuel, transport, overhead) gets ONE ESCALATOR PER DRIVER CLASS, never one blended index across all of them — a globally-traded input (coal, imported energy) escalates on its own commodity-price path through the model's own FX, never a domestic CPI-convergence proxy; only genuinely domestic lines (local wages/services) get that. Adopted after a reconciliation against an EFG Hermes sell-side report on ARCC showed the model's whole forecast margin decline was a mechanical artifact of the price path being set below a single blended cost-inflation index in every year, by construction — invisible until priced out line by line, and already contradicted by a realised Q1-2026 margin of 42.9%, above the FY2025 full-year average. The cost side must clear the same source-discipline the price side already does: a disclosed, dated, near-term figure sought before defaulting to a house macro proxy. And the first realised quarter after publication is a standing check against that study's own forecast — a miss above the >5%-of-central threshold escalates like any other critique finding, not something a user has to surface. Full rationale: Standing_Research_Protocol.md. EXPERT APPENDIX: three experts from the Persona Library by class, labeled "Expert 1/2/3" only. Genuinely different methods, shown workings, a falsification condition each. QC GATE (unprompted, as the last step; output as a filled table with actual evidence per item — never self-certify): structure, content, format AND DEPTH match the MODEL REPORT (ADNOCLS) — 16-section Word, 16-sheet Excel, standalone bibliography document, all eight depth-bar standards attested via ModelStudyChecklist · Step 0.0 data-quality gate passed · Step 0 passed under the scale-normalized carry-anchored gate · SIGCM passed — historicals official-only (no vendor/broker/press); forecast built ground-up (product/service, volume × price, growth in both) to the finest sourced level with every estimate flagged; debt split LC/FX; asset-conversion cycle studied and BS/CF projected from it; competitors studied for KPIs & multiples; beta produced by beta_regression.own_stock_beta() against the exchange's PUBLISHED index and attested by research_protocol.assert_beta_provenance() — the record's index_file must sit under raw_indices/, a fit failing the usability gate must have fallen to tier 2/3 rather than kept a composite number, and any interim regressor must quote its disclosure note; every constructed statement formula-based (driver→IS→BS→CF→DCF); all gaps flagged pre-issue; STOP-and-inform honoured wherever official data was inaccessible · Sweep Register shows primary-source depth — company site attempted and logged (success or failure), ≥2 audited fiscal years (target 4) cited to the filing itself, every study-year quarter already disclosed swept in before the build, ≥1 investor-relations/call source tagged distinctly (COMPANY_IR) · cost stack shows one escalator per driver class (no blended index across physically distinct cost lines) and the cost path carries a sourced near-term anchor, not just a macro-convergence assertion · 3yr hist + 5yr fwd DCF with the FULL FCFF waterfall to PV of FCFF shown inline (stopping at FCFF = hard fail) · WACC built under the v2 cost-of-capital method (rf normalized by the sovereign's own default spread — no double-count; marginal Kd above the sovereign; 2–5yr/bottom-up beta; market-value weights; both ERP bases) · expert appendix in full · crux sensitized in real observable units · every non-sourced driver logged to the Driver Ledger · Sweep Register validated · script-built files verified by a cell-by-cell diff against the delivered file (a clean recalc is necessary but NOT sufficient). STANDING RULES: ledgers (Calibration + Driver) are append-only — no published forecast is ever retro-edited. Post-delivery corrections fold back into the build scripts, not just the delivered file. Dual-framing rule: when a figure has two legitimate framings (gross/net), state both. NEVER a rating or a price target — fair-value ranges and distributions only. A matured 1-month or 3-month cohort is graded and rolled forward under the current engine WITHOUT waiting for a separate publish request — the ONE carve-out from "publishing needs an explicit ask." That carve-out covers permission only: I don't ask before doing the grading/roll-forward work. A genuine outlier grade triggers an immediate out-of-cycle re-fit. ENFORCEMENT — THE RULES THAT MAKE THE OTHER RULES BIND [ADDED 23-Aug-2026, per instruction; full account in Standing_Research_Protocol.md rev. 6]. Adopted on one measured fact: an audit of all 90 covered stocks on 23-Aug-2026 found 63 NOT built ground-up and only 4 carrying an attestable beta, while every rule requiring both was already written in this block and in the full protocol. Nobody disagreed with a rule; the rules simply were not present at the moment they bound and nothing outside the study was looking. NO RESEARCH METHOD CHANGES HERE — no lens, driver rule, cost-of-capital construction or calibration procedure is altered and no delivered number moves; this is only about whether the existing rules execute. [R-ENF-01] A RULE THAT CAN BE CHECKED MUST BE CHECKED FROM OUTSIDE THE THING IT GOVERNS, AND A SELF-ATTESTED BOOLEAN IS NEVER A CHECK. Where a standing rule can be expressed as a test, the test exists in code, runs over the work rather than inside it, and FAILS rather than warns; where a rule genuinely cannot be tested (a peer-set judgement, a lens choice) it stays prose and the QC gate carries the evidence. This is the generalisation of three lessons already learned separately — the composite beta behind a self-set flag, the technical-read carve-out that protected staleness, and the digest drift — each fixed in its own place and never generalised, which is why the identical hole stayed open in eight of the nine SIGCM clauses. WHEN A DEFECT OF THIS SPECIES IS FOUND AGAIN, CLOSE THE CLASS, NOT THE INSTANCE. [R-ENF-02] EVERY STUDY CALLS THE THREE GATES AND A JOB OUTSIDE THE STUDY VERIFIES IT: a study must call assert_sigcm(), assert_beta_provenance() and assert_model_study() in its own committed code, and scripts/check_study_provenance.py runs over every engine/*_study/ from outside, in CI (.github/workflows/study-provenance.yml), failing any study that calls none and refusing a surviving study-local regression script. Written because 13 of 21 study directories called none and no automated job ran any — A STUDY PASSED BY NOT CHECKING ITSELF. It is a RATCHET, not a cliff: knowingly-outstanding studies are listed in engine/build_depth_audit/outstanding.json and are allowed to fail, the build breaks only on a NEW violation or a new study directory with no gate, and the list may only ever SHORTEN (--prune rewrites it). A permanently red check is one everyone learns to ignore. [R-SIGCM-02] THE GROUND-UP CLAUSE IS ATTESTED ON A RECORD, NOT A FLAG: forecast_ground_up is no longer a boolean a study sets on itself. A study records for EVERY revenue line the physical unit, the disclosure it came from, the price basis and the cost-per-unit basis, and research_protocol.assert_ground_up() inspects it. Four levels, only the first is the standard — unit (volume x price on a DISCLOSED unit, cost per unit, margin an OUTPUT) / derived (real unit economics on an indexed, estimated or back-solved volume) / segment (disclosed segment on its own driver) / topdown (growth path plus a margin assumption, the floor of last resort). Two refusals are built in: the lines must cover 100% OF REVENUE, because a line left out of the record is a line nobody checked; and any line below unit must carry a GAP NOTE — the rule has always permitted a coarser level where the disclosure stops and has never permitted going quiet about it. Claiming unit without naming the unit, its source and the price basis also fails. [R-BETA-04] THE BETA RECORD HAS A REQUIRED SHAPE AND THE SHAPE IS CHECKED EVEN WHEN THE STUDY IS SILENT: assert_beta_provenance() already demands beta/r2/se/n/usable/index_file/index_asof/market/exchange/conforming and a raw_indices/ PATH; the hole was that a study which never called it could record whatever it liked. Across 21 directories the regressor was recorded four ways — full path, bare filename, prose name with no file, nothing — and the weakest shape silently defeated the gate. The repo-level job now applies the same test from outside. [R-STD-01] EVERY STUDY IS STAMPED WITH THE STANDARD IT WAS BUILT TO: research_protocol.STANDARD_VERSION names the current standard, a study records the version it was built against, and the repo-level job reports any study built to an older one. Bump it ONLY when a change would alter a delivered number or a required artefact, never for prose. Written because 'is this study finished or finished-for-now?' had no answer in the repo — without a version a book-wide re-issue is open-ended and nobody can tell which names are current; with one the rebuild queue is finite and its remainder countable. [R-IDX-01] ONE INDEX, ONE FILENAME, REGISTERED — OR DOCUMENTED AS DELIBERATELY HELD: every .csv under engine/raw_indices/ is either registered in wacc_builder.EXCHANGE_INDEX or listed with a reason under held_unregistered in outstanding.json; nothing else may sit there. Written because ADXGENERAL.csv was a byte-identical duplicate of FADGI.csv under a filename the resolver does not register, and ADNOCDIST and ADNOCDRILL regressed against that copy — the right number with provenance that cannot resolve. DFMGI.csv is the documented held case. [R-DOC-01] STANDING RULES CARRY AN IDENTIFIER AND BOTH DOCUMENTS ARE CHECKED AGAINST EACH OTHER: every rule adopted from 23-Aug-2026 carries a stable [R-AREA-NN] identifier appearing in the full protocol, in this block, and in the code that enforces it; scripts/check_protocol_sync.py compares the identifier sets and FAILS on any rule in one document and not the other. Rules adopted before 23-Aug-2026 are NOT retro-tagged in bulk — each acquires an identifier the next time it is amended, so the tagged set grows from the bottom up. Written because this block is kept in sync by an instruction to remember, and on 23-Aug-2026 the copy held outside the repository was found one amendment behind. Three further rounds of 'is this the version to adopt?' that same day each pasted back a copy one edit stale, because every revision of a 54,000-character block looks identical to every other. BOTH DOCUMENTS THEREFORE CARRY A REVISION STAMP AS THEIR FIRST LINE — a copy without the current stamp is stale on its face, without reading a word of it. Bump the stamp on EVERY edit, however small: an unbumped stamp is worse than none, because it certifies a copy that has moved. VERIFY BY IMPORT, NOT BY PARSE. market_profiles.py must be checked by actually IMPORTING it before any commit — nu=Gaussian is a bare identifier that parses perfectly and only dies at import. That exact bug once reached main and left the engine unloadable while a regex check reported it "intact." The Gaussian limit is written as the numeric sentinel nu=250.0. The same import-not-parse rule applies to engine/wacc_builder.py, engine/research_protocol.py, engine/adaptive_width.py, engine/research_sweep.py, engine/technicals.py, engine/apply_technicals.py, engine/ta_chart.py, and engine/rollforward_one.py. The JS analogue is MANDATORY too: node --check on assets/data.js and assets/app.js, then LOAD data.js in node and assert on the parsed TICKERS/LEDGER objects — an assert-guarded string replacement verifies the old text existed, it cannot see whether the surrounding structure survived, and a stitch point (a missing comma before an appended LEDGER row) is valid-looking text and invalid JavaScript. COUNT AGAINST A KNOWN TOTAL, never trust a tool's own "0 skipped": a regex matching unquoted object keys only silently dropped "2POINTZERO" — which MUST be quoted, a JS identifier cannot start with a digit — from three separate tools, and each reported success. AFTER ANY LEDGER WRITE, additionally assert the lifecycle invariant (exactly one open latest-anchor row per (instrument, horizon)). GIT / PUBLISH MECHANICS: reads use anonymous raw.githubusercontent.com (repo is public). Engine and protocol changes go on a feature branch with an open PR, never a direct push to main — same materiality-gate spirit as the automated pipeline: a human reviews before it merges. [CHANGED 07-Aug-2026] PUBLISHING itself no longer stops for a token: "publish" is the authorisation to go all the way to merged-and-deployed (see DEFAULT BEHAVIOR above and Publish_Protocol.md) — merge via the GitHub tools directly if scripts/publish_site.py can't (no GITHUB_TOKEN in the environment). The old PAT-in-memory rule is retired along with the token gate it protected. RESPONSE STYLE: 3–4 sentences max, no preamble, lead with the answer. Expand only if asked. TOP OPEN ITEM: [ADOPTED 23-Jul-2026, EG-ONLY, HISTORY-GATED — no longer proposed] a NAME-LEVEL width_cal shrunk toward the market fit, engine/adaptive_width.py — the real fix for the "bands are too broad" complaint. The robust FAILs that motivated it failed for the SAME reason and it was NOT mis-centring: they were OVER-COVERED (one name had cov80=1.00 and cov90=1.00 — every outcome inside the 80% band — with a PIT of 0.471, perfectly centred). A market-level cone over-widens any name whose own vol sits below the panel average — and under-widens any name whose own vol sits above it; the overlay corrects in both directions, which is why it is not a narrowing device. It cleared the SAME LONO gate that killed the CRPS-selection idea — 30-name EG panel, held-out FINAL split, block bootstrap {2,3,4}: proper score held at PARITY (zero cost), pooled |std_u−1| improved 0.096→0.069, 24/30 names moved closer to std_u=1. It is HISTORY-GATED (MIN_WINDOWS=28 resolved 3-month windows) and activates NAME BY NAME as each name's own library lengthens past 28 windows — never market-wide, and never on a date this block can name. WHICH EG NAMES CARRY IT IS A LIVE READING, NOT A FACT STORED HERE: the sentence that stood here said every live EG cone was unaffected, and it went false silently the first time a name crossed the gate, exactly as [R-DOC-02] predicts of any status claim. EG-ONLY: every other market stays on the flat market-level cone until it clears this same LONO gate on its own panel — don't assume it applies elsewhere. MERGED AND LIVE — re-verified 23-Aug-2026 against main, not against this block: engine/adaptive_width.py is on main and market_profiles.py carries width_overlay_active=True for EG. This block said "committed on a feature branch with an open PR" long after the merge, so treat every status claim here the same way — [R-DOC-02] A STATUS SENTENCE IN A STANDING DOCUMENT IS A CLAIM ABOUT THE WORLD AND IT ROTS: anything of the form "as of this entry, X is on a branch / pending / not yet merged" is re-verified against the repository at the moment it is relied on, or it is not written here at all. BEING LIVE IS NOT BEING ACTIVE — the overlay is history-gated and forces exact 1.0 below MIN_WINDOWS, so read live whether any EG name has cleared it before describing a published cone as carrying it. Also open: the engine's per-origin vol estimation is still not break-aware; metals needs silver/copper history; GBCO (EGX) + STC (Tadawul) WACCs predate the v2 cost-of-capital method and must be re-issued; a NAME-LEVEL width_cal (see above) is the standing answer to "bands are too broad" — BUILT AND MERGED FOR EG, re-verified against main 23-Aug-2026, and NOT built for any other market, each of which must clear the same LONO gate on its own panel first; and LIBRARIES GO STALE AND SELF-REPORT IT on every page via the tech as-of stamp — but WHICH ones is a LIVE READING, NEVER A ROSTER RECORDED HERE: compare each engine/raw_ohlc/{MARKET}/{TICKER}.csv's last session against the newest session held anywhere, or read the stamp on the page. Posting a fresh export at engine/raw_ohlc/{MARKET}/{TICKER}.csv is the fix; nothing else unblocks them. [R-DOC-02, 24-Aug-2026] This sentence used to name eleven; by 24-Aug-2026 four of the eleven had been refreshed while it still listed them as stale, and names it never mentioned had gone stale in their place — the same rot as the width-overlay dormancy claim, and the reason this block opens by calling stock counts VOLATILE.
