# PHDC — QC gate, 13-Sep-2026

Palm Hills Developments · Egyptian Exchange: **EGX:PHDC** · EGP · edition **PHDC_Valuation_Study_10-09-2026**

Audited from outside the study by an evaluator who did not build it [R-ENF-02]. Every row names
the artefact, the command or the number that carries it. No file in this directory was written by
this audit except this one. All builders were re-run in a sandbox engine tree at
`/tmp/.../scratchpad/qc13/sb`, never in the study directory.

**THE BRIEF.** Seventeen findings (F1)–(F17) were raised in `QC_GATE_10-09-2026.md` and all
seventeen were worked in `b2eddd235` and `c4bfea9f7`. This gate establishes from evidence which
are actually closed and what remains.

**THE REPOSITORY MOVED TWICE DURING THIS AUDIT, AND ONE OF THE MOVES CLOSED A FINDING OF THIS
GATE.** At the start of the pass HEAD was `c4bfea9f7` and the three delivered PDFs were the
**pre-fix renders** — the study PDF was text-identical to a LibreOffice 24.2 render of the docx at
`b2eddd235~1` (0 differing lines) and differed from a render of the current docx by **1,717
lines**, 27 pages against 32. Commit `0081ebd4c` landed at 13:12 and re-baked all three. That
finding is recorded below as **CLOSED mid-pass**, with the evidence that established it, rather
than quietly dropped. Everything measured on the `.docx` and `.xlsx` still stands: those three
files are byte-identical between `c4bfea9f7` and HEAD (zip-part diff: study 0 parts, bibliography
0 parts, workbook 1 part — `docProps/core.xml`, a timestamp).

**VERDICT: NOT DELIVERABLE.** Eight defects block it. Thirteen of the seventeen findings are
genuinely closed and the arithmetic underneath is as sound as it was — the numbers file rebuilds
with **0 of 5,722** leaves differing, the workbook with **0 of 2,244** cells, the two documents
with **0 of 215** and **0 of 41** items. What blocks delivery is: **the column fitter introduced to
close (F5) took both documents from 0 table-width defects to 24**, measured on the model report's
own criterion; (F8) and (F10) are not closed on the surfaces that carry them; financial numerals
are typed into three builders and print to all three delivered files; and the document
misdescribes its own forecast horizon.

---

## The seventeen findings, one row each

| F | What it was | Status | The evidence that settles it |
|---|---|---|---|
| F1 | The QC gate on disk was two editions behind | **CLOSED** | `QC_GATE_10-09-2026.md` committed at `1aa8f8180`; this file supersedes it. |
| F2 | `gate_check.py` audited the 3-September files | **CLOSED** | `gate_check.py:88-99` now reads `edition.py`: `study = os.path.join(HERE, _ed.STUDY_DOCX)` and the same for `BIBLIO_DOCX` / `MODEL_XLSX`. Re-run from outside: four gates PASS, `ISSUABLE: True`, `sections 44`, `sheets 16`, `scrub_hits []`, `column_audit []`, `recalc_checks 231`, `recalc_mismatches 0`. |
| F3 | `recalc.py` recalculated the superseded workbook | **CLOSED** | `recalc.py:15-20`: `import edition as _ed` … `XL = os.path.join(HERE, _ed.MODEL_XLSX)`. Run in the sandbox against the delivered file: **231 checks, 0 mismatches, 0 unparseable, 0 skipped**, reconciling the fifteen discounted FCFF years, the 52,181.67 explicit total and 21.0897 per share. |
| F4 | The 3-September delivered workbook had been overwritten | **CLOSED** | `md5sum` of `git show f35710466:…/PHDC_Valuation_Model_03092026.xlsx` against the file on disk: **both `3c7449df6038bfc54ca9f85b2f816fc6`**. The 03-09 bibliography likewise: both `5855490cb635ee03db324a371f71df30`. Both restored to what a reader received. |
| F5 | Four columns too narrow; the page printed `1,152,92` | **CLOSED, AND IT REGRESSED — see (H1)** | `python3 scripts/check_column_widths.py` → OK, no PHDC entry; page 5 of the delivered PDF now prints `1,152,921` and `1,658,388` whole. **But on the model report's own criterion the two documents went from 0/0/0 to 6 starved + 4 bloated and 14 starved.** |
| F6 | The supersession prose was wrong three ways | **CLOSED** | Page 1: *"it changes ONE INPUT in it: the long-run real interest rate … Two other numbers move with it … the rate applied to all fifteen forecast years falls from **25.102%** to 24.965%; and the terminal cost of equity is now built by splitting the country premium out."* 25.102% is `prior_edition.wacc_adopted` in `study_numbers.json`, read from the 3-September workbook. The headline no longer claims the ERP basis moved in this edition. One imprecision remains — see (G7). |
| F7 | Two different headline ranges under the same words | **CLOSED** | Page 2: *"Across the full observed range of cash conversion ALONE … EGP 7.65 to EGP 46.85. The wider pair published beside it, EGP 4.98 to EGP 55.07, moves a second driver as well: the weak case also carries the whole cost-of-capital schedule **2 points HIGHER** and the strong case **1 point lower**."* `lens_record.primary.range_note` carries it too: *"with the whole schedule shifted rather than flattened"*. |
| F8 | Gross margin is an input and four surfaces said "output" | **NOT CLOSED — see (H2)** | Three of the four are fixed (§1.1 *"Gross margin is an INPUT here"*; the A.1 caption *"Gross margin is a HELD input and cost per unit is solved from it"*; the row label *"Gross margin (held input)"*). **The fourth is the `DriverLine` record `assert_ground_up()` actually reads** — `gate_check.py:50-53` still says *"gross margin is an OUTPUT of price against cost on the same completion clock, never an input."* |
| F9 | "the average of the last two disclosures"; workbook B8 retired | **CLOSED** | Page 5: *"the gross margin is held at 35.5 per cent, which is the LATEST disclosure on its own — the first quarter of 2026 — and not an average. FY2025 reported 41.2 per cent; their average is 38.3 per cent, and that average was the anchor the superseded edition used."* `Assumptions!B8 = 0.3548387096774194`, source cell C8 *"the first quarter of 2026 on its own, the latest disclosure; NOT an average"*. |
| F10 | Three delivered places said no half-year filing exists | **NOT CLOSED — see (H3)** | READ FIRST and §7 are fixed and honest. **§5 Catalysts still reads *"The half-year 2026 results. None had been published as at 30 August 2026"*** (delivered PDF line 681, read as a rendered image on page 14), and **`GAP_REVIEW_10-09-2026.md`'s own Verdict still reads *"the half to 30 June 2026 — due, and **not out at this date**"*** — 120 lines below its own "CORRECTED 13 September" note. |
| F11 | §7 listed five gaps under a heading that said six | **CLOSED** | §7 table now has **6 data rows**; `study_numbers.gaps` has **6 keys** (`cash_flow_statement_detail`, `fy2025_results_release`, `h1_2026_results`, `securitisation_pricing`, `per_project_pricing`, `backlog_cost_to_complete`); bibliography §6 has 6 rows. All three agree. |
| F12 | The workbook published three numbers the model does not run | **CLOSED, WELL** | `Assumptions` B8 `0.3548`, B11 `0.07`, B12 `0.2496` labelled *"Cost of capital — ADOPTED (traded default-swap basis)"*, B13 `0.2583` labelled *"…not used … no cell on this sheet reads it"*. `recalc.py:214-225` resolves all six **by label at `1e-6`**, not by row at `0.01`. |
| F13 | A price-solved quantity sat in the numbers file | **CLOSED** | `market_implied_cash_conversion` does not appear anywhere in `study_numbers.json` (`'market_implied_cash_conversion' in json.dumps(N)` → `False`); the six consumers read `diagnostics.json`. |
| F14 | The contested-judgement record was two editions stale | **CLOSED** | `contested_judgements.json`: `as_of 2026-09-10`, `central_adopted 21.08966329162309`, `generated_by engine/phdc_study/contested.py — never hand-edited`. Three of five repriced against the live model (terminal growth alternative now **38.84** against the 21.00 it carried); the two whose levers were retired are named as priced at the edition they were retired in. |
| F15 | Workbook and bibliography stamped two editions stale | **CLOSED** | Workbook `A2` = *"Edition of 10 September 2026. Supersedes 3 September 2026."*; bibliography masthead *"edition of 10 September 2026"*. Both read `edition.py`. Confirmed in the delivered workbook PDF as a rendered image. |
| F16 | Three peer names truncated mid-word | **PARTIAL** | `docx_phdc.py:442-447` `_short_name()` cuts at a word boundary and strips a trailing `&`; B.1 now prints *"Sixth of October Development…"*, *"Pioneers Properties for Urban…"*. **The second half of the finding stands: the table calls that company "Sixth of October Development…" while Figure 4 immediately beneath it calls it "OCDI SODIC"** — read as a rendered image, page 27. |
| F17 | An engine module word reached the reader in a figure title | **CLOSED** | `figures.py:215` `ax.set_title("Published price cone — a distribution of outcomes, not a forecast")`. Read as a rendered image on page 13 of the delivered PDF: the title is exactly that. |

**Closed: 13. Not closed: 2 (F8, F10). Partial: 1 (F16). Closed but regressed: 1 (F5).**

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates run from outside, over every study directory | **PASS** | `python3 scripts/check_study_provenance.py` → *"study directories: 25 · registered indices: 7 · OK — no new violations"*; PHDC is not on the outstanding list of 8. `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` → **2026.09.10**. |
| 2 | The three gates called in the study's own committed code | **PASS** | `gate_check.py:26` `assert_beta_provenance`, `:170` `assert_ground_up`, `:174` `assert_sigcm`, `:182` `assert_model_study`; `wacc.py:101` calls `assert_beta_provenance` again. Run from outside in the sandbox: all four **PASS**, `ISSUABLE: True`. Entry point now reads `edition.py` for its three filenames — (F2) closed. |
| 3 | The study's stamped standard version is the live one | **FAIL — see (H8)** | `study_numbers.json` stamps **`2026.09.01`** (`build_numbers.py:33 _STD_VERSION = "2026.09.01"`) against a live `research_protocol.STANDARD_VERSION` of **`2026.09.10`**. `gate_result.json` in the same directory stamps **`2026.09.10`** (`gate_check.py:21 STANDARD_BUILT_AGAINST = RP.STANDARD_VERSION`). The freeze is deliberate and reasoned — the study is ratcheted on [R-ASSET-01] — and `scripts/check_standard_claim.py` reports OK. It is still a study below the live standard, and two committed files in one directory make two different claims. |
| 4 | **The beta, inspected on its record, not on a boolean** | **PASS, with one gap named** | Re-derived independently of the study: `beta_regression.own_stock_beta("PHDC","EG","EGX")` → **β 1.0481**, n **256** weekly, R² **30.0%**, SE **0.1808**, `index_file = raw_indices/EG/EGX30.csv`, `index_asof 2026-09-08`, `conforming True`; `research_protocol.assert_beta_provenance()` passes on it. **Regressor under `engine/raw_indices/`, not a constituent composite.** Listing exchange read from `assets/data.js:1937` `code: "EGX:PHDC"` → EGX; EGX30 is that exchange's published index. Usability: n 256 ≥ 24, R² 29.97% ≥ 5%, SE 0.1808 < |β| 1.0481. No interim note owed (not DFM), no Qatari caveat owed. `wacc_result.json.beta_record.beta = 1.048052` is the number the model discounts on. **The gap: `beta_result.json` in the same directory is a SUPERSEDED regression — β 1.0493, n 251, `index_asof 2026-07-22` — and `scripts/check_study_provenance.py:52` reads that file, not the one the study uses.** Both conform, so nothing is concealed; the gate is again measuring an object the study does not consume. |
| 5 | **The ground-up clause, inspected on the driver record** | **FAIL — see (H2)** | `research_protocol.assert_ground_up(gate_check.ground_up_record(), ticker="PHDC")` run independently → `{'lines': 1, 'share_by_level': {'unit': 0, 'derived': 0, 'segment': 1.0, 'topdown': 0}, 'unit_share': 0}`. **100% of revenue at `segment` level, 0% at `unit`**, with a gap note that names exactly what is undisclosed (no per-project unit mix, unit area, price per sqm or construction cost per sqm) — that part is honest and correct. **The record's `price_basis` field states the opposite of what the model does**: *"gross margin is an OUTPUT of price against cost … never an input"* against `bottom_up_model.py:224 cost_per = rev_per * (1 - GM_FORWARD)`. |
| 6 | SIGCM's eight clauses | **PASS on seven; the eighth is (H2)** | `research_protocol.assert_sigcm(gate_check.sigcm_checklist())` passes. Historicals official-only: 123 of 123 registry inputs tier A or B, every company figure sourced to `ir.palmhillsdevelopments.com/en-us/financial/resultcenter`. Debt LC/FX split, asset-conversion cycle (§1.1's collection table: 1,045 days receivable, 304 days WIP, 66 days payables), competitors (B.1, seven Egyptian developers on one procedure), formula-based model, beta against the exchange index (row 4), flags raised before issue, stop-and-inform honoured. **`forecast_ground_up` is attested on a record whose price basis is false** — row 5. |
| 7 | Step 0.0 data-quality gate | **PASS** | `own_stock_beta()` runs `data_quality` on both series and returned `stock_dq: []`, `index_dq: []` — zero flags on 256 weekly observations of PHDC and on the EGX30 series to 2026-09-08. `python3 scripts/check_data_freshness.py` → *"93 entries checked against 93 libraries — 0 failure(s), 0 warning(s)"*. |
| 8 | Step 0 calibration under the band record | **PASS** | `band_record.by_key()[('EG','PHDC')]` → `n=57, hits=52, cov50=0.5088, cov80=0.8596, cov90=0.9123, width=1.4689, strength='long', flag=None, p_value=1.0` — two-sided binomial at the 5% level, not flagged. §3 publishes it as plain language with the **count beside the percentage**: *"Over **57** resolved three-month forecasts on this company, the price finished inside the 90 per cent band 91.2% of the time, and inside the middle band 50.9% of the time."* Width ratio disclosed and not gated: *"about 1.47 times as wide as a simple random-walk band."* No calibration appendix — the document has exactly three (A, B, C). |
| 9 | Depth 1 — standalone bibliography at full register depth | **PASS** | `PHDC_Bibliography_10-09-2026.docx` / `.pdf`, 12 tables. §1 primary documents (10 rows, each naming publisher and what it was used for); §2 **full input register, 123 inputs grouped by layer** in seven headings (Company — balance sheet / balance sheet 31 Mar 2026 / borrowings / cash flow / income statement / operating; Market); §3 judgements each with what would overturn it (9); §4 negative results (5); §5 where two sources disagree (4); §6 what is not disclosed (6). B.3 in the study says *"This study is built on **123** sourced inputs"* and the register carries 123. |
| 10 | Depth 2 — four-field provenance on every input, validated | **PASS, with one gap named** | Counted, not asserted: **123 of 123** registry entries carry `value` + `date` + `source` + `tier` (121 also carry `unit`, 2 also carry `note`); zero entries missing any field. Layer is carried as the bibliography's group heading. **The gap: `gate_check.py:142 provenance_four_field=True` is a hard-coded literal — nothing in the study measures what I just counted.** |
| 11 | Depth 3 — numeric traceability, workbook recalculated independently | **FAIL on the builders — see (H4); PASS on the recalculation** | `recalc.py` on the delivered workbook: **231 checks, 0 mismatches**, raising on anything unparseable rather than skipping, resolving `SUM()` ranges rather than stepping over them. Cell-by-cell rebuild diff: **0 of 2,244** non-empty cells differ. **But the rule is that no financial numeral is typed into a builder, and three builders type them**: `docx_phdc.py:669-677` (`"9,330"`, `"34.3%"`, `"3,255"`, `"123,437"`, `"14,625"`, `"263,000 (at 31 Mar 2026)"`), `docx_phdc.py:389` (`PRIOR_WACC_T = 0.1615`), `build_xlsx_phdc.py:266,269` (`"1,308 / 1,281 / 1,500 / 2,000"`), `docx_bibliography.py:203-233` (nine more). |
| 12 | Script-built files verified by a cell-by-cell diff against the delivered file | **PASS** | Full rebuild in the sandbox: `python3 build_all.py` → *"all 15 steps clean"*. Then, against the delivered files: `study_numbers.json` **5,722 leaves, 0 differing**; workbook **2,244 non-empty cells, 0 differing**; study document **215 paragraph-and-table items, 0 differing**; bibliography **41 items, 0 differing**; all four figures **byte-identical** (`cmp`). |
| 13 | Depth 4 — external-reader scrub | **PASS** | `band_record.assert_no_verdict_tokens()` over all three delivered PDFs, the two current `.docx` and a fresh render: **0 hits on every one**. A separate 40-term internal-vocabulary scan returns 3 and 4 apparent hits, all false positives on ordinary words (*measuring*, *bearing*, *offering*, *during*, *bring*, *clustering*); zero real hits. No PASS/PARITY/FAIL and no CRPS beside the company name; calibration evidence lives in §3 as prose with the statistics inline. |
| 14 | Depth 5 — figure discipline, every figure read as a rendered image | **PASS, with two gaps named** | Independent PIL check outside the study: four figures, **alpha extrema (255,255) on every one — zero transparency**, corner pixels `(252,252,251)`, luminance 252 on all four corners of all four. All four rebuilt in-pass and byte-identical. **I read all four as rendered images.** Figure 2's bolding is correct and matches its title on all five rows (the two bracketing 14.40 bold, the three rows with no bracketing pair carrying none). Figure 3's title is fixed — (F17). Figure 4 matches the re-derived beta (PHDC 1.05, ±1 SE bars, market line at 1.00). **Figure 1's caption still names the wrong prior edition and its "6.63" label still abuts the diamond** — (G2), (G3). |
| 15 | Depth 6 — table discipline, programmatic across every table | **FAIL — see (H1)** | Measured with the model report's own criterion (`adnocls_study/qc_checks.py`: over-wide > 7.0in, starved < 0.55in, bloated > 75% of table width). **PHDC study: 35 tables — over-wide 0, starved 6, bloated 4. PHDC bibliography: 12 tables — over-wide 0, starved 14, bloated 0.** ADNOCLS on the same code: 51 tables, **0 / 0 / 0**. The study's own `column_audit` returns `[]` because it sets starved at 1.0cm rather than 0.55in and **does not test bloated at all**. |
| 16 | Depth 7 — expert appendix at maximum detail | **PASS** | C.1/C.2/C.3 each carry worldview, when-it-works / when-it-fails, a worked table with every intermediate line (C.2 runs the NAV from investment property to per-share 6.63), a named sensitivity with numbers (C.2: *"A 10 per cent write-down of that book would remove roughly EGP 9,923 million, about EGP 3.47 a share"*) and a falsifier stated in advance. C.4 cross-examination: four challenges, three CONCEDED and one REJECTED with the reason. C.5 the three in one room. C.6 divergence table isolating which assumption drives which gap. **Labels are "Expert 1/2/3" only** — no persona name anywhere; the verdict-token scan confirms. |
| 17 | Depth 8 — the central contested judgement computed both ways | **PASS** | The crux is cash conversion and it is published as a range, never averaged, in all four required places: the summary table (4.98 / 21.09 / 55.07 with the "What moves it" cell reading *"cash conversion, then the cost of capital"*), the body (§1.7, §1.9's 5×5 grid), the workbook (`Sensitivity` 5×5 and `Summary` rows *"Weak cash conversion (3.9%) 7.65"* / *"Average 21.09"*), and Expert 1's range 7.65–46.85. Separately, the FY2025 cash-flow wedge is published **two ways rather than reconciled by a plug** — A.3 *"If cash conversion holds"* and *"If the collection cycle holds"*, and the `Cash Flow` sheet is headed *"forecast, both…"*. |
| 18 | Structure and sheet list match the model report | **PASS** | 44 Word headings in the model order, section for section against `research_protocol.MODEL_STUDY['word_skeleton']`: masthead + READ FIRST · Headline · Valuation summary · Company overview · 1.1–1.9 · 2 · 3 · 4 · 5 · 6 · 7 · Appendix A (A.1–A.3) · Appendix B (B.1–B.3) · Appendix C (C.1–C.6) · About · Disclosure. Workbook sheets: `wb.sheetnames == MODEL_STUDY['excel_sheets']` — all 16, in order. The excluded section *"What changed in these editions, and why"* is absent. |
| 19 | The class fits none of the three reference names, so the nearest pattern is named and justified | **FAIL, minor but unstated** | `lens_record.class = "real-estate developer, off-plan, percentage-of-completion"` — not ADNOCLS's operating company, not ADCB's bank, not ALPHADHABI's holdco. `grep -ni "adnocls\|reference pattern\|nearest pattern\|modelled on"` over both delivered documents returns **nothing**. The study adapts the operating-company lens inside ADNOCLS's skeleton in fact, and says so nowhere. |
| 20 | 3y historical + 5y forecast with the full FCFF waterfall shown inline to PV | **PASS** | A.1/A.2 carry FY2023 / FY2024 / FY2025 actual and 2026–2040 forecast. §1.1's *"The cash-flow calculation in full"* runs every line inline: operating profit → cash from operations → as a share of revenue → plus finance cost after tax → less capital expenditure → **free cash flow to the firm** → cost of capital that year → discount factor → present value; then the bridge, every deduction on its own line, to *"Value per share, EGP 21.09"*. It does not stop at FCFF. |
| 21 | v2 WACC — country risk once, marginal Kd, market-value weights, both ERP bases published | **PASS** | `wacc_result.json.schedule.cds`: `rf_observed 0.2300` − `default_spread 0.0341` = `rf_star 0.1959`; `ke_exp = rf* + β·ERP_mature + λ·CRP` = 0.1959 + 1.048052×0.042268 + 1.0×0.051832 = **0.292031**, country premium charged once and never multiplied by beta. `kd_pretax 0.2550` = sovereign + 250bp, above the sovereign. Weights `we 0.5510` / `wd 0.4490` at market value. **Both bases published side by side** in §1.8's table: credit-rating 25.83%, traded default-swap **24.96% ← ADOPTED**. `python3 scripts/check_cost_of_capital.py` → OK, no PHDC entry; `check_macro_coherence.py` → OK; `ke_terminal_construction = split_premium`, declared. |
| 22 | [R-SENS-01] grids centred on the struck values | **PASS** | §1.9's 5×5 grid: rows 3.9 / 6.0 / **8.7** / 12.0 / 17.9 %, columns 20.96 / 22.96 / **24.96** / 26.96 / 28.96 %. The centre cell is **21.09**, equal to the published central. |
| 23 | [R-DCF-01] the valuation assembled on one page from the study's own numbers | **PASS** | §1.1's bridge: PV of the explicit 15 years 52,181.7 + PV beyond year 15 29,482.4 = EV 81,664.1 − net debt 23,244.7 + associates 3,838.7 + investment property 1,020.5 = 63,278.6 − minorities 2,963.8 = 60,314.7 ÷ 2,859.9 shares = **21.09**. The printed steps add up. |
| 24 | Sweep Register primary-source depth | **FAIL on depth; PASS on what was done** | **No committed sweep register in this directory** — no `sweep.py`, nothing importing `engine/research_sweep.py`. The IR register and fetch log live in `engine/phdc_walkforward/` and the delivered documents name neither. Company site attempted and logged either way: `fetch_attempts.json` holds **89 fetches, 89 successful, 0 failed**, each with a sha256 — but all 89 are one 27-second burst at **2026-08-30T15:45Z** and nothing was attempted at or after this edition. Audited fiscal years from the filing itself: **FY2023, FY2024, FY2025 — three against a target of four**, plus the reviewed 1Q2026. Results releases swept with their statements for FY2023, FY2024 and 1Q2026; FY2025 has statements and **no** release, correctly recorded as a gap. **No COMPANY_IR tag distinct from the audited-statement class** — `tier` is A on 122 of 123 inputs and B on one. |
| 25 | One escalator per driver class with a sourced near-term cost anchor | **PASS, with the gap named** | `CPI = 0.2520` — Egyptian CPI, 2023-25 mean, World Bank — for the FY2024→FY2025 revenue-per-unit step; thereafter `price_growth(y) = _PATH.inflation(y)`, the house Egyptian path. Near-term cost anchors are sourced and named: `EFFECTIVE_PL_RATE = finance_cost_fy25 / INTEREST_BEARING` = **13.91%**, against a 25.50% marginal rate used only for discounting; `sga_ratio` and `da_ratio` from FY2025 as reported. **The gap, stated in the study and not hidden: price and cost escalate on the SAME path, so the margin is flat by construction** — defensible only because the filings disclose no cost decomposition at all, which §1.1 says. |
| 26 | Margins as OUTPUTS wherever the filings disclose enough to build cost per unit | **PASS on the reasoning; FAIL on the record — see (H2)** | `bottom_up_model.py:224 cost_per = rev_per * (1 - GM_FORWARD)` with `GM_FORWARD = GM_1Q26 = 0.354839` held for fifteen years. The reason is now stated in the document and it is a good one: no delivered-unit count is published after FY2024, so cost per unit collapses to price × (1 − margin) **by construction**, whichever way it is written. The delivered surfaces now say "held input". **The driver record the gate reads still says "OUTPUT".** And the measured real cost drift of **+9.65%** is carried at zero, which the study states and sensitises rather than hides. |
| 27 | Walk-forward scope stated in the study, in the protocol's own words if skipped | **PASS** | Ten origins on 2011–2025 at horizons 1–5y is FULL scope, so the protocol's skip wording is not owed. The substance reaches the reader in §7: *"Tested across ten annual starting points on this company's history from 2011 to 2025, this method forecast revenue tolerably but **overstated net profit by about three times**, consistently and in **97 per cent** of tested cases, and **did worse than simply assuming no change**."* `python3 scripts/check_driver_coverage.py` → *"PHDC 12 drivers, all carrying a verified pair"*. |
| 28 | Every non-sourced driver logged to `engine/Fundamental_Driver_Ledger.md` | **FAIL, named** | The PHDC entry is dated **30-Aug-2026** and records the **walk-forward's** ten drivers D1–D10. The drivers this edition actually turns on and cannot source are **not in it**: `DELIVERY_FADE_YEARS = 10` (*"the fade is linear to zero over ten years and it is STATED rather than fitted"*), the 250bp corporate spread over the sovereign (*"the one cost-of-capital input in this study that is not sourced"*, §1.8's own words), the fifteen-year explicit window, and the asymmetric +2.00 / −1.00 point cost-of-capital shift behind the 4.98–55.07 pair. |
| 29 | Asset base vintage against the information set [R-ASSET-01] | **FAIL, on the ratchet with an open escalation** | `python3 scripts/check_asset_base.py` → *"PHDC: asset base as at **2024-12-31** is BEHIND the information set ending **2026-03-31**, and the study declares no `not_restated_since`"*. **Fifteen months on a developer's land bank**, 33.0 million sqm from the FY2024 earnings release, printed to the delivered workbook at `build_xlsx_phdc.py:336`. Ratcheted with a signature, escalation `PHDC-landbank-current-vintage` open; `check_escalations.py` → *"31 escalation(s), every one shaped, searched, and still unanswered or written down"*. Not a new breach; it is why row 3 exists. |
| 30 | The delivered workbook is live | **FAIL — see (H7)** | **175 formula cells against 2,244 non-empty — 7.8% live**, against the model report's 80%. Per sheet: DCF 152, Fundamental Valuation 10, Relative & Normalized 5, SOTP Bridge 3, Summary Financials 3, Balance Sheet 2 — and **zero** on Income Statement, Cash Flow, Segments, Assumptions, Monte Carlo, Sensitivity, Per-Share & Ratios, Peer & Sector and Summary. |
| 31 | **The answer** — the study's own central against the spot it was struck at | **PASS on the artefact; the framing is one-sided** | `meta.central = 21.08966329162309`, `meta.spot = 14.40` (close of 3 September 2026), `meta.gap_vs_spot = 0.46455995`. **The central sits +46.5% ABOVE the price, not below**, so [R-GAP-01]'s below-price trigger does not bite — but the repo gate reads a ±10% band and PHDC is inside its scope. `python3 scripts/check_valuation_gap.py` → **clean**, *"OUTSIDE THE BAND, AND REVIEWED (21): … PHDC +46.5% GAP_REVIEW_10-09-2026.md (audits 21.0897)"*. The review covers **all eight headings** — LATEST FILINGS (l.37) · BASE YEAR (54) · MACRO COHERENCE (65) · DISCOUNT RATE (77) · TERMINAL (90) · BALANCE SHEET (108) · CLAIMS AGAINST THE RECORD (128) · MULTIPLE CROSS-CHECK (141) — and carries `AUDITED CENTRAL: 21.0897`, `AUDITED GAP: +46.5%`. **What a reader still cannot see: the document never states the +46.5%.** The summary table publishes only *"vs the central −32%"*. Both framings belong in the same table. |
| 32 | Delivered PDFs current with the documents they render | **CLOSED MID-PASS — see (H0)** | At `c4bfea9f7` all three were the pre-fix renders and `check_artefact_freshness.py` reported **3 Tier-A breaches on PHDC**. `0081ebd4c` re-baked them at 13:12. Re-run now: **no PHDC section in the output**, Tier-B breaches for PHDC gone. Delivered study PDF is 32 pages, `CreationDate 13:14:24`, and **0 differing text lines** against my own render of the current docx. |

---

## What blocks delivery

**(H0) — CLOSED DURING THIS AUDIT, recorded because it is the largest thing this pass found.**
At the start of the pass the three files a reader opens were the **pre-fix renders**. Proof, not
inference: `pdftotext -layout` on the delivered study PDF diffed against a LibreOffice 24.2 render
of the docx at `b2eddd235~1` gave **0 differing lines**; against a render of the current docx,
**1,717**. Both PDFs 27 pages, the current docx 32. `pdfinfo` put the delivered file's creation at
**06:39:02**, six hours before the first fix commit. Read as rendered images, page 1 still said
*"it changes ONE thing"*, *"Nothing else in the study moves"* and *"No half-year 2026 figures had
been released at that date"*; page 4 still printed the row label *"Gross margin (output)"*, the
caption *"what price per unit and cost per unit leave behind"*, the prose *"the average of the last
two disclosures"* and the broken order-book figures *"1,152,92 / 1"* and *"1,658,38 / 8"*. Every
document-facing finding was live in the delivered file. `0081ebd4c` closed it, and `build_all.py`
now carries `bake_docs_pdf.py` and `bake_model_pdf.py` as steps, which is the durable half.

**(H1) The fix for (F5) took both documents from zero table-width defects to twenty-four.**
Measured on the model report's own criterion, `engine/adnocls_study/qc_checks.py` — starved
< 0.55in, bloated > 75% of a multi-column table, over-wide > the text block:

| | tables | over-wide | starved | bloated |
|---|---|---|---|---|
| study, at `b2eddd235~1` | 35 | 0 | **0** | **0** |
| study, at HEAD | 35 | 0 | **6** | **4** |
| bibliography, at `b2eddd235~1` | 12 | 0 | **0** | 0 |
| bibliography, at HEAD | 12 | 0 | **14** | 0 |
| ADNOCLS model report, same code | 51 | 0 | 0 | 0 |

The shape is one defect repeated: the fitter sizes the first column to its longest **header** and
squeezes the text columns. §7's gaps table (table 18) carries widths **10.2 / 2.9 / 2.7 cm** against
longest cell contents of **40 / 549 / 77 characters** — a 549-character paragraph in a 2.9 cm
column. Read as rendered images of the delivered PDF, **that six-row table now prints one row per
page across pages 15 to 20**, and page 20 is four-fifths blank. It is why the document grew from 27
pages to 32. The same shape is on B.2's risk register (11.8 / 2.0 / 2.4 against 61 / 52 / 77) and
B.1's peer table, whose Company column is 2.6 cm so every name wraps to three or four lines. The
study's own `column_audit` reports CLEAN and correctly so on what it measures — it sets starved at
1.0 cm and **does not test bloated at all**, which is one of the three counts the depth standard
names.

**(H2) (F8) is not closed on the one surface the gate actually reads.** `gate_check.py:50-53`, the
`DriverLine.price_basis` that `assert_ground_up()` inspects and that this gate is required to
inspect on the record rather than on a flag:

```
price_basis=("Realised revenue per period from the audited statements; gross "
             "margin is an OUTPUT of price against cost on the same completion "
             "clock, never an input."),
```

against `bottom_up_model.py:224 cost_per = rev_per * (1 - GM_FORWARD)`. The commit message says
*"All four now say it is a held input and why"*; three do. `bottom_up_model.py:15`, the module
docstring, also still reads *"GROSS MARGIN IS AN OUTPUT of the two, never an input"* — contradicted
by its own correction 205 lines below at `:220-222`. A third disagreement sits between them: that
correction says the margin *"is declared as one at the **'derived'** level"* while the record
declares `level="segment"`.

**(H3) (F10) is not closed: two delivered places still say the half-year filing does not exist.**
§5 Catalysts, on page 14 of the delivered PDF, read as a rendered image: *"The half-year 2026
results. **None had been published as at 30 August 2026**, which is itself notable this late in the
year."* The release the study's own `inputs.py` names is dated **18 August 2026** — twelve days
before that. The same document's page 1 now says the opposite. And `GAP_REVIEW_10-09-2026.md`'s own
**Verdict**, 120 lines below its *"CORRECTED 13 September"* note, still reads: *"If the half to 30
June 2026 — **due, and not out at this date** — prints conversion near the bottom of that span…"*.
A correction that leaves the same claim standing twice in the same two files has not been made.

**(H4) Financial numerals are typed into three builders and print to all three delivered files.**
The depth standard is that every builder reads the committed numbers file and no financial numeral
is typed into one. `docx_phdc.py:669-677`, the company-overview table on page 3:

```
["Gross profit (EGP mn)", money(v("gross_profit_fy25")), "9,330"],
["Gross margin",          pct(D["gross_margin_fy25"]),   "34.3%"],
["Profit after tax and minority (EGP mn)", money(v("npat_mi_fy25")), "3,255"],
["Order book (EGP mn)",   "263,000 (at 31 Mar 2026)",    money(v("backlog_fy24"))],
["Total assets (EGP mn)", money(v("total_assets")),      "123,437"],
["Shareholders' funds (EGP mn)", money(v("total_equity")), "14,625"]],
```

The 2025 column reads the numbers file; the **2024 comparative column is typed**, and the order
book is typed on the 2025 side. Three of the four typed figures already exist in
`study_numbers.json` — `123437`, `14625` and `9330` are all in it; `gross_profit_fy24` is
`revenue_fy24 − cogs_fy24` = 27,167.3 − 17,837.2 = **9,330.1** from two committed registry rows.
They are right today and they cannot follow a restatement. Also `docx_phdc.py:389
PRIOR_WACC_T = 0.1615` — the superseded terminal rate the supersession sentence prints — typed,
two lines above `_PRIOR = N["prior_edition"]`, which is the committed record it should have come
from. `build_xlsx_phdc.py:266,269` types *"1,308 / 1,281 / 1,500 / 2,000"* into the delivered
workbook. `docx_bibliography.py:203-233` types nine more into the delivered bibliography.

**(H5) The document misdescribes its own forecast horizon and its own delivery path.**
§1.1's opening sentence, page 4: *"The model runs **five years** from the audited 2025 base and then
a terminal value."* `bottom_up_model.py:47 YEARS = list(range(2026, 2041))` — **fifteen**. The same
document's own bridge two pages later reads *"Present value of the **explicit 15 years** 52,181.7"*
and page 1 reads *"the rate applied to all **fifteen** forecast years"*. A.1's caption repeats it:
*"**Five forecast years**. … Years three to five should be read against the range in section 1.9"*,
over a table whose columns run 2026 to 2040. **And the delivered workbook's
`Fundamental Valuation!A4` still reads *"Present value of the explicit five years"*** beside
52,181.7 — flagged in `QC_GATE_03-09-2026.md` and now in its third edition.

The same sentence also misdescribes the driver: *"2,035 units in 2026, **growing 15 per cent a
year** on the company's own disclosed run of handovers."* `delivery_growth(i)` fades linearly to
zero over ten years — the printed row is 2,035 → 2,340 → 2,656 → 2,975 → 3,287 → 4,395 (2035) →
4,461 (2040), which is +15.0%, +13.5%, +12.0%, +10.5% and then less. **A flat 15% run is the
alternative `contested_judgements.json` records as the one this study REJECTED.** The document
describes the rejected case as the adopted one.

**(H6) The delivered workbook's first page cuts three of its own sentences in half.** Read as a
rendered image, page 1 of `PHDC_Valuation_Model_10092026.pdf`. `READ FIRST` rows 6, 8 and 10 hold
**185, 223 and 327 characters** at `row_dimensions[r].height = 30.0` — two lines. Row 10 loses more
than half of the sentence that names the study's crux: *"The single most important number in this
workbook is cash conversion — the share of revenue that reaches operating cash flow. The company
does not disclose its collection t…"* and it stops. No programmatic check in this repository looks
at it; it is visible the moment the page is opened.

**(H7) The workbook tells the reader every blue cell recomputes the model, and the two most
consequential ones are read by nothing.** `Assumptions!A1` = *"Assumptions — every blue cell is an
input"*, `A2` = *"Change one and the whole workbook recomputes."* Scanning every formula in the
workbook for `Assumptions!` references, the cells actually consumed are **B5, B8, B9, B14–B18, B21,
B23, B24**. Not consumed by any formula: **B12, the adopted cost of capital** — labelled *"the rate
this model discounts at"* — and **B11, terminal growth**, and B6, B7, B10, B19, B20. The DCF's
*"Cost of capital, that year"* row is pasted values (`0.249647, 0.213967, 0.185423, 0.164015, …`)
and the discount factors read that pasted row, not B12. *"Terminal value, discounted"* is a pasted
`29482.4`. A reader who changes the discount rate on the sheet that invites them to changes
nothing. This is the same claim as (G9) in the prior gate, now traced to the specific cells.

**(H8) Two costs of equity live in one document, and the two that reach the reader are the
non-adopted one.** §1.2, page 7: *"Against a cost of equity of **30.8%**, the company is not
currently earning its cost of capital on book."* §1.4: *"would capitalise at the cost of equity of
**30.77%** less growth."* `wacc_result.json`: `schedule.rating.ke_exp = 0.3077459`,
`schedule.cds.ke_exp = 0.2920311`. §1.8's own table marks the credit-default-swap basis
**"← ADOPTED"**. Both prose figures are the rejected basis, neither says so, and the normalised-
earnings lens capitalises at `W["ke_rating"]` (`valuation_v2.py:195, 230, 232`).

---

## Gaps named rather than passed over

| | Gap | Evidence |
|---|---|---|
| G1 | The information set is dated three ways in one document, none of them the edition date. Page 1: *"PRICE DATE **3 September 2026**"* and *"published as at **2 September 2026**"*. §2: *"closed at EGP 15.20 on **23 August 2026**"*. §5: *"as at **30 August 2026**"*. | Pages 1, 12, 14 of the delivered PDF, read as images. |
| G2 | Figure 1's caption says *"the **30 August 2026** edition's central figure"*; its legend reads *"prior edition base **17.86**"*, which is the **3 September** central. The 30-August central was 10.94. | Caption p3, legend inside the PNG, both read as images. Named in the 10-Sep gate, unfixed. |
| G3 | Figure 1's *"6.63"* label still abuts the book-value diamond. | Read as a rendered image, p3. Named in the 03-Sep and 10-Sep gates. |
| G4 | §2 and §3 are struck on the 23-August close of **15.20** while the valuation is struck on **14.40**. Figure 3 now labels the anchor *"close 14.40"* and draws a cone whose one-month 25th percentile is **14.67** — above the price it starts from — because the distribution was computed off 15.20. §2 is at least now explicit about its own close and dates it. | `technical.close = 15.2`, `price_map.spot = 14.4`, `price_map.asof.mc_data = 2026-08-23`, `dist.m1.p25 = 14.67`. |
| G5 | The registry's `spot` row carries `date = "2026-08-23"` while its own source text reads *"3 September 2026"* — and that source text says *"no study in this series is delivered against a stale price"*. It ships in the bibliography's Date column. | `study_numbers.registry.spot`. Named in the 10-Sep gate, unfixed. |
| G6 | `beta_result.json` is a superseded regression (β 1.0493, n 251, as-of 2026-07-22) against the `wacc_result.json.beta_record` the model runs on (β 1.048052, n 256, as-of 2026-09-08) — and `check_study_provenance.py:52` reads the superseded one. Both conform, so nothing is concealed. | Row 4. |
| G7 | READ FIRST: *"the rate applied to all fifteen forecast years falls from 25.102% to 24.965%"*. It is the **anchor** of a glide, not a rate applied to fifteen years: `forward_wacc` runs 24.965 → 21.397 → 18.542 → 16.402 → **14.974%** and stays there. §1.1's table publishes the glide two pages later, so the reader can see it; the sentence still reads as one number. | `wacc_result.schedule.cds.forward_wacc`; §1.1 *"Cost of capital, that year"*. |
| G8 | Expert 2's NAV working closes on *"less other liabilities, **balancing** (52,352.9)"* — a named line rather than a silent plug, which is the exemption the rule allows, but the name is the only thing standing in for a meaning. | Appendix C.2 working table. |
| G9 | The §1.9 grid is cash-conversion × WACC. The model skeleton also names a **terminal-cost-of-capital × terminal-growth** grid, and there is none. The study's justification — that the crux is conversion — is stated in §1.7 and is a reasonable adaptation, but the substitution is not declared. | `research_protocol.MODEL_STUDY['word_skeleton']`, §1.1 clause; `Sensitivity` sheet. |
| G10 | The backlog is a display roll-forward that constrains nothing: read once at `bottom_up_model.py:189`, rolled at `:253`, printed at `:342`, never read back. It is why the reported EGP 284bn — the one figure from the half-year release the study tested — moves the answer by exactly zero. | Carried from the 10-Sep gate; the code is unchanged. |
| G11 | The margin comment above `GM_FY25` still reads *"holding margin at the **average of the two most recent disclosures**"*, which is the anchor the superseded edition used and which (F9) corrected everywhere else. | `bottom_up_model.py`, the block immediately above `GM_FY25 = …`. |

---

## What the filings disclose that the model does not consume

Asked explicitly against the primary-documents table, the IR register and the fetch log, rather
than inferred from the work already done.

- **A half-year 2026 disclosure exists and the model consumes none of it.** `inputs.py:113-116`
  names an 18-August release with an EGP 284bn order book at 30 June and EGP 75bn of Hacienda Ras
  El Hekma sales in the fortnight after. That release also carries half-year revenue, units
  delivered, new sales and profit — **the drivers this model actually runs on**. The study's
  position, that a trade-press report of a filing is a lead and never a source, is correct and is
  now said plainly on page 1 and in §7. What is not defensible is that **nothing was attempted
  after learning of it**: the last logged fetch of the company's own result centre is
  `2026-08-30T15:45:49Z`, twelve days after the release and eleven days before this edition. This
  is still the single most likely thing to overturn the edition.
- **Cost of revenue and units delivered are both disclosed for FY2024, so cost per delivered unit
  is constructible — and is constructed once and then abandoned.** The study computes it, measures
  a real cost drift of **+9.65%**, carries zero, and back-solves cost from a fixed margin for
  fifteen years. The reason it gives is sound (no delivered-unit count after FY2024) and the
  consequence is real.
- **Regional new sales, units sold and price per unit are disclosed for three regions across five
  years** and reconcile to the group total in code (44,570 + 95,082 + 11,364 = 151,016 for 2024).
  They drive the order-book roll. Revenue is driven by units *delivered* through a single blended
  revenue-per-delivered-unit, so the regional price detail the company publishes never reaches the
  revenue line.
- **The 4 February 2026 securitisation discloses four tranches with sizes, tenors and
  national-scale ratings** (13, 36, 60 and 84 months at AA+, AA, A+ and A−). The study uses none of
  it because no coupon is published, and records that correctly as a gap. The tranche sizes and
  tenors are still a disclosed maturity profile the debt stack does not consume.
- **`engine/phdc_walkforward/ir_register.json` holds 45 period entries from 1Q2015 to 1Q2026, each
  with its statements AND its results release, and `fetch_attempts.json` holds 89 primary fetches
  with a sha256 each.** The valuation consumes three fiscal years of it. The rest is available to
  the walk-forward and unused by the study — and **the delivered documents name neither file, and
  this directory holds no sweep register that would**.

---

## Commands run for this gate

```
python3 engine/lessons.py PHDC --class "real-estate developer, off-plan, percentage-of-completion"
python3 scripts/check_study_provenance.py
cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"
python3 scripts/check_artefact_freshness.py       python3 scripts/check_artefact_currency.py
python3 scripts/check_delivered_pdf_currency.py   python3 scripts/check_column_widths.py
python3 scripts/check_valuation_gap.py            python3 scripts/check_standard_claim.py
python3 scripts/check_macro_coherence.py          python3 scripts/check_cost_of_capital.py
python3 scripts/check_bridge.py                   python3 scripts/check_lens_design.py
python3 scripts/check_forecast_anchor.py          python3 scripts/check_workbook_structure.py
python3 scripts/check_asset_base.py               python3 scripts/check_escalations.py
python3 scripts/check_driver_coverage.py          python3 scripts/check_data_freshness.py
# in a sandbox engine tree, never in the study directory:
python3 build_all.py            (15 steps, all clean)
python3 gate_check.py           (4 gates PASS, ISSUABLE True)
python3 recalc.py               (231 checks, 0 mismatches, against the DELIVERED workbook)
# independent of the study's own tools:
beta_regression.own_stock_beta("PHDC","EG","EGX") + research_protocol.assert_beta_provenance
research_protocol.assert_ground_up(gate_check.ground_up_record(), ticker="PHDC")
research_protocol.assert_sigcm(gate_check.sigcm_checklist())
band_record.by_key()[('EG','PHDC')] + assert_no_verdict_tokens() over all three delivered PDFs
                                     and a 40-term internal-vocabulary scan
grep of assets/data.js for the listing exchange code -> "EGX:PHDC"
md5sum of the 03-09 workbook and bibliography against git show f35710466
git show b2eddd235~1 -> LibreOffice render -> pdftotext diff against the delivered PDF
zip-part diff of the docx/xlsx between c4bfea9f7 and HEAD
independent table audit on adnocls_study/qc_checks.py's own criteria, both documents,
   at b2eddd235~1 and at HEAD
PIL alpha + corner-luminance check on all four figures
formula-reference scan for every Assumptions! address the workbook consumes
cell-by-cell openpyxl diff (2,244) · json leaf diff (5,722) · docx item diff (215 / 41)
pdftoppm -r 90..100 -> the study PDF read page by page, the workbook PDF page 1,
   and all four figures, as rendered images, in BOTH the pre- and post-rebake states
```

Deliverable when (H1)–(H8) are closed and re-rendered. Thirteen of the seventeen findings are
genuinely closed and several — (F4), (F12), (F14) — are closed better than they were raised. The
arithmetic reproduces to the cell and the cost of capital is still the most carefully built in the
book. What fails is that one fix regressed a standard it was not aimed at, two fixes stopped one
surface short of the record that carries them, and the document still describes a five-year model
it stopped being three editions ago.
