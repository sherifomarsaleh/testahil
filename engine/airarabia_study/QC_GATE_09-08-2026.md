# AIRARABIA — QC gate, 09-Aug-2026 (filled evidence table)

Study: Air Arabia PJSC (DFM: AIRARABIA), new coverage. Deliverables:
`AIRARABIA_Valuation_Study_09-08-2026_public.docx/pdf` (17 pages, 8 figures),
`AIRARABIA_Valuation_Model_09082026_public.xlsx/pdf` (16 sheets / 39 pages),
`AIRARABIA_Bibliography_09-08-2026.docx/pdf` (14 pages). NOT published to the site
(publishing to be requested separately, per instruction).

**RE-RUN 10-Aug-2026** after the critique-response implementation
(`CRITIQUE_RESPONSE_10-08-2026.md`, buckets A/B implemented in full). Every gate in
this table was re-run on the corrected edition; the evidence cells below show the
POST-FIX state, and the before/after of everything that moved is at the end of this
file ("Post-critique re-run — what changed").

| Item | Verdict | Evidence (actual, not self-certified) |
|---|---|---|
| (a) Structure/content/format/depth match the MODEL STUDY | PASS | 16-section Word skeleton and 16-sheet Excel in the model order, asserted in the builder (`assert wb.sheetnames == ORDER and len(ORDER) == 16`); standalone bibliography document ships; `ModelStudyChecklist` attested and `assert_model_study()` raised nothing (`attest.py` output). Lens pattern: SWDY operating-company set (FCFF DCF primary, relative, normalised, book) adapted to an airline unit build. |
| (b) Tables and graphs formatted as the reference | PASS | Same house palette/masthead/table style (`docx_base.py` shared verbatim); `qc_checks.py`: 23 study + 8 bibliography tables, 0 over-wide / 0 starved / 0 bloated; 8 figures in the house style. |
| (c) Best indicators for the sector (airlines/LCC) | PASS | Unit build on passengers × load factor × per-passenger fare/ancillary; cost stack per passenger with fuel (37% of direct costs) on its own commodity path; fleet-led capacity; EV/EBITDA + P/E relative frame vs LCC peers; JV-network handled explicitly in the bridge — the airline-specific value question. Seat/ASK level not disclosed by the company; flagged, not fabricated. |
| (d) 5-year backtest beats random walk on CRPS skill (>0), roughly uniform PIT | PASS (stated honestly) | `backtest_5y.json`: FULL history 58 windows 2012→2026, scale-normalised skill **+0.0066** (raw +0.0022), coverage 50/80/90 = 0.55/0.79/0.86, PIT chi²(9)=11.31 p=0.255, KS p=0.118 → roughly uniform. Last-5-years subset: skill **+0.0009**, cov 0.58/0.84/0.89, KS p=0.083 (chi² p=0.031 on 19 windows — small-sample; reported, not hidden). Production window set: skill +0.0028, PARITY across blocks {2,3,4}. Market-panel gate (the standing gate): PARITY +0.0068 CI[−0.000,+0.014] on the 19-name refit AE panel. |
| (e) 3y hist + 5y forecast IS/BS + complete DCF with the full waterfall | PASS | Appendix A.1/A.2/A.3 (FY2023-25 audited + FY26-30E); DCF sheet rows 33–44: Revenue → EBITDA → −D&A → EBIT → NOPAT(EBIT×(1−t)) → +D&A → −Capex → −ΔWC → FCFF → discount factor → PV of FCFF, all five years, all live formulas. FOUR audited years obtained (FY2022–FY2025) — above the three item (e) wants; FY2022 carried as context column. |
| (f) Expert appendix worked in maximum detail | PASS | C.1–C.3: worldview, when-it-works/fails, worked table with every intermediate line, named sensitivity with numbers, falsifier in advance, per expert; C.4 cross-examination (concessions and rejections); C.5 three in one room + Figure 8; C.6 divergence table isolating which assumption drives which gap. |
| (g) Experts labelled Expert 1/2/3 | PASS | Labels only, cast by method (earnings power / owner cash / cash returns vs cost of capital); no persona names anywhere (scrub bans them). |
| (h) Figure numbers readable on dark background | PASS | All 8 figures: opaque solid-light canvas, corner luminance 248/248/248/248, zero transparency (`qc_checks.py` programmatic check). |
| (i) Summary valuation table present | PASS | "Valuation summary — every read at a glance" (page 1-2): four lenses bear/base/bull/weight/vs-spot + weighted central + both contested framings + panel median + market price. |
| (j) Calibration backtest removed from the appendix | PASS | No calibration appendix exists; calibration evidence appears only as plain-language sentences with the statistics inline in §3. |
| (k) No internal-procedure references | PASS | Programmatic scrub (`qc_checks.py`): 0 hits in the study, 0 in the bibliography (banned list incl. step/gate/sweep/register jargon, engine module names, verdict tokens). |
| (l) Bibliography table in a separate document | PASS | `AIRARABIA_Bibliography_09-08-2026.docx/pdf`: primary-documents table (16 documents), FULL input register (163 inputs, four-field, grouped by layer), judgements table with what-would-overturn-each, negative-results table, and two discrepancy notes (the FY2025 filing's own restated-2024 footing inconsistency; the index-feed lag). |
| (l2) Every deliverable ships as a PDF | PASS | `make_pdf.py`: study 17 pages · 16 images · 777 KB; bibliography 14 pages · 377 KB; workbook 37 pages · 88 KB. (Converter initially failed — LibreOffice was installed without writer/calc filter packages; installed them, per the standing lesson, rather than recording an environment limitation.) |
| (l3) Rendered PDFs read before delivery | PASS | All 17 study pages inspected as rendered images on BOTH editions. First edition caught and fixed in-pass: a garbled fuel-sensitivity sentence with a WRONG per-share delta (0.29 → 0.63/7.5% step), a clipped fundamental-central line on the fan chart, overlapping spot/median labels on the distribution figures, a colliding margin label on Figure 2, and a blank page 11 (removed page break). Corrected 10-Aug edition re-read in full (17/17 pages): headline, waterfall, bridge, WACC table, sensitivity captions, A.1 cash-basis restatement, peer frame and live expert legs all render as built; no new defects. Workbook and bibliography PDFs spot-read. |
| (m) = (k) | PASS | Same scrub, kept as a second letter deliberately. |
| (n) Graph text readable, no overwriting | PASS | Fixed the three label collisions found in the rendered read (above); re-rendered and re-inspected. |
| (o) Table column widths | PASS | Programmatic check: fixed layouts, all totals inside the text block, no column < 0.55in, no column > 75% of a multi-column table — 31/31 tables. |
| (p) Terminal value share of EV visible to the reader | PASS | In the EV→equity bridge table (94% row, banded — up from 86% after the leased-fleet charge cut the explicit-window FCFF; stated, not softened), beside the DCF lens in the summary table, and in the workbook: `DCF!C57 = C54/C56` (live formula), linked green on Summary (`=DCF!C57`) — never typed. |
| (q) The workbook calculates | PASS | **673 formula cells** (644 before the critique re-build) vs pasted numeric values confined to the named classes: audited/disclosed history, unit-build bases, the Monte Carlo map, sensitivity grids, Expert 3's invested-capital legs and the published scenario-output vectors — READ FIRST names them. Now ALSO live in-sheet: Experts 1 and 2 (full formula legs), the fee-stream annuity, the split anchor roll, the gross-debt walk with the booked-rate average, the dividend floor (`=MAX`), and a six-cell Scenario engine (passenger/fare/fuel/capex multipliers, rate shift, high-fuel switch) woven through the base formulas so bear/bull/high-fuel reproduce in-sheet from neutral defaults. |
| (r) Formula cells reproduce the model; drivers propagate | PASS | On the delivered file: "**673 of 673 formula cells reproduce the model, 0 unresolvable, 0 unchecked**" + 43 headline reconciliations (`recalc.py`); driver test: **31 drivers each move the headline in the asserted direction, zero dead inputs** (`driver_test.py` — now including the jet-fuel price path, fuel intensity, leased-aircraft units, the booked finance rate and all six scenario cells). Two driver-test failures across the build were each diagnosed as wrong EXPECTATIONS, mechanism decomposed and documented in the test file before re-pointing (JV growth reaches no lens by construction post-de-JV: non-cash, dividends floored — it accrues in forecast equity, which is what the case now asserts). |
| (s) Primary-source access | PASS | Company site/IR attempted and logged (SUCCESS — both airarabia.com IR and the DFM page); audited FS obtained for **4 complete fiscal years** (FY2022–FY2025), each cited to the filing itself; study-year quarter Q1-2026 swept in before the build; IR presentations tagged separately (3 sources). No aggregator anywhere in the FS build path (the only aggregator use: the DFM index series for the beta regressor and labelled peer cross-checks). FY2022-24 PDFs are scans — machine-read locally with figures cross-checked against later filings' typed comparatives. |
| SIGCM (standing hard gate) | PASS | `assert_sigcm()` raised nothing: historicals official-only; forecast ground-up at the finest sourced level with gaps flagged; debt all-AED (stated in lieu of an FX split — no FX tranches exist); asset-conversion cycle (deferred income / payables / maintenance provisions) measured from the statements and projected; competitors studied in and outside the UAE as cross-checks; beta from own history vs the stock's own local index (DFMGI); every constructed statement formula-based; flags raised before issue; nothing required a stop-and-inform (primary sources were reachable). |
| Model-study depth bar (8 standards) | PASS | `assert_model_study()` raised nothing; per-standard evidence in `attest.py` comments and this table. |
| Verify by import | PASS | market_profiles (AE ν=8.0/0.979 asserted), research_protocol, wacc_builder, adaptive_width, research_sweep, technicals, apply_technicals, ta_chart, rollforward_one, data_quality, mc_v3 — all imported, not parsed. assets/data.js and app.js untouched (no publish), so the JS gate is N/A this session. |
| Sweep register validation | PASS | `sweep.py`: 4/4 rings, 24 findings (15 S · 3 D · 3 C · 3 negative searches), driver-gate 8 rows (7 bottom-up, 1 evidenced top-down), primary access logged (2), FS years 4, study-year Q1-2026 covered, IR findings 3 — zero validation errors. |
| Cost-stack escalators | PASS | One escalator per driver class: fuel on the EIA/IATA commodity paths (dual-framed, never a CPI proxy), staff on wages, maintenance on MRO inflation, airport charges on UAE CPI, other-direct held flat against the wet-lease unwind — each stated in the drivers table with its sourced near-term anchor (jet $158.77 IATA monitor; Brent $81.9→$64.8 EIA July STEO). |
| Ledger discipline | N/A (by instruction) | No site publish, no ticker page, no forecast-ledger rows written this session — the user will request publishing separately; the strike (`strike_result.json`) is committed with the study so the eventual publish uses this anchor. |

## The materiality item for PR review (calibration side)

Adding AIRARABIA refit the AE panel 18→19 names (261→279 pooled windows): pooled ν 10→8
with width_cal unchanged at 0.979 — the published 90% cone moves **0.66%**, inside the 5%
band. Market verdict PARITY unchanged. **ENBD: BOUNDARY(PARITY-flagged) → PARITY** — a
verdict-category change, which is exactly what the unattended pipeline stops for; it is an
improvement (the flag clears; no verdict worsens, no new FAIL, no lost PASS) and this PR is
its human review. AIRARABIA itself arrives PARITY (+0.0013, CI_b2[−0.026,+0.013]).

## Shortfalls stated openly (none silent)

- **PIT uniformity on the small recent subset**: chi² p=0.031 on 19 windows (KS p=0.083);
  full-history PIT is roughly uniform (p=0.255). Stated in §3 in plain language.
- **Fleet-capex owned/leased split undisclosed** — the build's weakest driver, top-down by
  evidenced absence, sensitised ±30%.
- **Fuel hedge ratios undisclosed** — sensitivity overstates both directions somewhat.
- **DFMGI index feed lags 3 weeks** (to 16-Jul-2026) — beta window truncated to overlap,
  3 of ~260 weekly observations lost; recorded in the bibliography.

## Post-critique re-run — what changed (10-Aug-2026)

Implemented from `CRITIQUE_RESPONSE_10-08-2026.md` (71 findings: 49 accept-implement,
1 accept-defect/reject-fix, 4 researched-now, 15 rejected-with-receipts, 2 judgement)
plus the deeper bottom-up rebuild. Every gate above re-run on the corrected edition.

| Quantity | Before (09-Aug) | After (10-Aug) | Why it moved |
|---|---|---|---|
| Weighted central (base framing) | 3.86 | **3.61** | leased-fleet additions charged in FCFF; observed-spread rf*; de-JV'd normalised lens; NCI at audited book |
| Weighted central (JV-capitalised) | 4.12 | **3.86** | same engine, JV framing unchanged in method |
| DCF lens (base) | 3.83 | **3.51** | leased gross ROU additions (AED 175mn/aircraft × 1/2/2/2/2 units) now inside the waterfall |
| Net risk-free rate rf* | 4.06% | **4.44%** | netting the OBSERVED July-2026 auction spread (4bp) instead of the rating-table 42bp; rating-table netting published as the alternative |
| Cost of equity / cost of capital (explicit) | 9.35% / 8.87% | **9.73% / 9.21%** | rf* +38bp flows through Ke; gross-debt weights and the kd_path glide re-built |
| Terminal value share of EV | 86% | **94%** | explicit-window FCFF falls under the leased-fleet charge — an honest consequence, stated in the bridge |
| Fair-value range published | 0.81–7.53 (the widest lens's own span presented as the central range) | **1.81–5.66 probability-weighted**, with the widest single-lens span 0.47–7.23 labelled beside it | critique: the old construction conflated one lens's dispersion with the panel's |
| FY2030E net debt | +1,224mn | **+3,380mn** | gross-debt walk (booked-rate interest on the average balance, amortisation schedule, AED 1,400mn dividend floor) replaces the simpler financing plug |
| Expert legs | E1 4.15 / E2 3.45 / E3 3.51, all pasted | **E1 4.13 / E2 2.08 as live formulas**; E3 3.46 pasted + labelled | workbook must calculate, not store; E2's fall to 2.08 is the leased-fleet charge reaching average FCFF — disclosed, not smoothed |
| Formula cells | 644 | **673** | scenario engine, fee annuity, split roll, debt walk, expert legs |
| Driver-test cases | 24 | **31** | jet price path, fuel intensity, leased units, booked rate, six scenario cells |
| recalc reconciliations | 36 | **43** | new live blocks each get an independent check |

New defects the fixes SURFACED (disclosed, not smoothed): Expert 2's owner-cash leg
falls to 2.08 (the leased-fleet charge hits average FCFF hardest — the panel spread now
honestly shows it); the terminal share rising to 94% makes the DCF more terminal-dependent,
said plainly in §1.1 and the lens-comparison table; FY2030E turns net-debt-positive, which
reverses the bridge's net-cash story by FY2030 and is now shown in A.2. The fuel path and
the JV network remain the two dual-framed contested judgements, each computed both ways.
