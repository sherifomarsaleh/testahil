# TMGH — quality-control gate, edition of 10 September 2026, re-audited 13 September 2026 (second pass)

**Instrument:** Talaat Moustafa Group Holding (EGX:TMGH) · market EG · exchange EGX
(read from `assets/data.js:1971` → `code:"EGX:TMGH"`, never from the `raw_ohlc/EG/` folder)
**Standard stamped:** 2026.09.10 (`study_numbers.json` → `meta.standard_version`), which
equals the live `research_protocol.STANDARD_VERSION` → **2026.09.10**
**Audited at commit:** `4efa98cd9`. All six delivered files were verified **byte-identical**
to the files read at the start of this audit — `sha256sum` matches on the study `.docx`, the
SOURCES `.docx` and the workbook `.xlsx`, and nothing in `engine/tmgh_study/` is modified in
the working tree. (HEAD moved twice during this audit, on SWDY and ADIB work; no commit
touched TMGH.)
**Deliverables audited:** `TMGH_Valuation_Study_10-09-2026.docx` (+ PDF, 27 pages) ·
`TMGH_Valuation_Model_10092026.xlsx` (+ PDF, 48 pages) · `TMGH_Sources_10-09-2026.docx`
(+ PDF, 11 pages)

Filled from **outside** the study [R-ENF-02]. Nothing here is copied from `gate_result.json`
or from the study's own printed table. The builders were re-run in an isolated copy of the
repository and every output diffed cell-by-cell against the delivered file; the delivered
workbook was recalculated by an independent engine; the rendered pages were read as images.
The only file this audit wrote is the one you are reading.

**The procedural finding still bites.** `gate_check.py`, the study's own gate entry point,
is **not read-only** — it calls `DT.main(); DB.main(); XL.main(); FG.main()` at lines 107-110
and rewrites all three delivered files as a side effect of being run. Every run in this
audit was done in an isolated copy for that reason.

---

## VERDICT: **NOT DELIVERABLE.** Nineteen rows fail.

Five of the twenty-five raised on 13 September are genuinely closed, and one more is
substantially closed with a narrower gap named. **All five were verified independently, not
taken on the commit message.** No new defect was found on this pass; the study reproduces
exactly and the repairs landed where they were aimed.

What blocks delivery, shortest list first:

1. **§1.7's crux table still disagrees with §1.7's crux sentence by 30%.** The ladder
   prints *10 years → EGP 100.35*; four paragraphs later the same section says "the faster
   one EGP 77.13", and the faster case IS ten years. (#49)
2. **C.4 still tells the reader §1.9 sensitises the development margin. It does not.**
   There is no margin sensitivity anywhere in the document, and the "roughly a fifth"
   attached to it reproduces from no committed record. (#50)
3. **`GAP_REVIEW_10-09-2026.md` still contradicts itself in the line the gate reads.** It
   states an audited central of 108.19 beside a four-case envelope (60.89 / 62.19 / 138.21 /
   141.39) whose median is **100.20** — those are the at-book cases, and 108.19 is the median
   of the value-share cases. (#52)
4. **The SOURCES primary-documents table still dates the 1H 2026 earnings release as
   reporting the period ended 2025-06-30.** (#51)
5. **Four figures in the delivered documents still contradict the study's own committed
   numbers**: C.4's "the house method levers Egypt's whole country risk premium by a beta of
   1.47" against §1.8 on the same document (#19); §2 and figure 3's last close of **97.80**
   against the document's own **96.60** (#20); §1.5's "twenties to sixties" against a
   cash-flow lens at 77.13–140.74 (#21); and the workbook's `Summary!B17`/`B18` sitting on
   two different premium bases with neither labelled (#53).
6. **The workbook PDF still orphans every continuation column from its row labels**, on
   twelve of its forty-eight pages. (#47)

Two further items are material to the answer rather than to the page: the explicit window
ends **13.92 percentage points** above the terminal on the leg that is capitalised (#14),
and §3 states neither the coverage percentage nor the count while the band record holds
**53 of 57** (#2).

---

## What is genuinely closed since `QC_GATE_13-09-2026.md`

| Was (13-09 gate) | Now | Evidence, verified independently |
|---|---|---|
| **#13 — the delivered workbook's bridge deducted the row labelled "reference".** `SOTP Bridge!B14 = =B11+B13`; the PDF printed 284,807 and 138.21 against the study's 284,849 and 138.23 | **`=B11+B12`, and the reference row now says so on its own line** | Read directly from the delivered `.xlsx`: `B12 = -75,610.96021155221` labelled *"Non-controlling interests at their share of value (adopted)"*; `B13 = -75,652.8` labelled *"  reference: minority at book — **NOT deducted below**"*; **`B14 = =B11+B12`**. **Independently evaluated** by LibreOffice headless, values read back with `openpyxl(data_only=True)`: `B5 = 273165.673549848`, `B11 = 360459.573549848`, **`B14 = 284848.613338296`**, **`B16 = 138.232153959241`** — equal to `valuation.json['rating\|capacity']['per_share_nci_value_share'] = 138.2321539592412` to the last digit. The delivered workbook PDF, read as a rendered image (page 14), prints **284,849** and **138.23**; the study document §1.1 (page 5) prints the same. Present in the 02-09 workbook too, so it had shipped three times; it does not ship a fourth. |
| **#25 (half) — `contested_judgements.json` priced the rejected 15% terminal growth from a `.get` default of 98.17 on a key that never existed**, and carried `as_of: "2026-09-02"` on a 10-09 edition | **Computed at 172.88, and the record is restruck on this edition** | `contested_judgements.json`: `as_of "2026-09-10"`, `published_central 108.19426939740487`, and the terminal-growth judgement reads `value_adopted 140.74273696296459` / **`value_alternative 172.87898361981343`**. `contested.py` now re-runs the model with `M.TERMINAL_GROWTH` swapped and restores it in a `finally` block (lines 55-60), so the alternative is a re-run rather than a default. The literal `98.17` — edition 2's `full` fair value — appears nowhere in the file. |
| **#17 — B.2 typed 44% against §1.6's committed 33.4%**, in three places | **One committed figure, read in all three** | `peers.py:106-112` now interpolates `_IMPLIED_FIN_RATE` from `model.ratios()['implied_finance_rate_fy25'] = 0.3335395`. B.2 of the delivered document, read as a rendered image (page 20): *"the reported charge implies **33.4%** on interest-bearing debt against the 25.50% marginal borrowing rate"*. §1.6 (page 8) and §7 carry the same 33.4%. `inputs.py`'s `finance_cost_split` gap note no longer types a rate at all — it now says *"The implied rate on opening interest-bearing debt — which section 1.6 computes and prints"*. Counted in the delivered study: **"44%" 0 hits**. |
| **#18 — B.2 said cash-negative "from FY2030" against A.3's and the model's 2035** | **FY2035, read off the model's own rows** | `peers.py:81-87` now computes `_FIRST_CASH_NEGATIVE` from `statements.recovery.full_rows`. B.2 (page 20): *"the faster reading goes **cash-negative from FY2035** and needs funding"*. A.3 (page 18) says *"go cash negative **from 2035**"*. Recomputed independently from the committed rows: cash is **+34,444.1 in 2034 and −27,941.1 in 2035**; 2030 closes at **+149,275.8**. |
| **#22 — the risk-free quote's age was typed as "26 days" when it was 35** | **Computed, 35 days** | `wacc.py:21` `_QUOTE_AGE_DAYS = (_ed.EDITION - _dt.date(2026, 8, 6)).days`; imported and evaluated from outside → **35**. `wacc.py:79` and `docx_tmgh.py:834` both interpolate it. §1.8's caveat, read as a rendered image (page 10): *"The quote adopted is **35 days** old at the date of this study."* Counted in the delivered study: **"26 days" 0 hits**. |
| **#4 — the SOURCES masthead read "1 September 2026" on a 10-09 file, and the workbook masthead read "2026-09-02"** | **Both derive from `edition.py`** | `docx_bibliography.py:27` is now `DATE = _ed.WORDS`. Page 1 of `TMGH_Sources_10-09-2026.pdf`, read as a rendered image: *"Sources, inputs and judgements · **10 September 2026**"*. The workbook's `READ FIRST!A2`, read from the delivered `.xlsx`: *"Egyptian Exchange · TMGH · Egyptian pounds · **2026-09-10**"*. The study masthead was already correct and still is, with PRICE DATE 2 September and ISSUE DATE 10 September stated separately. |
| **#3 — three sites typed a date** | **PASS, with one gap named** | `build_numbers.py:15` imports `edition as _ed` and line 62 writes `"edition_date": _ed.ISO`; `docx_bibliography.py:27` reads `_ed.WORDS`. **The gap: `wacc.py:102 SPOT_DATE = "2026-09-02"` is still a typed literal**, and it is the sole source of `meta.spot_date`, so `scripts/check_typed_dates.py` cannot see it — the same shape that hid `build_numbers.py:57`. It is a *price* date, not the edition date, and the study's price date genuinely is 2 September 2026, so this is a gap rather than a wrong figure. |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | Step 0.0 data-quality gate on every price series | **PASS** | Run inside `beta_regression.own_stock_beta("TMGH","EG","EGX")`, whose record is committed: `stock_dq = ["dropped 1 rows carrying a non-positive or non-finite OHLC value (2013-05-07) — vendor fills, not tradeable prices"]`, `index_dq = []`. Both series screened before any return was computed. Read from `beta_result.json`, not from a checklist flag. |
| 2 | Step 0 calibration under the band record — coverage against target, count printed beside the percentage | **FAIL** | The record exists and is per-name: `assets/data.js:3312` → `TMGH: {mkt:"EG", n:57, hits:53, c50:0.4912, c80:0.8246, c90:0.9298, width:1.437, strength:"long", flag:null}` — **53 of 57 = 93.0%**, two-sided binomial flag `null`. §3 of the delivered study, read as a rendered image (page 13), states **neither the percentage nor the count**: *"Across the whole book of companies covered on this market the ninety-per-cent band has caught the outcome close to the ninety per cent it promises… The honest reading of a band that holds about as often as it says is that nothing needs saying, which is why nothing is said here"* (`docx_tmgh.py:916`). The bar requires the statistics inline and the count is never optional. Width ratio 1.437 is in the record and correctly not gated. Prior gate item #2, **unrepaired**. |
| 3 | `edition.py` is the single source of the date | **PASS, with one gap named** | See the table above. `wacc.py:102` still types the price date. |
| 4 | Delivered documents carry the edition's own date | **PASS — repaired on all three** | Study masthead (page 1): *"Valuation study · **10 September 2026**"* with PRICE DATE / ISSUE DATE split, the better form. SOURCES masthead (page 1 of that PDF): *"**10 September 2026**"*. Workbook `READ FIRST!A2`: *"**2026-09-10**"*. Prior gate item #4, **repaired**. |
| 5 | 16-section Word document in the model report's order | **PASS** | `scripts/check_document_structure.py` → *"MATCHES THE MODEL REPORT (23): … TMGH"*. Confirmed by reading the rendered pages beside `engine/model_report/MODEL_REPORT_09-08-2026.docx`: Read first · Headline · Valuation summary · Company overview · 1 (1.1–1.9) · 2 · 3 · 4 · 5 · 6 · 7 · Appendix A (A.1–A.3) · Appendix B (B.1–B.3) · Appendix C (C.1–C.6) · About this study · Disclosure. Titles adapted to the class, which the model report permits. |
| 6 | 16-sheet workbook, the model report's names and order | **PASS on shape** | `scripts/check_workbook_structure.py` → *"MATCHES THE MODEL REPORT (23): … TMGH"*. Opened directly: `wb.sheetnames` equals `research_protocol.MODEL_STUDY['excel_sheets']` in order. **988** non-empty cells, **74** live formula cells. Depth and correctness are #13 (repaired) and #53. |
| 7 | Standalone source register with all five required parts | **PASS, with two gaps named** | `TMGH_Sources_10-09-2026.docx`, 10 tables, all 11 pages read as rendered images: primary documents (8 rows) · the full input register grouped by layer (income statement 41 · reviewed interim 20 · balance sheet 57 · operating indicators 20) · reliability key · judgements with what would overturn each (7 rows) · negative results (7 rows) · "Where two sources disagree" (3 rows) · "How these numbers were checked". `scripts/check_bibliography.py` clean. **The gaps: the primary-documents period column (#51) still ships inside this document, and the register is unreadable to the repository's four-field gate (#8).** The masthead and the typed 44% are both repaired. |
| 8 | Every input four-field complete, validated by assertion | **PASS, with one gap named** | Counted at import from `inputs.py` in an isolated copy: IS 41 + H1_26 20 + BS 57 + KPI 20 = **138** entries; entries missing any of `value` / `source` / `date` / `tier`: **0** (a `None` value is permitted only where a `gap` note stands in its place). Asserted at import, and all 138 appear in the delivered register. **The gap, unrepaired: `scripts/check_four_field.py` still lists TMGH under "NO READABLE REGISTER: BOROUGE, EGCH, PHDC, TMGH, XPT"** — the register lives in `inputs.py` and reaches `study_numbers.json` in a shape that gate cannot read. Same cause as `scripts/check_source_integrity.py`'s *"TMGH UNREADABLE — the committed register carries no dated historical at all"*. |
| 9 | Numeric traceability — the numbers file reproduces, no financial numeral typed into a builder | **FAIL, narrowed to one constant** | The model reproduces exactly (see the reproduction table below). Four of the five typed figures the last gate named are gone: `peers.py`'s 44% and FY2030, `inputs.py:251`'s 44%, `docx_tmgh.py:830`/`wacc.py:69`'s "26 days" and `contested.py:66`'s `98.17` default. **What remains is `docx_tmgh.py:730`**: `_ERPM = W["erp_mature"] if "erp_mature" in W else dam["total_erp_cds"] - dam["sovereign_cds"] * 1.52`. Checked from outside: `'erp_mature' in N["wacc"]` is **`False`**, so the fallback fires and the **constant 1.52** is live. See #10 for what it costs. |
| 10 | [R-COC-03] country risk charged once, never multiplied by beta — reproduced from the record | **PASS, with one gap named** | **The identity reproduces exactly, from the committed record, on both bases and both legs.** Swap basis: `0.195900 + 1.4686659 × 0.042268 + 0.051832 = 0.3098095714` against a recorded `ke_exp` of `0.3098095714`, **delta 0.00e+00**; terminal `0.105000 + 1.4686659 × 0.042268 + 0.027732 = 0.1948095714`, **delta 0.00e+00**. Rating basis: `0.166300 + 1.4686659 × 0.042576 + 0.096824 = 0.3256539205`, **delta 0.00e+00**. `ke_construction = "split_premium"` on both legs of both bases. `scripts/check_ke_reproduction.py` reads TMGH among its 11 records and does not list it as a breach. **The gap, unrepaired:** because `'erp_mature' in N["wacc"]` is False (#9), the swap basis's mature leg, **0.042268**, is applied to the rating basis too — whose own record holds **`schedule.rating.erp_mature = 0.042576`**. Re-run from outside, the rating basis computes 0.32550957 against a recorded 0.32565392, **a 1.44bp miss that passes on a 5e-4 (50bp) tolerance**. The comment three lines above criticises exactly this — *"passes on tolerance rather than on arithmetic"*. |
| 11 | The delivered workbook PDF is the delivered workbook | **PASS** | `TMGH_Valuation_Model_10092026.pdf`, 48 pages, re-exported. Read as images and verified against the `.xlsx`: the SOTP Bridge prints **284,849** and **138.23** (page 14, #13 repaired); the DCF sheet prints discount factor **0.760123** and enterprise value **273,166**. Its legibility is #47. |
| 12 | Independent recalculation of the DELIVERED workbook — anything unparseable reported as a FAILURE | **PASS** | The delivered `.xlsx` was recalculated end to end by LibreOffice in headless conversion and the cached values read back with `openpyxl(data_only=True)`: **74 formula cells, 74 evaluated, 0 unevaluated, 0 error values.** `DCF!B9:K9` hold the forward rate for each year as a blue input — 31.56% / 27.03% / 23.41% / 20.69% / 18.88% flat thereafter — and `B10 = =1/(1+B9)`, `C10 = =B10/(1+C9)` chains the factor off the year before. `B13 = SUM(B11:K11)` = **213,065.10448636** against the model's 213,065.1045; `B16 = B13+B14+B15` = **273,165.673549848** against the study's 273,165.6735. Nothing was unparseable, so nothing is skipped. `scripts/check_workbook_formula_targets.py` → *"25 delivered workbooks examined, 24,234 formula cells parsed"*, clean. `scripts/check_workbook_values.py`: TMGH among the CLEAN (24). |
| 13 | The workbook bridge stands on the adopted minority basis | **PASS — repaired** | See the table above. `SOTP Bridge!B14 = =B11+B12`, evaluated independently to **284,848.613338296** and **138.232153959241**. Prior gate item #13, **repaired**. |
| 14 | [R-MACRO-01] the explicit window runs until growth is within 2pp of terminal | **FAIL** | `macro_record.growth_at_horizon_end = 0.07` is the field `assert_macro_coherence()` tests, and it is **declared, not measured** — `build_numbers.py:320` writes `M.TERMINAL_GROWTH` straight into it. Recomputed here from the study's own committed `statements.capacity.full_rows`: total revenue grows **19.83%** in 2035, the last explicit year (438,428.5 against 365,865.3). The leg actually capitalised as a growing perpetuity — hospitality plus other recurring — grows **20.92%** in 2035 (171,811.4 against 142,088.5) and is then capitalised at **7.00%**: a **13.92 percentage-point** step-down on `pv_terminal_recurring = 39,399` of a `273,166` enterprise value (**14.4%**). The record's own note says *"nothing is capitalised at a rate the model never reached"*; the committed rows say otherwise. `scripts/check_macro_coherence.py` reports TMGH among the conforming, because it reads the declared field. Prior gate item #14, **unrepaired**. |
| 15 | [R-MACRO-01] one house macro path, every growth rate on it | **FAIL** | `macro_record.growth_lines` holds **two** lines against a model carrying **four** growth rates. `model_parameters` carries `HOSP_GROWTH 0.20` and `OTHER_GROWTH 0.22`; neither appears in `growth_lines`, so the coherence check never sees them. The second line, contracted-sales replenishment at 15% nominal, is exempted with `exempt_reason: "a physical sales ladder held flat in REAL terms"` — and `assert_macro_coherence()` skips exempt lines. Computed against the study's own house inflation path (16 / 12 / 9 / 7.5 / 7%): **−0.86% real in 2026, +2.68%, +5.50%, +6.98%, +7.48% in 2030 — +23.48% cumulative real over five years.** The stated reason for the exemption is arithmetically false. Prior gate item #15, **unrepaired**. |
| 16 | [R-COC-01] cost-of-capital schedule from the module, country risk once | **PASS** | `scripts/check_cost_of_capital.py` → *"TMGH EG, 30.10% gliding to 18.86%"*, conforming, not on the outstanding list. Committed record: regime `transition`; rf observed 23.00% less Egypt's own swap-basis default spread 3.41% → rf\* 19.59%, and less the rating-basis spread 6.37% → rf\* 16.63%, the two bases never mixed inside a schedule; ERP 9.41% swap / 13.94% rating, both published, neither averaged; marginal Kd 25.50% = sovereign + 250bp, above the sovereign as a same-currency corporate must be, and cross-checked against the company's own effective rate (24.32% FY2025, 25.08% 1H2026 annualised, `within_tolerance_bp: 42`); market-value weights 92.13% equity / 7.87% debt. See #53 for the one place the two bases ARE mixed on a delivered page. |
| 17 | Prose agrees with the numbers it describes — the finance rate | **PASS — repaired** | See the table above. §1.6, §7 and B.2 all carry **33.4%**, read from `model.ratios()['implied_finance_rate_fy25'] = 0.3335395`; the SOURCES gap note no longer types a rate. Prior gate item #17, **repaired**. |
| 18 | Appendices agree with each other — the cash-negative year | **PASS — repaired** | See the table above. A.3 and B.2 both read **2035**, computed from `statements.recovery.full_rows`. Prior gate item #18, **repaired**. |
| 19 | The document describes its own cost-of-capital construction consistently | **FAIL** | §1.8's "Cost of equity" row (page 9, read as an image): *"the normalised risk-free rate, plus beta times the MATURE premium, plus Egypt's country premium charged once and flat. Beta measures this share's exposure to its own equity market, not to its sovereign, so it is not applied to the country leg"* — correct. Appendix C.4, sixteen pages later (page 25, read as an image; built at `experts.py:300`): *"**The house method levers Egypt's whole country risk premium by a beta of 1.47.**"* That is the identity the study no longer uses and the standard retired at this edition, told to a reader as the house method. Had it been true the answer would be **243bp** higher on the swap basis and **454bp** on the rating basis. Prior gate item #19, **unrepaired**. |
| 20 | The document publishes one last traded price | **FAIL** | Headline, valuation summary, §1.9, figure 1, figure 2 and the workbook `Summary!B2` all carry **96.60** (Egyptian Exchange close, 2 September 2026). §2 opens *"The price closed **97.80** above a flat 50-day (97.45)…"* and derives its whole read from it (*"the last close sits 6% below that high"* — true of 97.80, false of 96.60). Figure 3, read as a rendered image on page 12, is labelled *"**close 23 Aug 2026 97.80**"* under a caption reading *"against the last close"*. Cause: `docx_tmgh.py:88 site_block()` reads the published technical and distribution blocks live out of `assets/data.js`, whose TMGH entry records the split — `px: 96.6, pxDate: "2026-09-02", lib: "2026-08-23"` — and whose `tech` and `levels` are struck on the library close. The SOURCES register states plainly that 96.60 *"SUPERSEDES the 23 August 2026 close of 97.80"*. Prior gate item #20, **unrepaired**. |
| 21 | §1.5 synthesis describes this edition's numbers | **FAIL** | §1.5, read as a rendered image (page 6; `docx_tmgh.py:531`): *"The cash-flow model and the earnings-power model land **in the twenties to sixties**… The relative lens lands **closest to the traded price**."* The delivered cash-flow cases are **77.13 / 78.16 / 138.23 / 140.74** on the adopted basis (53.47–141.39 across all three minority bases); normalised earnings power is 18.58–27.42; the relative lens is 34.04–58.00 against a price of 96.60. The cash-flow lens is the one that straddles the price; the relative lens is the furthest from it. §4's own comparison table, seven pages later, prints the numbers that refute both sentences. Prior gate item #21, **unrepaired**. |
| 22 | The age of the risk-free quote is stated correctly | **PASS — repaired** | See the table above. **35 days**, computed at `wacc.py:21` from `edition.EDITION`. Prior gate item #22, **repaired**. |
| 23 | [R-SIGCM-02] ground-up construction on a driver record, share by level | **PASS on the assertion, FAIL on the committed record** | `RP.assert_ground_up(gate_check.ground_up_record(), "TMGH")` re-run from outside returns **3 revenue lines covering 100.0% of revenue**; **share by level: unit 0% · derived 0% · segment 100% · topdown 0%**; coverage by line: development **58.73%**, hospitality **23.83%**, other recurring **17.44%**; every line carries a `unit`, a `unit_source`, a `price_basis`, a `cost_basis` and a gap note naming what would close it (*"SEGMENT, not unit. TMG publishes unit counts only occasionally — 3,196 homes delivered in FY2025 … and never as a continuous series"*). Unit level is not achieved and is **not claimed**. **But `study_numbers.json` holds no driver-line key at all** — checked directly, no key matching `driver` or `ground` exists — so `scripts/check_ground_up.py` reports *"known TMGH commits no driver-line record at all; [R-SIGCM-02] retired the boolean that used to stand in for one"* and passes it only on the ratchet. Prior gate item #23, **unrepaired**. |
| 24 | SIGCM clause 6 — beta from the exchange's published index, inspected on the record | **PASS** | `beta_result.json` read directly, not the checklist boolean: β **1.4686659268986124**, R² **32.52%**, SE **0.18326339**, n **256** weekly points on `W-THU`, window **4.94 years**, 90% CI **[1.1671976, 1.7701342]**, `dimson true`, `usable: true`, `conforming: true`, `interim_note: null`. `index_file = "raw_indices/EG/EGX30.csv"` — under `engine/raw_indices/`, **not a constituent composite**. The exchange is EGX, read from `assets/data.js:1971` → `code:"EGX:TMGH"`, and `wacc_builder.EXCHANGE_INDEX` maps EG/EGX → EGX30, so the regressor is the published index of the exchange the stock is listed on. No DFM interim note is owed and no `index_interim_note('AE','DFM')` quotation arises; no QATAR10 weekly-only caveat is owed. The usability gate clears on all three legs — n 256 ≥ 24, R² 32.5% ≥ 5%, SE 0.183 < \|β\| 1.469 — so no same-country peer or 1.0 fallback arises. Produced by `beta_regression.own_stock_beta` through `gate_check.beta_gate()` and `wacc.py:113`, not a study-local script. `python3 scripts/check_study_provenance.py` → *"OK — no new violations"*; TMGH is neither outstanding nor exempt. |
| 25 | [R-ENF-05] the reverse read published and kept out of the model | **FAIL, halved** | Published and reproducing: `lenses.implied_discount_rate(96.60)` returns **0.3231986232 / 0.1757914038**, exactly `diagnostics.json`, and both appear in the Headline, C.4 and `Summary!B15`/`B16`. **The sign-test half is repaired** — `contested_judgements.json` is restruck at `as_of "2026-09-10"` and its terminal-growth alternative is computed at **172.88** rather than defaulting to 98.17. **What remains: the reverse read is still inside the model's own record.** `'implied_discount_rate' in lenses.json` is `True` and `'implied_discount_rate' in study_numbers["lenses"]` is `True`; `build_xlsx_tmgh.py:145-147` reads `LENS["implied_discount_rate"]` from there rather than from `diagnostics.json`, as do `docx_tmgh.py:210`, `docx_bibliography.py:221`, `experts.py:291` and `gap_review_calcs.py:303`. |
| 26 | [R-BRIDGE-01] the bridge as a checked record | **PASS** | `scripts/check_bridge.py` → *"TMGH stands on the 2026-06-30 sheet; minority on the value_share basis"*, conforming. Footed independently from `valuation.json`: 213,065.10 + 20,701.71 + 39,398.86 = 273,165.67 = `enterprise_value`; + 75,773.4 − 16,493.0 − 513.9 + 25,228.0 + 3,299.4 = 360,459.57; × (1 − 0.2098) = **284,848.61**; ÷ 2,060.6538 = **138.2322**. §1.1 of the delivered study prints exactly that chain (page 5, read as an image) and it divides — and the workbook now agrees with it (#13). |
| 27 | [R-LENS-03] one class primary is the central, cross-checks beside it | **PASS, with one gap named** | `scripts/check_lens_design.py` → *"TMGH primary dcf, 1 cross-checks"*, conforming. `lens_record.class = "real-estate developer, off-plan, point-in-time on handover"`; primary DCF published as a range 77.13–140.74 with `range_basis` naming the driver, the units and the evidence; cross-checks published beside it and §4 states *"The four are not weighted into a single figure."* **The gap, unrepaired: the record is not declared two-sided.** `lens_record.primary` is a single `dcf` with a `range`, so `northern_star.from_numbers('TMGH')` collapses to the median 108.19 and never puts the faster-conversion branch — **77.13 and 78.16, −20.2% and −19.1% against the price** — on its own [R-STAR-01] bar. |
| 28 | [R-STAR-01] the traded price as the northern star | **PASS, on the collapsed central** | `northern_star.from_numbers('TMGH')` run from outside returns `{"direction": "above", "gap": 0.10628, "tier": "none", "verdict": "PASS"}`. A central above the price needs no written case, so nothing is owed. Two observations recorded rather than passed over: the module used `spot_latest = 97.8 (2026-08-23)` because the OHLC library differs from the study's struck 96.60 — a *staler* price than the study's own — and see #27 for the branch that never got a bar. |
| 29 | [R-DCF-01] the valuation on one page | **FAIL** | §1.1 carries a waterfall (revenue → cost → gross profit → gross margin → overheads → D&A → operating profit → tax → operating profit after tax → add back D&A → capital spend → two working-capital movements → free cash flow to the firm → discount factor → present value of free cash flow) and, after a page break, a separate bridge table. Against the rule's list, **the per-year discount rate is absent** — only the factor is shown — and the two tables are not one. Only **five of the ten explicit years** are printed (2026–2030). `engine/dcf_table.py` is imported by no TMGH module — `grep -rn "dcf_table" engine/tmgh_study/*.py` returns nothing — so `dcf_table()`'s assertion that the present values reproduce the published enterprise value, and its bridge-footing guard, have never run against this study. Prior gate item #29, **unrepaired**. |
| 30 | [R-SENS-01] a sensitivity grid centred on the adopted case | **FAIL** | The model report requires a terminal-cost-of-capital × terminal-growth grid in §1.1, centred on the adopted case. **There is no two-dimensional grid anywhere in the document** — confirmed by reading every page as an image. §1.9 carries a one-dimensional discount-rate ladder of seven rows (22.10 / 26.10 / 28.10 / 30.10 / 32.10 / 34.10 / 38.10%) with irregular steps, whose centre row 30.10% → 140.74 is one of the four published cases and not the study's committed central of 108.19. §1.7 carries a second one-dimensional ladder on conversion years. `engine/dcf_table.sensitivity_grid()` is imported nowhere, so its centre-cell assertion has never run. Prior gate item #30, **unrepaired**. |
| 31 | [R-EPS-01] the company's own reported earnings per share registered beside the profit divided | **FAIL, with one mitigation named** | `scripts/check_eps_reconciliation.py` → *"TMGH registers no reported earnings per share beside its attributable profit, so the gap cannot be seen"*; no `eps_reconciliation` record exists in `study_numbers.json`. **The mitigation: `inputs.py:70` does register `eps_fy25 = 6.98` at tier A sourced to the FY2025 statements**, and the SOURCES register's `shares_outstanding` note uses it as a cross-check. But 14,383.9 ÷ 2,060.6538 = 6.980 exactly, so nothing in the delivered record distinguishes the filed figure from the study's own division, and the rule's first named class — an Egyptian statutory employees' profit share standing between attributable profit and shareholders — is neither confirmed nor excluded anywhere. On the unreadable ratchet, so the gate reports OK; the study is stamped with the standard that adopted the rule. Prior gate item #31, **unrepaired**. |
| 32 | [R-NEWS-01] / [R-PRIME-01] / Sweep Register — primary-source depth | **FAIL on the register, PASS on four of the obligations** | **There is no Step 2A sweep register in the study directory** — no `sweep.py`, no register file, no import of `engine/research_sweep.py`; `python3 scripts/check_sweep_module.py` → *"TMGH no sweep script in the study directory"* (on the ratchet, so the gate is green). With no register, `research_sweep.MANDATORY[Ring.COMPANY]`'s "announced projects, ventures and capacity" category cannot have been closed by a finding or by a dated negative search, and `engine/tmgh_walkforward/ir_register.json` holds **three unread Arabic "latest group news" disclosures dated 10-3-2026, 7-6-2026 and 5-7-2026**. No evidence of an [R-PRIME-01] research primer. What **is** evidenced: the company site was attempted and logged either way — *"138 of the 140 documents sought were obtained, and the two that were not are dead links on the company's own site — the first-half 2009 and second-quarter 2017 results releases"*, with both failures in the negative-results table; audited fiscal years read from the filings themselves are **three** (FY2023, FY2024, FY2025) against a floor of two and a target of four; the FY2024, FY2025 and 1H-2026 results releases are swept **with** their statements; and the whole primary set is `COMPANY_IR`, tagged distinctly with *"via https://talaatmoustafa.com/investor-relations/"* on every row. **What is NOT met: the study-year quarter to 31 March 2026 is disclosed and not swept.** Prior gate item #32, **unrepaired**. |
| 33 | One escalator per driver class with a sourced near-term cost anchor | **PASS** | Each segment's cost is a disclosed ratio of its own revenue taken from the **reviewed first half of 2026** — development 73.4%, hospitality 54.6%, other recurring 68.1% — so the gross margins 26.6% / 45.4% / 31.9% are residuals. `scripts/check_forecast_anchor.py` → *"TMGH group gross margin latest 0.3226 forecast 0.3163 −0.63pp"*, not a breach. Revenue escalators, one per class: development on the disclosed order book converted at a stated rate; hospitality `HOSP_GROWTH 0.20`; other recurring `OTHER_GROWTH 0.22`; replenishment `REPLENISHMENT_SALES 300,000` faded at `SALES_FADE 1.15`. Interest at the marginal 25.50% on interest-bearing debt only. (That two of those four escalators are missing from the macro record is #15, a different failure.) |
| 34 | Margins as OUTPUTS wherever the filings disclose enough | **PASS** | §1.6's driver table states it and the model does it: every cost line is a ratio of its own segment's revenue, so no margin is an input anywhere in the model. `forecast_anchor.note`: *"No margin in this model is an input."* The company-overview table prints the three first-half 2026 margins as outputs of the disclosed cost lines with that stated in the caption, read as a rendered image on page 3. |
| 35 | 3y historical + 5y forecast with the full FCFF waterfall inline to PV of FCFF | **PASS** | Appendix A.1 carries 2023 / 2024 / 2025 as reported and 2026–2030 projected; years 3–5 carry ranges from the walk-forward's own error distribution (2028 revenue 84,517–193,380 on 7 tests; 2029 112,587–315,112 on 6; 2030 90,300–504,013 on 5). §1.1 runs the waterfall to **Present value of free cash flow** (18,893 / 22,150 / 22,956 / 22,878 / 22,532), so it does not stop at FCFF. `scripts/check_waterfall_assertions.py` → *"TMGH OK … 5 waterfall table(s)"*. `scripts/check_forward_ranges.py` → *"TMGH prints a far-year range, shape C"*. |
| 36 | Walk-forward scope stated in the study, in the protocol's own words if skipped | **PASS** | `walkforward_scope`: `{"rule": "R-FCAL-01", "scope": "FULL", "sourceable_fiscal_years": 16, "earliest_sourceable": "FY2009", "status": "run"}`. `scripts/check_walkforward_scope.py` → *"TMGH FULL, 16 years, run"*. §1.6 states it in plain language and About this study repeats it. Not skipped, so no protocol wording is owed. |
| 37 | Every non-sourced driver logged to the Driver Ledger | **PASS** | `engine/Fundamental_Driver_Ledger.md` line 103 opens *"## TMGH — Talaat Moustafa Group Holding (EGX) · 1 September 2026"* and carries the name's driver decisions including the −58% new-sales miss registered as [L-118] (line 136). `scripts/check_lessons_register.py` → *"tmgh_walkforward — 17 harvested finding(s), all resolved (7 registered, 10 declined with a reason)"*. |
| 38 | External-reader scrub — zero internal vocabulary, zero verdict tokens | **PASS** | `docx_helpers.scrub()` run from outside on the delivered files: **`([], 49113)`** on the study and **`([], 16277)`** on SOURCES — **0 hits across 65,390 characters**. `cd engine && python3 -c "import band_record as br; br.assert_no_verdict_tokens('EG')"` run separately: no raise. `scripts/check_delivered_vocabulary.py` lists TMGH under CLEAN (23). No PASS / PARITY / FAIL token and no CRPS appears beside the company name; §3's calibration material is prose in the price section and there is no calibration appendix — the document has exactly three appendices, A, B and C. (That §3's prose omits the statistics is #2.) |
| 39 | Figure discipline — solid light canvas, zero transparency, every figure read as an image | **PASS, with one content defect named under #20** | Four figures, each rebuilt in an isolated copy and **sha256-identical** to the delivered files. Opacity verified on the delivered PNGs from outside: `Image.getchannel('A').getextrema()` returns **(255, 255)** on all four, and the corner pixel is **(251, 250, 247)** — a solid light canvas, no transparency. `scripts/check_figure_opacity.py` → *"every delivered figure is opaque to the pixel"*. All four read as rendered images: fig1 the football field, seven bars each labelled with the basis adopted and its three-basis span (page 2); fig2 the two discount-rate curves with the previous edition's 18% annotated (page 11); fig3 the one- and three-month distribution (page 12); fig4 the segment stack with a reported/projected divider — all legible, no label collisions. **fig3's "close 23 Aug 2026 97.80" is the defect at #20**, not a canvas defect. |
| 40 | Table discipline — fixed layout, explicit widths, the three counts per document | **PASS** | Counted independently from outside against each document's own section text block (study 6.61in, SOURCES 6.77in; starved < 0.4in; bloated > 75% of its own table; over-wide > the text block). **Study: 33 tables — starved 0 / bloated 0 / over-wide 0**, widest table 6.62in, **0 columns with no explicit width, 0 fully empty rows. SOURCES: 10 tables — starved 0 / bloated 0 / over-wide 0**, widest 6.30in, 0 without a width, 0 empty rows. The [L-014] trap was tested explicitly: the `w:tblGrid` the renderer actually reads was compared against the cell widths on every table — **0 disagreements** in each document, and `docx_helpers.column_audit()` re-run from outside returns `[]` on both. `scripts/check_column_widths.py` does not name TMGH. |
| 41 | Expert appendix at maximum detail | **PASS, with one gap named** | Three experts cast by method, read as rendered images (pages 22–26). C.1 (the order book as a receivable stream): worldview, when-it-works, when-it-fails, a 15-line worked table running 491,000 → 89,772 → 86,004 → **41.74** where every intermediate line is printed and the arithmetic foots, a named sensitivity (8/10/12/14/18 years → 45.99 / 43.58 / 41.74 / 40.31 / 38.27) with a caption explaining why it runs the opposite way to §1.7, and a falsifier stated in advance. C.2 (net asset value, goodwill excluded): 9-line table 477,898 → 154,574 → 78,921 → **38.30**, uplift sensitivity +0/25/50/100% → 38.30 / 56.29 / 74.29 / 110.27, falsifier stated. C.3 (capitalise only what recurs): 13-line table 14,890 → 25,789 → 10,795 → 9,054 → 7,017 → 45,939 → 29,567 → **14.35**, growth sensitivity 10/12/15/18/20% → 8.65 / 11.15 / 14.35 / 18.61 / 24.57, falsifier stated. C.4 five challenges, each CONCEDED IN PART / REJECTED / CONCEDED / CONCEDED AS A LIMIT / ACKNOWLEDGED AND PUBLISHED. C.5 the three in one room. C.6 a divergence table (1v2 3.44, 1v3 27.39, 2v3 23.95), each gap reproducing from the three per-share figures. Labels are "Expert 1 / 2 / 3" throughout; **no persona name anywhere**, confirmed by the scrub. **The gap: C.3's central capitalises at "Ke 32.57% less growth 15%" (`experts.py:240`), and the study's own `contested_judgements.json` calls 15% perpetual growth *"eight points of real growth a year for ever, which nothing disclosed supports"*.** An expert may disagree with the house; nothing in C.3 or C.5 tells the reader that this one stands on the assumption the same document rejects. (C.4's two content defects are #19 and #50.) |
| 42 | The central contested judgement computed both ways, never averaged | **PASS** | The order-book conversion period is published at **14 and 10 years** in the summary table (four rows, three minority bases each — 138.23 / 138.21 / 95.84, 77.13 / 60.89 / 53.47, 140.74 / 141.39 / 97.58, 78.16 / 62.19 / 54.19), in the body (§1.7), in the workbook (Summary rows 7–10 and the Sensitivity sheet's two blocks) and in an expert's range (C.1's 8–18 ladder). §1.7 states the two answers side by side. Nothing is averaged; the Headline says so and §4 repeats it. `fair_value_range` names itself *"the envelope of four published cases … It is a range, not a target, and the cases are never averaged"*. (That §1.7's own ladder disagrees with that sentence is #49.) |
| 43 | [R-ASSET-01] the asset base is not older than the information set | **PASS** | `asset_base_record`: land bank **20.0 mn sqm as at 2026-06-30**, sourced to the 1H 2026 earnings release's indicator panel, against `information_set_ends: 2026-06-30` — the same day, so the ordering holds. `scripts/check_asset_base.py` → *"studies IN SCOPE: 11 (… TMGH) … OK — no new breach"*. |
| 44 | The landbank note distinguishes remaining developable land from the group total | **PASS, with one gap named** | `inputs.py:194` states it in terms: *"THIS IS THE REMAINING DEVELOPABLE LAND AND NOT THE GROUP TOTAL, and the distinction is worth six times the number"*. `landbank_group_total_msqm` is committed beside it at **128.0 mn sqm**, sourced to the 31 May 2026 Baghdad-licence disclosure, which appears in the delivered primary-documents table with its own wording. **The gap, unrepaired: the `landbank_msqm` note at `inputs.py:201` still says the group total is "about 115mn sqm"** — the pre-licence figure — while the sibling input and the primary-documents table both say 128. Two figures for the same quantity, one disclosure apart. |
| 45 | Study-local checks point at the delivered edition [L-066/L-067] | **PASS, with one gap named** | `scripts/check_recalculator_target.py` → *"tmgh_study DERIVED target is derived, not typed — cannot go stale"*. `scripts/check_record_survives_rebuild.py` → *"TMGH build_numbers.py survives"*. `engine/fv_movement.py check` → *"[ok] … 12 run directories, 12 records, 12 editions current"*. **The gap, unrepaired: `gate_check.py` is not read-only** — lines 107-110 call `DT.main(); DB.main(); XL.main(); FG.main()` and overwrite all three delivered files when run, so "running the study's gate" is an act that changes the delivery. |
| 46 | Script-built files verified by a cell-by-cell diff against the delivered file | **PASS** | `build_all.py` re-run end to end in an isolated copy of the repository at `HEAD` — **all 19 steps clean** — and every output diffed against the delivered originals. **Workbook: 988 cells across 16 sheets, 0 differences, sheet order identical. Study document: 1,194 paragraph and table-cell items, 0 differences. SOURCES: 1,004 items, 0 differences.** The delivered files are exactly what the committed builders produce, so every defect below is a defect in a builder and not in a delivery. |
| 47 | The delivered PDFs are legible renderings of the delivered files | **FAIL** | All three rendered and read as images. **The workbook PDF orphans every continuation column from its row labels.** Measured with `pdftotext -layout` and scored page by page: of 48 pages, **twelve carry numbers with no row labels at all** — pages **14, 21, 22, 24, 25, 26, 28, 29, 31, 32, 34, 35**. Page 13 carries the fifteen SOTP Bridge row labels with no numbers; page 14 carries the fifteen numbers with no labels — and that is now worse than a nuisance, because the label that says *"reference: minority at book — NOT deducted below"* lives on page 13 while `-75,611` and `-75,653` sit side by side on page 14, where a reader adding the column gets 209,196 and not the 284,849 printed beneath it. The same split runs through the DCF sheet, the income statement, the balance sheet, the cash flow, the summary financials, the sensitivity grid and the peer table. One near-blank study page carries two lines (page 15). Prior gate item #47, **unrepaired**. |
| 48 | **[R-GAP-01] the answer: the study's committed central against the spot it was struck at** | **PASS, with one gap named — and see #52** | `study_numbers.json` → `meta.central = 108.19426939740487`, `meta.spot = 96.6` (Egyptian Exchange close, 2 September 2026), `meta.gap_vs_spot = +0.1200234927`. The central is **12.0% ABOVE** the price, so [R-GAP-01]'s ">10% BELOW" trigger does not fire and no review is compelled. `GAP_REVIEW_10-09-2026.md` exists anyway and covers **all eight required headings** — LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK — plus WHAT MOVED, WHAT THE PRICE IMPLIES and a VERDICT, and states *"THIS FIRES ON THE HIGH SIDE. The question below is not whether the market is right. It is whether WE made an error in the company's favour."* `python3 scripts/check_valuation_gap.py` is **clean** (*"TMGH +12.0% GAP_REVIEW_10-09-2026.md (audits 108.1900)"*, *"OK — no new violations"*). The central is the median of the four value-share cases — (78.156 + 138.232) / 2 = 108.194 — and `meta.central_note` says so. **The gap: `scripts/check_delivered_pdf_currency.py` reports "TMGH … missing: its opening pages carry none of 108.19"** — counted directly, the string "108.19" appears **zero** times in the whole delivered study PDF. The study says why (*"THIS STUDY DELIBERATELY PUBLISHES NO CENTRAL"*), which is an honest design and is recorded rather than scored as a defect. **But the review document itself contradicts the number — #52.** |
| 49 | **§1.7's crux ladder disagrees with §1.7's crux sentence** | **FAIL** | §1.7 is headed *"Everything in this study turns on one number"*. Its table (page 8, read as a rendered image) prints `8 → 71.89 · **10 → 100.35** · 12 → 121.60 · 14 → 138.23 · 16 → 151.66 · 20 → 172.10`, captioned *"At this study's discount rate, with the minority deducted at its share of value"*. Four paragraphs later the same section says *"The slower reading is worth EGP 138.23 a share and **the faster one EGP 77.13**"* — and the faster reading IS ten years (`model_parameters.RECOVERY_YEARS = 10`). Ten years reads **100.35** in the table and **77.13** in the prose, the summary table and the headline range: a **30% disagreement inside one section**, on the study's own declared crux. Cause: `lenses.py:195-201` builds the ladder by varying `M.CAPACITY_YEARS` and calling `VAL.sotp("capacity", …)`, so every row runs the **capacity ramp 0.25**, while the published faster case runs `sotp("recovery", …)` at **ramp 0.35**. The two are not comparable and nothing in the document says so. `scripts/check_prose_figures.py` reports *"prose figures checked: 150; unmatched: 0"* because both numbers are committed — they are just committed to different constructions. Prior gate item #49, **unrepaired**. |
| 50 | **C.4 cites a sensitivity the document does not contain** | **FAIL** | C.4's third challenge (page 25, read as a rendered image; `experts.py:279-280`): *"CONCEDED. The margin is an output of the disclosed cost ratio and it has compressed steadily. **Section 1.9 sensitises it; a 5-point further compression moves the central reading by roughly a fifth.**"* §1.9 sensitises the **discount rate** and nothing else — seven rows, 22.10% to 38.10% — verified by reading page 11 as an image and by counting occurrences of "sensitis"/"sensitiv" in the extracted text: §1.9's own heading, §1.2's *"the most discount-rate-sensitive lens"*, C.1's *"names the sensitivity that would move it most"*, and this sentence. **There is no margin sensitivity anywhere in the document, and "roughly a fifth" reproduces from no committed record.** Prior gate item #50, **unrepaired**. |
| 51 | **The SOURCES primary-documents table mis-states a document's period** | **FAIL** | Read as a rendered image, page 1 of `TMGH_Sources_10-09-2026.pdf`: row 1 is *"TMG Holding 1H 2026 earnings release, Cairo, 12 August 2026"* with **"Period it reports: 2025-06-30"**. The 1H 2026 release reports the period ended **30 June 2026**. Cause: `docx_bibliography.py:61-69` builds the column as `sorted({(e["source"], e["date"]) …})` and keeps the FIRST date seen per source string, so the period printed is the earliest input date that happens to cite that document. `inputs.py:187` registers `backlog_jun25 = 363,800` against `ER26H1` at `"2025-06-30"`, and that comparative is what the table now calls the release's reporting period. The provenance column a reader of a source register checks first is not measuring what its heading says. Prior gate item #51, **unrepaired**. |
| 52 | **The gap review contradicts its own audited central** | **FAIL** | `GAP_REVIEW_10-09-2026.md`, lines 3–6: *"AUDITED CENTRAL: 108.19 — EGP 108.19 a share, the MEDIAN of the four published cases. This study publishes an ENVELOPE, not a point: **rating\|recovery 60.89, cds\|recovery 62.19, rating\|capacity 138.21, cds\|capacity 141.39**."* Those four are the **at-book** minority basis, read straight out of `valuation.json` (`per_share_nci_book`: 60.8894 / 62.1894 / 138.2118 / 141.3888). Their median is **(62.19 + 138.21) / 2 = 100.20**. The audited central of 108.19 is the median of the **value-share** cases — 77.13 / 78.16 / 138.23 / 140.74 — which is the basis the study adopts and `meta.central_note` names. The [R-GAP-01] artefact states a central and, one line later, lists an envelope that cannot produce it. `scripts/check_valuation_gap.py` reads 108.19 and passes, because it reads the central and not the list beside it. Prior gate item #52, **unrepaired**. |
| 53 | **The workbook mixes the two premium bases in adjacent Summary rows, and its terminal rate is not the one the study publishes** | **FAIL** | Read from the delivered `.xlsx`: `Summary!B17 = 0.3155765623218869` labelled *"Cost of capital, first year"* — that is `wacc.wacc_rating`, the **rating** basis — and `Summary!B18 = 0.1886260236772954` labelled *"Cost of capital, once normalised"* — that is `cost_of_capital_record.wacc_terminal`, the **swap** basis. Neither row says which basis it is on, in a workbook whose READ FIRST tells a reader the four cases are *"two ways of measuring Egypt's equity risk premium"*; `build_xlsx_tmgh.py:148-149` writes them from two different dictionaries. Separately, the DCF sheet terminates at `DCF!K9 = 0.1887590108371082`, which the delivered PDF renders as **18.88%** (page 21, read as an image), against the **18.86%** the Summary, §1.8 and §7 of the study all publish as the normalised rate. **A third instance of the same shape in the document:** §1.9's ladder is centred on 30.10% and reads 140.74 / 78.16 (the swap cases) while §1.7's crux sentence and §1.1's waterfall caption are on the rating basis at 31.56% and read 138.23 / 77.13, and §1.9's caption names neither. Immaterial to value and material to the claim that both bases are published and never mixed. Prior gate item #53, **unrepaired**. |

---

## Repository gates, run from outside

| Command | Result |
|---|---|
| `python3 scripts/check_study_provenance.py` | **OK — no new violations.** *"study directories: 25 · registered indices: 7 · current study standard: 2026.09.10 · stamped: 10 · unstamped: 15 · known outstanding: 8 · exempt: 1"*. TMGH is neither outstanding nor exempt |
| `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` | **2026.09.10** — equals `meta.standard_version` exactly |
| `engine/tmgh_study/gate_check.py` (the study's own entry point, run in an isolated copy) | exit **0**; *"SIGCM failures: none · Model-report failures: none"*. AST walk over all 27 `.py` files confirms it calls `RP.assert_beta_provenance` (line 37), `RP.assert_ground_up` (146), `RP.assert_sigcm` (160) and `RP.assert_model_study` (173) in the study's own committed code, and `wacc.py:113` calls `assert_beta_provenance` a second time before the record is written. **It also rewrites all three delivered files — #45** |
| `scripts/check_valuation_gap.py` | **OK — no new violations.** *"TMGH +12.0% GAP_REVIEW_10-09-2026.md (audits 108.1900)"*; price basis *"TMGH last close 2026-09-02"*. See #52 |
| `scripts/check_publish_block.py` | *"HELD TMGH — inside the band at +12.0% of 96.60 (2026-09-02), but Phase 1 is not proven — 2 of 6 acceptance criteria open"*. The hold is a framework state, not a study defect |
| `scripts/check_cost_of_capital.py` | **OK.** *"TMGH EG, 30.10% gliding to 18.86%"*, conforming |
| `scripts/check_ke_reproduction.py` | 11 records read; TMGH not a breach. Its only FAIL is FERTIGLOBE |
| `scripts/check_bridge.py` | **OK.** *"TMGH stands on the 2026-06-30 sheet; minority on the value_share basis"* |
| `scripts/check_macro_coherence.py` | **OK.** TMGH among the conforming. See #14 and #15 — the declared horizon field and the exempt line are what it does not test |
| `scripts/check_lens_design.py` | **OK.** *"TMGH primary dcf, 1 cross-checks"*. See #27 |
| `scripts/check_workbook_structure.py` · `check_document_structure.py` | **OK.** TMGH among the 23 matching the model report on both |
| `scripts/check_workbook_formula_targets.py` | **OK.** *"25 delivered workbooks examined, 24,234 formula cells parsed"* |
| `scripts/check_workbook_values.py` | **OK.** TMGH among the CLEAN (24) |
| `scripts/check_edition_date.py` | **OK — no new violations.** TMGH passes, and now genuinely: all three mastheads carry 10-09 |
| `scripts/check_typed_dates.py` | **OK — no study types a date its own record already states.** See #3 for the one it cannot see |
| `scripts/check_four_field.py` | **OK**, with *"NO READABLE REGISTER: BOROUGE, EGCH, PHDC, **TMGH**, XPT"*. See #8 |
| `scripts/check_source_integrity.py` | **OK** on the ratchet; *"TMGH UNREADABLE — the committed register carries no dated historical at all"*. See #8 |
| `scripts/check_ground_up.py` | **OK** on the ratchet; *"known TMGH commits no driver-line record at all"*. See #23 |
| `scripts/check_eps_reconciliation.py` | **OK** on the ratchet; *"TMGH registers no reported earnings per share beside its attributable profit"*. See #31 |
| `scripts/check_sweep_module.py` | **OK** on the ratchet; *"TMGH no sweep script in the study directory"*. See #32 |
| `scripts/check_terminal_module_use.py` | *"TMGH module — no record committed"*, on the ratchet |
| `scripts/check_terminal_floor.py` | **OK.** *"TMGH — the terminal exposes no tv"* |
| `scripts/check_standard_claim.py` | **OK** — *"TMGH 2026.09.10"*. The gate maps a small set of requirements across two versions and does not map [R-DCF-01], [R-SENS-01] or [R-EPS-01]; the stamp passes on an incomplete map, which is why #29, #30 and #31 are scored here and not there |
| `scripts/check_delivered_pdf_currency.py` | *"TMGH … missing: its opening pages carry none of 108.19"*. See #48 |
| `scripts/check_walkforward_scope.py` · `check_walkforward_actuation.py` | **OK** on scope (*"TMGH FULL, 16 years, run"*); TMGH on the actuation outstanding list, allowed for now |
| `scripts/check_asset_base.py` · `check_forecast_anchor.py` · `check_waterfall_assertions.py` · `check_forward_ranges.py` | **OK** on all four; TMGH conforming |
| `scripts/check_recalculator_target.py` · `check_record_survives_rebuild.py` | **OK** on both |
| `scripts/check_figure_opacity.py` · `check_delivered_vocabulary.py` · `check_column_widths.py` · `check_table_footing.py` · `check_prose_figures.py` · `check_bibliography.py` | **OK** on all six. *"TMGH tables examined: 43; unreconciled totals: 0"*; *"TMGH prose figures checked: 150; unmatched: 0"* — and every one of #19 to #21 and #49 to #53 survives that last one, because they are prose statements or committed-but-incomparable figures rather than numerals it can bind to a key |
| `scripts/check_workbook_live_inputs.py` (new this week) | *"TMGH — 16 input cell(s) no formula reads"*, passed as labelled references |
| `scripts/check_calibration_deliverables.py` | **OK** — *"TMGH edition 2026-09-13"*, which is this gate's own date and not a defect in the study |
| `engine/fv_movement.py check` | **[ok]** — 12 run directories, 12 records, 12 editions current |

---

## Reproduction of the delivered files

| Artefact | Method | Result |
|---|---|---|
| full build | `build_all.py` re-run in an isolated copy at `HEAD` | **all 19 steps clean** |
| `TMGH_Valuation_Model_10092026.xlsx` | rebuilt, then cell-by-cell diff | **988 cells across 16 sheets, 0 differences**; sheet order identical |
| `TMGH_Valuation_Study_10-09-2026.docx` | rebuilt, then paragraph- and table-cell diff | **1,194 items, 0 differences** |
| `TMGH_Sources_10-09-2026.docx` | rebuilt, then paragraph- and table-cell diff | **1,004 items, 0 differences** |
| four figures | alpha channel + corner pixel on the delivered PNGs | alpha extrema **(255, 255)** on all four; corner **(251, 250, 247)** |
| delivered workbook | **independent evaluation: LibreOffice headless recalculation, values read back with `openpyxl(data_only=True)`** | **74 of 74 formula cells evaluated, 0 unevaluated, 0 error values.** `SOTP Bridge!B14 = 284,848.613338296`; `B16 = 138.232153959241`; `DCF` enterprise value 273,165.673549848 |
| delivered workbook | `recalc.py`, the study's own re-derivation by row label | 85 checks, 0 mismatches — necessary and not sufficient, which is the whole lesson of the last three editions |
| delivered files against the start of this audit | `sha256sum` on the study `.docx`, the SOURCES `.docx` and the workbook `.xlsx` | **identical**, and `git status` on `engine/tmgh_study/` is empty, so this audit is of a stationary edition |

Every defect above is therefore a defect in a builder, reproduced faithfully into the
delivery.

---

## What the rendered read caught that no programmatic check could

Reading the pages as images, and recomputing the study's own claims against its own
committed model, produced these findings on an edition every gate above reported clean.

1. §1.7's ladder printing **10 years → 100.35** four paragraphs above *"the faster one EGP 77.13"* (#49).
2. C.4 telling the reader **§1.9 sensitises the development margin**, which it does not (#50).
3. The SOURCES primary-documents table dating the **1H 2026** release **2025-06-30** (#51).
4. The workbook's DCF sheet terminating at **18.88%** against the 18.86% the study publishes, and `Summary!B17`/`B18` sitting on two different premium bases (#53).
5. C.4's *"levers Egypt's whole country risk premium by a beta of 1.47"* against §1.8 on the same document (#19).
6. §2 and figure 3's **97.80** against the document's own 96.60 (#20).
7. §1.5's *"twenties to sixties"* against §4's own table seven pages later (#21).
8. The workbook PDF orphaning row labels on twelve of forty-eight pages, and putting the adopted and reference minority figures side by side on a page that carries neither label (#47).

Recomputation from the committed model adds: the explicit window ending **13.92pp** above
the terminal on the capitalised leg (#14); the *"flat in REAL terms"* exemption being
**+23.48% real** over five years (#15); and the gap review's envelope having a median of
**100.20** beside an audited central of 108.19 (#52).

**And the rendered read is also what confirmed the repairs.** The five closed rows were each
checked on the page a reader sees — 284,849 and 138.23 on page 14 of the workbook PDF,
33.4% and FY2035 on page 20, 35 days on page 10, *"10 September 2026"* on page 1 of SOURCES
— rather than on the commit message or on the builder's own comment.

---

## Asking the question the self-audit cannot: what do the filings disclose that the model does not consume?

Answered against `engine/tmgh_walkforward/ir_register.json`, **375 documents**, which is the
closest thing this study has to a Sweep Register — it has no register of its own (#32). The
delivered primary-documents table lists **eight**. By class the register holds earnings
releases 70, consolidated statements 27, **separate-entity statements 27**, investor
presentations 19, market updates 11, annual reports 6, executive-management reports 5, board
annual reports 2, other 208. Nineteen documents touch 2026.

* **The first quarter of the study year, and it is a clean miss.** The register holds *"TMG
  Consolidated financials as of 31 March 2026"*, the separate statements for the same date,
  the Arabic executive-management report for the period to 31 March 2026, the audit
  committee's summary and the business-results summary for that period. None is in the
  primary-documents table and none is the source of any of the 138 inputs. The 30-June
  interim carries the three- and six-month columns, so nothing the model uses is stale — but
  the quarter's own operating anchors were never read, and the depth bar asks for every
  study-year quarter already disclosed.
* **Three Arabic group-news disclosures**, dated 10-3-2026, 7-6-2026 and 5-7-2026, held and
  unread. This is [R-NEWS-01]'s announced-projects category, filed in the language the issuer
  files in. The fourth of that family — the 31-5-2026 Iraq disclosure — **is** read, and it is
  what produced `landbank_group_total_msqm = 128.0`. One of four was consumed, which is the
  proof that the class is material on this name. Two board-resolution summaries (22-2-2026,
  12-5-2026) and *"TMG Disclosure Final 24 June 2026"* are also unread.
* **Nineteen investor presentations, none consumed**, against a study whose largest declared
  gap is unit economics — area, price per square metre, cost per square metre. An investor
  presentation is the document class that carries exactly those, and closing that gap is what
  would move the ground-up record at #23 off 100% segment.
* **Twenty-seven separate-entity financial statements, none consumed**, against a group where
  45.2% of consolidated equity belongs to minorities and whose second declared gap is *"TMG's
  economic share of each project company"*. The minority's claim is proxied by its filed
  profit share of 21.0% precisely because that share is not disclosed; the separate statements
  are the obvious first place to look — and the difference between the value-share and at-book
  bases is EGP **16.24** a share on the faster-conversion case, which is the same question
  #52's envelope is confused about.
* **Eleven market updates and five executive-management reports**, held and unconsumed.
* **The order-book quality series.** B.2 cites cancellations *"around 4% to 4.5% of accumulated
  sales"* and the model consumes no cancellation driver; the EGP 491,000mn order book enters
  gross.

None of these is a defect in what the study says. Each is a question the study did not ask of
documents it had already downloaded, and there is no register in which the asking would have
been recorded either way.

---

## What is NOT claimed

* **Unit-level ground-up is not achieved and is not claimed.** The driver record says SEGMENT
  on all three lines, 100% of revenue, with what would close each gap.
* **Peer multiples are not published**, and §1.3 and B.1 both say why.
* **The risk-free rate is 35 days old** at this edition — now correctly stated on the page
  (#22 repaired) and sensitised across 22.10%–38.10% in §1.9.
* **The live site carries a pre-calibration range and says so.** `assets/data.js` holds
  `fair: {bear: 83.6, base: 147.12, full: 189.6}`, `fairAsof: "2026-06-17"`, with a note that
  the calibrated range is deliberately held back until the whole book publishes together.
  Recorded so it is not mistaken for a stale number; it is a declared hold.
* **This gate does not certify the answer.** It certifies that the study document's answer is
  what the committed model produces, that the model reproduces to the cell and to the
  paragraph, and that the delivered workbook now both discounts and bridges correctly.

---

## What must be true before this edition can ship

1. `lenses.py:195-201` building the §1.7 ladder on the same construction the published cases
   use, or the ladder captioned with the ramp it holds and why its ten-year row is not the
   ten-year case.
2. `experts.py:279-280` rewritten to cite a sensitivity that exists, or the sensitivity built;
   `experts.py:300` rewritten to the identity the study now uses.
3. `GAP_REVIEW`'s envelope list put on the same minority basis as the central it states.
4. `docx_bibliography.py:61-69` taking each primary document's period from the document, not
   from the earliest input that cites it.
5. §2 and figure 3 struck on the same price as the rest of the document, or captioned with the
   date they are struck on and why it differs — which means `docx_tmgh.site_block()` either
   reading a technical block struck on 96.60 or labelling the one it reads.
6. §1.5 recomputed from the committed lens ranges rather than typed, or deleted.
7. `growth_at_horizon_end` measured from `statements.capacity.full_rows` rather than declared
   at `build_numbers.py:320`; `HOSP_GROWTH` and `OTHER_GROWTH` recorded in
   `macro_record.growth_lines`; the sales-ladder exemption either given a reason that is
   arithmetically true or removed.
8. §3 given TMGH's own band record with its count — 53 of 57, 93.0% — inline.
9. `Summary!B17`/`B18` labelled with their premium bases, the DCF sheet's terminal rate
   reconciled with the 18.86% the study publishes, and §1.9's ladder captioned with the basis
   it runs on.
10. The workbook PDF exported with row labels repeated on every continuation page.
11. The three driver lines and the 138-entry input register committed to `study_numbers.json`
    in the shape the repository gates read.
12. `implied_discount_rate` removed from `lenses.json` and read by every builder from
    `diagnostics.json`.
13. `docx_tmgh.py:730` reading each basis's own `schedule.{basis}.erp_mature` and the tolerance
    tightened, so the assertion passes on arithmetic rather than on 50 basis points; the
    constant 1.52 removed.
14. `engine/dcf_table.py` imported and used, so §1.1 is one table with a per-year discount rate
    and all ten explicit years, and so its bridge-footing guard and `sensitivity_grid()`'s
    centre-cell assertion run against this study at all.
15. An `eps_reconciliation` record committed, naming any gap between the filed EPS numerator
    and attributable profit rather than leaving it unseen.
16. `gate_check.py` made read-only, so running the gate cannot change the delivery.
17. `inputs.py:201`'s landbank note corrected from 115 to 128.
18. A Step 2A sweep register in the study directory, with the 31-March-2026 quarter, the three
    Arabic group-news disclosures, the nineteen investor presentations and the separate-entity
    statements either swept in or closed by a dated negative search.
19. `wacc.py:102` reading the spot date from the record rather than typing it.
