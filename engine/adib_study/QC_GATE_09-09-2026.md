# ADIB-Egypt (EGX: ADIB) — QC gate, 09-09-2026

## READ THIS FIRST — WHO FILLED THIS TABLE, AND WHY THAT WEAKENS IT

**[R-ENF-02] requires this gate to be filled from OUTSIDE the study, by the
`testahil-qc-auditor` subagent, precisely so that a study does not grade its own work.
THE SUBAGENT TOOL WAS NOT AVAILABLE IN THIS SESSION** — `Task` returned *"No such tool
available: Task. Task is disabled for this session, in subagents as well as here."*

So this table was filled by the run that produced the study. **That is a weaker form of
evidence and it is declared rather than papered over.** Every row below quotes a command
and its actual output, or an artefact and a figure inside it, so a later auditor can
re-run the lot; but nobody outside the work has yet looked at it, and the rows most
exposed to that are the judgement rows — the external-reader scrub and the
figure-and-table discipline — which are marked accordingly.

**What should happen next:** re-run this gate through `testahil-qc-auditor` in a session
where the tool is available, before this study is ever published. Publication is a
separate, explicitly-requested step and it has not been requested.

---

## The gate

| # | Standard | Verdict | Evidence — the artefact, command or number that carries it |
|---|---|---|---|
| 1 | **Which walk-forward** — the study names which of the three tests it rests on | PASS | The study, the training record, the driver-ledger entry and this gate all say FUNDAMENTAL and say why. `engine/adib_walkforward/scores.json` carries `"which": "the FUNDAMENTAL walk-forward [R-FCAL-01]"`. Section 3 of the study separately names the price-engine lens as a different thing and does not combine them. |
| 2 | **Identity** — the right company | PASS | Masthead, READ FIRST, `study_numbers.json.meta.not_to_be_confused_with`, `panel.py`'s module docstring and the driver-ledger entry all state: EGX: ADIB, Cairo-listed, EGP, formerly National Bank for Development — NOT ADIB Group PJSC (ADX: ADIB), the covered name ADIBUAE. The frozen baseline is in EGP, which is itself the check. |
| 3 | **Historicals from official sources only** | PASS | 18 filings, all from adib.eg, listed with document, date, tier and route in `panel.SOURCES` and reproduced as Table 1 of the bibliography. Every one is tier A. There is no tier B or tier C company figure anywhere in the study. |
| 4 | **Every statement foots** | PASS | `python3 engine/adib_walkforward/panel.py` → `income statements that do not foot: none`, and the balance-sheet loop prints nothing. 16 consolidated years plus two interim periods. |
| 5 | **The raster route is recorded and settled by arithmetic** | PASS | Seven filings have no text layer. `extract_pages.py` runs two independent passes (native embedded image upscaled; page render) and keeps both. `panel.py`'s docstring records that FY2020's balance sheet did not foot at 400 dpi page-render (equity 5,548,307 against a total-liabilities read of 33,979,364) and foots exactly by the native-image route. Two figures settled only by the footing identity are labelled DERIVED with the formula in `panel.DERIVED_NOTES`. |
| 6 | **Point-in-time discipline** | PASS, with one declared exception | `panel.IS` / `panel.BS` carry every year as its OWN filing reported it; the six restatements are in `panel.RESTATEMENTS` beside the figures they would replace and are never substituted. THE EXCEPTION: no English FY2021 consolidated filing exists on the issuer's site, so FY2021 comes from the FY2022 comparative column. Recorded as basis break B9, in the study's §7 caveats and in the bibliography's negative results. |
| 7 | **Forecast built ground up** | PASS | `python3 engine/adib_study/gates.py` → `ground-up PASS — {"lines": 3, "unit_share": 0.8537869282022124}`. 85.4% of FY2025 operating income is built at unit level (average total assets and average interest-bearing liabilities, times a yield and a funding rate); the 14.6% that is not carries a stated gap note naming exactly what the filings do not disclose. |
| 8 | **Margins are outputs** | PASS | `compute.ASSET_YIELD` and `compute.COST_OF_FUNDS` are the inputs; NIM, cost-to-income, ROA and ROE are computed in `project()` and appear nowhere as inputs. The workbook enforces the same thing — `Income Statement!E17` is a formula reading `Balance Sheet!C6`, not a typed rate. |
| 9 | **The dividend is derived, not typed** | PASS | `project()` pins equity at the target ratio and solves the dividend as the residual; `Balance Sheet!C13` is a formula. External check: the derived FY2026 payout is 18.2% against the 11.7% ADIB-Egypt actually paid on FY2024 earnings (`Cash Flow` tab, FY2025 dividends paid EGP 1,055.866m from the filed cash-flow statement). |
| 10 | **Beta provenance** | PASS | `gates.py` → `beta provenance PASS — regressed against raw_indices/EG/EGX30.csv as at 2026-09-08`. `beta_regression.own_stock_beta('ADIB','EG','EGX')`: beta 1.0747, R² 0.248, n = 256 weekly to 2026-08-20, SE 0.1634, Dimson-corrected, week rule W-THU, `conforming: True`. The coefficient is 6.6× its own standard error. No study-local composite anywhere. |
| 11 | **Country risk counted once** [L-004] | PASS | `compute.rf_star = RF − SOV_SPREAD` = 23.00% − 3.42% = 19.58%, then + beta × TOTAL premium. The retired double-counting construction (33.23%) is computed and printed for contrast only, and §1.8 prices all three constructions. |
| 12 | **Cost of equity reproduces from its own inputs** [R-COC-02] | PASS | `study_numbers.cost_of_capital` carries rf, spread, rf_star, beta, erp, ke, ke_terminal, the glide, and `ke_terminal_construction: same_beta` with the reason (a deposit-funded bank has no leverage to relever). Workbook `Assumptions!B30` recomputes ke live from B22..B25. |
| 13 | **Terminal coherent with the inflation inside it** | PASS | Terminal growth 7.00% = terminal inflation 7.00%, against a terminal risk-free of 12.50% (itself derived as 7.00% + a 5.50% real convention). The AMOC defect — growth of 5% against a rate embedding 7% inflation — cannot occur because the growth rate IS the inflation figure, from the same held path. |
| 14 | **One macro path** | PASS | Every rate derives from `engine/macro_path.load('EG')`, as of 2026-09-02. §1.8 prints inflation, the policy rate and the cost-of-equity glide in one table. The only figure not from that path is a 4.5% real-GDP rate used in the gap review's coherence table, and it is labelled there as entering no valuation input. |
| 15 | **[R-LENS-03] one class primary, no typed weights** | PASS | `research_protocol.LENS_REGISTRY['bank']` = `('ddm', ['residual_income','relative_multiple','book_value'])`. `compute.PRIMARY = 'ddm'`, `CENTRAL = PV_READS['ddm']`. `grep -c "WEIGHTS\[" compute.py` → 0. The envelope is `min`/`max` of the three present-value reads. An earlier draft of this model DID publish a four-lens weighted average at EGP 40.81; it is gone, and the gap review records what removing it did. |
| 16 | **16-section Word document** | PASS | `python3 engine/adib_study/docx_adib.py` → `sections: 16 tables: 19`. The section list is the model report's, in order. Rendered text extracted and checked: 47,479 characters, zero occurrences of `%%`, `None` or an unsubstituted placeholder. |
| 17 | **16-sheet workbook, live formulas** | PASS | `python3 scripts/check_workbook_structure.py` → ADIB now appears in `MATCHES THE MODEL REPORT (23)`. 381 formulas. Change any blue cell on `Assumptions` and the central on `Summary` recomputes. |
| 18 | **Independent recalculation of the DELIVERED workbook, zero mismatches** | PASS | `python3 engine/adib_study/recalc.py` → `gate 1 — formulas found 381, unresolvable 0` · `gate 2 — projected cells reconciled 120` · `gate 3 — headline reconciliations 7` · `RESULT: PASS — zero mismatches`. The evaluator (`xlcalc`) is not the library that wrote the file, and it runs on the delivered `.xlsx`. **IT CAUGHT TWO REAL DEFECTS**: a circular reference in the market-cap formula, and a terminal residual income charged at the year-5 cost of equity instead of the terminal one, worth EGP 3.4bn. Both are fixed and both are recorded in the code that fixed them. |
| 19 | **Standalone bibliography** | PASS | `ADIB_Bibliography_09-09-2026.docx` — primary documents, the full four-field input register, judgements with what would overturn each, negative results, where two sources disagree, and what is not disclosed. `gates.py` asserts its existence and `assert_model_study()` raised until it existed. |
| 20 | **SIGCM** | PASS, with three documented N/A | `gates.py` → `SIGCM PASS`. Three clauses are N/A with reasons in `gates.SIGCM.na_reasons`: `asset_conversion_cycle` (a bank has no inventory, DSO, DIO or DPO — its balance sheet IS the cycle and is modelled in full), `debt_lc_fx_split` (the filings do not split the funding balances by currency; the disclosed FX exposure on subordinated financing is quoted), `competitors` (one same-day peer PRICE, no same-day peer BOOK VALUE for any Egyptian bank). |
| 21 | **Model-report depth** | PASS, with one documented N/A | `gates.py` → `model-report depth PASS`. `figure_discipline` is N/A: the study carries NO CHARTS and every quantity a chart would show is in a numbered table. Recorded as a deliberate absence in `gates.MODEL.na_reasons` so it does not read as an omission. |
| 22 | **Valuation-input block, every origin** | PASS | `python3 scripts/check_valuation_inputs.py` → `conforming 12 ... failing 0`, ADIB included. 11 origins plus a `prior_year_anchor` for FY2013. Share count FOOTS at every origin (issued capital ÷ LE 10 par, and the LE 10 is quoted from FY2018 note 34/2). `wc` is MISSING WITH A REASON at every origin; FY2023 `dep` is missing with its reason and the derived identity recorded beside it. |
| 23 | **Disclosed useful life** [R-TERM-01] | PASS (recorded, not adopted) | `engine/adib_walkforward/useful_lives.json`. Route one FAILS — the policy note gives a life per class spanning 1 to 20 years, and finding a range is not finding a life. Route two derives 10.75 years (FY2018) and 12.30 (FY2017) from gross cost ÷ charge, with the land overstatement disclosed. **No life is adopted**: a bank's terminal is built on equity, and net fixed assets are 0.27% of total assets. |
| 24 | **[R-GAP-01] the answer is audited** | PASS | Central EGP 37.1779 vs latest known EGP 52.05 (3 Sep 2026) = −28.6%. `GAP_REVIEW_09-09-2026.md` covers all eight required headings and states `AUDITED CENTRAL: 37.1779` / `AUDITED GAP: -28.6%`. `python3 scripts/check_valuation_gap.py` → `ADIB -28.6% GAP_REVIEW_09-09-2026.md (audits 37.1779)`, no ADIB violation. **The review found two real defects and one false claim** — see row 27. |
| 25 | **The price is the LATEST KNOWN one** | PASS | EGP 52.05, 3 Sep 2026, from `engine/prices/SUPPLIED_03-09-2026.json`. Confirmed independently: `check_valuation_gap.latest_known_price('ADIB')` → `(52.05, '2026-09-03', 'engine/prices/SUPPLIED_03-09-2026.json')`. `assets/data.js` still carries 54.40 at the 23 Aug close and the study says so rather than using it. |
| 26 | **Two clocks stated** | PASS | The fair value is struck against the 3 Sep close; §3's percentiles are anchored on the 23 Aug close, the last session in the price library. Stated in READ FIRST, in §3 in a box, and on the `Monte Carlo` tab. |
| 27 | **Claims checked against the record** | PASS — and one was FALSE | The gap review's heading 7 checks eight superlatives. **One was false**: "the highest return on equity in the sixteen years parsed" — FY2024's 48.3% is higher than FY2025's 43.7%. The claim was REMOVED rather than qualified, and the fact that it was made is left on the record. This is the AMOC "best ever" defect, found by the process built to find it. |
| 28 | **Years 3–5 published as ranges** | PASS | Appendix A.3 and the `Per-Share & Ratios` tab publish FY2028–FY2030 attributable profit as low/point/high with the cell count. `forward_ranges.json` builds them from this run's own attributable-profit error dispersion, CENTRED on that distribution's median — the level error is NOT carried, because the study does not hold share flat and carrying it would double-count. The construction and its reason are in `forward.py`'s docstring. |
| 29 | **No correction from the walk-forward entered the drivers** | PASS | `corrections_log.json` → `adopted: 0`, four watch flags. `study_numbers.walkforward.corrections_promoted` = 0. The training record §8 gives the second-clause reasoning for the refusal that mattered. |
| 30 | **No rating, no price target, no buy/sell language** | PASS | Searched the rendered document text for rating, target, buy, sell, hold, recommend, overweight, underweight — the only hits are the disclaimers saying there are none. |
| 31 | **External-reader scrub** | **PARTIAL — SELF-ASSESSED** | The document was read for internal vocabulary: no rule identifiers, no verdict tokens, no calibration appendix, experts cast by method as Expert 1/2/3. **But the scrub is a judgement and this row was filled by the run, which is exactly what a scrub is supposed to defeat.** Re-run through `testahil-qc-auditor` before publication. |
| 32 | **Figure and table discipline** | **PARTIAL — SELF-ASSESSED** | 19 tables, numbered in document order by a counter (`_TN`), never typed — the two-tables-numbered-11 defect cannot occur. No figures at all, recorded as a deliberate absence. **Judgement row, filled by the run; see the header.** |
| 33 | **Every figure inspected as a rendered image** | **NOT DONE** | The document has no figures, so there is nothing to inspect. The rendered PDF was NOT produced or read in this session. Stated rather than passed over. |
| 34 | **Nothing published** | PASS | `assets/data.js` is untouched by this run — `git status` shows no modification to it. Moving `fair{}` is part of the rebuild and is Step 10, after the lesson scopes are ruled; putting anything live is a separate, explicitly-requested step that has not been requested. |

---

## What is outstanding, named

1. **This gate has not been filled from outside.** Rows 31, 32 and 33 are the exposed
   ones. Re-run through `testahil-qc-auditor` before publication.
2. **The rendered PDF was not produced or read.** Row 33.
3. **The Egyptian ten-year yield is 34 days old and four live re-source routes failed.**
   Declared in the register entry, in §1.8, in the gap review's heading 4 and in the
   bibliography's negative results; sensitised ±200bp. It is the study's first data
   request and it is worth EGP 6.57 a share on the only recent market evidence found.
4. **No same-day peer book value could be sourced** for any Egyptian bank. The relative
   lens is a band, carries no weight, and is the lens pointing highest.
5. **The lesson scopes are not ruled.** 68 drafts carry a proposed ruling and `confirmed:
   false`; `lessons_add.py` has not been run. The fair value is not yet recorded in
   `fv_movement.json` for the same reason — `record` takes the lesson ids.
