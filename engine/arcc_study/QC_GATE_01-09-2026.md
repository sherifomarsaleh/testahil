# ARCC — QC gate, edition of 01-Sep-2026 (revision 4)
### Campaign re-issue, position 2 of 90. **Filled with evidence, never self-certified.**

Run `python3 gates.py`, `python3 recalc.py`, `python3 label_gate.py` and
`python3 ../fv_movement.py check` to reproduce every row below.

| # | item | evidence | verdict |
|---|---|---|---|
| 1 | Structure matches the model report | 16-section Word document and 16-sheet workbook, `assert_model_study()` called in `gates.py` and passing | **PASS** |
| 2 | Standalone bibliography document | `ARCC_Bibliography_01-09-2026.docx`, rebuilt this edition; the beta entry rewritten to describe the regressor that now ships | **PASS** |
| 3 | Four-field provenance on every input | `study_numbers.json` `inputs`; the walk-forward panel carries value / source document / date / tier on every figure | **PASS** |
| 4 | Numeric traceability | `recalc.py`: **923 of 923 formula cells reproduce the model, 0 unresolvable, 0 unchecked, 56 headline reconciliations passed** | **PASS** |
| 5 | External-reader scrub | `label_gate.py`: 18 labels checked, every one describes its own row | **PASS** |
| 6 | Table discipline | `label_gate.py` walks every delivered table and matches each label against the cell beside it | **PASS** |
| 7 | Step 0.0 data-quality gate | `step0.py`; both series (ARCC and EGX30) screened inside `beta_regression.own_stock_beta`, `stock_dq` and `index_dq` both empty | **PASS** |
| 8 | SIGCM — historicals official only | Every historical figure read off ARCC's own audited consolidated filings, downloaded from arabiancementcompany.com. `fetch_attempts.json` holds URL, timestamp, HTTP status, byte count and sha256 for all 34. **No aggregator, broker or press anywhere in the chain.** | **PASS** |
| 9 | SIGCM — ground-up forecast | `assert_ground_up()` in `gates.py`: 3 driver lines covering **100.000%** of revenue — unit 67.973%, derived 30.649%, segment 1.378%; every line below `unit` carries a gap note | **PASS** |
| 10 | SIGCM — debt split LC/FX | Note 25: CIB facility in EGP, NBE loan in EUR, EBRD tranche EUR 15.9mn. The cost-of-debt build is currency-blended and asserts that 91.1% of the book is euro-denominated | **PASS** |
| 11 | SIGCM — asset-conversion cycle | Working capital projected from the disclosed cycle; the FY2025 cash-flow statement's own movements reconcile in the workbook | **PASS** |
| 12 | SIGCM — competitors studied | Peer sheet; five Egyptian heavy-industrial names regressed for the beta, Suez Cement examined and excluded on its own statistics | **PASS** |
| 13 | **SIGCM — beta from the exchange's PUBLISHED index** | `beta_reg.py` rewritten this edition. **The retired version regressed against an equal-weight composite of the covered EGX names — a hard fail.** Now `beta_regression.own_stock_beta('ARCC','EG','EGX')` against `raw_indices/EG/EGX30.csv` as of 2026-07-22. The own-stock fit returns 0.6981 with R²=0.047 and **FAILS the usability gate**, so the strict preference order falls to a tier-2 same-country peer median of 1.0302. `assert_beta_provenance(rec, tier2_fallback_documented=True)` passes; `scripts/check_study_provenance.py` reports no new violation | **PASS** |
| 14 | SIGCM — formula-based model | 923 live formula cells; change a blue input and the value per share recomputes | **PASS** |
| 15 | SIGCM — gaps flagged before issue | Three stated in the delivered text: fuel not separately disclosed inside raw materials; peer betas not unlevered/re-levered; export cost of sales not disclosed by destination | **PASS** |
| 16 | Sweep — company site attempted and logged | 34 documents retrieved from the company's own site, each logged with its outcome. **`arabiancementcompany.com` was recorded in project memory as `connect_rejected` and answered 200 when re-probed** — the block was re-tested rather than believed | **PASS** |
| 17 | Sweep — ≥2 audited fiscal years (target 4) | **Twelve**: FY2014 through FY2025, each read from its own filing | **PASS** |
| 18 | Sweep — every study-year quarter swept before the build | Q1-2026 and Q2-2026 audited interims both read; H1-2026 group sales EGP 6,080.6mn against EGP 5,499.9mn, +10.6% | **PASS** |
| 19 | Sweep — ≥1 investor-relations source tagged COMPANY_IR | Eleven investor presentations and ten earnings releases, tagged separately from the audited filings | **PASS** |
| 20 | One escalator per driver class | Note 5 splits cost of sales into raw materials, transportation, depreciation, licence and right-of-use amortisation and overheads. Raw materials escalate on the imported/USD path; transportation and overheads on domestic inflation. **No blended index across physically distinct lines** | **PASS** |
| 21 | Cost path carries a sourced near-term anchor | Q1-2026 audited: cost of sales 1,709,813,469 on sales 2,995,959,350 | **PASS** |
| 22 | 3yr history + 5yr forward DCF, full FCFF waterfall | The waterfall runs to PV of FCFF inline: EBIT → NOPAT → +D&A → −capex → −ΔNWC → FCFF → PV, then the EV→equity bridge | **PASS** |
| 23 | WACC under the v2 method | rf* = 22.95% local 10Y − 3.40% Egypt CDS default spread = 19.55%; **country risk charged once, via the ERP**; CDS default spread stripped and CDS ERP added, same basis both sides; marginal Kd above the sovereign; market-value weights | **PASS** |
| 24 | Expert appendix in full | Appendix C: three experts by method, cross-examination, the three in one room, divergence table | **PASS** |
| 25 | The crux sensitised in observable units | Terminal return on capital against the hurdle; beta grid spanning 0.63 to 1.30, which brackets both the retired composite figure and the adopted peer median | **PASS** |
| 26 | Contested judgement computed BOTH ways | Replacement-cost against book-cost terminal capital, published side by side and never averaged | **PASS** |
| 27 | **[R-FCAL-01] fundamental walk-forward** | **FULL scope. `engine/arcc_walkforward/`: 12 audited fiscal years, 7 origins FY2018–FY2024, horizons 1–5, 25 graded cells per driver.** Pre-registration written before a single error was computed; one amendment (A-1) recorded with its sourcing reason; basis-break register with 11 entries; block bootstrap at {2,3,4}; macro/company split whose negative control (volume must return exactly zero macro share) passes to machine precision | **PASS** |
| 28 | Walk-forward corrections tested under both clauses | Two drivers passed clause 1; **both refused at clause 2 and recorded as WATCH FLAGS. Nothing promoted into the live drivers.** The mechanical adjusted-vs-raw test corroborates: cost per tonne improves in 1 of 4 origins, finance costs in 2 of 3 | **PASS** |
| 29 | Years 3–5 published as ranges | Built from this run's own measured error distribution — at five years the spread is a factor of 3.4× on the level — with the caveat that it measures how wrong the method has been, not how wide the company's outcomes will be | **PASS** |
| 30 | [R-LESSON-01] lessons document | 26 candidates harvested from the run's own numbers, **all 26 resolved: 4 registered (L-034 ALL, L-035 ALL, L-115 CLASS, L-206 STOCK), 22 declined each with a reason.** All four PROVISIONAL — the method rests on two names | **PASS** |
| 31 | [R-STD-01] standard stamp | `standard_version` 2026.08.23 at the top level of `study_numbers.json`, which moves ARCC from the `reissue` tier to `current` in `campaign_queue.py` | **PASS** |
| 32 | Fair-value movement recorded | Baseline frozen **before** the run touched `data.js`. Edition 2 delivered: base 54.65 → 50.20 (−8.1%). Edition 1 superseded and kept on the record with the reason | **PASS** |
| 33 | Driver ledger entry | Appended to `engine/Fundamental_Driver_Ledger.md` | **PASS** |
| 34 | Page-integrity gates after touching `data.js` | `node --check` plus a load-and-assert in node (90 tickers parsed, fair applied); `coverage_floor.py`, `check_band_vocabulary.py`, `check_technical_read.py` and `check_ta_chart_overlay.js` (186 chart pages) all pass | **PASS** |
| 35 | No rating, no price target | A fair-value range and the reasoning behind it | **PASS** |

## What this edition does NOT claim

- **The rendered PDF has not been read, and no figure has been inspected as an
  image, because neither was produced in this pass.** The Word document and the
  workbook are delivered; the PDF conversion and the figure-by-figure visual
  inspection are outstanding, and under the depth bar those are gates rather than
  formalities. **This edition must not be described as fully verified until they
  are run**, and it is recorded here rather than left for a reader to discover.
- The peer betas are not unlevered and re-levered. The direction is known and is
  conservative; the gap is stated in the study and in the bibliography.
- The method is not validated. Two companies have been through it.
