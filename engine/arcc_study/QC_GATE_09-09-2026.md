# ARCC — QC gate, 09-09-2026

Arabian Cement Company S.A.E. · Egyptian Exchange: ARCC · EGP · edition of 09-09-2026

Audited from OUTSIDE the study per [R-ENF-02] by a reader who did not build it. Every row
names the artefact, the command or the number that carries it. Nothing here is
self-certified, and where a row fails it says so plainly.

**VERDICT: NOT DELIVERABLE.** Ten hard fails, listed at the foot. The arithmetic is in
excellent order and should not be lost: the workbook reproduces cell for cell against an
independent sandbox rebuild (2,079 cells, 0 differing), both documents rebuild
paragraph-for-paragraph identical, `compute.py` passes 42 assertions and calls all four
standing gates, the beta is on the sanctioned EGX30 series and falls correctly to a peer
median when its own regression misses the usability floor, and Appendix A.4 — the new
content of this edition — reproduces exactly from the walk-forward's committed multipliers
with nothing typed. What fails is the PAPER, the REGISTER and the ANSWER'S OWN
COMPLETENESS: the bibliography of a 9 September edition is dated 6 August on its face and
carries neither of the two tables the study's own closing panel promises a reader; the
sweep register written to disk is not the object its eight invariants were run against; a
caveat says the investor material could not be reached while the file sits in the directory
and drives the whole volume build; a figure caption says the margin narrows when the study
says on the next page that it widens; and the study's largest contested judgement, worth
EGP 26.77 a share on its own committed record, is published nowhere.

---

## The edition audited, and how it was resolved

Resolved by date from disk and asserted non-empty before anything was opened, then checked
against `edition.py` rather than against a name typed here.

| Deliverable | File | Also on disk |
|---|---|---|
| Study, Word | `ARCC_Valuation_Study_09-09-2026_public.docx` | 06-08, 08-08, 02-09, 03-09 |
| Study, PDF | `ARCC_Valuation_Study_09-09-2026_public.pdf` — 34 pages, 9 embedded figures, rendered 12:00:36 on 09-09 | 06-08, 08-08, 02-09, 03-09 |
| Workbook | `ARCC_Valuation_Model_09092026_public.xlsx` — 16 sheets, 925 formula cells, 314 pasted numerics, 2,079 populated | 06-08, 02-09, 03-09 |
| Bibliography, Word | `ARCC_Bibliography_09-09-2026.docx` — 22 pages | 06-08, 02-09, 03-09 |
| Bibliography, PDF | `ARCC_Bibliography_09-09-2026.pdf` — rendered 12:00:47 on 09-09 | 06-08, 02-09, 03-09 |

`edition.py` holds `EDITION = date(2026, 9, 9)` and derives all three filenames. The study
masthead is derived from it through `meta.edition_date` (`docx_arcc.py:100`). **The
bibliography masthead is not** — see row 20.

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates are called in the study's own committed code, and the entry point runs | **PASS** | `compute.py:2511` `RP.assert_ground_up(_gu, ticker="ARCC")`; `:2588` `RP.assert_beta_provenance(BETA['own_stock'], tier2_fallback_documented=True)`; `:2589` `RP.assert_sigcm(SIGCM)`; `:2590` `RP.assert_model_study(MODEL_CHECK)`. Run from outside in a sandbox copy: `PYTHONPATH=engine python3 compute.py` → exit 0, "42 assertions passed", "[Gates] … all called in this study's own code and all passed". |
| 2 | Provenance gate over every study directory | **PASS** | `python3 scripts/check_study_provenance.py` → "study directories: 24 · registered indices: 7 · known outstanding: 8 · OK — no new violations". ARCC appears on no outstanding list. |
| 3 | Live standard version, and the study's stamp | **PASS, with one gap named** | `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` → **2026.09.07**. The study is stamped **2026.09.01** (`study_numbers.json:430`), deliberately frozen at `compute.py:2549` under [R-STD-02] because it is ratcheted against two of 2026.09.07's requirements. `python3 scripts/check_standard_claim.py` → exit 0, "no study claims a standard it is listed as not meeting". **The gap:** the study is therefore not built to the version that names the gap-review artefact, and says so by its stamp rather than by a sentence. |
| 4 | Beta — inspected on the record, not on a boolean | **PASS** | `beta_result.json`: `index_file` = **`raw_indices/EG/EGX30.csv`**, under `engine/raw_indices/`, not a constituent composite. `assets/data.js:569` gives `code: "EGX:ARCC"`, so the listing exchange is EGX and EGX30 is its published index — resolved from the code prefix, not from the `raw_ohlc/EG/` folder. No interim note is owed (not a DFM name), no QATAR10 caveat is owed. `interim_note: null`, `conforming: true`, `week_rule: W-THU`, 253 weekly observations to 2026-07-16 over 4.89 years, index as-of 2026-07-22. |
| 4a | The usability gate, and what the study did when it failed | **PASS** | The own-stock fit FAILS: R² = **0.0468** against the 5% floor, SE(β) = 0.2284 against β = 0.6981 — `usable: false`, `gate_msg: "R^2=0.047 < minimum 0.05 — index barely explains the stock's returns"`. The study does not keep it. It falls to the **median of four same-country peer regressions against the same EGX30** — LCSW 0.8145, EGAL 0.9186, ORAS 0.9364, EGCH 1.0302, median **0.9275** — and prints the failed diagnostics in §1.5 of the delivered study ("a beta of 0.698 on 253 observations — with an R-squared of 4.7% and a standard error of 0.228"). SCEM is named and deliberately excluded as weaker still. This is the fallback the rule asks for. |
| 4b | The withdrawn composite | **PASS on withdrawal; see row 15 for how it is described** | Revisions 1–3 carried 0.628 from an equal-weight composite of covered Egyptian names. It is withdrawn in this edition and §1.5 explains why in plain language. No composite number survives in the adopted build. |
| 5 | Ground-up clause — the driver record, not a flag | **PASS on the record, FAIL on committing it** | Four `DriverLine`s at `compute.py:2463-2513`, covering 100.0000% of FY2025 revenue. Share by level: **unit 94.05%** — local cement 67.09%, export cement 8.80%, export clinker 18.17%, each naming its unit, its unit source (FY2025 investor presentation, performance highlights) and its price basis (derived from the audited note-4 revenue line over the disclosed tonnage, recalibrated on the H1-2026 half) — and **segment 5.95%** for transportation services, which carries a full gap note (no haulage volume or rate per tonne-kilometre is disclosed; the walk-forward's attempt to split the cost across 45 period pairs returned a local rate spanning -425 to +722 EGP/t and nine economically impossible solves, so the split is unidentified and not built). **The failure:** `study_numbers.json` has no `ground_up` key — the record is computed and never committed. `python3 scripts/check_ground_up.py` → "known ARCC commits no driver-line record at all; [R-SIGCM-02] retired the boolean that used to stand in for one" (ratcheted, so the gate stays green while the record stays absent). |
| 6 | Depth 1 — standalone bibliography document | **FAIL** | `ARCC_Bibliography_09-09-2026.docx`/`.pdf`, 22 pages, ships. It carries the primary-documents list, a source catalogue, the **full 217-input register** (Company 161, House 30, Country 14, Industry 11, Market 1) each with value / date / source-and-construction, a derived-figures section, a research trail and a driver-basis table, plus a real aggregator-discrepancy note (an aggregator print of EGP 2,894.13mn for total liabilities that would not close, identified as total CURRENT liabilities). **It carries no judgements table with what-would-overturn-it and no negative-results table.** Programmatic scan of every paragraph and cell: "overturn" **0 hits**, "not found" **0**, "looked for" **0**. The model report's bibliography carries both (ADNOCLS: 16 judgements with overturn conditions, 8 negative results; its register column is headed `Outcome, and how the study handled it`). The delivered study's own About panel, page 34, tells the reader they are there: *"a standalone bibliography listing every registered input with its source, the judgements with what would overturn each one, and the negative results — the things looked for and not found."* Two of those three claims are false. |
| 7 | Depth 2 — four-field provenance, counted | **PASS, with one gap named** | 217 inputs, every one complete on all four fields; a shape scan over `study_numbers.json['inputs']` returns exactly one field-set for all 217. `python3 scripts/check_four_field.py` → "OK — every committed input carries all four fields" across 19 readable registers and 4,068 inputs. **The gap:** the fourth field is named `ring`, not `layer`; the gate accepts either, but the name is what then reaches the delivered page (row 12). |
| 8 | Depth 3 — numeric traceability, recalculated independently | **PASS** | Sandbox rebuild from a clean copy of the study directory: `study_numbers.json` and `xlsx_expected.json` regenerate **byte-identical** (`diff` → 0 lines each). `build_xlsx_arcc.py` rebuilt the workbook and an independent **cell-by-cell diff against the delivered file compared 2,079 populated cells across all 16 sheets and found 0 differing** — a clean recalc alone would not have shown that. `python3 recalc.py` on the delivered workbook: **"925 of 925 formula cells reproduce the model, 0 unresolvable, 0 unchecked; 57 headline reconciliations passed"** — nothing unparseable, nothing skipped. `python3 footing_check.py` → "tables examined: 32; unreconciled totals: 0". `python3 prose_check.py` → "prose figures checked: 660; unmatched: 0". `python3 label_gate.py` → "18 labels checked, every one describes its own row". Live share 925/(925+314) = **75%**. |
| 8a | Driver propagation on the delivered workbook | **FAIL** | `python3 driver_test.py` **crashes**: `KeyError: "no Assumptions row labelled 'Beta (own-stock weekly regression)'"` at `driver_test.py:73`, after 28 drivers pass. The Assumptions sheet row A10 of the delivered workbook now reads `Beta — median of same-country PEER regressions (tier 2; the own-stock regression is rejected, see below)`; the label changed when the beta fell to tier 2 and the driver test was never re-pointed. The committed `driver_test_result.json` is dated **5 September** and was produced against a superseded workbook. So no driver-propagation evidence exists for the file that ships. |
| 9 | Depth 4 — external-reader scrub | **FAIL** | `python3 scrub_gate.py --check` → exit 0, "scrubbed 3 delivered document(s), 218,388 characters … external-reader scrub: CLEAN — 0 hits across 39 terms". That result is real for the 39 terms it holds. **The list does not hold `ring`, `step`, `sweep` or `register`**, and the delivered bibliography prints all four registers of internal vocabulary: §4 is headed "Research trail" and opens *"The research was carried out on 2026-08-06 across four concentric rings — global, country, industry and company"*, its table's first column is headed **`Ring`**, and the masthead reads "Source Register". The depth bar names "sweep/ring vocabulary" and "register jargon" explicitly. The model report's bibliography uses **`Layer`**. A check that returns zero on vocabulary the document does print is the failure [R-ENF-01] exists to name. |
| 9a | Verdict tokens and CRPS | **PASS** | `band_record.assert_no_verdict_tokens()` run from outside over every visible string of all three delivered files: study 101,583 chars **CLEAN**, bibliography 87,542 chars **CLEAN**, workbook 33,071 chars **CLEAN**. No PASS/PARITY/FAIL beside a company name; no CRPS anywhere. `python3 scripts/check_band_vocabulary.py` and `check_delivered_vocabulary.py` both green (ARCC in the CLEAN list of 22). |
| 9b | Calibration evidence in §3, no calibration appendix | **PASS** | The document has exactly three appendices — A financial statements, B peer set and risk, C expert panel. Calibration lives in §3 as plain sentences with the statistics inline: *"Over 44 resolved three-month forecasts this name's price finished inside the 90% band 93.2% of the time, against a 90% target"* and *"inside the fifty-per-cent band 59% of the time. The count is printed beside the percentage because a percentage without its count is the number that misleads."* |
| 10 | Depth 5 — figure discipline | **FAIL** | Programmatic: all 10 PNGs on disk are RGBA with alpha extrema **(255, 255)** — zero transparency — on a solid canvas at corner luminance 249–250; 9 are embedded in the study. Every figure was then **inspected as a rendered image**, and that is what found the defect: **Figure 6's title is clipped at the canvas edge**, ending mid-sentence at *"…anchored on the last session of the price history rather than on the"*. The full string is at `figures.py:167-169` and does not fit the axes width at `loc='left'`. Visible in `fig4_fan.png` and on page 21 of the delivered PDF. |
| 10a | Figure 2's caption against its own figure | **FAIL** | `docx_arcc.py:425` captions Figure 2 *"The margin is the gap, and the gap narrows across the forecast."* The margin path in `study_numbers.json['forecast']['margin']` is **0.3903, 0.3949, 0.3999, 0.4020, 0.4040** — rising in every year. Figure 2's own printed labels say margin 39% / 39% / 40%, and §1.6 on the next page says *"The EBITDA margin dips once, to 39.0% in FY2026 from the audited 39.3%, and then rises in every year after it to 40.4% by FY2030"*. The caption says the opposite of the figure it labels and of the paragraph two pages later. |
| 10b | Figure 1's bar label against the study's own record | **FAIL** | `figures.py:53,55,63` hard-code the fifth bar as **"Weighted central"**, and `lens_ranges` still carries a key of that name whose three values are identical to the DCF lens. The study retires the blend explicitly — §1 *"There are no weights in this model to report"*, §4 *"the weights are retired and so is the description"*, and assertion 40 *"the central IS one lens, not a blend of several: there are no weights left to sum"*. A reader of Figure 1 is told the published central is a weighted blend of the four bars above it. |
| 11 | Depth 6 — table discipline, three counts per document | **FAIL, and the counts are given** | Independent audit reading each document's own text frame off `sections[0]` rather than assuming it. **Study** (frame 17.78 cm): 34 tables — **starved 0, over-wide 0, no explicit grid 0**. **Bibliography** (frame 24.89 cm): 6 tables — **starved 0, over-wide 0, no explicit grid 0**. Every table carries an explicit `gridCol` width. **The failure is a NEW one the study's own audit cannot see:** `python3 scripts/check_column_widths.py` → exit 1, `[NEW] engine/arcc_study/ARCC_Bibliography_09-09-2026.docx::# declared 0.76cm, needs 0.84cm for its widest figure`. The input register runs to 217 rows, so the `#` column must hold three digits and wraps. `scrub_gate.py`'s `column_audit()` only tests min-width < 0.7 cm and total > frame, so it reports "table column audit: CLEAN" on a column that is demonstrably too narrow for the figure it prints. |
| 12 | Depth 7 — expert appendix at maximum detail | **FAIL** | Present: three experts, each with a worldview paragraph, a stated valuation range and central, and an explicit falsifier ("What would prove this wrong"); C.4 cross-examination with six objections each conceded or rejected and the arithmetic beside each; C.5 the three in one room; C.6 a divergence table. **Absent: the per-expert worked table with every intermediate line, and the named per-expert sensitivity with numbers.** Table C1 is a three-row summary of the panel, not a worked build — nothing corresponds to the model report's "Expert 1 runs 13 lines from mid-cycle earnings to fair value per share". §7 states the nine panel figures are built as formulas in the workbook; the depth bar asks for the intermediate lines in the appendix, and they are not there. |
| 12a | Expert labels | **PASS** | Headings read "C.1 Expert 1 — Replacement-cost industrialist", "C.2 Expert 2 — Mid-cycle earnings-power analyst", "C.3 Expert 3 — Cash-return and distribution investor". Figure C1's y-axis reads "Expert 1 / Expert 2 / Expert 3". No persona name anywhere in any of the three files. |
| 13 | Depth 8 — the central contested judgement computed both ways | **FAIL** | Three judgements are published both ways in Table 13, the body, `contested_judgements.json` and the workbook: cost of debt 13.36% vs 7.89% (+0.2%), beta 0.928 vs 0.698 (+12.0%), capex USD 3.23/t vs book depreciation (+6.2%). The study names the beta as "the study's own most consequential contested judgement". **It is not.** The study's own committed `useful_lives.json` records the terminal useful life and prints its sensitivity: *"the terminal at 14.3 years … is 21,658 and BELOW its own floor; at the disclosed 20 years 31,318; at 30 years 39,365; at 50 years 45,805. The choice is worth **EGP 55.92 / 66.63 / 75.55 / 82.69** per share, so it is a material contested judgement and belongs in the [R-ENF-05] register."* That is a swing of **EGP 26.77 a share, 40% of the central** — more than twice the beta's EGP 8.01. It appears in no summary table, in no body table, in `contested_judgements.json`, in the workbook or in any expert's range. The delivered study states the 20-year life three times and never states what it is worth. |
| 14 | Structure, sheet list and format match the model report | **PASS** | `python3 scripts/check_document_structure.py` → ARCC in "MATCHES THE MODEL REPORT (22)". `python3 scripts/check_workbook_structure.py` → same. Verified directly: `wb.sheetnames == research_protocol.MODEL_STUDY['excel_sheets']` returns **True** for all 16 sheets in order. The class is a single-asset cement operating company, which is ADNOCLS's operating-company pattern inside ADNOCLS's skeleton — the nearest of the three reference names, and §1 says why a sum-of-the-parts is refused ("A sum-of-the-parts here would be a sum of one part"). |
| 15 | Appendix A.4 — the new content of this edition | **PASS on the arithmetic, FAIL on one count** | Every cell recomputed independently from `study_numbers.json['forecast']` × `arcc_walkforward/forward_ranges.json` at horizons 3/4/5. Revenue point 16,440 / 17,688 / 18,934; low 8,062 / 14,344 / 17,168 (×0.4904, ×0.8109, ×0.9067); high 49,203 / 71,149 / 71,453 (×2.9929, ×4.0224, ×3.7738). Volume point 4.89 / 4.91 / 4.93; low 3.35 / 4.44 / 4.45; high 7.41 / 7.06 / 5.45. Profit before tax point 6,499 / 7,102 / 7,732; low 345 / 2,642 / 3,926 (×0.0532, ×0.3720, ×0.5077); high 42,134 / 268,321 / 360,862 (×6.4833, ×37.7805, ×46.6684). All nine rows reproduce to the printed digit. Nothing in the table is typed — `docx_arcc.py:1660-1678` multiplies the study's own path by the run's own record. **The gap:** the last row is headed "Tested cases behind each band" and prints **5 / 4 / 3**, which is revenue's n. Volume matches. **Profit before tax at FY2028E rests on n = 4, not 5** (`forward_ranges.json` `pbt."3".n = 4`). One of the nine bands is described by the wrong count. |
| 15a | Far-year range gate | **PASS** | `python3 scripts/check_forward_ranges.py` → exit 0, "ARCC prints a far-year range **shape B** (`ARCC_Valuation_Study_09-09-2026_public.docx`)", and ARCC no longer appears on the ratchet (AMOC is the only "POINTS ONLY [ratcheted]" entry left). "10 runs committing a band · 9 delivered documents read · 8 printing a range." |
| 15b | Walk-forward scope stated in the study | **PASS** | `study_numbers.json['walkforward_scope']`: rule R-FCAL-01, scope **FULL**, 12 sourceable fiscal years from FY2014, status **run** (7 origins FY2018–FY2024, 650 scored cells, 01-09-2026). Stated to the reader in A.4's own caption in plain English — "rebuilding this company's forecast as it would have stood at each of 8 past year-ends … 25 tested cases over FY2014-FY2025" — with the one adopted correction (manufacturing depreciation at 1.0298) and the 11 watch flags named. `python3 scripts/check_walkforward_scope.py` → "ARCC FULL, 12 years, run". `python3 scripts/check_corrections_applied.py` → "manufacturing depreciation reconciles claimed 1.0298, exp(-bias/2) 1.0298". |
| 16 | Edition date — masthead, filenames, meta | **FAIL** | Study masthead: *"issued 9 September 2026"*, derived from `meta.edition_date` (`docx_arcc.py:100-102`). `meta.edition_date` = **2026-09-09**. Filenames all derived from `edition.py`. **The bibliography masthead reads "6 August 2026"** — `docx_biblio.py:121`, `r2 = p.add_run('   6 August 2026')`, a hard-typed string. That module imports `edition` and uses it for the output filename only. A delivered document of the 9 September edition is dated on its face a month and three days earlier, and this is exactly the defect `edition.py` was written to close. `python3 scripts/check_edition_date.py` does not catch it because it examines only `*_Valuation_Study_*` files. |
| 17 | Step 0.0 data-quality gate | **PASS, with one gap named** | `step0_result.json`: **2,957 clean rows**, span 12.22 years, 242.0 sessions a year, `dq_log: []`, `repairs: []` in `technicals.json`. Run before the fit, as the record shows. **The gap:** the screened series ends **2026-08-06 at EGP 59.00** while the valuation is struck against **77.00 on 2026-09-03**, a 30.5% gap. The study discloses this three times — in Table 17's caption, Table 18's caption and Table 20's caption — rather than hiding it, but §2 and §3 are read on a month-old tape. |
| 18 | Step 0 calibration under the band record | **PASS** | `band_record.by_key()[('EG','ARCC')]` → `n=44, hits=41, cov50=0.5909, cov80=0.9318, cov90=0.9318, width=1.4455, flag=None, p_value=0.6210`. The document reproduces every one: 44 forecasts, 93.2% against a 90% target, 59% against 50%, **count printed beside the percentage** in both places; width **1.45×** a naive carry-anchored random walk, *"disclosed rather than judged"* and never gated; the two-sided test at 5% earns no flag (p = 0.62) and §3 says so in words. Market gate in `step0_result.json`: PASS, skill 0.0158, 90% CI [0.009, 0.022], 30 panel names, 494 windows. |
| 19 | SIGCM's eight clauses | **PASS on seven, FAIL on the escalator clause** | `RP.assert_sigcm(SIGCM)` passes at `compute.py:2589`. Clause 1 evidenced by the audited statements themselves (FY2023/24/25 Deloitte, plus the reviewed H1-2026); clause 2 by the four-line `DriverLine` record (row 5); clause 6 by the beta record (rows 4/4a) rather than by the boolean that revisions 1–3 set True on a composite; clause 7 by 925 live formulas. **Clause on escalators — see row 21.** |
| 20 | Sweep Register — primary-source depth | **FAIL, and the failure is structural** | `python3 sweep.py` writes `sweep_register.json` and prints "validated through the SHARED register: 2 invariant error(s), 1 warning(s)". **The object validated is not the object written.** `sweep.py:55` defines a hand-written `FINDINGS` list of 26 dicts; those are re-added to the shared `SweepRegister` `R` at `:406-408`, then two more findings are added to `R` alone — the H1-2026 filing at `:431` and the COMPANY_IR investor-presentation finding at `:453` — and `record_primary_access()` is called on `R` at `:396`. Line `:519` then serialises **`findings=FINDINGS`**. Measured directly: `R.findings` holds **31** findings with source types `{REPUTABLE_PRESS 12, COMPANY_OFFICIAL 9, REGULATOR_OFFICIAL 3, SEARCH 3, AGGREGATOR 2, PRIMARY_MARKET_DATA 1, COMPANY_IR 1}`; the committed `sweep_register.json` holds **26** with `{REPUTABLE_PRESS 11, AUDITED_FINANCIAL_STATEMENTS 8, REGULATOR_OFFICIAL 3, DATA_AGGREGATOR 2, INDUSTRY_BODY 1, HOUSE_ENGINE 1}` — a different vocabulary, **no COMPANY_IR finding**, **no `primary_access` key at all**, and no record of the H1-2026 filing. `grep -c COMPANY_IR sweep_register.json` → **0**. So on its own contents the delivered register fails IR COVERAGE and PRIMARY ACCESS, and the eight invariants pass against an object nobody can read. |
| 20a | Company site attempted and logged either way | **PASS in code, FAIL on the record** | `sweep.py:396` logs `https://arabiancementcompany.com`, `reachable=False`, 2026-08-06, "connect_rejected at the environment proxy" — a failed attempt logged, which is the case the invariant exists to surface. It is in `R` and **not in the committed JSON**, per row 20. |
| 20b | Audited fiscal years from the filing itself | **PASS on the floor, below target** | Three audited years are read from the filings (`ARCC_FY2023/FY2024/FY2025_Consolidated_Financials.pdf` on disk, Deloitte Wafik Ramy & Partners, opinions signed 25-Feb-2026 and 23-Mar-2025), plus the reviewed H1-2026. Floor is two, target four. The register's own warning: "FS DEPTH: 2 distinct fiscal years ['FY2024','FY2025'] — target is 4; below target but above the floor". The walk-forward separately reaches FY2014. |
| 20c | Study-year quarters swept in before the build | **FAIL** | `sweep_date = 2026-08-06`. The register declares `study_quarters_disclosed=['Q1-2026','H1-2026']`, but its only company-ring trading finding is *"First-quarter 2026 net sales EGP 2.995bn … 31 March 2026"*, dated 2026-03-31. **The H1-2026 reviewed interim was filed on 13 August 2026** — the register's own input entries cite "filed 13 August 2026" for `auc_h1_26`, `capex_h1_26` and `capital_h1_26` — and it is the filing that sets FY2026 revenue, the EBITDA margin, the export-subsidy treatment and the 30-June-2026 balance sheet the whole bridge stands on. It was consumed by the model a week after the sweep closed and never swept. Not one of the 26 committed findings carries a date later than 2026-08-06. |
| 20d | Results releases swept with their statements | **PASS** | The FY2025 Investor Presentation is on disk and drives the volume build; it is registered as a COMPANY_IR finding in `R` at `sweep.py:453` and cited by page in the input register. Fails only through row 20's serialisation. |
| 20e | The register's own open invariants | **FAIL** | Two errors still fire and are named in the file: *"COVERAGE: INDUSTRY / 'technology substitution' unclosed"* and *"COVERAGE: COMPANY / 'one-off base-resetting transactions' unclosed — needs a finding or a dated negative search."* The second is not an empty category: §7 of the delivered study discusses **EGP 468mn of export subsidy collected in the second quarter of 2026 against EGP 32.6mn for the whole of FY2025**, excluded from forward operating profit — a textbook one-off base-resetting transaction the study found and the register never recorded. |
| 21 | One escalator per driver class, with a sourced near-term cost anchor | **FAIL** | `compute.py:1119-1122`: `c_mat = cc_mat_clk * infl * (1 - af_saving) * clk_prod`, `c_tra = cc_tra_t * infl * sold`, `c_ovh = cc_ovh_t * infl * sold` — **materials-and-fuel, transportation and overheads all escalate at the single `cost_infl` ladder**, the national CPI path [11.5%, 12.0%, 9.0%, 7.5%, 7.0%]. The only differentiation is `af_saving`, a saving overlay on one line, not a second escalator. The FY2025 note discloses the three cost classes separately, so per-class escalators are buildable. This is the standing one-escalator-per-driver-class rule and it is [L-009], learned **on this name** on 07-Aug-2026: *"Coal, imported fuel, local wages and transport do not all rise at the same speed."* The input's own source note defends the choice at length; it is a reasoned breach, not an oversight, and it is still a breach. **No sourced near-term cost anchor exists**: the register's nearest industry finding is an energy-intensity benchmark ("3.2-3.6 GJ per tonne of clinker and 90-110 kWh per tonne of cement"), not a price. `python3 scripts/check_frozen_escalator.py` separately records "ARCC: frozen and undeclared — finance_costs" (on the ratchet). |
| 22 | Margins as OUTPUTS wherever the filings disclose enough | **PASS** | Table 3 builds the cost stack per tonne and the EBITDA margin falls out — assertion 30/31: the unit build reproduces audited FY2025 revenue to **+0.000%** and audited FY2025 EBITDA to **+0.000%**; assertion 34: the FY2026 margin of 39.0% reproduces the margin the reviewed half implies to within half a point; assertion 35: with the alternative-fuel saving switched off the FY2030 margin is 35.5% against 36.8% in FY2026, so the escalators expand nothing on their own. `python3 scripts/check_forecast_anchor.py` → "ARCC EBITDA margin latest 0.3925 forecast 0.3903 −0.22pp". |
| 23 | 3y historical + 5y forecast with the full FCFF waterfall inline to PV of FCFF | **PASS** | A.1 and A.2 carry FY2023/24/25 audited and FY2026E–FY2030E forecast. §1.6 Table 9 prints every line: Revenue → EBITDA → EBITDA margin → depreciation and amortisation → plus other operating income → EBIT → effective tax rate → NOPAT → plus depreciation → less capital expenditure → less change in working capital → remaining fraction of the year → **free cash flow to the firm → discount factor → present value of FCFF**, five years. It does not stop at FCFF. §1.7 then carries PV of explicit FCFF 11,277 + PV of terminal 12,976 = EV 24,253 + net cash 687 − minority 0 = equity 24,940 ÷ 374.867mn shares = **66.53**. Assertion 17 checks the bridge closes; assertions 9–13 check the waterfall foots on every one of the five years. |
| 24 | v2 WACC | **PASS, with one gap named** | `cost_of_capital_record`: rf observed **22.95%**, less Egypt's **own** Damodaran default spread **3.40%**, normalised rf* **19.55%** — country risk enters once, and Table 6's caption says so ("leaving it in would have put the cost of equity at 31.68% instead of 28.28%"). **Both ERP bases published**: CDS 9.41% adopted, and the rating basis (Caa1, 6.37% spread, 13.94% total) computed consistently through to an explicit WACC of **28.78% against the adopted 27.60%** — §1.4 tells the reader to read 28.78% wherever the study prints 27.60% if they prefer it. Market-value weights: equity 96.22%, debt 3.78%. `python3 scripts/check_ke_reproduction.py` green; terminal construction named `relevered`, tax rate stated at the statutory 22.50% with the reasoning. `python3 scripts/check_cost_of_capital.py` → "ARCC EG, 27.60% gliding to 18.34%". **The gap:** `kd_gate.bound_met = false` — the adopted marginal Kd of **13.36% sits below the normalised sovereign 19.55%**, because 91.1% of the book is euro. The record flags it, §1.4 explains it and publishes the contracted alternative at 7.89%, and it is not described as conforming. |
| 25 | Terminal value share visible to the reader | **PASS** | **53.5%**. In the summary table's own column on page 2, again in Table 10 in §1.7, and discussed in §1.7's text against the two-thirds-to-four-fifths norm. In the workbook as a live cell. Assertion 20 checks it. |
| 26 | Non-sourced drivers logged to the driver ledger | **PASS** | `engine/Fundamental_Driver_Ledger.md:570` — "ARCC — Arabian Cement · EGX · integrated cement (registered class: cement and heavy industrial) · re-struck 3 September 2026", recording that the ledger had carried no ARCC entry against 23 House-ring drivers until that date. 30 inputs carry `ring: House` in the register and the House drivers are named in the bibliography's §5. |
| 27 | Rendered PDFs read as images, not as extracted text | **DONE — and it is what caught the most** | All 34 study pages and the bibliography rendered at 100 dpi with `pdftoppm` and read as images. Rows 6, 10, 10a, 10b, 16, 28 and 29 were found only this way: a clipped figure title, a caption saying the opposite of its own figure, a retired construction as a bar label, a bibliography masthead a month stale, a caveat contradicting §1.2, and an alternative construction attributed to the wrong beta. No programmatic check in this repository sees any of them. |
| 28 | Does the study contradict itself? | **FAIL** | §7 caveat, `docx_arcc.py:1465-1467`: *"The company discloses despatch volumes in its investor material, which would settle it; that material could not be reached from here, and the audited statements are image-only scans that carry no volume table."* §1.2 of the same document: *"The company publishes sales volumes and production indicators in its FY2025 investor presentation — clinker production, cement production, local sales, cement exports and clinker exports — and this build reproduces every one of them to within 0.02%."* `ARCC_FY2025_Investor_Presentation.pdf` is in the study directory; the bibliography cites it by page; assertion 23 reproduces all six disclosed tonnages. A stale paragraph from an earlier edition survives beside the correction that superseded it. |
| 29 | The alternative construction is attributed to the right beta | **FAIL** | §1.5, `docx_arcc.py:683-686`: *"on the **withdrawn basket** figure the cash-flow lens would read 74.54 against 66.53 on the adopted one, a difference of +12.0%."* The figure printed is `contested_judgements` entry 1's `fv_alternative`, whose own `name` field is *"Beta: same-country peer median (adopted, tier 2) **vs the own-stock regression against the EGX30**"* — beta **0.698**, not the withdrawn composite's 0.628. Table 8 on the same page brackets it: 0.60 → 79.16, 0.80 → 70.35, so 0.628 is worth roughly **EGP 77.9**, about EGP 3.4 above the printed number. The sentence names a construction SIGCM clause 6 calls a hard fail as the source of the study's most consequential published pair, and names it wrongly. |
| 30 | The answer, not only the process [R-GAP-01] | **PASS, with one gap named** | Committed central **EGP 66.5300** against the spot it was struck at, **EGP 77.00** on 2026-09-03 — **13.60% below**, and 13.1% below the latest known close of 76.60 (2026-09-06). Past the 10% trigger. `GAP_REVIEW_03-09-2026.md` exists and covers **all eight headings** — LATEST FILINGS (§1), BASE YEAR (§2), MACRO COHERENCE (§3), DISCOUNT RATE (§4), TERMINAL (§5), BALANCE SHEET (§6), CLAIMS AGAINST THE RECORD (§7), MULTIPLE CROSS-CHECK (§8). `python3 scripts/check_valuation_gap.py` → exit 0, "OK — no new violations", "ARCC −13.1% GAP_REVIEW_03-09-2026.md (audits 66.5300)". **The gap:** the review is the prior edition's. The fair value did not move — 66.53 in both editions — so the review still audits the number it was written against, and the gate accepts it; but no 09-09 review was written for an edition that added a new appendix. |
| 30a | Publication routing | **PASS — routing, not a defect** | `python3 scripts/check_publish_block.py` → "HELD ARCC the central is −13.1% below the price of 76.60 (2026-09-06), past the 10% publication limit — and no market dissent is filed". Referred to the principal under the traded-price gate, as stated. |
| 31 | Every deliverable ships as a PDF, current | **PASS** | Study PDF 34 pages, bibliography PDF 22 pages, both rendered 09-09-2026 at 12:00, after the `.docx` files were written at 11:46/11:48. `python3 scripts/check_delivered_pdf_currency.py` and `check_artefact_currency.py` green — "ARCC contested_judgements.json / efg_bridge.json / scenario_margin.json all built against 66.5300". |
| 32 | No rating, no price target | **PASS, with one note** | The Disclosure page states "No rating and no price target is expressed or implied", and none appears. §1.10 reports a **third party's** rating and target (EFG Hermes, Buy, EGP 69.75, 6 Aug 2026) and reconciles it item by item to 66.53 in a bridge whose bars sum to the gap exactly (−3.22 vs −3.22). That is evidence about someone else's model, not a recommendation, and it is labelled as such. |

---

## What the self-audit never looked at: the filings against the model

The question the Sweep Register exists to force, answered rather than skipped.

**1. The filing discloses eight useful lives and the model consumes one — and never prices
the choice.** `useful_lives.json` records, from note 2 and note 3.2.1 of the FY2025 filing
(OCR off the rendered pixels, route recorded): buildings 10–20 years, machinery and
equipment 20, furniture and fixtures 16, vehicles 10, other installations 20, computers 3–5,
plus an operating licence at 20 and an electricity contract at 10, and a separate schedule
for each of three subsidiaries. The terminal collapses all of them to a single 20-year
machinery life. That is a defensible choice and it is defended in the file. **What is not
defensible is that the file also computes what the choice is worth — EGP 55.92 / 66.63 /
75.55 / 82.69 a share — writes "it is a material contested judgement and belongs in the
register", and then nothing carries it.** It is the largest single swing in this model, more
than twice the beta, and the delivered study does not tell a reader it exists.

**2. The H1-2026 interim was consumed a week after the research closed.** The reviewed
half filed 13 August 2026 sets the FY2026 revenue calibration, the channel price recalibration
(local ×1.0788, export ×0.8711, cost ×0.9764), the services share falling from 6.325% to
4.42%, the export-subsidy treatment and the 30-June-2026 balance sheet the entire bridge
stands on. The sweep closed 6 August 2026 and no finding in the committed register is dated
after it. The single most consequential document in the base year was never swept.

**3. Note 5's cost split is read for its totals and not for its dynamics.** The filing
discloses materials-and-fuel, transportation, overheads and depreciation as four separate
lines. The model reads all four, builds the per-tonne stack correctly from them — and then
escalates three of them at one national inflation ladder (row 21). The disclosure supports
per-class escalators; the model does not build them.

**4. The three derived prices fail their own market test and the study says so.** Export
clinker derives to USD 30.0/t against a trade-press range of USD 44–48. That is honestly
published in §1.2, §7 and the risk register rather than tuned away, and it is the one item
in this class the study found on its own.

---

## The ten hard fails, in the order they would have to be cleared

1. **The bibliography of the 9 September edition is dated 6 August 2026 on its masthead** —
   `docx_biblio.py:121`, a hard-typed string in the one module that imports `edition.py` and
   uses it only for the filename. (row 16)
2. **The sweep register on disk is not the register the invariants ran against.** 26 findings
   serialised against 31 held; no COMPANY_IR finding, no primary-access log, no H1-2026
   filing, and a source-type vocabulary that is not the shared enum's. `sweep.py:519` writes
   `FINDINGS`, not `R`. (rows 20, 20a, 20d)
3. **The study's largest contested judgement is published nowhere.** The terminal useful life
   is worth **EGP 26.77 a share, 40% of the central**, on the study's own committed record,
   against the beta's EGP 8.01 that is called "most consequential". Depth-bar standard 8 is
   met for three judgements and missed for the biggest one. (row 13)
4. **The bibliography carries no judgements-with-overturn table and no negative-results
   table** — 0 hits for "overturn", 0 for "looked for" — while the study's own About panel
   tells the reader both are in it. (row 6)
5. **§7 says the investor material could not be reached** while §1.2 says it was read and
   reproduces every disclosed tonne to 0.02%, and the file is in the study directory. (row 28)
6. **Figure 2's caption says the margin narrows across the forecast.** It rises every year,
   39.03% → 40.40%, and §1.6 says so two pages later. (row 10a)
7. **§1.5 attributes EGP 74.54 to "the withdrawn basket figure"** when the committed record
   attributes it to the EGX30 own-stock regression — two different betas, ~EGP 3.4 apart, and
   the basket is a SIGCM hard fail that may not stand as the live alternative. (row 29)
8. **Figure 6's title is clipped mid-sentence** at the canvas edge in both the PNG and the
   delivered PDF. (row 10) **Figure 1 labels the published central "Weighted central"** on a
   study that retires the blend in four places. (row 10b)
9. **One blended cost escalator across three disclosed cost classes**, against the standing
   rule and against [L-009], which was learned on this name. No sourced near-term cost
   anchor. (row 21)
10. **`driver_test.py` crashes on the delivered workbook** at driver 29 of ~52
    (`KeyError: 'Beta (own-stock weekly regression)'`), and the committed result is from
    5 September and a superseded file. (row 8a) Plus the NEW column-width failure in the
    bibliography's `#` column, 0.76 cm against 0.84 cm needed. (row 11)

The H1-2026 sweep gap (row 20c), the two open coverage invariants (row 20e), the missing
committed ground-up record (row 5), the A.4 count row (row 15) and the un-committed 09-09
gap review (row 30) sit below those and are named rather than passed over.

---

## What is genuinely strong here, and should survive the repair

The model underneath is the best-evidenced part of this study and none of the failures above
touch it. **The workbook reproduces cell for cell against an independent sandbox rebuild —
2,079 populated cells, 0 differing — and a clean recalc on top of that: 925 of 925 formula
cells, 57 headline reconciliations, 0 unparseable.** `study_numbers.json` regenerates
byte-identical from a clean copy, and so do both documents, paragraph for paragraph. The
free-cash-flow waterfall foots by hand on all five years and runs inline to the present
value; the bridge closes exactly and charges the cash once. The physical build reproduces
every one of the six disclosed FY2025 tonnages to within 0.012% and the Egyptian sector
balance closes to 0.01Mt, both after three earlier editions that reconstructed tonnes from
an assumed price and came out 28% low. Margins are outputs of a disclosed cost stack and the
window is shown not to manufacture margin expansion. The beta was moved off a composite onto
the exchange's own published index, failed its usability gate honestly, and fell to a peer
median with the failed diagnostics printed rather than tucked away. The cost of capital nets
the sovereign spread once and publishes both equity-risk-premium bases at full size. And the
new appendix does exactly what it was asked to do: **years three to five are published as
ranges built from this company's own tested error, every cell reproducible from the run's
committed record, with the counts falling from five cases to three and the reason given.**

The defects are in the words, the labels, the register and one judgement that was measured
and never published. That is what makes reading the rendered pages and auditing the
provenance from outside the study gates rather than formalities.
