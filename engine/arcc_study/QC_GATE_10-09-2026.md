# ARCC — QC gate, 10-09-2026

Arabian Cement Company S.A.E. · Egyptian Exchange: ARCC · EGP · edition of 10-09-2026

Audited from OUTSIDE the study per [R-ENF-02] by a reader who did not build it. Every row
names the artefact, the command or the number that carries it. Nothing here is
self-certified, and where a row fails it says so plainly. The previous gate,
`QC_GATE_09-09-2026.md`, returned NOT DELIVERABLE with ten hard fails; its verdict is not
carried forward and nothing in it is assumed fixed. Each of its ten is re-established below
against the 10-09 artefacts, and the row that closes it says which.

**VERDICT: NOT DELIVERABLE.** **Twelve hard fails.** Three of the previous ten are cleared
and seven still stand; this edition then introduced six more, and the worst of them is not
subtle. **Four of the ten delivered figures were rendered on 8 September against a central
of EGP 66.53 and never rebuilt for this edition's answer of EGP 77.18.** On page 2 the
summary table prints 77.18 and the figure directly beneath it prints "base 66.5". On page 15
the sensitivity table prints 80.29–74.69 and the heat map beneath it prints 73.2–64.4, all
twenty-five cells different, on axis labels that do not match either. On page 17 the
reconciliation bridge ends at "Testahil central 66.53" while the table on page 16 ends at
77.18. **The delivered workbook PDF is a whole edition stale** — it prints a central of
77.49 and a cost of capital of 27.60% against the workbook's own 77.18 and 27.96% — and no
gate in this repository looks at it. **Table 6 no longer foots to its own cost of equity**:
the model moved to a split-premium construction and the page did not, so a reader
multiplying the printed rows gets 28.28% against the 28.65% printed beside them.

The arithmetic underneath remains in excellent order and should not be lost. The workbook
reproduces cell for cell against an independent sandbox rebuild (2,089 populated cells, 0
differing), both documents rebuild paragraph for paragraph identical, `study_numbers.json`
and `xlsx_expected.json` regenerate byte-identical, `compute.py` passes 42 assertions and
calls all four standing gates, `driver_test.py` now runs clean at 145 driver assertions and
0 dead inputs, and the terminal useful life — the judgement the last gate said was published
nowhere — is now published four ways. What fails is the PAPER, the REGISTER, and a set of
side artefacts that stopped following the answer.

---

## The edition audited, and how it was resolved

Resolved by date from disk and asserted non-empty before anything was opened, then checked
against `edition.py` rather than against a name typed here. **The repository moved under
this audit** — a concurrent edit rewrote `compute.py:2713` at 07:29 and
`study_numbers.json` at 07:31 today — so the audited state is pinned by checksum.

| Deliverable | File | md5 | Written |
|---|---|---|---|
| Study, Word | `ARCC_Valuation_Study_10-09-2026_public.docx` | `4cd90305…` | 13-09 06:29:25 |
| Study, PDF | `ARCC_Valuation_Study_10-09-2026_public.pdf` — 34 pages, 18 embedded images | `86fc5234…` | 13-09 06:29:57 |
| Workbook | `ARCC_Valuation_Model_10092026_public.xlsx` — 16 sheets, 928 formulas, 316 pasted, 2,089 populated | `eca308f9…` | 13-09 06:29:12 |
| Workbook, PDF | `ARCC_Valuation_Model_10092026_public.pdf` — 68 pages | `138462f7…` | **10-09 12:51** |
| Bibliography, Word | `ARCC_Bibliography_10-09-2026.docx` — 23 pages | `a1ef62e8…` | 13-09 06:29:41 |
| Bibliography, PDF | `ARCC_Bibliography_10-09-2026.pdf` | — | 13-09 06:29:58 |

`edition.py` holds `EDITION = date(2026, 9, 10)` and derives all four filenames plus the
PDF names. Superseded editions on disk: 06-08, 08-08, 02-09, 03-09, 09-09. **The workbook
PDF's timestamp is not cosmetic — see row 8b.**

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates are called in the study's own committed code, and the entry point runs | **PASS** | `compute.py:2768` `RP.assert_ground_up(_gu, ticker="ARCC")`; `:2845` `RP.assert_beta_provenance(BETA['own_stock'], tier2_fallback_documented=True)`; `:2846` `RP.assert_sigcm(SIGCM)`; `:2847` `RP.assert_model_study(MODEL_CHECK)`. Run from outside in a sandbox copy of the current tree: `PYTHONPATH=engine python3 compute.py` → exit 0, "42 assertions passed", "[Gates] … all called in this study's own code and all passed". |
| 2 | Provenance gate over every study directory | **PASS** | `python3 scripts/check_study_provenance.py` → "study directories: 25 · registered indices: 7 · known outstanding: 8 · OK — no new violations". ARCC appears on no outstanding list; the eight named are ADNOCDIST, ADNOCDRILL, AIRARABIA, BOROUGE, DU, ELEC, EMPOWER, MODON. |
| 3 | Live standard version, and the study's stamp | **FAIL — and it went red during this audit** | `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"` → **2026.09.10**. At 07:01 the study was frozen at 2026.09.01 by a typed literal and `check_standard_claim.py` was green. A concurrent edit at 07:29 replaced `compute.py:2713` with `STD_VERSION = RP.STANDARD_VERSION`, the stamp is now **2026.09.10**, and `python3 scripts/check_standard_claim.py` → **exit 1**, "ARCC claims 2026.09.10 but is ratcheted against: [R-ASSET-01] an asset-base record". `python3 scripts/check_asset_base.py` confirms: "ARCC the study states no information-set end date, so its asset base cannot be held to anything" — no `asset_base_record` is committed. The study now claims a standard it does not meet. |
| 4 | Beta — inspected on the record, not on a boolean | **PASS** | `beta_result.json`: `index_file` = **`raw_indices/EG/EGX30.csv`**, under `engine/raw_indices/`, not a constituent composite. `assets/data.js:569` gives `code: "EGX:ARCC"`, so the listing exchange is EGX and EGX30 is its published index — resolved from the code prefix, not from the `raw_ohlc/EG/` folder. No interim note owed (not a DFM name), no QATAR10 caveat owed. `interim_note: null`, `conforming: true`, `week_rule: W-THU`, 253 weekly observations to 2026-07-16 over 4.89 years, index as-of 2026-07-22, `index_dq: []`, `stock_dq: []`. |
| 4a | The usability gate, and what the study did when it failed | **PASS** | The own-stock fit FAILS: R² = **0.046838** against the 5% floor, SE(β) = **0.22836** against β = **0.69809** — `usable: false`, `gate_msg: "R^2=0.047 < minimum 0.05 — index barely explains the stock's returns"`. The study does not keep it. It falls to the **median of four same-country peer regressions against the same EGX30** — LCSW 0.81452, EGAL 0.91864, ORAS 0.93640, EGCH 1.03021, median **0.92752** — and prints the failed diagnostics on page 10 ("a beta of 0.698 on 253 observations — with an R-squared of 4.7% and a standard error of 0.228"). SCEM (0.60724, R² 0.0245) is named and deliberately excluded as weaker still. The unlever-and-relever step is declared NOT PERFORMED with its direction named. |
| 4b | The withdrawn composite | **PASS on withdrawal; FAIL on how it is described — see row 7a** | `beta_result.json` `adopted.retired`: beta 0.6281, "regressed against an equal-weight composite of the covered Egyptian names. SIGCM clause 6: a constituent composite is a HARD FAIL, not a tier." No composite number survives in the adopted build. |
| 5 | Ground-up clause — the driver record, not a flag | **PASS on the record, FAIL on committing it** | Four `DriverLine`s at `compute.py:2721-2767`, recomputed independently from the committed inputs and covering **100.0000%** of FY2025 revenue. **Share by level: unit 94.05%** — local cement 67.0864%, export cement 8.7951%, export clinker 18.1700%, each naming its unit, unit source (FY2025 investor presentation, performance highlights, tonnages 2,923.6 / 629.5 / 1,300.5 thousand) and price basis (derived from the audited note-4 revenue line over the disclosed tonnage, recalibrated on the H1-2026 half) — and **segment 5.95%** for transportation services, carrying a full gap note (no haulage volume or rate per tonne-kilometre is disclosed; the walk-forward's attempt to split the cost across 45 period pairs returned a local rate spanning −425 to +722 EGP/t with nine economically impossible solves). **The failure is unchanged from 09-09:** `study_numbers.json` has no `ground_up` key — `'ground_up' in d` → **False**. `python3 scripts/check_ground_up.py` → "known ARCC commits no driver-line record at all" (ratcheted, so the gate stays green while the record stays absent). |
| 6 | Depth 1 — standalone bibliography document | **FAIL — unchanged from 09-09** | `ARCC_Bibliography_10-09-2026.docx`/`.pdf`, 23 pages, ships. It carries the primary-documents list, a 14-row source catalogue, the **full 232-input register** (Company 176, House 29, Country 14, Industry 12, Market 1) each with value / date / source-and-construction, a 10-row derived-figures section, a 27-row research trail, an 11-row driver-basis table and a real aggregator-discrepancy note (an aggregator print of EGP 2,894.13mn for total liabilities identified as total CURRENT liabilities). **It still carries no judgements table with what-would-overturn-it and no negative-results table.** Programmatic scan of all 1,689 paragraphs and cells: "overturn" **0 hits**, "not found" **0**, "looked for" **0**, "negative result" **0**. The delivered study's own About panel, page 34, still tells the reader they are there: *"a standalone bibliography listing every registered input with its source, the judgements with what would overturn each one, and the negative results — the things looked for and not found."* Two of those three claims are false. |
| 6a | The bibliography's own driver table contradicts the model | **FAIL — not reported by the 09-09 gate** | §5, row "Terminal growth": *"Held at the **5% house default** for an established emerging-market industrial once currency turbulence has passed, sensitised 3-7%."* The committed input is `g_term = 0.07` ("Terminal growth from the house macro path: terminal inflation plus a STATED real growth of zero") and page 14 of the study says *"Terminal growth is held at 7%."* The string is hard-typed at `sweep.py:379`. It was in the 09-09 bibliography too and was missed. |
| 7 | Depth 2 — four-field provenance, counted | **PASS, with one gap named** | **232 inputs**, every one complete on all four fields; a shape scan over `study_numbers.json['inputs']` returns exactly one field-set — `('date','ring','source','value')` — for all 232. `python3 scripts/check_four_field.py` → exit 0, "OK — every committed input carries all four fields". **The gap:** the fourth field is named `ring`, not `layer`; the gate accepts either, and the name is what then reaches the delivered page (row 9). |
| 7a | The alternative construction is attributed to the right beta | **FAIL — still standing, and the error is now larger** | Page 10, `docx_arcc.py`: *"on the **withdrawn basket** figure the cash-flow lens would read 88.96 against 77.18 on the adopted one, a difference of +15.3%."* The figure printed is `contested_judgements.json` entry 2's `value_alternative` = **88.95568**, whose own `name` field reads *"Beta: same-country peer median (adopted, tier 2) **vs the own-stock regression against the EGX30**"* — beta **0.698**, not the withdrawn composite's **0.628**. Table 8 on the same page brackets it: 0.60 → 95.83, 0.80 → 82.86, so 0.628 is worth roughly **EGP 94.4**, about **EGP 5.4** above the printed number (it was EGP 3.4 at 09-09). The sentence names a construction SIGCM clause 6 calls a hard fail as the source of the study's published pair, and names it wrongly. |
| 8 | Depth 3 — numeric traceability, recalculated independently | **PASS** | Sandbox rebuild from a clean copy of the current study directory: `study_numbers.json`, `xlsx_expected.json` and `formula_count.json` all regenerate **byte-identical** (`diff` → 0 lines each). `build_xlsx_arcc.py` rebuilt the workbook and an independent **cell-by-cell diff against the delivered file compared 2,089 populated cells across all 16 sheets and found 0 differing**. Both documents rebuilt and compared paragraph-and-cell by paragraph-and-cell: study 1,373 vs 1,373, **0 differing**; bibliography 1,689 vs 1,689, **0 differing**. `python3 recalc.py` on the delivered workbook: **"928 of 928 formula cells reproduce the model, 0 unresolvable, 0 unchecked; 57 headline reconciliations passed"** — nothing unparseable, nothing skipped. `python3 footing_check.py` → "tables examined: 33; unreconciled totals: 0". `python3 prose_check.py` → "prose figures checked: 672; unmatched: 0". `python3 label_gate.py` → "18 labels checked, every one describes its own row". Live share 928/(928+316) = **75%**. |
| 8a | Driver propagation on the delivered workbook | **PASS — the 09-09 crash is cleared** | `python3 driver_test.py` on the delivered file → **exit 0**, "DRIVER TEST OK — **145 driver assertions, every one in the asserted direction; 0 dead inputs**", plus three rows exempted because their own label declares them inert. The `KeyError: "no Assumptions row labelled 'Beta (own-stock weekly regression)'"` that crashed it at driver 29 is gone, and the committed `driver_test_result.json` now records `n_cases 145, n_failed 0, dead 0` against the file that ships. A new country-risk assertion is present at `driver_test.py:313` — "country risk is charged once and flat, and more of it still costs value". |
| 8b | The delivered workbook PDF | **FAIL — NEW, and no gate in this repository sees it** | `ARCC_Valuation_Model_10092026_public.pdf` has `CreationDate: Thu Sep 10 12:24:45 2026` — **three days and one rebuild before the .xlsx it names**, written 13-09 06:29:12. `pdftotext` on it: the Summary sheet's "DCF (cash flow)" and "CENTRAL FAIR VALUE" both read **77.49**, and "27.60%" appears four times as the blended cost of capital. The delivered workbook carries **77.18** and **27.96%**, and 77.18 appears in the PDF **zero times**. `scripts/check_delivered_pdf_currency.py:70-72` selects only files whose basename contains `Valuation_Study` or `Valuation_Report`, so it reports ARCC clean while the workbook a reader downloads publishes a central this study does not. |
| 9 | Depth 4 — external-reader scrub | **FAIL — unchanged from 09-09** | `python3 scrub_gate.py --check` → exit 0, "scrubbed 3 delivered document(s), 225,282 characters … external-reader scrub: CLEAN — 0 hits across 39 terms". That result is real for the 39 terms it holds. **The list still does not hold `ring`, `step`, `sweep` or `register`**, and the delivered bibliography prints them: §4 is headed "Research trail" and opens *"The research was carried out on 2026-08-06 across four concentric **rings** — global, country, industry and company — with 27 recorded findings"*, the table's first column is headed **`Ring`** (12 lower-case and 2 capitalised occurrences across the document), the masthead reads "Source **Register**" and the word appears 11 times. The depth bar names "sweep/ring vocabulary" and "register jargon" explicitly; the model report's bibliography uses **`Layer`**. |
| 9a | Verdict tokens and CRPS | **PASS** | `band_record.assert_no_verdict_tokens()` run from outside over every visible string of all three delivered files: study **102,072 chars CLEAN**, bibliography **93,600 chars CLEAN**, workbook **54,913 chars CLEAN**. "CRPS" occurs **0** times in all three. `python3 scripts/check_band_vocabulary.py` → "[R-CAL-02] OK — no verdict vocabulary in any page text"; `check_delivered_vocabulary.py` → ARCC in the CLEAN list of 23. |
| 9b | Calibration evidence in §3, no calibration appendix | **PASS** | The document has exactly three appendices — A financial statements, B peer set and risk, C expert panel. Calibration lives in §3 and §4 as plain sentences with the statistics inline: *"Over 44 resolved three-month forecasts this name's price finished inside the 90% band 93.2% of the time, against a 90% target"*, and *"inside the ninety-per-cent band 93% of the time, and inside the fifty-per-cent band 59% of the time. The count is printed beside the percentage because a percentage without its count is the number that misleads."* |
| 10 | Depth 5 — figure discipline, transparency | **PASS on transparency** | Programmatic: all 10 PNGs on disk are RGBA with alpha extrema **(255, 255)** — zero transparency — on a solid canvas at corner luminance **249** (250 on `fig_efg_bridge.png`). `python3 scripts/check_figure_opacity.py` → exit 0, "OK — every delivered figure is opaque to the pixel"; `check_figure_axes.py` → exit 0. |
| 10a | Depth 5 — every figure inspected as a rendered image | **FAIL — NEW, and it is the worst defect in this edition** | All 10 PNGs read as images and all 34 PDF pages rendered at 100 dpi with `pdftoppm` and read as images. **Four delivered figures are stale.** Rebuilt independently in a sandbox from the current committed numbers and diffed pixel by pixel: `fig1_football` **67,070 of 970,200 pixels differ (6.9%)**, `fig2_sens` **550,250 of 832,050 (66.1%)**, `figD1_experts` 855 (0.1%), `fig_efg_bridge` differs. All ten PNGs are dated **8 September 23:34–23:50**, against delivered documents of 13 September. What a reader sees: **page 2** — the summary table prints "DCF (cash flow) 77.18" and Figure 1 immediately beneath it prints "Discounted cash flow (primary) … **base 66.5**" and "Weighted central … base 66.5". **Page 15** — Table 11 prints 80.29/80.31/80.26/80.10/79.80 down to 75.15/…/74.69 on rate labels 24.96–30.96%, and Figure 3 beneath it prints 73.2/72.4/71.4/70.2/68.7 down to 68.5/…/64.4 on rate labels 24.6–30.6%; **all twenty-five cells differ and so do all five axis labels**. Figure 3's own title reads "higher growth gives a LOWER value" while the caption directly under it reads "The growth axis is almost flat" and the caption above it says the value moves EGP 0.49 across a row — the rebuilt figure's title reads "growth barely moves it — 0.6% across the whole range". **Page 17** — Figure 4's waterfall ends at "**Testahil central 66.53**" with bars −2.12 / −6.69 / +9.77 / **+11.67** / **−6.19** / **−3.96** / −5.67 / **−0.04**, against Table 14 on page 16 which ends at **77.18** with +9.74 / **+21.92** / **−6.12** / **−3.60** / −5.67 / **−0.03**; its subtitle prints "the whole EGP **3.22** gap" where page 16 says "the whole EGP **-7.42** gap". `efg_bridge.json` is current (built against 77.1781, per `check_artefact_currency.py`) and the figure built from it is not. Figure C1 on page 30 prints Expert 3 "central 64.9" against Table C1's 64.84. |
| 10b | Figure 6's clipped title | **FAIL — unchanged from 09-09** | Figure 6's title is still cut at the canvas edge, ending mid-sentence at *"…illustrative, anchored on the last session of the price history rather than on th"*. Visible in `fig4_fan.png` and on **page 21** of the delivered PDF. The full string is at `figures.py:167-169` and does not fit the axes width at `loc='left'`. |
| 10c | Figure 1's bar label against the study's own record | **FAIL — unchanged from 09-09** | `figures.py:53,55,63` still hard-code the fifth bar as **"Weighted central"**. The study retires the blend explicitly — page 3 *"There are no weights in this model to report"*, page 18 *"the weights are retired and so is the description"*, page 2's own caption *"A previous edition blended all four at fixed weights of 50, 20, 22 and 8 per cent and published EGP 63.89"*, and assertion 40 *"the central IS one lens, not a blend of several: there are no weights left to sum"*. A reader of Figure 1 on page 2 is told the published central is a weighted blend of the four bars above it. |
| 10d | Figure 2's caption against its own figure | **FAIL — unchanged from 09-09** | `docx_arcc.py:446-447` captions Figure 2 *"The margin is the gap, and the gap **narrows** across the forecast."* The margin path in `study_numbers.json['forecast']['margin']` is **0.39028, 0.39495, 0.39986, 0.40200, 0.40404** — rising in every year. Figure 2's own printed labels on page 6 read margin 39% / 39% / 40%, and page 11 says *"the EBITDA margin dips once, to 39.0% in FY2026 … and then rises in every year after it to 40.4% by FY2030"*. The caption says the opposite of the figure it labels. |
| 11 | Depth 6 — table discipline, three counts per document | **PASS** | Independent audit reading each document's own text frame off `sections[0]` rather than assuming it, and each table's `gridCol` widths off the XML. **Study** (frame 17.78 cm): **34 tables — starved 0, bloated 0, over-wide 0, no explicit grid 0**. **Bibliography** (frame 24.89 cm): **6 tables — starved 0, bloated 0, over-wide 0, no explicit grid 0**. `python3 scripts/check_column_widths.py` → exit 1, but the four columns it names are PHDC's; **ARCC appears nowhere in its output**. The 09-09 failure — the bibliography's `#` column at 0.76 cm against 0.84 cm needed for a 232-row register — is cleared. |
| 12 | Depth 7 — expert appendix at maximum detail | **FAIL — unchanged from 09-09** | Present: three experts, each with a worldview paragraph, a stated valuation range and central, and an explicit falsifier ("What would prove this wrong"); C.4 cross-examination with six objections each conceded or rejected and the arithmetic beside each (four conceded, two rejected); C.5 the three in one room; C.6 a divergence table with four pairs. **Absent: the per-expert worked table with every intermediate line, and the named per-expert sensitivity with numbers.** Table C1 on page 31 is a four-row summary of the panel (low / central / high / versus spot), not a worked build — nothing corresponds to the model report's "Expert 1 runs 13 lines from mid-cycle earnings to fair value per share". Page 24 states the nine panel figures are built as formulas in the workbook; the depth bar asks for the intermediate lines in the appendix, and they are not there. |
| 12a | Expert labels | **PASS** | Headings read "C.1 Expert 1 — Replacement-cost industrialist", "C.2 Expert 2 — Mid-cycle earnings-power analyst", "C.3 Expert 3 — Cash-return and distribution investor". Figure C1's y-axis reads "Expert 1 / Expert 2 / Expert 3". Table C1 and Table C3 use the same labels. No persona name anywhere in any of the three files. |
| 13 | Depth 8 — the central contested judgement computed both ways | **PASS, with two gaps named — the 09-09 hard fail is cleared** | **The terminal useful life is now published.** `contested_judgements.json` carries four judgements, each computed through the study's own bridge: cost of debt 13.36% vs 7.89% (77.31, +0.2%), beta 0.928 vs 0.698 (88.96, +15.3%), capex USD 3.23/t vs book depreciation (81.31, +5.4%), and **terminal useful life 20 years vs 50 years (94.53742, +22.5%)**. It appears in **Table 13 on page 16** with both framings and the value; in the **body**, page 2 — *"The study's LARGEST contested judgement is a different one — terminal useful life, worth +22.5% — and it is set out with the rest in section 1.9"* — and again at §1.8; and in the **workbook** at `Fundamental Valuation` A23 / E23 = 94.53742109. The ranking is computed in `diagnostics_arcc.py` rather than typed, so it cannot go stale again. **Gap one:** the depth bar also asks for it in the summary table and in an expert's range, and it is in neither — the summary table on page 2 carries no contested pair at all, and none of the three experts runs a terminal life. **Gap two — row 13a.** |
| 13a | A judgement the study calls the most consequential, and does not price | **FAIL — NEW in substance, the same shape as the defect just closed** | Page 13: *"Growth in the terminal state has to be paid for, and **the choice of what capital it is paid on is the single most consequential judgement in this model**."* That choice — replacement-cost invested capital (EGP 45,911mn) against the audited book (return on capital 11.73% vs **60.6%**) — is in no summary table, in no body table, in `contested_judgements.json`, in the workbook or in any expert's range. Page 25 §7 describes it and declines to price it: *"A reader who prefers the book basis should know it **lifts the valuation substantially**, and should say why a plant carried at a tenth of replacement cost is the right denominator."* Two superlative claims stand in one document — page 2's "LARGEST contested judgement" for the useful life, page 13's "single most consequential judgement" for the capital base — and only one of the two is computed both ways. |
| 14 | Structure, sheet list and format match the model report | **PASS** | `python3 scripts/check_document_structure.py` → ARCC in "MATCHES THE MODEL REPORT (23)". `python3 scripts/check_workbook_structure.py` → same. Verified directly against `MODEL_REPORT_09-08-2026.docx`: masthead + READ FIRST · Headline · Valuation summary · Company overview · §1 with §1.1–1.11 · §2 · §3 · §4 · §5 · §6 · §7 · Appendix A (A.1–A.4) · Appendix B (B.1–B.3) · Appendix C (C.1–C.6) · About · Disclosure — the model's skeleton with the sub-section titles a single-plant cement producer needs. `wb.sheetnames == research_protocol.MODEL_STUDY['excel_sheets']` returns **True** for all 16 sheets in order. The class is a single-asset cement operating company, which is ADNOCLS's operating-company pattern inside ADNOCLS's skeleton — the nearest of the three reference names — and page 3 says why a sum-of-the-parts is refused ("A sum-of-the-parts here would be a sum of one part"). |
| 15 | Appendix A.4 — years three to five as ranges | **PASS on the arithmetic, FAIL on one count — unchanged from 09-09** | Every cell recomputed independently from `study_numbers.json['forecast']` × `arcc_walkforward/forward_ranges.json` at horizons 3/4/5. Revenue point 16,440 / 17,688 / 18,934; low 8,062 / 14,344 / 17,168 (×0.49038, ×0.81091, ×0.90673); high 49,203 / 71,149 / 71,453 (×2.99294, ×4.02241, ×3.77380). Volume point 4.89 / 4.91 / 4.93. Profit before tax point 6,498 / 7,101 / 7,731; low 345 / 2,641 / 3,925 (×0.053151, ×0.371972, ×0.507708); high 42,128 / 268,271 / 360,783 (×6.48326, ×37.78054, ×46.66837). All nine rows reproduce to the printed digit; nothing is typed. **The failure:** the last row is headed "Tested cases behind each band" and prints **5 / 4 / 3**, which is revenue's n and volume's n. **Profit before tax at FY2028E rests on n = 4, not 5** (`forward_ranges.json` `pbt."3".n = 4`). One of the nine bands is still described by the wrong count. |
| 15a | Far-year range gate | **PASS** | `python3 scripts/check_forward_ranges.py` → exit 0, "ARCC prints a far-year range **shape B** (`ARCC_Valuation_Study_10-09-2026_public.docx`)", and ARCC is not on the ratchet. |
| 15b | Walk-forward scope stated in the study | **PASS** | `study_numbers.json['walkforward_scope']`: rule R-FCAL-01, scope **FULL**, 12 sourceable fiscal years from FY2014, status **run**. Stated to the reader in A.4's own caption in plain English — "rebuilding this company's forecast as it would have stood at each of 8 past year-ends … 25 tested cases over FY2014-FY2025" — with the one adopted correction named. `python3 scripts/check_walkforward_scope.py` → "ARCC FULL, 12 years, run". `python3 scripts/check_corrections_applied.py` → "ARCC manufacturing depreciation reconciles claimed 1.0298, exp(-bias/2) 1.0298" (the gate's own FAIL is ADIB). `check_driver_coverage.py` → "ARCC 26 drivers, all carrying a verified pair". |
| 16 | Edition date — masthead, filenames, meta | **PASS — the 09-09 hard fail is cleared** | Study masthead: *"valuation as of 30 June 2026, the date of the latest disclosed balance sheet; **issued 10 September 2026**"*, derived from `meta.edition_date` = **2026-09-10**. **The bibliography masthead now reads "Testahil · Arabian Cement Company S.A.E. (EGX: ARCC) — Source Register   10 September 2026"**, derived at `docx_biblio.py:126-127` from `_ed.EDITION` rather than the hard-typed "   6 August 2026" that shipped at 09-09. All four filenames derive from `edition.py`. `python3 scripts/check_edition_date.py` → exit 1, but the two it names are ADIB and SCEM; **ARCC appears nowhere**. `check_typed_dates.py` → exit 0. |
| 17 | Step 0.0 data-quality gate | **PASS, with one gap named** | `step0_result.json`: **2,957 clean rows**, span **12.219 years**, **242.0 sessions a year**, `dq_log: []`, no repairs. Run before the fit, as the record shows. **The gap, unchanged:** the screened series ends **2026-08-06 at EGP 59.00** while the valuation is struck against **77.00 on 2026-09-03**, a 30.5% gap. The study discloses it three times — Table 17's, Table 18's and Table 20's captions each say so explicitly ("THE DATES DIFFER AND THAT MATTERS HERE") — rather than hiding it, but §2 and §3 are read on a month-old tape. `python3 scripts/check_macro_anchor_age.py` → exit 1 with ARCC "listed": "struck 28 days after the EG currency anchor it derives from", against a 14-day bound; on the ratchet, and the FAIL it reports is ADIB. |
| 18 | Step 0 calibration under the band record | **PASS** | `band_record.by_key()[('EG','ARCC')]` → `n=44, hits=41, cov50=0.59091, cov80=0.93182, cov90=0.93182, width=1.44548, flag=None, p_value=0.62099`. The document reproduces every one: 44 forecasts, 93.2% against a 90% target, 59% against 50%, **count printed beside the percentage** in both places; width **1.45×** a naive carry-anchored random walk, *"disclosed rather than judged"* and never gated; the two-sided test at 5% earns no flag and §4 says so in words ("No flag is earned on a two-sided test at the five-per-cent level"). Market gate in `step0_result.json`: PASS, skill 0.0158, 90% CI [0.009, 0.022], 30 panel names, 494 windows. |
| 19 | SIGCM's eight clauses | **PASS on seven, FAIL on the escalator clause** | `RP.assert_sigcm(SIGCM)` passes at `compute.py:2846`. Clause 1 evidenced by the audited statements themselves (FY2023/24/25 Deloitte Wafik Ramy & Partners, opinions signed 25-Feb-2026 and 23-Mar-2025, plus the reviewed Q1-2026 and H1-2026); clause 2 by the four-line `DriverLine` record (row 5); clause 6 by the beta record (rows 4/4a) rather than by the boolean that revisions 1–3 set True on a composite; clause 7 by 928 live formulas. **Clause on escalators — see row 21.** |
| 20 | Sweep Register — primary-source depth | **FAIL, and the failure is structurally unchanged** | `python3 sweep.py` writes `sweep_register.json` and prints "wrote sweep_register.json — 27 findings … validated through the SHARED register: 2 invariant error(s), 1 warning(s)". **The object validated is still not the object written.** `sweep.py:574` serialises **`findings=FINDINGS`**, the hand-written list, while every invariant runs against the shared register `R`. Measured directly by importing the module: **`R.findings` holds 32** with source types `{REPUTABLE_PRESS 13, COMPANY_OFFICIAL 9, REGULATOR_OFFICIAL 3, SEARCH 3, AGGREGATOR 2, PRIMARY_MARKET_DATA 1, COMPANY_IR 1}` and one `PrimaryAccessAttempt`; the committed `sweep_register.json` holds **27** with `{REPUTABLE_PRESS 12, AUDITED_FINANCIAL_STATEMENTS 8, REGULATOR_OFFICIAL 3, DATA_AGGREGATOR 2, INDUSTRY_BODY 1, HOUSE_ENGINE 1}` — a different vocabulary, **no COMPANY_IR finding**, **no `primary_access` key**. `grep -c COMPANY_IR sweep_register.json` → **0**; `grep -c primary_access` → **0**. Its own `qc_line` says "26 findings" against the 27 in the file. So on its own contents the delivered register fails IR COVERAGE and PRIMARY ACCESS, and the eight invariants pass against an object nobody can read. `python3 scripts/check_sweep_module.py` → exit 0, because it checks that the study runs through the shared register, not that it writes it out. |
| 20a | Company site attempted and logged either way | **PASS in code, FAIL on the record** | `sweep.py` logs `https://arabiancementcompany.com`, `reachable=False`, 2026-08-06, "connect_rejected at the environment proxy. The company's own site could not be reached from this build…" — a failed attempt logged, which is the case the invariant exists to surface. It is on `R.primary_access` and **not in the committed JSON**, per row 20. |
| 20b | Audited fiscal years from the filing itself | **PASS on the floor, below target** | Three audited years are read from the filings on disk (`ARCC_FY2023/FY2024/FY2025_Consolidated_Financials.pdf`), plus the reviewed Q1-2026 and H1-2026 interims. Floor is two, target four. The register's own warning: **"FS DEPTH: 2 distinct fiscal years ['FY2024','FY2025'] — target is 4; below target but above the floor"** — which under-counts what the study actually holds, because FY2023 is read and not registered. The walk-forward separately reaches FY2014. |
| 20c | Study-year quarters swept in before the build | **FAIL — unchanged from 09-09** | `sweep_date = 2026-08-06`. The register's distinct source dates run 2025-05-01 … 2026-08-06 plus one press item at **2026-09-10**. **The H1-2026 reviewed interim, filed 13 August 2026, is nowhere in it**: `grep -c 'H1-2026'` → **0**, `grep -c '13 August 2026'` → **0**. The bibliography's own input register cites "filed 13 August 2026" for `auc_h1_26`, `capex_h1_26` and `capital_h1_26`, and that filing sets FY2026 revenue, the channel recalibration (local ×1.0788, export ×0.8711, cost ×0.9764), the services share, the export-subsidy treatment and the 30-June-2026 balance sheet the whole bridge stands on. It was consumed by the model a week after the sweep closed and was never swept. |
| 20d | Results releases swept with their statements | **PASS in code, FAIL on the record** | The FY2025 Investor Presentation is on disk, drives the volume build and is registered as the single COMPANY_IR finding on `R` (page 5, sales volumes and production indicators, the Egyptian market balance). It is cited by page in the bibliography's input register. Fails only through row 20's serialisation — `grep -c 'Investor Presentation' sweep_register.json` → **0**. |
| 20e | The register's own open invariants | **FAIL — unchanged from 09-09** | Two errors still fire and are named in the file: *"COVERAGE: INDUSTRY / 'technology substitution' unclosed"* and *"COVERAGE: COMPANY / 'one-off base-resetting transactions' unclosed — needs a finding or a dated negative search."* The second is not an empty category: page 25 §7 of the delivered study discusses **EGP 468mn of export subsidy collected in the second quarter of 2026 against EGP 32.6mn for the whole of FY2025**, excluded from forward operating profit — a textbook one-off base-resetting transaction the study found and the register still has not recorded. |
| 20f | This edition's own new sector finding reaches nobody | **FAIL — NEW** | `compute.py:1056` registers `egy_new_licence_mt = 3.5` ("two production licences planned from October 2025, each stated at 1.5-2.0 million tonnes a year … It sits ON TOP of the 12.6 million tonnes of revival"), and `:2454-2455` puts `new_licence_mt` and `forward_capacity_mt` (76.0 + 3.5 = 79.5 Mt) into the `sector` record. **Neither reaches a reader and neither moves anything.** The delivered study contains "3.5Mt" twice and both are the local-cement price paragraph's unrelated figures; "new production licence" **0 hits**, "79.5" **0 hits**, "licences" **0 hits**. The delivered workbook carries **0 cells** labelled with either. They are not on the Assumptions sheet, so `driver_test.py`'s dead-input sweep cannot reach them and its "0 dead inputs" does not cover them. The study's sector paragraphs on pages 3 and 28 still compute idle capacity against 76.0 Mt of nameplate — *"the 12.6Mt restart programme would take idle capacity to about 23.5Mt"* — with the newly licensed 3.5 Mt absent. `GAP_REVIEW_10-09-2026.md` records it as "**NEW. Registered as sector supply**, 3.5 Mt at the midpoint"; registered, and consumed by nothing. |
| 21 | One escalator per driver class, with a sourced near-term cost anchor | **FAIL — unchanged from 09-09** | `compute.py:1230-1232`: `c_mat = cc_mat_clk * infl * (1 - V['af_saving'][i]) * ph['clk_prod']`, `c_tra = cc_tra_t * infl * ph['sold']`, `c_ovh = cc_ovh_t * infl * ph['sold']`, where `infl = V['cost_infl'][i]` — **materials-and-fuel, transportation and overheads all escalate at the single `cost_infl` ladder**, the national CPI path [11.5%, 12.0%, 9.0%, 7.5%, 7.0%]. The only differentiation is `af_saving`, a saving overlay on one line, not a second escalator. The FY2025 note discloses the three cost classes separately, so per-class escalators are buildable. This is [L-009], learned **on this name** on 07-Aug-2026: *"Coal, imported fuel, local wages and transport do not all rise at the same speed."* **No sourced near-term cost anchor exists**: the register's seven industry-ring findings are a quota abolition, a supply pipeline, a demand-and-price note, a capacity ratio, an export volume, a peer-set note, and cost benchmarks that are **energy intensities and a replacement cost** ("3.2-3.6 GJ per tonne of clinker and 90-110 kWh per tonne of cement; replacement cost … USD 120-1xx"), not a price for coal, fuel, power or haulage. `python3 scripts/check_frozen_escalator.py` separately records "ARCC: frozen and undeclared — finance_costs" (on the ratchet). |
| 22 | Margins as OUTPUTS wherever the filings disclose enough | **PASS** | Table 3 on page 6 builds the cost stack per tonne and the EBITDA margin falls out. Assertions 30/31: the unit build reproduces audited FY2025 revenue to **+0.000%** and audited FY2025 EBITDA to **+0.000%**. Assertion 34: the FY2026 margin of 39.0% reproduces the margin the reviewed half implies to within half a point. Assertion 35: with the alternative-fuel saving switched off the FY2030 margin is **35.5%** against 36.8% in FY2026, so the escalators expand nothing on their own. `python3 scripts/check_forecast_anchor.py` → "ARCC EBITDA margin latest 0.3925 forecast 0.3903 −0.22pp" (its FAIL is ADIB). |
| 23 | 3y historical + 5y forecast with the full FCFF waterfall inline to PV of FCFF | **PASS** | A.1 and A.2 carry FY2023/24/25 audited and FY2026E–FY2030E forecast. §1.6 Table 9 on page 11 prints every line: Revenue → EBITDA → EBITDA margin → depreciation and amortisation → plus other operating income → EBIT → effective tax rate → NOPAT → plus depreciation → less capital expenditure → less change in working capital → **remaining fraction of the year** → **free cash flow to the firm → discount factor → present value of FCFF**, five years. It does not stop at FCFF. §1.7 then carries PV of explicit FCFF 11,376 + PV of terminal 16,868 = EV 28,245 + net cash 687 − minority 0 = equity 28,932 ÷ 374.867mn shares = **77.18**. Assertion 17 checks the bridge closes exactly; assertions 4–13 check the waterfall foots on all five years. `python3 scripts/check_bridge.py` → ARCC "stands on the 2026-06-30 sheet; minority on the value_share basis" (its FAIL is ADIB); `check_bridge_reaches_answer.py` → exit 0. |
| 24 | v2 WACC | **PASS on the record, FAIL on the page — see rows 24a and 24b** | `cost_of_capital_record`: rf observed **22.95%**, less Egypt's **own** Damodaran default spread **3.40%**, normalised rf* **19.55%** — country risk enters once. `ke_construction: "split_premium"`, `erp_mature: 0.04242`, `crp: 0.05168`, `lambda_country: 1.0`, and Ke reproduces: 0.1955 + 0.92752 × 0.04242 + 0.05168 = **0.286525**. **Both ERP bases published**: CDS 9.41% adopted, and the rating basis (Caa1, 6.37% spread, 13.94% total) computed consistently through to an explicit WACC of **29.46% against the adopted 27.96%** — page 9 tells the reader to read 29.46% wherever the study prints 27.96% if they prefer it. Market-value weights equity 96.22% / debt 3.78%. `python3 scripts/check_ke_reproduction.py` — ARCC among the 11 records read, no ARCC breach (its FAIL is FERTIGLOBE); terminal construction named `relevered`, tax rate stated at the statutory **22.50%** with the reasoning. `python3 scripts/check_cost_of_capital.py` → exit 0, "ARCC EG, 27.96% gliding to 16.74%". `check_macro_coherence.py` — ARCC coherent (its FAIL is ADIB). **The standing gap:** `kd_gate.bound_met = false` — the adopted marginal Kd of **13.36% sits below the normalised sovereign 19.55%**, because 91.1% of the book is euro. The record flags it, page 8 explains it and publishes the contracted alternative at 7.89%, and it is not described as conforming. |
| 24a | Table 6 does not foot to its own cost of equity | **FAIL — NEW** | Page 9's Table 6 prints, in order: Risk-free rate 22.95% · Less sovereign default spread (3.40%) · Normalised risk-free rate 19.55% · Beta 0.928 · **Equity risk premium 9.41%** · **Cost of equity 28.65%**. A reader doing the arithmetic the rows invite gets 0.1955 + 0.928 × 0.0941 = **28.28%**, not 28.65%. The 37-basis-point difference is the split-premium construction the model moved to on 10-09, and **it appears nowhere in the delivered document**: "mature premium" **0 hits**, "country premium" **0 hits**, "split" never in this sense, 4.24% and 5.17% **0 hits**. The two legs exist in the workbook — `build_xlsx_arcc.py:321/333`, Assumptions A43 `Equity risk premium, TOTAL (split into the two legs below)` = `=B44+B45`, A44 `Mature equity risk premium — beta applies to THIS leg and to nothing else` = 0.04242, A45 `Egypt country premium — charged FLAT, once, never multiplied by beta` = 0.05168 — and the reader's page shows only the total. The terminal column still foots (10.50% + 1.074 × 7.00% = 18.02%), which is what makes the explicit column's failure invisible at a glance. |
| 24b | Table 6's caption compares two different constructions | **FAIL — NEW** | The caption reads *"leaving it in would have put the cost of equity at **31.68%** instead of 28.65%."* `compute.py:3377` computes that figure as `ke_raw_retired = V['rf'] + beta_used * V['erp_cds']` = 0.2295 + 0.92752 × 0.0941 = **0.31678** — the **retired total-premium** construction. Under the construction actually adopted, not netting the spread gives 0.2295 + 0.92752 × 0.04242 + 0.05168 = **32.05%**. So netting the sovereign spread is worth **3.40 points**, exactly the spread, and the caption implies 3.03. Two numbers in one sentence, each on a different model. |
| 25 | Terminal value share visible to the reader | **PASS** | **59.7%**. In the summary table's own column on page 2, again in Table 10 in §1.7 on page 13, and discussed there against the two-thirds-to-four-fifths norm with the direction of travel named ("the prior revision put the terminal share at 45.5%"). In the workbook as a live cell (`recalc.py`: "Summary — terminal value share beside the DCF lens: workbook=0.5972 model=0.5972"). Assertion 20 checks it. `check_terminal_basis.py`, `check_terminal_floor.py`, `check_terminal_module_use.py`, `check_terminal_record_shape.py` all exit 0 with ARCC named and conforming. |
| 25a | The terminal's own committed reinvestment | **FAIL — on the ratchet, and it is a real number** | `python3 scripts/check_terminal_spread.py` → "known ARCC reinvestment is committed as **−191.8806**. A share of terminal profit reinvested lies in [0,1]; this is a unit or sign error rather than a position on growth." Ratcheted, so the gate returns OK, and the committed record still carries a reinvestment share two orders of magnitude outside its own domain. |
| 26 | Non-sourced drivers logged to the driver ledger | **PASS** | `engine/Fundamental_Driver_Ledger.md:570` — "ARCC — Arabian Cement · EGX · integrated cement (registered class: cement and heavy industrial) · re-struck 3 September 2026", recording at `:572` that the ledger had carried **no ARCC entry at all** against 23 House-ring drivers until that date. 29 inputs carry `ring: House` in the register and the House drivers are named in the bibliography's §5. |
| 27 | Rendered PDFs read as images, not as extracted text | **DONE — and it is what caught the most, again** | All 34 study pages and all 23 bibliography pages rendered at 100 dpi with `pdftoppm` and read as images, plus all 10 PNGs read directly. Rows 10a, 10b, 10c, 10d, 13a, 24a, 28 and 29 were found only this way: four figures two editions stale on pages 2, 15, 17 and 30; a clipped figure title; a retired construction as a bar label; a caption saying the opposite of its own figure; a table whose rows do not produce its own answer; a superlative claim about a judgement that is never priced; a caveat contradicting §1.2; and a live percentage under a stale word. **No programmatic check in this repository sees any of them.** Page 33 also carries roughly one-sixth of a page of content and five-sixths white. |
| 28 | Does the study contradict itself? | **FAIL — unchanged from 09-09** | Page 24 §7: *"The company discloses despatch volumes in its investor material, which would settle it; **that material could not be reached from here**, and the audited statements are image-only scans that carry no volume table."* Page 4 §1.2 of the same document: *"The company publishes sales volumes and production indicators in its FY2025 investor presentation — clinker production, cement production, local sales, cement exports and clinker exports — and **this build reproduces every one of them to within 0.02%**."* `ARCC_FY2025_Investor_Presentation.pdf` is in the study directory, the bibliography cites it by page, and assertion 23 reproduces all six disclosed tonnages to within 0.012%. A stale paragraph from an earlier edition still survives beside the correction that superseded it. |
| 29 | A live number under a stale word | **FAIL — NEW** | Page 24, `docx_arcc.py:1466-1471`: *"The central fair value moves from EGP 61.30 to EGP 77.18, and the conclusion moves from a premium over the market of that day to a **discount of +0.2%** against the latest close — **large enough that this edition carries the gap review its own standing threshold requires**, rather than being waved through as a small difference."* The number is computed live — `sg(LN["central"] / SPOT - 1)` — and is now **positive**: 77.18 against 77.00 is a **premium**, not a discount. And `GAP_REVIEW_10-09-2026.md` says the opposite of the second clause: *"Both are inside the ten per cent trigger on either side, so this name is **not referred**."* The word was written when the central sat below the price and the sentence was never re-read after it crossed. |
| 30 | The answer, not only the process [R-GAP-01] | **PASS on the trigger, FAIL on what the site publishes** | Committed central **EGP 77.1781** against the spot it was struck at, **EGP 77.00** on 2026-09-03 — **+0.23% above** — and **+0.76% above** the latest known close of 76.60 (2026-09-06). **The central is ABOVE the price, so the 10%-below trigger is not engaged** and no eight-heading review is owed. One exists anyway and it is current: `GAP_REVIEW_10-09-2026.md` audits **77.1781**, matching the committed central, and supersedes the 09-09 review that audited 69.2217. It deliberately does not carry the eight headings, because it is not a triggered review and says so in its second paragraph. `python3 scripts/check_valuation_gap.py` → **exit 0**, "OK — no new violations", "ARCC: struck at 77.00, latest known 76.60". `check_records_follow_the_answer.py` and `check_artefact_currency.py` both exit 0 — `contested_judgements.json`, `efg_bridge.json` and `scenario_margin.json` all built against 77.1781. **The failure:** `python3 scripts/check_published_gap.py --list` → **"ARCC −28.7% fair 54.65 spot 76.6 listed"**. `assets/data.js` publishes a fair value of **54.65** while this study publishes **77.18** — a 41% difference between the answer on the site and the answer in the document. On the ratchet, so the gate does not go red on it. |
| 30a | Publication routing | **PASS — routing, not a defect** | `python3 scripts/check_publish_block.py` → "HELD ARCC inside the band at +0.8% of 76.60 (2026-09-06), but Phase 1 is not proven — 2 of 6 acceptance criteria open". Held on the framework's own adoption conditions rather than on the traded-price gate, and stated as such. |
| 31 | Every deliverable ships as a PDF, current | **FAIL — see row 8b** | Study PDF 34 pages and bibliography PDF 23 pages both rendered 13-09 at 06:29:57/06:29:58, after the `.docx` files were written at 06:29:25/06:29:41, and both carry 77.18. `python3 scripts/check_delivered_pdf_currency.py` → exit 0 — but it reads only `*Valuation_Study*` and `*Valuation_Report*`. **The workbook PDF is three days and one rebuild stale and publishes a central of 77.49.** |
| 32 | No rating, no price target | **PASS, with one note** | The Disclosure page states "No rating and no price target is expressed or implied", and none appears. §1.10 reports a **third party's** rating and target (EFG Hermes, Buy, EGP 69.75, 6 Aug 2026) and reconciles it item by item to 77.18 in a bridge whose bars sum to the gap exactly (+7.42 vs +7.42, asserted by `efg_bridge.py`: "GATE (t) OK — 9 invariants, 8 steps, 0 failures"). That is evidence about someone else's model, not a recommendation, and it is labelled as such. |
| 33 | The workbook's READ FIRST states a count of itself, and the count is wrong | **FAIL — not reported by the 09-09 gate** | READ FIRST A60-A62: *"Anything else pasted would be a defect, and **the claim is AUDITED rather than asserted**. Of the **306** pasted cells: 136 are audited or disclosed history; 77 are genuine forecast drivers; 26 are the simulation ladder; 78 are whole-model re-runs. **That is all 306**, and ZERO are derivable-but-pasted."* The breakdown sums to **317**. An independent count of the delivered file gives **316** pasted numerics, which is also what `formula_count.json` and `build_xlsx_arcc.py`'s own print say. Three numbers for one fact — 306 claimed, 317 in the breakdown, 316 in the file — in the panel whose sentence is that the claim is audited. Typed at `build_xlsx_arcc.py:196-198`. |
| 34 | compute.py's own scrub runs one build behind | **PASS, with the ordering named** | The committed assert log says the scrub covered **225,271** characters; `scrub_result.json` and a live `scrub_gate.py --check` both say **225,282**. `compute.py` scrubs the delivered documents before those documents are rebuilt from the numbers it is writing, so its own scrub result always describes the previous build. `scrub_gate.py --check` run afterwards covers the shipped files and is clean, so nothing escapes — but the number inside the numbers file is never the number of the edition it belongs to. |

---

## What the self-audit never looked at: the filings against the model

The question the Sweep Register exists to force, answered rather than skipped.

**1. The H1-2026 interim was consumed a week after the research closed, and is still not
swept.** The reviewed half filed 13 August 2026 sets the FY2026 revenue calibration, the
channel price recalibration (local ×1.0788, export ×0.8711, cost ×0.9764), the services
share falling from 6.325% to 4.42%, the export-subsidy treatment and the 30-June-2026
balance sheet the entire bridge stands on. The sweep closed 6 August 2026 and the committed
register contains **zero** occurrences of "H1-2026" or "13 August 2026". The single most
consequential document in the base year has now gone two editions without being swept.

**2. Note 5's cost split is read for its totals and not for its dynamics.** The filing
discloses materials-and-fuel, transportation, overheads and depreciation as four separate
lines. The model reads all four, builds the per-tonne stack correctly from them — and then
escalates three of them at one national inflation ladder (row 21). The disclosure supports
per-class escalators; the model does not build them, and no near-term price anchor for any
of the three exists in the register.

**3. The filing discloses eight useful lives and the model consumes one — and now prices
two of them.** `useful_lives.json` records, from note 2 and note 3.2.1 of the FY2025 filing
(OCR off the rendered pixels, route recorded): buildings 10–20, machinery and equipment 20,
furniture and fixtures 16, vehicles 10, other installations 20, computers 3–5, plus an
operating licence at 20 and an electricity contract at 10, and separate schedules for
Andalus Ready-Mix and Evolve. The terminal collapses them to a single 20-year machinery
life. **This edition closes the 09-09 failure**: the choice is now published both ways, 20
against 50, worth EGP 17.36 a share. The full disclosed menu runs 3 to 50 years and the
published pair is its two ends, so the middle of the range is still unpriced.

**4. The company's own forward programme is disclosed and deliberately not consumed, and
that is stated.** Finding F19b of 2026-09-10 records six dated-by-nobody plans attributed to
the chief executive — hydrogen injection to ~55% thermal substitution by 2031, a multi-phase
alternative-fuel programme to 2030, 24 MWh of solar, waste-heat recovery, a mill optimiser,
a calcined-clay silo — and states plainly that the company's own channel carries none of
them and nothing is taken into the forecast. That is the right treatment and it is the one
item in this class the study handled well.

**5. Two new production licences ARE consumed — into a record nobody reads.** 3.5 Mt at the
midpoint, registered this edition, reaching no document and no cell and moving no number
(row 20f). The study's sector paragraphs still balance 76.0 Mt of nameplate against 65.1 Mt
of cement sold and call 10.9 Mt idle. On the study's own new input the forward number is
79.5 Mt.

**6. The three derived prices fail their own market test and the study says so.** Export
clinker derives to USD 30.0/t against a trade-press range of USD 44–48. Honestly published
on pages 5, 6 and 24 rather than tuned away.

---

## The twelve hard fails, in the order they would have to be cleared

1. **Four delivered figures are two editions stale**, rendered 8 September against a central
   of 66.53 and shipped inside documents that publish 77.18. Page 2's figure contradicts the
   table above it; page 15's heat map contradicts the table above it in all 25 cells and all
   5 axis labels and its title contradicts its own caption; page 17's bridge ends at 66.53
   where page 16's table ends at 77.18. (rows 10a)
2. **The delivered workbook PDF publishes a central of 77.49 and a cost of capital of
   27.60%** — a whole edition behind the .xlsx it names — and `check_delivered_pdf_currency.py`
   cannot see it because it filters on `Valuation_Study` in the basename. (rows 8b, 31)
3. **Table 6 does not foot to its own cost of equity.** 19.55% + 0.928 × 9.41% = 28.28%
   against the 28.65% printed beside it; the split-premium construction that explains the
   gap is in the workbook and nowhere on the reader's page. (row 24a) **And its caption
   compares the new construction against the retired one**, understating what netting the
   sovereign spread is worth by 0.37 points. (row 24b)
4. **The sweep register on disk is still not the register the invariants ran against.**
   27 findings serialised against 32 held; no COMPANY_IR finding, no primary-access log, no
   H1-2026 filing, a source-type vocabulary that is not the shared enum's, and a `qc_line`
   that says 26. `sweep.py:574` writes `FINDINGS`, not `R`. (rows 20, 20a, 20d)
5. **The bibliography carries no judgements-with-overturn table and no negative-results
   table** — 0 hits for "overturn", 0 for "looked for", 0 for "not found" — while the study's
   own About panel tells the reader both are in it. (row 6) **And its driver table tells a
   reader the terminal growth is 5% when the model runs 7%.** (row 6a)
6. **§7 says the investor material could not be reached** while §1.2 says it was read and
   reproduces every disclosed tonne to 0.02%, and the file is in the study directory. (row 28)
7. **A live percentage under a stale word**: "a discount of +0.2%" where +0.2% is a premium,
   followed by a claim that the gap review's threshold was breached when the review itself
   says it was not. (row 29)
8. **§1.5 attributes EGP 88.96 to "the withdrawn basket figure"** when the committed record
   attributes it to the EGX30 own-stock regression — two different betas, now ~EGP 5.4 apart,
   and the basket is a SIGCM hard fail that may not stand as the live alternative. (row 7a)
9. **Figure 6's title is clipped mid-sentence** at the canvas edge on page 21. **Figure 1
   labels the published central "Weighted central"** on a study that retires the blend in
   four places. **Figure 2's caption says the margin narrows** when it rises 39.03% → 40.40%.
   (rows 10b, 10c, 10d)
10. **One blended cost escalator across three disclosed cost classes**, against the standing
    rule and against [L-009], which was learned on this name. No sourced near-term cost
    anchor exists in the register. (row 21)
11. **The study now claims standard 2026.09.10 and does not meet it** — no asset-base record,
    `check_standard_claim.py` exit 1. The stamp changed under this audit at 07:29 today.
    (row 3)
12. **This edition's own new sector input reaches no document, no cell and no driver test**
    (row 20f), and **the workbook's READ FIRST states a pasted-cell count that is wrong three
    ways** — 306 claimed, 317 in its own breakdown, 316 in the file (row 33).

The H1-2026 sweep gap (row 20c), the two open coverage invariants (row 20e), the missing
committed ground-up record (row 5), the A.4 count row (row 15), the unpriced terminal
capital base (row 13a), the terminal reinvestment share of −191.88 (row 25a) and the site
publishing 54.65 against the study's 77.18 (row 30) sit below those and are named rather
than passed over.

---

## What was cleared since 09-09, and what still stands

Of the previous gate's ten hard fails, **three are cleared** and **seven stand**.

| 09-09 fail | Status at 10-09 | Evidence |
|---|---|---|
| 1. Bibliography masthead dated 6 August | **CLEARED** | Masthead reads "10 September 2026", derived at `docx_biblio.py:126-127` from `_ed.EDITION`. `check_edition_date.py` names ADIB and SCEM, not ARCC. |
| 2. Sweep register on disk ≠ register validated | **STANDS** | `sweep.py:574` still `findings=FINDINGS`; R holds 32, the file holds 27, COMPANY_IR 0, primary_access absent. |
| 3. Largest contested judgement published nowhere | **CLEARED** | Terminal useful life 20y vs 50y, 77.18 vs 94.54, in `contested_judgements.json`, Table 13, the body twice, and `Fundamental Valuation` A23/E23. |
| 4. No judgements-overturn and no negative-results table | **STANDS** | 0 hits for "overturn", "looked for", "not found", "negative result" across 1,689 paragraphs and cells. |
| 5. §7 contradicts §1.2 on the investor material | **STANDS** | Page 24 against page 4, verbatim. |
| 6. Figure 2's caption says the margin narrows | **STANDS** | `docx_arcc.py:446-447`; margin path 0.39028 → 0.40404. |
| 7. §1.5 attributes the alternative to the wrong beta | **STANDS, larger** | 88.96 is the 0.698 regression's value; the basket at 0.628 is worth ~94.4 on Table 8's own brackets. |
| 8. Figure 6 clipped · Figure 1 "Weighted central" | **BOTH STAND** | Page 21 title cut at "…rather than on th"; `figures.py:53,55,63`, visible on page 2. |
| 9. One blended escalator, no cost anchor | **STANDS** | `compute.py:1230-1232` all on `V['cost_infl'][i]`; industry ring holds intensities, not prices. |
| 10. `driver_test.py` crashes · bibliography `#` column | **BOTH CLEARED** | `driver_test.py` exit 0, 145 assertions, 0 dead inputs, result file current. `check_column_widths.py` names PHDC, not ARCC. |

---

## What is genuinely strong here, and should survive the repair

The model underneath remains the best-evidenced part of this study and none of the twelve
failures touches it. **The workbook reproduces cell for cell against an independent sandbox
rebuild — 2,089 populated cells, 0 differing — and a clean recalc on top of that: 928 of 928
formula cells, 57 headline reconciliations, 0 unparseable, 0 unchecked.** `study_numbers.json`,
`xlsx_expected.json` and `formula_count.json` all regenerate byte-identical from a clean
copy, and both documents rebuild with zero differing paragraphs or cells. The driver sweep
that crashed at 09-09 now runs to completion: **145 driver assertions, every one in the
asserted direction, 0 dead inputs**, and the defect it was hiding — a TOTAL equity premium
typed into a cell that nothing consumed — is now a live formula over its own legs. The
free-cash-flow waterfall foots by hand on all five years and runs inline to the present
value; the bridge closes exactly and charges the cash once. The physical build reproduces
every one of the six disclosed FY2025 tonnages to within 0.012% and the Egyptian sector
balance closes to 0.01Mt. Margins are outputs of a disclosed cost stack and the window is
shown not to manufacture margin expansion. The beta stands on the exchange's own published
index, fails its usability gate honestly at R² 4.7%, and falls to a peer median with the
failed diagnostics printed rather than tucked away. The cost of capital nets the sovereign
spread once, charges country risk flat and never multiplies it by beta, and publishes both
equity-risk-premium bases at full size. And the judgement the last gate said was measured
and never published is now published four ways, with its ranking computed rather than typed
so it cannot go stale again.

What fails is what a reader actually opens. **Four figures, one workbook PDF and one
cost-of-capital table did not follow the answer when it moved from 66.53 to 77.18**, and no
programmatic check in this repository looks at any of them. That is what makes reading the
rendered pages as images, and auditing from outside the study, gates rather than formalities.
