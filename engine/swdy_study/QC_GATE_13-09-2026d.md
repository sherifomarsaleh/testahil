# SWDY — QC gate, 13-09-2026 (fourth pass)

Elsewedy Electric Company S.A.E. · Egyptian Exchange: SWDY · EGP · **edition of 13-09-2026**

Audited from OUTSIDE the study per [R-ENF-02] by a reader who did not build it and did not
write the repairs. Every row names the artefact, the command or the number that carries it.
Nothing in the study, its builders, its numbers, its figures or its delivered documents was
touched; the only file this audit wrote is the one you are reading.

**VERDICT: NOT DELIVERABLE.**

Of the twenty-one hard fails raised on the first pass and re-tested on the second,
**all thirteen that the brief names as worked are genuinely closed on the delivered
13-09 artefacts.** Eleven of gate "c"'s own rows are also closed. **Nine of gate "c"'s
findings are still live**, two of them only partly repaired. **Twelve further defects were
found by this pass**, four of them material and three of those in the terminal — the line
carrying 89.4% of enterprise value.

The shortest list of what blocks delivery:

1. **A published sensitivity row is centred 13.3% above the study's own central.** Page 15's
   "Terminal return on invested capital | 15%–30% | 66.63 – 116.48" has, at the ADOPTED
   return of 22.43%, a value of **99.66** against a published central of **87.94** — because
   `dcf_roic()` re-implements the terminal on the construction [R-TERM-01] retired and
   charges no employees' statutory share. The caption two paragraphs above tells the reader
   that exact defect was fixed this edition.
2. **The §1.1 terminal block prints a cash flow the model does not use, and its own
   instruction does not reach its own answer.** "Terminal-year free cash flow 30,397 …
   capitalised at 15.3% − 9% = 493,872": 30,397/(0.15343513−0.0914) = **490,003**. The
   model's terminal cash flow is the sanctioned module's **28,072**.
3. **The retired terminal is not "published unused" — it builds the currency-of-discounting
   alternative**, which is this study's declared central contested judgement computed both
   ways (depth-bar standard 8) and is published at **EGP 79.09** in the summary table.
   `compute.py` logs that the retired construction "feeds nothing"; the delivered workbook
   says the same at `DCF!A68`.
4. **The volatility caveat contradicts itself and understates itself by nine points.**
   Page 17: "Realised volatility over the last 50 sessions is 49.4%; strip out that one
   session and it falls to **49.4%**." The true figure excluding the 4-August session is
   **40.6%**.
5. **A typed percentage contradicts the driver table two pages earlier.** Page 20:
   "Constructions and infrastructure revenue growth (**18%** in FY2026E)"; page 10 and
   `inputs.construct_growth[0]` both say **27.4%**.
6. **§3 explains a calibration difference with a cause that is not the cause.** 91% against
   94.7% are both measured over **57 windows**; the page tells the reader to "check which
   window it is on".
7. **Four internal-procedure terms reach an external reader**, and the study's own
   numeric-traceability scan records 22 typed numerals and passes on all of them.
8. **§7 still deducts the raw EGP 1.85 dividend** and prints a gross accretion of +13.70
   against a true 13.8168 — the same defect the §1.1 bridge was repaired for.

---

## The edition audited, and how it was resolved

`edition.py` holds `EDITION = date(2026, 9, 13)`; all six delivered filenames derive from it.
**All 31 artefacts in `build_manifest.json` hash-match the files on disk** (SHA-256, 0
problems). The audited commit is `ad4cf7280` on branch `claude/adnocls-h1-2026-restrike`. HEAD advanced
once during this audit, to `052e874d1` (an ADIB contested-judgement record), and
`git diff --stat ad4cf7280 HEAD -- engine/swdy_study/ assets/data.js engine/prices
engine/raw_ohlc/EG/SWDY.csv engine/Fundamental_Driver_Ledger.md` is **empty** — every artefact
this gate reads is byte-identical at both commits, so no finding below turns on which one is
used. `git status --porcelain engine/swdy_study/` shows only this file.

| Deliverable | File | Measured |
|---|---|---|
| Study, Word | `SWDY_Valuation_Study_13-09-2026_public.docx` | 1,645 text blocks, **40 tables** |
| Study, PDF | `SWDY_Valuation_Study_13-09-2026_public.pdf` | **30 pages, 8 figures — every page rendered at 115 dpi and read as an image** |
| Workbook | `SWDY_Valuation_Model_13092026_public.xlsx` | 16 sheets in the model order, **630 formulas, 1,564 populated cells** |
| Workbook, PDF | `SWDY_Valuation_Model_13092026_public.pdf` | 52 pages |
| Bibliography | `SWDY_Bibliography_13-09-2026.docx` / `.pdf` | 21 pages, 13 tables, **222-row printed register** |
| Gap review | `GAP_REVIEW_13-09-2026.md` | 195 lines, all eight headings present and in order |
| Sweep register | `sweep_register.json` + `sweep.py` | 32 findings, 12 driver rows, sweep date **2026-09-10** |
| Attestation | `attest.py` + `attestation.json` | the three gates + `assert_ground_up()`, stamped `2026.09.10` |
| Rebuild ledger | `rebuild_ledger.json` | 7 levers, 6 rules, cumulative **+58.5%** |

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker and exchange | SWDY · EGX · market code EG | `assets/data.js:642` `code: "EGX:SWDY"` — read from the `code` prefix, never from `raw_ohlc/EG/`. |
| Regressor the beta rule demands | **EGX30**, `engine/raw_indices/EG/EGX30.csv` | `wacc_builder.EXCHANGE_INDEX[('EG','EGX')]` → `EGX30`, registered and present. Not DFM and not QSE, so no interim note and no weekly-only caveat apply. |
| Class and reference pattern | Diversified industrial with a contracting arm; nearest pattern **ADNOCLS** (operating company) inside ADNOCLS's skeleton | `lessons_register.CLASSES`; `REFERENCE_SET == ('ADNOCLS','ADCB','ALPHADHABI')` — SWDY itself was displaced 19-Aug-2026 and is not a template. `LENS_REGISTRY['diversified industrial with a contracting arm'] = ('dcf', ('ev_ebitda_own_history','relative_multiple','sotp','book_value'))`. |
| Committed central | **EGP 87.94253027526973** | `study_numbers.json.central`; `lens_record` primary kind `dcf`. |
| Committed envelope | **28.5669 – 138.1751** | `study_numbers.json.span`. |
| Committed spot | **EGP 130.00, close of 2026-09-03** | `meta.spot` / `inputs.spot.date`; `gap_today.latest_price_per_ticker()['SWDY']` → `{price 130.0, date 2026-09-03, file SUPPLIED_03-09-2026.json}`. |
| Gap on the struck spot | **−32.35%** | 87.94253 / 130.00 − 1. |
| Gap on the latest known price | **−35.43%** | `python3 scripts/check_valuation_gap.py` → *"SWDY −35.4% … struck at 130.00, latest known 136.20 (2026-09-06)"*. |
| Live standard | **2026.09.10** | `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"`; the study stamps `2026.09.10`. |
| Lessons read first | 266, of which 4 bind on this class | `python3 engine/lessons.py SWDY --class "diversified industrial with a contracting arm"` — L-294 (the EPS reconciliation this name produced), L-295 (legs on different contract structures), L-296 (normalised earnings is not a lens for this class), plus L-371/372/373 from this name's own walk-forward. |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates called in the study's own committed code [R-ENF-02] | **PASS** | Grep over all 31 `.py` files in the directory: `assert_model_study` → `attest.py:113`; `assert_sigcm` → `attest.py:163`; `assert_beta_provenance` → `attest.py:166` and `beta_reg.py:84`; `assert_ground_up` → `attest.py:218`; `assert_bridge` → `bridge_record.py:222`. Entry point run: `python3 attest.py` → *"assert_model_study PASSED … assert_sigcm PASSED … assert_beta_provenance PASSED — 1.2249 vs EGX30.csv (conforming=True) … ground-up record: 3 lines, 55.4% of revenue at unit level"*, exit 0. `build_all.py` declares 21 steps in dependency order with `qc_checks.py`, `gap_review.py` and `attest.py` last. From outside: `python3 scripts/check_study_provenance.py` → *"OK — no new violations"*, 25 study directories, SWDY not on `outstanding.json`. |
| 2 | Beta inspected on its record, never on a boolean — SIGCM clause 6, [R-BETA-04] | **PASS** | `beta_result.json`: `index_file "raw_indices/EG/EGX30.csv"` — under `engine/raw_indices/`, registered for `('EG','EGX')`, the exchange the `code` prefix names. `beta 1.2249065740479688`, `r2 0.3682`, `se 0.16986`, `n 256`, `window_years 4.94`, `week_rule "W-THU"`, `dimson true`, `index_asof 2026-09-08`, `conforming true`, `interim_note null`. Usability gate: n 256 ≥ 24 ✓, R² 36.8% ≥ 5% ✓, SE 0.1699 < \|β\| 1.2249 ✓ — no fall to a peer beta or 1.0 is owed, and none is taken. The withdrawn 31-name equal-weight composite (1.0087 at R² 0.291) is committed as a labelled cross-check with `why_withdrawn` naming it a hard fail rather than a tier. |
| 3 | Step 0.0 data-quality gate | **PASS** | `step0_result.json`: **3,755 clean rows**, `dq_log ["dropped 1 interior stale/no-trade rows"]`, screened against the EGX calendar and the ±20% EGX daily limit. Run on BOTH series inside `own_stock_beta()` — `beta_result.json` carries `stock_dq` and `index_dq` separately. |
| 4 | Ground-up clause on a driver record, not a flag [R-SIGCM-02] | **PASS** | Three `DriverLine`s covering 100% of revenue. Share by level: **unit 55.43% · derived 0 · segment 44.57% · topdown 0**. Cables names the unit (tonnes of cable shipped), its source (the issuer's own quarterly releases — 144,997 / 156,748 / 167,665 / 185,449 and 99,239 against 89,636) and the price basis (the LME copper forward path × the house EGP/USD path as a measured pass-through). Both segment lines carry gap notes stating why no unit exists in any filing. `python3 scripts/check_ground_up.py` → *"OK — every committed driver-line record passes the ground-up assertion when it is actually run."* |
| 5 | Step 0 calibration under the band record [R-CAL-02] | **FAIL — the count is right and the explanation is wrong** | §3 publishes the count beside every percentage (*"42% inside the 50% band, 79% inside the 80%, 84% inside the 90%, over those 19 windows"*), a two-sided exact binomial on each band (p = 0.65, 1.00, 0.43), a uniformity test (p = 0.86) and the width ratio disclosed and explicitly not gated (*"about 21% wider than a naive carry-anchored band … a real limitation"*). `backtest_5y.json.fit = {nu 5.0, width_cal 0.951}` = the live EG profile. **The defect is the new full-history paragraph (N6 below):** *"over all 57 windows the 90% band caught 91% … a reader comparing this page against a coverage figure quoted elsewhere for this name should check which window it is on"*. `backtest_5y.json.full` = 57 windows, `cov90 0.9122`. `assets/data.js:3309` = `SWDY: {n:57, hits:54, c90:0.9474}`. **Both are 57 windows.** The page directs the reader to an explanation that cannot be the explanation. |
| 6 | SIGCM's eight clauses | **PASS, one clause declared N/A with its reason** | `attest.py` sets each field from evidence: `historicals_official_only True`, `forecast_ground_up True`, `debt_lc_fx_split True` (28% pound / 72% hard currency, back-solved from the FY2025 interest-rate note and labelled inferred), `asset_conversion_cycle True`, **`competitors False` with an `na_reasons` entry** (no listed comparable for this mix; an earlier draft's unsourced *"peers trade at 8-11x"* was withdrawn rather than sourced — the withdrawal is printed on page 7), `beta_own_history_vs_egx30 True`, `formula_based_model True` (630 formulas), `flags_raised_before_issue True`, `stop_and_inform_honoured True` (8 dated negative searches). A peer FRAME is published at B.1 with no peer multiple. |
| 7 | **Numeric traceability — every builder reads the committed numbers file** | **FAIL** | `qc_checks.py` prints *"2 builder(s), 22 typed numeral(s)"* and then *"ALL AUTOMATED QC CHECKS PASSED"* — it records and never refuses, which is declaration without execution. Its regex requires a decimal point, so bare percentages and thousands-separated integers cannot match, and it does not scan `build_xlsx_swdy.py`. **Two of the figures it cannot see are wrong or inconsistent:** `docx_swdy.py:1274` types **`18%`** for Constructions FY2026E growth against a committed `construct_growth[0] = 0.2744` (N5); `docx_swdy.py:1278` types **`22.8%`** for the FY2025 payout twice while page 3 computes `dps_payout_fy25` = 22.853% → **22.9%** (N10). |
| 8 | Independent recalculation of the delivered workbook | **PASS** | Delivered file converted with `soffice --headless --convert-to xlsx` and read `data_only` by this audit, independently of the study's own evaluator: **630 formula cells, 0 error values, 0 unparseable, 0 None-valued**; sheet names and order identical to `MODEL_STUDY['excel_sheets']`. The study's own `recalc.py`, run in a clean `git archive HEAD` sandbox: *"RECALC OK — 630 formulas, 0 unresolvable, 630 cell-level agreements with the model, 41 headline reconciliations passed"*. `python3 scripts/check_workbook_values.py` lists SWDY CLEAN. |
| 9 | Script-built files verified by a cell-by-cell diff against the delivered file | **PASS** | `build_xlsx_swdy.py` re-run inside the sandbox and every cell of all 16 sheets compared with the delivered workbook: **1,564 populated cells compared, 0 differences**, sheet order identical. Both documents rebuilt the same way and compared block by block: study **1,645 blocks, 0 text differences**; bibliography **1,078 blocks, 0 differences**. `python3 scripts/check_workbook_structure.py` lists SWDY as matching the model report's 16 sheets. |
| 10 | **Read the rendered PDF, as images** | **FAIL — six defects no programmatic check saw** | All 30 pages rendered at 115 dpi and read as images. Found N1 (the volatility sentence), N2 (the terminal block), N4 (the sensitivity row, confirmed against the committed grid), N5 (18% against 27.4%), N6 (§3's explanation) and N9 ("the latest known close"). Every one of them passed `prose_check.py` (*679 figures, 0 unmatched*), `footing_check.py` (*47 tables, 0 unreconciled*) and `qc_checks.py` (*0 scrub hits*) — because each figure is individually computable from the committed numbers and the defect lives in the relationship between figures, or in a word. |
| 11 | External-reader scrub — depth-bar standard 4 | **FAIL** | `qc_checks.py` → *"study: 0 hits · bibliography: 0 hits"* against its own 43 patterns, which is true of that list and the list is short. Re-counted independently over every paragraph and table cell of both delivered documents: the **study** carries **"walk-forward" ×2** (§3 *"Tested by walk-forward simulation over nearly five years"*; About *"tested by walk-forward simulation against a random-walk benchmark"*) and **"persona" ×1** (Appendix C intro, *"with different persona choices"*); the **bibliography** carries **"SIGCM" ×2** and **"depth bar" ×2**. The study's own `BANNED` list contains `persona library` but not `persona`, and contains neither `walk-forward`, `SIGCM` nor `depth bar`. |
| 12 | Verdict tokens [R-CAL-02] | **PASS** | `cd engine && python3 -c "import band_record as br; br.assert_no_verdict_tokens('EG')"` → no raise. No PASS/PARITY/FAIL beside the company name in either delivered document (the only matches are *pass-through*, *interest-rate parity*, *fails to stabilise*). `CRPS` appears nowhere. Calibration evidence lives in §3 as plain-language sentences with the statistics inline; the document has exactly three appendices — A, B, C — and no calibration appendix. |
| 13 | Standing repository gates, run from outside | **PASS** | Twenty-eight gates run over the whole book at `ad4cf7280`. Three are red and **none of them names SWDY**: `check_artefact_currency` (ADIB), `check_rebuild_ledger` (ADIB), `check_macro_anchor_age` (ADIB). `check_delivered_vocabulary` → *"CLEAN (23): … SWDY …"* — the repository path gate "c" caught in the bibliography is gone. SWDY is on four ratchets and every one is reported in its own row below. |
| 14 | Figure discipline — depth-bar standard 5 | **PASS** | Eight figures, each read as a rendered image inside the delivered PDF (pages 8, 11, 15, 16, 18 ×2, 19, 29). Transparency verified programmatically on the source PNGs (`alpha extrema (255,255)` on all eight, corner `(251,249,244)`) **and on the delivered PDF** (8 distinct embedded images, minimum alpha 255, **0 with a transparent pixel**). `python3 scripts/check_figure_opacity.py` → *"OK — every delivered figure is opaque to the pixel."* `python3 scripts/check_figure_axes.py` → *"OK — no new violations."* **One label defect, carried — see row 22.** |
| 15 | Table discipline — depth-bar standard 6 | **PASS, with one gap named** | Independent audit over every table in both documents against each section's own text width: **study 40 tables — starved 0, bloated 0, over-wide 0**; **bibliography 13 tables — starved 0, bloated 0, over-wide 0**. Every table carries explicit `w:gridCol` widths. `python3 scripts/check_table_footing.py` → *"SWDY tables examined: 47; unreconciled totals: 0"*. **Gap:** 3 of the 53 (2 in the study, 1 in the bibliography) carry no `w:tblLayout type="fixed"` and rely on the grid alone. |
| 16 | Expert appendix at maximum detail — standard 7 | **FAIL on one claim and one table** | Structure is complete: each of the three carries worldview, when-it-works / when-it-fails, a worked table, a named sensitivity with numbers (*"each one-turn change in the multiple is worth EGP 15.87 per share"*), and a falsifier stated in advance; C.4 cross-examination with each challenge conceded or rejected, C.5 the three in one room, C.6 a divergence table. Labels are "Expert 1/2/3" — no persona NAME appears anywhere. **FAIL:** page 27 states *"This is the harshest of the three readings, at EGP 97.72"* while `experts.e3.base = 89.43` and C.5 on the next page says *"The panel spans EGP 89.43 to 176.13"*. **FAIL:** Expert 1's worked table (page 26) runs EBIT 49,293 − net interest 1,056 → *"After tax and minority interests — earnings per share 15.87"*; recomputed from the printed lines at `tax_eff 0.245` and `nci_share 0.09675` the answer is **15.3661**. The missing line is the FY2026E share of equity-accounted investees, `assoc_fy25 1,568.90`, worth **0.4998 per share** — printed in §1.4's own table and not in this one. |
| 17 | **The central contested judgement computed both ways — standard 8** | **FAIL — the two framings are not on the same terminal** | The currency of discounting is published side by side: EGP **87.94** primary against EGP **79.09** on the hard-currency leg, in the summary table (page 2), the body (§1.8, §4, §7), the workbook (`Fundamental Valuation`) and Expert 3's upper bound — never averaged, and two further alternatives priced beside it (rating basis 33.67, minority sequencing 99.33). **The defect is how 79.09 is built.** `compute.py:2277` and `:2281` compute both terminals of that alternative as `nopat_term * (1 - rr_term) / (w - g)` with `rr_term = g / roic_term` — the reinvestment-identity terminal [R-TERM-01] retired, while the primary uses `terminal_value.build()`. The same construction also drives `dcf_roic()` (row 25). `compute.py:2094` logs of it: *"[Terminal value, the RETIRED construction, published unused] … Recorded so the change is visible, and it feeds nothing."* It feeds two published numbers. |
| 18 | Standalone bibliography document — standard 1 | **PASS** | `SWDY_Bibliography_13-09-2026.docx` / `.pdf`, 21 pages: a primary-documents table of 10 documents — **now including the reviewed condensed interim statements for the six months ended 30 June 2026, approved 11 August 2026**, which gate "c" found missing; the FULL input register of **222 rows** across eight layer tables, each with value / date / source-and-construction; a judgements table of 11 rows each with what would overturn it; a negative-results table of 4; and aggregator-discrepancy notes. **The `anchor_days` justification is fixed:** it now reads *"Days from the DCF's construction date (31 Dec 2025 …) to the anchor date of 3 Sep 2026"* — 246 days from 31-Dec-2025 IS 3 September 2026. |
| 19 | Four-field provenance on every input — standard 2 | **PASS** | **222 inputs**, every one carrying value / source / date / ring, asserted in `attest.py` (*"222 inputs, four-field complete=True"*) and printed in full in the bibliography's register, where all 222 committed keys resolve to a printed label. `python3 scripts/check_provenance_fields.py` reports no new violation. |
| 20 | Sweep Register primary-source depth | **PASS, with two gaps named** | `python3 scripts/check_sweep_module.py` → *"RUNS THROUGH THE SHARED REGISTER (18): … SWDY …"* — it imports `research_sweep` and calls `validate()`. Result: *"PASS — 4/4 rings, 32 findings (2 B · 5 S · 15 D · 2 C · 8 NEG), driver gate 12 rows (8 bottom-up / 4 top-down) \| primary access: attempted (1) \| FS years: 3 \| IR findings: 7 \| warnings: 1"*. Company site **attempted and logged either way**: `primary_access[0]` records `elsewedyelectric.com`, `reachable: true`, `attempt_date 2026-08-14`, with the earlier contrary assertion explicitly withdrawn. Both study-year periods disclosed (`Q1-2026`, `H1-2026`) are swept; results releases are swept with their statements; **7 findings tagged `COMPANY_IR`** distinctly from 10 `COMPANY_OFFICIAL`. **The [R-NEWS-01] false negative is closed:** F27 carries the three plants announced 24 June 2026 and F28 the Datagrid / Transpower 360 MVA transformer order, both `entity: "Elsewedy Electric Company S.A.E."`, `entity_is_issuer: true`; F29's negative search is re-scoped to *"a capacity addition … NOT already inside the capital-expenditure path"*, which is a true negative. **Gaps:** 3 audited fiscal years against a target of 4, warned (brief names this as open); and `sweep_date` is **2026-09-10**, so the register was not re-run for this edition. |
| 21 | One escalator per driver class, with a sourced near-term cost anchor | **PASS, with one gap named** | Copper on its own LME path (`copper_fcst` 13,400 → 15,453 USD/t, FY2026 set between the realised Q1-2026 average and the forward), the pound on the house relative-purchasing-power ladder derived from `macro_path.load("EG")`, domestic lines on the house CPI, the corporate cost load on its own reversion, capex on its own taper from the FY2025 peak of 4.7% — no blended index across physically distinct lines. **Gap:** the Cables COST side is not escalated at all; it is a flat margin — next row. |
| 22 | **Margins as OUTPUTS wherever the filings disclose enough to build cost per unit** | **FAIL** | §1.1 states *"Margins are therefore OUTPUTS of the build rather than assumptions fed into it"*, §1.6 states *"Margins are outputs of the build in all three, not inputs to it"*, and page 10's table is headed *"What the build produces — margins as outputs"*. Only the GROUP margin is an output, as a mix of three INPUTS: `inputs.cables_margin = [0.114341] × 5`, `construct_margin = [0.089871] × 5`, `elecprod_margin = [0.236221] × 5` — each flat for five years and each printed as a driver row on the page immediately above. The sensitivity table on page 15 shocks them as one multiplicative driver (*"Segment margins (all three, multiplicative) −15% to +15%"*, swing EGP 74.00 — the largest in the study), which an output could not be. For Cables — **55.4% of revenue** — the tonnage is disclosed and the copper and currency paths are already registered, so a conversion cost per tonne is reachable and the margin could be the residual. |
| 23 | 3y historical + 5y forecast with the full FCFF waterfall shown inline to PV of FCFF | **PASS** | Page 4 runs the whole waterfall inline: Revenue → EBITDA → less D&A → EBIT → NOPAT at 24.5% → add back D&A → less capex → less ΔWC → **FCFF** → forward cost of capital → discount factor → **present value of FCFF**, five years. Checked by hand on FY2026E: 38,286 − 3,961 = 34,325; ×0.755 = 25,916; +3,961 − 13,232 − 16,965 = **−319**; ×0.7896 = **−252** ✓. FY2027E: 47,221 − 4,807 = 42,414; ×0.755 = 32,022; +4,807 − 16,056 − 15,546 = **5,227**; ×0.6425 = **3,359** ✓. Appendix A.1 carries FY2023-25 audited and FY2026E-30E forecast; A.2 the balance sheet; A.3 the forecast balance sheet and cash-flow markers. |
| 24 | The valuation on one page [R-DCF-01] | **PASS** | Page 5: sum of present values **23,195** + PV of the terminal **195,998** = enterprise value **219,193**; less net debt 20,560, plus associates 6,758, less minorities 19,872, less the employees' statutory share 22,620 = equity value **162,899**; ÷ 2,141 = **76.09**; rolled = **87.94**. Every line reproduces. `engine/dcf_table.py` raises if the present values do not reach the published enterprise value. |
| 25 | **Sensitivity grids centred on the adopted case [R-SENS-01]** | **FAIL on one published row** | The two 5×5 GRIDS are exactly centred: `sens.grid_wacc_g[2][2]` = `sens.grid_exp_term[2][2]` = **87.94253027526975**, identical to `central` to the last digit, and page 15's caption says so. **But the one-way sensitivity table on the same page carries six rows and one of them is not:** `sens.grid_roic` at the ADOPTED `roic_term = 0.22433` returns **99.6619**, **+13.33%** against the published central of 87.94253. Cause, read off `compute.py:2601-2606`: `dcf_roic()` re-implements the terminal as `nopat[-1]*(1+g)*(1 − g/ROIC)/(w−g)` — the retired construction — and its equity step is `(ev − nd + assoc) × (1 − nci_share)` with **no `(1 − emp_rate)` term**. 99.6619 × (1 − 0.12193) = **87.51**, so the omitted employees' statutory share is essentially the whole error. The row is published on page 15 (*"Terminal return on invested capital \| 15% – 30% \| 66.63 – 116.48 \| 49.85"*) and again in B.2's risk register. Page 15's own caption says this defect was fixed this edition: *"Until this edition the grids ran through a second function that re-implemented the terminal on a construction the study had already retired and omitted the employees' statutory share of profit that the bridge charges"*. Two further rows are off their adopted point for a benign reason and are not failures: beta (axis `[0.6, 0.8, 1.15, 1.225, 1.3]`, a sorted LIST with the adopted value in its place, centre 88.45) and working capital (axis middle 0.199 against an adopted 0.1967, centre 87.10). |
| 26 | v2 WACC — country risk once, marginal Kd, market-value weights, both ERP bases | **PASS, with one gap named** | `cost_of_capital_record`: `rf_observed 0.2231` less `default_spread 0.034` on the SAME (CDS) basis = `rf_star 0.1891`; the premium re-enters through the CRP, so the sovereign is charged once. Ke reproduces exactly under [R-COC-03]: 0.1891 + 1.224907 × 0.04242 + 0.5928 × 0.05168 + 0.4072 × 0.0226 = **0.280899** = `ke_exp`. `ke_terminal 0.168681` under the declared construction `beta_to_one_split`. `python3 scripts/check_ke_reproduction.py` → *"OK — no new breach"* across 11 records. `weight_equity 0.9312` on market capitalisation of 278,301. Kd 9.5% adopted against an independently computed FY2025 effective of **9.84%** (interest 5,967 over the average of 58,797 and 62,462) — inside the 150bp bound at 34bp and under the 50bp peak test — with the currency composition established FIRST from the FY2025 interest-rate note (21.3% pound, 5.3% blended hard currency, back-solving to ~28%/72%) per [R-COC-01 AMENDED 08-09]. Both ERP bases published: CDS 9.41% primary and rating 13.9% as an alternative worth EGP 33.67. `python3 scripts/check_cost_of_capital.py` → *"NOW PASSING — remove from the outstanding list (1): SWDY"*. **Gap:** the §1.8 table publishes only the ERP TOTAL (9.4% / 7.0%), so a reader computing `rf* + beta × ERP` reaches **30.4%** against a printed **28.1%**; the split is stated in §4 and §7 prose but not in the table where a reader reproduces it. |
| 27 | **Every non-sourced driver logged to the Driver Ledger** | **FAIL** | `engine/Fundamental_Driver_Ledger.md:1000` now carries a SWDY entry, which is new since gate "c" — and it does not answer this item. **(a) It is stale:** it records *"four levers, three rules, cumulative −5.02%"* against a current `rebuild_ledger.json` of **7 levers, 6 rules, +58.5%**, and *"the terminal carrying 84% of enterprise value"* against a published **89.4%**. **(b) It logs the walk-forward's drivers, not the study's own.** The study registers **28 House-ring inputs**, every one a judgement and none logged: `ev_ebitda_just 6.5`, `pe_just 9.0`, `roe_sust 0.235`, `g_term_real 0.02`, `erp_term 0.07`, `kd_term 0.0888`, `wd_term 0.15`, `usd_erp 0.075`, `usd_kd 0.065`, `usd_wd 0.25`, `usd_g_term 0.035`, the three flat segment margins and the three growth tapers. The GBCO section immediately below in the same file names that exact distinction in its own words — *"The GBCO section of this ledger logs the WALK-FORWARD's drivers … It logs none of the study's OWN forward drivers, which is a different requirement and was not met"* — and closes it for GBCO. SWDY's was not. |
| 28 | The walk-forward scope decision stated in the study [R-FCAL-01] | **FAIL, with the mitigation named** | `python3 scripts/check_walkforward_scope.py` → *"NO DECISION (15): … SWDY …"*; the word `scope` appears 0 times in `study_numbers.json`. §7 states the run in substance (*"rebuilt at twelve past year-ends of Elsewedy Electric's own history … 750 driver-years in all"*) and publishes years 3-5 as ranges with the basis and count per cell (*"percentile (9)"*, *"span (8)"*, *"span (7)"*) and the orientation declared (*"a low of 0.55 and a high of 2.60 means the outturn has landed between 0.55 and 2.60 times what this method projected"*), which [R-FCAL-01 AMENDED 07-09] asks for; `python3 scripts/check_forward_ranges.py` → *"SWDY prints a far-year range shape A"*. **Mitigation:** the ruling itself IS made and IS on the record — the Driver Ledger entry states *"FULL scope: seventeen sourceable fiscal years FY2009–FY2025 … twelve origins FY2014–FY2025, horizons 1–5"*. It is simply not in the study and not committed where the gate reads. |
| 29 | **The answer, not only the process [R-GAP-01]** | **PASS on the artefact, with one gap named — and the study is HELD** | Committed central **87.94253** against a committed spot of **130.00** (2026-09-03) = **−32.35%**; against the latest known **136.20** (2026-09-06) = **−35.43%**. More than 10% below, so a review is owed: `GAP_REVIEW_13-09-2026.md` exists, is GENERATED from `study_numbers.json` by `gap_review.py`, states `AUDITED CENTRAL: 87.9425` = the published central to the last digit, and covers all eight headings in order — LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK. `python3 scripts/check_valuation_gap.py` → *"OK — no new violations"*. The study is stamped `2026.09.10` = the live `research_protocol.STANDARD_VERSION`, which names this artefact. [R-GAP-04]'s hunt is recorded and priced (five candidates on full re-runs: margins +113.47, cost of capital +52.56, terminal growth +30.26, copper +5.85, the pound +2.34); [R-STAR-01] resolves through `northern_star.from_numbers('SWDY', …)` → `tier "priced"`, `verdict "PASS"`, `missing []`. **Gap:** the review states no `AUDITED GAP` line, which [R-GAP-01 AMENDED 03-Sep] asks for and the gate tolerates by returning `None`; it states −32.4% in prose, 3.0pp from the current −35.4% and inside the 5-point tolerance either way. **Standing consequence:** `python3 scripts/check_publish_block.py` → *"HELD SWDY — the central is −35.4% below the price of 136.20 (2026-09-06), past the 10% publication limit — and no market dissent is filed"*; no `MARKET_DISSENT_*.md` and no `PUBLISH_AUTHORISATION_*.md` exist. |
| 30 | Bridge on the latest disclosed balance sheet [R-BRIDGE-01] | **FAIL, ratcheted — and honestly recorded** | `python3 scripts/check_bridge.py` → *"BRIDGE FAIL — SWDY: the bridge stands on the 2025-12-31 balance sheet while the latest disclosed is **2026-06-30**"*. The bibliography's own primary-documents table now lists that June filing, and the register cites it for revenue, operating profit, segment revenue and profit, net financial debt, cash, borrowings, investees and the minority's share. `bridge_record.balance_sheet_reason` states the position without dressing it up: *"THE RULE DOES NOT ADMIT THAT ARGUMENT and the record does not pretend otherwise: the construction is committed as it is, this study stays on the bridge ratchet, and the re-strike onto the June sheet — which moves the explicit window's start with it — is a rebuild rather than a patch."* **The bridge itself foots.** Printed steps: 162,899 → 76.09/share → ×1.1816 → less (1.97/sh) → **87.94**. Recomputed: 76.09346801 × 1.18157726 = 89.91031; − 1.9677813 = **87.94253** = the published central. `div_at_anchor` is the 1.85 dividend carried the 91 days from its 4-June ex-date at the 28.09% cost of equity: 1.85 × 1.2809^(91/365) = **1.96778** ✓. Gate "c"'s N9 is closed **in this table** — and not in §7, see row 34. |
| 31 | Forecast anchor [R-ANCHOR-01] | **FAIL, ratcheted — the record is exemplary** | `python3 scripts/check_forecast_anchor.py` → *"ANCHOR FAIL — SWDY: the forecast opens at 0.1234 against a latest reviewed 0.1400 — 1.65 points below it, 11.8% RELATIVE — and names no mechanism"*. The record does not hide behind the flattering comparison and says so: *"Against the audited FY2025 year of 12.2719% the forecast opens only −0.57% low and nothing would fire — the stale full-year rate is the flattering comparison, which is why it is not the one this record is struck on"*, and it names the seasonality instead (H1 FY2025 14.1369% against H2 10.8036%, the forecast carrying 0.7885x against a filed 0.7642x). `forecast_path` RISES across the window (12.34 → 12.72%), so clause two does not fire. Still a breach on the closed mechanism list. |
| 32 | Macro coherence [R-MACRO-01] | **FAIL, ratcheted — the one the brief names as deliberately open** | `python3 scripts/check_macro_coherence.py` → *"MACRO COHERENCE FAIL — SWDY (EG): the explicit window ends with growth at 11.37% against a terminal of 9.14% — 2.2pp apart"*, against a 2pp bound. Stated in the gap review's own MACRO COHERENCE and TERMINAL headings as structural and left for the principal. Everything else in the record conforms: `inflation_inputs` declares one key, `unit_price_inflation`, exempted as retired and read by no formula (verified — no formula consumes it); the currency path is `macro_path.load("EG").fx_path(5, base=47.69)` off the company's own realised FY2025 average rate; `terminal.rf 0.105` = 7.0% inflation + a 3.5% real convention; `g_nominal 0.0914` = the exact Fisher product of 7.0% and a stated 2.0% real [R-MACRO-02]. `anchor_staleness_accepted` is declared with a reason (struck 28 days after the EG currency anchor against a 14-day bound), so `python3 scripts/check_macro_anchor_age.py` does not list SWDY. |
| 33 | **Terminal built through the sanctioned module [R-TERM-01]** | **PASS in the model, FAIL on the page and in two published numbers** | The MODEL is right: `dcf.terminal_record` reproduces the published terminal to 1e-6 — FCFF 28,071.64 × 1.0914 / (0.15343513 − 0.0914) = **493,871.53** = `dcf.tv` — on maintenance at current cost over a **17.2627-year** life DERIVED by identity from note 17 and corroborated twice, and `python3 scripts/check_terminal_floor.py` passes SWDY (*"cycle 11.1 y sits on 1/g of 10.9 BY COINCIDENCE"*). The delivered workbook says the same in terms at `DCF!A20` (*"the LAST EXPLICIT year (FY2030), NOT grown"*), `DCF!A64` (*"MAINTENANCE AT CURRENT COST, NEVER g x IC"*) and `DCF!A68` (the terminal return on capital *"drives nothing here"*). **THE DOCUMENT SAYS OTHERWISE.** Page 6 prints *"Terminal-year free cash flow \| **30,397** \| FY2030E NOPAT grown 9% × (1 − reinvestment rate 40.7%); the reinvestment rate is forced to g ÷ terminal return on capital (22.4%)"* — that is `nopat[-1]×(1+g)×(1−g/ROIC)` at `docx_swdy.py:369`, the retired reinvestment identity, described to a reader as this study's own on the line carrying 89.4% of enterprise value. And the row beneath it asserts an arithmetic that does not work: 30,397 / (0.15343513 − 0.0914) = **490,003**, against a printed **493,872** — about EGP 0.67 a share at the anchor. The module's own figure, 28,072 or 30,637 after one year's growth, appears in neither delivered document. |
| 34 | Waterfall assertions [R-ENF-01 04-Sep] | **FAIL, ratcheted — and row 33 is the consequence** | `python3 scripts/check_waterfall_assertions.py` → *"SWDY **NO IMPORT (ratchet)** … 10 waterfall table(s); first says 'Less depreciation and amortisation'"*. `grep -n "table_residual\|waterfall("` over the study directory returns nothing. The study instructs a reader down ten tables and asserts none of them at build time; the terminal block in row 33 is one of the ten. `python3 scripts/check_sign_convention.py` → *"OK — no new violations"*. |
| 35 | EPS reconciliation [R-EPS-01] | **PASS** | `python3 scripts/check_eps_reconciliation.py` → *"ok SWDY FY2025 +13.5% gap, NAMED: the employees' statutory share of distributable profits, plus the ESOP adjustment to the weighted-average share count"*. `eps_reconciliation` registers the company's own reported EPS 7.13 beside an attributable 17,330.245 over 2,140.778 shares = 8.0953, names the 2,073.105 employees' share (note 39) and the 1.42216mn ESOP shares, and both P/E readings (16.1× and 18.2×) are published on page 7. The charge is carried in the bridge at 22,620 and the cap is measured at 9.1× of headroom against a disclosed wage bill of 18,906. |
| 36 | Rebuild ledger [R-REBUILD-01] | **PASS, with the bundle named** | `python3 scripts/check_rebuild_ledger.py` → *"SWDY 7 lever(s), 6 rule(s), cumulative +58.5%"*, walkable end to end, each lever naming the rule it serves. Lever 7 is this edition's own: the currency path derived rather than hand-set, **87.76333 → 87.94253**, naming [R-MACRO-01]. Lever 6 is the declared bundle the brief names — *"UNRECORDED: the levers between the beta re-derivation and 13-09-2026"*, **43.510763 → 87.76333, +101.7%** — the largest move in it, and it says so. The gate's one new violation across the book is ADIB, not SWDY. |
| 37 | Output records [R-ENF-05] | **PASS** | `python3 scripts/check_output_records.py` → *"OK — no new violations"*. The reverse read lives in the study's diagnostics record and no builder reads it back in; `assert_reverse_dcf()` would refuse otherwise. The contested-judgement register carries both framings with the side adopted, and the sign test is printed. |
| 38 | The workbook's inputs are live, and its drivers move it | **PASS, with 6 labelled references** | `python3 scripts/check_workbook_live_inputs.py` → *"SWDY — 6 input cell(s) no formula reads"*, each labelled on the page as a disclosed reference and passed on that basis. `driver_test.py` in the sandbox: *"DRIVER TEST OK — 17 drivers each reprice the workbook in the right direction"* and *"DEAD-INPUT SWEEP … none — every remaining driver reprices the model"*. |
| 39 | **The delivered workbook PDF** | **FAIL** | `SWDY_Valuation_Model_13092026_public.pdf`, 52 pages. Scored page by page on extracted text: **7 pages carry 20 or more lines of which more than 85% are bare numerals with no row label** — pages **10, 11, 13, 21, 26, 39, 45**. Page 10 read as a rendered image: four column headers (FY27E–FY30E) over **67 numbers and not one row label on the page**. The label column falls off the previous page. Carried unfixed from gate "c". |
| 40 | Prose figures and table footing | **PASS on the instruments** | `python3 scripts/check_prose_figures.py` → *"SWDY prose figures checked: 679; unmatched: 0"*. `python3 scripts/check_table_footing.py` → *"SWDY tables examined: 47; unreconciled totals: 0"*. Both re-run in the sandbox with the same result. **Both are blind to rows 10, 25 and 33 by construction:** every figure in those tables is individually computable from the committed numbers, and the defect is in the relationship between them. |
| 41 | This gate's own side effects | **None** | Every script was run inside a `git archive HEAD` sandbox at `/tmp/…/sbx`, never in the working tree. `git status --porcelain engine/swdy_study/` is clean before and after; `git rev-parse HEAD` is `ad4cf7280` before and after. |

---

## What is new, and what made it

| # | Defect | Evidence | Made by |
|---|---|---|---|
| **N1** | **The volatility caveat says a number "falls" to itself, and the real figure is nine points lower.** Page 17: *"The 49.3% annualised figure is dominated by a single session: the shares rose 14.1% on 4 August 2026 … Realised volatility over the last 50 sessions is **49.4%**; strip out that one session and it falls to **49.4%**."* | `docx_swdy.py:1101` computes `_rx = np.delete(_r, -2)` — it deletes the SECOND-TO-LAST log return, which on this frame is **2 September 2026, +0.66%**, not the 4-August session the sentence names. Recomputed off `price_series.frame()`: 50-session realised volatility **49.39%**; with the true 4-August +14.10% session removed, **40.63%**. The named session and its move are both correct (2026-08-04, 95.55 → 109.02, +14.10%); the arithmetic beside them removes a different day. The caveat is real and is understated by 8.8 points. | carried from the price-frame repair, never measured |
| **N2** | **The terminal block prints the retired construction's cash flow, and the page's instruction does not reach the page's answer.** Page 6: *"Terminal-year free cash flow \| 30,397"* over *"Present value of the terminal value \| 195,998 \| the terminal cash flow capitalised at 15.3% − 9% = 493,872"*. | 30,397 = `nopat[-1] × (1+g) × (1 − g/roic_term)` (`docx_swdy.py:369`), the reinvestment identity [R-TERM-01] retired. 30,397 / (0.15343513 − 0.0914) = **490,003**, not 493,872. The model's terminal is `terminal_record.fcff` **28,071.64**, grown one year to 30,637.39 and capitalised to 493,871.53. Neither 28,072 nor 30,637 appears in any delivered document. `DCF!A64` of the same study's workbook reads *"THE TERMINAL'S CAPITAL CHARGE — MAINTENANCE AT CURRENT COST, NEVER g x IC"*. | row 34 — nothing asserts a waterfall at build time |
| **N3** | **The retired terminal is not "published unused".** `compute.py:2094` logs *"[Terminal value, the RETIRED construction, published unused] g x IC on the same inputs gives 490,003 (−0.8%) … Recorded so the change is visible, and it feeds nothing."* | It feeds `tv_f_usd` (`compute.py:2277`) and `tv_d` (`:2281`), the two terminals of the CURRENCY-OF-DISCOUNTING alternative published at **EGP 79.09** in the summary table, §4, §7 and Expert 3's upper bound — this study's declared central contested judgement computed both ways. It also feeds `dcf_roic()` (`:2604`). So the study's two framings of its own biggest question stand on two different terminal disciplines. | the terminal rebuild reached the primary lens and not the alternative |
| **N4** | **A published sensitivity row is centred 13.3% above the study's own answer.** Page 15: *"Terminal return on invested capital \| 15% – 30% \| 66.63 – 116.48 \| 49.85"*, repeated in B.2. | `sens.roic_grid = [0.15, 0.18, 0.22433, 0.26, 0.30]`; `sens.grid_roic = [66.63, 83.25, **99.66**, 108.81, 116.48]`. The middle cell is the ADOPTED `roic_term` and reads **99.6619** against a published central of **87.94253**, +13.33%. `compute.py:2601-2606`: `dcf_roic()` re-implements the terminal on the retired identity AND its equity step omits `(1 − emp_rate)` — 99.6619 × (1 − 0.12193) = **87.51**. Page 15's caption asserts the opposite: *"Every grid above is produced by the SAME valuation function as the headline … Until this edition the grids ran through a second function that re-implemented the terminal on a construction the study had already retired and omitted the employees' statutory share"*. | the [R-SENS-01] repair fixed `dcf_at()` and left its twin |
| **N5** | **A typed percentage contradicts the driver table two pages earlier.** Page 20, catalyst *"Order intake"*: *"whether Constructions and infrastructure revenue growth (**18%** in FY2026E) holds up or decelerates faster than assumed"*. | `docx_swdy.py:1274` types the string `18%`. `inputs.construct_growth = [0.2744, 0.20, 0.15, 0.115, 0.09]`, and page 10's driver table prints **27.4%** for the same year. `qc_checks.py`'s numeral scan requires a decimal point, so a bare `18%` cannot match it. | the catalyst table, typed |
| **N6** | **§3 explains a calibration difference by a cause that is not the cause.** Page 17: *"THE FULL-HISTORY FIGURES ARE DIFFERENT AND BOTH ARE PUBLISHED: over all **57 windows** the 90% band caught **91%** … That is one method measured over two samples and not two answers to one question — the longer window is the better-covered of the two, and a reader comparing this page against a coverage figure quoted elsewhere for this name should check which window it is on."* | `backtest_5y.json.full` = `{windows 57, cov90 0.9122, cov50 0.4561}`. `assets/data.js:3309` = `SWDY: {mkt:"EG", n:57, hits:54, c50:0.5263, c90:0.9474}`. **Both are 57 windows** — 52 hits against 54. Checking the window explains nothing; the real difference is [R-REC-01]'s (a record scored as published against the same windows re-scored under today's fit). The paragraph added this edition to close a gap has directed the reader to a false explanation. | the full-history disclosure added this edition |
| **N7** | **§7 still deducts the raw dividend and prints a gross accretion 0.12 wrong.** Page 22: *"every lens value is rolled 246/365 of a year to the 3-Sep-2026 anchor at the 28.1% cost of equity less the **EGP 1.85** dividend paid in the window — worth about **+13.70** gross on the primary lens."* | `docx_swdy.py:1361` computes `DCF['ps'] − DCF['ps_dec'] + IN['dps_fy25']` = 87.94253 − 76.09347 + **1.85** = 13.69906. The model deducts `div_at_anchor` = **1.9677813**; the gross accretion is 76.09347 × 1.18157726 − 76.09347 = **13.81684**. The §1.1 bridge was repaired to print 1.97 and to foot; this sentence, four sections later, was not. | a partial repair of gate "c"'s N9 |
| **N8** | **The SWDY Driver Ledger entry is stale, and it logs the wrong drivers.** | `engine/Fundamental_Driver_Ledger.md:1000`, dated 7 September, records *"four levers, three rules, cumulative −5.02%"* against a current `rebuild_ledger.json` of **7 levers, 6 rules, +58.5%**, and *"the terminal carrying 84% of enterprise value"* against a published **89.4%**. It logs the walk-forward's mechanical drivers; the study's own **28 House-ring judgement inputs** are logged nowhere. The GBCO section immediately below names that exact distinction — *"It logs none of the study's OWN forward drivers, which is a different requirement and was not met"* — and closes it for GBCO alone. | the ledger entry was written for the walk-forward, not for this item |
| **N9** | **"The latest known close" is not the latest known close.** Page 1: *"PRICE DATE 3 September 2026 — the latest known close, the price this valuation is measured against."* | `engine/raw_ohlc/EG/SWDY.csv` carries `09/06/2026, 136.20`. `python3 scripts/check_valuation_gap.py` → *"SWDY: struck at 130.00, latest known **136.20 (2026-09-06)**"*; `northern_star.from_numbers('SWDY', …)` reads `spot_latest 136.20`, `spot_latest_date 2026-09-06`. The STRIKE route is correct — 130.00 at 2026-09-03 IS the latest committed supplied price under [R-GAP-01 AMENDED 08-09] — but the sentence a reader meets is false, and the price's **ten-day age at issue** is not disclosed, which the same amendment requires. The wording is generated by the shared `engine/doc_dates.py:header_line()`, so it is a house-template defect that will read the same way on every study struck against a supplied price older than its own library. | the shared [R-DOC-03] header |
| **N10** | **Two figures for one payout ratio.** Page 3: *"22.9% of FY2025 attributable EPS"*. Page 20: *"22.8% of attributable EPS"*, twice. | `dcf.dps_payout_fy25 = 0.22852758705288223` → page 3's `pc()` renders **22.9%**; `docx_swdy.py:1278` types **22.8%** in both cells of the dividend-policy catalyst row. | a typed numeral the decimal-point regex cannot see |
| **N11** | **The gap review carries a doubled percent sign.** `GAP_REVIEW_13-09-2026.md` line 38: *"falling 16%% to 7%%"*, and *"depreciating the pound at a flat ~6%% a year"*. | An f-string escaping defect in `gap_review.py` reaching a committed deliverable that this gate and `check_valuation_gap.py` both read. | the generated gap review |
| **N12** | **The study's own terminal log does not foot.** `assert_log`: *"terminal NOPAT 51,298 + book D&A 6,902 − maintenance at replacement cost 12,377 … − growth capital 4,573 … − inflation on working capital 8,882 = FCFF 28,072."* | The printed lines reach **32,368**. The module uses `terminal_record.inputs.nopat = 47,001.98` — the LAST EXPLICIT year, not grown — and 47,002 + 6,902 − 12,377 − 4,573 − 8,882 = **28,072** ✓. The log prints `nopat_term` (51,298, already grown) in a waterfall computed from the ungrown figure. Internal record; it reaches no delivered document. | a prose line beside an arithmetic it does not describe |

## Gate "c"'s findings, re-tested on the 13-09 artefacts

**Closed (11):** N1 terminal growth (§7 now reads *"Terminal growth of 9.14% is 2.0% in real
terms … (1+7%)(1+2.0%)−1 = 9.14% … The growth grid runs 7.14% to 11.14%"*); N2 the 52-week
high (*"the high IS this study's own anchor close of 130.00 on 2026-09-03"*, read off
`price_series.frame()`); N3 Figure 3's caption (*"the grid tops out at 221.10"* over a figure
topping out at 221.10); N4 the sweep register's false negative (F27, F28, F29 above); N5 the
repository path (`check_delivered_vocabulary` → CLEAN); N6 the "full Egyptian equity risk
premium" (all three sites now read *"a country premium of 3.98% … weighted at 0.5928 …
levied FLAT rather than multiplied by beta"*); N9 the §1.1 bridge (foots to 87.94253); row 14's
"rather than a reconstructed unit model" in §1.6 and B.2; row 16's "5 Aug 2026" in the
register's `anchor_days` justification; row 18's missing H1-2026 statements in the
primary-documents table; and two of the three retired-blend weighting sentences.

**Still live (9):**

1. **Figure 1 still labels a published lens "not published for this class"** (`figures.py:50`),
   on page 8, directly above the table that publishes it at 79.91 / 103.30 / 132.54 and beside
   a summary table that publishes 103.30. The STUDY TABLE was fixed (`docx_swdy.py:560` now
   reads *"RETIRED for this class — computed and shown, never weighted"*); the FIGURE and
   `gap_review.py:126` were not. One repair, three sites, one closed.
2. **§7's far-year band table still labels the Note-16 segment-profit series "Gross profit"**
   — *"FY2028E — Gross profit (EGP mn) … 65,101"* against A.1's *"Segment profit, three
   disclosed segments (Note 16 basis)"* 65,101 and a separate audited-face gross profit row.
3. **"This is the harshest of the three readings, at EGP 97.72"** (page 27) against
   `experts.e3.base = 89.43` and C.5's own *"The panel spans EGP 89.43 to 176.13"*.
4. **Expert 1's worked table omits the line that reconciles it** — printed lines reach
   **15.3661**, the table prints **15.87**; the gap is the associate line, 0.4998/share.
5. **"An earlier edition did settle it that way and reported EGP 86.16"** (page 9, and the gap
   review's lens table). No delivered edition published 86.16: the 05-08-2026 edition's
   central is 43.51, the 09-09-2026 edition's is 39.55, and 86.16 first appears in the
   10-09 edition as this model's own `retired_blend_value`.
6. **"it is weighted accordingly"** survives on page 7 of §1.3 — the third of the three
   retired-blend weighting sentences.
7. **Margins as OUTPUTS** — row 22.
8. **The external-reader scrub** — row 11.
9. **The workbook PDF's seven label-free pages** — row 39.

## What the filings disclose that the model does not consume

Asked explicitly against `sweep_register.json` [L-006].

1. **Cost per tonne for Cables — 55.4% of revenue, and the largest one left.** Revenue is now
   tonnes × price and the workbook computes it live. Cost is not: `cables_margin` is
   **11.4341% flat for five years**. The tonnage is in the releases the register logs, the
   copper path is registered and the pound path is derived, so a conversion cost per tonne is
   reachable from what the study already holds — and would make the margin the output the
   document three times says it already is.
2. **The reviewed balance sheet at 30 June 2026.** Read for eight separate quantities and
   listed in the bibliography's own primary-documents table; the bridge still subtracts the
   31 December 2025 lines. `scripts/check_bridge.py` names it; the record admits it.
3. **A fourth audited fiscal year.** The sweep carries FY2023-25 against a target of 4 and
   warns. FY2022 comparatives sit in the FY2023 statements the study already holds.
4. **The full Note 5-2 geographic split.** Consumed as one scalar, `lambda_country = 0.5928`.
   On a valuation whose §7 calls country risk the biggest single question, the per-country
   detail behind that weight enters nothing.
5. **The Q2-2026 release's export shares** (79% group, 84% cables, 67% engineering, F03).
   Registered as establishing the DIRECTION and a ceiling and explicitly not used to set the
   currency weights of 65 / 30 / 45 — a declared, reasoned exclusion, and the right one.
6. **The engineering backlog, EGP 293bn at December 2025 and 346bn at 30 June 2026.** Read and
   explicitly not burnt down, because the releases disclose no burn profile. Declared.
7. **The facility-by-facility currency split.** Not disclosed; the 28% pound share is
   back-solved and labelled inferred, and the absence is recorded as a negative result.

## What is genuinely strong here, and should not be lost in a rebuild

The workbook reproduces **cell for cell across 1,564 populated cells** against a clean
`git archive` rebuild, **630 of 630 formulas evaluate with zero errors under an evaluator the
study does not own**, and both documents rebuild to **0 text differences**. The free-cash-flow
waterfall foots by hand on every year and reaches present value inline; the one-page valuation
table foots from present values to value per share; the two 5×5 grids are centred on the
struck case to the last digit. The beta is on the sanctioned route against the published EGX30
with `assert_beta_provenance()` genuinely called before the record is written, and the
correction it forced moved the answer **further below** a price it already sits far below. The
cost of equity reproduces to the basis point under a construction named from a closed list,
with the country premium charged once and never multiplied by beta. The terminal MODEL stands
on a life derived by identity from the audited note and reproduces to 1e-6. The employees'
statutory share is found, measured, charged, capped against a disclosed wage bill and
reconciled to the company's own reported earnings per share. And two records in this directory
are the best of their kind in the book: `bridge_record.balance_sheet_reason` states in terms
that the rule does not admit its own argument and that the study stays on the ratchet, and
`forecast_anchor.note` refuses the flattering comparison in favour of the one that makes its
own breach larger. Both are the opposite of the self-certification these gates exist to stop.

---

*Audited from outside the study, pinned to `ad4cf7280`. Every command re-run in a
`git archive` sandbox; no file in the study was modified.*
