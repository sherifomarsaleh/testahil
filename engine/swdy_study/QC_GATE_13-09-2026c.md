# SWDY — QC gate, 13-09-2026 (third pass)

Elsewedy Electric Company S.A.E. · Egyptian Exchange: SWDY · EGP · edition of 10-09-2026

Audited from OUTSIDE the study per [R-ENF-02] by a reader who did not build it and did not
write the repairs. Every row names the artefact, the command or the number that carries it.
Nothing in the study, its builders, its numbers, its figures or its delivered documents was
touched; the only file this audit wrote is the one you are reading.

**VERDICT: NOT DELIVERABLE.** Of the twenty-one hard fails raised on 13 September,
**thirteen are genuinely closed, six are closed with a gap that still reaches a reader, and
two are not closed** — one of those two is now wrong in a new way created by its own repair.
**Nine further defects were found, six of them made or exposed by the repairs.**

The shortest list of what blocks delivery:

1. **§7 tells the reader terminal growth is "EXACTLY zero in real terms" and derived from "a
   stated real growth of zero". The committed record states real growth of 2.0%.** Four false
   sentences in one bullet, about the assumption carrying 89.4% of enterprise value.
2. **The sweep register's mandatory announced-plans category is closed by a dated negative
   search saying nothing was found**, while the study's own `compute.py` carries three plants
   announced 24 June 2026 and a Datagrid/Transpower transformer order.
3. **§2 tells the reader the 52-week closing high "IS the +14.1% print of 4 August 2026, one
   session before the anchor".** The 52-week closing high is 130.00 on 2026-09-03 — the anchor
   date's own close. Created by the price-frame repair.
4. **Figure 3's caption quotes the maximum of a different grid** — "the grid tops out at
   143.24" over a figure that tops out at 221.10. Created by the caption repair.
5. **§1.6's opening sentence and B.2's risk row still say the forecast is a taper on segment
   revenue growth "rather than a reconstructed unit model"**, on the page facing the driver
   table that grows Cables on disclosed tonnage.
6. **Three places say the model "charges the full Egyptian equity risk premium"** while
   `lambda_country = 0.5928` and `crp_effective = 3.98%` against a full 5.168%.
7. **The §1.1 bridge's printed steps do not reach its printed answer** — 88.06 against 87.94 —
   because the page prints the unrolled dividend of 1.85 where the model deducts 1.9678.
8. **`scripts/check_delivered_vocabulary.py` was RED on this study at the audited commit**, on
   a repository path the bibliography repair put on a reader's page.

---

## THE TREE MOVED UNDER THIS AUDIT, AND THAT IS STATED RATHER THAN GLOSSED

This gate is pinned to commit **`b290bb3f7`**, the commit named in the brief. During the
audit HEAD advanced four commits to **`eb3d327f8`** (`5ad22c310`, `db219a311`, `c6c6be520`,
`eb3d327f8`), and all four rebuilt delivered artefacts under the same filenames:

| Artefact | md5 at `b290bb3f7` | md5 at `eb3d327f8` |
|---|---|---|
| `SWDY_Valuation_Study_10-09-2026_public.pdf` | `845d5e74…` | `4552114c…` |
| `SWDY_Bibliography_10-09-2026.docx` | `b52648c2…` | `5668dc6e…` |
| `SWDY_Valuation_Model_10092026_public.xlsx` | `8ed8e37e…` | `b1d1578d…` |
| `study_numbers.json` | `3c813734…` | `3c813734…` (unmoved) |

**Every finding below was re-verified against the bytes at `b290bb3f7`**, extracted with
`git archive b290bb3f7 engine/swdy_study` into a clean directory. Re-tested against
`eb3d327f8`, **exactly one finding has since been closed** (the repository path in the
bibliography); the other fifteen text findings are still present. The numbers did not move.

**This is the second consecutive gate on this study to have to write this paragraph**, and it
is itself a finding: `edition.py`'s own docstring says *"rebuilding over its filename destroys
the only record of what was said"*, and `EDITION = date(2026, 9, 10)` has now been rebuilt
over at least eight times since 10 September. [R-DOC-03] requires a recalibration to issue a
NEW document. A reader holding the 10-September file does not hold the 10-September numbers.

---

## The edition audited, and how it was resolved

`edition.py` holds `EDITION = date(2026, 9, 10)` and derives all six filenames. The directory
holds **30 Python files** (27 at the last gate; `price_series.py`, `sweep.py` and `attest.py`
are new). All 25 artefacts in `build_manifest.json` hash-match the delivered files.

| Deliverable | File | Measured at `b290bb3f7` |
|---|---|---|
| Study, Word | `SWDY_Valuation_Study_10-09-2026_public.docx` | 196 paragraphs, **40 tables** |
| Study, PDF | `SWDY_Valuation_Study_10-09-2026_public.pdf` | **30 pages, 8 figures — every page read as a rendered image at 115 dpi** |
| Workbook | `SWDY_Valuation_Model_10092026_public.xlsx` | 16 sheets, **630 formulas, 315 numeric literals, 1,564 populated cells** |
| Workbook, PDF | `SWDY_Valuation_Model_10092026_public.pdf` | 52 pages |
| Bibliography | `SWDY_Bibliography_10-09-2026.docx` / `.pdf` | 21 pages, **13 tables, 222-row printed register** |
| Gap review | `GAP_REVIEW_13-09-2026.md` | 172 lines, eight headings present |
| Sweep register | `sweep_register.json` + `sweep.py` | **NEW** — 30 findings, 12-row driver gate |
| Attestation | `attest.py` + `attestation.json` | **NEW** — the three gates plus `assert_ground_up()` |

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker and exchange | SWDY · EGX · market code EG | `assets/data.js:642` `code: "EGX:SWDY"` — read from the `code` prefix, never from `raw_ohlc/EG/`. |
| Regressor the beta rule demands | **EGX30**, `engine/raw_indices/EG/EGX30.csv` | `wacc_builder.EXCHANGE_INDEX[('EG','EGX')]` → `EGX30`, registered and present. Not DFM, not QSE — no interim note and no weekly-only caveat apply. |
| Class and reference pattern | Diversified industrial with a contracting arm; nearest pattern **ADNOCLS** (operating company) inside ADNOCLS's skeleton | `lessons_register.CLASSES`; `REFERENCE_SET == ('ADNOCLS','ADCB','ALPHADHABI')`, SWDY displaced 19-Aug-2026. `LENS_REGISTRY['diversified industrial with a contracting arm'] = ('dcf', ('ev_ebitda_own_history','relative_multiple','sotp','book_value'))`. |
| Committed central | **EGP 87.94253027526973** | `study_numbers.json.central`; `lens_record.primary.kind = "dcf"`. |
| Committed envelope | **28.566916442324477 – 138.175135007335** | `study_numbers.json.span`. |
| Committed spot | **EGP 130.00, close of 2026-09-03** | `meta.spot` / `meta.asof`. |
| Shares | **2,140.777876mn** | `meta.shares_mn`, the company's own filed issued count. |
| Gap on the struck spot | **−32.35%** | 87.94253 / 130.00 − 1. |
| Gap on the latest known price | **−35.4%** | `python3 scripts/check_valuation_gap.py` → *"SWDY −35.4% GAP_REVIEW_13-09-2026.md (audits 87.7633) … struck at 130.00, latest known 136.20 (2026-09-06)"*. |
| Live standard | **2026.09.10** | `cd engine && python3 -c "import research_protocol as rp; print(rp.STANDARD_VERSION)"`; the study is stamped `2026.09.10`. |
| Lessons read first | 266, of which 4 bind on this class and 3 on this name | `python3 engine/lessons.py SWDY --class "diversified industrial with a contracting arm"` — L-295 (legs on different contract structures), L-296 (normalised earnings is not a lens for this class), L-294 (the EPS reconciliation this name produced). |

---

## The twenty-one, one by one

| # | The 13-Sep hard fail | Now | Evidence |
|---|---|---|---|
| 1 | The document published `cables_real_growth` 3.0% as the live Cables driver | **CLOSED, one gap named** | Page 10 of the rendered PDF prints *"Cables — tonnage growth (the disclosed volume driver)"* 10.7/9.0/7.5/6.5/5.5% and *"Cables — copper/FX pass-through to revenue"* nil/−6.5/−4.4/−2.2/nil — the two live inputs. `grep -rn cables_real_growth *.py` returns three hits, all comments or the registration itself. **GAP:** the delivered register still prints the retired driver with its live construction formula — *"Cables segment revenue growth = (1+copper growth)(1+FX growth)(1+this) − 1"* — and never says it is read by no line. [L-018]. |
| 2 | The workbook's READ FIRST panel described the retired construction | **CLOSED** | `READ FIRST!A26-A30`: *"Cables on DISCLOSED TONNAGE (185,449 tonnes in FY2025, from the company's own quarterly releases) times a copper and currency pass-through … The AUDITED filings disclose no tonnage, order-book or backlog figure for any segment — the releases do, they are the issuer's own."* |
| 3 | §1.8 printed a terminal beta of 1.225 where the model uses 1.0 | **CLOSED on the beta, one gap named on the Ke** | Page 12: `Beta \| 1.225 \| 1.000`, sourced to `cost_of_capital_record.beta_terminal = 1.0`, construction named `beta_to_one_split`. Ke reproduces: `ke_exp = 0.280899` from `0.1891 + 1.224907 × 0.0424 + 0.5928 × 0.0517 + 0.4072 × 0.0226`; `ke_terminal = 0.168681`; `python3 scripts/check_ke_reproduction.py` → *"OK — no new breach"* across 11 records. **GAP:** the table publishes only the ERP TOTAL (9.4% / 7.0%), so a reader computing `rf* + beta × ERP` reaches **30.4% / 17.5%** against a printed **28.1% / 16.9%**. [R-COC-03] requires the components published *"so a study publishes a Ke a reader can take apart"*; `erp_mature 0.04242`, `crp 0.05168`, `lambda 0.5928` and `crp_foreign 0.0226` appear nowhere on that page. |
| 4 | §2, §3 and Figure 5 stood on the 5-August series ending at 105.20 | **CLOSED** | `price_series.frame()` reads `engine/raw_ohlc/EG/SWDY.csv` and cuts at 2026-09-03, close **130.00**; it refuses if the library's close on that date is not the study's spot. Page 16: SMA20 **119.65**, SMA50 **102.80**, SMA100 **94.30**, SMA200 **86.19** — recomputed independently off that frame and reproducing. Figure 5 (page 18) opens at **130.00**. The discredited `SWDY_Stock_Price_History.csv` is read by nothing. |
| 5 | "Probability above spot" printed 56% and 61%, computed against 105.20 | **CLOSED** | Page 18: 1 month (to **2026-10-04**) 59%; 3 months (to **2026-12-03**) 63%. Both horizons resolve off the 2026-09-03 anchor. |
| 6 | §6 said the central sits "below even the 5th percentile (79.13)" — it did not | **CLOSED** | Page 21: *"Where the fundamental central sits \| **87.94** \| below even the 5th percentile (**98.49**)"* — true on the restruck cone. |
| 7 | Figure 3's caption said no cell reaches 130.00 over a grid containing four that do | **NOT CLOSED — wrong in a new way** | Page 15's caption now counts *"4 of the 25 cells reach the market price of 130.00"* — correct — and then says *"the grid tops out at **143.24**"*. **The figure it captions tops out at 221.10.** `figures.py:78` draws `fig2_sens.png` from `d['sens_wg']['table']` (`grid_wacc_g`, max **221.10**); `docx_swdy.py:915` computes the caption from `SN['grid_exp_term']` (max **143.24**), which is the *other* table, printed below it. The cell count agrees by coincidence — both grids happen to hold four cells ≥ 130. |
| 8 | §1.9's copper caption claimed a mechanism and a direction the repaired grid does not have | **CLOSED** | Page 15: *"Note the copper row — it is the SMALLEST swing of any operating driver at EGP 8.78 a share across a ±15% move, and it runs upward throughout"*. `sens.grid_copper = [83.55, 85.75, 87.94, 90.14, 92.33]` — monotone up, swing 8.78. Read off the grid, not asserted. |
| 9 | The [R-STAR-01] case rested on a premise the repair falsified | **CLOSED** | `star_case.case` rewritten around the reinvestment charge; `star_case.decomposition` prices five candidates on full re-runs (margins +113.47, cost of capital +52.56, terminal growth +30.26, copper +5.85, the pound +2.34); `hunt_recorded: true`; a falsifier stated in advance. `northern_star.from_numbers('SWDY', …)` → `tier "priced"`, `verdict "PASS"`, `missing []` against a gap of −35.4%. |
| 10 | Figure 1 drew a fifth bar labelled "Weighted central" | **CLOSED** | Page 8, read as a rendered image: **four** bars — *"FCFF DCF — THE ANSWER"* in gold, then Relative, Normalised and Book in grey. No fifth bar. |
| 11 | Two of three expert bases sat outside their own ranges | **CLOSED** | `experts.e1` 176.13 in 129.26–222.99; `e2` **97.72** in 69.84–160.97; `e3` **89.43** in 66.70–112.16. `attest.py:73` asserts `expert_bracketed` and the assertion runs. Figure 8 (page 29) read as an image: all three brass ticks sit inside their bars. |
| 12 | The headline said "the four lenses centre at EGP 87.76" | **CLOSED** | Page 2: *"the cash-flow lens — which is the answer here, not one vote of four — values the company at EGP 87.94 per share"*. `grep "four lenses centre"` returns nothing. |
| 13 | Appendix A.1's "Gross profit" row spliced two bases | **CLOSED, one gap named** | Page 23 prints two rows: *"Gross profit (audited face: revenue less cost of sales)"* 29,077/43,899/40,762 then five em-dashes, and *"Segment profit, three disclosed segments (Note 16 basis — the forecast build)"* 26,416 … 82,058. Each on one basis. **GAP:** §7's far-year band table (page 21) still labels the Note-16 series *"FY2028E — Gross profit (EGP mn) … 65,101"*, which is the segment-profit row A.1 was just split to keep apart. |
| 14 | §7 withdrew "the releases were not reachable" and five other places republished it | **PARTLY CLOSED — two survive** | Fixed in §5 (*"no order book or backlog figure is disclosed in any AUDITED filing. The company's own quarterly releases disclose an engineering backlog of EGP 346bn at 30 June 2026, which is read"*), in B.3 and in the workbook panel. **Still wrong:** §1.6, page 9 — *"None of the three audited filings … discloses a tonnage, unit-volume or order-book figure for any segment, **so the forecast is built as a taper on each segment's own recent revenue growth and margin path rather than a reconstructed unit model**"* — fifteen lines above the driver table whose caption reads *"Cables grows on DISCLOSED TONNAGE — 185,449 tonnes in FY2025 … rather than on a revenue growth assumption"*; and B.2's risk row, page 25 — *"the forecast tapers on segment revenue growth rather than a reconstructed unit or backlog-burn model"*. Both describe 55.4% of revenue wrongly. |
| 15 | The bridge said "no filing discloses the cap's headroom" while the study computes it | **CLOSED** | Page 6: *"the wage bill is disclosed in three notes of the same audited statements at EGP 18,906mn against a statutory share of EGP 2,073mn, so the charge sits at 9.1x of headroom. It is the measured charge, not an upper bound."* |
| 16 | "5-Aug-2026" survived as the anchor in four places | **PARTLY CLOSED — a fifth place remains** | Gone from the study document and from the workbook (`grep '5[- ]Aug'` over both returns nothing but source dates). **Still wrong:** the bibliography's input register prints, as the justification for `anchor_days = 246`, *"Days from the DCF's construction date (31 Dec 2025 …) to the anchor date **5 Aug 2026**"* (`compute.py:1330`). 246 days from 31-Dec-2025 is **3 September 2026**; 5 August is 217. [R-ENF-01 EXTENDED 03-Sep] reaches the register's own justification text. |
| 17 | Fifteen of 220 registered inputs never reached the delivered register | **CLOSED** | `study_numbers.json.inputs` holds **222**; the bibliography's register tables hold **222 data rows** across three layer tables and **every committed key resolves to a printed label — 0 missing**, checked by name-matching all 222 keys against the printed labels. `attest.py` asserts four-field completeness on all 222. |
| 18 | §3 published 84% from a backtest at `nu 6.0` beside a cone at `nu 5.0`, the site said 94.7%, and no binomial anywhere | **PARTLY CLOSED** | The binomial is published: page 17 — *"a two-sided exact binomial against each band's own rate returns p = 0.65, 1.00 and 0.43"*, with the count beside every percentage (*"42% inside the 50% band, 79% inside the 80%, 84% inside the 90%, over those 19 windows"*) and the width ratio disclosed, never gated (*"about 21% wider than a naive carry-anchored band … a real limitation"*). The fit now agrees: `backtest_5y.json.fit = {nu: 5.0, width_cal: 0.951}` = the live `market_profiles` EG profile = `strike_result.json`. **GAP:** two coverage records for one name still reach readers — the study's 84% over 19 non-overlapping quarterly windows, and `assets/data.js:3309` `SWDY: {n:57, hits:54, c90:0.9474, width:1.371, flag:null}`, 94.7% over 57. Each names its own sample, which is what [R-CAL-02] asks; they are still two answers to one question. |
| 19 | No Sweep Register existed | **CLOSED as an artefact — and it carries a false negative search** | `python3 scripts/check_sweep_module.py` → *"NOW CONFORMING — remove from the list (1): SWDY"*. `sweep.py` imports `research_sweep` and calls `validate()`: *"PASS — 4/4 rings, 30 findings (1 B · 5 S · 14 D · 2 C · 8 NEG), driver gate 12 rows (8 bottom-up / 4 top-down) \| primary access: attempted (1) \| FS years: 3 \| IR findings: 5 \| warnings: 1"*. **HARD FAIL inside it — see new finding N4 below:** the [R-NEWS-01] mandatory category *"announced projects, ventures and capacity (entity verified as the listed issuer)"* is closed by a NEGATIVE_SEARCH dated 2026-09-10 reading *"Nothing found that clears the entity test, so no capacity addition is modelled"*, while `compute.py:1925` states *"THE THREE ANNOUNCED PLANTS ARE ALREADY PAID FOR IN THIS MODEL … On 24 June 2026 the company announced a copper-recycling complex (US$80mn, 20,000 t/yr), a copper-tube plant (US$65mn, 15,000 t/yr) and an aluminium-rod line (US$55mn, 50,000 t/yr)"* and `compute.py:910` registers *"high-voltage transformers of up to 360 MVA for Datagrid's Southland AI data centre via Transpower New Zealand"* as evidence behind `elecprod_growth`. `grep Datagrid sweep_register.json` → 0. |
| 20 | The gap review's eight headings audited EGP 59.28 | **CLOSED, one gap named** | `GAP_REVIEW_13-09-2026.md`, 172 lines, all eight headings present and in order, computed on this edition's numbers; `python3 scripts/check_valuation_gap.py` → *"OK — no new violations"*. **GAP at the audited commit:** the review states `AUDITED CENTRAL: 87.7633` and prints the lens row `28.56 \| 87.76 \| 137.73` while the study publishes **87.9425** and **28.5669 – 138.1751**. Inside the gate's `AUDIT_TOL = 0.005`, so it passes; it is a review of the previous lever's answer. Closed by `c6c6be520` after the audited commit. The review states no `AUDITED GAP`, which [R-GAP-01 AMENDED 03-Sep] asks for and the gate tolerates. |
| 21 | No `DriverLine` record existed | **CLOSED** | `attest.py:176-218` builds three `DriverLine`s and calls `assert_ground_up(lines,'SWDY')`. Run: *"ground-up record: 3 lines, 55.4% of revenue at unit level"* — Cables **unit** 55.4% on tonnes shipped with the source and price basis named; Constructions **segment** 32.4% with a gap note; Electrical products **segment** 12.2% with a gap note. Committed to `study_numbers.json.driver_lines` and `ground_up.share_by_level = {unit 0.5543, derived 0, segment 0.4457, topdown 0}`. `python3 scripts/check_ground_up.py` → *"OK — every committed driver-line record passes the ground-up assertion when it is actually run."* |

**13 closed · 6 closed with a gap · 2 not closed.**

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates called in the study's own committed code [R-ENF-02] | **PASS** | AST walk over all 30 `.py` files: `assert_model_study` → `attest.py:113`; `assert_sigcm` → `attest.py:163`; `assert_beta_provenance` → `attest.py:166` and `beta_reg.py:84`; `assert_ground_up` → `attest.py:218`; `assert_bridge` → `bridge_record.py:222`. Entry point run in a sandbox at `b290bb3f7`: `python3 attest.py` → *"assert_model_study PASSED … assert_sigcm PASSED … assert_beta_provenance PASSED — 1.2249 vs EGX30.csv (conforming=True)"*, exit 0. `build_all.py` declares 21 steps in dependency order and runs `qc_checks.py` then `attest.py` last, on the delivered artefacts. `python3 scripts/check_study_provenance.py` → *"OK — no new violations"*, 25 study directories. |
| 2 | Beta inspected on its record, never on a boolean — SIGCM clause 6, [R-BETA-04] | **PASS** | `beta_result.json`: `index_file "raw_indices/EG/EGX30.csv"` — under `engine/raw_indices/`, registered for `('EG','EGX')`, the exchange the `code` prefix names. `beta 1.2249065740479688`, `r2 0.3682291359`, `se 0.16985536`, `n 256`, `window_years 4.94`, `week_rule "W-THU"`, `index_asof 2026-09-08`, `conforming true`, `interim_note null`. Usability gate: n 256 ≥ 24 ✓, R² 36.8% ≥ 5% ✓, SE 0.1699 < \|β\| 1.2249 ✓ — no fall to a peer beta or 1.0 is owed. Not DFM and not Qatari, so no interim note and no weekly-only caveat apply. The withdrawn 31-name composite is published as a labelled cross-check (1.0087 at R² 0.291) and named a hard fail, not a tier. |
| 3 | Step 0.0 data-quality gate | **PASS** | `step0_result.json`: **3,755 clean rows**, span **15.67 years**, `dq_log ["dropped 1 interior stale/no-trade rows"]`, screened against the EGX calendar and the ±20% EGX limit. Run on both series inside `own_stock_beta()` — `beta_result.json` carries `stock_dq` and `index_dq`. |
| 4 | **Ground-up clause on a driver record, not a flag [R-SIGCM-02]** | **PASS** | Three `DriverLine`s, 100% of revenue, share by level **unit 55.43% / derived 0 / segment 44.57% / topdown 0**. Cables names the unit (tonnes shipped), its source (the issuer's own quarterly releases, 144,997/156,748/167,665/185,449 and 99,239 against 89,636) and the price basis (the LME forward path × the house EGP/USD path as a measured pass-through). Both segment lines carry gap notes stating why no unit exists. `scripts/check_ground_up.py` clean. |
| 5 | Step 0 calibration under the band record [R-CAL-02] | **PASS, with one gap named** | §3 publishes coverage with the count always beside the percentage — *"42% inside the 50% band, 79% inside the 80%, 84% inside the 90%, over those 19 windows"* — a two-sided exact binomial on each band (p = 0.65, 1.00, 0.43), a uniformity test (p = 0.86) and the width ratio disclosed and explicitly not gated (*"about 21% wider than a naive carry-anchored band, which is a real limitation"*). Fit `nu 5.0 / width_cal 0.951` = the live EG profile. **Gap:** the site's published band record for the same name is 54 of 57 = **94.7%**, width **1.371**, `flag:null` — a different sample, reaching a different reader. `backtest_5y.json.fit` also carries `market_verdict: "PARITY"`, a token [R-CAL-03] retired; it is internal and reaches no delivered surface. |
| 6 | SIGCM's eight clauses | **PASS, one clause declared N/A with its reason** | `attest.py` prints each from its own evidence: `historicals_official_only True` (audited sources across every `rev_*`/`ebit_*`/`assets_*` key, no "estimate"), `forecast_ground_up True`, `debt_lc_fx_split True` (`w_egp_implied` plus `kd_egp_note`/`kd_hard_note`), `asset_conversion_cycle True`, **`competitors False` with an `na_reasons` entry** stating no listed comparable exists for this mix and that an earlier draft's unsupported "peers trade at 8-11x" was withdrawn rather than sourced, `beta_own_history_vs_egx30 True`, `formula_based_model True` (630 formulas), `flags_raised_before_issue True`, `stop_and_inform_honoured True` (8 dated negative searches). A peer FRAME is published at B.1 without a peer multiple. |
| 7 | Numeric traceability — every builder reads the committed numbers file | **FAIL** | `docx_swdy.py:231`, `:1277` and `:1529` type the tonnage series **144,997 / 156,748 / 167,665 / 185,449** and **99,239 / 89,636** into f-strings while `inputs.cables_tonnage_hist` holds exactly those figures; `build_xlsx_swdy.py:137` types **185,449** into the workbook panel. The new scan in `qc_checks.py:155` uses `\d{1,3}(?:,\d{3})*\.\d{2,}` — it requires a decimal point, so thousands-separated integers cannot match, and it does not scan `build_xlsx_swdy.py`. It records **21 typed numerals** (`docx_register.py:198 105.20`, `:199 130.00`, `:277 1.85`, `docx_swdy.py:890 22.57`, …) and **does not fail on any of them**; `attest.py` reads the count into `EVIDENCE` and asserts nothing about it. A scan that records and never refuses is declaration without execution. |
| 8 | Independent recalculation of the delivered workbook | **PASS** | Delivered file converted with `soffice --headless --convert-to xlsx` and read `data_only`: **630 formula cells, 0 error values, 630 of 630 committed expected values reproduce to 1e-6**. The study's own `recalc.py` run in a clean `git archive` sandbox: *"RECALC OK — 630 formulas, 0 unresolvable, 630 cell-level agreements with the model, 41 headline reconciliations passed"*. `python3 scripts/check_workbook_values.py` lists SWDY CLEAN. |
| 9 | Script-built files verified by a cell-by-cell diff against the delivered file | **PASS** | `build_xlsx_swdy.py` re-run inside a `git archive b290bb3f7` sandbox and every cell of all 16 sheets compared with the delivered workbook: **1,564 populated cells compared, 0 differences**, sheet names and order identical. `python3 scripts/check_workbook_structure.py` lists SWDY as matching the model report's 16 sheets. Both delivered documents rebuild to 0 text differences (`driver_test.py`, `prose_check.py`, `footing_check.py` all green in the sandbox). |
| 10 | **Read the rendered PDF, as images** | **FAIL — six defects no programmatic check saw** | All 30 pages of `SWDY_Valuation_Study_10-09-2026_public.pdf` rendered at 115 dpi and read as images. Found: the terminal-growth bullet (N1), the 52-week-high row (N2), Figure 3's caption (N3), the "full Egyptian equity risk premium" sentences (N6), the retired-blend weighting language (N7), and the normalised lens labelled "not published for this class" in the row that publishes it (N8). Every one passed `prose_check.py` (662 figures, 0 unmatched), `footing_check.py` (47 tables, 0 unreconciled) and `qc_checks.py` (0 scrub hits). |
| 11 | External-reader scrub — standard 4 | **FAIL** | `qc_checks.py` → *"study: 0 hits · bibliography: 0 hits"* against its own 43 patterns. Re-counted independently over every paragraph and table cell of both delivered documents: the **study** carries **"walk-forward" ×2** (§3 *"Tested by walk-forward simulation over nearly five years"*; About *"tested by walk-forward simulation against a random-walk benchmark"*) and **"persona" ×2** (Appendix C intro *"with different persona choices"*, *"the divergence is the persona doing what it says"*). The **bibliography** carries **"SIGCM" ×2**, **"depth bar" ×2** and, at the audited commit, the repository path **`engine/raw_ohlc/EG/SWDY.csv`**. The study's own pattern list contains `persona library` but not `persona`, and does not contain `walk-forward`, `SIGCM` or `depth bar`. |
| 12 | Verdict tokens [R-CAL-02] | **PASS** | `cd engine && python3 -c "import band_record as br; br.assert_no_verdict_tokens('EG')"` → no raise. No PASS/PARITY/FAIL beside the company name in either delivered document; `CRPS` appears nowhere; calibration evidence lives in §3 as plain-language sentences with the statistics inline and there is no calibration appendix — the document has exactly three (A, B, C). |
| 13 | **A standing repository gate red on this study** | **HARD FAIL at the audited commit** | `python3 scripts/check_delivered_vocabulary.py` → *"examined 48 document(s) across 25 study directories … **FAIL — 1 new leak(s): SWDY / SWDY_Bibliography_10-09-2026.docx — a repository path: engine/raw_ohlc/EG/SWDY.csv**"*. `git log -S` locates it in `47aca929d`, one of the repair commits. Closed at `eb3d327f8` — after the commit this gate audits. |
| 14 | Figure discipline — standard 5 | **PASS** | Eight figures, each read as a rendered image inside the delivered PDF (pages 8, 11, 15, 16, 18 ×2, 19, 29). Transparency verified programmatically on the source PNGs (`alpha extrema (255,255)` on all eight) **and on the delivered PDF** (8 distinct embedded images, **0 with a transparent pixel**). Solid light canvas on every one. |
| 15 | Table discipline — standard 6 | **PASS, with one gap named** | Independent audit over every table in both documents against the section's own text width: **study 40 tables — 0 starved, 0 bloated, 0 over-wide**; **bibliography 13 tables — 0, 0, 0**. Every table carries explicit `w:gridCol` widths. **Gap:** 3 of the 53 (2 in the study, 1 in the bibliography) do not carry `w:tblLayout type="fixed"` and rely on the grid alone. |
| 16 | Expert appendix at maximum detail — standard 7 | **PASS on structure, FAIL on one claim and one table** | Each of the three carries worldview, when-it-works / when-it-fails, a worked table, a named sensitivity with numbers (*"each one-turn change in the multiple is worth EGP 15.87 per share"*), and a falsifier stated in advance; C.4 cross-examination with four challenges each conceded or rejected; C.5 the three in one room; C.6 a divergence table. Labels are "Expert 1/2/3" — no persona name. **FAIL:** page 27 states *"This is the harshest of the three readings, at EGP 97.72"* while Expert 3 reads **89.43** and C.5 on the next page says *"The panel spans EGP 89.43 to 176.13"* (`docx_swdy.py:1604`, a typed claim about a computed number). **FAIL:** Expert 1's worked table runs EBIT 49,293 − net interest 1,056 → *"After tax and minority interests — earnings per share 15.87"*; the printed lines reach 15.37, so a line the computation uses is not printed. |
| 17 | The central contested judgement computed both ways — standard 8 | **PASS** | The currency of discounting: EGP 87.94 primary against EGP **79.09** on the hard-currency leg, published side by side in the summary table (page 2), the body (§1.8, §4, §7), the workbook (`Fundamental Valuation`, live formula for the USD cost of capital) and Expert 3's range (*"the upper bound is the hard-currency discounting case"*, 66.70–112.16). Never averaged. Two further alternatives are priced beside it — the rating-basis cost of capital at 33.67 and the minority sequencing at 99.33. |
| 18 | Standalone bibliography document — standard 1 | **PASS, with two gaps named** | `SWDY_Bibliography_10-09-2026.docx`, 21 pages: a primary-documents table (9 documents, four of them the company's own filings plus the Q1-2026 interim), the FULL input register grouped by layer (**222 rows: Market 1 · Company 168 · House 27 · Country 10 · Industry 2 · and the mixed rings**), a judgements table each with what-would-overturn-it, a negative-results table (now recording the order-book result as *partly answered*), and aggregator-discrepancy notes. **Gaps:** the register's `anchor_days` justification still names a 5 Aug 2026 anchor (row 16 above); and the primary-documents table still does not list the **reviewed H1-2026 statements to 30 June 2026**, which `inputs.emp_share_h1_26`, `nwc_pct` and five sweep findings all cite. |
| 19 | Four-field provenance on every input — standard 2 | **PASS** | **222 inputs**, every one carrying value / source / date / ring, asserted in `attest.py:66` and printed in full. `python3 scripts/check_provenance_fields.py` reports no new violation. |
| 20 | Sweep Register primary-source depth | **FAIL — the register exists and one mandatory category is closed by a false negative** | Company site attempted **and logged either way**: `primary_access[0]` records `elsewedyelectric.com` IR, `reachable: true`, `attempt_date 2026-08-14`, with the note that *"an earlier edition of this study asserted the opposite"*. **3 audited fiscal years** (FY2023-25) against a floor of 2 and a target of 4 — warned, not failed. Every study-year quarter disclosed is swept (Q1-2026 and the reviewed H1-2026). Results releases swept with their statements. **5 findings tagged `COMPANY_IR`**, distinctly from `COMPANY_OFFICIAL` (10). **The failure is N4:** the [R-NEWS-01] announced-plans category is closed by a dated negative search that the study's own registered drivers contradict. |
| 21 | One escalator per driver class, with a sourced near-term cost anchor | **PASS, with one gap named** | Copper on its own LME path (`copper_fcst` 13,400 → 15,453 USD/t, FY2026 set between the realised Q1-2026 average and the forward), the pound on the house PPP ladder, domestic lines on the house CPI, the corporate cost load on its own reversion to the FY2023-24 average, capex on its own taper from the FY2025 peak — no blended index across physically distinct lines. **Gap:** the Cables COST side is not escalated at all; it is a flat margin (next row). |
| 22 | Margins as OUTPUTS wherever the filings disclose enough to build cost per unit | **FAIL** | §1.1 and §1.6 both state *"Margins are therefore OUTPUTS of the build rather than assumptions fed into it"*, and the workbook panel says *"group EBITDA margin is an OUTPUT of it, not an input"*. Only the GROUP margin is an output, as a mix of three INPUTS: `inputs.cables_margin = [0.114341] × 5`, `construct_margin = [0.089874] × 5`, `elecprod_margin = [0.236221] × 5` — each flat for five years and each printed as a driver row on page 10. For Cables — **55.4% of revenue** — the filings disclose the tonnage and the model already carries the copper path, so a conversion cost per tonne is reachable and the margin could be the residual. ELEC, the other listed Egyptian cable maker in this same book, is built that way. |
| 23 | 3y historical + 5y forecast with the full FCFF waterfall shown inline to PV of FCFF | **PASS** | Page 4 runs the whole waterfall inline: Revenue → EBITDA → less D&A → EBIT → NOPAT at 24.5% → add back D&A → less capex → less ΔWC → **FCFF** → forward cost of capital → discount factor → **present value of FCFF**, five years. Checked by hand on FY2026E: 370,194 → 38,286 → 34,325 → 25,916 → 29,877 → 16,645 → **−319**; × 0.7896 = **−252**. Appendix A.1 carries FY2023-25 actual and FY2026E-30E forecast; A.2 the balance sheet; A.3 the forecast balance sheet and cash-flow markers. |
| 24 | The valuation on one page [R-DCF-01] | **PASS** | Page 5: sum of present values 23,195 + PV of the terminal 195,998 = **enterprise value 219,193**; less net debt 20,560, plus associates 6,758, less minorities 19,872, less the employees' statutory share 22,620 = **equity value 162,899**; ÷ 2,141 = 76.09; rolled = **87.94**. Every figure read from the committed numbers; the bridge foots to the pound and `engine/dcf_table.py` raises if it does not. |
| 25 | Sensitivity grids centred on the adopted case [R-SENS-01] | **PASS** | Page 5's grid steps `terminal WACC 13.34–17.34%` × `terminal g 7.14–11.14%` with the **centre cell 87.94 = the study's own central**, both axes stepping around the struck values. `sens.grid_wacc_g[2][2]` reproduces the headline. The second grid (explicit × terminal cost of capital) centres on 87.94 likewise. |
| 26 | v2 WACC — country risk once, marginal Kd, market-value weights, both ERP bases | **PASS, with one gap named** | `rf_observed 0.2231` less `default_spread 0.034` on the SAME (CDS) basis = `rf_star 0.1891`; the premium is added back through the CRP inside the ERP, so the sovereign enters once. `weight_equity 0.9312` on market capitalisation of 278,301. Kd 9.5% adopted against an independently computed FY2025 effective of **9.8%** (interest 5,967 over the average of 58,797 and 62,462) — inside 34bp of the bound and under the 50bp peak test, with the currency composition established FIRST from the FY2025 interest-rate note (21.3% on pound liabilities, 5.3% on the blended hard-currency book, back-solving to roughly 28%/72%) per [R-COC-01 AMENDED 08-09]. Both ERP bases published: CDS 9.41% primary and rating 13.9% as an alternative worth EGP 33.67. **Gap:** `rf_terminal 10.50%` sits **200bp below** the house path's derived 12.50%, recorded in `rf_terminal_note` as a lever not yet taken rather than conformed. |
| 27 | Every non-sourced driver logged to the Driver Ledger | **FAIL** | `engine/Fundamental_Driver_Ledger.md` carries no SWDY entry. The study registers House-ring drivers that are judgements — `ev_ebitda_just 6.5`, `pe_just 9.0`, `roe_sust 0.235`, `g_term_real 0.020`, `kd_term 0.0888`, `erp_term 0.07`, the 25% payout, the 15% terminal debt weight — each argued in its own register entry and none logged to the ledger. |
| 28 | The walk-forward scope decision stated in the study [R-FCAL-01] | **FAIL** | `python3 scripts/check_walkforward_scope.py` → *"NO DECISION (15): … SWDY …"*. `"scope"` appears 0 times in `study_numbers.json`. In substance §7 states the run — *"rebuilt at twelve past year-ends … 750 driver-years in all"* — and publishes years 3-5 as ranges with the basis and count per cell (*"percentile (9)"*, *"span (8)"*, *"span (7)"*) and the orientation declared (*"a low of 0.55 and a high of 2.60 means the outturn has landed between 0.55 and 2.60 times what this method projected"*), which [R-FCAL-01 AMENDED 07-09] asks for. The ruling itself — FULL / LIGHT / SKIP in the protocol's own words — is committed nowhere. |
| 29 | **The answer, not only the process [R-GAP-01]** | **PASS on the artefact, and the study is HELD** | Committed central **87.94253** against a committed spot of **130.00** (2026-09-03) = **−32.35%**; against the latest known **136.20** (2026-09-06) = **−35.4%**. More than 10% below, so a review is owed: `GAP_REVIEW_13-09-2026.md` exists and covers all eight headings — LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK. `python3 scripts/check_valuation_gap.py` → *"OK — no new violations"*. The study is stamped `2026.09.10` = the live `research_protocol.STANDARD_VERSION`. **Standing consequence:** `python3 scripts/check_publish_block.py` → *"HELD SWDY — the central is −35.4% below the price of 136.20 (2026-09-06), past the 10% publication limit — and no market dissent is filed"*; there is no `MARKET_DISSENT_*.md` and no `PUBLISH_AUTHORISATION_*.md` in the directory. |
| 30 | Bridge on the latest disclosed balance sheet [R-BRIDGE-01] | **FAIL, ratcheted** | `python3 scripts/check_bridge.py` → *"BRIDGE FAIL — SWDY: the bridge stands on the 2025-12-31 balance sheet while the latest disclosed is **2026-06-30** (the study's own committed input register)"*. The study reads that reviewed half for segment revenue, margins, capex, net working capital, the employees' share and five sweep findings — and subtracts the December lines. |
| 31 | Forecast anchor [R-ANCHOR-01] | **FAIL, ratcheted** | `python3 scripts/check_forecast_anchor.py` → *"ANCHOR FAIL — SWDY: the forecast opens at 0.1234 against a latest reviewed 0.1400 — 1.65 points below it, **11.8% RELATIVE** — and names no mechanism"*. Above the borrowed 5% threshold, and the closed mechanism list is not invoked. |
| 32 | Macro coherence [R-MACRO-01] | **FAIL, ratcheted — and the study's own record contradicts its §7** | `python3 scripts/check_macro_coherence.py` → *"MACRO COHERENCE FAIL — SWDY (EG): the explicit window ends with growth at 11.37% against a terminal of 9.14% — 2.2pp apart"*. Deliberately left open per the brief. The `market` field is now populated so the check runs at all, which it could not before. The inflation-inputs block declares one key, `unit_price_inflation`, exempted as retired and read by no formula; the currency path derives from `macro_path` with `fx_base 47.69` as a dated observed scalar and `fx_path [53.97, 58.97, 62.71, 65.77, 68.66]`. `anchor_staleness_accepted` is declared with a reason (28 days against a 14-day bound), so `scripts/check_macro_anchor_age.py` does not list SWDY. **See N1 — the terminal real growth the record states is 2.0% and §7 tells the reader it is zero.** |
| 33 | Terminal built through the sanctioned module [R-TERM-01] | **PASS on construction, FAIL on the gate's own key** | `dcf.terminal_record` is complete and reproduces: `inflation 0.07 × real_growth 0.02 → nominal 0.0914` by the exact Fisher identity [R-MACRO-02], maintenance at current cost on a **17.2627-year** life derived by identity from note 17 with the disclosure quoted, `payout_of_nopat 0.5972`, `tv 493,871.53` reproducing the published terminal to 1e-6. `python3 scripts/check_terminal_floor.py` passes SWDY. **FAIL:** `python3 scripts/check_terminal_module_use.py` → *"SWDY module — no record committed"* — the record is filed under a key that gate does not read. |
| 34 | EPS reconciliation [R-EPS-01] | **PASS** | `python3 scripts/check_eps_reconciliation.py` → *"ok SWDY FY2025 +13.5% gap, NAMED: the employees' statutory share of distributable profits"*. The study registers the company's own reported EPS 7.13 beside an attributable 8.10 and prices both P/E readings (18.2× and 16.1×) on page 7. |
| 35 | Rebuild ledger [R-REBUILD-01] | **PASS, with the bundle named** | `python3 scripts/check_rebuild_ledger.py` → *"SWDY 7 lever(s), 6 rule(s), cumulative +58.5%"*, walkable end to end. Lever 7 is the currency-path fix, **87.76333 → 87.94253**, naming [R-MACRO-01]. Lever 6 is the declared bundle — *"UNRECORDED: the levers between the beta re-derivation and …"*, **43.510763 → 87.76333, +101.7%** — the largest move in the ledger and, as the brief states, a bundle that says so. |
| 36 | Output records [R-ENF-05] | **PASS** | `python3 scripts/check_output_records.py` → *"OK — no new violations"*. The reverse read lives in the diagnostics record and no builder reads it back in. |
| 37 | The workbook's inputs are live | **PASS, with 6 labelled references** | `python3 scripts/check_workbook_live_inputs.py` → *"SWDY — 6 input cell(s) no formula reads"*, each labelled on the page as a disclosed reference and passed on that basis. `driver_test.py` in the sandbox: *"DRIVER TEST OK — 17 drivers each reprice the workbook in the right direction … none — every remaining driver reprices the model"*. |
| 38 | The delivered workbook PDF | **FAIL** | `SWDY_Valuation_Model_10092026_public.pdf`, 52 pages. Scored page by page on extracted text: **7 pages carry 20 or more lines of which more than 85% are pure numerals with no row label** — pages **10, 11, 13, 21, 26, 39, 45**. Page 10 read as a rendered image: four column headers (FY27E–FY30E) over 68 numbers and **not one row label on the page**. |
| 39 | Prose figures and table footing | **PASS** | `python3 scripts/check_prose_figures.py` → *"SWDY prose figures checked: 662; unmatched: 0"*. `python3 scripts/check_table_footing.py` → *"SWDY tables examined: 47; unreconciled totals: 0"*. Both re-run in the sandbox with the same result. |
| 40 | Waterfall assertions [R-ENF-01 04-Sep] | **FAIL, ratcheted** | `python3 scripts/check_waterfall_assertions.py` → *"SWDY **NO IMPORT (ratchet)** … 10 waterfall table(s); first says 'Less depreciation and amortisation'"*. The study instructs a reader down ten tables and asserts none of them at build time. The consequence is measurable — see N9. |
| 41 | This gate's own side effect, recorded rather than hidden | **Stated exception** | Running `sweep.py` in the working tree to exercise its validator rewrote `sweep_register.json`; the file is **byte-identical** afterwards (`md5 3b7a9a725f10c930d8636c1efae252bf` before and after), and `git status --porcelain engine/swdy_study/` is clean. Everything else — `attest.py`, `recalc.py`, `build_xlsx_swdy.py`, `qc_checks.py`, `driver_test.py`, `prose_check.py`, `footing_check.py` — was run inside a `git archive` sandbox, never in the tree. |

---

## What is new, and what made it

| # | Defect | Evidence | Made by |
|---|---|---|---|
| **N1** | **§7 tells the reader the terminal assumes no real growth. The record says 2.0%.** Page 22, bold: *"**Terminal growth of 9% is EXACTLY zero in real terms.** The terminal rate embeds 7% inflation and the terminal growth rate is DERIVED from it and a stated real growth of zero … A 9% nominal terminal growth rate assumes the company stops growing in real terms forever … and the **6% and 7% columns** of the growth grid are not aggressive."* | `dcf.terminal_record.real_growth = 0.02`; `inputs.g_term_real = 0.020` whose own source string says *"STATED real terminal growth: 2.0% … **THE RETIRED FIGURE WAS ZERO**"*. 7% inflation with zero real growth gives **7.00%**, not 9.14%. The grid runs **7.14%–11.14%**; there is no 6% column. `compute.py:1166` still carries the stale comment *"g_term above reads (1 + 0.0 real growth) × (1 + 7.0% …)"*. Four false statements in one bullet, on the leg carrying **89.4%** of enterprise value, in the section whose job is to state the caveats. | real growth moved 0 → 2.0% and the prose did not |
| **N2** | **§2 misdates the 52-week high.** Page 16: *"52-week high (closing basis) \| **130.00** \| +0.0% from the high — the high IS the +14.1% print of **4 August 2026, one session before the anchor**"*. | `docx_swdy.py:1034-1035` — the level is computed (`hi52 = max(px[-252:])`) and the sentence beside it is typed. On the frame `price_series.frame()` returns, the 52-week closing high is **130.00 on 2026-09-03** — the anchor date's own close. 4 August closed **109.02**, twenty sessions earlier. | the price-frame repair (fix 1) |
| **N3** | **Figure 3's caption quotes a different grid's maximum.** *"…the grid tops out at 143.24."* | `figures.py:78` draws `fig2_sens.png` from `sens_wg` = `grid_wacc_g`, **max 221.10**. `docx_swdy.py:915` computes the caption from `grid_exp_term`, **max 143.24**. The cell count (4 of 25) is right for both by coincidence. | the caption repair (fix 9) |
| **N4** | **The sweep register asserts an absence the study contradicts.** The [R-NEWS-01] mandatory Company-ring category is closed by `F27`, a `NEGATIVE_SEARCH` dated 2026-09-10: *"Nothing found that clears the entity test, so no capacity addition is modelled."* | `compute.py:1925` — *"THE THREE ANNOUNCED PLANTS … On 24 June 2026 the company announced a copper-recycling complex (US$80mn, 20,000 t/yr), a copper-tube plant (US$65mn, 15,000 t/yr) and an aluminium-rod line (US$55mn, 50,000 t/yr) … about US$200mn"*, argued to be inside the capital path. `compute.py:910` registers the **Datagrid / Transpower New Zealand** 360 MVA transformer order as evidence behind `elecprod_growth`; [R-NEWS-01]'s own text names that order as verified as the listed company's. `grep -c Datagrid sweep_register.json` → **0**. The rule's own words: *"a negative search is a search somebody ACTUALLY RAN, so inventing one to clear a coverage check is worse than the gap it clears."* | the new sweep register (fix 13) |
| **N5** | **A repository path reached a reader's page and turned a CI gate red.** | `scripts/check_delivered_vocabulary.py` → *"FAIL — 1 new leak(s): SWDY / SWDY_Bibliography_10-09-2026.docx — a repository path: engine/raw_ohlc/EG/SWDY.csv"*. Introduced by `47aca929d`; closed by `eb3d327f8`, after the audited commit. | the bibliography repair (fix 1) |
| **N6** | **Three places say the model charges the full Egyptian equity premium. It charges 77% of it.** §4 page 19: *"Charging it the full Egyptian equity risk premium — which is what our primary construction does"*; §7 page 22: *"Our primary construction charges the full Egyptian equity risk premium"*; workbook `READ FIRST!A50`: *"The primary model charges the full Egyptian cost of capital."* | `cost_of_capital_record.lambda_country = 0.5928`, `crp 0.05168`, `crp_foreign 0.0226`, `crp_effective = 0.039839` — **3.98pp of a full 5.17pp**. The record's own note says *"lambda is 0.5928 because that share of this company's revenue is earned in Egypt"*. | the [R-COC-03] premium split |
| **N7** | **The retired blend's weighting language survives in three places.** Page 6: *"This is the weakest of the four lenses … and **carries the lowest weight**"*; page 7: *"…is a sanity check on the cash-flow model rather than an independent valuation, and **it is weighted accordingly**"*; page 25: *"That is why **the relative lens carries a fifth of the weight** and not more."* | [R-LENS-03] retires the typed blend; the summary table on page 2 says in terms *"THE CENTRAL — the cash-flow lens, not an average"* and *"NOT AVERAGED — the retired blend, published unused"*. Three sentences tell the same reader the answer is weighted, and one of them states the weight. | carried, unfixed |
| **N8** | **A lens is labelled "not published for this class" in the row that publishes it.** Figure 1 (page 8) draws a Normalised earnings power bar labelled *"not published for this class"*; the table beneath it prints **79.91 / 103.30 / 132.54** with a Role of *"not published for this class"* and a vs-price of **−21%**; the summary table on page 2 publishes 103.30; §1.4 runs a full worked table. | `lens_record` declares **2 cross-checks** and `check_lens_design.py` reports *"SWDY primary dcf, 2 cross-checks"*, while the document publishes three beside the primary. `LENS_REGISTRY` for this class does not admit `normalized`, which is [L-296] exactly. The record conforms and the document does not. | carried, sharpened by the class registry |
| **N9** | **The §1.1 bridge's printed steps do not reach its printed answer.** Page 6: `162,899` → `EGP 76.09 per share` → `Rolled 246/365 of a year to the anchor ×1.1816` → `Less the FY2025 dividend paid in the window (1.85/sh)` → `Fair value per share at the anchor **87.94**`. | 76.09346801 × 1.18157726 = **89.91031**; less 1.85 = **88.06031**, which is **0.1178 per share away** from the printed 87.94253. The model deducts `dcf.div_at_anchor = 1.9677813` — the dividend rolled 91 days — not the 1.85 the page prints. The same 0.12 appears in §7's *"worth about **+13.70** gross on the primary lens"* (`docx_swdy.py:1291` computes `ps − ps_dec + dps_fy25`) against a true gross accretion of **13.8168**. | carried, unfixed; row 40 is why nothing caught it |

Beneath those, carried and unfixed from the last gate: *"This is the harshest of the three
readings, at EGP 97.72"* against an Expert 3 of 89.43 (row 16); *"An earlier edition did settle
it that way and reported EGP 86.16"* — **no delivered edition published 86.16**; the 05-08
edition's number is the 69.73 still on the site (`docx_swdy.py:565` prints this edition's own
`retired_blend_value`); Expert 1's worked table omitting the line that reconciles it (row 16);
and the segment margins as inputs under a sentence saying margins are outputs (row 22).

---

## What the filings disclose that the model does not consume

Asked explicitly against the Sweep Register [L-006], which for the first time exists.

**1. Cost per tonne for Cables — 55.4% of revenue, and the largest one left.** Revenue is now
built as tonnes × price and the workbook computes it live. Cost is not: `cables_margin` is
**11.4341% flat for five years**. The tonnage is disclosed in the releases the register now
logs, the copper path is registered (`copper_fcst` 13,400 → 15,453 USD/t) and the pound path
is derived — a conversion cost per tonne is reachable from what the study already holds, and
would make the margin the output the document twice says it already is.

**2. The reviewed balance sheet at 30 June 2026.** Read for segment revenue, margins, capex,
working capital, the employees' share and five separate sweep findings; the bridge still
subtracts the 31 December 2025 lines. `scripts/check_bridge.py` names it.

**3. The three plants announced 24 June 2026 and the Datagrid / Transpower order.** Both are in
the study's own model record; neither is in the sweep register, which instead records that
nothing was found. N4.

**4. The engineering backlog — EGP 293bn at December 2025, EGP 346bn at 30 June 2026.** Read
and explicitly NOT burnt down, because the releases do not disclose the burn profile. A
declared, reasoned exclusion, and the right one.

**5. The full Note 5-2 geographic split.** Consumed as a single scalar, `lambda_country =
0.5928`. On a valuation whose own §7 calls country risk *"the biggest single question"*, the
per-country detail behind that weight enters nothing.

**6. A fourth audited fiscal year.** The register carries three (FY2023-25) against a target of
four and warns on it. FY2022 figures are quoted in the comparatives of the FY2023 statements
the study already holds.

---

## What is genuinely strong here, and should not be lost in a rebuild

The workbook reproduces **cell for cell across 1,564 populated cells** against a clean
`git archive` rebuild, **630 of 630 formulas evaluate with zero errors under LibreOffice** and
**630 of 630 committed expected values reproduce to 1e-6** under an evaluator the study does
not own. The free-cash-flow waterfall foots by hand on every year and reaches present value
inline; the one-page valuation table [R-DCF-01] foots from present values to value per share
and raises if it does not; the sensitivity grid is centred on the struck case and its centre
cell IS the published central. The beta is on the sanctioned route against the published EGX30
with `assert_beta_provenance()` genuinely called before the record is written, and the
correction it forced moves the answer **further below** a price it already sits far below. The
cost of equity now reproduces under a construction named from a closed list, with the country
premium charged once and never multiplied by beta. The terminal stands on a life derived by
identity from the audited note, reproduces to 1e-6, and its nominal rate is the exact Fisher
product of a house inflation and a stated real growth. The employees' statutory share is found,
measured, charged and reconciled to the company's own reported earnings per share. **Five of
the six things this pass was asked to verify are real and two are exemplary:** `price_series.py`
refuses to run if the library's close on the valuation date is not the study's spot, and
`attest.py` sets every checklist field from a file, a count or a number rather than from a
conviction — including `competitors=False` with its reason attached, which is the opposite of
the self-certification SIGCM exists to stop.

---

*Audited from outside the study. Pinned to `b290bb3f7`; HEAD advanced to `eb3d327f8` during
the audit and every finding was re-verified against the pinned bytes. No file in the study was
modified.*
