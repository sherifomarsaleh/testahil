# SWDY — QC gate, 09-09-2026

Elsewedy Electric Company S.A.E. · Egyptian Exchange: SWDY · EGP · edition of 09-09-2026

Audited from OUTSIDE the study per [R-ENF-02] by a reader who did not build it. Every row
names the artefact, the command or the number that carries it. Nothing here is
self-certified, and where a row fails it says so plainly.

**VERDICT: NOT DELIVERABLE.** Fifteen hard fails, listed at the foot. The thing this
edition was rebuilt to fix IS fixed — the retired four-lens blend at EGP 69.73 appears in
no artefact of this edition, the cash-flow lens is the central at EGP 43.51, and the
workbook publishes it. What fails is everything downstream of the last strike that was not
regenerated with it. **Four of the eight figures are from a superseded strike**, including
the football field, which shows a central of 53 where the study publishes 43.51. **The
bridge in §1.1 does not foot** — 116,164 − 20,560 + 6,758 − 9,904 = 92,458 against a
printed 81,185; the missing EGP 11,273mn is the employees' statutory profit share, and the
whole of §1.9 is computed on the construction that omits it, so every sensitivity in the
study is centred on 49.71 for a valuation of 43.51. **The beta is attributed in two
delivered documents to the equal-weight composite the study withdrew**, while
`beta_result.json` records the sanctioned EGX30 regression. **§2 and §3 are anchored on the
5-Aug price of 105.20** in a study struck at 130.00 on 3-Sep, so a column headed
"Probability above spot" prints 59% where the true figure against the stated spot is 18%.
And the **bibliography is the previous edition's document under this edition's filename** —
it says so in its own subtitle — with twelve of the study's 193 registered inputs missing
from its register.

---

## The edition audited, and how it was resolved

The edition date comes from `edition.py`, which derives every filename from one `EDITION`
constant. Nine of the study's 33 Python files import it. The glob was resolved by date and
asserted non-empty before anything was opened.

| Deliverable | File | Also on disk |
|---|---|---|
| Study, Word | `SWDY_Valuation_Study_09-09-2026_public.docx` — 37 tables | 05-08-2026 |
| Study, PDF | `SWDY_Valuation_Study_09-09-2026_public.pdf` — **27 pages, 8 figures**, read as rendered images | 05-08-2026 |
| Workbook | `SWDY_Valuation_Model_09092026_public.xlsx` — 16 sheets, 594 formulas, 1,527 populated cells | 05-08-2026 |
| Bibliography | `SWDY_Bibliography_09-09-2026.docx` / `.pdf` — 16 pages, 9 tables, 181-row register | 05-08-2026 |
| Gap review | `GAP_REVIEW_08-09-2026.md` — 156 lines, eight headings + verdict | 04-09, 06-09, 07-09 |

`recalc.py:27` and `driver_test.py:17` both open **`SWDY_Valuation_Model_05082026_public.xlsx`**
by name — the superseded workbook, not the delivered one. L-066/L-067 again. It is
survivable only by accident: the two files are byte-for-byte identical across all 1,527
populated cells, which is itself the second finding — the engine copy of the 05-08 delivered
workbook has been overwritten with this edition's numbers, against `edition.py`'s own
docstring ("rebuilding over its filename destroys the only record of what was said"). The
genuine 05-08 artefacts survive in `files/` (different md5), where the delivered document
does publish EGP 69.73 against a spot of 105.20.

## Derivations stated before the audit, and the evidence for each

| Derived | Value | Evidence |
|---|---|---|
| Ticker and exchange | SWDY · EGX · market code EG | `assets/data.js` `code: "EGX:SWDY"`, read through a real `node` parse of the rendered object, never a regex [R-ENF-03]. |
| Regressor the beta rule demands | **EGX30**, `engine/raw_indices/EG/EGX30.csv` | `wacc_builder.EXCHANGE_INDEX[('EG','EGX')]` → `EGX30`; registered and present. No interim note applies — not a DFM name, not Qatari. |
| Class | Diversified industrial with a contracting arm | `lessons_register.CLASSES`; `study_numbers.json.lens_record.class`. `meta.klass` = "diversified industrial operating company". Nearest reference pattern is ADNOCLS (operating company) inside ADNOCLS's skeleton; the study matches the 16-section / 16-sheet standard exactly. |
| Committed central | **EGP 43.510763** | `study_numbers.json.central`; `lens_record.primary.kind = "dcf"`, `value = 43.5108`. |
| Committed span | **16.1816 – 83.7988** | `study_numbers.json.span` — the cash-flow lens under its own two scenarios, not a spread across four methods. |
| Committed spot | **EGP 130.00, close of 2026-09-03** | `meta.spot` / `spot_date`. |
| Gap on the struck spot | **−66.5%** | 43.5108 / 130.00 − 1. |
| Gap on the latest known price | **−68.1%** | `scripts/check_valuation_gap.py` → `SWDY −68.1% GAP_REVIEW_08-09-2026.md (audits 43.5108)`; `SWDY: struck at 130.00, latest known 136.20 (2026-09-06)`. |
| What the site publishes today | fair `{bear 19.95, base 69.73, full 138.73}`, spot 136.20 | Real `node` parse of `assets/data.js` line 639. **Stated exception, not a defect** — see row 40. |
| Retired blend, recomputed | **EGP 60.784** | `study_numbers.json.retired_blend_value`. Published as a labelled unused row; it is NOT the 69.73 the 05-08 edition delivered, and the study says "an earlier edition did settle it that way and reported EGP 60.78" — see row 12. |

---

## The gate

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | The three gates called in the study's own committed code [R-ENF-02] | **FAIL — the repo gate passes it on a docstring** | `grep -n "assert_sigcm\|assert_model_study\|assert_ground_up" engine/swdy_study/*.py` returns **zero matches**. The only `research_protocol` import in the directory is `bridge_record.py:57`, which calls `RP.assert_bridge()` — not one of the three. `check_study_provenance.audit('engine/swdy_study')` returns `(True, [])` — and it returns True because `GATE_CALLS` is a substring test and `beta_reg.py:29` contains the words *"so **assert_beta_provenance()** can inspect the record rather than trust a boolean"* **inside its module docstring**. A gate satisfied by prose is a gate that did not run. This is the exact shape [R-ENF-02] exists to close, arriving one level up. |
| 2 | Beta on its record, never on a boolean — SIGCM clause 6, [R-BETA-04] | **PASS on the record** | `beta_result.json`: `beta 1.2249065740, r2 0.36823, n 256, se 0.169855, ci90 [0.9455, 1.5043], usable true, gate_msg "passes minimum usability gate", dimson true, week_rule W-THU, index_file **raw_indices/EG/EGX30.csv**, index_asof 2026-09-08, conforming true, interim_note None`. `beta_reg.py` hand-rolls nothing: it calls `beta_regression.own_stock_beta('SWDY','EG','EGX')` and asserts `conforming` and `index_file.startswith('raw_indices/')` before writing. The withdrawn composite is kept beside it (`withdrawn_composite`: 1.0087 / R² 0.291 / n 258) with the correction measured at **+21.4%**, and the study states that correcting it moved the answer **further below** a price it already sat far below. The usability gate passes on all three conditions: n = 256 ≥ 24, R² = 0.368 ≥ 0.05, SE 0.170 < 1.225. |
| 2a | What the two delivered documents say the regressor was | **HARD FAIL — both name the construction the study withdrew** | Study §1.8, beta row, verbatim: *"own-stock weekly regression against a **31-name equal-weight local composite** over five years: R-squared 0.368, n = 256, standard error 0.170, 90% interval [0.95, 1.50]"* (`docx_swdy.py:629`, hard-coded string). Every statistic in that sentence belongs to the **EGX30** regression; the composite's own figures are 1.0087 / 0.291 / 258 / 0.0983. The bibliography's primary-documents table, last row, is worse: *"Daily price history for the covered Egyptian equity library · House data library · to August 2026 · **The 31-name equal-weight composite used as the market proxy in the beta regression**"*. `engine/raw_indices/EG/EGX30.csv` appears in **neither delivered document**. The study did the right regression and then told its reader it did the SIGCM clause-6 hard fail. |
| 3 | Ground-up clause on a driver record, not a flag [R-SIGCM-02] | **FAIL — no record exists** | `"ground_up"` occurs **0 times** in `study_numbers.json`, so `assert_ground_up()` has no `DriverLine` list to run on. `engine/build_depth_audit/ground_up_outstanding.json`: `"SWDY": "commits no driver-line record at all; [R-SIGCM-02] retired the boolean that used to stand in for one"`. **Share-by-level, reconstructed from `bottomup` and `seg_fy25`: `derived` on 100% of revenue — Cables 55.4%, Constructions and infrastructure 32.4%, Electrical products and digital 12.2%. `unit`: 0%.** Segment revenue growth × segment margin, no tonne anywhere. |
| 3a | The gap flag SIGCM clause 2 requires | **PASS** | §7, first bullet: *"No order book, backlog or unit-volume figure is disclosed in the audited statements … no tonnage, MVA, meter count or order-book figure for any segment … An earlier wording said 'disclosed anywhere', which overclaimed — the scope of the negative result is the audited statements."* Repeated in Appendix B.3 and in the bibliography's negative-results table with what was searched for. The narrowing is itself honest and correct — see row 47. |
| 4 | SIGCM clause 1 — historicals from official sources only | **PASS on the numbers, FAIL on the register** | Every FY2023–FY2025 statement line is sourced to the company's own audited consolidated statements in `study_numbers.json.inputs`; `scripts/check_source_integrity.py` reports no SWDY breach. The bibliography's own note states *"no aggregator or press figure remains in the model"* and records the one discovered discrepancy (a syndicated "FY2025" balance sheet that reproduced the audited 31-Dec-2024 figures exactly, one year stale, discarded). **But there is no `filings/` directory in this study** — the primary documents are cited, not held — and there is no Sweep Register to log the access either way (row 32). |
| 5 | Numeric traceability — depth-bar standard 3 | **PASS, independently re-run against the delivered file** | `recalc.py` as committed opens the 05-08 name. Re-pointed at the delivered workbook in a sandbox: **"RECALC OK — 594 formulas, 0 unresolvable, 594 cell-level agreements with the model, 41 headline reconciliations passed"**, including `Summary weighted central: workbook=43.5108 model=43.5108`, `Summary terminal value share: workbook=0.8209`, `Fundamental — currency alternative: workbook=57.8610`. `driver_test.py` re-pointed the same way: **"DRIVER TEST OK — 17 drivers each reprice the workbook in the right direction"**, with an explicit dead-input sweep reporting none. `scripts/check_workbook_values.py` lists SWDY among the 22 CLEAN. Nothing unparseable, so nothing skipped. |
| 6 | Script-built file verified by a cell-by-cell diff against the delivered file | **PASS** | The whole study directory was copied to a sandbox and `build_xlsx_swdy.py` re-run there, so the real tree was never written to. Diffing the rebuilt workbook against the delivered one across every populated cell of all 16 sheets: **1,527 cells compared, 0 differences.** The delivered 05-08-named workbook diffs to the same 1,527 cells with 0 differences, which is how the row-above mis-pointing survives. |
| 7 | **Figure 1 — the football field** | **HARD FAIL — a superseded strike, shipped** | Read as a rendered image on page 7 and confirmed against `fig1_football.png` on disk (mtime 2026-09-07 17:45; `compute.py` 2026-09-09 07:32, documents 11:34). The bars read **"20–101 · base 53"** (FCFF DCF, primary), **"59–90 · base 72"** (relative), 76–126 · base 98 (normalised), **"36–63 · base 56"** (book), and a fifth bar labelled **"Weighted central" at "20–101 · base 53"**. `study_numbers.json.lenses` holds dcf 16.18/**43.51**/83.80, relative 58.20/**70.15**/88.09, book 33.36/**49.21**/55.40, and no weighted central at all. **The study's headline figure tells a reader the cash-flow lens is worth 53 where the study publishes 43.51 — 22% apart — and prints the retired construction as a bar.** `figures.py:34-61` reads `d['lenses']` correctly; it was simply never re-run. |
| 8 | **Figure 3 — the terminal-rate × growth heat map** | **HARD FAIL, two ways** | (i) The title reads **"DCF fair value (EGP/share) — terminal cost of capital × terminal growth; bold ≈ spot 130"**. **No cell is bold and the largest cell is 81** against a spot of 130 — a title promising a highlight that cannot exist. Its own caption, four lines below, says the opposite and is correct: *"No cell in the tested range reaches the market price of 130.00."* (ii) The figure's axis runs terminal cost of capital 13.9%–17.9% with cells 43–81; the committed grid is `sens.wt_grid` 15.2%–19.2% with `sens.grid_wacc_g` running 37.7–64.6, and the delivered Table 17 immediately beneath prints **that** grid. Two grids for one quantity on one page, and the figure is the superseded one. |
| 9 | **Figure 5 — the forward price cone** | **HARD FAIL, three ways** | (i) The cone begins at **105.20** — `strike_result.json`: `anchor_date "2026-08-05"`, `spot 105.20` — in a study struck at 130.00 on 2026-09-03. (ii) The dotted line is labelled **"spot 130.00"** and sits 24 points above the cone's own origin, so the figure draws a spot the simulation was never run from. (iii) The in-figure dashed label reads **"fundamental central ≈ 53"** while the caption four lines below reads *"The dashed brass line is the fundamental central estimate of **43.51**"*. Caption and figure disagree on the study's own answer. |
| 10 | **Figure 8 — the expert panel** | **HARD FAIL — superseded, and the caption contradicts it** | Bars read **"111–192 · base 151"**, **"37–67 · base 74"**, **"48–71 · base 55"**. The document publishes Expert 1 112.29–193.82 base **153.05**, Expert 2 34.12–59.75 base **65.48**, Expert 3 42.23–57.86 base **45.29**. The caption reads *"The gold band is the panel centre of 65.48"*; the gold band in the figure is drawn at about **75**. |
| 10a | Figure discipline — the programmatic half, standard 5 | **PASS** | `python3 qc_checks.py`: all eight PNGs `transparent=False`, corner luminance `[248, 248, 248, 248]` on every one — solid light canvas, zero transparency, measured. **Every figure was also inspected as a rendered image, which is how rows 7–10 were found and is the entire reason that is a gate.** Figures 2, 4, 6 and 7 are current and legible; no label collisions. |
| 11 | **The enterprise-to-equity bridge foots** [R-BRIDGE-01] | **HARD FAIL — EGP 11,273mn missing from the printed bridge** | Delivered Table 5, in order: Enterprise value **116,164**; Less net bank debt **(20,560)**; Plus equity-accounted investees **6,758**; Less minority interests **(9,904)**; Equity attributable at 31 December 2025 **81,185**. 116,164 − 20,560 + 6,758 − 9,904 = **92,458**. The printed subtotal is **11,273 lower**, and `study_numbers.json.dcf` carries the same non-footing set (`ev 116164.186, nd 20560.003, assoc 6757.651, nci_val 9903.541, eq_attr 81185.112`). The missing line is `emp_charge` at `compute.py:1427` — the **employees' statutory share of profit, 12.19% of parent equity value, EGP 11,273mn = EGP 5.27 a share**. It is a real and well-defended charge: registered with four-field provenance (`emp_share_fy24/fy25/h1_26`, note 39 and note 38), named in §1.3's as-reported P/E row, named in Appendix A.1's caption, and named in `lens_record.cross_checks[0].value_adjustment` for the relative lens. **It is not in the bridge a reader is given, so 81,185 cannot be reproduced from anything printed.** `footing_check.py` reports "42 tables examined, 0 unreconciled totals" and does not see it. |
| 12 | **§1.9's sensitivities do not reproduce the central** | **HARD FAIL — the whole risk display is 14% high** | `dcf_at()` (`compute.py:1743-1751`) returns `to_anchor(((_ev − nd + assoc) × (1 − nci_share)) / SH)` — **no employees' charge**. Evaluated at the adopted inputs (explicit 28.8359%, terminal 17.2189%, g 7.00%) it returns **49.7076** against a published central of **43.5108**. Every §1.9 artefact stands on it: Table 17's own base cell (`terminal 17.2%` × `28.8%`) prints **49.71**; Table 18's "Terminal growth 3% – 7% | 45.05 – 49.71" puts the adopted 7% at 49.71; the beta row spans 46.50–94.47 with the adopted 1.225 near 48; and Figure 3 is the same function on a superseded grid. Appendix B.2's risk register then quotes those swings back to the reader ("the explicit-against-terminal grid spans 31.41 per share"). A reader stress-testing this valuation is stress-testing a different one. |
| 13 | **§2 and §3 are anchored a month behind the strike** | **HARD FAIL — and one printed probability is impossible against the stated spot** | `strike_result.json` / `study_numbers.json.strike`: `anchor_date "2026-08-05"`, `spot 105.20`. `SWDY_Stock_Price_History.csv` ends **08/05/2026 at 105.20** — the whole price library this study holds stops four weeks before its own anchor. Consequences read off the delivered pages: Table 19 heads §2 *"Last close **130.00** — the anchor for everything in this study"* above 20/50/100/200-session averages of 93.77 / 90.04 / 86.07 / 81.88 computed on the 5-Aug series, and a "52-week high (closing basis) **109.02**" that the stated last close sits **+19.2% above**; Figure 4's chart tops out near 109 and never reaches 130. Table 20 prints horizons **"1 month (to 2026-09-06)"** — already resolved before this edition — and **"3 months (to 2026-11-05)"**, medians 106.68 / 109.91, and a column headed **"Probability above spot" reading 55% / 59%**. Computed directly from `paths_3M.npy` (20,000 paths): **P(above 105.20) = 59.5%** — which is what is printed — and **P(above the stated spot of 130.00) = 18.0%**. Table 21's whole level-touch ladder is likewise ±10% of 105.20. Meanwhile `assets/data.js` carries the current SWDY cone, refit 2026-09-06 off 136.20, with medians 139.29 / 144.55: the up-to-date map existed and the August one shipped. |
| 14 | **The bibliography is the previous edition's document** | **HARD FAIL — it says so itself** | `SWDY_Bibliography_09-09-2026.docx`, first line under the title: *"Companion document to the valuation study **dated 5 August 2026**."* (`docx_register.py:108`, a hard-coded string in a file that imports `edition.py`). Its masthead band carries **no edition line at all**, where the study's reads "Edition of 9 September 2026". Its negative-results section states the items are *"what is still not disclosed anywhere in **the four primary filings**"* — FY2023, FY2024, FY2025 audited and the Q1-2026 interim — while the study's own register cites the **reviewed condensed interim statements for the six months ended 30 June 2026, approved 11 August 2026**, for at least six inputs (payables note 10-3, receivables 9-B, cash 9-E, credit interest 14-B, cost of sales by nature 15-A, employees' share note 38). **That filing appears in neither the primary-documents table nor the negative results.** The bibliography also states *"The study's anchor is the closing price on 5 August 2026"* (row 13's defect, restated as fact). |
| 15 | **The full input register — depth-bar standard 1** | **HARD FAIL — 12 of 193 inputs never reach the reader** | `study_numbers.json.inputs` holds **193** inputs. The delivered register prints **181**: Market 1 + Company 143 + Country 9 + House 28. `docx_register.py:190` filters `v['ring'] == ring` over exactly those four strings, so every input with a hybrid or other ring is silently dropped. **The twelve dropped are the study's most contested drivers**: `cables_margin`, `construct_margin`, `elecprod_margin` (the three segment margins, ring `Company/House`), `tax_eff` 0.245, `kd` 0.095, `corp_load`, `opex_pct`, `copper_hist` and `copper_fcst` (ring `Industry` — a layer the register does not print at all), `fx_hist`, `asset_life_derived` 17.2627 (the terminal's own life), and `electra_mto`. The document's READ FIRST asserts *"Nothing in the study is computed from a figure that does not appear here."* Confirmed by text search: "cables margin", "tax eff", "copper fcst", "asset life derived", "fx hist" all return **0 hits** in the delivered bibliography. This is L-015 exactly — a probe that examined 181 things, found nothing missing, and was never counted against a total held elsewhere. |
| 16 | Four-field provenance on every input — standard 2 | **PASS in the record, FAIL in the delivered document** | Programmatic sweep of all **193** registered inputs for value / source / date / ring: **0 missing fields**. By layer: Company 143, House 28, Country 9, Company/House 7, Industry 2, Market 1, Country/House 1, Company/derived 1, Market/Company 1. The 181 that reach the bibliography carry all four. The 12 in row 15 carry all four and reach nobody. |
| 17 | **Retired-blend language survives in six places** | **FAIL** | The study retires the weighted central explicitly and repeatedly — the summary table's own row reads *"THE CENTRAL — the cash-flow lens, not an average"*, and §1.5 says *"THE CENTRAL DOES NOT SIT BETWEEN THEM"*. Against that: (i) the **headline**, first sentence of the valuation, *"On our primary construction **the four lenses centre at** EGP 43.51 per share"*; (ii) §1.2, *"This is the weakest of the four lenses … and carries **the lowest weight**"*; (iii) §1.3, *"it is a sanity check … and **it is weighted accordingly**"*; (iv) §4's comparison table, whose first row is labelled **"Fundamental (weighted)"** and prints 43.51, which is not the weighted figure; (v) Appendix B.1, *"That is why the relative lens **carries a fifth of the weight** and not more"*; (vi) Figure 1's fifth bar, labelled **"Weighted central"** (`figures.py:37`). |
| 18 | **Expert 2's base sits outside its own range** | **HARD FAIL — the ADNOCLS defect, recurring** | Appendix C.2's worked table ends *"Fair value, rolled to the anchor (EGP per share) **65.48**"* over a banded row *"Range **34.12 – 59.75**"*. The base is **above its own bull bound**. Figure 8 draws it the same way ("37–67 · base 74"). ADNOCLS adopted an assertion in August that every expert range must bracket its own base; this study carries no such assertion and the defect is here. |
| 19 | "The harshest of the three readings" | **FAIL** | C.2, immediately under the table: *"This is the harshest of the three readings, at **EGP 65.48**."* C.3's Expert 3 reads **45.29**, and C.5 states the panel spans *"EGP 45.29 to 153.05"* on the same page. |
| 20 | Far-year forecasts published as ranges [R-FCAL-01] | **PASS, with one mislabel named** | `scripts/check_forward_ranges.py`: **`SWDY prints a far-year range shape A`**. Table 25 carries FY2028E–FY2030E low/point/high with the **basis** named per row (`percentile (9)`, `span (8)`, `span (7)`) and the observation count in its own column, and §7 explains the switch: *"Where nine or more observations exist the band is a tenth-to-ninetieth percentile; below that it is the SPAN of the observations, which is a smaller claim and is labelled as one."* **The gap:** the three rows headed *"Gross profit (EGP mn)"* carry points **56,606 / 62,040 / 67,601**, which are the FY28E/FY29E/FY30E **segment profit** figures from §1.6's own table — not gross profit, which Appendix A.1 leaves blank for every forecast year. |
| 21 | 3 years historical + 5 years forecast, full FCFF waterfall inline to PV of FCFF | **PASS** | Appendix A.1 and A.2 carry FY2023/24/25 audited and FY2026E–FY2030E. §1.1's Table 4 runs every line: Revenue → EBITDA → EBITDA margin → less D&A → EBIT → NOPAT = EBIT×(1−24.5%) → add back D&A → less capital expenditure → less change in working capital → **free cash flow to the firm** → forward cost of capital → discount factor → **present value of FCFF**. Footed by hand on the rendered page: FY2026E 25,171 + 4,625 − 16,281 − 17,783 = **−4,268** against a printed −4,268, × 0.7762 = **−3,313** against −3,313; FY2030E 38,170 + 6,940 − 17,212 − 8,912 = **18,986** against 18,986, × 0.3653 = **6,935** against 6,935; the five present values sum to 20,805 against a printed 20,806. Stopping at FCFF would be a hard fail; it does not stop. |
| 22 | Terminal value share visible to the reader | **PASS** | **82%** (`dcf.tv_share` 0.820893). Printed in the summary table's caption, as its own row in the bridge, in §1.9's preamble and in §7's fourth bullet. Live in the workbook and reconciled: `recalc.py` → `Summary terminal value share: workbook=0.8209 model=0.8209`. |
| 23 | Terminal on a disclosed asset life [R-TERM-01] | **PASS** | `scripts/check_terminal_floor.py`: *"SWDY cycle 14.3 y sits on 1/g of 14.3 **BY COINCIDENCE** — its committed R-TERM-01 record reproduces the published terminal value to 1e-6 on a disclosed life of **17.2627 years**"*, sourced to *"Audited FY2025 consolidated financial statements, note 17 read with the accounting-policies note on depreciation: average depreciable gross cost over the year's own charge, excluding land … and projects under construction"*. Maintenance capital charged at current cost (`maintenance 12,445`, escalator 1.7932 on a mid-life age of 8.63 years), working capital charged (`wc_charge 7,734`), growth capital never charged as g × IC. `tv_vs_floor` +17.8%. |
| 24 | Cost of capital under the v2 method [R-COC-01] | **FAIL on the terminal rate, disclosed and ratcheted** | Country risk once: `rf_observed 0.2231 − default_spread 0.0340 = rf_star 0.1891`, premium added back on the same credit-default-swap basis, with the retired un-netted construction priced beside it (cost of equity 31.8%). Market-value weights (`we_exp 0.9312 / wd_exp 0.0688`), forward rate per explicit year on a glide inherited from the cost-of-debt path, both premium bases published (CDS 9.4% adopted, rating 13.9% priced at EGP 25.23). Marginal Kd 9.5% is **below** the 22.31% sovereign and is defended on three pieces of evidence — the disclosed currency-bucket rates (21.3% EGP / 5.3% hard currency) and an independently computed effective rate of 9.8%, with both bounds shown. **The failure:** `scripts/check_cost_of_capital.py` → *"SWDY the terminal risk-free rate **0.105000** is not the derived **0.125000**"*. The record states this against itself: *"the house Egyptian path derives a terminal risk-free of 12.50% … and this study carries 10.50%, 200bp below it. A lower terminal risk-free … RAISES the value, and 82% of this company's enterprise value sits in the terminal."* Recorded, ratcheted, not corrected. |
| 25 | Ke reproduces from its own inputs [R-COC-02] | **PASS** | `scripts/check_ke_reproduction.py` reads 10 cost-of-capital records and SWDY is among the 8 reproducing; its two outstanding names are FERTIGLOBE and STC. |
| 26 | Macro coherence [R-MACRO-01] | **FAIL, ratcheted** | `scripts/check_macro_coherence.py` → **`SWDY MacroPathError: '' is not a covered market`**. `study_numbers.json.macro_record` carries `path: "EG"` and an `anchor_staleness_accepted` block but **no `market` field**, so the coherence test cannot run at all. The staleness the record does declare is real and disclosed: the strike is 2026-09-03 against a house EG path anchored 2026-08-06, **28 days against a 14-day bound**, accepted deliberately with the reason stated. |
| 27 | One escalator per driver class, with a sourced near-term anchor | **PASS, with one gap named** | No blended index runs across physically distinct lines. Copper is its own driver anchored on the tape (`copper_fcst` USD 13,400/t FY2026 *"between the Q1-2026 average actually realised and the current cash price"*, flat 14,000 thereafter, and §1.6 says why: *"a directional view on the metal would dominate the valuation, and it is carried in the sensitivity instead"*). The currency runs its own path (49.5 → 64.5). Capital expenditure tapers on its own disclosed history (4.7% FY2025 → 3.1%, against 3.1% FY2023 and 3.7% FY2024, with the cost of holding it at the peak priced at EGP 1.8 a share). The corporate cost load glides UP toward the FY2023-24 average and the caption calls that *"the single most conservative choice in the build"*. **The gap:** each of the three segments is escalated on its own revenue driver but its margin is held flat — see row 28. |
| 28 | Margins as OUTPUTS | **FAIL — the group margin is an output, the segment margins are inputs** | §1.1 and §1.6 both state *"Margins are therefore **OUTPUTS** of the build rather than assumptions fed into it."* The registered drivers say otherwise: `cables_margin`, `construct_margin` and `elecprod_margin` are **five-year constant vectors** (11.4341% / 8.9871% / 23.6221%), each sourced *"RE-ANCHORED and held flat"*, and the delivered Table 10 prints them as driver rows. What is genuinely an output is the **group** EBITDA margin, which falls out of the segment mix (10.3% → 10.4%). That is a weaker claim than the one made, and L-005 is explicit that typing a margin is assuming the answer to the most important question. Compounding it, `engine/Fundamental_Driver_Ledger.md:1015` records that this issuer's own investor "results at a glance" sheets **publish cable tonnage, price per tonne and cost per tonne from FY2012 to 3Q2022** — real unit economics for 55.4% of revenue, which this model does not consume. `scripts/check_forecast_anchor.py`: **`SWDY carries no forecast_anchor record`**, so nothing outside the study tests the forecast margin against the latest actual either. |
| 29 | The historical segment build reconciles | **PASS — and it is a test that could fail** | §1.6: segment revenue (Note 5-3) *"sums to consolidated revenue **exactly** in every year shown — there is no elimination or apportionment"*, and segment profit (Note 16) reconciles to consolidated operating profit through an explicit corporate cost load stated on the same basis as the audited history (5.70% / 4.30% / 3.16%). `recalc.py` confirms in the workbook: `Segments total FY2025 revenue: workbook=281,049.0817 model=281,049.0817`, `Segments FY2025 share sums to 100%: workbook=1.0000`. The FY2026E build is separately cross-checked against the disclosed Q1-2026 print (370,030 against a grossed-up 356,323, **+4%**) and the caption says it is a check and not a calibration. |
| 30 | The enterprise-to-equity bridge stands on the latest disclosed sheet | **FAIL, ratcheted, and declared against itself** | `scripts/check_bridge.py`: **`BRIDGE FAIL -- SWDY: the bridge stands on the 2025-12-31 balance sheet while the latest disclosed is 2026-06-30`**. `bridge_record` names the reviewed 30-Jun-2026 statements as the latest disclosed, gives the study's reason (the +8,069 deterioration over the half is the same working-capital absorption FY2026E already carries, so deducting it again charges one outflow twice), and then says plainly: *"**THE RULE DOES NOT ADMIT THAT ARGUMENT** and the record does not pretend otherwise … this study stays on the bridge ratchet."* Minority deducted from **equity** on a `value_share` basis with book (5,119), profit-share (9,904) and proportional (7,279) all published. Cash: the record declares that net weights **and** a net-debt deduction charge the same cash twice, computes the gross-weighted alternative (26.17% against 28.84%), states that it runs against the value, and records it uncorrected for a later lever. Honest, and still a failed clause. |
| 31 | Asset base [R-ASSET-01] | **Not in scope, derived not asserted** | `scripts/check_asset_base.py` puts 11 studies in scope from `LENS_REGISTRY`'s seven asset-based classes; SWDY is not among them (ADNOCLS, AMOC, ARCC, BOROUGE, EGCH, FERTIGLOBE, MODON, PHDC, RIYADHCABLE, SCEM, TMGH). The class resolves, so this is a derived exclusion and not a skip. |
| 32 | **Sweep Register primary-source depth** | **HARD FAIL — the register does not exist** | `scripts/check_sweep_module.py`: **`SWDY no sweep script in the study directory`**. There is no `sweep.py`, no import of `engine/research_sweep.py`, and `"sweep"` and `"primary_access"` each occur **0 times** in `study_numbers.json`. So: no `record_primary_access()` — the company's own site is cited (`ir.elsewedyelectric.com`, four times in `compute.py`) but no attempt is logged either way, success or failure; **no fiscal-year FS-depth count** — three audited years are named in the bibliography against a floor of two and a target of four, but nothing counts them; **no COMPANY_IR source tagged distinctly**; **no study-year quarter coverage** — Q1-2026 is read and H1-2026 is read for six inputs, and neither is registered as a swept quarter; **no results-release sweep** — the study states the quarterly earnings releases *"were not reachable from this research environment"* and that is the single largest disclosure gap in the build (row 47), recorded in prose and in no register. Every primary-source-depth item in the standing list is unevidenced. |
| 33 | Walk-forward scope stated in the study [R-FCAL-01] | **FAIL in the record, met in substance** | `walkforward_scope_outstanding.json`: `"SWDY": "no scope decision stated"`. `"walkforward"` occurs once in `study_numbers.json`, in a note about an empty corrections log. **The run itself is real and large**: `engine/Fundamental_Driver_Ledger.md:1000` records *"FULL scope: seventeen sourceable fiscal years FY2009–FY2025 from the company's own investor-relations archive (233 documents retrieved), twelve origins FY2014–FY2025, horizons 1–5, **750 scoreable driver cells and 105 recorded unscoreable** — the largest fundamental sample this book holds"*, with skill against both naive benchmarks (`FREEZE +15.7%…+46.0%`; `TREND +0.0% / −3.3% / −13.1% / −6.4% / −5.2%`) and `adopted_corrections: []` committed empty rather than absent. §7 states it to the reader in the protocol's own terms. What is missing is the machine-readable scope record the gate reads. |
| 34 | Step 0.0 data-quality gate | **PASS** | `step0_result.json`: **3,735 clean rows**, `dq_log: ["dropped 1 interior stale/no-trade rows"]`, span **15.59 years**, density **239.6 sessions a year** against the EGX calendar. Run on both series by `own_stock_beta`: `index_dq: []`, `stock_dq: ["dropped 1 interior stale/no-trade rows"]`, weekly grid matched to the exchange's real week (`week_rule: W-THU`). |
| 35 | Step 0 calibration under the band record [R-CAL-02] | **FAIL — the study publishes its own backtest, not the published record** | The published record, read through a real `node` parse of `assets/data.js`: `SWDY: {mkt:"EG", **n:57, hits:54**, c50:0.5263, c80:0.8070, **c90:0.9474**, width:1.371, strength:"long", flag:null}`. §3 publishes instead the study-local `backtest_5y.json` `five_year` block: *"**19** independent non-overlapping quarterly windows with origins from 2021-10-05 to 2026-04-12 … **47%** inside the 50% band, **68%** inside the 80%, **84%** inside the 90%, over those 19 windows … a uniformity test returns **p = 0.28**"*. The count IS printed beside every percentage, which is the substance of the rule, and the width is disclosed and explicitly not gated (*"about 22% wider than a naive carry-anchored band, which is a real limitation … stated here rather than left out"*). But the numbers are not the record's: 84% against 94.7% on the 90% band, 47% against 52.6% on the 50%, width 1.22 against 1.371. No flag is earned and none is printed, which is right. |
| 36 | Verdict tokens and the external-reader scrub — standard 4 | **PASS** | `band_record.assert_no_verdict_tokens('EG')` passes. `python3 qc_checks.py`: **0 hits** in the study and **0 hits** in the bibliography against the banned-vocabulary list, plus 0 leaked placeholders in each. `scripts/check_delivered_vocabulary.py` lists SWDY among the 22 CLEAN. Direct search of both delivered documents for `PARITY`, `CRPS`, `PASS`, `FAIL`: **0**. `step0_result.json` carries `"verdict": "PARITY"` internally and it reaches no reader. |
| 37 | No calibration appendix | **PASS** | Exactly three appendices — A financial statements, B peer frame and risk register, C expert panel. Calibration evidence sits in §3 as plain-language sentences with the statistics inline. |
| 38 | Table discipline — standard 6 | **FAIL — a NEW red on the repo gate** | The study's own check reports clean: `qc_checks.py` → study **37 tables, over-wide 0, starved 0, bloated 0**; bibliography **9 tables, 0 / 0 / 0**. The gate that reads the shipped object does not: `scripts/check_column_widths.py` → **`[NEW] engine/swdy_study/SWDY_Bibliography_09-09-2026.docx::Date  declared 1.83cm, needs 1.94cm for its widest figure`**, and the script exits **FAIL**. This is L-014 arriving again in the same directory: a study-local check inspecting a slightly different object from the renderer's, reporting clean forever. `scripts/check_table_footing.py` is green (`SWDY tables examined: 42; unreconciled totals: 0`) and did not see row 11. |
| 39 | Sixteen-section Word structure and sixteen-sheet Excel structure | **PASS** | `scripts/check_document_structure.py` lists SWDY among the **22 MATCHING THE MODEL REPORT**. `wb.sheetnames == research_protocol.MODEL_STUDY['excel_sheets']` → **True**, in the model order, no renames. `scripts/check_workbook_structure.py` agrees. |
| 40 | **[R-GAP-01] the answer, not only the process** | **PASS, with the date named** | Central **43.5108** against a struck spot of **130.00** = **−66.5%**, and −68.1% against the latest known 136.20 of 2026-09-06 — far past the 10% trigger. `GAP_REVIEW_08-09-2026.md` exists, carries **`AUDITED CENTRAL: 43.5108`** and **`AUDITED GAP: −66.5%`**, both current, and covers **all eight headings** — LATEST FILINGS · BASE YEAR · MACRO COHERENCE · DISCOUNT RATE · TERMINAL · BALANCE SHEET · CLAIMS AGAINST THE RECORD · MULTIPLE CROSS-CHECK — plus a verdict. `scripts/check_valuation_gap.py` is clean and lists SWDY among the reviewed. Its own opening explains why it replaced the 07-09 review one day later (the beta lever moved the gap seven points, past the five-point staleness tolerance). **The gap in this row:** it is dated **08-09-2026** against an edition of **09-09-2026**. The central did not move on the 9th, so the review is not stale in substance — but the artefact does not carry the edition's own date. |
| 41 | [R-GAP-02] publish block | **Correctly HELD** | `scripts/check_publish_block.py`: **`HELD SWDY  the central is -68.1% below the price of 136.20 (2026-09-06), past the 10% publication limit — and no market dissent is filed`**. The gap review's own verdict says the same: *"This study remains HELD under [R-GAP-02] and nothing here reaches the live site."* |
| 42 | The site still carries 69.73 | **Stated exception, not a defect of this edition** | `assets/data.js` line 639: `fair: { bear: 19.95, base: 69.73, full: 138.73 }`, `fairAsof: "2026-08-05"`, files pointing at the 05-08 artefacts. `scripts/check_published_gap.py` → **`SWDY −48.8% review GAP_REVIEW_08-09-2026.md audits 43.5108; the site publishes 69.73`**, on `published_gap_outstanding.json` with the reason recorded: *"the study was rebuilt and the site was not, because the book is HELD. The review is not stale; the PAGE is."* The number moves when the referral is answered. Two things worth naming beside it: the ratchet entry still quotes `GAP_REVIEW_06-09-2026.md` and a central of 55.4822, both superseded; and the site's own three-month cone (refit 2026-09-06, median 144.55) is a completely different map from the one §3 delivers (median 109.91), so a reader who opens both sees two. |
| 43 | The 69.73 is gone from every artefact of this edition | **PASS** | Direct count: `"69.73"` occurs **0 times** in `SWDY_Valuation_Study_09-09-2026_public.docx`, **0 times** in `SWDY_Bibliography_09-09-2026.docx`, and **0 times** across all 1,527 populated cells of `SWDY_Valuation_Model_09092026_public.xlsx`. The genuinely delivered 05-08 document in `files/` carries it nine times (*"the four lenses centre at EGP 69.73 per share against a market price of 105.20"*). The retired blend is published in this edition at its **recomputed** value, 60.78, in a separately banded row labelled *"NOT AVERAGED — the retired blend, published unused"*. |
| 43a | …but §1.5 attributes 60.78 to the earlier edition | **FAIL** | §1.5, last sentence: *"An earlier edition did settle it that way and reported **EGP 60.78**, -53% against the price where this study holds -67%."* The earlier edition reported **69.73** against a price of **105.20**, i.e. −34%. 60.78 is what the retired weights produce on **today's** lens values; it is not what any reader ever received. |
| 44 | Edition date in one place | **PASS on the study, FAIL on the bibliography** | `edition.py` holds `EDITION = date(2026, 9, 9)` and derives all six filenames; nine files import it. Study masthead: **"Edition of 9 September 2026"** — agrees with the filename and with `SUPERSEDES = (2026-08-05,)`. Bibliography: no edition line in the masthead and *"the valuation study dated 5 August 2026"* in its subtitle (row 14). |
| 45 | Standard version stamp [R-STD-01] | **FAIL** | `research_protocol.STANDARD_VERSION` is **`2026.09.07`**. `"2026.09.07"`, `"STANDARD_VERSION"` and `"standard_version"` each occur **0 times** anywhere in `engine/swdy_study/`. `scripts/check_study_provenance.py` counts SWDY among the **18 unstamped** against 6 stamped. The live standard names the three gate calls, the driver record and the dated gap review; this edition meets one of the three cleanly, so the absent stamp is at least not a false claim. |
| 46 | Lens design [R-LENS-03], rebuild ledger [R-REBUILD-01], output records, artefact currency | **PASS, with two label gaps named** | `scripts/check_lens_design.py`: **`SWDY primary dcf, 2 cross-checks`** — the class primary IS the central and the document says so seven times. `scripts/check_rebuild_ledger.py`: **`SWDY 5 lever(s), 4 rule(s), cumulative −21.6%`**, decomposed `SIGCM-6 −17.4% · R-TERM-01 −5.0% · R-GAP-01 +1.4% · depth-bar standard 3 +0.0%`, every lever naming the rule it serves and the audit point declared after lever 5. `scripts/check_output_records.py` and `scripts/check_artefact_currency.py` both report no SWDY entry. **The two gaps:** (i) the summary table's central row calls the book lens *"a disclosed floor that is never weighted"*, while `lens_record.below_floor_reason` states in its own words that this figure *"is the book lens AT ITS JUSTIFIED PRICE-TO-BOOK of 1.3665x (EGP 49.2077) … **not a disclosed figure**. The floor that IS disclosed is equity attributable to the parent over shares in issue, EGP 31.2367 … rolled to the anchor = EGP 35.5129"*; (ii) Table 8's Role column reads *"not published for this class"* against the normalised lens, in a table that publishes it — as does the summary table, as does Figure 1. |
| 47 | No rating, no price target; standard 8 — the contested judgement both ways | **PASS** | The masthead, the READ FIRST panel, §4's closing line and both Disclosure sections all state it; every lens is a range with its base marked. **Standard 8 is met in all four required places.** The central contested judgement is the currency of discounting, and it is published as two numbers side by side and never averaged: in the **summary table** (*"Currency of discounting … 57.86 … −55%"*, in a separately banded ALTERNATIVE READINGS block); in the **body** (§1.8's six-row contested table, §4's comparison table, §7's fifth bullet); in the **workbook** (`recalc.py` → `Fundamental — currency alternative: workbook=57.8610 model=57.8610`); and in **an expert's range** — Expert 3's row is banded *"Range — the upper bound **is** the hard-currency discounting case: 42.23 – 57.86"*. §1.5 and §4 both refuse to split it: *"neither is hidden inside an average"*. |

---

## What the filings disclose that the model does not consume

Asked explicitly against the Sweep Register, because a self-audit that only re-checks the
work it did will keep missing the work it never did. The register cannot help with any of
these, because there is no register (row 32).

**1. The company's own investor sheets publish the unit economics of 55% of revenue, and
the model builds none of them.** `engine/Fundamental_Driver_Ledger.md:1015`, written for
this name two days ago: *"The company's own 'results at a glance' investor sheets publish
cable **tonnage, price per tonne and cost per tonne from FY2012 to 3Q2022** and then stop …
**the next name of this class should look for the investor sheet before assuming the
statements are the finest level.**"* The study's negative result is scoped to the audited
statements and is true there. But 233 documents were retrieved from that archive for the
walk-forward, the unit series is in them, and the delivered forecast still runs Cables on
a segment margin held flat at 11.4341%. That is the difference between a `derived` build
and a `unit` build on the largest segment.

**2. The reviewed H1-2026 statements are read for six notes and appear in neither
delivered document.** The register cites *"the reviewed condensed interim consolidated
financial statements for the six months ended 30 June 2026, approved for issuance by the
board on 11 August 2026 (note 2-1)"* for payables (10-3), receivables (9-B), cash (9-E),
credit interest (14-B), cost of sales by nature (15-A) and the employees' share (note 38).
`bridge_record.latest_disclosed_date` is `2026-06-30`. The bibliography's primary-documents
table stops at the Q1-2026 interim of 13 May 2026, and its negative results are scoped to
"the four primary filings". A filing the model consumes and the bibliography does not name
is the same defect in the other direction.

**3. The half-year balance sheet is read and the bridge stands on the December one.** The
record states the half added **EGP 8,069mn** of net debt and gives a defensible reason for
not deducting it twice — and then says the rule does not admit that reason. Restriking onto
the June sheet moves the explicit window's start with it, which is a rebuild. It is named
here because it is a disclosure the model has open and does not use in the one place it
determines the answer.

**4. The quarterly earnings releases, which carry the order book, are recorded as
unreachable and never re-attempted.** §7 and Appendix B.3 both state the releases
*"historically disclosed order-book and volume data (the FY2024 release, for example,
disclosed 167,665 tons of cable sold); those releases were not reachable from this research
environment"*. Meanwhile the walk-forward pulled **233 documents from the same investor
archive**. Whether the releases are genuinely unreachable or were simply not sought in this
pass is not established anywhere, because nothing logs the attempt.

**5. The employees' statutory share is registered, computed, and printed nowhere a reader
can foot it.** Three years of it are in the register from notes 38 and 39 with full
provenance. It removes 12.19% of the equity value. It is absent from the bridge (row 11)
and from every sensitivity (row 12). The disclosure is consumed by the answer and not by
the document.

## The fifteen hard fails, in the order they would have to be cleared

1. **Figure 1 is from a superseded strike** and shows the primary lens at base 53 against a
   published 43.51, with a fifth bar labelled "Weighted central". (row 7)
2. **The §1.1 bridge does not foot** — 116,164 − 20,560 + 6,758 − 9,904 = 92,458 against a
   printed 81,185, the missing EGP **11,273mn** being the employees' statutory share, EGP
   5.27 a share. (row 11)
3. **Every sensitivity in §1.9 omits that same charge** and reproduces the base case at
   **49.71** against a published central of **43.51** — 14.2% high, across Table 17, all
   seven rows of Table 18, Figure 3 and Appendix B.2's risk register. (row 12)
4. **§2 and §3 are anchored on the 5-Aug spot of 105.20** in a study struck at 130.00 on
   3-Sep; "Probability above spot" prints 59% where P(>130.00) on the study's own
   `paths_3M.npy` is **18.0%**. (row 13)
5. **The beta is credited to the composite the study withdrew**, in §1.8 and again in the
   bibliography's primary-documents table, printing the EGX30 statistics; the registered
   index file appears in neither document. (row 2a)
6. **Figure 5's cone starts at 105.20** under a "spot 130.00" line, labelled "fundamental
   central ≈ 53" four lines above a caption saying 43.51. (row 9)
7. **Figure 3's title promises bold cells near spot 130** over a grid whose largest cell is
   81 and none of which is bold, on a superseded axis. (row 8)
8. **Figure 8 is superseded** and its caption places the panel centre at 65.48 over a band
   drawn at 75. (row 10)
9. **The bibliography is the previous edition's document** under this edition's filename,
   by its own subtitle, missing the H1-2026 filing the model reads. (row 14)
10. **Twelve of 193 registered inputs never reach the delivered register** — the three
    segment margins, the tax rate, the cost of debt, copper, FX, the corporate load and the
    terminal's own asset life — under a heading asserting that nothing is computed from a
    figure absent from it. (row 15)
11. **No Sweep Register exists**, so primary access, filing depth, quarter coverage,
    results releases and the COMPANY_IR tag are all unevidenced. (row 32)
12. **No `assert_ground_up()` driver record**, and no study code calls any of the three
    gates — the repo gate passes SWDY on a phrase in a docstring. (rows 1, 3)
13. **Expert 2's base of 65.48 sits above its own published range of 34.12–59.75**, and is
    called "the harshest of the three readings" beside an Expert 3 at 45.29. (rows 18, 19)
14. **`check_column_widths.py` goes RED on a new SWDY column**, and no standard-version
    stamp exists anywhere in the directory. (rows 38, 45)
15. **Retired-blend language survives in six places**, starting with the headline sentence
    of the valuation; §1.5 attributes EGP 60.78 to an edition that published 69.73. (rows
    17, 43a)

Beneath those: `recalc.py` and `driver_test.py` open the superseded workbook by name, and
the engine copy of that superseded workbook has been overwritten with this edition's
numbers; Table 25 labels segment profit as gross profit; the summary table calls the book
lens a disclosed floor when the study's own record says it is not; Appendix A.1 leaves
profit before tax, tax and minorities blank for every forecast year; the macro record
carries no market field so the coherence test cannot run; the bridge stands a filing behind
and the same cash is charged twice, both declared and neither corrected.

## What is genuinely strong here, and should not be lost in a rebuild

The beta is on the sanctioned route with the withdrawn number kept beside it and the
correction measured at +21.4% — and it moves the answer **further below** a price the study
already sits far below, which is the only direction that proves the discipline is not
fitting to the market. The workbook reproduces the model on all 594 formula cells and
reproduces **cell for cell against a sandbox rebuild across 1,527 populated cells**. The
free-cash-flow waterfall foots by hand on every year and reaches present value inline. The
terminal stands on a life derived from note 17 and reproduces to 1e-6. The segment build
ties to the audited revenue exactly in all three years and to operating profit through an
exactly-reconciling load, and the forecast year is cross-checked against a disclosed
quarter in a test that could have failed. The contested currency judgement is published
both ways in all four required places and averaged in none. The bridge record, the
cost-of-capital record and the gap review each state, in their own words, the clause they
fail and why it was not fixed in this pass — which is worth more than a green tick.

The defects above are almost entirely in the artefacts that were not regenerated with the
last strike, and in the words around numbers that are themselves correct. Four stale
figures, a missing bridge line and a month-old price map got past `recalc.py`,
`driver_test.py`, `prose_check.py`, `footing_check.py` and `qc_checks.py` — all five ran,
all five were clean — because each of them checks how a number was built, and every one of
these numbers was built correctly somewhere else. Reading the rendered pages is what found
them.
