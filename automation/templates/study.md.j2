# {{ name }} ({{ exchange }}:{{ ticker }}) — Independent Valuation Study

**Educational Analysis** · Anchor {{ ccy }} {{ anchor }} ({{ close_date }} close) · {{ shares_bn }}bn shares · refreshed {{ today }}

> **READ FIRST.** This is a valuation exercise and personal analytical opinion, published free for educational purposes. It is NOT investment advice, NOT a recommendation, NOT a solicitation. The preparer is not FRA-licensed, manages no money, accepts no fees or clients. All values are model outputs shown as ranges. Consult a licensed advisor before any decision.

## Headline
Risk-adjusted fair value **{{ ccy }} {{ base_value }}** ({{ base_pct }} vs market {{ anchor }}); full pipeline execution **{{ ccy }} {{ full_exec }}**. T+20 median {{ mc.percentiles[20][50] }}, T+60 median {{ mc.percentiles[60][50] }} — distribution skews right on launch + sovereign-inflow events. This study estimates values and probabilities; it does not recommend actions.

## 1. Fundamental — sum-of-the-parts
| Project | Risk-adj {{ ccy }}/sh |
| --- | --- |
{% for p in sotp -%}
| {{ p.label }} | {{ "%.2f"|format(p.value) }} |
{% endfor -%}
| **Sum of parts (gross)** | **{{ "%.2f"|format(sotp_gross) }}** |
| Less: net debt | ({{ "%.2f"|format(net_debt) }}) |
| **Base-case equity value** | **{{ base_value }}** |

Scenarios: Bear {{ bear }} · Base {{ base_value }} ({{ base_pct }}) · Full execution {{ full_exec }}.

## 3. Monte Carlo — {{ paths }} paths, seed {{ seed }}, 10 factors
| Horizon | 5% | 25% | Median | 75% | 95% |
| --- | --- | --- | --- | --- | --- |
{% for h in horizons -%}
| T+{{ h }} | {{ mc.percentiles[h][5] }} | {{ mc.percentiles[h][25] }} | **{{ mc.percentiles[h][50] }}** | {{ mc.percentiles[h][75] }} | {{ mc.percentiles[h][95] }} |
{% endfor %}
### Level-touch probabilities (T+20 / T+60)
| Level | T+20 | T+60 |
| --- | --- | --- |
{% for lvl in touch_levels -%}
| {{ "%.2f"|format(lvl) }} | {{ (mc.touch[20][lvl]*100)|int }}% | {{ (mc.touch[60][lvl]*100)|int }}% |
{% endfor %}

## 7. Caveats
Bottom-up valuation is assumption-driven; Monte Carlo factors are independent (no cross-correlation yet); every drift/probability/impact is judgment, not estimation.

**RSI reminder** — Hold while price and RSI rise together; worry when price rises but RSI stalls/falls (bearish divergence). Buy-interest when price falls but RSI rises (seller exhaustion); stay away when both fall together.

*Generated {{ today }} from data/{{ ticker }}.yaml. Edit the config, not this output.*
